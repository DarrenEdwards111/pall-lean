# WHAT IS PROVED — the converged picture

A single honest ledger of the observer‑boundary / inverter‑frontier programme. All "proved" entries are
Lean 4 / Mathlib, `sorry`‑free, axioms `[propext, Classical.choice, Quot.sound]` (some pure‑`Nat`/`decide`
lemmas use a subset; `native_decide` uses are flagged and absent from the load‑bearing results). Branch
`razborov-recoverRho-wip`.

**This is not a proof of `P ≠ NP`.** It is a formalized restricted lower‑bound theory in which *every reducible
piece has been discharged* and the separation is reduced to **one named statement**, proved equivalent in
strength to `P ≠ NP` and shown — as a theorem — to be unreachable by the methods at hand.

---

## The one‑paragraph summary

A single explicit predicate, **Majority**, has been formalized as **optimal** (`AI(Maj_n)=⌈n/2⌉`) and shown to
**unconditionally resist all four restricted inverter classes** the programme can express — low‑degree algebraic,
`AC⁰[p]`, bounded‑crossing, and bounded‑locality. The last of these, `AC⁰[p]`, was closed by formalizing the
full `MOD_q ≤_{AC⁰[p]} Majority` reduction circuit, making `Majority ∉ AC⁰[p]` an unconditional theorem. The
earlier *complementarity obstruction* — that the `AC⁰[p]`‑resisting witness (parity/`MOD_q`) provably *fails*
low‑degree resistance — is thereby **resolved**: Majority does both, with no remaining hypothesis.

From there the wall was pushed across three axes in turn — **space**, then **time**, then **explicitness** — each
closed to its exact ceiling (Part 4½). The space axis is *settled*: the separating boundary is **exactly `r`**. On
the decision axis a Williams‑style cash‑out lands an **unconditional** lower bound — *some* function escapes every
cheap decider — but only nonconstructively. The remaining residue is isolated as a single named conjunct
(`InNP`) and shown *unbridgeable by counting*: the counting property is large and useful, so under crypto the
**natural‑proofs barrier** forbids it from being constructive. The strongest *explicit*, *non‑natural* decision
bound the corpus reaches is the **Nečiporuk frontier** (`n²/log n`) — real, explicit, barrier‑evading, and
provably capped there. The single gap that remains is resistance against **all** polynomial‑time inverters /
**explicit** decision hardness above `n²/log n`, which appears in five equivalent forms, all `= P ≠ NP`, and which
a proved gap theorem shows the action/space machinery cannot by itself reach.

**The `ACC⁰` arc, and a finished `AC⁰` package.** A second, self‑contained line built the Yao–Beigel–Tarui /
Razborov–Smolensky polynomial method *from scratch* in Lean and ran it to its exact ceiling. First it proved the
obstruction is real: an exact unbounded‑fan‑in `OR`/`AND` over `F₂` has degree **= fan‑in** (`…ACC0ExactDegreeNoGo`),
so no single exact low‑degree polynomial works — the only route is *approximate*‑then‑*decode*. That route was then
built end to end: the balancedness of random `F₂` linear forms, degree‑`t` boosting (correct on a `1−2^{-t}`
fraction), a self‑contained **finite Chernoff** + union‑bound sampling to a quasipolynomial majority‑correct family,
and the **basis bridge** showing the boosted parity predictor *is* a low‑monomial‑`AND`‑degree object (its wide sets
live in the degree‑1 *linear* part). On top, an unbounded‑fan‑in circuit datatype and the **depth induction** were
formalized, yielding the **complete, quantitative `AC⁰` theorem**: *every `MOD`‑free circuit has an `F₂` approximant
of degree `≤ t^depth` and error `≤ size·2^{-t}`* (`…ACC0QuantDegree`, `…ACC0QuantError`) — for `t = O(log size)` and
constant depth, polylog degree and `<1/2` error, the textbook Razborov–Smolensky bound. This is **classical**
(`AC⁰`/`AC⁰[2]`‑level: `PARITY ∉ AC⁰`, `MOD ∉ AC⁰[p]`), *not* new mathematics and *not* progress toward `P ≠ NP`; its
value is a clean sorry‑free reconstruction. Crucially the construction *stops exactly where the method stops*: it
extends to `MOD` only for **prime‑power** moduli (`MOD₂` done, `…ACC0Mod2Exact`), and **composite `MOD_m` has no
low‑degree representation over any single field** — the genuine `ACC⁰` barrier, the same wall the rest of this corpus
documents, and the reason `NEXP ⊄ ACC⁰` required Williams' algorithmic (separation‑strength) method rather than the
polynomial method. So the `AC⁰` package is *finished*; the `ACC⁰` frontier is open mathematics, not Lean engineering.

### Why this does not prove `ACC⁰` — the composite‑`MOD` barrier (the honest ceiling of the polynomial method)

The polynomial method *extends cleanly to a prime modulus* and *provably stops at a composite one*. Both halves are
now formalized, so the boundary is a theorem, not a hope:

* **Prime side — `MOD_p` is exactly low‑degree over `F_p` (`…ACC0ModPExact`, proved, clean axioms).** By Fermat's
  little theorem, over `F_p` the count‑`≡0` indicator is `MOD_p(x) = 1 − (∑_{i∈S} x_i)^{p−1}` *exactly*
  (`modp_exact_eval`), of total degree `≤ p−1` (`modpPoly_totalDegree_le`), and so lies in the degree‑`≤(p−1)`
  monomial‑`AND` span over `F_p` (`modp_mem_monoAND_span`). This is the prime‑`p` generalisation of the `MOD₂`/parity
  case (`…ACC0Mod2Exact`); together with the OR/AND boosting it gives the full Razborov–Smolensky picture for
  `AC⁰[p^k]` (prime powers reduce to `F_{p^k}` by the same argument).
* **Composite side — there is no such field (the barrier).** For `m = p·q` with two distinct prime factors, the
  Fermat exponent is pinned to the order of the *chosen* field: `a^{p−1} = 1` for `a ≠ 0` in `F_p`, but `a^{m−1}` is
  **not** a `{0,1}`‑indicator there (the order is `p−1`, not `m−1`), and over `F_q` it fails symmetrically. So no
  *single* field sees `MOD_m` as a low‑degree `{0,1}` function. Formalized as the mixed‑modulus split: `MOD₆`
  decomposes by CRT into `MOD₂ ∧ MOD₃` (`mod6_eq_mod2_and_mod3`, `…Layer3MixedModulus`), each component low‑degree
  over its **own** prime field with **no common field** — and the `MOD_q`‑over‑`F_p` reachable residue set is provably
  **not** an `F`‑subspace for `q ≥ 3` (`modq_residue_image_not_subspace`, `…ACC0ModQFeasibility`), so the `2^{rank}`
  linear‑algebra shortcut that closes the parity case is genuinely `MOD₂`‑specific.

This is *exactly* why `NEXP ⊄ ACC⁰` (Williams) needed a fundamentally different, **algorithmic** method
(faster‑than‑brute‑force `ACC⁰`‑SAT ⇒ the separation, opened but not closed here — the `fastSat` socket in
`…WilliamsCashout` / `…ObserverWilliams`) rather than the polynomial method. The composite‑`MOD` low‑degree representation is not a missing
Lean lemma — it is a genuine mathematical obstruction, the same wall the rest of this corpus documents from the
observer/effective‑rank side. **The polynomial‑method package is complete up to `AC⁰[p^k]` and provably no further; it
is not `NEXP ⊄ ACC⁰` and not `P ≠ NP`.**

---

## 1. Proved unconditionally

**The headline — one predicate, every restricted class.**
- **Optimal algebraic immunity.** `AI(Maj_{2t-1}) = ⌈n/2⌉` (`majority_AI_optimal`): the two‑sided lower bound
  (`majority_algebraic_immunity_two_sided`, via the from‑scratch `F₂` Möbius inversion `anf_involutive` +
  degree‑preserving complement symmetry `degreeLt_compl`) and the matching upper bound `AI(f) ≤ ⌈n/2⌉` for
  *every* `f` (`algebraic_immunity_le_ceil`). No `decide`; scales with arity.
- **`Majority ∉ AC⁰[p]` — UNCONDITIONAL** (`…ModqReducesMajority`): the full `MOD_q ≤_{AC⁰[p]} Majority`
  reduction circuit (`thresholdCirc` via `padInputs`; `modqCirc = ⋁_{k≡0}[#ones=k]`; eval‑correctness, `AC⁰[p]`,
  constant depth, polynomial size, and an `IsPolyBounded` closure built from scratch) discharges `AC0pReduction`
  ⇒ `majority_not_in_AC0p`.
- **The simultaneous‑resistance wall, realized on Majority.** `majority_simultaneous_resistance`: `majorityF2`
  unconditionally satisfies **both** binding resistances (low‑degree *and* `AC⁰[p]`), so
  `SimultaneousAlgAC0pResistance` holds with **no remaining hypothesis** — the binding pair of
  `…UnifiedInverterFrontier`, formerly only conditional, now a single‑predicate theorem.

**The engines and invariants.**
- **Dynamical debt accounting.** `debtCount`; conservation (`debt_conservation_varying`, `merge_pay`,
  `correct_needs_action`); mechanism `foolingSet_forces_debt` (`debt ≥ K − 2^B`). Gauge invariance
  `debtCount_relabel_invariant` (no frame change lowers debt).
- **Observer‑boundary invariant.** Fooling‑set principle: `K` non‑mergeable sectors ⇒ boundary `≥ log₂ K`.
- **The `F₂` Möbius / ANF inversion** (Mathlib lacks it): `anf_involutive`, from the interval count
  `card_filter_subset_between`.
- **AI ⇒ separator resistance.** `majority_defeats_low_degree_separator`: no nonzero degree‑`<t` function
  annihilates `Maj`/`¬Maj`.
- **The geometric core of the gap.** `dimension_gap_forces_debt` / `positive_gap_forces_debt`: a low‑`d_obs`
  observer of a high‑`d_res` residual carries debt `≥ 2^{d_res} − 2^{d_obs}`.
- **The cash‑out chains (pure logic, no axioms).** `williams_cashout`, `ravel_wedge`,
  `observer_centric_williams`, `goldreich_observer_williams`, `majority_observer_williams`.
- **The speedup ingredient is PROVED, not assumed** (`…NFrameSpeedupBridge`): a low‑action / low‑boundary
  inverter's reachable‑set DP visits only `∑ 2^{Bτ} = action` configurations, so `lowAction_beats_bruteforce`
  (`action < 2^n` ⇒ DP cost `< 2^n`) and `lowSpace_beats_bruteforce` (via `subcritical_of_lowspace`) make
  `separatorSpeedup` a theorem — the N‑frame side genuinely supplies the Williams cash‑out's speedup half.
  `nframe_speedup_then_no_lowaction` closes the chain, leaving `noCollapse` (the hardness) as the only assumed
  input.
- **The raveling ingredient is PROVED for a concrete `K`** (`…RavelingConcrete`): for `K = {effective dimension
  < r}`, `raveling_lowAction` shows a low‑action observer lands in `K` — the *dynamical* action bounds the
  *structural* dimension (a single layer's capacity `2^{Bτ}` is one summand of `action`, so the decision
  alphabet `≤ 2^{B(T-1)} ≤ action < 2^r`).  With `boundedDim_noSeparator` (`residual_view_card_forces_debt`),
  `lowAction_no_separator`: **no low‑action observer separates the dimension‑`r` residual.**  So for restricted
  `K` *both* observer‑Williams premises (`raveling`, `separatorSpeedup`) are now theorems — only the hardness
  (`noCollapse`) is assumed.  The catch: `K` here is not all of `P` (a poly‑*space* decider keeps a
  full‑dimension view — the brute‑force escape), and extending `raveling` to every `P` observer is the
  `P ≠ NP`‑strength step.
- **`raveling` extended to bounded‑space `P` observers** (`…RavelingBoundedSpace`): a *space* bound genuinely
  bounds the boundary (a space‑`s` machine has `≤ 2^s` configs, so its boundary view takes `≤ 2^s` values).
  `boundedSpace_raveling` (`s < r ⇒` in `K`) and `boundedSpace_no_separator` (a `SPACE(s)`, `s<r`, decider
  cannot separate the dimension‑`r` residual); `boundedSpaceTime_subcritical` is the dynamical form
  (`Tb·2^s < 2^r ⇒ action < 2^r`, via `subcritical_of_lowspace`).  This captures `SPACE(s)` for `s < r ≈ Ω(n)`
  (sub‑residual‑dimension space) — **not** all of `P`: a poly‑*space* decider (`s = n^k ≫ r`) keeps a
  full‑dimension view and escapes `K`.  That last gap is exactly `P ≠ NP`.
- **The separating boundary is TIGHT — exactly `r`** (`…TightSeparatingSpace`): `residual_separates` (the
  boundary‑`r` residual view is a zero‑debt separator — the upper bound) matched with `below_r_fails` (no
  boundary‑`<r` view separates — the lower bound) gives `separating_boundary_tight`: separating boundary `= r`
  exactly.  The hard family is *space‑`r`‑decidable* (the full‑boundary decider), so space is pinned and the
  obstruction to a fast algorithm is **not space** — it is **time**.  The debt/space machinery is exact on the
  space axis and provably cannot reach the time axis (`distinguishability_debt_not_time_lower_bound`) — which is
  where `P ≠ NP` lives.

## 2. Proved for restricted classes (the four resisted inverter classes + the corpus)

The four restricted inverter classes — each `InversionHardness` discharged unconditionally:
- **Low‑degree algebraic** — `no_low_degree_algebraic_inverter` (from `AI(Maj)=t`): the linearization / Gröbner
  attack provably fails.
- **`AC⁰[p]`** — `no_AC0p_inverter_modq` / `no_AC0p_inverter_parity` (Razborov–Smolensky chain: `Layer3`
  approximation + `Layer4` no‑approximation core `mod_q_indicators_false` + `Layer7` family bounds), lifted to
  Majority by the reduction above.
- **Bounded‑crossing** — `boundedCrossing_not_correct_inverter` (`q^w<2^r` ⇒ not a separator), the
  crossing‑sequence bridge `crossingSequence_no_separator`.
- **Bounded‑locality (junta)** — `boundedLocality_not_correct_inverter` (`2^{|W|}<2^r` ⇒ not a separator), via
  `bounded_support_forces_debt`.

Supporting restricted lower bounds:
- **Proof‑space.** `tseitin_totalSpace_lower_bound` (expander‑Tseitin total space `≥ c·t`), via the width kernel.
- **Boundary–time tradeoffs.** `bounded_boundary_tradeoff`, average/burst variants, explicit `2^n` witness,
  switch‑cost bound.
- **The decomposition ladder** (each = "effective boundary `< r` ⇒ debt"): read‑set, `F₂`‑linear, locality,
  holonomy, many‑loop amplification.
- **Calibrations** (known bounds through the invariant): AC⁰[p] = Razborov–Smolensky, Nečiporuk `n²/log n`,
  deterministic communication rectangles.

## 3. The Goldreich / local‑PRG terminus

`…GoldreichMajorityPRG` assembles `goldreichMaj` — the Goldreich local function with the optimal‑AI Majority
predicate over an expander hypergraph — and proves its structural properties: `goldreich_eval_local` (locality),
`majPred_const_true/false` (non‑degeneracy), `goldreichMaj_no_lowAction_inverter` (the conditional separation via
observer‑Williams). The family provably resists every restricted class of §2. Its **full** security
`GoldreichMajHard` (no poly‑time inverter) is the local‑PRG / one‑way‑function assumption — `P ≠ NP`‑strength
(OWF ⇒ `P ≠ NP`) — and is *the wall*: no construction discharges it. This is the honest terminus of the
constructive route: the right primitive, provably hard against every formalized restricted attack, single
remaining hypothesis = exactly `P ≠ NP`.

**The convergence, made one object (`…GoldreichHolonomyTerminus`).** The programme's threads are gathered onto
this single candidate: `GoldreichMajorityConvergence` bundles the *discharged* arrows (`raveling`,
`separatorSpeedup`) with the **lone open field** `goldreichMajorityHard`, and `GoldreichMajorityConvergence.terminus`
proves — *depending on no axioms at all* — that no low‑action observer inverts the family, conditional only on that
one field. Every other thread is referenced as discharged: optimal AI (`majority_AI_optimal` ⇒
`majority_defeats_low_degree_separator`), `AC⁰[p]` resistance (`majority_not_in_AC0p`,
`majority_simultaneous_resistance`), the four restricted classes, and the expander hard side
(`goldreich_expander_holonomy_full`: a `DisjointCycles` gadget from the hypergraph has full holonomy rank `2^m`).
*Honest calibration recorded there:* holonomy is an `F₂`/parity (Tseitin) invariant, so it attaches to the
candidate's **expander structure**, not to the non‑linear Majority predicate directly — stated, not forged. So the
constructive route is reduced to one named statement on an explicit, maximally‑calibrated candidate, with
everything else unconditionally discharged and `goldreichMajorityHard = P ≠ NP`‑strength.

## 4. Retired routes (proved false or proved dead — not merely abandoned)

- **`P_ne_NP_unconditional` (old).** Parked `P`‑side content in unproved sockets; retired for honest conditionals.
- **The `B < r` / brute‑force escape.** `hypercube_brute_force_escape`: a full‑boundary view resolves the `2^n`
  geometry with **zero debt** — the fooling‑debt mechanism is *empty* above boundary `≈ n`.
- **Time ⇒ boundary.** `action_unbounded_by_time`: poly *time* yields **no** boundary/action bound. "P‑time ⇒
  cheap observer" is *false*, not open (holds only given a space bound: `subcritical_of_lowspace`).
- **Proof‑hard ≠ decision‑hard.** `tseitin_unsat_of_odd_charge`: expander‑Tseitin is proof‑hard but
  **decision‑easy** (one parity bit).
- **The complementarity obstruction — now RESOLVED.** `parity_not_resists_lowDegree`: the `AC⁰[p]`‑resisting
  parity is affine (`AI ≤ 1`) and provably fails low‑degree resistance — so the *natural* witnesses are
  incompatible. This obstruction is dissolved by the reduction: **Majority resists both unconditionally**
  (`majority_simultaneous_resistance`). What once looked like a barrier to one‑predicate simultaneity is a
  theorem about the wrong witnesses; the right witness (Majority) clears all four.
- **The AND gadget.** `AI = 1` — filtered out; superseded by TSA (`AI=2`) then Majority (`AI=⌈n/2⌉`).
- **Diagonal SPDP / `χ_φ` bound.** Disproved (`rk ≪ #SAT`); barriered short of `VP` vs `VNP` on the permanent.
- **The HM / metacomplexity socket.** Proved *vacuous* by its own iff — repackaging does not reduce strength.

## 4½. The wall's journey across axes — to the explicit frontier

After the restricted‑inverter classes were closed, the wall was driven across three axes in sequence. Each axis
is now closed to its exact, provable ceiling; the residue that survives every axis is the single irreducible
`P ≠ NP` step. The progression is the substance of the explicit‑frontier picture.

**(a) Space — settled, boundary exactly `r` (`…TightSeparatingSpace`, `…RavelingBoundedSpace`).** `raveling` and
`separatorSpeedup` are theorems for restricted `K`; `boundedSpace_raveling` extends `raveling` to `SPACE(s)` for
`s < r ≈ Ω(n)`; and `separating_boundary_tight` pins the separating boundary at **exactly `r`** (`residual_separates`
upper bound + `below_r_fails` lower bound). The hard family is space‑`r`‑decidable, so the obstruction to a fast
algorithm is provably **not space** — it is time. (Summarized in §1, "engines and invariants".)

**(b) Time — the wall relocated, and shown unreachable by space machinery (`…TimeAxisWall`, `…BoundedWidthBPTime`).**
`space_axis_settled` (separator at `r`, none below); `ResidualSeparatorRequiresSuperpolyTime` (the named missing
bridge: the existing boundary‑`r` separator's decision time is super‑poly); `timeAxis_wall` (bridge + threshold ⇒
family ∉ `P`); `space_machinery_cannot_supply_bridge` (the gap — an action/debt bound of *any* size is hit by a
poly‑time single‑step trajectory, so the space‑exact machinery yields *no* time bound). The first restricted *time*
model is bounded‑width branching programs: `bp_width_no_separator` shows a width‑`W` BP with `W < 2^r` carries debt
`≥ 2^r − W > 0` **for every length `L`** — time cannot compensate for bounded width in *realizing* the separator
(honest caveat: realization/classifier bound, not a `1`‑bit decision bound; that gap *is* the time‑axis bridge).

**(c) Decision — an unconditional lower bound, by counting (`…RestrictedCashout`).** A Williams‑style cash‑out that
bites on the `1`‑bit *decision* axis with **no conjecture**: `cheap ⇒ enumerable` (a bounded‑resource decider has a
short description — the proved speedup `…NFrameSpeedupBridge` quantifies it) and contradiction by
counting/diagonalization. `card_boolFun` (`|BoolFun n| = 2^{2^n}`); `cheap_class_misses_function` (a class
`Fin N → BoolFun n` with `N < 2^{2^n}` misses some function); `restricted_cashout` (a cheap enumerable class is
*not surjective* onto all Boolean functions). A genuine, unconditional **decision** lower bound — *some* function
escapes every cheap decider — but the classical counting limitation bites: it is the **existence** (Shannon) form,
*some* hard function, not an *explicit* family.

**(d) Explicitness — the residue isolated as `InNP` (`…ExplicitnessWall`).** The wall moves from decision to
*explicit* decision. One shared target object: `ExplicitFamily`, `HardFor`, the **opaque** `InNP` parameter,
`ExplicitNPHard := ∃ F, InNP F ∧ HardFor F`. `counting_hard_at_each_length` (per‑length hardness, from the
cash‑out); `hard_family_exists` (choice assembles a hard *family* — with **no** `InNP` guarantee);
`explicitNPHard_imp_hardFamily` (the explicit‑NP target is *strictly stronger* — it implies the `InNP`‑free family
counting already gives). So the **entire** residue between what counting proves and what `P ≠ NP` needs is the
single `InNP` conjunct. `InNP` is left an opaque parameter precisely so no "explicitness by counting" move can
sneak in.

**(e) The natural‑proofs barrier — that residue can't be reached by counting (`…NaturalProofsBarrier`).** Blocks
the "explicitness by counting" shortcut directly. `Hard cheap f`, `LargeProperty` (more than half of functions),
`UsefulAgainst`. Proved: `nonHard_card_le` (non‑hard functions `≤ N`), `counting_property_is_large` (for
`2N < 2^{2^n}`, `Hard cheap` is large), `hard_property_useful` (it is useful). `RazborovRudichBarrier` is a *named
hypothesis* (under crypto, no large+constructive property is useful); `counting_property_not_constructive` then
proves the counting property — large and useful — **cannot be constructive** under crypto+RR. So "most functions
are hard" provably cannot be upgraded into an efficient property isolating `NP`; crossing to explicit hardness
needs a **non‑natural** argument. (RR and crypto are parameters, not claims.)

**(f) The explicit, non‑natural frontier — Nečiporuk, capped at `n²/log n` (`…ExplicitNeciporukHardness`).** The
genuine successor to counting and width: an explicit family with a real *decision* lower bound that *evades* the
barrier of (e). Reusing the crossing‑capacity / Nečiporuk machinery, `StorageAccess m` (explicit indirect
addressing) has `2^m` distinct crossing subfunctions, so `storageAccess_decomposable_lb` (every `CrossingModel`
needs `≥ 2^m` states), `storageAccess_escapes_cheap_decomposable` (no capacity‑`<2^m` decomposable decider computes
it), `explicit_family_beats_decomposable` (an explicit family escapes every bounded capacity). This is genuinely
**explicit** (a named function), a **decision** bound (computing `StorageAccess`, not separating a residual),
against a class **richer than width** (decomposable/communication, by distinct subfunctions), and **non‑natural**
(the subfunction count is not a large/constructive property — which is *why* it is provable). Honest ceiling: per
block `2^m`, summed over `Θ(n/log n)` blocks it gives formula size `Θ(n²/log n)` (`…NeciporukHardFunctionAsymptotic`)
— the decades‑old explicit frontier. The method provably tops out here (`crossing_capacity` bounds states by
subfunction count `≤ 2^{block}`). It does **not** reach `P/poly` or `P ≠ NP`.

**(g) Above the Nečiporuk ceiling — the shrinkage / Andreev mini‑arc.**  Can an *explicit* formula‑size bound beat
`n²/log n`?  Recorded honestly across three files:
* **Ceiling, proved (`…NeciporukCeiling`).**  *Why* subfunction counting caps: per block of `b` variables,
  `log₂ #subfunctions ≤ min(2^b, n-b)` (`log_subfunctions_le_min` — a subfunction is a `b`‑bit function *and*
  there are only `2^{n-b}` other‑variable settings).  The two caps cross at `b ≈ log n`, forcing the method to
  `n²/log n`.  So super‑`n²/log n` is **impossible by subfunction counting** — a different method is required.
* **Non‑counting structural base, proved (`…RelevanceLeafBound`).**  The replacement style: `leaves F ≥
  #{variables the function is *sensitive* to}` (`leaves_ge_relevant_card`) — a leaf per relevant variable, *no
  counting of functions*.  `gAnd_needs_n_leaves`: the `n`‑bit `AND`, sensitive to every variable, needs `≥ n`
  leaves.  Modest (linear) alone, but the genuine non‑counting core.
* **Shrinkage amplifier, cited (`…AndreevShrinkageRoute`, `…RestrictionSensitivity`).**  Håstad's shrinkage
  (`Γ=2`, the deep random‑restriction theorem — *named, cited, not reproved, not open*) multiplies the base
  bound: `andreev_leaf_lower_bound` (shrinkage `leaves_restricted·D ≤ leaves` + surviving complexity `H ≤
  leaves_restricted` ⇒ `H·D ≤ leaves`) and `shrinkage_above_neciporuk` (at balanced Andreev params `k³·2^{2k} <
  2^{3k}`, i.e. `n^{3-o(1)} > n²/log n`).  `RestrictionSensitivity` grounds the surviving complexity `H` in the
  *relevance* base: a restriction of a formula doesn't increase leaves (`restrict_leaves_le`) and the restricted
  formula still needs `≥` its surviving‑relevance leaves, so `shrinkage_plus_surviving_relevance` gives
  `(surviving relevance)·D ≤ leaves` — the relevance base and the shrinkage schematic, tightly connected.

So the mini‑arc is closed: subfunction counting *provably* tops out at `n²/log n`; the non‑counting (relevance +
shrinkage) route *provably* exceeds it to `n^{3-o(1)}` — but that is still **polynomial**, vastly short of the
super‑polynomial explicit bound `P ≠ NP` would need.  The deep shrinkage theorem itself is cited, not reproved;
proving it is a separate project, and even with it the explicit frontier remains polynomial.

**The shape of the residue.** Space is exact; time cannot be reached by space machinery; decision hardness is
unconditional but non‑explicit; the explicit residue is exactly `InNP`; counting provably cannot supply it
(natural‑proofs barrier); and the strongest explicit, *non‑natural* bound reachable caps at `n²/log n`. The one
thing left unbeaten is precisely the part no current method touches: an explicit, non‑natural decision lower bound
**above the Nečiporuk frontier** — which is `P ≠ NP`.

## 4¾. The N‑Frame / Book‑1 CEW route, audited — and the contextual‑invariant dichotomy

The N‑Frame "Book 1" programme proposes to close the separation via a contextual‑entanglement‑width (CEW) / SPDP
route.  It is fully formalized (`…Book1CEWRoute`) as the obligation structure `Book1CEWSPDPEpistemicBoundaryPort`,
whose four fields are Book 1's pillars — **(A1)** every poly‑time SAT decider has polylog CEW; **(A2)** polylog
CEW ⇒ poly SPDP rank; **(A3)** the hard family has super‑poly SPDP rank; **(A4)** a decider transports the hard
rank into its P‑side rank — and these **assemble into the separation** (`no_DTMDecidesSATWithEncoding_of_book1CEWSPDP`
⇒ `standardPvsNP_of_book1CEWSPDP`).  So "A1–A4 ⇒ `P ≠ NP`" is a theorem; the whole content is in the fields.

**A1 is the live wall (`…NFrameBook1Audit`).**  A2 is a proved counting lemma, A3 the calibrated Tseitin/Ramanujan
bound, A4 a transport certificate; **A1** — bounded contextual width for *all* of `P` — is the only `P ≠ NP`‑strength
field, and in `…Book1CEWRoute` it is discharged *only* by a **syntactic** log‑window surrogate
(`book1LogSyntacticPCEW`), explicitly flagged as not a claim about arbitrary deciders.  The audit pins this to
proved theorems: `naive_time_cew_bound_false` (no function of the time bound alone bounds the observer action —
from `action_unbounded_by_time`); `space_bound_does_supply_A1` (a *space* bound does, via `subcritical_of_lowspace`
— the working surrogate is always space/contextual, never time‑only); `correct_decider_escapes_boundary_A1` (a
*correct* full‑boundary decider exists, so "every decider has low boundary" is false even semantically — CEW must
be strictly finer than boundary).

**The contextual‑invariant dichotomy — both horns proved.**  A1 asks for a contextual invariant that is
**bounded for `P`** *and* **super‑polynomial on the hard family**.  We define both natural unweighted forms and
prove neither can be both at once:

* **Additive horn (`…TimeContextualWidth`).**  `tcw = ∑_{τ<T} log₂(contextWidth (views τ))` — cumulative
  contextual log‑width, tied to the *realized image* at each step (so it dodges `action_unbounded_by_time`, which
  the raw action did not).  `tcw_le_steps_mul_bits` (`tcw ≤ T·n`) and `tcw_A1_bounded_for_polyTime`
  (`≤ n^{k+1}` for `T ≤ n^k`) make **A1 a theorem** — bounded contextual width for *all* of `P`.  But
  `additive_tcw_below_superpoly_threshold` proves the price: being additive, `tcw` is capped at `T·n` = poly, so
  at infinitely many lengths *every* poly‑time observer (any correct SAT decider included) has `tcw` strictly
  below the super‑poly threshold `n^{log₂ n/4}` — **A3 is provably false** for it.
* **Multiplicative / rank horn (`…RankContextualWidth`).**  `crank M = ` #distinct rows of the past/future cut
  matrix — a genuine rank/communication quantity, super‑additive across the cut.  `crank_cube_full`
  (`crank(equality matrix) = 2^a`) and `crank_unbounded` (`∀ N, ∃ a, N < crank`) make **A3 reachable** — the
  super‑poly half the additive horn lacked.  But `rank_space_bound_tight` proves the price: the only bound the
  budget yields is `crank ≤ 2^{space}`, and it is **tight**; a poly‑*time* observer has only poly *space*, so its
  rank ceiling is `2^{poly}` — exponential, not poly, and achievable — so **A1 does not follow from the budget**.
  Raw rank fails A1 even more sharply: linear‑time functions (inner‑product, equality) already have *full* rank
  `2^{n/2}`, so raw `crank` is in `P` at the top of its range.

| invariant | shape | A1 (bounded for `P`) | A3 (super‑poly on hard family) |
|---|---|---|---|
| `tcw` (additive) | `∑ log width` | **provable** (`≤ T·n`) | **provably false** (capped at `T·n`) |
| `crank` (multiplicative) | `#distinct rows` | **the wall** (only `≤ 2^{space}`, tight; false for raw rank) | **provable** (`= 2^a`, unbounded) |

So an *unweighted* contextual invariant is provably either too small to separate (additive) or achieved by easy
functions and bounded only by space (multiplicative).  A1 can hold *and* A3 hold only for a rank that is low
**because of the time structure** — a *projected* SPDP rank where the poly‑time presentation forces the projection
low.  That projection is exactly Book 1's A2/A1 bridge, which the corpus audit (`…NFrameHypercubeConstraint`)
already found **assumed, not derived** — the one irreducible `P ≠ NP`‑strength step.  The dichotomy is *proved*,
not asserted: it converts "you would need a finer invariant" into a machine‑checked no‑go on both natural forms.

**The projected‑rank arc — and its closing verdict (`…ProjectedContextualRank` → `…AffineIndicatorCollapse`).**
The dichotomy left exactly one opening: a *projection* `proj : (row) → R` that collapses easy high‑rank rows but
preserves hard ones.  This was made a concrete target and then probed to its conclusion:

* **The certificate framework** (`…ProjectedContextualRank`): `pcrank proj M = ` #distinct projected rows.  Sanity
  laws proved — `pcrank_le_crank`, `pcrank_id_eq_crank`, `pcrank_le_of_factor` (a richer projection only raises
  rank), `pcrank_eq_crank_of_injOn` (rank survives an injective projection), `pcrank_ge_of_injOn` (A3‑survival
  certificate).  A1 becomes a checkable certificate: `BoundedRangeProjection` ⇒ poly `pcrank`, and
  `boundedRange_cannot_be_separating` proves the cheap (small‑codomain) A1 is fatal to A3.  The isolated target:
  `SeparatingProjection` — and `separatingProjection_forces_selective` proves any solution must *merge* easy rows
  yet stay injective on hard rows (distinguish hardness from easiness at the row‑vector level).
* **Two concrete projections, calibrated.**  *Low‑degree* (`…LowDegreeProjection`): the row's restriction to
  low‑Hamming‑weight inputs.  It collapses the easy equality matrix to poly (`lowDegProj_eqMatrix_le`) and escapes
  the deathtrap (feature space `2^{poly}`).  *SPDP* (`…SPDPFeatureProjection`): the order‑`≤k` discrete
  derivatives on low‑weight inputs.  `spdp_refines_lowDeg` proves SPDP **dominates** low‑degree (the `S=∅`
  coordinate *is* the value), so it preserves at least as many rows — the strictly stronger candidate.
* **The A3 probes.**  On the *linear* core (`…SPDPHardSurvivalProbe`, the inner‑product/parity matrix) SPDP
  **survives** — `spdp_ipMatrix_survives` gives full rank `2^a` — but so does low‑degree (`ipMatrix` is degree 1,
  and is in `P`), so this separates nothing.  The decisive test is the *high‑degree* affine‑indicator residual
  (`…AffineIndicatorCollapse`): rows `= {v : v|_P = s}` (degree `|P|`), raw rank `2^{|P|}` (super‑poly).
* **The closing verdict — locality kills it (`…AffineIndicatorCollapse`).**  `spdpProj_zero_of_vanishing`:
  **SPDP at constant order `(k,d)` only ever evaluates a row on the weight‑`≤(k+d)` ball** — it is a *local*
  probe.  So `spdp_constraintMatrix_collapse` gives `pcrank (spdpProj a k d) ≤ N(|P|, k+d)+1` = **poly** while the
  raw rank is `2^{|P|}`: **SPDP collapses the high‑degree affine / Tseitin‑style residual to polynomial rank, just
  as low‑degree does** (`lowDeg_constraintMatrix_collapse`).  The locality/feature‑count tension is the
  quantitative shadow of the SPDP‑rank barrier: a *bounded*‑order feature map is too local to separate
  high‑codimension residuals, and making the order scale with the codimension (`k+d = Ω(distance)`) blows the
  feature space to `2^{super‑poly}`, breaking A1's poly feature‑count.  **A projected rank with a *fixed* feature
  order is ruled out** — the surviving live direction is precisely a projection whose order scales with the
  instance, which is the SPDP‑rank lower‑bound program itself (the barriered `P ≠ NP`‑strength step).  Net: the
  one opening the dichotomy left is closed for every *fixed*‑order projection, by a proved locality no‑go.

**The scaling‑order arc — chasing the one surviving opening to a single named bridge.**  The fixed‑order no‑go
left exactly one route: let the projection order *scale*.  Three amplifiers were tried and the Gödel‑hierarchy
tower built; all converge on one theorem.

* **Expander amplification (`…ExpanderAmplificationBoundary`).**  Can a Ramanujan/expander layer rescue survival?
  `highDistance_spdp_collapse`: if every accepting input has weight `≥ Δ` (the expansion distance) and the probe
  order `k+d < Δ`, then `pcrank ≤ 1` — coupling many high‑distance copies amplifies the *zero* feature, so the
  collapse becomes **total**.  An expander amplifies *visibility, not invisibility*.  `lowDeg_full_order_eq_crank`
  (at radius `a`, `pcrank = crank`) marks the other end: the rank turns on only once the radius crosses `Δ`.
* **PAC operations (`…PACProjectionBoundary`).**  The `p vs np1` PAC layer helps two real ways —
  `pac_postProjection_nonincrease` (PAC restrictions are rank‑non‑increasing: P‑side A1 control) and the
  positive‑minor geometry (A3 side) — but `pacRelabel_spdp_collapse` proves a local PAC relabelling (weight blowup
  `≤ c`) only rescales the visibility radius by `c`, so a high‑distance residual still collapses when `c·(k+d)<Δ`.
  `pacPositiveMinorSurvival_needs_scaling`: a surviving positive minor (`s ≥ 2`) *forces* `Δ ≤ k+d`.  PAC's only
  opening is scaling order.
* **The Gödel hierarchy tower (`…GodelHierarchySPDPScaling`).**  Book 1's Gödel hierarchy = an *ascending*
  projection tower `levelProj L`, `godelLevel n = (log₂ n, log₂ n)`.  `levelProj_monotone` (proved): higher levels
  see at least as much (lower‑level features are coordinates of higher).  `levelProj_full_radius_eq_crank`
  (proved): at the top of the tower `pcrank = crank` — full visibility.  So the tower provably does what fixed
  SPDP could not: **visibility rises with level**, and a high enough level sees the hard residuals.
* **But the feature budget is vacuous at the scaling level (`…GodelFeatureCount`).**  `godel_feature_bound_vacuous`:
  the only generic bound is `pcrank ≤ 2^{count}`, and `godel_feature_count_superlinear` proves `count > n/2` at the
  Gödel level (`count ≥ 2^{log₂ n}`, `n < 2·2^{log₂ n}`).  So the ceiling is `2^{>n/2}` — **exponential, no better
  than the trivial `crank`**.  A polynomial A1 therefore **cannot** come from bounding the *size* of the feature
  space; it must come from poly‑time *structure* forcing few *realized* features.  A1 is provably **not a counting
  fact**.

The whole arc converges on one statement, named in `…GodelHierarchySPDPScaling`: **`PolyTimeLowGodelSPDP`** — at
the Gödel‑scaled level, every poly‑time observer's *realized* pcrank is polynomial, while the hard family's is
super‑polynomial (`HardFamilyHighGodelSPDP`); `godelSPDP_no_shared_rank` proves these are incompatible, so the
pair *is* the separation.  This is the **`ScalingSPDPBridge`**: order `≥` distance (to see the hard residual) with
*realized* feature count polynomial for `P` (to bound it) — exactly the barriered SPDP‑rank lower bound, and the
single irreducible `P ≠ NP`‑strength step the entire projected/scaling‑rank programme reduces to.

**ACC⁰ calibration — the method's credibility test (`…ACC0DynamicSPDPCalibration` → `…ACC0Composition`).**
*Diagnostic, explicitly necessary‑not‑sufficient: `NP ⊄ ACC⁰` is strictly weaker than `P ≠ NP` (`ACC⁰ ⊊ P/poly`),
and even Williams only has `NEXP ⊄ ACC⁰`.*  ACC⁰ is where AC⁰[p] (Razborov–Smolensky, handled) meets the
mixed‑moduli wall, so it tests whether dynamic‑SPDP has teeth.  A modular gate is a function of a few **weight
statistics**; `statRow_realized_le` proves a class of statistic‑rows whose reachable statistics lie in a set `R`
has `≤ 2^{|R|}` realized features.  Consequences, all proved:

* **single modulus is tame** — `godel_symmetric_realized_poly`: one statistic (total weight) gives `≤ 2n`
  realized features at the Gödel level — *polynomial*.  The framework explains AC⁰[p]/single‑modulus cleanly.
* **`k` moduli cost `2^{(d+1)^k}`** — `kStat_realized_le`: `k=1` poly, `k=2` quasi‑poly (`2^{O(log² n)}`),
  growing `k` super‑poly.
* **composition drives `k` to the gate count** — `…ACC0Composition`: a depth‑2 circuit of `m` modular gates
  factors through the combined `m`‑tuple statistic (`composed_realized_le`, `≤ 2^{(d+1)^m}`), and
  `composed_budget_ge_double_exp` gives `2^{2^m} ≤ 2^{(d+1)^m}` — doubly exponential in the gate count.

So the reach is sharp and matches the AC⁰[p] → ACC⁰ frontier exactly: **counting statistics works for one gate and
is vacuous for composed ACC⁰** (the budget leaves polynomial at the first mixed‑modulus stacking).  An A1 bound for
the full class (`ACC0LowRealizedGodelSPDP`, named) **cannot come from counting** — it needs the genuine
Razborov–Smolensky / correlation structure, the open `NP ⊄ ACC⁰`‑strength content.  The calibration *locates* that
obstruction precisely (the number of independent moduli the projection must jointly resolve); it proves no new
lower bound, and clearing ACC⁰ would still be a waypoint below `P ≠ NP`, not the separation.

**The naturalness range — why dynamic‑SPDP reaches ACC⁰ but provably not `P ≠ NP` (`…DynamicSPDPNaturalnessRange`).**
Dynamic‑SPDP is a *feature‑counting* method, so its lower‑bound certificate ("`f` has high realized features" =
`f` is `Hard` for the low‑feature class) is a **natural property** — large, useful, constructive.  The
Razborov–Rudich barrier we already formalized (`…NaturalProofsBarrier`) is *class‑dependent* (`Crypto` = the class
contains PRFs):

* `dynamicSPDP_certificate_large` / `dynamicSPDP_certificate_useful` — the certificate is natural (reuses the
  barrier file's `counting_property_is_large`, `hard_property_useful`).
* `dynamicSPDP_blocked_of_crypto` — `Crypto` (PRFs, e.g. `P/poly`) ⇒ the barrier derives `False` from a
  constructive certificate: **dynamic‑SPDP cannot reach `P ≠ NP`**, by the very barrier we proved.
* `dynamicSPDP_unblocked_of_no_crypto` — `¬ Crypto` (no PRFs, e.g. ACC⁰) ⇒ the barrier is *vacuous*: a natural
  lower bound is **permitted**.
* `dynamicSPDP_range_dichotomy` — the three together.

So the method's reach is **exactly the PRF‑free classes**: ACC⁰ is a legitimate, un‑barriered avenue (the
composition theorem is worth attempting, via a natural/algebraic route distinct from Williams' non‑natural
algorithmic method), while `P/poly`/`P ≠ NP` is *blocked by the same barrier* — capped precisely where PRFs
appear.  "Stronger than Williams" is plausible only at the ACC⁰ level, and is provably not a path to the
separation.

**The holonomy arc — a global cycle detector that sees what local SPDP missed, fully characterized
(`…HypergraphHolonomySPDP` → `…HolonomyPSideControl` → `…HolonomyBoundedCycleRank` → `…HolonomyCompositionRank` →
`…HolonomyEffectiveRank` → `…HolonomyHardEffectiveRank`).**  Within the un‑barriered ACC⁰ avenue, the most promising
invariant: not a local Hamming‑ball probe but a *cycle‑obstruction* one.  On a Tseitin constraint graph the
**holonomy of a cycle `C`** is the `F₂` sum of vertex charges around it, `⊕_{v∈C} charge(v)` — zero for a
satisfiable charge, nonzero exactly when the cycle is globally inconsistent.

* **Hard side (`…HypergraphHolonomySPDP`).**  `cycleHolonomy_singleton` — a single charge on a cycle gives
  *nontrivial* holonomy: the obstruction is **visible at Hamming weight 1**, the regime where the affine‑indicator
  collapse killed local SPDP.  `holonomy_realizes_all` — `m` disjoint cycles realize **all `2^m` holonomy
  signatures** (`holonomy_zero_charge` gives the trivial/satisfiable collapse).  This is the first invariant in
  the arc that does what fixed SPDP could not — a genuine global‑cycle detector, not renamed geometry.
* **The linear asset (`…HolonomyPSideControl`).**  `holSigZ_add` / `holSigZ_zero` — over `F₂` holonomy is a
  **linear** map of the charge, so the realized signatures form a *subspace* and the class count is `2^{rank}`,
  governed by the **cycle rank** rather than the gate count.  `holonomy_acyclic_trivial` (no cycles ⇒ 1 class) and
  `holonomy_classes_le_of_generators` (`k` generators ⇒ `≤ 2^k`) bracket it.
* **Tame side, provable (`…HolonomyBoundedCycleRank`).**  `holonomy_classes_le_of_basis` — **cycle rank `≤ r` ⇒
  `≤ 2^r` holonomy classes**, for any charge family, independent of `m`/gate count; `holonomy_classes_le_of_few_distinct`
  proves the factoring concretely (`m` cycles from `r` distinct basis cycles).  So bounded‑treewidth/planar
  constraint graphs (cycle rank `O(\log n)`/`O(1)`) are *provably tame* (poly/constant classes), expanders
  (`Ω(n)`) hit `2^{Ω(n)}` — conditional only on a **graph property**, not a complexity assumption (not a socket).
* **Composition — rank is subadditive (`…HolonomyCompositionRank`).**  `holonomy_classes_submultiplicative`
  (`#classes(A ⊞ B) ≤ #classes(A)·#classes(B)`) and `holonomy_rank_subadditive` (`2^{rₐ}·2^{r_b} ⇒ 2^{rₐ+r_b}`):
  composing `k` sources of rank `≤ s` gives `≤ 2^{k·s}` — exponent **linear** in `k`, versus the feature budget's
  `2^{(d+1)^k}` (exponent *exponential* in `k`).  The genuine quantitative advantage over feature‑counting.
* **Effective rank of *realized* charges, two‑sided (`…HolonomyEffectiveRank`, `…HolonomyHardEffectiveRank`).**
  The right object is not the raw graph's cycle rank but the rank the circuit's *actual* charges span.
  `realized_le_of_factorThroughStat` + `modular_layer_realized_le` — a `k`‑gate modular layer's realized charges
  factor through `k` statistics ⇒ `≤ q^k` classes (effective rank `≤ k·log₂ q`, *additive*): the AC⁰[p] success in
  holonomy form.  `expander_realizedClasses_eq` — the matching lower bound: the charged expander gadget realizes
  the full `2^m` classes (effective rank `= m`).

So the holonomy invariant is now **fully characterized, both ends proved on the same object** (`realizedClasses`):
tame computations (low statistic count) → low effective rank `≤ k·log₂ q`; the Tseitin/expander family → full
effective rank `= m`.  It genuinely does what fixed SPDP could not — weight‑1 visibility, linear `2^{rank}` control,
*additive* composition — and it cleanly separates tame from hard.  The one missing implication is exactly
**poly‑time ⇒ low *effective* cycle rank on hard instances** (= `ACC0LowRealizedGodelSPDP`), which is *false* for
the raw constraint graph (an ACC⁰ circuit can encode an expander) so it is genuinely about the *realized* charges;
it is the open `NP ⊄ ACC⁰`‑strength step, and the PRF‑free naturalness ceiling
(`…DynamicSPDPNaturalnessRange`) caps even that below `P ≠ NP`.

**The direct bridge attempt, and the exact wall (`…ACC0BridgeAttempt`).**  Attacking the bridge head‑on yields a
genuine *restricted* lower bound, not the separation: `effectiveRank_gate_lower_bound` — a `MOD q` circuit whose
realized charges have effective rank `≥ m` and factor through `k` modular statistics needs `2^m ≤ q^k`, i.e.
`k ≥ m/log₂ q` gates (so the expander charge family needs `≥ m/log₂ q` modular gates).  `logGate_bridge_holds` —
the bridge *holds* for `O(log n)`‑gate circuits (`≤ n^q` classes, poly).  `polyGate_counting_bound_ge_two_pow` —
the wall: at poly gates the counting bound is `q^n ≥ 2^n`, vacuous.  The attempt stops at two precise barriers:
(i) **model gap** — it bounds charge *realization*, not language *decision*; (ii) **the log→poly gate jump** — the
method proves tameness only for `O(log n)` gates.  Either way it does not reach `NP ⊄ ACC⁰`, and even if it did
the naturalness ceiling caps it below `P ≠ NP`.

**The restriction / switching surrogate — proved for the fragment, named for general ACC⁰
(`…ACCHolonomyRestriction`, `…FragmentSwitching`).**  The sharpest two‑sided formulation of what an ACC⁰ lower
bound needs, with one side proved.  *Hard side, proved:* `tseitin_holonomy_survives_restriction` — a restriction
leaving `K` cycles free preserves all `2^{|K|}` holonomy classes (Tseitin/expander holonomy is robust under
restriction).  *Tame side, the switching surrogate, proved for the modular‑statistic fragment:*
`restriction_lowers_effective_holonomy_rank` — a restriction fixing `k − j` statistics leaves the charge factoring
through `j` free ones, so `realizedClasses ≤ q^j` and `q^j < q^k` (the rank bound strictly drops).  Combined,
`fragment_below_surviving_tseitin`: when `q^j < 2^{|K|}` the fragment realizes *strictly fewer* classes than the
hard family retains — a genuine fragment‑level lower bound, the switching mechanism (restriction lowers the
circuit, Tseitin survives) closing end‑to‑end **on the fragment**.  *The open core:* the same surrogate for
*poly‑gate* ACC⁰ — `ACC0LowEffectiveHolonomyRank` (`acc0_holonomy_separation` is the conditional it feeds) —
fails to generalize for a precise reason: a poly‑gate ACC⁰ circuit need not factor through few statistics, and a
restriction need not reduce its statistic count (it can encode an expander).  That exact hypothesis is
`NP ⊄ ACC⁰`‑strength, still under the naturalness ceiling.  So the mechanism is *correct and complete on the
fragment*; extending it to poly‑gate ACC⁰ is the major missing theorem, not a gap in the surrogate.

**The rank‑lowering rung‑ladder — climbing fragments toward ACC⁰, with the wall exposed.**  Following the route of
proving rank lowering for ever‑richer fragments (rather than ACC⁰ directly), four rungs are proved, each
*deriving* the rank drop from circuit structure:

| rung | why incidence / statistic count is controlled | file |
|---|---|---|
| modular‑statistic | restriction fixes `k − j` statistics directly (`restriction_lowers_effective_holonomy_rank`) | `…FragmentSwitching` |
| read‑once (disjoint supports) | fixing a gate's whole support makes it constant (`gate_constant`); free gates `{i ∉ T}` give `q^{#free}` (`readonce_restriction_lowers_rank`) | `…ACCRankLoweringTarget` |
| bounded overlap (incidence `≤ d`) | `#free gates ≤ d · #free vars` (`bounded_overlap_free_gates_le`), so `q^{d·u}` (`bounded_overlap_restriction_lowers_rank`) | `…BoundedOverlapRankLowering` |
| bounded‑depth tree (no re‑use) | `incid v t ≤ depth t + 1` (`incid_le_depth`) ⇒ incidence `≤ D+1` ⇒ the bounded‑overlap bound with `d = D+1` | `…BoundedDepthTreeRung` |

Climbing exposes the **common mechanism**: every rung relies on a restriction fixing some gate's *whole support*,
making it constant, which drops the realized statistic count; each rung supplies a different reason the incidence
(hence surviving‑gate count) stays controlled — disjointness → bounded incidence → bounded depth.  And it
characterizes the **wall** exactly: *poly‑gate ACC⁰ with unbounded re‑use defeats that hook* — a single variable
can feed unboundedly many gates, so a restriction need not fix any gate's whole support, no gate need become
constant, and the statistic count need not drop (the circuit can stay fully live, encoding an expander).  That is
`ACCRestrictionLowersEffectiveRank` (`…ACCRankLoweringTarget`), `NP ⊄ ACC⁰`‑strength, under the naturalness
ceiling.  Bounded *re‑use* (`≤ r` gates per variable) is already covered (it is bounded incidence with `d = r`);
the genuinely‑open case is *unbounded* re‑use — the wall itself.  The ladder climbs exactly as far as the
whole‑support‑fixing mechanism reaches, and names where it stops.

**The reuse boundary — the route's terminus (`…ACCReuseSwitchingTarget`).**  Pushing one step past the ladder
confirms it is genuinely exhausted, with the negative outcome proved.  `restricted_rank_le_two_pow_free` — a
*fully general* rank lowering surviving arbitrary re‑use: after any restriction leaving `u` free variables the
restricted charge depends only on those coordinates, so `realizedClasses ≤ 2^u` for **any** circuit
(`override_eq_extend`).  So restrictions do lower rank despite reuse — but to the **circuit‑independent** bound
`2^{#free vars}`, which *cannot separate*: it is tight (the expander achieves it, `expander_realizedClasses_eq`),
so a restriction leaving `u` free variables lowers **both** the circuit and the hard family to `≤ 2^u` — no gap.
The whole‑support hook gave a *circuit‑specific* drop (`q^{#free gates} ≪ 2^{#free vars}`) that beat the hard
family; unbounded re‑use kills it, leaving only the non‑discriminating generic bound.  A separation therefore
needs a *circuit‑specific* drop under reuse — `RandomRestrictionLowersEffectiveRankDespiteReuse`, the named target
— which is Håstad‑shrinkage / Razborov–Smolensky‑correlation content, `NP ⊄ ACC⁰`‑strength under the naturalness
ceiling.  **The counting / restriction family of mechanisms is now provably exhausted for this purpose**; the next
move is the Williams / correlation mechanism, not another rung.

So this exploration is **closed as a characterized invariant**: the strongest the arc produced, separating tame
from hard, with the single remaining bridge named precisely and proved to lie beyond the method's reach — not
bridged, and honestly not bridgeable here.  (Adjacent and also closed: the explicit‑Nečiporuk frontier — the
corpus's arc reaches the `n²/log n` method ceiling, and `…NeciporukCeiling` formalizes *why* subfunction counting
cannot exceed it — `log₂ #subfunctions ≤ min(2^b, n-b)` per block — so super‑`n²/log n` explicit bounds require a
different method, shrinkage/Andreev, not this one.)

### 4¾.bis  The correlation / Williams mechanism — opened, with the engine and the fragment test proved

The reuse boundary closed the *counting / restriction* family; the named successor was the **correlation /
Williams** mechanism (non‑natural, evading the Razborov–Rudich barrier that caps the rank route).  That mechanism
is now **opened and tested**, not merely named.

**The frontier and the cash‑out shape (`…ACCWilliamsCorrelationTarget`).**  `class_agreement_le_majority` — a
predictor with a fixed value on a class agrees with the target on at most that class's *majority*
(`max(#true,#false)`); summed over a coarse predictor's few classes, a balanced target is matched only `≈ ½`.
`acc0_williams_cashout` — the algorithmic‑method skeleton `smallACC0 → fastSat → collapse`, so `¬collapse ⇒
¬smallACC0` (no axioms); the hard `smallACC0 → fastSat` step (Williams' fast ACC⁰‑SAT algorithm) is a named,
*non‑natural* hypothesis.  Open target named: `ACC0CorrelationAgainstTseitin`.

**The engine — measure bias, not class count (`…HolonomyCorrelationEngine`).**  This is the genuinely new object.
`agreement_le_sum_majority` — **the correlation bridge**: a class‑constant predictor `g∘π` agrees with `f` on `≤`
the sum over its classes of the per‑class majority of `f`.  `low_rank_predictor_low_correlation_with_full_holonomy`
— **the seed**: if `f` is *balanced on every predictor class*, then `2·agreement ≤ #inputs` — the predictor
correlates `≈ ½`, **no advantage**.  Correlation is forced by the predictor's *coarseness*, not by counting
functions — the route the naturalness barrier does not cap.  The open input was *balance‑per‑class*.

**The first real test — balance for restricted predictors (`…HolonomyBalanceFragments`), proved.**  Balance is now
*discharged* for restricted predictors against the genuine holonomy/parity target `fParity D x = ⊕_{i∈D} x i`.
Mechanism: a **missed parity variable**.  `balanced_per_class_of_involution` — a class‑preserving,
target‑flipping involution forces per‑class balance (it swaps `f=true` with `f=false` inside each class).  The
involution is `flipAt v` (flip an *unread* coordinate `v∈D`): `factorsThrough_flipAt` (a predictor that ignores
`v` is flip‑invariant) + `fParity_flipAt_mem` (flipping `v∈D` toggles the parity, via `parityCharge_flipAt_mem`
and the `ZMod 2` toggle).  Hence `parity_balanced_of_missed_var`, and `parity_balanced_of_card_gap` (pigeonhole:
`#read set < #parity support ⇒` a missed variable).  Combined with the seed:
`restricted_fragment_low_correlation` — **a predictor reading fewer variables than the holonomy parity spans has no
correlation advantage against it** (`2·agreement ≤ #inputs`).  The three named fragments each supply the read‑set
bound from their gate structure: `logGate_predictor_classes_balanced` (gate‑count × fan‑in), `readOnce_…`
(disjoint supports ⇒ `∑|supp|`), `boundedOverlap_…` (direct read‑set bound).  All clean axioms
(`[propext, Classical.choice, Quot.sound]`), no `sorry`.

**The honest finding.**  At the *rank* level the three fragments were genuinely distinct (incidence drove distinct
`q^{·}` bounds).  At the *correlation* level they **collapse to one condition** — does the read set miss a parity
variable? — and the overlap parameter that mattered for rank is *irrelevant* to balance.  A real structural
observation, not a gap.

**Where it stops (`ACC0ApproximatesByLowRankPredictors`, named open).**  This bites *restricted* predictors whose
read set is provably smaller than the parity support.  A general poly‑size ACC⁰ circuit reads *all* `n` variables,
so no single unread coordinate exists — balance via a fixed flipped coordinate fails, and one needs an
*approximate* low‑dimensional predictor (ACC⁰ ≈ a coarse holonomy predictor up to a `½−ε` deficit).  That bridge,
fed into `restricted_fragment_low_correlation` + `acc0_williams_cashout`, would give `NP ⊄ ACC⁰` — and is itself
`NP ⊄ ACC⁰`‑strength under the PRF‑free naturalness ceiling.  So the correlation route is **opened and proved on
fragments**; the load‑bearing ACC⁰‑approximation step is named and lies (as expected) at the same wall.

**The bridge tested explicitly on a `MOD q` gate (`…ModQGateBalance`).**  Running the smallest genuine
all‑variable ACC⁰ gate confirms the engine's prediction *precisely*.  A `MOD q` gate is the cleanest low‑rank
predictor — it factors through the count statistic `modQStat q x = (∑_i x_i) ∈ ZMod q` (`q` classes) — but it
reads every variable, so the missed‑variable involution `flipAt v` breaks: one flip changes the count by `±1` and
leaves the `MOD q` class.  **What survives is a count‑preserving *pair* swap** `pairSwap v w` (flip `v∈D` and a
witness `w∉D` together): on the off‑diagonal `{x_v ≠ x_w}` it preserves the count *exactly*
(`modQStat_pairSwap_offdiag`: one coordinate `0→1`, the other `1→0`) and toggles the parity (`fParity_pairSwap`),
so `balanced_per_class_of_involution` gives **exact per‑class balance off‑diagonal** (`modQ_class_balanced_offdiagonal`,
`offdiag_balanced`) and hence **no `MOD q`‑gate correlation advantage against the holonomy parity on the
off‑diagonal** (`modQ_gate_low_correlation_offdiagonal`: `2·agreement ≤ #off‑diagonal inputs`).  All clean axioms,
no `sorry`.  **The residual is exactly the diagonal** `{x_v = x_w}` (there the pair swap changes the count by `±2`
and leaves the class) — and this is now a *theorem*, not an observation: `modQ_class_imbalance_on_diagonal` proves
the full‑class holonomy‑parity imbalance **equals** the diagonal imbalance (split each class off/diagonal via
`card_eq_offdiag_add_diag`; the off‑diagonal part is exactly balanced via `balanced_of_involution`, so it cancels).
That diagonal residual is the classical `MOD q`‑vs‑parity correlation, exponentially small in `n−|D|` by a
Fourier/character recursion but *not* by any involution (`ModQParityCorrelationExpSmall`, named open).  So the
approximate bridge **partly survives** — a count‑preserving symmetry recovers exact balance on the symmetric part,
and *localizes* the entire deficit to the diagonal — and the irreducible remainder is, once again, the same
Razborov–Smolensky‑flavoured `NP ⊄ ACC⁰`‑strength estimate.

**Attacking the diagonal recursively settles the modulus split.**  Pushing the recursion onto the diagonal answers
the closing question — involutive bound or character sum? — with *it depends on `q`*.  On the diagonal the only
count‑preserving parity‑toggling move is the flip‑both involution `pairSwap v w` (which keeps `x_v=x_w` and changes
the count by `±2`).  **For `q = 2` that `±2` is `0`**, so `modQStat_two_pairSwap` preserves the `MOD 2` count
*unconditionally*, the diagonal is balanced (`modQ2_diagonal_balanced`), and combined with the localization the
recursion **closes**: `modQ2_class_balanced` / `modQ2_gate_zero_correlation` prove a `MOD 2` gate has **exactly
zero** correlation advantage against *any* holonomy parity over `D ⊊` variables, over *all* inputs, fully by
involution (no character sum, no `sorry`, clean axioms).  **For `q > 2`** the `±2` is nonzero mod `q`, the diagonal
involution is blocked, and only there does the genuine character‑sum residual remain
(`ModQParityCorrelationExpSmall`, now scoped to `q > 2`).  So the involution method reaches *all the way* at the
modulus `2` (an exact RS‑style zero‑correlation theorem, proved) and bottoms out only for higher moduli — a clean,
honest delineation of exactly where the elementary method stops.

**Disjoint pair stacking extends the recursion to `q > 2` (`imbalance_localize_step`, `imbalance_stacked`).**  The
single‑pair flip‑both is blocked for `q > 2` only on the *diagonal*; a *fresh disjoint* pair `(v_i ∈ D, w_i ∉ D)`
(all coordinates distinct from earlier pairs) flips one‑up / one‑down on its own off‑diagonal — count change `0`
mod **any** `q` — and leaves the earlier pairs' coordinates untouched, so it peels another exactly‑balanced layer.
Proved (clean, all `q`, by induction over a `Nodup`‑coordinate list of pairs): `imbalance_stacked` —
**the holonomy‑parity imbalance of a `MOD q` class equals that of its `k`‑fold diagonal** `⋂_{i≤k} {x_{v_i} =
x_{w_i}}`.  So even for `q > 2` involution does real work: it shrinks the imbalance‑carrying support pair by pair
(general step `imbalance_localize_step` parameterised by any ambient set closed under the flip‑both on its
off‑diagonal).  The recursion runs until no fresh disjoint `(in‑D, out‑D)` pair remains — `min(|D|, n−|D|)` steps —
and only that final core is the irreducible character sum.  Net delineation: **modulus `2` is fully involutive;
higher moduli are involutive down to a shrinking diagonal core**, the character sum required only on the core.

**The shrinkage becomes a numeric bound (`flipAt_card_eq_ne`, `diag_card_mul`, `imbalance_stacked_bound`).**  Each
disjoint equality constraint *halves the cube* — flipping a fresh coordinate `v` is a bijection between
`{x_v = x_w}` and `{x_v ≠ x_w}` (`flipAt_card_eq_ne`) — so the `k`‑fold diagonal has size exactly `2^{n-k}`
(`diag_card_mul`: `2^{|L|} · #(k‑fold diagonal) = 2^n`, proved by induction).  Since the imbalance equals the
`k`‑fold‑diagonal imbalance (`imbalance_stacked`) and any set's imbalance is bounded by its size,
`imbalance_stacked_bound` gives — for **all `q`**, clean, no `sorry` — the two‑sided bound
`|#{parity=true} − #{parity=false}| ≤ 2^{n-k}` on a `MOD q` class, with `k` the number of disjoint stacked pairs
(`k ≤ min(|D|, n−|D|)`).  This is a **genuine involutive exponential bound** on the `MOD q`‑vs‑parity correlation,
exp‑small whenever `min(|D|, n−|D|)` is large — the elementary method's quantitative reach, made explicit, with the
sharper `|cos(π/q)|^{n-|D|}` character estimate needed only to push past it on the residual core.

**Composition: two (and many) gates (`…TwoGateCorrelation`).**  The first mixed‑moduli ACC⁰ composition test.  The
general engine `balanced_offdiag_of_pres` / `low_correlation_of_pres` proves: *any* predictor `π` preserved by the
off‑diagonal flip‑both has exact off‑diagonal balance and no correlation advantage against the holonomy parity.
Two cases of the composition question:
- **Full‑support gates (any number, mixed moduli) compose freely.**  The off‑diagonal flip‑both moves one
  coordinate `0→1` and the other `1→0`, preserving the *integer weight* `∑_i x_i` exactly — hence every statistic
  factoring through it (`MOD q_j` for all `j`, threshold, exact‑count) at once.  `twoStat_pairSwap_offdiag` /
  `twoGate_low_correlation_offdiagonal`: the *single* involution `pairSwap v w` handles both gates, so mixed
  moduli are no obstacle.
- **Different supports give the product‑class obstruction.**  For supports `A, B` the swap must preserve both
  support‑counts: `weightOn_pairSwap_eq` shows `∑_S x` is preserved by the flip‑both **iff `v, w` lie on the same
  side of `S`**.  So a usable `D`‑witness pair needs `v ∈ D`, `w ∉ D` *and* `v, w` in a common cell of the
  partition `{A∩B, A∖B, B∖A, (A∪B)ᶜ}` — `twoGateOn_offdiag_balanced` (balance under `v∈A↔w∈A`, `v∈B↔w∈B`).  Same
  support collapses the cell conditions to one; full support makes them automatic; disjoint / bounded‑overlap
  supports turn finding witnesses into a **matching / flow problem** — the composition version of the engine, and
  `NP ⊄ ACC⁰`‑strength once the supports are adversarial.

**Many gates: the cell‑covering obstruction, made precise (`…ManyGateCorrelation`).**  For `k` gates with supports
`S₁,…,S_k`, the **cell** of a coordinate is its membership pattern `(v∈S₁,…,v∈S_k)`; the flip‑both preserves every
support‑count (hence every `MOD q_j`, any moduli) **iff `v,w` share a cell** (`SameCell`), so the `k`‑gate weight
vector is preserved (`weightVec_pairSwap`).  Proved: `kGate_offdiag_balanced` / `kGate_low_correlation_offdiagonal`
— a same‑cell `D`‑witness pair `(v∈D, w∉D)` gives exact off‑diagonal balance and no correlation advantage for *any*
gate function of the `k` statistics.  The frontier is then a clean **covering characterization**
(`cellWitness_iff_not_respects`): a witness exists *iff* `D` does **not** respect the cell partition (is not a union
of cells).  Two regimes: coarse supports (few gates → few cells) leave most `D` crossing a cell, so the engine
bites; but if the supports **shatter** `Fin n` into singleton cells (`respectsCells_of_separating`: separating
supports ⇒ *every* `D` respects the cells ⇒ no witness), the involution engine has nothing to act on.  Shattering
needs `≥ ⌈log₂ n⌉` independent modular statistics — so the elementary involution method survives exactly until an
ACC⁰ predictor reads enough gates to separate the coordinates, at which point composition becomes the
matching/shattering wall, `NP ⊄ ACC⁰`‑strength.  This pins where the correlation engine stops: not at a modulus,
not at two gates, but at the point where the gates' cells refine past the holonomy support's structure.

**The pigeonhole converse makes the `log₂ n` threshold a theorem (`…ManyGateCorrelation`).**  The cell map
`v ↦ (v∈S₁,…,v∈S_k)` lands in `Bool^{Fin k}` (size `2^k`); if `2^k < n` it cannot be injective
(`Fintype.exists_ne_map_eq_of_card_lt`), so two distinct coordinates share a cell — `exists_sameCell_pair_of_card_lt`
— and the singleton of one of them is a holonomy support the engine bites on — `exists_cellWitness_of_card_lt`
(clean, `2^k < n ⇒ ∃ D, CellWitness`).  This is the exact converse of `respectsCells_of_separating` (which needs
the supports to *separate* every coordinate, i.e. `≥ ⌈log₂ n⌉` gates): **below `k = ⌈log₂ n⌉` the engine always has
a `D` to act on; at or above it the supports can shatter the coordinates and kill the involution.**  So the
elementary correlation engine's survival is pinned to a sharp gate‑count threshold — `k` vs `log₂ n` — turning
"few gates ⇒ engine bites" from intuition into a proved dividing line, and locating the ACC⁰ wall exactly where the
modular‑statistic count crosses `log₂ n`.

**Past the shattering wall: restriction collapses cells (`…ACCRandomRestrictionCellCollapse`).**  The route through
the `log₂ n` wall (switching‑lemma strategy): a restriction simplifies the supports so the live coordinates regain
large cells, after which the same‑cell witness machinery bites on the restricted instance.  The *combinatorial*
half is proved deterministically; the *probabilistic* half is named.  Cell‑collapse mechanism: if the supports are
**trivial on the live set `L`** (each support lies entirely in or out of `L`, `TrivialOn`), then *all* live
coordinates share one cell (`sameCell_of_trivialOn`), so any holonomy support `D` separating two live coordinates
is a witness (`cellWitness_of_trivialOn`) and the `k`‑gate predictor has no correlation advantage
(`collapse_gives_low_correlation`, via `kGate_low_correlation_offdiagonal`).  A concrete instance needing **no
probability**: if `⋃_j S_j` misses `≥ 2` coordinates (fan‑in too small to cover), those untouched coordinates share
the empty cell and the engine bites (`exists_cellWitness_of_small_union`).  The one open step is named:
`ACC0RestrictionCollapsesCells` (a small‑depth ACC⁰ family admits a trivializing restriction on `≥ 2` live
coordinates) — proving a *random* restriction achieves this is the Håstad / Razborov–Smolensky switching content,
`NP ⊄ ACC⁰`‑strength; granted it, `acc0_collapse_gives_cellWitness` discharges the rest (collapse ⇒ witness ⇒
engine bites again below the live‑variable threshold).  So route 2 stands in skeleton: the deterministic cell
combinatorics are fully proved, the single probabilistic switching step isolated and named.

**A *proved* deterministic switching instance — bounded fan‑in.**  On the bounded‑fan‑in fragment the named
hypothesis becomes a theorem.  The explicit, non‑random restriction is the simplest possible: **kill every touched
coordinate**, i.e. take the live set `L = (⋃_j S_j)ᶜ`.  Then every support is disjoint from `L`
(`trivialOn_compl_union`: `TrivialOn` holds via the all‑disjoint branch), and for fan‑in `≤ s` the live set is
large — `card_compl_union_ge`: `n − k·s ≤ |L|` (`card_biUnion_le` + `∑ s = k·s`).  Hence `boundedFanIn_collapsesCells`:
`|S_j| ≤ s` and `k·s + 2 ≤ n` ⇒ `ACC0RestrictionCollapsesCells` *outright*, no probability.  Chained end‑to‑end,
`boundedFanIn_cellWitness`: bounded fan‑in too small to cover the cube ⇒ the engine bites (a holonomy support the
predictor cannot correlate with).  This converts the switching hypothesis into a proved lemma for depth‑2 /
bounded‑fan‑in supports; the full probabilistic switching lemma is what remains to lift it to poly‑size ACC⁰
(fan‑in up to `n`, where `k·s ≫ n` and the deterministic "kill‑all‑touched" restriction leaves nothing live).

**The probabilistic half — first moment (`…ACCRestrictionSwitchingProb`).**  The genuinely probabilistic rung: keep
each coordinate live independently with probability `p` (a `p`‑biased random restriction, live set `L` with weight
`p^{|L|}(1-p)^{n-|L|}`).  Proved: `biased_sum_one` — the weights form a real distribution (binomial theorem via
`Finset.prod_add`); `survProb_le` — a support of fan‑in `m` is killed with probability `(1-p)^m`, hence survives
with probability `1-(1-p)^m ≤ m·p` (Bernoulli's inequality `one_add_mul_le_pow`); `expectedLive_eq` — expected live
coordinates `= n·p`; and the payoff `expectedSurviving_le` — **the expected number of surviving supports is `≤
k·s·p`**, shrinking linearly in `p`.  So heavy restrictions (small `p`) drive the expected surviving count below
`1`, forcing a support‑killing restriction to exist — the first‑moment skeleton of switching.  Honest scope: the
first moment alone reaches the same `k·s < n` regime as the deterministic restriction (keeping `≥ 2` live needs
`p ≥ 2/n`, and `k·s·p < 1` then needs `k·s < n/2`); beating it — a *constant* live fraction `p` while still
collapsing high‑fan‑in supports — needs the higher‑moment Håstad switching argument, the genuine
`NP ⊄ ACC⁰` content.  The random restriction is now modelled, its distribution validated, and the expected
surviving‑support count provably shrinks; the higher‑moment concentration is the remaining frontier.

**The probabilistic half — second moment (`…ACCRestrictionSwitchingVariance`).**  Chebyshev needs the variance.
For `X = ∑_j X_j` (surviving‑support count), the covariance of two survival indicators equals that of the kill
indicators, and kill events are "all coordinates dead": `cov_eq` —
`Cov(X_S, X_T) = (1-p)^{|S∪T|} − (1-p)^{|S|+|T|}`.  Proved: `cov_nonneg` (overlapping supports are *positively*
correlated, since `|S∪T| ≤ |S|+|T|` and `0 ≤ 1-p ≤ 1`); `cov_disjoint` (disjoint supports are *uncorrelated* —
independence, `|S∪T| = |S|+|T|`); `cov_self_le` (`Cov(X_S,X_S) = P(S kill)·P(S surv) ≤ P(S surv)`, an indicator's
variance `≤` its mean).  Hence the payoff `variance_disjoint_le` — **`Var[X] ≤ k·s·p` for pairwise‑disjoint
supports**: off‑diagonal covariances vanish, so `Var[X] = ∑_j Cov(X_j,X_j) ≤ ∑_j P(S_j surv) = E[X] ≤ k·s·p`.
This is the **Chebyshev input** (`P(|X−E[X]| ≥ t) ≤ Var/t²`) that upgrades "few survive in expectation" to "few
survive w.h.p." at constant `p` — the move beyond the first‑moment `k·s < n` regime, for disjoint supports.  For
*overlapping* supports `cov_nonneg` exposes the obstruction exactly: positive correlation inflates the variance,
and controlling that term is the higher‑moment Håstad switching content, `NP ⊄ ACC⁰`‑strength.  So the second
moment is computed in closed form, the disjoint case is Chebyshev‑ready, and the overlap term is the named
frontier.

**Bounded overlap: the variance bound past independence (`…ACCRestrictionSwitchingVariance`, cont.).**  The overlap
covariance is bounded by the intersection size: `cov_le` — `Cov(X_S, X_T) ≤ |S∩T|·p` (factor
`Cov = (1-p)^{|S∪T|}·(1−(1-p)^{|S∩T|})`, first factor `≤ 1`, second `≤ |S∩T|·p` by Bernoulli).  Then double counting
under **bounded overlap** (each coordinate in `≤ d` supports) gives `∑_{j,l}|S_j∩S_l| = ∑_v deg(v)² ≤ d·∑_v deg(v) =
d·∑_j|S_j| ≤ d·k·s` (proved per‑`j`: `∑_l|S_j∩S_l| = ∑_{v∈S_j} deg(v) ≤ d·|S_j| ≤ d·s`, via `card_filter` +
`sum_comm`).  Hence `variance_boundedOverlap_le` — **`Var[X] ≤ d·k·s·p`** when each coordinate lies in `≤ d`
supports (disjoint supports are the case `d = 1`, recovering `k·s·p`).  This is the genuine rung *past
independence*: the Chebyshev‑ready variance bound now holds for bounded‑overlap support families, with the overlap
degree `d` entering linearly.  Unbounded overlap (`d` up to `k`) is where the bound degrades to `k²sp` and the
higher‑moment Håstad argument is genuinely required — the precisely‑located `NP ⊄ ACC⁰` frontier.

**Chebyshev: the variance bound becomes a concentration statement (`…ACCSwitchingChebyshev`).**  The Markov and
Chebyshev inequalities are proved *over the exact `p`‑biased restriction measure* (`weight p L =
p^{|L|}(1-p)^{n-|L|}`, total `1` by `total`/`biased_sum_one`; `Pr`/`Exp` the weight of an event / average of a
function).  Proved: `markov` (`a·Pr(f ≥ a) ≤ Exp f` for `f ≥ 0`), `chebyshev`
(`Pr((g−Eg)² ≥ t²) ≤ Exp((g−Eg)²)/t²`, Markov on the squared deviation), and the packaging
`chebyshev_of_variance_le` (`Exp((g−Eg)² ) ≤ B ⇒ Pr((g−Eg)² ≥ t²) ≤ B/t²`).  Taking `g = X` the surviving‑support
count, `Exp((X−EX)²) = Var[X] ≤ d·k·s·p` (the second‑moment bound) gives **`Pr(|X − E[X]| ≥ t) ≤ d·k·s·p / t²`** —
so a constant‑`p` random restriction leaves few surviving supports *with high probability*, the concentration that
turns first‑moment existence into a robust restriction (for bounded‑overlap supports).  This completes the
switching skeleton's probabilistic packaging: the concentration inequalities are proved in full over the genuine
measure; the only remaining inputs are the (proved) variance bound and its routine identification with
`Exp((X−EX)²)`.  Unbounded overlap remains the higher‑moment frontier.

**The end‑to‑end pipeline, assembled (`…ACCSwitchingPipeline`).**  The whole random‑restriction programme is chained
into one conditional theorem with the single higher‑moment gap isolated.  Proved: `exists_of_pr_lt_one` (the
probabilistic method over the `p`‑biased measure — an event of probability `< 1` has a complementary outcome, from
`total`); `exists_low_survival` (**a low‑survival restriction exists** — by Markov, expected survivors `≤ B < a` ⇒
some restriction leaves `< a` surviving supports); `cellWitness_gives_low_correlation` (**a same‑cell `D`‑witness
defeats the predictor** — `2·agreement ≤ #off‑diagonal`, via `kGate_low_correlation_offdiagonal`).  The assembly
`bounded_overlap_acc0_low_correlation_whp` chains them: from the expected‑survivor bound (`hE`, the first‑moment
value `≤ k·s·p`) a low‑survival restriction exists; the cell bridge (`hbridge`: few survivors ⇒ same‑cell witness —
the second‑moment/pigeonhole collapse, proved for bounded overlap, the higher‑moment frontier for unbounded) turns
it into a `CellWitness`; the witness defeats the bounded‑overlap ACC⁰ predictor.  So **every link of the pipeline is
proved except the single named cell bridge**, and the theorem makes the full
"bounded‑overlap ACC⁰ predictor fails on the holonomy parity under a random restriction" argument explicit, with
the genuine `NP ⊄ ACC⁰` hardness localized to exactly one combinatorial step (unbounded‑overlap cell collapse).

**The cell bridge proved via pigeonhole (`…ACCSwitchingPipeline`, cont.).**  The named gap `hbridge` is now
*discharged* by pigeonhole, with the key observation that a *live* coordinate cannot belong to a *killed* support
(it would witness non‑disjointness), so a live coordinate's cell pattern is determined entirely by the **surviving**
supports — at most `2^{#survivors}` patterns, not `2^k`.  Proved: `exists_sameCell_pair_of_survivors`
(`2^{#survivors} < |L| ⇒` a same‑cell live pair, via `exists_ne_map_eq_of_card_lt_of_maps_to` mapping `v ↦ {j : v∈S_j}`
into the powerset of the surviving set, card `2^{#survivors}`); `exists_cellWitness_of_survivors`
(`⇒ ∃ D, CellWitness`, taking `D` the singleton of one of the pair); and `predictor_fails_of_survivors`
(`2^{#survivors} < |L| ⇒ ∃ D, the k‑gate ACC⁰ predictor cannot correlate with the holonomy parity D`, with **no extra
hypothesis**).  So for a restriction whose surviving‑support count is below `log₂` of its live‑coordinate count, the
predictor *provably* fails — the bridge is closed by pigeonhole.  The probabilistic half (`exists_low_survival`)
delivers few survivors; the dual first moment (`E[#live] = n·p`) keeps the live count large; together they place a
restriction in this regime for bounded overlap.  The genuine remaining hardness is only the joint
**few‑survivors‑and‑many‑live** control at unbounded overlap (where the survivor count can reach `~log₂|L|`) — the
higher‑moment Håstad frontier, now isolated to that single quantitative tension.

**The joint tension closed by a union bound (`…ACCSwitchingPipeline`, cont.).**  The two tails are combined.  Proved:
`Pr_union_le` (union bound over the `p`‑biased measure, `Pr(E₁∨E₂) ≤ Pr E₁ + Pr E₂`, via `sum_filter` +
`split_ifs`); `exists_both_of_pr_add_lt_one` (if the two tail probabilities sum to `< 1`, some restriction avoids
*both* bad events — joint existence from the probabilistic method); and `bounded_overlap_predictor_fails_whp` — **if
the "too many survivors" tail (`≥ a`) and the "too few live" tail (`≤ b`) sum to `< 1` and `2^a ≤ b`, then a random
restriction defeats the bounded‑overlap ACC⁰ predictor** (`∃ D`, no correlation advantage), with *no cell‑bridge
hypothesis*: the joint restriction has `survivors < a` and `live > b`, so `2^{survivors} < 2^a ≤ b < live`, and
`predictor_fails_of_survivors` fires.  The two tails are Markov‑controlled by the first moments (`E[#surviving] ≤
k·s·p`, `E[#live] = n·p`); the feasibility `< 1` *is* the joint few‑survivors‑and‑many‑live condition.  So the
predictor failure is reduced to a single feasibility inequality, attainable for bounded overlap — and the precise
quantitative tension (survivor count reaching `~log₂` of the live count) at unbounded overlap is the residual
higher‑moment Håstad frontier.  The whole pipeline — moments, concentration, pigeonhole cell bridge, union‑bound
joint control — is now machine‑checked end to end, with the genuine `NP ⊄ ACC⁰` hardness isolated to that one
feasibility tension.

**An actual circuit class, with support extraction (`…ACC0CircuitModel`).**  The machinery now speaks about real
circuits, not abstract support families.  Defined: `ACC0Circuit` (the target class — AND/OR/NOT over `MOD q` gates,
with `depth` and `size`); `ModGate`, `Depth2ModCircuit` (the depth‑2 `MOD`‑bottom fragment) and its `supports`
family.  Proved: `eval_factors` — **support extraction**: a depth‑2 `MOD`‑bottom circuit's output factors through
its bottom statistics, `eval x = g (weightVec supports x)` for an explicit `g` (by `rfl`, axioms `[propext,
Quot.sound]` only); and `depth2_circuit_fails_of_survivors` — **the bridge**: such a circuit fails to correlate with
the holonomy parity after any restriction whose surviving‑support count is below `log₂` of its live count
(support extraction `+` the pigeonhole cell bridge).  So the proved lower‑bound machine now attaches to a named
circuit class: *bounded‑overlap depth‑2 `MOD`‑bottom ACC⁰ circuits fail to correlate with holonomy parity after a
suitable random restriction.*  The remaining wall is precisely located: lifting past depth 2 needs the
depth‑reduction switching (random restrictions collapsing AND/OR layers to `MOD`‑bottom survivors), and lifting
past bounded overlap is the higher‑moment Håstad tension — together, **unbounded support reuse / high‑overlap
depth‑reduction**, which is exactly where full ACC⁰ difficulty lives.

### Summary of the restricted ACC⁰ lower‑bound machine

The complete proved arc, attached to a real circuit class, with the honest remaining wall:

| stage | proved content | file |
|---|---|---|
| target | holonomy parity `fParity D` on the hypercube | `…HolonomyBalanceFragments` |
| engine | `MOD q` / many‑gate correlation via count‑preserving involutions | `…ModQGateBalance`, `…TwoGateCorrelation`, `…ManyGateCorrelation` |
| threshold | cells shatter iff `≥ ⌈log₂ n⌉` gates (pigeonhole + converse) | `…ManyGateCorrelation` |
| restriction | `p`‑biased measure; first moment `E≤k·s·p`, second `Var≤d·k·s·p`; Markov/Chebyshev | `…SwitchingProb`, `…SwitchingVariance`, `…SwitchingChebyshev` |
| cell bridge | `2^{survivors} < |L| ⇒` witness (killed supports blind to live coords) | `…ACCSwitchingPipeline` |
| joint control | union bound: few survivors ∧ many live ⇒ predictor fails | `…ACCSwitchingPipeline` |
| circuit class | `ACC0Circuit`; support extraction; depth‑2 `MOD`‑bottom bridge | `…ACC0CircuitModel` |
| depth reduction | locality (any depth); MOD‑support extraction; depth‑2 transfer bridge | `…ACC0DepthReduction` |
| high‑overlap probe | star family: core blowup `Var ≤ ksp + k²·\|core\|·p`; core surgery | `…ACCOverlapStar` |
| spread‑overlap probe | design `Var ≤ ksp + k²λp`; surgery cost `= \|overlapCoords\|`; star ⊆ core | `…ACCOverlapDesign` |
| core decomposition | killing `overlapCoords` ⇒ pairwise‑disjoint restricted family, `Var ≤ ksp` | `…ACCCoreDecomposition` |
| restriction tree | recursive depth `d → 2` descent (one‑step switching named) | `…ACCRestrictionTree` |
| Williams cash‑out | whole arc as one conditional: no small ACC⁰ predictor correlates | `…ACCWilliamsCashout` |
| depth-3 switch step | *real* deterministic switch on CNF-of-`MOD`: forced clause drops | `…ACCDepth3Switch` |
| Williams `NEXP ⊄ ACC⁰` scaffold | algorithmic‑method cash‑out; N‑frame route (speedup bridge named) | `…WilliamsNEXP_ACC0` |
| N‑frame ACC⁰‑SAT kernel | SAT collapses to a `weightVec`‑image (cell) search via support extraction | `…NFrameACC0Speedup` |
| ACC⁰‑SAT time-cost model | cell search `≤ (n+1)^k < 2^n` (small-gate regime): beats brute force | `…ACC0SatTimeCost` |
| branched ACC⁰‑SAT bound | `2^{killed}·(n+1)^r < 2^n` (few-survivor regime): past small-gate | `…ACC0SatBranched` |
| ACC⁰‑SAT time model | `cellSearch` algorithm: decides SAT, `steps` = #cells, `< 2^n` proved | `…ACC0SatMachine` |
| operational step machine | `step`/`runFor` interpreter: decides SAT in `#cells` transitions, `< 2^n` | `…ACC0SatStepMachine` |
| elementary op count | `totalOps = k·#cells ≤ k·(n+1)^k < 2^n`: every gate‑eval counted | `…ACC0SatOpCount` |
| survivor → cell count | `#cells ≤ (n+1)^{#active}`: speedup governed by active (surviving) gates | `…ACC0SatSurvivorCells` |
| speedup capstone | few survivors ⇒ correct, sub‑`2^n` SAT decider (correctness ∧ time) | `…ACC0SatSpeedupCapstone` |
| restriction ⇒ active | `#active(S·∩L) = survivingCount`; `#cells ≤ (n+1)^{surviving}` | `…ACC0SatRestrictionActive` |
| branch correctness | `SAT ↔ ∃ branch, branchSAT` (the branch‑and‑restrict decomposition) | `…ACC0SatBranchCorrect` |
| master bridge | `NFrameGivesACC0SatSpeedup`: whole pipeline, one socket (few survivors) | `…NFrameACC0Master` |
| socket reduction | `socket_of_expectation`: `Exp(survivingCount) ≤ r` + `(n+1)^r < 2^n` ⇒ the socket | `…ACC0SatSocketReduction` |
| first moment / regime | `prDisjoint`: `Pr(S∩L=∅)=(1-p)^{|S|}`; `Exp(survivingCount)=∑survProb≤ksp`; `socket_of_regime`: `ksp ≤ r` + `(n+1)^r < 2^n` ⇒ the socket | `…ACC0SatFirstMoment` |
| second moment / concentration | `Exp((survivingCount−E)²)=variance`; `survivingCount_concentration`: `Pr(\|X−E\|"≥"t)≤dksp/t²`; `most_restrictions_good`: most restrictions within `t` of mean | `…ACC0SatSecondMoment` |
| exponential tail (independent) | factorial moment `Pr(t≤X)≤Exp(C(X,t))=∑_{\|T\|=t}Pr(all T survive)`; kill+survival independence (disjoint); `exp_tail_disjoint`: `Pr(t≤X)≤C(k,t)(sp)^t` | `…ACC0SatExpTail` |
| **clause‑count‑free switching tail** | `pweight_total`: `∑pweight=1`; **`hastad_switching_prob_tail`: `∑_{Bad}pweight ≤ (4pw/(1-p))^s`** (depth‑`≥s` mass, NO clause‑count factor), reconstruction discharged by `reconstructionCorrect_fullpath` | `…Depth3SwitchingProbTail` |
| depth reduction whp | **`depth_collapse_mass_ge`: `{depth<s}` carries mass `≥ 1−(4pw/(1-p))^s`** (complement of the tail) — width‑`w` DNF collapses to depth‑`≤2` whp | `…Depth3SwitchingDepthReduction` |
| cross‑model bridge (NO‑GO) | `modQStatOn_flip_ne`: MOD statistic fully sensitive (`q≥2`); **`mod_gate_parity_nonconstant`**: parity gate non‑constant on any cube with a free support coord ⇒ switching (leaves coords free) CANNOT drive `switch_step` (needs full support) — the Razborov–Smolensky wall | `…ACCSwitchingModBridge` |
| **PIVOT → polynomial method** | road‑map: switching capped, MOD no‑go proved, RS layer already complete; next = effective‑dimension/correlation fusion | `ACC_ROADMAP.md` |
| N‑frame target = parity (RS bridge) | `fParity_univ_eq_parity`: holonomy target = `decide(Odd #ones)`; **`nframe_parity_target_size_lower_bound`**: N‑frame target needs `2^{Ω(n^{1/2d})}` AC⁰[p] size (inherits the proved RS bound) | `…Layer3NFrameParityRS` |
| **socket 1: eff‑dim ↔ holonomy fusion** | top‑frequency functional kills `V_D`, separates parity; **`holonomy_parity_not_lowDegEval`**: holonomy target `∏pmOne` ∉ degree‑`≤D` span for `D<n` (eff‑dim `≥n`). Per‑class engine genuinely does NOT fuse (parity determined, never balanced) | `…Layer3LowDegHolonomy` |
| **socket 2: ACC⁰ ≈ low‑rank predictors** | `eval_mem_lowDegSpan`: degree‑`≤D` poly's cube‑eval ∈ `V_D` (multilinear reduction); **`acc0_approx_by_lowRankPredictor`**: AC⁰[p] circuit (`p^t≥4·#subcirc`) `3/4`‑approximated by a function in `V_D`, `D=((p-1)t)^depth` — same `V_D` socket 1's target escapes | `…Layer3ACC0LowRank` |
| **socket 3: modulus boundary (WALL)** | `fermat_indicator` (`1−a^{p−1}=1[a=0]`); **`modp_eval_mem_lowDegSpan`**: `MOD_p` detector exactly degree‑`(p−1)` over `F_p` (any fan‑in) ⇒ ∈ `V_{p−1}` — WHY `modulus=p` is load‑bearing. `MOD_q` (q≠p) / composite / `NEXP⊄ACC⁰` = algorithmic‑method frontier, NOT polynomial method (delimited, not faked) | `…Layer3ModulusBoundary` |
| **Tier 3 frontier: mixed modulus** | `fp_statistic_eq_count` (F_p stat = count mod p); **`mod6_eq_mod2_and_mod3`** (CRT: MOD_6 = MOD_2 ∧ MOD_3); `mod2/3_detector_lowdeg` (each component low‑degree over its OWN prime field — no common field). `MixedModulusStratifiedObserverSocket` = named OPEN target (hybrid observer; Williams arc, not RS). Consolidated map: `ACC_THEOREM_MAP.md` | `…Layer3MixedModulus` |
| **mixed‑modulus SAT speedup (residue compression)** | `modResVec` + `eval_factors_residue` (MOD‑circuit factors through residue vector ∈ ∏ZMod q_j); `sat_iff_residue_image` (SAT ⇔ residue‑cell search); `residue_cell_count_le` (≤ ∏q_j, const/gate); **`mod_circuit_sat_speedup`** / **`mod6_circuit_residue_speedup`**: `∏q_j<2^n` (resp `6^k<2^n`) ⇒ `<2^n` cells — Williams' compression kernel | `…ACC0ModResidueSpeedup` |
| **operational residue machine** | `residueSearch` (step‑counted SAT algorithm, residue analogue of `cellSearch`); **`residueSearch_decides`** (decides SAT), `residueSearch_steps_le_prod_moduli` (`steps≤∏q_j`), **`residueSearch_beats_bruteforce`** / `…_mod6_…` (`∏q_j<2^n` resp `6^k<2^n` ⇒ `steps<2^n`) | `…ACC0ResidueMachine` |
| **branched residue cost** | `branched_residue_cost_le` (`≤2^killed·∏surviving q_j`); `branched_residue_regime` (`killed+live=n`, `∏q_j<2^live` ⇒ `<2^n`); **`branched_residue_beats_bruteforce`** / `…_mod6_…` — past the `∏q_j<2^n` base regime (per‑branch `∏q_j` vs cell‑search `(n+1)^r`) | `…ACC0BranchedResidue` |
| **restriction ⇒ few surviving residue gates** | `residue_image_card_le_active` (`\|cells\|≤∏_{active}q_j`); `residue_active_after_restriction` (active = surviving = `¬Disjoint`); **`residue_cells_le_surviving_moduli`**: `\|cells(C↾L)\|≤∏_{surviving}q_j` — discharges step 2's per‑branch bound | `…ACC0ResidueRestriction` |
| **depth iteration (socket)** | `MixedModResidueSearchable` / `MixedACCDepthReductionSocket` (Yao–Beigel–Tarui depth‑2 normal form, OPEN); `residueSearchable_base` (nonvacuous); **`acc0_depth_reduction_speedup`**: socket ⇒ residue search decides `Satisfiable(eval C)` in `<2^n` (switching CAN'T supply the collapse — MOD no‑go) | `…ACC0ResidueDepthReduction` |
| **Williams cash‑out interface** | `mixedACC_speedup_of_depthReduction` (PROVED link: depth socket ⇒ residue speedup ∀ACC⁰); **`residue_cashout_bundled`** (headline: depth socket + `UniformWilliamsRealizationSocket` ⇒ `NEXP⊄ACC⁰`); `williams_socket_iff_separation` / `realization_socket_iff_separation` (SELF‑AUDIT: each cash‑out socket ⟺ the separation — reduces nothing; abstract `Prop` params, NOT a proof) | `…ACC0WilliamsCashout` |
| **residue‑observer algebra** | `ObservedBy`; `observed_sat_iff`/`observed_cellCount_le`; `.comp`/`.and`/`.or`/`observer_sat_branch`; **`observed_top_pi`** (composition law: top over observed ⇒ product observer) + `observed_pi_cellCount_le` (`≤∏card`); `modGate_observedBy`/`depth2_observedBy` (residue speedup re‑derived) | `…ACC0ResidueObserver` |
| **toy Beigel–Tarui** | `boundedGate_observedBy` (bounded‑support gate observed by projection, `card 2^{\|T\|}`); **`toy_bounded_bottom_searchable`**: arbitrary top over fan‑in‑`≤w` bottom gates is searchable in `<2^n` when `∏2^{\|T_i\|}<2^n` (the SYM‑of‑AND_w form — depth‑reduction socket discharged for the bounded‑bottom fragment) | `…ACC0BeigelTaruiToy` |
| **depth‑3 MIXED fragment** | **`depth_mixed_searchable`**: arbitrary top over a bottom MIXING `MOD_q` (`card q`) and bounded‑`AND_w` (`card 2^{\|T\|}`) gates — observer types vary per gate — is searchable in `<2^n` when `∏(per‑gate states)<2^n`; `modGate_card_le`/`boundedGate_card_le` (the two qualifying gate observers) | `…ACC0Depth3Mixed` |
| **support normal form (ACC⁰ syntax ⇒ observer)** | `support`/`eval_depends_on_support` (value depends only on read‑vars, induction on circuit); **`acc0_observed_by_projection`**: ANY `ACC0Circuit` (any depth) observed by projection to its support; `acc0_junta_searchable` (junta ⇒ searchable `<2^n`); **`acc0_top_over_subcircuits_searchable`** (top over ACC⁰ subcircuits jointly reading `<n` vars ⇒ searchable, via union support) — chips `ACC⁰⊆mixed‑bottom` at syntax level | `…ACC0SupportNormalForm` |
| **depth‑3 MOD normalization (residue gain)** | `mod_bottom_circuit_searchable` (arbitrary top over MOD family ⇒ residue‑searchable `<2^n` when `∏q_i<2^n`, ANY supports); **`and_of_mods_searchable`**: syntactic `MOD_{q₁}∧MOD_{q₂}` normalizes to a `Depth2ModCircuit`, residue‑searchable when `q₁·q₂<2^n` even with `S₁=S₂=univ` (where junta gives `2^n`) | `…ACC0Depth3ModNormalize` |
| **AC⁰‑over‑MOD normalization (general)** | `ModComb` (boolean comb over MOD leaves); `combEval_eq_combTop` (value factors through MOD outputs, induction); **`modComb_normalizes`**: any AND/OR/NOT comb of a MOD family `= ⟨gates, combTop C⟩.eval` (a `Depth2ModCircuit`); `modComb_searchable` (residue‑searchable `<2^n` when `∏q_i<2^n`) | `…ACC0ModCombNormalize` |
| **observer extracted from raw `ACC0Circuit` syntax** | `stateBound`/`ModsPos` (occurrence product, positive‑modulus predicate); **`acc0_residueObserved`**: by induction on the circuit, ANY `ACC0Circuit` (positive MOD moduli) is `ObservedBy` a product statistic of state count `≤ stateBound C` — `MOD` leaf→`modGate_observedBy` (`ZMod q`, `card q`), `var`→coordinate projection (`Bool`, `card 2`), gates→`.comp`/`.and`/`.or`; **`acc0_modcircuit_searchable`** (`stateBound C<2^n` ⇒ SAT‑searchable `<2^n` cells). The MOD‑bottom is now *derived* from syntax, not assumed (subsumes `modComb`/`and_of_mods`, adds `var` leaves). Honest gap unchanged: `stateBound` is the *occurrence* product, so the gain is for few‑leaf circuits; a *small* bound for arbitrary ACC⁰ is full Yao–Beigel–Tarui (open) | `…ACC0ExtractObserver` |
| **deduplicated state bound (collapses the occurrence overcount)** | `modOcc` (`MOD`‑occurrence product; `var`/`const` cost `1` — vars read *inside* a `MOD` gate absorbed into its residue) + `varSupp` (deduplicated *set* of vars read by `var`‑leaves); **`acc0_dedupObserved`**: by induction, `eval C` observed by `(mstat, ONE projection to `varSupp C`)` with the residue part of card `≤ modOcc C` — the `∧`/`∨` arms **share a single** projection onto `varSupp a ∪ varSupp b` (each arm recovered by restriction along `vsₐ ⊆ vsₐ∪vs_b`), so the bound carries `2^\|vsₐ∪vs_b\|` not `2^(\|vsₐ\|+\|vs_b\|)`; **`acc0_dedup_searchable`** (`modOcc C·2^\|varSupp C\| < 2^n` ⇒ SAT‑searchable `<2^n`). Strict tightening of `stateBound` (vars counted once, `MOD`‑internal vars free); fires where neither pure‑support (`2^\|support\|`) nor pure‑occurrence reaches. Honest gap unchanged: `varSupp C = univ` ⇒ `2^n`; small bound for arbitrary ACC⁰ = full Yao–Beigel–Tarui (open) | `…ACC0DedupShrink` |
| **syntactic restriction + boundary shrinkage** | `subst1 C i v` (substitute `v` for var `i`; on `MOD`: `mod q S t ↦ mod q (S.erase i)(t-[i∈S]·v)` — fixed coordinate dropped, target shifted by its residue contribution); `weightOn_update` (support count splits across the fixed coord); **`eval_subst1`**: `eval (subst1 C i v) x = eval C (Function.update x i v)` (induction — syntactic restriction = semantic); `varSupp_subst1_subset` (`⊆ (varSupp C).erase i`), `modOcc_subst1_le`, `ModsPos_subst1` (boundary monotone, moduli preserved); **`varSupp_subst1_card_lt`** (`i ∈ varSupp C` ⇒ `\|varSupp(subst1 C i v)\| < \|varSupp C\|` — restricting a read var strictly shrinks the dedup boundary); **`sat_branch_subst1`** (`Satisfiable(eval C) ↔ Satisfiable(eval(subst1 C i false)) ∨ Satisfiable(eval(subst1 C i true))`). The restriction algebra for shrinkage. Honest: NOT a switching lemma — one branch halves each boundary (`2·modOcc·2^{\|vs\|-1}=modOcc·2^{\|vs\|}`), no asymptotic gain alone; the random‑restriction collapse is the deep open content | `…ACC0RestrictShrink` |
| **random‑restriction collapse of the dedup boundary** | `restrList` (multi‑var restriction = iterated `subst1`) + `applyRestr`; **`eval_restrList`** (`= eval C (applyRestr l x)`, induction — genuine restriction); `varSupp_restrList` (`⊆ varSupp C \ fixed`), `modOcc_restrList_le`, `ModsPos_restrList` (lift from single‑var lemmas, no `MOD`‑shift re‑proof); `fixedListOf Lᶜ β` + `varSupp_restr_compl_subset` (fixing `L`'s complement ⇒ `varSupp ⊆ varSupp C ∩ L`, only live vars survive); `exp_coord_eq` (single coord live w.p. `p`, via `exp_indicator_eq_survProb` on `{i}`); **`exp_liveBoundary_eq`**: `Exp p (\|varSupp C ∩ L\|) = p·\|varSupp C\|` (proportional collapse in expectation, `exp_sum`+linearity over the existing `p`‑biased measure); `exists_small_liveBoundary` (Markov + `exists_of_pr_lt_one`); **`dedup_restriction_collapses`**: ∃ live set `L` s.t. *for every* fixed‑assignment `β`, the restricted circuit's dedup boundary is `< a` (just above `p·\|varSupp C\|`). Honest: *proportional* (first‑moment) collapse, NOT switching — no SAT speedup alone (`2^{#fixed}·2^{p\|vs\|}=2^{\|vs\|}`); *super*‑proportional collapse (function depends on fewer vars = Håstad switching whp) is the deep open content | `…ACC0RandomRestrictShrink` |
| **function‑level collapse: decision‑tree observer** | **`dt_observed`**: `BoolDecisionTree.eval T` is `ObservedBy` a statistic of cell‑count `≤ 2^{T.depth}` (induction; the query node combines subtrees by a **sum** type `S_low ⊕ S_high` = additive leaf count, which is why the bound is `2^{depth}` not `2^{\#queried vars}`) — the boundary is the *function's leaf*, INDEPENDENT of how many vars it syntactically reads; **`dt_searchable`** (`2^{depth}<2^n` ⇒ SAT‑searchable `<2^n`); **`function_collapse_of_dt`** (any function *computed by* a shallow DT is SAT‑searchable below brute force, via `Computes`). The cash‑out converting decision‑tree depth — the complexity measure switching reduces — into the observer boundary. AC⁰ layer only (switching provably can't collapse `MOD` — the MOD no‑go) | `…ACC0DecisionTreeObserver` |
| **`DTree → BoolDecisionTree` bridge: switching's canonical tree, observed** | **`toBoolDT`** (constructor‑for‑constructor map `leaf/node ↦ leaf/query`); **`toBoolDT_eval`** (`[propext]` only) + **`toBoolDT_depth`** (semantics & depth preserved, one‑line inductions — the two datatypes are isomorphic, same branch convention & depth recurrence); `dtree_observed`/`dtree_searchable` (the `dt_observed`/`dt_searchable` cash‑out transferred to `Depth3.DTree`); **`canonicalDTree_observed`/`canonicalDTree_searchable`**: the switching arc's `canonicalDTree` (width‑`w` DNF, fuel `F`, depth `≤ F·w` by `canonicalDTree_depth_le`) has a `≤ 2^{F·w}`‑cell observer boundary, SAT‑searchable `<2^n` when `2^{F·w}<2^n`. The representation step previously flagged separate is now DONE — the function‑level collapse composes end‑to‑end with switching's canonical tree. Caveats: `DTree.eval(canonicalDTree …)=restricted DNF` is cited canonical‑tree soundness (not re‑proved); MOD layer excluded (MOD no‑go) | `…ACC0DTreeBridge` |
| **AC⁰ + MOD composition across the no‑go** | **`dt_oracle_observed`**: a depth‑`d` decision tree over Boolean *oracle*‑subfunctions `h : Fin m → (Fin n→Bool)→Bool` (each query branches on `h i x`, not a raw bit) computes a function `ObservedBy` a `≤ 2^d` boundary (same sum‑typed leaf observer as `dt_observed`); **`acc0_over_mod_observed`**: instantiating `h j = (gⱼ).eval` for `MOD` gates, `x ↦ T(g₁(x),…,g_m(x))` (`T` a depth‑`d` DT top) is `ObservedBy` a `≤ 2^d` boundary — the `MOD` gates are QUERIED as oracles along each path, **never collapsed** (respecting the no‑go), and the boundary is independent of the gates' moduli and of how many variables they read; **`acc0_over_mod_searchable`** (`2^d<2^n` ⇒ SAT‑searchable `<2^n`). Honest: this is the *deterministic* composition law — *given* a shallow `T` over the gate outputs, boundary `2^d`. Supplying such a `T` is the OPEN part: gate outputs `gⱼ(x)` are determined by `x` (not independent random bits), so the switching random‑restriction does **not** collapse the top over them — the no‑go's deeper form. Composes a *single* AC⁰‑over‑MOD level; iterating switching through a `MOD` layer stays blocked (the genuine open ACC⁰ frontier) | `…ACC0ModComposition` |
| **oracle‑control syntax → decision‑tree observer** | **`OracleControl`** (Boolean control circuit `leaf j`/`const`/`¬`/`∧`/`∨` over `m` oracle leaves) + `controlEval`; **`treeOf`/`treeOf_eval`/`treeOf_depth_le`** (complete query tree: `eval = f` on the path‑determined assignment; depth `≤ |L|`) + **`fullTree`** (query all `m` positions: ANY `f : (Fin m→Bool)→Bool` reduces to a depth‑`≤m` DT computing it); **`control_to_decision_tree`** (every control reduces to a depth‑`≤m` `BoolDecisionTree`); **`oracle_control_dt_searchable`** (control computed by a depth‑`d` DT ⇒ `x ↦ controlEval C (fun j => (gⱼ).eval x)` SAT‑searchable `<2^n` when `2^d<2^n`); **`oracle_control_over_mod_searchable`** (unconditional fragment speedup: an `AC⁰` control over `m` `MOD` oracles is SAT‑searchable below brute force whenever `2^m<2^n` — `<≈n` `MOD` gates — regardless of control structure). Honest: deterministic control→DT reduction (depth `m` in general) | `…ACC0OracleControl` |
| **step 3 (DNF fragment): random restriction makes the control shallow** | **`dnf_control_switching_searchable`**: for a DNF control `D` over `k` oracle positions (per‑term width `≤w`, `≤M` clauses, switching union‑bound `(2p/(1-p))(2wM)<1` and `r^s/(1-r)<1`) and `MOD` gates `gate : Fin k → ModGate n` with `s≤n`, there **exists a position‑restriction `ρ`** under which `(canonicalDT D F ρ)` is shallow (depth `<s`) and `x ↦ (canonicalDT D F ρ).eval (fun j => (gateⱼ).eval x)` is SAT‑searchable in `<2^n`. The collapse is the **proved fully‑discharged** switching lemma `Depth3.exists_shallow_all_tight_uncond` (NO reconstruction socket — only width/clause‑count + the union‑bound smallness condition, instantiated at `G={D}`); `canonicalDT` returns a `BoolDecisionTree` directly, so it composes with `acc0_over_mod_searchable` with no extra bridge (`2^{depth}<2^s≤2^n`). HONEST: `ρ : Restriction k = Fin k → Option Bool` restricts the oracle **positions**, not the `n` input vars `x` — the composed function plugs the gate outputs into the *position‑restricted* tree, which is **not** the original circuit restricted by an `x`‑restriction (gate outputs determined‑not‑independent = the no‑go's deeper form). So this is the control‑position‑level switching collapse composed with the oracles — a genuine fragment result — NOT an `x`‑level circuit speedup. The position/`x` bridge is the open frontier, not faked; shallow tree computes the `ρ`‑restricted DNF by cited `canonicalDT` soundness | `…ACC0DNFControlSwitching` |
| **position → `x` bridge: realizing oracle restrictions by input restrictions** | `Extends x σ` + **`RealizableByInputRestriction ρ gate`** (∃ input restriction `σ` s.t. every completion forces each fixed gate `j` (`ρ j = some b`) to output `b` — forcing a MOD gate needs its whole support fixed, per the no‑go); `modGate_eval_congr` (MOD value depends only on its support, via `weightOn` sum‑congr); **`realizable_of_disjoint`** (POSITIVE fragment: pairwise‑disjoint supports + per‑gate achievability `∀ j b, ρ j = some b → ∃ a, (gateⱼ).eval a = b` ⇒ realizable — `σ` fixes coord `i` to the witness of the unique fixed gate whose support contains `i`; disjointness ⇒ well‑defined & `Classical.choose` lands on the right gate); **`position_restriction_not_always_realizable`** (THE NO‑GO: `![⟨2,{0},0⟩,⟨2,{0},1⟩]`, `ρ ≡ some true` — both gates forced `true` would give `modQStatOn{0}2 x = 0` and `= 1`, so `0=1` in `ZMod 2`, impossible). **So the position→`x` bridge is TRUE on the disjoint fragment and FALSE in general** — exactly why it must stay fragment‑restricted (a socket for arbitrary circuits), not a free theorem. Honest: solves the *forcing* half (fixing the determined gates); the full switching composition also needs the *free* gates to stay free under `σ` (the disjoint case supports it; not assembled here) | `…ACC0OracleRestrictionRealization` |
| **disjoint-fragment `x`-level speedup (exact reduction)** | **`gate_vector_realizable`**: disjoint supports + each gate both-achievable (`∀ j b, ∃ a, (gateⱼ).eval a = b`) ⇒ the gate-output vector is SURJECTIVE onto `{0,1}^k` (`∀ y : Fin k → Bool, ∃ x, ∀ j, (gateⱼ).eval x = y j` — via `realizable_of_disjoint` instantiated at `ρ = some ∘ y`, witness `x = fun i => (σ i).getD false`); **`disjoint_fragment_sat_iff`**: the EXACT reduction `Satisfiable (x ↦ controlEval C (fun j => (gateⱼ).eval x)) ↔ Satisfiable (controlEval C)` (the `n`-variable circuit is satisfiable iff the `k`-variable control is — `→` takes `y := g(x)`, `←` realizes any satisfying `y` by surjectivity); **`disjoint_fragment_speedup`**: `f`-SAT over `Fin n` ↔ search over `Finset.univ (Fin k → Bool)`, whose card `2^k < 2^n` (`Fintype.card_fun`+`card_bool`+`card_fin`). The `n`-variable circuit collapses EXACTLY to the `k`-variable control — a genuine `x`-level restricted speedup. Disjointness is **load-bearing**: the no-go (`position_restriction_not_always_realizable`) breaks surjectivity for overlapping gates, so the reduction provably fails there — which is why the fragment restriction is essential, not cosmetic. The disjoint case is the *worst* case for the output-cell count (image is the full `2^k`), so `2^k` is tight | `…ACC0DisjointFragmentSpeedup` |
| **parity (MOD₂) constraint realization: realizable ⟺ F₂ system consistent** | **`realizable_iff_achievable`** (arbitrary MOD, NO disjointness: `RealizableByInputRestriction ρ gate ↔ ∃ x, ∀ j b, ρ j = some b → (gateⱼ).eval x = b` — realizability is *exactly* existence of an achieving input; disjointness was only used to *construct* it, the no-go is when none exists; proof: `→` via `x = fun i => (σ i).getD false`, `←` builds `σ` fixing the fixed gates' supports to `x`, forcing via `modGate_eval_congr`); `parityGate := ⟨2,S,t⟩`, `zmod2_ne_iff` (`¬s=t ↔ s=t+1` over `ZMod 2`, `revert;decide`), **`parity_force_linear`** (`(parityGate S t).eval x = b ↔ modQStatOn S 2 x = (if b then t else t+1)` — forcing a parity gate IS the F₂ equation); **`parity_realizable_iff_consistent`** (realizable ⟺ `∃ x, ∀ j b, ρ j = some b → modQStatOn (Sⱼ) 2 x = (if b then tⱼ else tⱼ+1)` — the F₂ linear system has a Boolean solution); **`control_sat_iff_reachable_image`** (arbitrary MOD: `Satisfiable (x ↦ controlEval C (g x)) ↔ ∃ y ∈ univ.image (gate-vector), controlEval C y = true` — SAT searches the *reachable* gate-output image, the genuine observer boundary, not pretended-independent `2^k`); `parityVector`, `modQStatOn_two_eq_sum`, **`parityVector_xor`** (`parityVector` of bitwise xor = `ZMod 2` sum — parity is additive), `parity_reachable_zero_mem`+**`parity_reachable_add_mem`** (the reachable parity image is an F₂-SUBSPACE: closed under `0` and `+`). So `\|reachable image\| = 2^rank` — `2^k` only in the disjoint/full-rank case; this REPLACES "disjoint-only" with linear-algebraic compatibility. Honest: the structural (subspace) half of `2^rank`; the exact `finrank`-card closed form is in `…ACC0ParityRankCardinality`; general `MOD_q` is a harder 0/1-feasibility problem | `…ACC0ParityConstraintRealization` |
| **exact `2^rank` cardinality of the reachable parity image** | **`parityLinMap S`** (the F₂-`LinearMap` `(Fin n→ZMod 2) →ₗ[ZMod 2] (Fin k→ZMod 2)`, `v ↦ fun j => ∑_{i∈Sⱼ} vᵢ`, with `map_add'`/`map_smul'` via `Finset.sum_add_distrib`/`Finset.mul_sum`); `parityVector_eq_linMap` (`parityVector S x = parityLinMap S (boolEmbed x)`); `zmod2_ite` (`if decide(z=1) then 1 else 0 = z` over `ZMod 2`, `revert;decide`); **`range_parityVector`** (`Set.range (parityVector S) = ↑(LinearMap.range (parityLinMap S))` — the Boolean-input embedding is surjective onto `Fin n → ZMod 2`); **`parity_subspace_card`** (`Fintype.card ↥(range parityLinMap) = 2 ^ Module.finrank (ZMod 2) (range parityLinMap)` via `Module.card_eq_pow_finrank` + `ZMod.card`); **`parity_reachable_card`**: `Nat.card (Set.range (parityVector S)) = 2 ^ Module.finrank (ZMod 2) (LinearMap.range (parityLinMap S))` (`↥(↑M)→↥M` via `SetLike.coe_sort_coe`). The parity-gate observer boundary is **exactly `2^rank`** — `2^k` iff the parity gates are independent (full rank), strictly smaller under linear dependence. Quantitative closure of the reachable-image story for `MOD₂`; general `MOD_q` is a `0/1`-feasibility problem over `ZMod q` (not free linear algebra) | `…ACC0ParityRankCardinality` |
| **rank-based `x`-level speedup (parity)** | **`image_parityVector_card`** (`(Finset.univ.image (parityVector S)).card = 2^rank`, via `Nat.card_eq_fintype_card`+`Set.toFinset_card`+`Set.toFinset_range`+`parity_reachable_card`); **`parity_rank_speedup`**: for a parity-gate control `C` over `(parityGate (Sⱼ) (tⱼ))`, `f`-SAT over `Fin n` ⟺ search the reachable gate-output image (the gate vector `= (fun y => fun j => decide (y j = tⱼ)) ∘ parityVector S` — a relabeling of `parityVector`), and that image's card is `< 2^n` once `2^rank < 2^n` (via `Finset.image_image`+`Finset.card_image_le`+`image_parityVector_card`). Strictly stronger than disjoint `2^k`: `rank ≤ k` with strict inequality under any linear dependence among the parity gates — a parity control whose gates span an `<n`-dimensional F₂ row space is sub-`2^n` searchable EVEN with overlapping supports. The rank analogue of `disjoint_fragment_speedup`, now with the F₂ rank in place of `k` | `…ACC0ParityRankSpeedup` |
| **general `MOD_q` realization = 0/1-feasibility (no rank for `q>2`)** | **`modGate_eval_true_iff`** (`G.eval x = true ↔ modQStatOn G.support G.modulus x = G.target`, via `decide_eq_true_eq`) / **`modGate_eval_false_iff`** (`= false ↔ count ≠ target`); **`modq_realizable_iff_feasible`** (`RealizableByInputRestriction ρ gate ↔ ∃ x : Fin n→Bool, ∀ j, (ρ j = some true → count_{Sⱼ}(x) = tⱼ) ∧ (ρ j = some false → count_{Sⱼ}(x) ≠ tⱼ)` over `ZMod qⱼ` — the general-`q` analogue of `parity_realizable_iff_consistent`, now an equality/disequality **0/1-integer-feasibility** system, NOT free linear algebra; `[propext, Quot.sound]`); `modQStatOn_singleton` (`modQStatOn {i} q x = if x i then 1 else 0`); **`modq_residue_image_not_subspace`** (THE OBSTRUCTION, proved: for a single `MOD₃` gate the reachable residue set is `{0,1}⊆ZMod 3` — `1` reachable, `1+1=2` NOT — so it is *not closed under +*, hence the `2^rank` F-subspace structure is genuinely **MOD₂-specific**). Honest: the `x`-level speedup is `q`-independent (`≤2^k` Boolean gate-output patterns, already `oracle_control_over_mod_searchable` for arbitrary moduli); what `MOD_q` changes is *which* of the `2^k` patterns are reachable — the 0/1-feasibility region, with **no closed-form cardinality / lattice shortcut for `q≥3`** (the honest barrier beyond parity) | `…ACC0ModQFeasibility` |
| **mixed moduli `MOD₆ = MOD₂ ∧ MOD₃` realization via CRT** | **`mod6_eval_true_iff`** (the CRT split: `(⟨6,S,t⟩ : ModGate n).eval x = true ↔ (weightOn S x ≡ t.val [MOD 2]) ∧ (weightOn S x ≡ t.val [MOD 3])` — proved via `modGate_eval_true_iff` → `ZMod.natCast_rightInverse`+`ZMod.natCast_eq_natCast_iff` to pass to `Nat.ModEq`, then `Nat.modEq_and_modEq_iff_modEq_mul` with `Nat.Coprime 2 3`); **`mod6_realizable_iff_mixed`** (a forced-true `MOD₆` family `(fun _ => some true)` is realizable ⟺ `∃ x : Fin n→Bool, ∀ j, (weightOn (Sⱼ) x ≡ (tⱼ).val [MOD 2]) ∧ (weightOn (Sⱼ) x ≡ (tⱼ).val [MOD 3])` — the simultaneous solvability of the mod-2 **and** mod-3 count systems). Combines the F₂-linear/rank story (mod-2 part) with the F₃-feasibility story (mod-3 part), **coupled through the shared `x`** — neither the F₂ rank nor the F₃ feasibility alone suffices; the realizability is the intersection of an F₂-affine condition and an F₃-feasibility region, with no product/closed-form shortcut. Honest: forced-true case (cleanest); a general `ρ` (mixing forced true/false) makes each gate's condition a conjunction/disjunction of the two CRT parts | `…ACC0Mod6Mixed` |
| **mixed moduli `MOD₆`, general `ρ` (forced true ∧, forced false ∨)** | **`mod6_eval_false_iff`** (`(⟨6,S,t⟩ : ModGate n).eval x = false ↔ ¬(weightOn S x ≡ t.val [MOD 2]) ∨ ¬(weightOn S x ≡ t.val [MOD 3])` — forcing false negates the CRT conjunction into a DISJUNCTION; proved `rw [Bool.eq_false_iff, ne_eq, mod6_eval_true_iff, not_and_or]`); **`mod6_realizable_iff_general`** (arbitrary `ρ : Fin k → Option Bool`: `RealizableByInputRestriction ρ (fun j => ⟨6, Sⱼ, tⱼ⟩) ↔ ∃ x, ∀ j, (ρ j = some true → (count ≡ tⱼ.val [MOD 2]) ∧ (≡ [MOD 3])) ∧ (ρ j = some false → ¬(count ≡ tⱼ.val [MOD 2]) ∨ ¬(≡ [MOD 3]))` — the fully-general mixed-modulus condition). Forced-true gates give a *conjunction* of CRT congruences, forced-false gates a *disjunction* of CRT disequalities; all coupled through the shared `x` — a genuine mixed CSP over the Boolean cube (linear mod-2 ∧ feasibility mod-3 atoms), no rank/product shortcut. Completes the MOD-realization arc for all `ρ` | `…ACC0Mod6MixedGeneral` |
| **YBT `SYM`-top count observer (the tractable heart of `SYM∘AND`)** | `gateCount g x := ∑ⱼ (if gⱼ x then 1 else 0)` (count of accepting sub-gates); `symEval g h x := h (gateCount g x)` (a symmetric = count-function of `m` sub-gates); `gateCount_le` (`≤ m`, via `Finset.sum_le_sum`); **`sym_observed`** (`symEval g h` is `ObservedBy (gateCount g)`, decoder `h`); **`sym_count_card_le`** (`(univ.image (gateCount g)).card ≤ m+1`, image `⊆ range(m+1)` — **the symmetric top collapses the boundary from `2^m` to `m+1`**); **`sym_searchable`** (`SYM∘(m gates)` SAT-searchable in `< 2^n` once `m+1 < 2^n`; at the YBT normal form `m = 2^{polylog n}` ⇒ `< 2^n`). Proves the structural reason the YBT `SYM` output gate is tractable — it sees only the *count* of accepting sub-gates — for **arbitrary** sub-gates (the YBT-specific content only fixes `m`). Honest: the YBT **reduction** (arbitrary `ACC⁰` → `SYM∘AND` with `m` quasipolynomial) is the deep structural theorem and is **NOT** formalized — the open `ACC⁰` wall, socketed as `MixedACCDepthReductionSocket`; a small cell count is not a uniform algorithm | `…ACC0SymmetricObserver` |
| **low-degree polynomial → `SYM∘AND` (YBT chip)** | `monoAND S x := decide (∀ i ∈ S, x i = true)` (monomial `AND` gate, fan-in `\|S\|`); `lowDegSubsets n D := (range (D+1)).biUnion (fun i => univ.powersetCard i)` + **`lowDegSubsets_card`** (`= ∑_{i ∈ range (D+1)} n.choose i`, via `Finset.card_biUnion` with `PairwiseDisjoint` powersetCards + `Finset.card_powersetCard`); **`monomial_count_le`** (`mono : Fin m → Finset (Fin n)` injective with `(mono j).card ≤ D` ⇒ `m ≤ ∑_{i≤D} C(n,i)` — the bottom-gate-count bound, via `card_image_of_injective` + `card_le_card` into `lowDegSubsets`); **`lowDegreePoly_searchable`** (a `SYM` gate over `m` monomial-`AND`s `symEval (fun j x => monoAND (mono j) x) h` is SAT-searchable in `< 2^n` once `m+1 < 2^n` — the polynomial value `∑_S monoAND_S` is `gateCount` over its monomial-`AND`s, so `sym_searchable` applies directly). The polynomial→`SYM∘AND` cash-out for a `0/1`-coefficient multilinear polynomial (a sum of distinct monomials); general `ℕ`/`F_p` coefficients duplicate each monomial gate (scaling the count by the coefficient bound). Honest: the hard direction — that an arbitrary `ACC⁰` circuit *is* such a low-degree polynomial (the Razborov–Smolensky approximation + composition) — is **NOT** proved; it is the open structural wall (`MixedACCDepthReductionSocket`). YBT pipeline: `ACC⁰ ─[RS approx: OPEN]→ low-deg poly ─[PROVED]→ SYM∘AND ─[PROVED]→ count observer ─[PROVED]→ searchable` | `…ACC0PolyToSymAnd` |
| **RS low-degree span → `SYM∘AND` bottom layer (the RS bridge)** | **`squarefreeEvalMonomial_eq_monoAND`** (`squarefreeEvalMonomial p S x = if monoAND S x then (1 : ZMod p) else 0` — the RS generator `∏_{i∈S} boolToZMod p (xᵢ)` is `1` iff every bit in `S` is set, i.e. **the low-degree-span generators ARE the monomial-`AND` gates**; proved by `by_cases (∀ i∈S, x i = true)`, `Finset.prod_eq_one`/`Finset.prod_eq_zero`); **`lowDegPolyEval_mem_monoAND_span`** (lifts Layer3's `eval_mem_lowDegSpan` through the bridge — a degree-`≤D` `MvPolynomial`'s Boolean‑cube eval lies in the `ZMod p`-`Submodule.span` of the monomial-`AND` indicators of degree `≤ D`, the `SYM∘AND` bottom layer, via `funext` + `rw [hgen]`). Connects the existing RS machinery (`…Layer3DimensionCount`) to the `SYM∘AND` world exactly. Honest gaps (both flagged): (1) RS is **approximate** (`acc0_approx_by_lowRankPredictor` agrees on `1-ε`), so this is an *approximate* `SYM∘AND`, not the exact YBT form; (2) the `ZMod p`-linear top → count-mod-`p` `SYM` gate needs (weighted) gate duplication; the exact `AC⁰[p] → SYM∘AND` across depth is the open structural wall (`MixedACCDepthReductionSocket`) | `…ACC0RSToSymAnd` |
| **weighted-count `SYM` observer (closes the `ZMod p`-linear → count-mod-`p` gap)** | `weightedGateCount c g x := ∑ⱼ cⱼ * (if gⱼ x then 1 else 0)` (the `ℕ`-coefficient polynomial value = count over the multiset with gate `j` duplicated `cⱼ` times) + `weightedSymEval c g h x := h (weightedGateCount c g x)`; `weightedGateCount_le` (`≤ ∑ⱼ cⱼ`, via `Finset.sum_le_sum`, `split <;> simp`); **`weightedSym_observed`** (`ObservedBy` the weighted count, `⟨h, fun _ => rfl⟩`); **`weightedSym_count_card_le`** (`(univ.image (weightedGateCount c g)).card ≤ (∑ⱼ cⱼ)+1`, image `⊆ range(∑cⱼ+1)` — **boundary is the total weight `+1`, not `2^m`**); **`weightedSym_searchable`** (SAT-searchable `< 2^n` once `∑ⱼ cⱼ + 1 < 2^n`); **`weightedGateCount_cast_eq`** (over `ZMod p` with `cⱼ = (coeffⱼ).val`, `((weightedGateCount … : ℕ) : ZMod p) = ∑ⱼ coeffⱼ * (if gⱼ x then (1:ZMod p) else 0)` — the **coefficient-duplication identity**: the weighted count's residue is the `ZMod p` polynomial value, via `Nat.cast_sum`/`Nat.cast_mul`/`ZMod.natCast_rightInverse`). Closes the gap exactly: a `ZMod p`-linear combination of monomial-`AND`s IS a weighted-count `SYM∘AND` (the count-mod-`p` top over the duplicated family, `≤ (p-1)·∑_{i≤D}C(n,i)` gates). Honest: RS is still approximate; the exact `AC⁰[p]→SYM∘AND` across depth is still the wall | `…ACC0WeightedSymAnd` |
| **approximate vs exact (what RS approximation buys)** | `disagreeSet f g := Finset.univ.filter (fun x => f x ≠ g x)`; **`sat_card_le_of_disagree`** (`(univ.filter (f ·=true)).card ≤ (univ.filter (g ·=true)).card + (disagreeSet f g).card` — `{f=true} ⊆ {g=true} ∪ disagree`, via `card_le_card`+`card_union_le`; an approximating `g` bounds `f`'s **solution count**, not its satisfiability); **`exact_sat_iff`** (`disagreeSet f g = ∅ ⇒ (Satisfiable f ↔ Satisfiable g)` — only **exact** agreement transfers SAT). Pins the approximate/exact boundary: the searchable cash-out needs the **exact** `SYM∘AND` (the RS approximation gives only a sparsity/correlation bound; the correlation side is where RS lower bounds live, the exact side is the wall) | `…ACC0ApproxConsequence` |
| **YBT exact normal-form socket (structural wall + conditional cash-out)** | **`HasExactSymAndForm C := ∃ m (mono : Fin m → Finset (Fin n)) (h : ℕ → Bool), eval C = symEval (fun j x => monoAND (mono j) x) h ∧ m+1 < 2^n`** (the exact YBT `SYM∘AND` normal form as a named socket); **`ybt_socket_searchable`** (`HasExactSymAndForm C ⇒ Satisfiable (eval C)` decided by a `< 2^n`-cell image search over `gateCount`, via `rw [heq]` + `lowDegreePoly_searchable`). The socket holding for **arbitrary** `ACC⁰` `C` (with `m` quasipolynomial) **is** the Yao–Beigel–Tarui theorem — a known classical result, **NOT** formalized (the open structural wall, cf. `MixedACCDepthReductionSocket`). The cash-out needs the **exact** form (an approximate one only bounds the solution count, per `…ACC0ApproxConsequence`); a small cell count is not a uniform algorithm | `…ACC0YBTSocket` |
| **YBT reduction core: `AC⁰[p]` circuit → `SYM∘AND`-bottom span (conditional on degree)** | **`acc0p_circuit_in_monoAND_span`**: for `c : BoolCircuitSyntax n` with `BoolCircuitSyntax.IsAC0Syntax c` and `(toPoly p c).totalDegree ≤ D`, the embedded Boolean function `(fun x => boolToZMod p (c.eval x)) ∈ Submodule.span (ZMod p) (range of degree-≤D monomial-`AND` indicators)`. Proof: Layer3's **exact** `toPoly_eval_AC0` (`eval (embed x) (toPoly p c) = boolToZMod p (c.eval x)`) rewrites the function to `x ↦ eval (embed x) (toPoly p c)`, then `lowDegPolyEval_mem_monoAND_span` (via `squarefreeEvalMonomial_eq_monoAND`) puts it in the monomial-`AND` (`SYM∘AND` bottom) span. **The degree bound IS the wall**: `toPoly` is EXACT but its degree is *uncontrolled* — an unbounded fan-in `∧` becomes a *product* of its inputs' polynomials, so degree grows with fan-in (and multiplicatively with depth); polylog degree is the *approximate* RS layer (`toAgree`, agreeing on `1-ε`), so the exact polylog-degree form for arbitrary `AC⁰[p]` is open. Honest: extracting the explicit `ZMod p`-combination from span membership and feeding `weightedSym_searchable` is the remaining mechanical glue; the degree bound is the genuine structural wall | `…ACC0YBTReduction` |
| **`toAgree` approximant in the bounded `SYM∘AND` span (degree bound DISCHARGED)** | **`acc0p_toAgree_in_monoAND_span`**: `(fun x => eval (fun i => boolToZMod p (x i)) (toAgree p t R C)) ∈ Submodule.span (ZMod p) (range of degree-≤((p-1)t)^{C.depth} monomial-`AND` indicators)` — **unconditional**, the degree bound is *proven* (`toAgree_totalDegree_le p t ht R C : (toAgree p t R C).totalDegree ≤ ((p-1)*t)^C.depth`) and fed into `lowDegPolyEval_mem_monoAND_span`. The difference from `…ACC0YBTReduction`: `toPoly` (exact) had *uncontrolled* degree so the membership was conditional; `toAgree` (the RS approximant) *has* the polylog degree bound, so the membership is unconditional — the approximant is genuinely a bounded `SYM∘AND` of quasipolynomial size (`∑_{i≤D}C(n,i)`, `D = ((p-1)t)^{depth}` polylog for constant depth + polylog `t`). Honest: the only remaining gap is **approximate vs exact** — `toAgree` agrees with the circuit on a `1-ε` fraction (Layer3), so this is the *approximant*'s `SYM∘AND` form; by `…ACC0ApproxConsequence` it bounds the circuit's solution count, not its SAT. The exact polylog-degree `SYM∘AND` for arbitrary `AC⁰[p]` (true YBT) is the open wall | `…ACC0ToAgreeDegree` |
| **symmetric-count exact trick (`AND`/`OR` are exactly symmetric, no approximation)** | **`gateCount_eq_card_iff`** (`gateCount g x = m ↔ ∀ j, g j x = true` — the count hits `m` iff every gate accepts, i.e. `AND`; via `Finset.sum_lt_sum` for `→`, `simp [h]` for `←`); **`gateCount_pos_iff`** (`1 ≤ gateCount g x ↔ ∃ j, g j x = true` — count positive iff some accepts, i.e. `OR`; via `ne_eq`+`Finset.sum_eq_zero_iff`+`push_neg`); **`and_exact_sym`** (`(fun x => decide (∀ j, g j x = true)) = symEval g (fun k => decide (k = m))` — `AND` is *exactly* the `SYM` count gate `[·=m]`, via `decide_eq_decide`); **`or_exact_sym`** (`(fun x => decide (∃ j, g j x = true)) = symEval g (fun k => decide (1 ≤ k))` — `OR` is *exactly* the `SYM` count gate `[1≤·]`); **`and_exact_searchable`/`or_exact_searchable`** (so `AND`/`OR` over `m` gates are SAT-searchable in `≤ m+1` cells, **exactly**, via `sym_searchable`). The Beigel–Tarui exact trick at the **gate level**: `AND`/`OR` (and `MOD`, already `modGate_eval_true_iff`) need *no* RS approximation — they are *exactly* symmetric count functions, the exactness coming from the `SYM` top reading the exact `gateCount`. Closes the approximate/exact gap **per gate**; the remaining step is composing these exact symmetric representations across constant depth while keeping the gate count quasipolynomial (the structural composition) | `…ACC0SymmetricExact` |
| **depth composition law (top over observed subcircuits → searchable)** | **`depth_compose_searchable`**: for `sub : Fin k → (Fin n → Bool) → Bool`, `stat : ∀ i, (Fin n → Bool) → S i` (`[∀ i, Fintype (S i)]`) with `∀ i, ObservedBy (subᵢ) (statᵢ)`, any `top : (Fin k → Bool) → Bool`, and `∏ᵢ Fintype.card (Sᵢ) < 2^n`: `(fun x => top (fun i => subᵢ x))` is SAT-searchable in `< 2^n` — joint statistic `fun x => fun i => statᵢ x` (the product), via `observed_top_pi` (composition) + `observed_sat_iff` + `observed_pi_cellCount_le` (`≤ ∏ᵢ card Sᵢ`). The depth-composition law in the observer framework — it iterates to any depth (each `subᵢ` may itself be such a composite). Honest: the boundary is the **product** `∏ᵢ |Sᵢ|`, *exponential* in the width `k` — the naive composition. The whole point of YBT is replacing this multiplicative blow-up by the **additive** degree composition of the polynomial method (degree adds across `∧`/`∨` ⇒ depth-`d` ⇒ polylog degree ⇒ quasipoly monomial-`AND`s, `…ACC0ToAgreeDegree`); that additive/quasipoly control is *approximate* (RS) and the *exact* quasipoly depth-composition (true YBT) is the open structural wall. (Needed `maxHeartbeats 1000000` for the dependent-Pi `Fintype.card_pi`; removed `[∀ i, DecidableEq]` so the image's `DecidableEq` matches `observed_pi_cellCount_le`'s Classical one) | `…ACC0DepthCompose` |
| **additive degree control (`AND` degree = sum of input degrees)** | `toPolyList_eq_map` (`toPolyList p cs = cs.map (toPoly p)`, induction); **`toPoly_andGate_totalDegree_le`**: `(toPoly p (andGate cs)).totalDegree ≤ (cs.map (fun c => (toPoly p c).totalDegree)).sum` — the `AND` gate's polynomial degree is the **sum** (not product) of its inputs' degrees, since `AND = ∏` and `MvPolynomial.totalDegree_list_prod` gives `totalDegree(∏) ≤ ∑ totalDegree`; **`toPoly_andGate_totalDegree_le_of_bounded`**: fan-in `cs.length ≤ w` and `∀ c ∈ cs, (toPoly p c).totalDegree ≤ D` ⇒ `(toPoly p (andGate cs)).totalDegree ≤ w * D` (one layer multiplies the degree by `≤` the fan-in, via `List.sum_le_card_nsmul`). This **additivity** is exactly why the polynomial method's gate count is *quasipolynomial* (degree adds across `∧`/`∨`, so a depth-`d` circuit has polylog degree `((p-1)t)^{depth}` — `toAgree_totalDegree_le` — hence quasipoly monomial-`AND`s) rather than the observer's *exponential* product boundary (`…ACC0DepthCompose`). Honest: this is the within-layer additive law; the full polylog-degree-across-depth is `toAgree_totalDegree_le` (proved, for the approximant); the exact quasipoly depth-composition (true YBT) is the open wall | `…ACC0AdditiveDegree` |
| **exact depth composition (Bool specialization)** | **`exact_depth_composes`**: for `sub : Fin k → (Fin n → Bool) → Bool` and any `top : (Fin k → Bool) → Bool`, once `2^k < 2^n`, `(fun x => top (fun i => subᵢ x))` is SAT-searchable in `< 2^n`, with the subcircuit-output vector `fun x => fun i => subᵢ x` as the cell statistic (boundary `2^k`). The **exact** side of composition (no approximation): the exact symmetric gates (`…ACC0SymmetricExact`: `AND`/`OR`/`MOD` *are* count functions) compose at any depth this way, but with the **exponential** `2^k` *product* boundary. Specializes `depth_compose_searchable` at `S = fun _ => Bool` (`Fintype.card_bool`, `Finset.prod_const`); the `DecidableEq (Fin k → Bool)` instance diverges from the Classical one baked into `depth_compose_searchable`, closed by `convert … using 2; congr!` (subsingleton instances). This makes the exact-vs-quasipoly tension precise as a *theorem*, not a missing lemma: exact composition ⇒ exponential `2^k` boundary; quasipoly composition (`…ACC0AdditiveDegree`/`…ACC0ToAgreeDegree`, additive degree) ⇒ only `1-ε`-*approximate*. Having *both at once* — an exact `SYM∘AND` of quasipolynomial size — is the Beigel–Tarui construction (exact ℤ-polynomial of polylog degree decoded by the symmetric top via CRT), the irreducible YBT wall | `…ACC0ExactCompose` |
| **the Williams `fastSat` ingredient, concrete + quantitative** | Grounds the abstract `fastSat`/`savings` `Prop` of `…ObserverWilliams`/`…WilliamsCashout` in the actual `SYM∘AND` combinatorics. `fastSatWork m := m+1` (count-cells examined). **`fastSat_savings_of_work_le`** (`work ≤ 2^{n−k} ⇒ 2^k·work ≤ 2^n` — savings `≥ 2^k`, via `SpeedupMargin.savings_ge_of_work_le`); **`fastSatWork_le_of_degree`** (degree-`≤D` injective monomial-`AND` family ⇒ `work ≤ (∑_{i≤D}C(n,i))+1`, via `monomial_count_le`); **`lowDegMonomialCount_le_pow`** (`∑_{i≤D}C(n,i) ≤ (D+1)·n^D` — quasipoly gate count, via `Nat.choose_le_pow`); **`symAnd_williams_fastSat`** (the headline: a degree-`≤D` injective `SYM∘AND` decides SAT by the count-cell image search, the cell count fits `2^{n−k}`, **and** the work delivers Williams savings `≥ 2^k`); **`symAnd_williams_fastSat_quasipoly`** (explicit regime `(D+1)·n^D+1 ≤ 2^{n−k} ⇒ savings ≥ 2^k`; `D` polylog ⇒ `k = n−polylog = Ω(n)`, the Williams margin, far above the `ω(log n)` the hierarchy needs). Makes the *combinatorial* `fastSat` a real counted procedure with an exact margin, not an assumed `Prop`. Honest — does NOT prove `NEXP ⊄ ACC⁰`; the two open links stay named and unfaked: (1) the YBT **exact** normal form for arbitrary `ACC⁰` (`MixedACCDepthReductionSocket`; method gives only the approximate form `…ACC0ApproxConsequence` and stops at prime-power `MOD`); (2) uniform TM realization (`UniformWilliamsRealizationSocket`, itself `NEXP ⊄ ACC⁰`-strength per `williams_socket_iff_separation`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0WilliamsFastSat` |
| **the YBT exact normal-form socket — exact `SYM∘AND` closure by mixed-radix counting** | Discharges the **existence/decoding** half of the Yao–Beigel–Tarui socket `HasExactSymAndForm` (`…ACC0YBTSocket`) as a genuine theorem, via Beigel–Tarui's base-`(m+1)` merge, and isolates the irreducible remainder (the **size**). `HasSymAndForm f s` (`f` equals a `SYM` count gate over `≤ s` monomial-`AND`s, size-tracked); count algebra `saCount_sum_elim` (disjoint union → additive) and `saCount_prod_const`/`fin_sum_const_nat` (replicate `r×` → `r·count`); base cases `hasSymAndForm_const`/`_monoAND`/`_var`/**`_mod`** (`MOD_q` *is* a symmetric function of its support literals — exact, size `|S|`); `hasSymAndForm_not`; **`hasSymAndForm_combine`** — the headline mixed-radix merge, closed under *any* `comb : Bool→Bool→Bool` (merged count `C = c₁+(m₁+1)·c₂`, decode by `mod`/`div (m₁+1)`, size `s₁+(s₁+1)·s₂`) → `_and`/`_or`; **`acc0circuit_hasSymAndForm`** — *every* `ACC0Circuit` has an exact `SYM∘AND` form (structural recursion over `const`/`var`/`not`/`and`/`or`/`mod`), explicit size `symAndSize C`; **`acc0circuit_hasExactSymAndForm`** — the bridge (reindex to `Fin m` via `Fintype.equivFin`) yields the socket `HasExactSymAndForm C` whenever `symAndSize C + 1 < 2^n`. Honest — the **exactness is proved in full**; the **size is the wall**: `symAndSize` is *multiplicative* at every `AND`/`OR`, hence exponential in general, so the socket fires only when it stays `< 2^n` (not ordinary poly-size). A quasipolynomial-size exact `SYM∘AND` (true YBT) needs the polynomial method's *additive* degree (`…ACC0AdditiveDegree`), available only **approximately** (`…ACC0ApproxConsequence`) and only at prime-power `MOD` (`…ACC0ModPExact` barrier). So this converts the whole socket into the sharp residue **"exact + quasipolynomial size at once"** (the `…ACC0ExactCompose` wall). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0YBTExactCompose` |
| **splitting the uniform-Williams-realization socket into named sub-sockets** | The same surgery as the YBT socket, on the *other* open Williams socket. The monolith `UniformWilliamsRealizationSocket := (∀ n, MixedACCResidueSatSpeedup n) → NEXPnotACC0` (`…ACC0WilliamsCashout`) is **factored** into a named chain: **`EncodingSocket`** (S1 — cell search → machine procedure, *routine*), **`CostBridgeSocket`** (S2 — cell count `< 2^n` → runtime `2^{n−n^ε}`, *routine*; arithmetic core proved), **`UniformitySocket`** (S3 — per-`n` algorithms form a uniform family, *standard assumption*), **`TimeHierarchySocket`** (S4 — Williams' algorithmic method: uniform speedup ⇒ `NTIME` collapse ⇒ ⊥, **the deep content**). **`realization_socket_factors`** proves `S1∧S2∧S3∧S4 ⇒` the monolith; **`routine_reduce_to_timeHierarchy`** proves that given S1–S3 the socket reduces to S4 alone; **`timeHierarchy_socket_iff_separation`** is the self-audit — once a uniform speedup is established, S4 is *logically equivalent* to `NEXP ⊄ ACC⁰` (proved with **no axioms**, pure logic), so S4 carries the entire difficulty and S1–S3 carry none. The cost bridge's quantitative heart is **proved**: **`cell_count_savings`** (`steps ≤ 2^{n−k} ⇒ 2^k·steps ≤ 2^n`, via `SpeedupMargin`); and **`proved_speedup_is_trivial_savings`** makes explicit that the *currently proved* `steps < 2^n` is only the `k = 0` (factor-`>1`) savings — the super-polynomial `k = n^ε` Williams needs is supplied only by a **quasipolynomial** exact form, routing back to the `…ACC0YBTExactCompose` size wall. So the realization socket is now split: routine parts grounded, one deep separation-strength assumption (S4) named and isolated. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0WilliamsRealizationSplit` |
| **the `ACC⁰` frontier summary — the dependency graph as Lean theorems** | A single readable file re-stating the converged boundary map so the corpus is checkable at a glance. Re-exports of the proved pillars: **`ac0_approximation_quantitative`** (`approximable_full`), **`modp_exact_low_degree`** (`modp_exact_eval`), **`exact_symAnd_decoding`** (`acc0circuit_hasSymAndForm`), **`fastSat_quantitative`** (`symAnd_williams_fastSat`), **`timeHierarchy_is_the_separation`** (the realization self-audit). The headline **`williams_route_reduces_to_two_sockets`** assembles the *entire* Williams route into **one conditional theorem**: given **(A)** the exact-quasipolynomial `SYM∘AND` / depth-reduction socket and **(B)** the time-hierarchy socket — plus the *routine* realization sub-sockets — `NEXP ⊄ ACC⁰` follows (composes `residue_cashout_bundled` with `routine_reduce_to_timeHierarchy`). The only non-routine inputs are the two named walls; everything else is proved or routine. **Now also folds in the three restricted fragments** where the size wall is *crossed*: `restricted_exact_by_leaves` (`O(log n)` leaves), `restricted_exact_by_footprint` (footprint `< n` / bounded-overlap `MOD`), `restricted_exact_by_depth` (bounded binary depth) — all from the one `psize = ∏ leaf-bases` identity — plus `restricted_speedup_by_footprint`/`_depth` (super-polynomial count-cell `ACC⁰`-SAT savings on those fragments). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP`  **Now also folds in the N-Frame route**: `nframe_bridge` (collapse ⇒ low correlation, proved), `nframe_collapse_composes` (lift through a layer), `nframe_unconditional_disjoint` (discharged fragment), `nframe_lower_bound` (holonomy lower bound from ONE socket — N-Frame analogue of the Williams two-socket reduction, sharper since the bridge is proved).  **Also folds in the rank route** (the sharpened N-Frame route): `rank_bridge` (`2^{cellRank} < |L| ⇒` low correlation), `rank_subsumes_survivor` (`cellRank ≤ survivingCount`), `rank_fragment_equal_supports` (equal supports, any `k`, unconditional), `rank_random_restriction` (`p`-biased low-rank live set), `rank_whp` (two-event intersection on the rank tail, subsumes the survivor whp route) — open: `ACC0ForcesLowCellRank` = the rank-flavoured switching lemma. | `…ACC0FrontierSummary` |
| **restricted YBT — bounded-leaf `ACC⁰` has an exact *quasipolynomial* `SYM∘AND` form** | Genuine progress on the YBT *size* wall, for the natural restricted fragments. Engine: a clean multiplicative identity **`symAndSize_succ_eq_psize`** — writing `psize C := symAndSize C + 1`, the exact size is *exactly the product of leaf base sizes* (`const 1`, `var 2`, `not` id, `and`/`or` product, `mod |S|+1`; proved by induction + `ring`). Then **`psize_le`** (`psize C ≤ maxBase C ^ leafCount C`), **`restricted_acc0_has_exact_symAnd`** (`psize C < 2^n ⇒ HasExactSymAndForm C`), and the headline **`restricted_acc0_quasipoly`** (`leafCount C ≤ ℓ`, `maxBase C ≤ B`, `B^ℓ < 2^n ⇒ HasExactSymAndForm C`). For `ℓ = O(log n)`, `B = poly(n)`: `B^ℓ = 2^{O(log²n)}` (quasipolynomial) `< 2^n` — so **bounded-leaf, poly-`MOD`-support `ACC⁰` has an exact quasipolynomial `SYM∘AND` form**, the genuine restricted Beigel–Tarui. Honest — restricted: quasipoly only when the leaf count is *logarithmic*; full `ACC⁰` (poly-many leaves) gives `B^{poly} = 2^{poly}`, exponential, the wall unchanged. A real fragment, not the full theorem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RestrictedYBT` |
| **bounded-overlap `MOD` — small support footprint ⇒ exact `SYM∘AND` form** | The *sharp* version of the restricted result, in the form that captures bounded-overlap / disjoint `MOD` gates. Define the **support footprint** `baseSum C` (`mod q S t → |S|`, `var → 1`, `const → 0`, `not`/`and`/`or` additive); then **`psize_le_two_pow_baseSum`** (`psize C ≤ 2^(baseSum C)`, since each base `b ≤ 2^{b−1}`) and **`acc0_exact_of_baseSum_lt`** (`baseSum C < n ⇒ HasExactSymAndForm C`) — *sharp*, as `baseSum = log₂ psize`. For an `AND` of a `MOD_q` list (`andOfModList`, footprint `= ∑|S_i|` via `baseSum_andOfModList`), **`boundedOverlap_mod_exact`** gives `∑|S_i| < n ⇒` exact form. And the structural fact **`disjoint_mod_footprint_le`** (`disjoint_foldr_union` → `sum_card_eq_card_union` → `sum_card_le_of_pairwise_disjoint`): pairwise-disjoint `MOD` supports have footprint `≤ n` (each variable counted once), so **`disjoint_mod_exact`** — pairwise-disjoint `MOD` gates not covering all `n` variables have an exact `SYM∘AND` form. Honest: the footprint criterion is *sharp* for the mixed-radix construction but caps at `< n` — it covers circuits reading `< n` literal-incidences (disjoint / bounded-overlap `MOD`, bounded total support); a general `ACC⁰` circuit reads all `n` variables many times over (footprint `≫ n`), so this precisely characterises the controllable fragment, untouched is the wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundedOverlapMOD` |
| **the restricted Williams speedup — small footprint ⇒ super-polynomial `ACC⁰`-SAT savings** | The *cash-out* of the restricted exact-form results into an actual Williams-style speedup theorem for the controllable fragments. **`restricted_acc0_searchable`** — every `ACC0Circuit` has its SAT decided by a count-cell search with cell count `≤ psize C` (the exact `SYM∘AND` size; via `acc0circuit_hasSymAndForm` + `observed_sat_iff` + the `saCount` range bound + `symAndSize_succ_eq_psize`). **`restricted_williams_speedup`** — if `baseSum C + k ≤ n` (footprint `≤ n−k`), SAT is decided by `≤ 2^(n−k)` cells with Williams savings `2^k · cells ≤ 2^n`. **`restricted_savings_by_footprint`** — the parametric form: the savings exponent is *exactly* the footprint deficit `n − baseSum C` (so footprint `= polylog` gives savings `2^{n−polylog}`, super-polynomial). **`boundedOverlap_mod_williams_speedup`** / **`disjoint_mod_williams_speedup`** — instantiated to an `AND` of `MOD_q` gates with bounded total support / pairwise-disjoint supports (`|⋃ Sᵢ| + k ≤ n ⇒` savings `2^k`). Honest: a *real* super-polynomial-savings `ACC⁰`-SAT algorithm in the count-cell model, but only for footprint-`< n` fragments (disjoint / bounded-overlap, where the exact form is below `2^n`); it **feeds** the realization split as a concrete instance of the `fastSat`/cost-bridge inputs on these fragments, and does **not** discharge the deep `TimeHierarchySocket` (separation-strength) nor the full-`ACC⁰` size wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RestrictedWilliamsSpeedup` |
| **the bounded-depth fragment — depth `d` ⇒ `≤ 2^d` leaves ⇒ polynomial exact `SYM∘AND` + speedup** | Controls the restricted fragment by **depth** (complementing leaf-count and footprint), again through the `psize` identity. **`leafCount_le_two_pow_depth`** (`leafCount C ≤ 2^(depth C)`: a depth-`d` binary circuit has `≤ 2^d` leaves). **`psize_le_of_depth`** (`depth C ≤ d`, `maxBase C ≤ B ⇒ psize C ≤ B^{2^d}` — *polynomial* for constant `d` and poly base). **`bounded_depth_exact`** (`B^{2^d} < 2^n ⇒ HasExactSymAndForm C`). The `psize`-parametric speedup **`restricted_williams_speedup_of_psize`** (`psize C ≤ 2^{n−k} ⇒` savings `2^k`), and the headline **`bounded_depth_williams_speedup`** (`depth C ≤ d`, `maxBase C ≤ B`, `B^{2^d} ≤ 2^{n−k} ⇒` SAT decided by `≤ 2^{n−k}` cells with savings `≥ 2^k`) — for constant `d` and poly `B`, `B^{2^d} = 2^{O(log n)}`, so `k = n − O(log n)`, super-polynomial. Honest: `depth` here is *binary* (`and`/`or` are 2-ary), so "depth 2" = `≤ 4` leaves (small balanced circuits); the classic *unbounded-fan-in* depth-2 `ACC⁰` circuit is a deep binary chain with only *linearly* many leaves, covered instead by the leaf-count (`O(log n)` leaves) and footprint fragments. A genuine complementary fragment, not the unbounded-fan-in model. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundedDepth` |
| **uniformity bookkeeping — the restricted constructions lift to uniform `ACC⁰` families** | Makes the *structural* half of the realization split's `UniformitySocket` concrete. A **uniform `ACC⁰` family** is a single Lean function `ACC0Family := (n : ℕ) → ACC0Circuit n` (manifestly uniform), with uniform-bound predicates `UniformFootprint`/`UniformLeaf`/`UniformBase`/`UniformDepth` (each bound a single `ℕ → ℕ`). Since every restricted theorem is applied *pointwise* by one structural recursion, a uniformly bounded family yields a uniform family of exact forms / speedups: **`uniform_symAndSize_bound`** (the *uniform `SYM∘AND` translation*: `symAndSize (F n) + 1 ≤ 2^(b n)`, exact-form size bounded by a single function of `n`); **`uniform_exact_of_footprint`** / **`uniform_exact_of_depth`** (the whole family has exact `SYM∘AND` forms); **`uniform_williams_speedup`** / **`uniform_depth_williams_speedup`** (the whole family gets the count-cell Williams speedup with a uniform savings exponent `k : ℕ → ℕ`). Honest: this is *structural* uniformity (one total function / one recursion over all `n`), which discharges that half of `UniformitySocket`; it does **not** define Turing machines, so it does **not** establish full *machine* uniformity (a single poly-time TM emitting the count cells) — that stays the socket's open content (`EncodingSocket`/`CostBridgeSocket`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniformFamily` |
| **the N-Frame cell-collapse route — bridge proved, restriction lemma socketed** | States the N-Frame route to a full-`ACC⁰` correlation bound in the explicit *cell-collapse* vocabulary, isolating the single hard open statement. `CellCollapse supports L := 2^{#survivors} < |L|` (few gate-supports survive on a live set); `LowHolonomyCorrelation` (the predictor `g ∘ weightVec` agrees with some `fParity D` at most half the time on an off-diagonal axis). **`cell_collapse_implies_low_holonomy_correlation`** is the *bridge* — **proved**, a re-export of `ACCSwitchingPipeline.predictor_fails_of_survivors`. **`FullACC0ForcesCellCollapse`** is the *hard open socket* — that a full-`ACC⁰` predictor's supports force the collapse on some live set, the N-Frame analogue of a switching/restriction lemma (same `NP ⊄ ACC⁰`-strength content as `RestrictionTreeSwitch`). **`cell_collapse_of_survival`** reduces the socket to a concrete condition (`< m` survivors on a live set of size `≥ 2^m`), pinning the open target to "a restriction leaving `< log₂|L|` survivors on a large live set `L`". **`nframe_route`** is the composition (socket ▸ proved bridge ⇒ low correlation). Honest: the probabilistic half is proved (`exists_low_survival`); the quantitative switching bound for *full* `ACC⁰` is open and `NP ⊄ ACC⁰`-strength — not deliverable by naive leaf-switching, by the proved `MOD` no-go (`mod_gate_parity_nonconstant`). This pins the open target precisely; it does **not** prove it. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCollapseRoute` |
| **quantitative survival bound for bounded fragments — N-Frame route closed UNCONDITIONALLY** | Discharges the `FullACC0ForcesCellCollapse` socket for two bounded fragments, making the N-Frame route unconditional there. **`survivingCount_le_card`** (≤ k supports survive any live set, `card_filter_le`). **`bounded_gate_forces_cell_collapse`** (`2^k < n ⇒` collapse; live set `univ`, `2^survivors ≤ 2^k < n`). **`survivingCount_eq_zero_of_disjoint`** + **`small_footprint_forces_cell_collapse`** (footprint `+ 2 ≤ n ⇒` collapse; live set = complement of supports' union, `0` survivors, `|L| = n−|⋃| ≥ 2`; via `compl_eq_univ_sdiff`+`card_compl`). **`bounded_gate_low_holonomy_correlation`** / **`small_footprint_low_holonomy_correlation`** (compose with the proved bridge `nframe_route`: predictor does NOT correlate with the holonomy parity — UNCONDITIONAL, no socket). Honest: bounded fragments only (`k < log₂ n` gates, or footprint `≤ n−2`); full `ACC⁰` has poly-many wide supports ⇒ `survivingCount` large on any live set ⇒ collapse fails = the wall, unchanged. Real unconditional low-correlation bound FOR THE FRAGMENTS. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundedSurvival` |
| **bounded-overlap (disjoint-support) cell collapse — N-Frame route discharged again** | The bounded-overlap regime at its clean extreme (pairwise-disjoint supports, overlap 0); works even when supports cover all `n` variables (unlike small-footprint). **`disjoint_supports_forces_cellCollapse`** — pairwise-disjoint supports + some `|S_{j₀}| ≥ 3` ⇒ `FullACC0ForcesCellCollapse` (live set `S_{j₀}`; only it meets itself so `survivingCount = 1`, `2^1 = 2 < |S_{j₀}|`; survivor filter `= {j₀}` via `not_disjoint_iff` + `card_pos`). **`disjoint_supports_low_holonomy_correlation`** — compose with the proved bridge (`nframe_route`): predictor does NOT correlate with the holonomy parity, unconditionally, no socket. Honest: overlap-0 (disjoint) only; degree-`d` overlap (`d ≥ 2`) is NOT forced into collapse by the overlap bound alone (`≤ d·|L|` supports meet any `L` ⇒ `2^{d·|L|} < |L|` impossible) — `d ≥ 2` needs a probabilistic restriction (second-moment cell bridge, socketed in `bounded_overlap_acc0_low_correlation_whp`), the frontier. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DisjointCollapse` |
| **the N-Frame ACC⁰ lower bound, stated beside Williams — one socket, bridge proved** | Packages the whole N-Frame route as ONE conditional theorem (Williams = two sockets; N-Frame = one, since collapse ⇒ low correlation is already proved). `PredictorClass ι n := ι → Σ k, Fin k → Finset (Fin n)`; **`NFrameCellCollapse sys`** = the hard open socket (every predictor's supports collapse on some live set); **`ACC0HolonomyLowerBound sys tops`** = the conclusion (every predictor fails to correlate with some holonomy parity). **`nframe_acc_lower_bound`** (HEADLINE, proved): `NFrameCellCollapse sys ⇒ ACC0HolonomyLowerBound sys tops` (one-socket reduction via `nframe_route` pointwise; N-Frame analogue of `williams_route_reduces_to_two_sockets`). **`nframe_lower_bound_disjoint`** — the socket DISCHARGED for disjoint-support classes ⇒ UNCONDITIONAL N-Frame lower bound (conditional not vacuous). Honest: the socket is the open `NP ⊄ ACC⁰`-strength content — discharged for bounded fragments, FALSE for arbitrary dense overlapping supports (the wall); genuine open statement = cell collapse for real `ACC⁰` under restriction (the switching lemma). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NFrameLowerBound` |
| **`collapse_lifts_through_layer` — cell collapse composes under a survivor budget** | The composition direction of the N-Frame route: stacking a layer appends its supports (`Fin.append`). **`survivingCount_append`** — survivor counts ADD across the layer (`survivingCount (append supp₁ supp₂) L = survivingCount supp₁ L + survivingCount supp₂ L`; `finSumFinEquiv` + `Fintype.sum_sum_type` + `Fin.append_left/right` + `card_filter`). **`collapse_lifts_through_layer`** (`2^{s₁+s₂} < |L| ⇒ CellCollapse (append) L`). **`collapse_lifts_of_budget`** (controlled-parameter form: layer survivors `≤ δ`, base slack `2^{s₁+δ} < |L|` ⇒ lift). **`full_collapse_lifts`** (packaged as `FullACC0ForcesCellCollapse (append …)`). Honest: collapse composes ADDITIVELY in survivors (multiplicatively in cell count), so it lifts only when the new layer adds FEW survivors (budget `δ`, `s₁+δ < log₂|L|`); a wide `MOD` layer surviving on most of `L` blows the budget = the wall. Supplying the small budget for a real `ACC⁰` layer under restriction = the switching lemma (blocked for leaf-switching by the proved `MOD` no-go). Proves collapse composes + lifts under budget; does NOT supply the budget for full `ACC⁰`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CollapseLift` |
| **rank-based cell collapse — the sharper collapse (rank, not survivor count)** | For overlapping supports many gates survive but induce few distinct cells; the cell count is governed by the `F₂`-RANK of the support incidence. **`incidenceVec`/`survivorIncidence`/`supportRank`** (`F₂` incidence vectors of surviving supports + dim of their span, `Set.finrank`). **`supportRank_le_survivingCount`** (rank ≤ #generators, via `finrank_span_finset_le_card` + `card_image_le`). `RankCellCollapse supports L := 2^{supportRank} < |L|` (sharp). **`cell_collapse_implies_rank_collapse`** (`CellCollapse ⇒ RankCellCollapse` — correct direction; rank ≤ survivors ⇒ rank collapse is WEAKER / more achievable; the rank→cell direction is the flip, false in general). **`FullACC0ForcesRankCollapse`** (weaker socket) + **`full_cell_collapse_implies_full_rank_collapse`** (survivor socket ⇒ rank socket). Honest: the rank route WEAKENS the open socket and is the correct cell count for `MOD₂`/overlapping supports (`#parity cells = 2^rank = parity_reachable_card`, `…ACC0ParityRankCardinality`). Genuine next target = sharp bridge `RankCellCollapse ⇒ low correlation` (pigeonhole with `#cells ≤ 2^rank`), NOT from composing the survivor bridge. Forcing `supportRank < log₂|L|` for real `ACC⁰` = the rank-flavoured switching lemma, open `NP ⊄ ACC⁰`-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankCollapseRoute` |
| **the sharp rank bridge: 2^rank < |L| ⇒ low holonomy correlation** | A GENUINE theorem (no socket) — the correlation bridge with the sharp RANK cell count, not the crude survivor count. `cellPatternVec supports v := fun j => if v∈supports j then 1 else 0` (column of incidence); **`sameCell_iff_pattern`** (`SameCell ⟺ cellPatternVec v = cellPatternVec w`); `cellSpan`/`cellRank` (F₂-span of cell patterns over L + its dim); **`cellPattern_image_card_le`** (#distinct cell patterns `≤ 2^cellRank` — patterns ⊆ span, `|span| = 2^finrank` via `Module.card_eq_pow_finrank` + `ZMod.card`, the `parity_reachable_card` technique); **`exists_sameCell_pair_of_rank`** (`2^cellRank < |L|` ⇒ two live coords share a cell, pigeonhole); **`rank_collapse_low_correlation`** (THE SHARP BRIDGE: `2^cellRank < |L| ⇒ LowHolonomyCorrelation`, via `CellWitness` + the proved `cellWitness_gives_low_correlation`). Strictly sharper than the survivor bridge: `cellRank` (column rank) `≤ survivingCount` (row count), far smaller for overlapping supports. Self-contained, no socket. Open content unchanged: FORCING `cellRank < log₂|L|` for real `ACC⁰` under restriction = the rank-flavoured switching lemma (`NP ⊄ ACC⁰`-strength). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP`  **Cleanup (proved): the rank bridge strictly subsumes the survivor bridge** — `cellPatternVec_mem_survivorSpan` (a cell pattern lies in the survivor-singles span, vanishing off survivors), `cellRank_le_survivingCount` (column rank ≤ row count, via `Submodule.finrank_mono` + `finrank_span_finset_le_card`), `survivor_collapse_implies_rank_collapse` (`2^survivingCount < |L| ⇒ 2^cellRank < |L|`). The full hierarchy is now machine-checked: few survivors ⇒ low rank ⇒ low cell count ⇒ same-cell witness ⇒ low holonomy correlation. | `…ACC0RankBridge` |
| **low-rank fragment of `acc0_restriction_forces_low_cellRank` — rank wins, survivors lose** | Discharges the rank socket for low-rank systems the survivor route cannot touch. `ACC0ForcesLowCellRank supports := ∃ L, 2^{cellRank} < |L|`; **`low_cellRank_low_correlation`** (socket ⇒ low correlation, ▸ the proved rank bridge); **`bounded_cellRank_univ_forces`** (`2^{cellRank univ} < n ⇒` socket); **`equal_supports_cellRank_le_one`** (equal supports ⇒ `cellRank ≤ 1`, patterns ∈ span{𝟙}, INDEPENDENT of gate count `k`); **`equal_supports_low_correlation`** (equal supports, any `k`, `n ≥ 3` ⇒ predictor fails to correlate, UNCONDITIONAL). Punchline: equal supports = all `k` gates survive (`survivingCount = k`, survivor route powerless), yet rank `≤ 1` so the rank route still gives the bound — proof the rank reformulation strictly strengthens. Honest: a fragment; general low-rank under restriction = open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LowRankFragment` |
| **random restriction toward low cell rank — probabilistic stepping stones** | Transfers the `p`-biased survivor concentration to cell RANK via pointwise `cellRank ≤ survivingCount`. **`expected_cellRank_le_expected_survivors`** (`Exp p (cellRank) ≤ Exp p (survivingCount)`, monotone expectation); **`randomRestriction_forces_low_cellRank`** (expected survivors `≤ B < a ⇒ ∃ L, cellRank < a`, via `exists_low_survival` + `cellRank ≤ survivingCount`). Honest: stepping stones — give `cellRank < a` (real threshold); the socket needs `2^{cellRank} < |L|` (`cellRank < log₂|L|` with `|L|` large simultaneously) = the open rank switching lemma. Supplies the probabilistic inputs (bounded-overlap `E[survivors]` bound ⇒ small `E[cellRank]` + low-rank `L`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RandomRestrictionRank` |
| **bounded-fan-in instantiation: low-cell-rank live set + the exact |L|-balance gap** | Instantiates the probabilistic rank machinery with the proved first-moment bound `exp_survivingCount_le` (`Exp p (survivingCount) ≤ k·s·p`). **`boundedFanin_forces_low_cellRank`** (fan-in `≤ s`, `k·s·p < a` ⇒ `∃ L, cellRank supports L < a`); **`heavy_restriction_forces_rank_zero`** (`k·s·p < 1` ⇒ `∃ L, cellRank = 0`). Honest: supplies the LOW-RANK half of the socket unconditionally for bounded fan-in (`cellRank < a`, real); does NOT close `2^{cellRank} < |L|` — the gap is precise, the Markov live set is NOT guaranteed large (`L = ∅` has `survivingCount = 0`, `cellRank = 0`, but `|L| = 0`, so socket fails). Need `L` simultaneously low-rank AND large (`|L| > 2^{cellRank}`) — the heavy-vs-light restriction tension, requiring two-sided concentration (lower-tail on `|L|`, not yet in corpus) + the structural rank input for general `ACC⁰` = the open rank-flavoured switching lemma. Pins exactly that balance. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundedOverlapRank` |
| **the rank whp route — two-event intersection with the rank tail (subsumes survivor whp)** | The rank version of `bounded_overlap_predictor_fails_whp`, with strictly weaker feasibility. **`Pr_mono`** (`E ⊆ F ⇒ Pr E ≤ Pr F`, `sum_le_sum_of_subset_of_nonneg`); **`rank_predictor_fails_whp`** (`2^a ≤ b ∧ Pr[cellRank ≥ a] + Pr[|L| ≤ b] < 1 ⇒ LowHolonomyCorrelation`, via `exists_both_of_pr_add_lt_one` ⟶ `2^{cellRank} < |L|` ⟶ sharp rank bridge); **`rank_feasibility_le_survivor`** (`Pr[cellRank ≥ a] + Pr[|L| ≤ b] ≤ Pr[survivingCount ≥ a] + Pr[|L| ≤ b]`, since `{cellRank ≥ a} ⊆ {survivingCount ≥ a}`); **`rank_whp_of_survivor_feasible`** (survivor feasibility `< 1` ⇒ rank route fires — STRICTLY SUBSUMES the survivor whp theorem). The two-event intersection (low-rank ∧ large-`L`) now uses the *rank* tail. Honest: the feasibility `Pr[cellRank ≥ a] + Pr[|L| ≤ b] < 1` with `2^a ≤ b` is still the open balance for poly-many overlapping gates (bounding `Pr[cellRank ≥ a]` = the structural rank input). Sharpest whp reduction; does not close it. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankWhp` |
| **structured case of ACC0ForcesLowCellRank — bounded distinct supports** | Discharges the rank socket for ≤ `d` distinct supports, regardless of gate count `k`. `classVec supports T := fun j => if supports j = T then 1 else 0` (support-class indicator); **`cellPatternVec_mem_classSpan`** (cell pattern ∈ span of the `≤ d` class indicators — constant on each support-class, `∑_T (v∈T)·classVec T` via `sum_eq_single_of_mem`); **`cellRank_le_distinct`** (`cellRank ≤ |image supports|`); **`bounded_distinct_forces_low_cellRank`** (`|image| ≤ d ∧ 2^d < n ⇒ ACC0ForcesLowCellRank`); **`bounded_distinct_low_correlation`** (⇒ predictor fails, unconditional, any `k`). Generalizes equal supports (`d=1`); survivor route powerless (`survivingCount = k`), rank route wins (`cellRank ≤ d`). Honest: a fragment — general `ACC⁰` has poly-many *distinct* wide supports (`d` large, `2^d > n`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundedDistinctRank` |
| **low-dimensional support-span ⇒ low cell rank — the natural next N-Frame theorem** | Generalizes bounded-distinct by bounding cell rank via the `F₂`-DIMENSION of the support indicators. `SupportsInRankSpan supports r := ∃ B c, ∀ j i, (i ∈ supports j) = ∑_m c j m · B m i` (indicators are `F₂`-combos of `r` fixed vectors); **`cellRank_le_of_span`** (`SupportsInRankSpan r ⇒ cellRank ≤ r`; `cellPatternVec v = ∑_m (B m v)·(c_·m)` ∈ span of `r` coefficient vectors — the column-rank `≤` row-rank bound made CONSTRUCTIVE via explicit coefficients, no abstract row=col rank); **`supportSpan_forces_low_cellRank`** (`SupportsInRankSpan r ∧ 2^r < n ⇒ ACC0ForcesLowCellRank`); **`supportSpan_low_correlation`** (⇒ predictor fails, unconditional, any `k`). Sharpest structured rank fragment: caps `cellRank` by the support-family *dimension*, subsuming bounded-distinct (`r ≤ #distinct`) and equal supports (`r = 1`). Honest: a fragment — general `ACC⁰` indicators span high dimension (`r ~ poly`, `2^r > n`); bounding support-span dim under restriction = the open rank switching lemma. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SupportSpanRank` |
| **rank growth through layers — `cellRank` is subadditive under `append`** | The depth-direction rank composition law. `embedFst`/`embedSnd` (`F₂`-linear inclusions `Fin k_i → Fin(k₁+k₂)` extending by zero; `map_add'`/`map_smul'` via `Fin.addCases` + `Fin.append_left/right`); `cellPatternVec_append_eq` (append pattern = `embedFst p₁ + embedSnd p₂`); **`cellRank_append_le`** (`cellRank (append supp₁ supp₂) L ≤ cellRank supp₁ L + cellRank supp₂ L`; `cellSpan(append) ≤ map embedFst cellSpan₁ ⊔ map embedSnd cellSpan₂`, `finrank_sup_add_finrank_inf_eq` + `Submodule.finrank_map_le`); **`rank_collapse_lifts_of_rank_budget`** (`2^{r₁+r₂} < |L| ∧ cellRank₁ ≤ r₁ ∧ cellRank₂ ≤ r₂ ⇒ 2^{cellRank(append)} < |L|`). Turns depth composition into RANK-budget accounting (vs survivor-budget): a layer adding only `r₂` observer degrees of freedom keeps composite rank `≤ r₁+r₂` — the sharpened analogue of the survivor `collapse_lifts_through_layer`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP`  **(also re-exported in `…ACC0FrontierSummary` as `rank_append_subadditive` / `rank_collapse_lifts_budget`).** | `…ACC0RankComposition` |
| **direct cell-count collapse route — the sharpest observer collapse (cells, not rank)** | The collapse keyed on the actual number of observer cells, strictly generalizing the rank route. `cellPatternImage`/`cellPatternCount`; `CellCountCollapse supports L := cellPatternCount < |L|`. **`exists_sameCell_pair_of_count`** (bare pigeonhole, no rank bound); **`cellCountCollapse_implies_low_correlation`** (`CellCountCollapse ⇒ LowHolonomyCorrelation`); **`rank_collapse_implies_cellCount_collapse`** (`2^{cellRank} < |L| ⇒ CellCountCollapse`, since `cellPatternCount ≤ 2^{cellRank}` — SUBSUMES the rank route). Hierarchy: few survivors ⇒ low rank ⇒ few cells ⇒ same-cell witness ⇒ low correlation. Open target sharpens to `ACC0ForcesLowCellCount`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountRoute` |
| **chain (nested) supports — few cells even at full rank** | The class the rank route misses: nested supports `S_1 ⊆ … ⊆ S_k` have FULL rank but `≤ k+1` cell patterns. `ChainSupports`/`suppSet`; **`suppSet_comparable`** (chain membership sets pairwise comparable, by_contra + chain comparability); `oneSet_eq_suppSet`/`cellPatternVec_eq_of_suppSet` (pattern↔membership dictionary); **`chain_cellPatternCount_le`** (`ChainSupports ⇒ cellPatternCount ≤ k+1`; card map injective on comparable patterns via `card_le_card_of_injOn` + `eq_of_subset_of_card_le`); **`chain_low_correlation`** (`ChainSupports ∧ k+1 < |L| ⇒ LowHolonomyCorrelation`, regardless of rank). The honest correction: for nested/laminar, cell COUNT (not rank) is the right observer invariant. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ChainCellCount` |
| **clustered supports — `cellRank ≤ d + r` (corollary of support-span)** | Clustered = `d` cluster centers + `r`-dim variation: `(i∈supports j) = ∑ cc·center + ∑ cv·V`. **`ClusteredSupports`**; **`clustered_supports_in_span`** (⇒ `SupportsInRankSpan supports (d+r)`; `B = append center V`, `c = append cc cv`, `finSumFinEquiv` sum-split + `Fin.append_left/right`); **`clustered_supports_low_rank`** (`cellRank ≤ d+r` via `cellRank_le_of_span`); **`clustered_low_correlation`** (`2^{d+r} < n ⇒` low correlation, any `k`). Caps observer rank by `#clusters + variation rank`; subsumes bounded-distinct (`r=0`) and equal supports (`d=1,r=0`). A cheap genuine corollary. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ClusteredRank` |
| **cell-count composition — `cellPatternCount` submultiplicative under `append`** | The cell-count analogue of rank composition. **`cellPatternVec_append`** (append pattern = concatenation `Fin.append (cPV A v)(cPV B v)`); **`cellPatternCount_append_le`** (`cellPatternCount (append A B) L ≤ cellPatternCount A L * cellPatternCount B L`; concatenation injective ⇒ `image ⊆ (img_A ×ˢ img_B).image append`, `card_image_le` + `card_product`); **`cellCount_collapse_lifts`** / **`cellCount_collapse_of_budget`** (`c₁·c₂ < |L| ⇒ CellCountCollapse (append A B) L`). Depth composition MULTIPLIES cell counts (vs additive survivor, subadditive rank). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountComposition` |
| **full cell-count switching socket — `FullACC0ForcesLowCellCount ⇒ holonomy LB`** | The SHARPEST (weakest, most achievable) N-Frame switching socket. **`FullACC0ForcesLowCellCount supports := ∃ L, cellPatternCount supports L < \|L\|`** (the open socket); **`nframe_cellcount_route`** (socket ⇒ `LowHolonomyCorrelation`, proved bridge); **`cellCollapse_implies_lowCellCount`** (survivor socket ⇒ cell-count socket, since `cellPatternCount ≤ 2^cellRank ≤ 2^survivingCount` — it is the WEAKEST socket); **`NFrameLowCellCount`** / **`nframe_cellcount_lower_bound`** (over a predictor class, socket ⇒ `ACC0HolonomyLowerBound`); **`nframe_lowCellCount_of_cellCollapse`** (survivor class-socket ⇒ cell-count class-socket — NOT vacuous, every disjoint/bounded discharge carries over). The official final target. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountSwitching` |
| **low VC dimension ⇒ few observer cells (Sauer–Shelah)** | The cell patterns ARE the gate-membership sets `{suppSet v : v ∈ L} ⊆ 2^(Fin k)`, so cell count = set-family size, bounded by Sauer–Shelah. The common generalization of bounded-distinct/clustered/laminar — *few observer patterns, not low rank* (the N-Frame observer idea). **`cellVCdim supports L`** := `(L.image (suppSet supports)).vcDim`; **`patternFamily_card`** (`\|{suppSet v}\| = cellPatternCount`, one-set bijection + `card_image_of_injOn`); **`lowVC_cellPatternCount_le`** (`cellVCdim ≤ d ⇒ cellPatternCount ≤ ∑_{i≤d} C(k,i)`, Mathlib Pajor `card_le_card_shatterer` + Sauer–Shelah `card_shatterer_le_sum_vcDim`); **`lowVC_cellCountCollapse`** / **`lowVC_low_correlation`** (`∑_{i≤d} C(k,i) < \|L\| ⇒` collapse ⇒ low corr). Converts `ACC0ForcesLowCellCount` to "bound observer VC under a restriction" for wide overlapping `MOD`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0VCCellCount` |
| **laminar (nested-or-disjoint) supports — `≤ k+1` cells** | Generalizes chains: any two supports nested OR disjoint, still `≤ k+1` cells despite possibly full rank. **`LaminarSupports`**; **`chain_isLaminar`** (chains ⊂ laminar); **`laminar_suppSet_eq`** (for `⊆`-minimal-card `j∈suppSet v`, `suppSet v = {i : supports j ⊆ supports i}` the up-set); **`laminar_cellPatternCount_le`** (every pattern is an up-set indicator or `∅`, `≤ k+1`; `card_le_card_of_injOn` into `insert ∅ (univ.image U)`, `exists_min_image`); **`laminar_low_correlation`** (`k+1 < \|L\|` ⇒ low correlation, regardless of rank). A genuine strengthening — the cardinality-injection trick of chains fails (laminar `suppSet`s not pairwise comparable). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LaminarCellCount` |
| **block-product supports — cell count is the PRODUCT of per-block counts** | `m` blocks of `b` gates (`supports : Fin m → Fin b → Finset (Fin n)`); cell count multiplies. **`blockCellCount`** / **`flatSupports`** (flattened to `Fin (m·b)` via `finProdFinEquiv`); **`blockCellCount_le_prod`** (`≤ ∏ i, cellPatternCount (supports i) L`; tuple map ↪ `Fintype.piFinset`, `Fintype.card_piFinset`); **`blockCellCount_le_pow`** (each block `≤ c` ⇒ `≤ cᵐ`); **`cellPatternCount_flat_eq`** (flattening = gate relabelling, `finProdFinEquiv` bijection ⇒ same cells); **`block_product_low_correlation`** (`∏ < \|L\|` ⇒ low correlation). The `m`-fold generalization of `cellPatternCount_append_le`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BlockProductCellCount` |
| **`e_d`-sparsity identity — each binomial moment is a sparse `d`-subset sum** | `C(∑_j b_j, d) = Σ_{|T|=d} ∏_{j∈T} b_j` for `0/1` values (`b_j ∈ {0,1}`). **`boolean_esymm`** (`S = {j:b_j=1}`; `∑b=\|S\|`; `∏_T = [T⊆S]`; `Σ_{|T|=d}[T⊆S] = \|powersetCard d S\| = C(\|S\|,d)`); **`andCount_choose_eq`** (applied to AND-indicators: `C(andCount x,d) = Σ_{\|T\|=d} ∏_{j∈T}[AND_j(x)]`). Makes each moment a kernel-computable sparse sum. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ElementarySymmetric` |
| **the prime-power obstruction, made precise — `MOD_{p^e}` is not a function of the mod-`p` residue** | Pins down *why* the field-indicator route stops at prime powers. **`modPrimePower_not_function_of_modP`** (PROVED): `0` and `p` share the mod-`p` residue (`(0:ZMod p)=(p:ZMod p)=0`) yet `MOD_{p^e}` accepts `0` (`p^e∣0`) and rejects `p` (`¬p^e∣p`, since `p<p^e` for `e≥2`) — so `MOD_{p^e}` is not determined by the mod-`p` residue, hence no `F_p` polynomial in the prime-`p` residue (any degree) computes it. **`modPrimePower_needs_ring`** (the deciding observer lives in the ring `ZMod(p^e)`, not a low-degree field poly). The honest `ACC⁰[composite]` boundary: a low-degree-field prime-power gate would be the open lower bound; obstruction recorded, not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerObstruction` |
| **prime-power observer candidates — refute the field family, confirm the richer (filtered ring / valuation) observers** | Tests the candidate richer observers the tri-aspect boundary could select instead of the failed field indicator. **`charP_field_observer_fails`** (PROVED, REFUTES the whole characteristic-`p` field family — `F_p`, `F_{p^k}`, `F̄_p`, …): in *any* field `F` of characteristic `p`, the count's image depends only on the mod-`p` residue (`(0:F)=(p:F)=0`) yet `MOD_{p^e}` separates `0` and `p` for `e≥2` — so no characteristic-`p` field can compute `MOD_{p^e}`. This generalises the single `F_p` obstruction to the *entire* field family (a bigger field of the same characteristic still sees only mod-`p`). **`ringPrimePower_observer_decides`** (PROVED, candidate #1 — the ring): `ZMod(p^e)` has characteristic `p^e` (not `p`), is *not* a field (has zero divisors), but sees the mod-`p^e` residue and decides `MOD_{p^e}` (`(s:ZMod(p^e))=0 ↔ p^e∣s`); it escapes the char-`p` refutation. **`valuation_observer_decides`** (PROVED, candidate #2 — the `p`-adic valuation): `p^e∣x ↔ e ≤ v_p(x)` (`x≠0`, via `Nat.Prime.pow_dvd_iff_le_factorization`); the valuation observer `x ↦ min(v_p x, e)` carries the mod-`p^e` information the field observer destroys (`MOD_{p^e}(x)⟺v_p(x)≥e`). Honest dichotomy: the field-observer family is OUT (proved), the filtered-ring / valuation observers are IN *information-theoretically* (proved); the open crux is whether any richer observer admits a *quasipolynomial low-degree sparse* representation — over `ZMod(p^e)` the zero divisors break the Fermat indicator, and the valuation is a threshold, not an obvious low-degree polynomial. N-Frame reading: the tri-aspect boundary must select a **filtered ring / valuation observer**, not a field one. The obstruction is sharpened (which observers can/can't see `MOD_{p^e}`), not circumvented; no quasipoly representation is claimed. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerObserverCandidates` |
| **candidate #3 — the layered residue tower observer `x ↦ (x mod p, x mod p², …, x mod p^e)`** | Analyses the third candidate: the filtration `ZMod p ← ZMod p² ← … ← ZMod p^e`. Three honest facts pin down exactly what the tower buys. **`tower_level_decides`** (PROVED — *graded ladder*): rung `i` decides `MOD_{p^i}` (`(s:ZMod(p^i))=0 ↔ p^i∣s`), so the whole tower is the ladder `{MOD_{p^i}}_{i=1}^e` of sharper and sharper divisibility tests. **`tower_projection_compatible`** (PROVED — *filtration, top determines all*): rung `i` is the canonical ring projection of rung `e` — `ZMod.castHom (h : p^i ∣ p^e) (ZMod(p^i)) : ZMod(p^e) →+* ZMod(p^i)` sends the level-`e` residue to the level-`i` residue (`map_natCast`); hence the top rung `ZMod(p^e)` determines the entire tower, so the tower carries *no information beyond* candidate #1 — it is a structured *presentation* of `ZMod(p^e)`, not a richer observer. **`tower_truncation_insufficient`** (PROVED — *depth exactly `e` required*): `0` and `p^{e-1}` agree on every rung `1 ≤ i ≤ e-1` (both `≡ 0 mod p^i`, via `pow_dvd_pow`) yet `MOD_{p^e}` accepts `0` (`p^e∣0`) and rejects `p^{e-1}` (`¬p^e∣p^{e-1}`, since `e-1<e`) — so no function of the truncated tower (levels `1 … e-1`) computes `MOD_{p^e}`; the full depth `e` is necessary, and the field observer is exactly the failing case `e=1`. Honest scope: the tower is information-*equivalent* to the ring `ZMod(p^e)` (proved), so it does **not** escape the low-degree wall — its top rung is still a zero-divisor ring where the Fermat indicator dies. What it adds is *structure* (the graded ladder) and a *localisation* of the loss (the depth-`e` requirement). No quasipolynomial low-degree sparse representation is claimed — that remains the open `ACC⁰[composite]` crux. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerTowerObserver` |
| **candidate #4 — the Witt-vector / `p`-adic digit observer `x = a₀ + p·a₁ + p²·a₂ + …`** | Analyses the last candidate: record `x` by its base-`p` digits `aᵢ := digit p i x = (x / pⁱ) mod p` (the `p`-adic / Witt components) rather than its residues. **`lowest_digit_eq_mod_p`** (PROVED): `digit p 0 s = s mod p` — a characteristic-`p` field observer sees *only* the lowest digit `a₀`, so the digit decomposition exposes the field as the bottom of the `p`-adic tower. **`digit_observer_decides`** (PROVED — by genuine digit-peeling induction on `e`, *not* collapsed to the residue statement): `p^e ∣ s ↔ ∀ i < e, digit p i s = 0` — divisibility by `p^e` is exactly the vanishing of the lowest `e` base-`p` digits `a₀ … a_{e-1}`. The induction peels one factor of `p` per step: base case `p^0 ∣ s`; the step extracts `s mod p = 0 ⇒ s = p·s'`, uses the digit shift `digit p (i+1) (p·s') = digit p i s'` (`pow_succ'` + `Nat.mul_div_mul_left`) to feed the induction hypothesis, and recombines `p^e ∣ s' ⇒ p^{e+1} ∣ p·s'`. **`field_digit_insufficient`** (PROVED): `0` and `p` share the lowest digit (`digit p 0 0 = digit p 0 p = 0`, since `p mod p = 0`) yet `MOD_{p^e}` accepts `0` and rejects `p` for `e ≥ 2` — the field observer (lowest digit only) cannot compute `MOD_{p^e}`; the higher digits `a₁ … a_{e-1}` are needed. Honest scope: the digit observer is information-*equivalent* to the ring `ZMod(p^e)` and the residue tower (the digits `a₀ … a_{e-1}` and the residue `x mod p^e` determine each other), so it does **not** escape the low-degree wall. Its value is the same as the tower's — it exposes *structure* (here the explicit Witt/digit decomposition) and *localises* the field obstruction to the lowest digit (`MOD_{p^e}` needs depth-`e` digits; the field sees only `a₀`). No quasipolynomial low-degree sparse representation is claimed — that remains the open `ACC⁰[composite]` crux. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerDigitObserver` |
| **Route 1 — non-field low-degree theory: why the prime-power indicator resists a low-degree representation** | Tests the open `ACC⁰[composite]` crux honestly: every observer that *sees* `MOD_{p^e}` is non-field (the scorecard); can such an observer admit a low-degree representation? Two bounding facts settle *why this is genuinely open*. **`field_root_card_le_natDegree`** (PROVED): over a field, a nonzero polynomial vanishing on a finset of size `k` has `natDegree ≥ k` (the distinct roots inject into `g.roots.toFinset`, whose multiset card is `≤ natDegree` by `card_roots'`). **`modPrimePower_field_indicator_high_degree`** (PROVED): the `MOD_{p^e}` indicator on the count rejects the `p^e-1` *consecutive* non-multiples `1,…,p^e-1`; in a characteristic-`0` field these are distinct (`Nat.cast_injective`), so any nonzero polynomial vanishing on them has `natDegree ≥ p^e-1` — *exponential* in `e`. So a **field** representation of `MOD_{p^e}` is high-degree, useless for the polynomial method (which needs polylog degree); the field route reaches `MOD_p` only by collapsing the count mod `p` to `{0,…,p-1}` (degree `p-1`, Fermat), and reaching `p^e` forces a reduction mod `p^e` — which *leaves the category of fields*. **`ring_root_count_fails`** (PROVED): over the ring `ZMod(p^e)` (`e ≥ 2`), the polynomial `C(p^{e-1})·X` has `natDegree ≤ 1` and is nonzero, yet vanishes at the two distinct points `0` and `p` (because `p^{e-1}·p = p^e = 0` in the ring) — so a degree-`≤1` polynomial has `≥ 2` roots, and the field lemma `field_root_card_le_natDegree` is *false* over the non-field ring. Honest verdict: the field route is *quantitatively* blocked (degree `≥ p^e-1`), and the only observers that escape are non-field, where the degree-bounding machinery (root counting) itself collapses — there is no degree obstruction to lean on. We do **not** construct a low-degree sparse representation over a non-field observer, and we do **not** prove one cannot exist; either would *be* the open `ACC⁰[composite]` lower bound. What is established is *why* Route 1 is genuinely open and not a corollary of root counting: the obstruction is the absence of a field, not a missing computation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NonFieldObserverTheory` |
| **Route 1 — valuation / `p`-adic sparse theory: the sparse top-structure of `MOD_{p^e}` and the carry barrier** | Pushes Route 1 forward by *isolating exactly where the cost lives* in the valuation observer, rather than restating the obstruction. Three facts give a clean anatomy of `MOD_{p^e}` along the `p`-adic tower. **`modPrimePower_eq_and_of_downshift_modP`** (PROVED): `p^e ∣ s ↔ ∀ i < e, p ∣ (s / p^i)` — `MOD_{p^e}` is an `e`-fold **AND of `MOD_p` tests on the down-shifted counts** `s, s/p, s/p², …, s/p^{e-1}` (equivalent to the digit decomposition `p ∣ (s/p^i) ↔ digit p i s = 0`). The *top* of the representation is sparse: `e` conjuncts, each a plain prime-modulus test. **`downshift_conjunct_decided_by_lowdegree_gate`** (PROVED): each conjunct `p ∣ (s/p^i)` is decided by the degree-`(p-1)` Fermat gate `modPGate p = 1 − X^{p−1}` over `F_p`, evaluated at the down-shifted count — so the *modular arithmetic* of each test costs only degree `p-1`, no blow-up. **`downshift_breaks_additivity`** (PROVED): `(p-1)/p + (p-1)/p ≠ (2(p-1))/p` for `p ≥ 2` (LHS `= 0`, RHS `= 1`) — the down-shift `s ↦ s/p` is **not additive**, so it destroys the linear/additive structure of the count `s = ∑ xᵢ` that makes `MOD_p` cheap (the cheap `MOD_p` polynomial is degree `p-1` in the *linear form* `∑ Xᵢ`); after one down-shift the argument is no longer a linear form in the input bits, so the higher conjuncts cannot reuse the linear-form mechanism. Honest verdict: the top structure is sparse (`e`-fold AND) and the modular arithmetic of each conjunct is low-degree; the *entire* residual difficulty is expressing the down-shifted count `s/p^i` as a low-degree object in the input bits, and the down-shift is provably non-additive — which is precisely why the linear-form trick does not transfer. This sharpens the open target — a sparse representation of the **down-shift / carry**, not of the modulus — without constructing one (which would *be* the open lower bound). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ValuationSparseTheory` |
| **Route 1 — carry sparse theory: the structure of `⌊s/p⌋` and the carry boundary** | Dissects the single object Route 1's wall reduced to — the down-shift `s ↦ ⌊s/p⌋` — proving how its non-additivity is structured, where it vanishes, and why it resists the down-shifts. **`downshift_add_carry_identity`** (PROVED): `⌊(a+b)/p⌋ = ⌊a/p⌋ + ⌊b/p⌋ + ⌊(a%p+b%p)/p⌋` — the defect from additivity is *exactly* the carry term produced by the low residues. **`carry_le_one`** (PROVED): `⌊(a%p+b%p)/p⌋ ≤ 1` (each residue `< p`, so their sum is `< 2p`) — the entire obstruction to additivity is **one carry bit per addition**. **`downshift_additive_no_carry`** (PROVED — the restricted-positive fragment): if `a%p + b%p < p` then `⌊(a+b)/p⌋ = ⌊a/p⌋ + ⌊b/p⌋`; in a no-carry regime the down-shifted count remains a linear form, so the `MOD_p` test on it stays cheap and the sparse representation of `MOD_{p^e}` goes through — this is the fragment where Route 1 *succeeds*. **`carry_not_function_of_downshifts`** (PROVED — the obstruction): the pairs `(0,0)` and `(p-1,1)` have identical down-shifts (`⌊·/p⌋ = 0` for both coordinates) yet carries `0` and `1`, so the carry bit is **not a function of the down-shifts** — a sparse theory of `⌊s/p⌋` must track the low residues `a%p, b%p`, not just the down-shifts; the carry couples the residue boundary (which the field observer already sees) *nonlinearly* into the next `p`-adic level. Dynamic-boundary reading: as the modulus refines `MOD_p → MOD_{p^e}`, the observer must refine from the field residue (mod `p`) to the carry-aware `p`-adic filtration, and the carry is the precise extra datum that refinement carries. Honest scope: this is a genuine *structure theory of the carry* — exact decomposition, single-bit bound, the no-carry success fragment, and the carry obstruction — but **not** a sparse/low-degree representation of `⌊s/p⌋` in the general (carry-present) regime over the input bits, which is the open `ACC⁰[composite]` lower bound. Proving full `ACC⁰` needs either a sparse theory of carries or a proof none exists. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CarrySparseTheory` |
| **Route 1 — the count carry is symmetric: `⌊s/p⌋` as a sum of threshold gates** | The carry obstruction of `…ACC0CarrySparseTheory` (carry not a function of the down-shifts) was an artifact of summing two independently-varying parts `a+b`; in the `ACC⁰` setting the argument of every `MOD` gate is a single **count** `s = ∑ᵢ xᵢ`, and the whole down-shift tower `s, ⌊s/p⌋, ⌊s/p²⌋, …` consists of functions of that one `s`, so the obstruction dissolves and a clean symmetric structure appears. **`count_carry_symmetric`** (PROVED): equal-weight inputs `∑xₖ = ∑yₖ` give equal carries `⌊(∑xₖ)/p^i⌋ = ⌊(∑yₖ)/p^i⌋` at every tower level — the count carry is a *symmetric* Boolean function (a function of `s` alone), in direct contrast to the general `a+b` carry. **`count_carry_threshold_card`** (PROVED): `#{j ∈ [1,N] : j·p ≤ s} = min N ⌊s/p⌋` — counting the multiples of `p` below `s` gives the carry (`j·p ≤ s ↔ j ≤ ⌊s/p⌋`, `Nat.le_div_iff_mul_le`). **`count_carry_eq_sum_thresholds`** (PROVED): `⌊s/p⌋ = ∑_{j∈[1,N]} [j·p ≤ s]` whenever `⌊s/p⌋ ≤ N` — the count carry is a **sum of `⌊n/p⌋` threshold (Majority-flavoured) indicators** `[s ≥ j·p]`, i.e. exactly a `SYM` form, the top structure the Beigel–Tarui route targets. **`count_carry_range_le`** (PROVED): `⌊s/p⌋ ≤ ⌊n/p⌋` for `s ≤ n` — a low-range symmetric function, far coarser than the count itself. Honest scope: this is the structural *gain* the count gives over the general `a+b` carry — the count carry is genuinely symmetric, a sum of `⌊n/p⌋` bounded-range thresholds, so the `SYM` top-structure is present; but the gain **re-localises** the difficulty rather than removing it. The thresholds `[s ≥ j·p]` are Majority-flavoured, and a *low-degree* (polynomial-method) handle on such thresholds of the count — composed across the tower — is exactly the open `ACC⁰[composite]` representation. We do **not** prove the thresholds are low-degree, nor that they are not; what is established is that the count carry's symmetric structure is a sum of bounded-range thresholds, isolating the open question to the *degree of a threshold on the count*. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CountCarrySymmetric` |
| **dynamic observer selection — the boundary forces observer refinement (+ the BT-closure socket)** | Formalises the N-Frame dynamic-boundary principle for the `MOD`/`ACC⁰` ladder: the boundary does not merely shrink states, it *forces the observer to refine* exactly when the coarser observer loses the information to evaluate the fragment. An **observer** `O : ℕ → α` reads the count `s = ∑ᵢxᵢ`; **`Sufficient O f := ∀ x y, O x = O y → (f x ↔ f y)`** (`O` carries the minimal state to evaluate `f`); **`Finer O₁ O₀`** (`O₁` distinguishes at least as much). Concrete observers: `resObs p` (residue mod `p`, the field/`MOD_p` observer), `padicObs p e` (the down-shift residue tower `(⌊s/p^i⌋ mod p)_{i<e}`), target `modPow p e` (`MOD_{p^e}`). **`resObs_insufficient_for_modPow`** (PROVED): the residue observer is **not** sufficient for `MOD_{p^e}` (`e≥2`) — `0` and `p` share their residue but `MOD_{p^e}` separates them, so refinement is *forced*. **`padicObs_sufficient_for_modPow`** (PROVED): the `p`-adic carry observer **is** sufficient (via the down-shift AND decomposition `p^e∣s ↔ ∀i<e, p∣⌊s/p^i⌋`), so refinement *succeeds* at the `p`-adic level. **`padicObs_finer_than_resObs`** (PROVED): the `p`-adic observer refines the residue observer (its `i=0` component is the residue). **`observer_refines_modp_to_padic`** (PROVED): the three together — the dynamic boundary refines the `MOD_p` observer to the carry-aware `p`-adic observer for `MOD_{p^e}`. **`carry_observer_eq_threshold_observer`** (PROVED, re-export): the carry observer factors through a Hamming-**threshold** observer (`⌊s/p⌋ = ∑_j [s≥j·p]`) — the next rung. Honest scope: the refinement *ladder* (incidence < residue < `p`-adic carry < threshold) is proved up to the threshold observer; the conceptual **closure** — that repeated refinement stabilises at a *quasipolynomial-size* Beigel–Tarui `SYM∘AND` observer, making BT the fixed point of refinement rather than an external trick — is **not** proved. It is recorded as explicit Prop sockets, and **`dynamic_boundary_to_acc0_sat_speedup`** is the pure-glue cash-out (modus ponens, depends on **no axioms**): `DynamicClosesAtBT → BTHasQuasipolySparse → Speedup`, every premise an unproved socket. Discharging the socket (or refuting it) **is** the open `ACC⁰[composite]` lower bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DynamicObserverSelection` |
| **the BT-closure frontier — the final conditional chain `dynamic closure ⇒ NEXP ⊄ ACC⁰`** | With the dynamic-observer capstone frozen, states the whole reduction as a single conditional theorem ending not at an abstract `Speedup` but at the codebase's actual separation statement `¬ NEXPHasACC0Circuits`. **`dynamicClosure_to_NEXP_not_ACC0`** (PROVED as glue, depends on **no axioms**): given the dynamic-closure socket `hClosure : DynamicClosesAtBT`, the implication `closure_to_quasipoly : DynamicClosesAtBT → BTHasQuasipolySparse`, the speedup implication `quasipoly_to_speedup : BTHasQuasipolySparse → ACC0SatSpeedup`, the Williams meta-glue `williams : ACC0SatSpeedup → NEXPHasACC0Circuits → Collapse`, and the nondeterministic time hierarchy `hierarchy : ¬ Collapse`, it derives `¬ NEXPHasACC0Circuits` — by pure composition through the proved Route-B reduction `…ACC0RouteBComplete.routeB_to_NEXP_not_ACC0`. The chain is exactly `DynamicClosesAtBT → BTHasQuasipolySparse → ACC0SatSpeedup → ¬ NEXPHasACC0Circuits`. **`observer_ladder_proved`** (PROVED, re-export): the proved foundation the chain rests on — the boundary refines the residue (`MOD_p`) observer to the sufficient `p`-adic carry observer for `MOD_{p^e}` (`…ACC0DynamicObserverSelection.observer_refines_modp_to_padic`). Honest scope: this is the **final scaffolding**, not a proof of `NEXP ⊄ ACC⁰`. The chain is valid and machine-checked, but its two load-bearing premises — `DynamicClosesAtBT` (refinement closes at a BT observer) and `closure_to_quasipoly` (that observer is quasipoly-size) — are **unproved sockets**, and together they *are* the open `ACC⁰[composite]` lower bound (equivalently, the Beigel–Tarui/Yao quasipolynomial `SYM∘AND` representation for arbitrary `ACC⁰`). Everything *around* the wall is proved: the observer-refinement ladder up to thresholds (`…DynamicObserverSelection`, `…CountCarrySymmetric`, `…CarrySparseTheory`, `…ValuationSparseTheory`) and the Route-B/Williams reduction (`…ACC0RouteBComplete`). Discharging `DynamicClosesAtBT` is the hard wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BTClosureFrontier` |
| **threshold/BT closure — `DynamicClosesAtBT` split into two proved size-factors + the depth-reduction socket** | **Important framing.** `DynamicClosesAtBT` is, in classical terms, the **Beigel–Tarui / Yao theorem** (every `ACC⁰` function has a quasipolynomial-size `SYM∘AND` representation), and `NEXP ⊄ ACC⁰` (Williams 2011, which uses it) is *also* a **proven** classical theorem. So the wall in this development is the **size of the Lean formalisation**, not mathematical openness — "proving `DynamicClosesAtBT`" means *formalising* a known result, and nothing in this arc is a new separation, an open problem, or `P ≠ NP`. The BT bound is a product of two size-factors. **`symmetric_observer_state_le`** (PROVED): a symmetric observer is a function of the count `s ∈ {0,…,n}`, so it has at most `n+1` states — the `SYM` top gate is *linear*-state, trivially quasipolynomial, not the bottleneck. **`bounded_fanin_feature_count_le`** (PROVED, re-export of `…ACC0BeigelTaruiSparsity.beigelTarui_monomial_count_le`): there are at most `(n+1)^D` monomial-`AND` features of fan-in `≤ D` — quasipolynomial for `D = polylog`, the bottom layer of `SYM∘AND`. **`FanInStaysPolylog D w := w ≤ D`** — the genuine remaining socket: that after reducing an `ACC⁰` circuit to `SYM∘AND` form the bottom AND fan-in stays `≤ D = polylog` (the Beigel–Tarui mixed-radix depth reduction); a *theorem*, not open, but its full Lean proof over arbitrary-depth `ACC⁰` is the large remaining formalisation; **not** proved here. **`quasipoly_BT_observer_of_fanin_preservation`** (PROVED glue): if `FanInStaysPolylog D w` then `(lowDegMonomials n w).card ≤ (n+1)^D` (quasipoly). Net: the two size-factors of the BT bound are proved (linear `SYM` top, quasipoly bounded-fan-in feature layer); `DynamicClosesAtBT` is refined to the single concrete lemma `FanInStaysPolylog`, which we do not prove. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ThresholdBTClosure` |
| **fan-in depth recurrence — the depth induction reducing `FanInStaysPolylog` to one per-layer merge** | Carries out the "induct over circuit depth" step of the Beigel–Tarui depth reduction: it proves that *if* each `ACC⁰` layer multiplies the bottom AND fan-in by at most a factor, *then* constant depth keeps the fan-in (hence the feature count) quasipolynomial — reducing the entry-167 socket `FanInStaysPolylog` to a single per-layer merge lemma. **`fanInAtDepth b factor d := factor^d · b`** is the fan-in after `d` layers; **`fanInAtDepth_succ`** (PROVED): `fanInAtDepth b factor (d+1) = factor · fanInAtDepth b factor d` (each layer multiplies by `factor`). **`fanin_bounded_by_recurrence`** (PROVED, the depth induction): if `layerFanIn 0 ≤ b` and `∀ d, layerFanIn (d+1) ≤ factor · layerFanIn d` (the per-layer merge), then `∀ d, layerFanIn d ≤ fanInAtDepth b factor d` (induction on `d`, `Nat.mul_le_mul_left`). **`fanInAtDepth_le`** (PROVED): for `factor ≤ L`, `d ≤ D`, `1 ≤ L`, `fanInAtDepth b factor d ≤ L^D · b` — polylog when `L, b` are polylog and `D` is *constant* (constant depth is essential; this is precisely why the BT bottom fan-in stays polylog). **`quasipoly_feature_count_of_layer_merge`** (PROVED, chains to entry 167): combining the two, the actual bottom fan-in `layerFanIn d ≤ L^D · b`, so via `…ACC0ThresholdBTClosure.quasipoly_BT_observer_of_fanin_preservation` the bottom AND-feature count is `≤ (n+1)^{L^D·b}` (quasipolynomial). Honest scope: the depth induction and the constant-depth arithmetic are proved; the *single* remaining premise is `hstep` (the per-layer merge `layerFanIn (k+1) ≤ factor · layerFanIn k`, supplied as a hypothesis) — the genuine Beigel–Tarui mixed-radix merge that composing a gate's children's `SYM∘AND` forms multiplies the bottom fan-in by only a polylog factor. That is a *theorem* (BT/Williams are proven classically), not an open problem, but its full Lean proof over a concrete `ACC⁰` gate datatype is the substantial remaining formalisation; we reduce `FanInStaysPolylog` to exactly it and do **not** prove it. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FanInRecurrence` |
| **the per-layer merge over the concrete `ACC0Circuit` datatype — fan-in tracking** | Carries out the per-layer merge concretely over the `ACC0Circuit` inductive datatype (`…ACC0CircuitModel`), refining the existing `HasSymAndForm` (which tracks only the AND *count*) to **`HasSymAndFormFanIn f s w`** = a count gate over `≤ s` monomial-`AND`s *each of fan-in `≤ w`*. Base cases proved: **`…_const`** (`0,0`), **`…_monoAND`** (`1, S.card`), **`…_var`** (`1,1`), **`…_mod`** (`S.card, 1` — `MOD_q` is a symmetric function of single-literal `AND`s). **`hasSymAndFormFanIn_not`** (preserves count and fan-in). The merge **`hasSymAndFormFanIn_combine`** (PROVED): any binary Boolean combiner merges two forms into one of count `s₁+(s₁+1)·s₂` (the multiplicative blow-up) but **fan-in only `max w₁ w₂` — no growth**, because the merged monomials are exactly the children's (the `Sum.elim` + replication construction of the count merge, via `saCount_sum_elim`/`saCount_prod_const`, leaves every literal-count unchanged); corollaries `…_and`/`…_or`. **`acc0circuit_hasSymAndFormFanIn`** (PROVED): every `ACC0Circuit` has such a form with count `symAndSize C` and fan-in `fanInBound C` (structural recursion). **`fanInBound_le_one`** (PROVED): `fanInBound C ≤ 1` for every circuit — the bottom `AND`s of the exact form are single literals. Key honest finding: in the *exact* `SYM∘AND` representation the fan-in is `≤ 1`, so **fan-in is not the Beigel–Tarui bottleneck** — this concretely discharges the fan-in side of `FanInStaysPolylog` (factor `1` in the entry-168 recurrence). The genuine wall re-localises to the **count** `symAndSize` (multiplicative `s₁+(s₁+1)·s₂` at each `AND`/`OR`, hence exponential); taming it to quasipolynomial is the polynomial-method / additive-degree content (`…ACC0AdditiveDegree`, available only approximately and at prime-power `MOD`), the recognised wall — and `DynamicClosesAtBT`/BT are proven classical theorems, so this is formalisation, not an open problem. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymAndFanIn` |
| **the additive count bound — degree-governed count is quasipolynomial; the exact gate-count blows up** | With the wall re-localised (entry 169) to the *count*, this makes the two count regimes explicit and contrasts them. **`count_quasipoly_of_degree_bound`** (PROVED): a count `cnt ≤ (n+1)^{δ^e}` satisfies `cnt ≤ (n+1)^{L^E}` whenever `δ ≤ L` and `e ≤ E` — for a polylog `L` and a *constant* `E` this is `n^{polylog}`, i.e. quasipolynomial; the count is governed by the **degree exponent** `δ^e` (the additive degree recurrence), not by the number of gates. **`compositeBT_count_quasipoly`** (PROVED): applies it to the already-proved `…ACC0CompositeBTCapstone.compositeBT_representation`, whose monomial-support count is `≤ (n+1)^{δ^{depth+1}}` — for polylog gate-degree `δ ≤ L` and constant depth `depth c ≤ D`, the count is `≤ (n+1)^{L^{D+1}}`, quasipolynomial. **`symAndSize_andTree_ge`** (PROVED, the contrast): the *exact* gate-count of a balanced depth-`d` `AND`-tree (`andTree`) is `≥ 2^d` — the multiplicative recurrence `symAndSize (AND a b) = sₐ + (sₐ+1)·s_b` makes the exact `SYM∘AND` count grow at least exponentially, so the exact form is *not* quasipolynomial-size, which is exactly why Beigel–Tarui uses the degree-governed (polynomial-method) count instead. Honest scope: the degree-governed (additive) count bound is quasipolynomial and is proved (built on the proved `compositeBT_representation`); the exact count is shown to blow up. The remaining gap is the *gate-polynomial instantiation* of `compositeBT_representation` for an actual `ACC⁰` circuit — which over a field exists only for prime-power `MOD` (the documented composite-`MOD` obstruction, `…ACC0PrimePowerObstruction`/`…ACC0NonFieldObserverTheory`). BT and `NEXP ⊄ ACC⁰` (Williams 2011) are proven classical theorems, so this is formalisation, not an open problem. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AdditiveCountBound` |
| **the composite-`MOD` field-gate instantiation — squarefree works low-degree, prime-power is the obstruction** | Resolves the remaining gap object — the low-degree field gate for a composite-`MOD` gate to feed `compositeBT_representation` — exactly along the dichotomy proved across entries 151–162. **`squarefree_modGate_all_lowdegree`** (PROVED): each per-prime gate `modPGate p = 1 − X^{p−1}` (`p ∈ S`) has total degree `≤ p−1`. **`squarefree_field_gate_instantiation`** (PROVED): for a finset `S` of *distinct* primes, the low-degree (`≤ p−1`) per-prime Fermat gate family over the product of fields `∏_{p∈S} F_p` decides `MOD_{∏S}` — i.e. `(∀ p∈S, modPGate p fires at s mod p) ↔ (∏ p∈S, p) ∣ s` (built from the proved `…ACC0CRTFinsetGate.modGateProd_decides` + the per-prime degree bound). This is a genuine low-degree field-gate representation of squarefree composite `MOD` — exactly what the BT/polynomial-method route needs, *here*. **`primepower_field_gate_obstruction`** (PROVED, re-affirming `…ACC0PrimePowerObstruction.modPrimePower_not_function_of_modP`): for `p^e` with `e ≥ 2`, `0` and `p` share the mod-`p` residue (`(0:ZMod p)=(p:ZMod p)`) yet `MOD_{p^e}` accepts `0` and rejects `p`, so `MOD_{p^e}` is not a function of the mod-`p` residue and *no* `F_p` polynomial of any degree computes it; `ZMod(p^e)` is not a field, so there is no Fermat-style low-degree indicator. Honest scope: the composite-`MOD` field gate **exists and is low-degree for the squarefree case** and **provably does not exist over a field for prime powers** — the field-gate route splits exactly at squarefree vs prime-power. The full Beigel–Tarui theorem handles `p^e` by a different, non-low-degree-field mixed-radix `SYM∘AND` argument (a proven classical theorem to formalise), and the only mathematically hard object here — a low-degree field indicator for prime-power `MOD` — is proved *not* to exist (honestly, not faked). NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeMODFieldGate` |
| **the end-to-end Beigel–Tarui instantiation — exact circuit polynomial fed into `compositeBT_representation`** | Completes the concrete pipeline `circuit → polynomial → degree bound + quasipoly count + sparse cube sum` by feeding the *exact* multilinear circuit polynomial `subst` (`…ACC0SubstitutionPoly`, proved to compute the circuit on the cube by `subst_eval`) into the proved packaging `compositeBT_representation`, with the concrete exact gate polynomials. **`guP`** / **`gbP`** — the exact unary/binary multilinear gate interpolations; **`guP_deg`** / **`gbP_deg`** (PROVED): both have total degree `≤ 2` (via `totalDegree_add`/`totalDegree_mul`/`totalDegree_neg`, the helper `oneSubX_deg` for `1−Xᵢ`, and `termBd` for `C a·(p·q)`). **`subst_una_eq`** / **`subst_bin_eq`** (PROVED): `subst (.una f c) = aeval ![subst c] (guP f)` and `subst (.bin g a b) = aeval ![subst a, subst b] (gbP g)` — `subst` composes through the gate polynomials via `aeval`, exactly the composition hypotheses `compositeBT_representation` requires. **`endToEnd_BT`** (PROVED): instantiating `compositeBT_representation` with `Q = subst`, `δ = 2` (discharging `hinp`/`hcst` from `totalDegree_X`/`totalDegree_C` and `hgu`/`hgb` from `guP_deg`/`gbP_deg`), for every circuit `c` the exact polynomial `subst c` (a) computes the circuit on the cube, (b) has total degree `≤ 2^{depth+1}`, (c) has `≤ (n+1)^{2^{depth+1}}` distinct monomial-supports (quasipolynomial for bounded depth), and (d) admits the sparse cube-sum read-off. A genuine, concrete, *exact* end-to-end instantiation of the BT packaging; the squarefree composite-`MOD` field gate of entry 171 (degree `≤ p−1`) slots into the same pipeline with `δ = max(2, p−1)`. Honest scope: being the *exact* representation, the count `(n+1)^{2^{depth+1}}` is quasipolynomial only for bounded depth (the exact route's known limitation, entry 170); the quasipoly count for all poly-size circuits needs the *approximate* low-degree gates (the probabilistic polynomial method) and, for `MOD`, the squarefree field gate — with the prime-power case remaining the proven obstruction. BT and `NEXP ⊄ ACC⁰` (Williams 2011) are proven classical theorems ⇒ formalisation, not an open problem. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0EndToEndBT` |
| **the approximate Beigel–Tarui instantiation — polylog degree, quasipolynomial count (the Razborov–Smolensky route)** | Step (a): upgrades entry 172's *exact* bounded-depth pipeline to the genuine BT-style quasipoly route by switching from exact gate polynomials (degree `2^{depth+1}`) to the **probabilistic** RS approximant. It assembles the repo's existing pieces: the concrete approximant `…Layer3.toApprox p t R C` (`∨`/`∧` gates via `genOrApprox`, `MOD_p` via the degree-`(p−1)` Fermat indicator) and its proved degree bound `…Layer3.toApprox_totalDegree_le : (toApprox p t R C).totalDegree ≤ ((p−1)·t)^{depth}`. **`toApprox_monomial_count_le`** (PROVED): the approximant's monomial-support count is `≤ (n+1)^{((p−1)·t)^{depth}}` — degree-governed, each support being a degree-`≤((p−1)·t)^{depth}` feature (`support_mem_lowDeg`) and there being `≤ (n+1)^{…}` such (`beigelTarui_monomial_count_le`). **`approx_endToEnd_BT`** (PROVED): for `(p−1)·t ≤ L` and `depth ≤ D`, the approximant has total degree `≤ L^D` *and* monomial-support count `≤ (n+1)^{L^D}` (degree via `toApprox_totalDegree_le` + `Nat.pow_le_pow_left/right`; count via the entry-170 `count_quasipoly_of_degree_bound`). For a polylog RS error parameter `(p−1)·t = polylog` (so `L = polylog`) and a *constant* depth `D`, this is **degree polylog and count quasipolynomial for every constant-depth circuit** — the genuine BT-style low-degree sparse bound, no longer bounded-depth-only as in the exact route. Honest scope: this is the degree + quasipoly-count half, fully proved from the RS degree bound; the **bounded-error** half is the repo's existing RS probabilistic machinery — the per-gate rate `…Layer3.orApprox_error_rate`/`genOrApprox`, the substitution-error hybrid `…ACC0CircuitSubstitution.circuit_error_bound` (error `≤ size·ε`), and the averaging `…Layer3.exists_form_few_errors` (a fixed low-error approximant exists) — cited here, *not* re-proved and *not* faked into this theorem (the full per-input cube-error assembly is the RS *agreement* content, `parity_function_lower_bound` and the `…Layer3` correlation lemmas). This is the prime-`p` (`AC⁰[p]`) route; squarefree composite `MOD` runs it per prime over `∏ F_p` (entry 171), and prime-power `MOD` is the proven field obstruction (step (b), the non-field mixed-radix route). Beigel–Tarui and `NEXP ⊄ ACC⁰` (Williams 2011) are proven classical theorems ⇒ formalisation, not an open problem. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ApproxBTInstantiation` |
| **prime-power mixed-radix `SYM∘AND` — the non-field route that clears the field obstruction (step b)** | The full Beigel–Tarui handling of prime-power `MOD`: *not* by a low-degree field polynomial (entry 171 proved one cannot exist for `p^e` — `ZMod(p^e)` is not a field) but by the **symmetric / mixed-radix `SYM∘AND` route**, since a `MOD_q` gate of *any* modulus `q` is exactly a symmetric function of its support literals. **`modPrimePower_symAndForm`** (PROVED): `MOD_{p^e}` (= `decide (∑_{i∈S} xᵢ ≡ t (mod p^e))`) has an *exact* `SYM∘AND` form of size `|S|` — instantiating `…ACC0YBTExactCompose.hasSymAndForm_mod` at `q = p^e` — with *no field polynomial*. **`modPrimePower_symAndFanIn`** (PROVED): the same with fan-in `1` (single-literal `AND`s). **`primePower_mixed_radix_combine`** (PROVED): a `MOD_{p^e}` gate combines with any other `SYM∘AND` form under any binary Boolean combiner via the base-`(|S|+1)` mixed-radix merge (`hasSymAndForm_combine`), size `|S| + (|S|+1)·s₂`. **`primePower_sym_clears_field_obstruction`** (PROVED, the contrast): for `e ≥ 2`, `MOD_{p^e}` has an exact `SYM∘AND` form *and* no `F_p` field gate computes it (`(0:ZMod p) = (p:ZMod p)` yet `p^e ∣ 0`, `¬ p^e ∣ p` — `…ACC0PrimePowerObstruction.modPrimePower_not_function_of_modP`). So the entry-171 field obstruction is *field-specific*: the symmetric/mixed-radix route represents `MOD_{p^e}` exactly, which is precisely how Beigel–Tarui sidesteps the obstruction. Honest scope: this is the *exact* representation (size `|S|` per gate, multiplicative under composition, entries 169/170), so it gives a quasipoly-size bound only combined with the approximate `AC⁰` count control of step (a) (entry 173); the full *quasipoly* count for prime-power `MOD` circuits is the genuine BT mixed-radix size analysis (a proven classical theorem to formalise in full). Beigel–Tarui and `NEXP ⊄ ACC⁰` (Williams 2011) are proven classical theorems ⇒ formalisation, not an open problem. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerMixedRadix` |
| **step (3) — the honest partial discharge of `DynamicClosesAtBT`** | Wires the two *proved* BT ingredients into entry 166's socket and isolates the single remaining residual. **`ACComponent`** / **`acComponent_proved`** (PROVED, from entry 173): the AC⁰ Razborov–Smolensky approximant `Layer3.toApprox p t R C` has total degree `≤ L^D` and monomial-support count `≤ (n+1)^{L^D}` for `(p−1)·t ≤ L`, `depth ≤ D` (polylog degree, quasipoly count). **`MODComponent`** / **`modComponent_proved`** (PROVED, from entry 174): a `MOD_{p^e}` gate has an exact `SYM∘AND` form of size `|S|` (the symmetric/mixed-radix route, no field polynomial). **`dynamicClosure_partial_discharge`** (PROVED as glue): `DynamicClosesAtBT` follows from `hSize` applied to the proved components, where `hSize : ACComponent → MODComponent → DynamicClosesAtBT` is the residual **BT size-analysis socket** — the mixed-radix/size argument that combines (a) and (b) into one quasipolynomial-size `SYM∘AND` for an arbitrary `ACC⁰` circuit. **`partialDischarge_to_NEXP`** (PROVED as glue): composes this with the entry-166 chain `…ACC0BTClosureFrontier.dynamicClosure_to_NEXP_not_ACC0` to derive `¬ NEXPHasACC0Circuits` from `hSize` + the closure→quasipoly→speedup bridges + the Williams meta-glue + the time hierarchy. Honest scope: this is an *honest partial* discharge — two of the three BT ingredients are genuinely proved (a, b), and `DynamicClosesAtBT` is reduced to exactly one named residual socket `hSize` (the full Beigel–Tarui size theorem, a *proven classical theorem* but a large formalisation, left as a socket) plus the entry-166 Williams sockets. It is **not** a proof of `DynamicClosesAtBT` or of `NEXP ⊄ ACC⁰`; the residual is named, not faked. The user's 3-step plan (a + b + discharge) is complete as a partial discharge. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DynamicClosureDischarge` |
| **the Beigel–Tarui size theorem over a concrete datatype (prime-`p` route)** | Discharges the *size* content of the entry-175 residual socket `hSize`, concretely over the `BoolCircuitSyntax` datatype. The `SYM∘AND` **size** of a representation is the number of distinct `AND`-features it uses: **`btSymAndSize p P := (P.support.image (fun d => d.support)).card`**. **`QuasipolyBTSize`** / **`quasipolyBTSize_proved`** (PROVED, directly from entry 173's `approx_endToEnd_BT`): for every `BoolCircuitSyntax` circuit `C`, the Razborov–Smolensky approximant `Layer3.toApprox p t R C` has `SYM∘AND` size `btSymAndSize p (toApprox p t R C) ≤ (n+1)^{L^D}` whenever `(p−1)·t ≤ L` and `depth ≤ D` — for a polylog RS error parameter `(p−1)·t = polylog` (so `L = polylog`) and a *constant* depth `D`, this is `n^{polylog}`, **quasipolynomial**. So the BT size theorem holds for the AC⁰[p]/prime route over the concrete datatype: the bottom `AND`-feature count (the `SYM∘AND` size, with the symmetric top reading off the cube sum) is genuinely quasipolynomial. Honest scope: this proves the **size half** of `hSize` for the prime-`p` route; it does **not** discharge `hSize` in full. Two residuals remain, both genuine BT content (proven classically, large to formalise): (1) **correctness/error** — that the size-`btSymAndSize` `SYM∘AND` actually computes (or `1/poly`-approximates) the circuit, the RS *agreement* content (`…Layer3` correlation lemmas, `…ACC0CircuitSubstitution.circuit_error_bound`, `parity_function_lower_bound`), separate from the size; (2) **composite / prime-power modulus** — the prime-`p` approximant computes only `MOD_p` correctly, while squarefree composite runs per prime over `∏ F_p` (entry 171) and prime-power `MOD` uses the exact mixed-radix `SYM∘AND` (entry 174) whose quasipoly size is the genuine BT mixed-radix size analysis. Beigel–Tarui and `NEXP ⊄ ACC⁰` (Williams 2011) are proven classical theorems ⇒ formalisation, not an open problem; nothing here is a new separation or `P ≠ NP` | `…ACC0BTSizeTheorem` |
| **the RS agreement/error bound — the correctness half (the complete RS BT representation)** | Discharges the *correctness/error* half (entry-176 residual (1)) for the AC⁰[p] route, by assembling the repo's already-proved agreement machinery — chiefly `…Layer3.composed_error_le`, the full Razborov–Smolensky agreement guarantee. **`rs_agreement_BT`** (PROVED): for an AC⁰[p] circuit `C` (`hmod`: every `MOD` gate has modulus `p`), there is a form choice `ω : FormSpace p t C` such that the RS agreement-side approximant `Layer3.toAgree p t (oracleOf p t C ω) C` *simultaneously* satisfies: **(a)** total degree `≤ ((p−1)·t)^{depth}` (`Layer3.toAgree_totalDegree_le`); **(b)** `SYM∘AND` size (distinct `AND`-features) `≤ (n+1)^{((p−1)·t)^{depth}}` (`support_mem_lowDeg` + `beigelTarui_monomial_count_le`); **(c)** bounded error — disagrees with the circuit on a set of size `s` with `s · p^t ≤ (#subcircuits)·2^n` (`Layer3.composed_error_le`, error rate `≤ (#subcircuits)·p^{-t}`). So for AC⁰[p] circuits the RS approximant has a **complete Beigel–Tarui representation** — low degree, quasipolynomial size, and small error — all proved *together* for one form choice. Honest scope: this discharges the correctness/error residual for the prime route; the sole remaining residual is (2) **composite / prime-power modulus** — `composed_error_le`'s hypothesis `hmod` covers only `q = p` (`MOD_p`), so the agreement holds for AC⁰[p]; squarefree composite runs per prime over `∏ F_p` (entry 171), and prime-power `MOD` uses the exact mixed-radix `SYM∘AND` (entry 174) whose quasipoly size is the BT mixed-radix analysis. Beigel–Tarui and `NEXP ⊄ ACC⁰` (Williams 2011) are proven classical theorems ⇒ formalisation, not an open problem. NOT full `DynamicClosesAtBT` (composite modulus remains), NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RSAgreementBound` |
| **composite mixed-radix quasipoly-size — combining a constant number of forms stays quasipoly** | Discharges the *size* content of the composite residual (entry-177 residual (2)): for a squarefree composite modulus `m`, `MOD_m = ⋀_{i} MOD_{pᵢ}` over the distinct prime factors (CRT, entry 171), and combining the per-prime `SYM∘AND` forms via the mixed-radix merge stays quasipoly-size for a *fixed* `m`. **`hasSymAndForm_mono`** (PROVED): `HasSymAndForm` is monotone in the size bound. **`mrBound Q`** is the mixed-radix size recurrence (one merge step `s ↦ Q + (Q+1)·s`, from `hasSymAndForm_combine`); **`mrBound_eq`** (PROVED): the closed form `mrBound Q k + 1 = (Q+1)^k`, i.e. `mrBound Q k = (Q+1)^k − 1`; **`mrBound_le_pow`** (PROVED): `mrBound Q k ≤ (Q+1)^k`. **`andAll`** is the `AND` of a list of Boolean functions; **`hasSymAndForm_andAll`** (PROVED): the `AND` of a list of `SYM∘AND` forms (each of size `≤ Q`) has a `SYM∘AND` form of size `≤ mrBound Q (forms.length)` (iterated `hasSymAndForm_combine`, by list induction). **`mixedRadix_quasipoly_size`** (PROVED): the `AND` of `forms` (each `≤ Q`) has `SYM∘AND` size `≤ (Q+1)^{forms.length}`. For the `ω(m)` distinct prime factors of a fixed squarefree modulus `m` — a *constant* — and per-prime quasipolynomial size `Q`, `(Q+1)^{k}` is quasipolynomial: the combined composite representation is `(quasipoly)^{constant} = quasipoly`, **not** exponential, precisely because the number of moduli is fixed. Combined with entry 177 (per-prime correctness over each `F_{pᵢ}`), the composite `ACC⁰` BT *size* is assembled. Honest scope: this is the size analysis (combining a constant number of quasipoly forms via mixed-radix); it does not itself perform the per-prime RS approximation of a whole composite circuit (entry 177 applied over each `F_{pᵢ}`), and it relies on `m` being fixed so its prime-factor count is a constant. Beigel–Tarui and `NEXP ⊄ ACC⁰` (Williams 2011) are proven classical theorems ⇒ formalisation, not an open problem. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeMixedRadixSize` |
| **the final integration — `DynamicClosesAtBT` PROVED for the AC⁰[p] route, chained to `¬ NEXP`** | The culmination of the BT-closure arc (entries 166–178): turns `DynamicClosesAtBT` from an abstract socket into a *proved theorem* for the AC⁰[p] route, and chains it to the separation. **`DynamicClosesAtBT_AC0p`** (concrete definition): every AC⁰[p] `BoolCircuitSyntax` circuit `C` (every `MOD` gate has modulus `p`) has a *complete* Beigel–Tarui representation — a form choice `ω` whose RS approximant `Layer3.toAgree p t (oracleOf p t C ω) C` simultaneously has polylog degree `≤ ((p−1)·t)^{depth}`, quasipolynomial `SYM∘AND` size `≤ (n+1)^{((p−1)·t)^{depth}}`, and bounded error `· p^t ≤ (#subcircuits)·2^n`. **`dynamicClosesAtBT_AC0p_proved`** (PROVED): this holds *universally*, directly from `rs_agreement_BT` (entry 177) — so for the AC⁰[p] route `DynamicClosesAtBT` is a genuine *theorem*, not a socket. **`acc0p_BTclosure_to_NEXP`** (PROVED): plugging the proved closure into the entry-166 chain `…ACC0BTClosureFrontier.dynamicClosure_to_NEXP_not_ACC0`, `¬ NEXPHasACC0Circuits` follows from the **Williams sockets alone** (`closure_to_quasi`, `quasi_to_speed`, `williams`, `hierarchy`) — the entire Beigel–Tarui (closure) side is discharged for AC⁰[p]. Honest scope: for the **AC⁰[p] route** the BT closure is now genuinely proved (the complete BT representation), and the chain to `¬ NEXP` rests only on the Williams meta-theorem sockets, which are themselves `NEXP ⊄ ACC⁰`-strength named classical theorems (Williams 2011), not BT content. For **arbitrary `ACC⁰`** (composite modulus) the closure is not collapsed into this single theorem — it is assembled from the proved per-route pieces (CRT per-prime, entry 171; prime-power exact mixed-radix `SYM∘AND`, entry 174; per-prime complete representation over each `F_{pᵢ}`, entry 177; mixed-radix quasipoly size for the constant prime-factor count, entry 178), with the cross-route bookkeeping (decomposing a composite circuit's correctness per prime) as the remaining integration. Beigel–Tarui and `NEXP ⊄ ACC⁰` (Williams 2011) are proven classical theorems ⇒ formalisation, not an open problem. NOT a new separation, NOT `NEXP ⊄ ACC⁰` (the Williams sockets remain), NOT `P ≠ NP` | `…ACC0DynamicClosesAtBTComplete` |
| **composite cross-route bookkeeping — the squarefree-composite BT closure, assembled** | Does the composite analogue of the entry-179 AC⁰[p] integration: assembles the squarefree-composite closure from the proved per-route pieces, stating the two halves explicitly. **`composite_count_decomposes`** (PROVED, entry 171's `prod_primes_dvd_iff`): for a finset `S` of distinct primes, `(∏ p∈S, p) ∣ cnt ↔ ∀ p∈S, p ∣ cnt` — the squarefree composite `MOD_{∏S}` *is* the per-prime conjunction `⋀_{p∈S} MOD_p`. **`composite_decide_decomposes`** (PROVED): its `decide`/Boolean form (`decide ((∏ p∈S, p) ∣ cnt) = true ↔ ∀ p∈S, p ∣ cnt`) — the composite `MOD_{∏S}` indicator on the count equals the conjunction of the per-prime indicators. **`composite_quasipoly_size`** (PROVED, entry 178's `mixedRadix_quasipoly_size`): the `AND` (`andAll`) of the per-prime `SYM∘AND` forms (each of size `≤ Q` — from the per-prime exact form of entry 174 or the per-prime RS representation of entry 177) has `SYM∘AND` size `≤ (Q+1)^{|forms|}`, quasipolynomial for a *constant* number of prime factors `|S|` and quasipolynomial per-prime size `Q`. Together: the squarefree composite `MOD_{∏S}` decomposes (CRT) into `|S|` per-prime tests, and their `AND` is a quasipoly-size `SYM∘AND` — so the composite gate has a quasipoly Beigel–Tarui representation, built entirely from the proved AC⁰[pᵢ] pieces over the distinct primes. Honest scope: this is the cross-route *bookkeeping* — the two assembled halves (CRT decomposition + mixed-radix quasipoly size) that together give the composite gate a quasipoly BT representation; it does not itself run the per-prime RS approximation of a whole composite *circuit* over each `F_{pᵢ}` (that is entry 177 applied per prime, then combined here), and relies on `m` being fixed so its prime-factor count is a constant. Beigel–Tarui and `NEXP ⊄ ACC⁰` (Williams 2011) are proven classical theorems ⇒ formalisation, not an open problem. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeCrossRoute` |
| **the physical universal `hstep` — decode → step → re-encode loop correctness, assembled** | The Williams-side mountain (`hstep`): the universal machine `U` simulates a machine `M` from `M`'s code on `U`'s own tape, performing one `M`-step as *decode → apply rule → re-encode*. The sub-machine contracts are proved in their own files (tape encode/decode round-trip `…ACC0TapeEncoding`/`…ACC0UniversalDecode`, rule lookup `…ACC0RuleLookup`, head movement `…ACC0HeadLocation`, tape rewrite `…ACC0TapeRewrite`, atomic single step `…ACC0PhysicalStep`); this file assembles them into the loop's correctness. **`univSimStep M c t`** is the encoded one-step universal transition: decode the tape `encodeSim M c`, apply the firing rule `t`, re-encode to `encodeSim M (applyTrans c t)`. **`univ_hstep_correct`** (PROVED): for a firing rule `t` at `c` (`t ∈ M`, `t.1 = (c.1, readSym c)`), the loop is faithful — (a) `decodeSim (encodeSim M c) = some (M, c)` (decode round-trip), (b) `reachIn (toNTM M) 1 c (applyTrans c t)` (a genuine one-step `M`-transition, from `firing_rule_step`), and (c) `decodeSim (univSimStep M c t) = some (M, applyTrans c t)` (re-encode round-trips) — so iterating `univSimStep` faithfully tracks `M`'s run. **`univ_hstep_concreteStep`** (PROVED): the re-encoded configuration `applyTrans c t` is reached from `c` by `concreteStep M`. Honest scope: this proves the `hstep` **correctness contract** — the decode → step → re-encode cycle is faithful and round-trips — by assembling the proved sub-machine contracts. It does **not** realise `univSimStep` as `U`'s *own* transition rules with an explicit step-overhead bound `B` (so that `U` performs each `M`-step in `B` of its own physical steps); that physical realisation of decode/lookup/rewrite as `U`-transitions, with the `B` bound, is the remaining large construction (the genuine `hstep` *machine* vs. its *correctness contract*). This is classical Turing-machine engineering, not an open problem; nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0UniversalHStep` |
| **the physical `hstep` realization with explicit overhead `B`** | Turns the entry-181 semantic `hstep` contract into an actual step-bounded universal-machine simulation lemma. The universal step is realised in four phases over `U`'s own transitions — decode (`c0 → c1`), rule-lookup (`c1 → c2`), tape-rewrite (`c2 → c3`), re-encode (`c3 → c4`). **`hstep_realized_with_overhead`** (PROVED): given the four per-phase reachabilities `reachIn U bDecode c0 c1`, `reachIn U bLookup c1 c2`, `reachIn U bRewrite c2 c3`, `reachIn U bReencode c3 c4` (each phase a run of `U`'s own transitions within its bound), `U` reaches `c4` from `c0` within `B = bDecode + bLookup + bRewrite + bReencode` steps — proved by composing the phases with the already-proved additive `…ACC0NTM.reachIn_add`. **`hstep_exists_overhead`** (PROVED): hence `∃ B, reachIn U B c0 c4`, with the explicit overhead `B` equal to the phase sum — the universal machine performs one simulated `M`-step in `B` of its own steps, the `B`-bounded form of `hstep`. Honest scope: this proves the **overhead composition** — *if* each of the four phases (decode / rule-lookup / tape-rewrite / re-encode) is realised as a run of `U`'s own transitions within its bound, *then* the whole universal step runs within the explicit `B = ∑` of those bounds, turning the semantic contract into a step-bounded lemma. The four per-phase reachabilities are taken as hypotheses: they are the genuine remaining construction — building `U`'s actual transition table so that each phase is a real `U`-transition sequence over the `encodeTape` layout (the proved sub-machine *contracts* of `…ACC0TapeEncoding`/`…ACC0RuleLookup`/`…ACC0TapeRewrite`/`…ACC0UniversalDecode` specify *what* each phase computes; realising them as transitions with the per-phase step counts is the remaining classical TM engineering). What is established here is that *once each phase is realised*, the overhead is exactly the additive sum. Classical Turing-machine construction, not an open problem; nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0UniversalHStepMachine` |
| **the universal rewrite phase — a concrete `reachIn` realizing the tape write** | Discharges the first of entry 182's four per-phase reachabilities (the *rewrite* phase) with a *concrete machine* and an *actual* `reachIn`, rather than as a hypothesis. **`rewriteRule c q' w m`** is the one transition `((c.1, readSym c), (q', w, m))` (at `c`'s state reading `c`'s current symbol, write `w`, move `m`, go to state `q'`); **`rewriteMachine c q' w m := [rewriteRule c q' w m]`** is the concrete one-rule machine. **`rewrite_phase`** (PROVED): `reachIn (toNTM (rewriteMachine c q' w m)) 1 c (applyTrans c (rewriteRule c q' w m))` — the rewrite happens in exactly **one** physical step (`…ACC0PhysicalStep.firing_rule_step`, the rule fires) — *and* `(applyTrans c (rewriteRule c q' w m)).2.2.getD c.2.1 false = w` — the read-after-write guarantee that the rewritten configuration's head holds the written symbol `w` (`…ACC0TapeRewrite.applyTrans_write`). So the rewrite phase of entry 182 is now an *actual* `reachIn U bRewrite c (rewritten c)` for a concrete `U` (the gadget), with explicit `bRewrite = 1` and the tape content certified. Honest scope: this realizes the rewrite phase's *core operation* — write a symbol, move the head, change state — as an actual transition of a concrete machine, with a proved `reachIn` step count and the proved tape-content guarantee. It does **not** embed this gadget into the *universal* `U` so that it rewrites the *encoded* simulated tape (the `encodeTape` layout) at the encoded head position — splicing the gadget into `U`'s transition table over the encoding is the remaining step (the analogous decode / lookup / re-encode phases, which touch the layout, are heavier). Classical Turing-machine construction, not an open problem; nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0UniversalRewritePhase` |
| **the universal lookup phase — the rule scan finds the firing rule, with scan-cost bound** | Discharges the second of entry 182's four per-phase reachabilities (the *lookup* phase) using the proved rule-lookup contract (`…ACC0RuleLookup`: `matchingRules`, `mem_matchingRules`, `concreteStep_iff_matching`, `matchingRules_length_le`). **`lookup_fires`** (PROVED): a rule found by the scan fires in one step — `t ∈ matchingRules M c.1 (readSym c) → reachIn (toNTM M) 1 c (applyTrans c t)` (the rule genuinely matches by `mem_matchingRules`, so `firing_rule_step` applies). **`lookup_phase`** (PROVED): when a step `concreteStep M c d` exists, the rule-table scan `matchingRules M c.1 (readSym c)` contains a rule `t` such that (a) `reachIn (toNTM M) 1 c (applyTrans c t)` (it fires in one physical step), (b) `d = applyTrans c t` (it reaches the intended next configuration), and (c) `(matchingRules M c.1 (readSym c)).length ≤ M.length` (the scan examines at most the whole table). So the lookup phase of entry 182 is realized as an actual reachability with the scan-cost bound `bLookup ≤ M.length`. Honest scope: this realizes the lookup's *core operation* — the scan of the transition table finds the firing rule, the found rule advances the configuration in one step, and the scan cost is bounded by the table size. It does **not** realise the scan as the *universal* machine `U` walking the *encoded* transition table on its own tape (the `encodeTape` layout) and matching the encoded `(state, sym)`; splicing the scan into `U`'s transition table over the encoding, with the exact per-rule step count, is the remaining step. Classical Turing-machine construction, not an open problem; nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0UniversalLookupPhase` |
| **the universal decode phase — faithful, unambiguous recovery of `(M,c)` from the tape** | Discharges the third of entry 182's four per-phase reachabilities (the *decode* phase) — the first that touches the tape *layout* — using the proved tape-encoding contracts (`…ACC0TapeEncoding`: `decodeTape_encodeTape`, `encodeTape_inj`) and the decode round-trip (`…ACC0UniversalDecode`). **`encodeSim_inj`** (PROVED): the simulation-tape encoding is injective — `encodeSim M c = encodeSim M' c' → M = M' ∧ c = c'`, since `encodeSim M c = encodeTape (machineEquiv M) c`, so `encodeTape_inj` gives equal codes and configs and `machineEquiv.injective` recovers the machine; the encoded tape determines `(M, c)` uniquely. **`decode_phase`** (PROVED): for a step `concreteStep M c d`, (a) `decodeSim (encodeSim M c) = some (M, c)` (round-trip recovery, `decodeSim_encodeSim`), (b) `reachIn (toNTM M) 1 c d` (the decoded machine reaches `d` in one step, `decoded_machine_steps`), and (c) the recovery is unambiguous (`∀ M' c', encodeSim M c = encodeSim M' c' → M = M' ∧ c = c'`, `encodeSim_inj`). So the decode phase of entry 182 is realized as a faithful, invertible recovery wired to the simulated step. Honest scope: this proves the decode's *correctness contract* — the recovery is faithful (round-trip) and unambiguous (injective), and connects to the simulated machine's step; with the rewrite (entry 183) and lookup (entry 184) phases, three of the four entry-182 phases now have proved realizations. It does **not** realise the parse as the universal machine `U` *scanning* the `encodeTape` bit-layout with its own transitions and a per-symbol step count (the `bDecode` step count over the layout); that tape-walking realisation is the remaining step. Classical Turing-machine construction, not an open problem; nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0UniversalDecodePhase` |
| **the universal re-encode phase — write the post-step state back, completing the four phases** | Fourth and last of entry 182's per-phase realizations (after rewrite 183, lookup 184, decode 185). The re-encode phase writes the post-step state `(M, d)` back as `encodeSim M d`; being the inverse of decode, the encoding contracts already cover it. **`reencode_phase`** (PROVED): the post-step tape is canonical — `decodeSim (encodeSim M d) = some (M, d)` (it round-trips, `decodeSim_encodeSim`) and `∀ M' d', encodeSim M d = encodeSim M' d' → M = M' ∧ d = d'` (it is the *unique* tape encoding `(M, d)`, `encodeSim_inj` of entry 185) — so writing it back faithfully records the post-step simulation state. **`universal_step_complete`** (PROVED): all four phases assembled into the full tape-level universal step — for a firing rule `t` at `c` (`t ∈ M`, `t.1 = (c.1, readSym c)`), the tape `encodeSim M c` decodes to `(M, c)`, a genuine one-step `M`-transition reaches `applyTrans c t` (`reachIn (toNTM M) 1 …`, from `firing_rule_step`), and the re-encoded tape `encodeSim M (applyTrans c t)` decodes to `(M, applyTrans c t)` and is the unique such tape — the complete decode → step → re-encode cycle, faithful and round-tripping. Honest scope: this completes the four per-phase *correctness* realizations, and `universal_step_complete` assembles them into the full tape-level universal step. What remains — as for the other phases — is realising the re-encode (and the parse/scan) as the universal `U`'s *own* transitions writing the `encodeTape` bit-layout with explicit per-symbol step counts; combined with entry 182's overhead composition, that yields the fully physical `hstep` *machine* with its `B` bound. Classical Turing-machine construction, not an open problem; nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0UniversalReencodePhase` |
| **the fast-SAT verifier — a correct, quasipolynomial-work SAT decider from the `SYM∘AND` representation** | Williams mountain 2/3: turns a `SYM∘AND` representation (the Beigel–Tarui closure output) into a SAT decider that beats brute force, packaging the *verifier* on top of `…ACC0WilliamsFastSat`'s count-cell combinatorics. **`fast_sat_verifier`** (PROVED): for a symmetric top `h` over a degree-`≤D` injective monomial-`AND` family `mono` (`hn : 1 ≤ n`), (a) SAT is decided by the **count-cell image** with no `2^n` enumeration — `Satisfiable (symEval (fun j x => monoAND (mono j) x) h) ↔ ∃ c ∈ image (gateCount …), h c = true` (correctness, `…ACC0ResidueObserver.observed_sat_iff`); and (b) the verifier's work, the size of that count-cell image, is `≤ (D+1)·n^D + 1` — quasipolynomial for `D = polylog`, *unconditional* in any speedup parameter `k` (bounded via `…ACC0SymmetricObserver.sym_count_card_le` + `fastSatWork_le_of_degree` + `lowDegMonomialCount_le_pow`). **`fast_sat_beats_bruteforce`** (PROVED): if the quasipoly work `(D+1)·n^D + 1 < 2^n` (as for polylog `D`), then the count-cell image has `< 2^n` cells — the verifier examines strictly fewer than `2^n`, faster than brute-force enumeration. Honest scope: this is the verifier — a *correct* SAT decision (count-cell search) with *explicit quasipolynomial work* and the strict beats-brute-force consequence; the savings-`2^k` form is already in `…ACC0WilliamsFastSat` (`symAnd_williams_fastSat`/`…_quasipoly`). It does **not** turn the verifier into a *uniform* `ACC⁰`-SAT algorithm realised on a machine within a time bound (the `UniformWilliamsRealizationSocket`, itself `NEXP ⊄ ACC⁰`-strength per `williams_socket_iff_separation`), nor does it supply the `SYM∘AND` representation itself (that is the BT closure, entry 179 for AC⁰[p]). Williams 2011 is a proven classical theorem ⇒ formalisation, not an open problem. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FastSATVerifier` |
| **the Williams contradiction — IKW/NW + fast SAT ⇒ `NEXP ⊄ ACC⁰`, the final capstone** | Williams mountain 3/3, assembling the complete contradiction: if `NEXP ⊆ ACC⁰`, then (IKW easy-witness lemma) accepting `NTIME[2ⁿ]` computations have small `ACC⁰`-witnesses, which (guess-and-verify, using the fast `ACC⁰`-SAT of the Beigel–Tarui `SYM∘AND` representation) collapse `NTIME[2ⁿ] ⊆ NTIME[2ⁿ/superpoly]`, contradicting the nondeterministic time hierarchy — hence `NEXP ⊄ ACC⁰`. **`williams_contradiction`** (PROVED as glue, depends on **no axioms**): `¬ (NEXP ⊆ ACC⁰)` from the BT closure `hClosure : DynamicClosesAtBT` (proved for AC⁰[p], entry 179) bridged to the `ACC⁰`-SAT speedup by `closure_to_speedup` (the BT → fast-SAT route, entries 179/187 plus the uniform realization), the IKW easy-witness lemma `ew`, guess-and-verify / NW-derandomisation `gv`, and the nondeterministic time hierarchy `hierarchy` — pure composition through `…ACC0EasyWitness.nexp_not_acc0_from_witness_parts` (the proved Williams glue) with the speedup supplied by the closure. Honest scope: this is the **complete Williams contradiction assembled**, reducing `NEXP ⊄ ACC⁰` to a precise set of named ingredients — the BT closure (proved for AC⁰[p], entry 179) and four genuinely-deep classical sockets: `closure_to_speedup` (the bridge from a quasipoly `SYM∘AND` representation to a *uniform* fast `ACC⁰`-SAT algorithm, subsuming the `NEXP ⊄ ACC⁰`-strength `UniformWilliamsRealizationSocket`), the **IKW** easy-witness lemma, **guess-and-verify / NW** derandomisation, and the **nondeterministic time hierarchy**. These four are *proven classical theorems* (Williams 2011, Impagliazzo–Kabanets–Wigderson, Nisan–Wigderson, the time hierarchy), each a major formalisation, left as named sockets; the composition is proved glue. This is **not** a proof of `NEXP ⊄ ACC⁰` from nothing — that is already Williams' theorem — but its formalisation *architecture* with the deep ingredients socketed; nothing here is a new separation or `P ≠ NP` | `…ACC0WilliamsContradiction` |
| **IKW refined — exposing the Nisan–Wigderson derandomisation socket** | Refines the entry-150 IKW decomposition. That entry split IKW into `NoEasyWitnessHardFn` (no easy witness ⇒ hard function) + `HardFnSeparation` (hard function ⇒ `¬(NEXP⊆ACC⁰)`); the second socket *bundled* the two genuinely distinct classical steps — **Nisan–Wigderson derandomisation** (hardness ⇒ a PRG defeating `ACC⁰`) and the **Karp–Lipton collapse** (derandomisation + circuit upper bound ⇒ the separation). This file exposes them separately. **`NWDerandomization HardFunction Derandomization := HardFunction → Derandomization`** — the NW socket; **`DerandKarpLiptonSeparation NEXP ACC0 Derandomization := Derandomization → ¬(NEXP⊆ACC0)`** — the Karp–Lipton socket. **`hardFnSeparation_from_NW`** (PROVED, depends on **no axioms**): `NWDerandomization` + `DerandKarpLiptonSeparation` ⇒ `HardFnSeparation`, by `fun hf => kl (nw hf)` — a hard function derandomises (NW), and the derandomisation collapses to the separation (Karp–Lipton). **`easyWitness_from_NW`** (PROVED): the IKW easy-witness lemma from the *refined* ingredients — `NoEasyWitnessHardFn` + `NWDerandomization` + `DerandKarpLiptonSeparation` ⇒ `ACC0EasyWitness.EasyWitnessLemma`, composing `hardFnSeparation_from_NW` into the entry-150 contrapositive `easyWitness_from_parts`. So the IKW chain now names the **Nisan–Wigderson** derandomisation explicitly. Honest scope: this refines the IKW socket by naming NW as its own ingredient; the glue is proved (no axioms for `hardFnSeparation_from_NW`). The three classical pieces — the no-easy-witness-to-hardness step, **Nisan–Wigderson** (hardness ⇒ PRG), and the **Karp–Lipton** derandomisation-to-separation collapse — remain named sub-sockets, each a *proven classical theorem* requiring circuit-complexity / pseudorandomness infrastructure absent here. This does **not** prove IKW or NW (both proven theorems); it exposes the NW core in the formalisation architecture. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0IKWNisanWigderson` |
| **the Nisan–Wigderson generator — combinatorial design (proved) + hybrid argument (socketed)** | Decomposes the entry-189 NW socket `NWDerandomization` into its two genuine classical components — a *combinatorial design* and the *hybrid argument* — proving the design base case and the decomposition glue. **`disjoint_design`** (PROVED): a genuine NW design, the *disjoint base case* — for any `m, k`, there exist `m` sets each of size `k` that are pairwise disjoint, namely the blocks `S i = Finset.Ico (i·k) (i·k+k)` (size `k` by `Nat.card_Ico`; pairwise disjoint by `Finset.disjoint_left` + the block ordering `i·k+k ≤ j·k` for `i<j`, proved via `calc i·k+k = (i+1)·k ≤ j·k` and `omega`). This is a real Nisan–Wigderson design with pairwise intersection `0`. **`NWGeneratorFromDesign Design HardFunction Derandomization := Design → HardFunction → Derandomization`** — the hybrid-argument socket: a design together with a hard function yields the derandomisation (the generator `G(y) = (f(y|_{S₁}), …, f(y|_{Sₘ}))` fools `ACC⁰`, since a circuit distinguishing `G` from uniform would give a small circuit for `f` — the NW hybrid argument). **`nwDerandomization_from_design`** (PROVED, depends on **no axioms**): a design `hd : Design` together with the hybrid argument `hyb` gives `NWDerandomization HardFunction Derandomization` (`fun hf => hyb hd hf`) — composing the entry-189 NW socket out of the design and the hybrid analysis. Honest scope: this decomposes the NW socket into a combinatorial design (proved here for the disjoint base case) and the hybrid argument (a named socket). The disjoint design is a *real* construction but the *inefficient* one (seed length `m·k`); the genuine NW generator needs the *low-intersection* designs (seed length `O(k²/log m)`, intersections bounded but nonzero) and the hybrid argument — both classical results (Nisan–Wigderson 1994) requiring circuit-complexity infrastructure absent here, left as the named socket. This does **not** prove the NW generator; it builds the combinatorial base case and the decomposition glue. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonGenerator` |
| **the efficient Nisan–Wigderson design — low intersection from polynomial graphs (PROVED)** | The quantitative refinement of the entry-190 disjoint base case: proves the **combinatorial heart of the efficient NW design** — the low-intersection property of the polynomial-graph family — completely and from first principles. Over a finite field `F`, the graphs of the `|F|^k` distinct degree-`<k` polynomials form `|F|^k` sets, each of size exactly `|F|`, with **pairwise intersection `< k`**; this is the optimal NW design (seed `|F|² = O(k²/log m)` for `m = |F|^k` sets), the source of the NW seed-length bound. **`poly_agree_le_natDegree`** (PROVED): two distinct polynomials `p ≠ q` agree (`p(a)=q(a)`) on at most `(p−q).natDegree` field points — the agreement set is exactly the root set of the nonzero polynomial `p−q`, so the entry-161 field root-count bound `field_root_card_le_natDegree` applies (`refine field_root_card_le_natDegree (p−q) _ (sub_ne_zero.mpr hpq) ?_; intro a ha; rw [mem_filter] at ha; rw [eval_sub, ha.2, sub_self]`). **`poly_agree_lt_of_degree_lt`** (PROVED): if additionally `deg p, deg q < k` then agreement `< k` (`lt_of_le_of_lt … ; natDegree_sub_le p q; omega`). **`nwSet p := univ.image (fun a => (a, p.eval a))`** — the graph (design block) of a polynomial. **`nwSet_card`** (PROVED): every block has size `|F|` (`card_image_of_injective` — recover `a` from the first coordinate — `+ card_univ`). **`nwSet_inter_le`** (PROVED): the intersection of two graphs is bounded by the number of agreement points (the intersection injects into the agreement filter via `card_le_card` + `card_image_le`, destructuring the two image memberships with `Prod.mk.injEq`). **`nw_low_intersection_design`** (PROVED, depends on **no axioms** beyond `propext, Classical.choice, Quot.sound`): the capstone — distinct degree-`<k` polynomials `p ≠ q` give graphs of size exactly `|F|` with pairwise intersection `< k` (`⟨nwSet_card p, nwSet_card q, lt_of_le_of_lt (nwSet_inter_le p q) (poly_agree_lt_of_degree_lt …)⟩`). Honest scope: this proves the **design** — the low-intersection property of the polynomial-graph family — completely, the quantitative refinement of entry 190's disjoint base case. What remains a named socket (entry 189/190 `NWGeneratorFromDesign`) is the **hybrid argument**: that a design + a hard function gives a generator fooling `ACC⁰` (a circuit distinguishing the generator yields a small circuit for the hard function), which needs circuit-complexity infrastructure absent here. This proves the *design*, not the *generator*. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonLowIntersection` |
| **the Nisan–Wigderson hybrid argument — the telescoping heart (PROVED) + the reduction sockets** | Opens the entry-190 `NWGeneratorFromDesign` socket (design + hard fn ⇒ a generator fooling `ACC⁰`) and proves its genuine analytic/combinatorial **heart**, the hybrid telescoping lemma. The full NW hybrid argument: a distinguisher with *global* advantage `ε` between the generator output (hybrid `0`) and uniform (hybrid `m`) ⟹ by telescoping, an *adjacent* hybrid pair is distinguished with advantage `≥ ε/m` ⟹ (Yao) a next-bit predictor for the hard function `f` on the active block ⟹ (reconstruction, hardwiring the other `m−1` blocks as small tables — possible *because* the low-intersection design gives each only `<k` shared bits) a small circuit for `f` ⟹ contradiction with hardness; hence no distinguisher, the generator fools `ACC⁰`. **`GlobalAdvantage f m ε := ε ≤ |f m − f 0|`**, **`AdjacentAdvantage f m ε := ∃ i, i<m ∧ ε/m ≤ |f(i+1)−f i|`** (`f i` = the distinguisher's acceptance probability on hybrid `i`). **`hybrid_advantage`** (PROVED, depends on **no axioms** beyond `propext, Classical.choice, Quot.sound`): `0<ε` and `GlobalAdvantage f m ε` ⇒ `AdjacentAdvantage f m ε`. Proof: first `0<m` (else `|f 0−f 0|=0<ε`, impossible); `by_contra` + `push_neg` gives every gap `<ε/m`; then `∑_{i<m}|f(i+1)−f i| < ∑_{i<m}(ε/m) = m·(ε/m) = ε` (via `Finset.sum_lt_sum_of_nonempty`, `Finset.sum_const`, `field_simp`); but `|f m−f 0| = |∑(f(i+1)−f i)| ≤ ∑|f(i+1)−f i|` (`Finset.sum_range_sub` + `Finset.abs_sum_le_sum_abs`), so `ε ≤ |f m−f 0| < ε`, `linarith` ⊥. **`low_intersection_table_card`** (PROVED): `r < k ⇒ Fintype.card (Fin r → Bool) = 2^r ≤ 2^k` (`Fintype.card_fun` + `Fintype.card_bool` + `Nat.pow_le_pow_right`) — the *quantitative payoff* of entry 191: a block sharing `r<k` seed bits is a Boolean function of `r` bits, hardwirable as a truth table of `≤2^k` entries, so the low-intersection design keeps reconstruction efficient. **`YaoPredictor`**, **`Reconstruction`**, **`HardnessExcludesCircuit`** — the three named sub-sockets (Yao distinguisher↔predictor; predictor + design ⇒ small circuit; hard fn has no small circuit). **`nw_hybrid_no_distinguisher`** (PROVED): `hybrid_advantage` + the three sockets + a design + a hard fn ⇒ a `GlobalAdvantage` is `False` (`hard hHard (recon (yao (hybrid_advantage …)) hdesign)`). **`nwGeneratorFromDesign_via_hybrid`** (PROVED): under the three sub-sockets, `NWGeneratorFromDesign LowIntersectionDesign HardFunction (¬ GlobalAdvantage f m ε)` — i.e. a design + a hard fn give the derandomisation (no `ε`-distinguisher), **discharging the entry-190 `NWGeneratorFromDesign` socket** with the telescoping engine proved. Honest scope: proves the hybrid argument's **combinatorial/analytic engine** (`hybrid_advantage`), the **low-intersection payoff** (`low_intersection_table_card`), and the **composition glue** (discharging the entry-190 socket). The two model-dependent steps — **Yao** (`YaoPredictor`, distinguisher↔predictor, Yao 1982) and **reconstruction** (`Reconstruction`, predictor + design ⇒ small circuit, the hardwiring argument), plus **hardness** (`HardnessExcludesCircuit`) — remain named sub-sockets, each a proven classical fact (Nisan–Wigderson 1994, Yao) needing circuit-complexity / probability infrastructure absent here. This proves the hybrid argument's *engine* and *decomposition*, not the full generator. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonHybrid` |
| **Yao's distinguisher↔predictor theorem — the predict-from-distinguish identity (PROVED)** | Discharges the first entry-192 sub-socket (`YaoPredictor`: an adjacent-hybrid distinguishing advantage ⇒ a next-bit predictor — Yao 1982) by proving its genuine quantitative **heart**, the predict-from-distinguish *success identity*. The construction: a distinguisher `D` for two adjacent hybrids differing by a real bit (acceptance `a`) vs a uniform random bit (acceptance `(a+c)/2`, where `c` is acceptance on the *wrong* bit) gives a predictor that guesses bit `g`, runs `D`, and outputs `g` if `D` accepts, `1−g` otherwise — succeeding with probability `a/2 + (1−c)/2 = 1/2 + (a−c)/2 = 1/2 + δ`, `δ` the distinguishing advantage. **`predictSuccess a c := a/2 + (1−c)/2`** (convention 1), **`predictSuccessFlip a c := (1−a)/2 + c/2`** (convention 2, flipped output), **`hybridGap a c := a − (a+c)/2`** (`= (a−c)/2` by `hybridGap_eq`, the distinguishing advantage of real-bit vs random-bit hybrid). **`yao_success_identity`** (PROVED, `ring`): `predictSuccess a c = 1/2 + hybridGap a c`. **`yao_success_flip_identity`** (PROVED, `ring`): `predictSuccessFlip a c = 1/2 − hybridGap a c`. **`yao_best_predictor`** (PROVED, depends on **no axioms** beyond `propext, Classical.choice, Quot.sound`): `max (predictSuccess a c) (predictSuccessFlip a c) = 1/2 + |hybridGap a c|` — proof rewrites both successes, then `rcases le_total 0 (hybridGap a c)` and uses `abs_of_nonneg`/`abs_of_nonpos` + `max_eq_left`/`max_eq_right` (+ `ring`); the better convention removes the sign of the advantage. **`yao_predictor_from_advantage`** (PROVED): a distinguishing advantage `δ ≤ |hybridGap a c|` yields the better predictor with success `≥ 1/2 + δ` (`rw [yao_best_predictor]; linarith`). **`hybridGap_realise`** (PROVED): `|hybridGap y (2x − y)| = |y − x|` — any prescribed gap is realised by explicit acceptance probabilities `a=y`, `c=2x−y`, so the identity applies to the entry-192 adjacent gap `|f(i+1) − f i|`. **`YaoNextBitPredictor f m ε`** := `∃ i, i<m ∧ ∃ a c : ℝ, |f(i+1)−f i| = |hybridGap a c| ∧ 1/2 + ε/m ≤ max (predictSuccess a c) (predictSuccessFlip a c)` — the concrete next-bit-predictor predicate. **`yaoPredictor_discharge`** (PROVED): `YaoPredictor (AdjacentAdvantage f m ε) (YaoNextBitPredictor f m ε)` — **discharges the entry-192 `YaoPredictor` socket** for the concrete predicate: from an adjacent advantage `ε/m ≤ |f(i+1)−f i|` it produces block `i` with explicit acceptance probabilities `a = f(i+1)`, `c = 2·f i − f(i+1)` realising the gap (`hybridGap_realise`), and the better predictor convention achieves success `≥ 1/2 + ε/m` (`yao_predictor_from_advantage`). Honest scope: proves Yao's **arithmetic engine** — the predict-from-distinguish success identity — completely and from first principles (real arithmetic), and discharges the entry-192 `YaoPredictor` socket for the concrete success-probability predicate. What remains a named residual socket, **`YaoCircuitEfficiency YaoNextBit SmallPredictorCircuit := YaoNextBit → SmallPredictorCircuit`**, is the *model-dependent* claim that the predictor is realisable as a **small circuit** (the distinguisher is a bounded-size `ACC⁰` circuit, hence so is the derived predictor) — this needs the circuit model and is not the probability identity. This proves Yao's *arithmetic engine* and the advantage bookkeeping, not the circuit-level reduction. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonYao` |
| **the NW reconstruction step — the hardwiring size accounting (PROVED)** | Discharges the second entry-192 sub-socket (`Reconstruction`: a next-bit predictor + the low-intersection design ⇒ a small circuit for the hard function `f`) by proving its genuine quantitative **heart**, the *size accounting*. The reconstruction builds a circuit for `f` from the Yao predictor plus hardwired copies of the other `m−1` design blocks `f(y|_{Sⱼ})`; by the low-intersection property (entry 191), each `Sⱼ` shares only `r_j < k` seed bits with the active block, so — by entry-192 `low_intersection_table_card` — is a Boolean function of `r_j` bits, hardwirable as a truth table of `2^{r_j} ≤ 2^k` entries. **`reconstruction_table_sum_le`** (PROVED): the total hardwiring cost `∑_j Fintype.card (Fin (r_j) → Bool) ≤ numOther · 2^k` when `∀ j, r_j < k` — via `Finset.sum_le_sum` (each term `≤ 2^k` by `low_intersection_table_card`) then `Finset.sum_const` + `Finset.card_univ` + `Fintype.card_fin` + `smul_eq_mul`. **`reconstruction_total_size_le`** (PROVED): the reconstructed circuit has size `predictorSize + ∑_j 2^{r_j} ≤ predictorSize + numOther · 2^k` (`Nat.add_le_add_left`). **`reconstruction_poly`** (PROVED): the polynomial-size payoff — if `2^k ≤ B` (i.e. `k ≤ log₂ B`, the NW regime `k = O(log m)`), the size is `≤ predictorSize + numOther · B`, polynomial in `numOther` (`le_trans` + `gcongr`); this is *why* the low-intersection design (intersection `< k` with `k` logarithmic) makes the derandomisation efficient. **`ReconNextBit _predictorSize ComputesF := ComputesF`** (the predictor carrier: it computes `f` on the active block with size `predictorSize`), **`ReconDesign numOther k r := ∀ j, r_j < k`** (the design carrier — exactly the entry-191 low-intersection property), **`SmallCircuitForFAt ComputesF actualSize bound := ComputesF ∧ actualSize ≤ bound`** (a small circuit for `f` = computes `f` *and* size within bound). **`reconstruction_socket_discharge`** (PROVED, depends on **no axioms** beyond `propext, Classical.choice, Quot.sound`): `Reconstruction (ReconNextBit predictorSize ComputesF) (ReconDesign numOther k r) (SmallCircuitForFAt ComputesF (predictorSize + ∑_j 2^{r_j}) (predictorSize + numOther · 2^k))` — **discharges the entry-192 `Reconstruction` socket** for the concrete size-carrying predicate: `fun hComputes hr => ⟨hComputes, reconstruction_total_size_le predictorSize numOther k r hr⟩`; the predictor's correctness `hComputes` and the design's low-intersection `hr` together give a circuit that computes `f` with size within the proved bound. Honest scope: proves the reconstruction's **size engine** — the hardwiring cost accounting showing the reconstructed circuit is small (polynomial when `k = O(log m)`) — completely and from first principles (ℕ arithmetic), reusing the entry-191/192 low-intersection bound, and discharges the entry-192 `Reconstruction` socket for the concrete size-carrying predicate. What remains a named residual socket, **`ReconstructionCorrectness Predictor TableHardwiring ComputesF := Predictor → TableHardwiring → ComputesF`**, is the *model-dependent* claim that the assembled circuit (predictor wired to the hardwired block tables) **actually computes `f`** — this needs the circuit semantics and is not the size accounting. This proves the reconstruction's *size engine*, not the circuit-correctness construction. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonReconstruction` |
| **NW reconstruction correctness — the assembled circuit computes `f` (PROVED)** | Discharges the residual `ReconstructionCorrectness` socket left by entry 194 (the assembled reconstructed circuit *actually computes* the hard function `f`), by building a minimal but real **Boolean-function semantics** and proving the model-level **composition/substitution correctness**. Model: a seed `s : S` is restricted to each of the `numOther` other design blocks via `restrict j : S → I`; block `j` is hardwired as a lookup table `table j : I → Bool` meant to reproduce its true value `blockVal j : I → Bool`; the Yao predictor `pred : (Fin numOther → Bool) → Bool` combines the block values into `f`'s next bit `target : S → Bool`. **`assembledOutput restrict table pred s := pred (fun j => table j (restrict j s))`** — the assembled circuit's output on seed `s`. **`tabulate h := h`** (the truth table of `h`), **`tabulate_apply`** (PROVED, `rfl`): `tabulate h x = h x` — lookup is *semantically exact* (entry 192/194 bound the table's *size* by `Fintype.card (Fin r → Bool) = 2^r ≤ 2^k`; this confirms its *correctness*). **`reconstruction_computes`** (PROVED, depends on **`[Quot.sound]` only** — no `propext`, no `Classical.choice`): if `htable : ∀ j s, table j (restrict j s) = blockVal j (restrict j s)` (tables correct on the restricted seed) and `hpred : ∀ s, pred (fun j => blockVal j (restrict j s)) = target s` (predictor correct on the *true* block values — the Yao guarantee), then `assembledOutput restrict table pred s = target s` for every seed `s`; proof: `funext (fun j => htable j s)` shows the predictor's argument `fun j => table j (restrict j s)` equals the true block-value vector, then `rw` + `hpred s`. **`PredictorCorrect`** (`∀ s, pred (fun j => blockVal j (restrict j s)) = target s`), **`TablesCorrect`** (`∀ j s, table j (restrict j s) = blockVal j (restrict j s)`), **`AssembledComputesF`** (`∀ s, assembledOutput … s = target s`) — the carriers. **`reconstructionCorrectness_discharge`** (PROVED, `[Quot.sound]`): `ReconstructionCorrectness (PredictorCorrect …) (TablesCorrect …) (AssembledComputesF …)` — **discharges the entry-194 `ReconstructionCorrectness` socket** (`fun hpred htable s => reconstruction_computes …`): the predictor's correctness (Yao) + the tables' correctness give that the assembled circuit computes `f`. **`reconstruction_computes_tabulated`** (PROVED): with each block hardwired as its *canonical* truth table (`table j = tabulate (blockVal j)`), table correctness holds definitionally (`fun _ _ => rfl`), so a correct predictor alone yields a correct assembled circuit. Honest scope: builds the standard *semantic* circuit model (a circuit ≡ the Boolean function it computes on a finite domain) and proves reconstruction correctness from two genuine inputs — the lookup tables are semantically exact (proved here) and the predictor is correct on the true block values (the Yao guarantee, supplied by entry 193's Yao socket). Together with entry 194's *size* bound, the reconstruction step is now **both size-bounded and correct**. This does not formalise gate-level circuit syntax or `ACC⁰` membership — those remain other named sockets. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonReconCorrect` |
| **Yao predictor circuit efficiency — the predictor is a small circuit (PROVED)** | Discharges the residual `YaoCircuitEfficiency` socket of entry 193 (the predictor derived from the distinguisher is a *small circuit* — the distinguisher is bounded-size, hence so is the predictor) by building a minimal but real **gate-level Boolean circuit syntax** with an explicit gate count — exactly the gate-count content the entry-195 *semantic* model deliberately omits. **`Circ n`** — a circuit over `n` inputs, basis `var (Fin n), ⊤, ⊥, ¬ (cnot), ∧ (cand), ∨ (cor), ⊕ (cxor)`. **`Circ.size`** — the gate count (`var/⊤/⊥ ↦ 1`, `cnot c ↦ 1 + size c`, binary gates `↦ 1 + size a + size b`). **`Circ.eval : Circ n → (Fin n → Bool) → Bool`** — the Boolean semantics. **`predictor D gidx := cxor (var gidx) (cnot D)`** — the Yao predictor circuit: outputs `g ⊕ ¬D(x)` where `g = x gidx`, i.e. the guess `g` if `D` accepts (`¬D = 0`) and the flipped guess `¬g` if `D` rejects — the entry-193 guess-and-correct rule — **referencing `D` exactly once**. **`yao_predictor_size`** (PROVED, `[propext]`): `(predictor D gidx).size = D.size + 3` — the predictor adds exactly three gates (`cxor`, `cnot`, the input read), no blow-up (`simp only [predictor, size]; ring`). **`yao_predictor_eval`** (PROVED, **depends on no axioms at all**): `(predictor D gidx).eval x = if D.eval x then x gidx else !(x gidx)` (`simp only [predictor, eval]; cases hD : D.eval x <;> cases hg : x gidx <;> rfl`) — semantic correctness of the guess-and-correct rule. **`SmallPredictor D gidx`** := `∃ P : Circ n, P.size ≤ D.size + 3 ∧ ∀ x, P.eval x = if D.eval x then x gidx else !(x gidx)` — the predictor is a small circuit computing the Yao rule. **`smallPredictor_exists`** (PROVED): witness `predictor D gidx` — `⟨predictor D gidx, le_of_eq (yao_predictor_size …), yao_predictor_eval …⟩`. **`yaoCircuitEfficiency_discharge`** (PROVED, `[propext]`): `YaoCircuitEfficiency YaoNextBit (SmallPredictor D gidx)` — **discharges the entry-193 `YaoCircuitEfficiency` socket** (`fun _ => smallPredictor_exists D gidx`): from the Yao next-bit predictor, the predictor is realisable as a circuit of size within `+3` of the distinguisher. Honest scope: builds a genuine *syntactic* circuit model (an inductive circuit datatype with an explicit gate count, distinct from the entry-195 semantic model) and proves the Yao step's additive `+3` size overhead together with the predictor's correctness — the gate-count fact the semantic model omits. The `cxor` gate is `MOD₂`, which lies in `ACC⁰`, so the predictor stays in the distinguisher's class. The model is a *tree* circuit, but the construction references `D` only once (a single `cxor` of an input with `¬D`), so there is no duplication blow-up. This does not formalise `ACC⁰` *membership* of `D` itself (depth / gate-type constraints), only the additive size overhead of the Yao step; combined with entries 193–195 it completes the Yao→predictor→reconstruction chain at the level of *success, size, and correctness*. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonYaoCircuit` |
| **HardnessExcludesCircuit — the definitional link + the reconstruction collision (PROVED)** | Discharges the third entry-192 sub-socket (`HardnessExcludesCircuit`: a hard function has no small circuit, so exhibiting one is contradictory). **Critical honesty: this is the *definitional* link, not a circuit lower bound.** "Hard against size `s`" *means* "no circuit of size `≤ s` computes `f`"; so `HardnessExcludesCircuit` — hardness plus a small circuit yield a contradiction — is genuinely provable precisely as the *unfolding of the definition of hardness*. This file does **not** prove that any *particular* function is hard: that is the genuine circuit lower bound the entire conditional anatomy rests on (the `NoEasyWitnessHardFn` / easy-witness side, itself of separation strength), the irreducible assumption. Over the entry-196 `Circ` gate model: **`Computes C f := ∀ x, C.eval x = f x`** (the circuit computes `f`), **`HasCircuitOfSize f s := ∃ C : Circ n, C.size ≤ s ∧ Computes C f`** (`f` has a size-`≤s` circuit), **`HardFor f s := ¬ HasCircuitOfSize f s`** (`f` is hard against size `s` — the *definition* of hardness). **`hardFor_excludes`** (PROVED, **depends on no axioms at all**): `HardFor f s → HasCircuitOfSize f s → False` (literally `hard hc` — the negation applied to the circuit; this is the *entire* content of `HardnessExcludesCircuit`, the definition unfolded, not a lower bound). **`hardness_collision`** (PROVED, **no axioms**): a concrete circuit `C` of size `≤ s` computing `f` contradicts `HardFor f s` (`hard ⟨C, hsize, hcomp⟩`) — the form in which the NW reconstruction (entries 194–196: the reconstructed circuit has size `≤ predictorSize + numOther·2^k` and computes `f`) collides with the hardness of the witness function, i.e. the actual use of `HardnessExcludesCircuit` in the hybrid argument. **`hardnessExcludesCircuit_discharge`** (PROVED, **no axioms**): `HardnessExcludesCircuit (HardFor f s) (HasCircuitOfSize f s)` = `fun hard hc => hard hc` — **discharges the entry-192 `HardnessExcludesCircuit` socket** for the concrete predicates over the `Circ` model. Honest scope: proves only the **definitional** content (hardness, defined as the absence of a small circuit, excludes a small circuit) over the gate model, plus the collision with the reconstructed circuit. It does **not** prove any function hard; that circuit lower bound is the irreducible input (`NoEasyWitnessHardFn`), of separation strength, and is *not* discharged here and not provable by these methods. Closing this socket completes the NW hybrid argument's *plumbing* down to the single genuine assumption (a hard witness function exists). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonHardness` |
| **NoEasyWitnessHardFn — the easy-witness↔hardness identification + the extraction (PROVED)** | Discharges the entry-150 `NoEasyWitnessHardFn` socket (`¬ SmallWitnessCircuits → HardFunction`: if witnesses are not small-circuit describable, a hard function exists). **Critical honesty: this is the *extraction* (the easy half), NOT the easy-witness lemma.** A NEXP witness is a truth-table string `w`; it is "easy" iff a small circuit *describes* `w`. In the truth-table representation the function `w : (Fin n → Bool) → Bool` whose table is that string is the *same object*, so "a small circuit describes the string `w`" is literally "a small circuit *computes* the function `w`". **`EasyWitness w s := HasCircuitOfSize w s`** (over the entry-196/197 `Circ` model — describe = compute). **`noEasyWitness_iff_hard`** (PROVED, **depends on no axioms at all**): `¬ EasyWitness w s ↔ HardFor w s` by `rfl` (since `EasyWitness w s` *is* `HasCircuitOfSize w s` and `HardFor w s` *is* its negation) — "no easy witness" and "the witness is a hard function" are the same statement, the structural pivot of `NoEasyWitnessHardFn`. **`ExistsHardFunction n s := ∃ f : (Fin n → Bool) → Bool, HardFor f s`**. **`noEasyWitness_extracts_hard`** (PROVED, **no axioms**): `¬ EasyWitness w s → ExistsHardFunction n s` via `⟨w, h⟩` — if the witness `w` has no small describing circuit then a hard function exists, *namely `w` itself*; the witness that defeats small description *is* the hard function. **`noEasyWitnessHardFn_discharge`** (PROVED, **no axioms**): `NoEasyWitnessHardFn (EasyWitness w s) (ExistsHardFunction n s)` = `fun h => ⟨w, h⟩` — **discharges the entry-150 `NoEasyWitnessHardFn` socket** for the concrete predicates over the `Circ` model. Honest scope: proves only the **identification/extraction** content — that a witness lacking a small circuit *is* a hard function (describe = compute in the truth-table representation). It does **not** prove the easy-witness lemma, nor that any NEXP language lacks easy witnesses; that the hypothesis `¬ EasyWitness` ever holds is the genuine circuit lower bound, of separation strength, and is *not* discharged here and not provable by these methods. Closing this socket isolates the irreducible Williams-side assumption to a single statement — *a NEXP language without easy witnesses exists* (equivalently, a hard function exists) — which is exactly the separation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonEasyWitness` |
| **DerandKarpLiptonSeparation — the Karp–Lipton collapse chain (composition glue PROVED)** | Opens the entry-189 `DerandKarpLiptonSeparation` socket (`Derandomization → ¬ (NEXP ⊆ ACC⁰)`: a derandomisation collapses to the separation) into the classical **Karp–Lipton collapse chain** and proves the composition glue. The chain (Babai–Fortnow–Lund / Impagliazzo–Kabanets–Wigderson + derandomisation + the hierarchy): (1) **Karp–Lipton collapse** `NEXP ⊆ ACC⁰ ⟹ NEXP = MA` (small circuits for `NEXP` ⇒ the doubly-exponential guess-and-verify collapses `NEXP` into Merlin–Arthur); (2) **derandomisation collapse** `MA = NP` (a PRG simulates Arthur's coins nondeterministically); (3) **nondeterministic time hierarchy** `NEXP ≠ NP`. Composing, `NEXP ⊆ ACC⁰ ⟹ NEXP = MA = NP`, contradicting `NEXP ≠ NP`. **`KarpLiptonCollapse NEXP ACC0 MA := NEXP ⊆ ACC0 → NEXP = MA`**, **`DerandCollapsesMAtoNP Derandomization MA NP := Derandomization → MA = NP`**, **`NexpNeqNp NEXP NP := NEXP ≠ NP`** — the three named sub-sockets. **`derandKarpLipton_discharge`** (PROVED, **depends on no axioms at all**): `KarpLiptonCollapse NEXP ACC0 MA → DerandCollapsesMAtoNP Derandomization MA NP → NexpNeqNp NEXP NP → DerandKarpLiptonSeparation NEXP ACC0 Derandomization`, by `fun hd hsub => hier ((kl hsub).trans (derand hd))` — a derandomisation `hd` and a circuit hypothesis `hsub : NEXP ⊆ ACC⁰` give `NEXP = MA` (Karp–Lipton) and `MA = NP` (derandomisation), so `NEXP = NP` by transitivity, contradicting `NEXP ≠ NP`; hence the derandomisation refutes `NEXP ⊆ ACC⁰`. **Discharges the entry-189 `DerandKarpLiptonSeparation` socket** from the three ingredients. Honest scope: proves the *logical composition* of the Karp–Lipton route (the transitivity of the two collapses contradicting the hierarchy); the three ingredients — the **Karp–Lipton/BFL collapse**, the **derandomisation collapse** `MA = NP`, and the **nondeterministic time hierarchy** — remain named sub-sockets, each a proven classical theorem requiring complexity-theory infrastructure (the doubly-exponential guess-verify, the hardness–randomness tradeoff, the diagonalisation hierarchy) absent here. This proves the composition, not the ingredients. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0KarpLiptonCollapse` |
| **NexpNeqNp / the nondeterministic time hierarchy — the diagonalization kernel (PROVED)** | Discharges the entry-199 `NexpNeqNp` sub-socket (`NEXP ≠ NP`). Unlike the other two Karp–Lipton ingredients, this one is classically **unconditional** — the Cook / Seiferas–Fischer–Meyer nondeterministic time hierarchy theorem — and this file proves its genuine heart, **Cantor diagonalization against an enumeration**. **`enc n := List.replicate n true`** (an injective encoding `ℕ → List Bool`), **`enc_injective`** (PROVED, distinct indices give distinct-length lists). **`diag enum w := ∀ i, enc i = w → ¬ enum i w`** — the diagonal language, which flips machine `i` on its own encoding `enc i`. **`diag_eval`** (PROVED): `diag enum (enc i) ↔ ¬ enum i (enc i)` (the `∀` collapses to `i` by injectivity of `enc`). **`diag_ne`** (PROVED, `[propext]`): `diag enum ≠ enum i` for every `i` — if they were equal, `congrFun` at `enc i` gives `enum i (enc i) ↔ ¬ enum i (enc i)`, i.e. `P ↔ ¬P`, refuted by `iff_not_self`. **`diag_not_mem_range`** (PROVED, `[propext]`): `diag enum ∉ Set.range enum` — the diagonal language escapes the entire enumeration (the unconditional Cantor core). **`NPEnumerable NP := ∃ enum : ℕ → Lang, NP = Set.range enum`** (NP is the range of a recursive enumeration of clocked poly-time nondeterministic machines; named socket), **`DiagonalInNexp NEXP NP := ∀ enum, NP = Set.range enum → diag enum ∈ NEXP`** (the diagonal language is `NEXP`-decidable; named socket). **`nexpNeqNp_discharge`** (PROVED, `[propext]`): `NPEnumerable NP → DiagonalInNexp NEXP NP → NexpNeqNp NEXP NP` — **discharges the entry-199 `NexpNeqNp` socket**: were `NEXP = NP = Set.range enum`, the diagonal (which lies in `NEXP`) would lie in `Set.range enum`, contradicting `diag_not_mem_range`. Honest scope: proves the genuine **unconditional diagonalization kernel** (`diag enum ∉ range enum`, real Cantor diagonalization) and the discharge of `NexpNeqNp` from it. What remain named model sockets are the two facts that make the kernel apply to NTMs: `NPEnumerable` (NP is machine-enumerable) and `DiagonalInNexp` (the diagonal lies in NEXP). The latter hides the genuinely NTM-specific subtlety: nondeterministic classes are not known to be closed under complement, so the *real* hierarchy theorem uses Cook's **lazy diagonalization** (delaying the flip across a range of inputs) rather than the direct complement `¬ enum i w` used here at the set level. This file proves the set-theoretic diagonalization core, not the lazy-diagonalization simulation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NondetTimeHierarchy` |
| **GuessVerify discharge — language-level collapse + entry-187 fast-SAT time bound (PROVED)** | Connects three completed pieces — the *proved language-level* guess-and-verify collapse (`…ACC0GuessVerify`: `gvDecider_eq` "guessing small witnesses decides exactly `L`", `guessVerify_subset` the resulting `NTIME[2ⁿ] ⊆ NTIME[2ⁿ/superpoly]`), the abstract entry-150 **`GuessVerify`** socket (`SmallWitnessCircuits → ACC0SatSpeedup → NTIME[2ⁿ] ⊆ NTIME[2ⁿ/superpoly]`), and the entry-187 **fast-`ACC⁰`-SAT verifier**. **`ProvidesFastVerifiers NTIME2n NTIME2nFast ACC0SatSpeedup SmallWitnessCircuits := SmallWitnessCircuits → ACC0SatSpeedup → ∀ L ∈ NTIME2n, ∃ V decode, HasWitness L V ∧ SmallWitnessRed V decode ∧ gvDecider V decode ∈ NTIME2nFast`** — the model socket: small witnesses + speedup yield, for each `NTIME[2ⁿ]` language, a verifier whose guess-and-verify decider is fast (the `NTM`-cost placement). **`guessVerify_discharge`** (PROVED, `[propext, Quot.sound]`): `ProvidesFastVerifiers → GuessVerify NTIME2n NTIME2nFast ACC0SatSpeedup SmallWitnessCircuits`, by `fun hsw hspeed => guessVerify_subset _ _ (prov hsw hspeed)` — **discharges the entry-150 `GuessVerify` socket via the proved language-level collapse** (the decider equals `L`, so `L` inherits its fast complexity). **`guess_verify_beats_bruteforce_abstract`** (PROVED): `verifyWork ≤ (D+1)·n^D + 1` and `guessCost + ((D+1)·n^D + 1) < 2ⁿ` ⇒ `guessCost + verifyWork < 2ⁿ` (`lt_of_le_of_lt (Nat.add_le_add_left …)`). **`guess_verify_beats_bruteforce`** (PROVED): the concrete tie-in — running the verify step with the entry-187 `fast_sat_verifier` (work being the count-cell image it examines, `≤ (D+1)·n^D + 1`), `guessCost + (verify work) < 2ⁿ` whenever `guessCost + ((D+1)·n^D + 1) < 2ⁿ`: guess the small witness circuit (cost `guessCost`), verify with fast `ACC⁰`-SAT, beat brute-force `2ⁿ` — *why* `ProvidesFastVerifiers` should hold. Honest scope: discharges the entry-150 `GuessVerify` socket from the proved language-level collapse and proves the concrete time bound connecting the guess cost to the entry-187 fast-SAT verify work; the remaining named socket `ProvidesFastVerifiers` is the `NTM`-cost placement of the decider in the fast `NTIME` class (the verify arithmetic here is exactly why it should hold, but the formal `NTIME` placement needs the machine model). Refines, does not overwrite, the existing `…ACC0GuessVerify`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GuessVerifyDischarge` |
| **Collapse ingredients — `DerandCollapsesMAtoNP` + `KarpLiptonCollapse` (antisymmetry glue PROVED)** | Opens the two *class-equality* entry-199 sub-sockets into their genuine two-inclusion structure and proves the antisymmetry glue (a class equality `X = Y` is `X ⊆ Y` ∧ `Y ⊆ X`, one trivial inclusion and one deep). For `DerandCollapsesMAtoNP` (`MA = NP`): **`DerandMASubsetNP Derandomization MA NP := Derandomization → MA ⊆ NP`** (the derandomisation direction — Arthur's coins replaced by nondeterministic guessing over PRG seeds), **`NPSubsetMA NP MA := NP ⊆ MA`** (trivial). **`derandCollapsesMAtoNP_discharge`** (PROVED, `[propext, Quot.sound]`): `fun hd => Set.Subset.antisymm (fwd hd) bwd` — discharges the entry-199 `DerandCollapsesMAtoNP` socket. For `KarpLiptonCollapse` (`NEXP = MA`): **`NexpSubsetMA_ofCircuits NEXP ACC0 MA := NEXP ⊆ ACC0 → NEXP ⊆ MA`** (the deep Babai–Fortnow–Lund collapse, only under the circuit hypothesis), **`MASubsetNexp MA NEXP := MA ⊆ NEXP`** (trivial, `MA ⊆ NEXPTIME`). **`karpLiptonCollapse_discharge`** (PROVED): `fun hsub => Set.Subset.antisymm (deep hsub) triv` — discharges the entry-199 `KarpLiptonCollapse` socket. Honest scope: proves the antisymmetry glue turning the two class-equalities into pairs of inclusions and discharging both sockets from them; each equality's trivial inclusion (`NP ⊆ MA`, `MA ⊆ NEXP`) and deep inclusion (`MA ⊆ NP` via derandomisation, `NEXP ⊆ MA` via the BFL collapse) remain named sub-sockets — the deep ones proven classical theorems needing complexity-theory infrastructure absent here. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CollapseIngredients` |
| **Hierarchy witness model — `NPEnumerable` + `DiagonalInNexp` discharged end-to-end (PROVED)** | Shows the two entry-200 model sockets are non-vacuous and the diagonalization discharge fires end-to-end, by an explicit model where both hold and the conclusion is genuinely true. Model: `NP := Set.range enum` for any `enum : ℕ → Lang`, `NEXP := Set.univ`. **`npEnumerable_range`** (PROVED, **no axioms**): `NPEnumerable (Set.range enum)` = `⟨enum, rfl⟩`. **`diagonalInNexp_univ`** (PROVED): `DiagonalInNexp Set.univ (Set.range enum)` = `fun e _ => Set.mem_univ (diag e)` (every diagonal lies in `Set.univ`). **`witness_nexpNeqNp`** (PROVED, `[propext]`): `NexpNeqNp Set.univ (Set.range enum)` via the entry-200 `nexpNeqNp_discharge` — yields the *genuinely true* `Set.univ ≠ Set.range enum`, true because the diagonal language `diag enum` lies in `Set.univ` but escapes `Set.range enum` (`diag_not_mem_range`); no enumeration exhausts all languages, the diagonalization biting. Honest scope: a **witness/consistency model**, not a claim about the real `NEXP`/`NP`. It discharges both entry-200 model sockets in an explicit model and runs the diagonalization to a true conclusion, demonstrating the kernel is non-vacuous and the socket interface satisfiable. It does **not** prove the actual `NEXP ≠ NP` (which needs the genuine facts — `NP` *is* the clocked-machine enumeration, `NEXP` *contains the diagonal via lazy diagonalization* — not the trivial `Set.univ`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0HierarchyWitness` |
| **Concrete transition-table run — a worked table-driven machine (PROVED)** | Realizes the existing transition-table apparatus (`…ACC0ConcreteNTM`: `TMTrans`, `applyTrans`, `concreteStep`, `toNTM`) on a worked example, exercising the whole step semantics together. **`flipTable := [((0, false), (1, true, (2 : Fin 3)))]`** — a one-rule table: in state `0` reading `false`, go to state `1`, write `true`, stay. **`flip_applyTrans`** (PROVED, `decide`): `applyTrans (0,0,[false]) rule = (1,0,[true])` — exercises `writeAt`, `moveHead`. **`flip_step`** (PROVED): `concreteStep flipTable (0,0,[false]) (1,0,[true])` — the left-hand side matches `(state, readSym)`. **`flip_reachIn`** (PROVED): `reachIn (toNTM flipTable) 1 (0,0,[false]) (1,0,[true])` — a one-step run in the abstract model. **`flip_accepts`** (PROVED): `acceptsWithin (toNTM flipTable) [false] 1` — from `init [false] = (0,0,[false])` the machine reaches accepting state `1` within one step. Honest scope: a worked realization confirming the transition-table semantics (`readSym`/`writeAt`/`moveHead`/`applyTrans`/`concreteStep`/`reachIn`/`acceptsWithin`) compute as intended; it does not build the time-bounded universal simulation (the clocked universal machine, the genuine content the hierarchy `DiagonalInNexp`-style sockets need). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ConcreteTMRun` |
| **BT additive bottom-layer merge — pooling into one symmetric gate (PROVED)** | The complementary *additive* merge to the multiplicative `SYM∘AND` tree merge (`hasSymAndFormFanIn_combine`, count `s₁ + (s₁+1)·s₂`, the Beigel–Tarui bottleneck). The tree blow-up arises because it keeps the two sub-counts *separate* (mixed-radix); pooling monomials into **one** symmetric gate makes the counts simply add. **`hasSymAndFormFanIn_pool`** (PROVED): when two monomial families both feed one symmetric gate — the top reads `h (saCount mono₁ x + saCount mono₂ x)` — the `SYM∘AND` has size `card ι₁ + card ι₂` (**additive**, no blow-up) and fan-in `w`; proof via the pooled index `ι₁ ⊕ ι₂`, the pooled count being the sum (`saCount_sum_elim`), and `Fintype.card_sum` for the additive size. **`hasSymAndFormFanIn_family`** (PROVED): the canonical `SYM∘AND` over a concrete family of `m` monomials (`mono : Fin m → Finset (Fin n)`, fan-in `≤ w`) has size exactly `m` — additive in the *number* of monomials (`⟨Fin m, _, mono, h, by rw [Fintype.card_fin], hw, rfl⟩`). Honest scope: proves the additive per-layer count of `SYM∘AND` — a single symmetric layer over polynomially many monomials, the `FanInStaysPolylog` regime — in contrast to the multiplicative tree. What it does **not** prove is the deep Beigel–Tarui theorem that an entire constant-depth `ACC⁰` circuit collapses to **one** such symmetric layer with only *quasipolynomially* many monomials (the multiplicative tree replaced by a single additive layer); that depth-collapse is the genuine BT construction and remains the socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymAndPool` |
| **Beigel–Tarui depth-collapse — low degree ⇒ quasipolynomial `SYM∘AND` (PROVED, RS approximation socketed)** | The **quasipolynomial** depth-collapse — the genuine BT contribution, vs the *exponential* exact collapse `acc0circuit_hasSymAndFormFanIn` (whose `symAndSize` is multiplicative). BT collapses a constant-depth `ACC⁰` circuit to a single symmetric gate over *quasipolynomially* many `AND`s, via the probabilistic polynomial method: approximate by a low-degree polynomial, then collapse that to a `SYM∘AND`. This file proves the **combinatorial heart** — *low degree ⇒ few monomials ⇒ quasipolynomial `SYM∘AND`*. **`LowDegRep f D := ∃ (m : ℕ) (mono : Fin m → Finset (Fin n)) (h : ℕ → Bool), Function.Injective mono ∧ (∀ j, (mono j).card ≤ D) ∧ f = fun x => h (saCount mono x)`** — `f` is a symmetric function of an injective family of degree-`≤D` monomials (the form an RS low-degree polynomial takes). **`lowDeg_symAnd_quasipoly`** (PROVED): such a family is a `SYM∘AND` of fan-in `≤ D` (`hasSymAndFormFanIn_family`) with size `m ≤ (D+1)·n^D` — quasipolynomial — by `monomial_count_le` (an injective degree-`≤D` family has `m ≤ ∑_{i≤D} C(n,i)`) and `lowDegMonomialCount_le_pow` (`∑_{i≤D} C(n,i) ≤ (D+1)·n^D`). **`btQuasipolyCollapse`** (PROVED): `LowDegRep f D → ∃ m, HasSymAndFormFanIn f m D ∧ m ≤ (D+1)·n^D` (destructure the representation, apply the heart). **`AccToLowDeg f p t d := LowDegRep f (((p−1)·t)^d)`** — the Razborov–Smolensky approximation socket: a depth-`d` `ACC⁰[p]` circuit (error parameter `t`) has a degree-`((p−1)·t)^d` representation. **`btDepthCollapse`** (PROVED, `[propext, Classical.choice, Quot.sound]`): `AccToLowDeg f p t d → ∃ m, HasSymAndFormFanIn f m (((p−1)·t)^d) ∧ m ≤ (((p−1)·t)^d + 1)·n^{((p−1)·t)^d}` — the depth-`d` `ACC⁰[p]` circuit has a `SYM∘AND` form of **quasipolynomial** size; for `d` constant and `t` polylog, the degree is polylog and the size is `n^{polylog} = 2^{polylog(n)}`, the YBT/Beigel–Tarui quasipolynomial normal form. Honest scope: proves the **degree⇒size** half of the collapse — that a low-degree representation collapses to a quasipolynomial `SYM∘AND` (the combinatorial heart, fixing the exponential naive `symAndSize`) — completely. What remains the named socket is the **circuit⇒degree** half, `AccToLowDeg`: that a depth-`d` `ACC⁰[p]` circuit actually *has* a low-degree representation — the **Razborov–Smolensky probabilistic polynomial method** (approximate each gate by a low-degree polynomial with small error, compose across the constant depth), the genuine analytic content, not proved here. This proves the degree-to-size half, not the circuit-to-degree approximation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BTDepthCollapse` |
| **Connecting `AccToLowDeg` to the proved Razborov–Smolensky approximation (PROVED)** | Discharges the entry-203 `AccToLowDeg` socket (the circuit⇒low-degree half of the BT depth-collapse) from the **already-proved** RS approximation in the repo, reducing it to the precise residual representation bridge. The key existing fact: **`Layer3ACC0LowRank.acc0_approx_by_lowRankPredictor`** (proved) — for an `AC⁰[p]` circuit `C` at horizon `t` with `p^t ≥ 4·#subcircuits`, there is an `F_p`-span approximant `f` of degree **`((p−1)·t)^C.depth`** (in the span of the squarefree monomials `squarefreeEvalMonomial p S`, `S ∈ lowDegMonomials n (((p−1)·t)^C.depth)`) agreeing with `C` on `≥ 3/4` of inputs — the genuine probabilistic polynomial method. The degree `((p−1)·t)^C.depth` matches `AccToLowDeg`'s `((p−1)·t)^d` **exactly** (`d = C.depth`). **`SpanApproxToLowDegRep C p t`** := (the RS approximant existence: `∃ f`, `f ∈ Submodule.span (ZMod p) (range squarefreeEvalMonomial over lowDegMonomials n (((p−1)·t)^C.depth))` `∧ 3·2^n ≤ 4·#{x | f x = boolToZMod p (C.eval x)}`) → `LowDegRep (fun x => C.eval x) (((p−1)·t)^C.depth)` — the residual span→`SYM∘AND` bridge (named socket), carrying the genuine BT representation content: replacing each monomial with `≤ p−1` copies so the `F_p` value becomes a **count-mod-`p`** of satisfied `AND`s (a symmetric top), plus the amplification from the `3/4`-approximant to the exact circuit. **`accToLowDeg_via_rs`** (PROVED, `[propext, Classical.choice, Quot.sound]`): for `AC⁰[p]` `C` with the `MOD`-gate hypothesis `hmod` and size `hsize`, `SpanApproxToLowDegRep C p t → AccToLowDeg (fun x => C.eval x) p t C.depth`, by `bridge (acc0_approx_by_lowRankPredictor p t ht C hmod hsize)` — feeding the proved RS approximant into the bridge. Honest scope: this **reduces** the entry-203 `AccToLowDeg` socket to the `SpanApproxToLowDegRep` bridge, discharging the *circuit⇒low-degree-approximant* half from the **already-proved** RS approximation (the genuine probabilistic polynomial method, degree matching exactly) — so that half is no longer assumed. What remains is the `SpanApproxToLowDegRep` bridge: turning an `F_p`-linear span approximant (degree `D`, `3/4` agreement) into an *exact* Boolean symmetric-of-`AND`s representation (`h ∘ saCount`) — the multiplicity/count-mod-`p` expansion and the `3/4`→exact amplification. This proves the *reduction* of `AccToLowDeg` to that bridge (the RS degree is taken from the existing proof, not assumed), not the bridge itself. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BTRSConnection` |
| **The count-mod-`p` multiplicity collapse — the arithmetic heart of the span→`SYM∘AND` bridge (PROVED)** | Splits the entry-204 `SpanApproxToLowDegRep` bridge into its two pieces — the **multiplicity / count-mod-`p` expansion** (provable arithmetic) and the **`3/4`→exact amplification** (deep) — and proves the arithmetic heart. The idea: an `F_p`-combination `∑_S c_S·[S satisfied]` (`c_S ∈ ℕ` multiplicities) is the value of a *count of `AND`-copies* — replace each monomial `S` by `c_S` copies, indexed by the sigma type `Σ s, Fin (c s)`. **`saCount_sigma_expand`** (PROVED): `saCount` over `Σ s, Fin (c s)` `= ∑_s c_s·(if monoAND (base s) x then 1 else 0)` (the weighted count = the polynomial value over `ℕ`); proof via `← Finset.univ_sigma_univ`, `Finset.sum_sigma`, and `Fin.sum_const` on the inner constant sum. **`saCount_sigma_cast`** (PROVED): that count cast to `ZMod p` `= ∑_s (c_s : ZMod p)·(if monoAND (base s) x then 1 else 0)` — the count mod `p` *is* the `F_p` polynomial value (`push_cast`), the BT representation pivot. **`countModP_collapse`** (PROVED, `[propext, Classical.choice, Quot.sound]`): a count-mod-`p` decode `fun x => h (saCount (fun q : Σ s, Fin (c s) => base q.1) x)` of the `AND`-copy multiset (monomials `base s` of degree `≤ D`, multiplicities `c s`) is `HasSymAndFormFanIn` of size `∑_s c_s` and fan-in `≤ D` — built directly from the sigma `Fintype` (`Fintype.card_sigma`), with **no injectivity needed** (multiplicity is allowed). **`expand_card_le_mult`** (PROVED): `∑_s c_s ≤ K·M` when each `c_s ≤ M`; with `M = p−1` and `K ≤ (D+1)·n^D` low-degree monomials, the `SYM∘AND` size is `≤ (p−1)·(D+1)·n^D` — quasipolynomial. **`ExactCountModPRep f D := ∃ K base c h, (∀ s, (base s).card ≤ D) ∧ f = fun x => h (saCount (fun q : Σ s, Fin (c s) => base q.1) x)`** — the residual amplification socket: `f` has an *exact* count-mod-`p` multiset representation of degree `≤ D` (the `3/4`→exact amplification combining several RS approximants, plus the extraction of explicit multiplicities from the span coefficients). **`bridge_via_countModP`** (PROVED): `ExactCountModPRep f D → ∃ m, HasSymAndFormFanIn f m D`. Honest scope: proves the **multiplicity / count-mod-`p` arithmetic** of the span→`SYM∘AND` bridge completely — that an `F_p`-combination of monomials, via `AND`-copy multiplicity, *is* a count-mod-`p` (hence symmetric) function collapsing to a quasipolynomial `SYM∘AND` (size `≤ (p−1)·#monomials`). This removes the count-mod-`p` content from entry 204's `SpanApproxToLowDegRep` bridge, leaving only the **amplification** (`ExactCountModPRep`: the `3/4`→exact step) as the residual. Proves the arithmetic half of the bridge, not the amplification. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CountModP` |
| **The `3/4`→exact amplification — the majority-vote exactness skeleton (PROVED)** | Proves the deterministic skeleton of the deep amplification residual (the `3/4`→exact step inside entry 205's `ExactCountModPRep`): a pointwise-majority-correct family of approximants has an *exact* majority vote. **`majBool v := decide (k < 2·#{i | v i = true})`** — the Boolean majority of `k` votes. **`count_compl`** (PROVED): `#{i | v i = true} + #{i | v i = false} = k`, via `Finset.card_filter_add_card_filter_not` (with `s := univ`, `p := fun i => v i = true`) + a `filter_congr` turning `v i = false` into `¬ v i = true` (`Bool.not_eq_true`). **`majBool_eq_of_count`** (PROVED): `k < 2·#{i | v i = b} → majBool v = b` — by `cases b`: `true` is direct (`decide_eq_true_eq`), `false` follows from `count_compl` + `omega`. **`MajVote g := fun x => majBool (fun i => g i x)`** — the majority vote of a family `g : Fin k → (… → Bool)`. **`maj_exact`** (PROVED, `[propext, Classical.choice, Quot.sound]`): `(∀ x, k < 2·#{i | g i x = f x}) → MajVote g = f` — if at *every* input strictly more than half the family is correct, the majority vote agrees with `f` everywhere (`funext x` + `majBool_eq_of_count`). **`MajorityGoodFamily f := ∃ k g, ∀ x, k < 2·#{i | g i x = f x}`** — the residual probabilistic socket: a pointwise-majority-correct family exists (the independence/Chernoff amplification driving per-input error below `2^{-n}`, union-bounded over the `2^n` inputs). **`exact_from_majorityGood`** (PROVED): `MajorityGoodFamily f → ∃ k g, MajVote g = f` — a pointwise-majority-correct family yields an exact majority-vote representation. Honest scope: proves the **deterministic majority-vote skeleton** of the amplification — that the majority of a pointwise-majority-correct family is exact — as genuine Boolean/counting arithmetic, completely. What remains the named socket, `MajorityGoodFamily`, is the *probabilistic* existence of such a family (independence + Chernoff + union bound), needing a probability framework absent here; a second residual (not addressed) is the `MAJ∘SYM∘AND` closure (that the majority vote of `SYM∘AND`s is itself a single `SYM∘AND`). This proves the amplification's logical skeleton, not the probabilistic existence or the `MAJ∘SYM∘AND` closure. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Amplification` |
| **Span-coefficient extraction — `F_p`-span ⇒ explicit count-mod-`p` multiplicities (PROVED)** | Discharges the *span-coefficient extraction* residual of the entries 204–205 span→`SYM∘AND` bridge — turning the RS `F_p`-span output into explicit monomials with `ZMod p`-valued coefficients representable as `AND`-copy multiplicities `0..p−1` — connecting entry 204's span format to entry 205's count-mod-`p` input. It is linear algebra plus the `ZMod.val` representative. **`squarefree_eq_monoAND_ind`** (PROVED): the squarefree monomial evaluation `squarefreeEvalMonomial p S x = ∏_{i∈S} boolToZMod(x_i)` equals the `monoAND` indicator `if monoAND S x then (1 : ZMod p) else 0` — by `by_cases` on `∀ i∈S, x i = true`: if all set, `Finset.prod_eq_one`; else `Finset.prod_eq_zero` at the unset coordinate (`boolToZMod` of `false` is `0`). **`span_eval_weightedCount`** (PROVED, `[propext, Classical.choice, Quot.sound]`): an `F_p`-span element `f` of `Set.range (fun S : {S // S ∈ lowDegMonomials n D} => squarefreeEvalMonomial p S.1)` has explicit coefficients `c : {S // S ∈ lowDegMonomials n D} → ZMod p` with (i) each multiplicity `(c S).val < p` (`ZMod.val_lt`, using `NeZero p` from `Fact p.Prime`) and (ii) `f x = ∑_S c_S·(if monoAND S.1 x then 1 else 0)` for every `x`. Proof: `Submodule.mem_span_range_iff_exists_fun` makes `f = ∑_S c_S • v_S`; then `Finset.sum_apply` + `Pi.smul_apply` + `smul_eq_mul` + `squarefree_eq_monoAND_ind` rewrite the evaluation to the weighted count. This is exactly the count-mod-`p` weighted-value form that entry-205's `saCount_sigma_cast` consumes — the `c_S.val` are the `AND`-copy multiplicities of monomial `S`. Honest scope: proves the **span-coefficient extraction** completely — the RS `F_p`-span output (entry 204) *is* an explicit weighted count of `monoAND` indicators with bounded (`< p`) multiplicities, the exact form entry 205's count-mod-`p` collapse consumes. Connects entry 204 → entry 205, discharging the multiplicity-extraction residual. What remains of the span→`SYM∘AND` bridge is the **`3/4`→exact amplification** (entry 206 proved its deterministic skeleton; the probabilistic `MajorityGoodFamily` existence remains the named socket). This proves the extraction, not the amplification. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SpanExtract` |
| **Chernoff amplification existence — the probabilistic method + union bound (PROVED), concentration socketed** | Discharges the entry-206 `MajorityGoodFamily` socket (a pointwise-majority-correct family of approximants exists) from a *single* per-input concentration socket, proving everything else: the probabilistic method and the union bound. Over the finite space of `k`-tuples `Fin k → ((Fin n → Bool) → Bool)`, a tuple is *bad* if at some input the majority errs (**`errsAt f g x := 2·#{i | g i x = f x} ≤ k`**). **`exists_good_of_bad_lt`** (PROVED): `#{bad tuples} < Fintype.card (Fin k → Fn) → ∃ g, ∀ x, ¬ errsAt f g x` — the probabilistic method, pure pigeonhole: `by_contra` + `push_neg`; if every tuple were bad the bad filter would equal `univ` (`Finset.filter_true_of_mem`), giving `#univ < #univ` (`card_univ`, `lt_irrefl`). **`union_bound`** (PROVED): `#{bad tuples} ≤ ∑_x #{tuples bad at x}` — `Finset.card_le_card` of the inclusion into the `biUnion` over inputs, then `Finset.card_biUnion_le`. **`ChernoffPerInput f k := ∃ M, (∀ x, #{tuples bad at x} ≤ M) ∧ Fintype.card (Fin n → Bool) · M < Fintype.card (Fin k → Fn)`** — the per-input concentration socket: at each input the bad tuples are a `< 2^{-n}` fraction (`#inputs · M < #tuples`). **`majorityGoodFamily_of_chernoff`** (PROVED, `[propext, Classical.choice, Quot.sound]`): `ChernoffPerInput f k → MajorityGoodFamily f` — the sum bound (`∑_x #{bad at x} ≤ ∑_x M = #inputs · M`, via `Finset.sum_le_sum` + `Finset.sum_const` + `card_univ`) chained with the union bound gives `#{bad} < #tuples`, so `exists_good_of_bad_lt` yields a tuple good at every input, and `not_le.mp` turns `¬ errsAt` into the `k < 2·#{i | g i x = f x}` of `MajorityGoodFamily`. Honest scope: proves the **probabilistic-method logic** (positive count ⇒ existence — pigeonhole) and the **union bound** + sum bound completely, discharging `MajorityGoodFamily` from the single per-input concentration socket `ChernoffPerInput`. That socket *is* the genuine **Chernoff concentration** — for `k` *independent* approximants each correct with probability `≥ 3/4`, the chance the majority errs at a fixed input is `exp(-Ω(k))`, below `2^{-n}` for `k = Ω(n)` — which needs the independence/concentration (probability) machinery absent from this development. This proves the existence *scaffolding* (everything except the concentration), not the concentration inequality itself. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ChernoffExist` |
| **The combinatorial Chernoff bound — the binomial-tail inequality (PROVED)** | The genuine analytic heart of the per-input concentration, proved **directly without measure theory** — pure `Nat` arithmetic. **Correction (honest):** entry 208's `ChernoffPerInput` was stated over *all* `k`-tuples of *all* Boolean functions, where it is **false** (uniform all-functions ⇒ each approximant correct with probability exactly `1/2` ⇒ majority errs with probability `≈ 1/2`, not `< 2^{-n}`); the genuine concentration must be over a **supply of `≥3/4`-correct approximants** (the RS-distribution support). **`term_mono`** (PROVED): for `j ≤ m ≤ k` and `b ≤ a`, `a^j·b^{k−j} ≤ a^m·b^{k−m}` — moving weight from the smaller `b`-part to the larger `a`-part increases the term, so the term at `m` is heaviest among `j ≤ m`; proof: `b^{k−j} = b^{k−m}·b^{m−j}` (as `k−j = (k−m)+(m−j)`) and `b^{m−j} ≤ a^{m−j}` (`Nat.pow_le_pow_left`). **`binomial_tail_le`** (PROVED, `[propext, Classical.choice, Quot.sound]`): the binomial tail `∑_{j ∈ range (m+1)} C(k,j)·a^j·b^{k−j} ≤ 2^k·a^m·b^{k−m}` (for `b ≤ a`, `m ≤ k`) — every tail term is `≤` the top term (`term_mono` + `Nat.mul_le_mul_left`), and `∑_{j≤m} C(k,j) ≤ ∑_{j≤k} C(k,j) = 2^k` (`Finset.sum_le_sum_of_subset` + `Nat.sum_range_choose`). In the `3/4` regime (`a ≥ 3b`, threshold `m ≈ k/2`) the right side is `exp(−Ω(k))` of the total `(a+b)^k` — the Chernoff decay, so for `k = Ω(n)` the per-input bad fraction drops below `2^{-n}`. Honest scope: proves the genuine **combinatorial Chernoff inequality** — the binomial-tail bound that is the analytic heart of the per-input concentration — completely and *without any probability/measure infrastructure*, and documents the entry-208 space correction (the concentration is over a `≥3/4` supply, not all functions). What it does **not** prove: (i) the *fiber-counting reduction* — that the number of bad approximant-tuples at a fixed input equals the binomial tail `∑ C(k,j)·a^j·b^{k−j}` (a standard but unformalised multinomial count); (ii) the *decay-to-`2^{-n}`* step — choosing `k = Ω(n)` so `2^k·a^m·b^{k−m}·2^n < (a+b)^k` (the `exp(−Ω(k))` arithmetic in the `3/4` regime). These remain the plumbing connecting this bound to the corrected `ChernoffPerInput`-over-supply. This proves the Chernoff inequality, not the full per-input concentration statement. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ChernoffBound` |
| **The fiber-count reduction — bad approximant-tuples bounded by the binomial tail (PROVED)** | Connects the entry-209 Chernoff bound to the actual tuple-counting: the number of `k`-tuples of supply approximants whose majority errs at a fixed input is `≤ 2^k·a^m·b^{k−m}` (with `a` correct and `b` wrong approximants at the input). The reduction maps each tuple `g : Fin k → α` to its **correctness pattern** `pat g := fun i => pred (g i) : Fin k → Bool`; whether the majority errs depends only on the pattern. **`fiber_card`** (PROVED): the pattern-fiber cardinality `#{g : Fin k → α | (fun i => pred (g i)) = p} = a^{#trues p}·b^{#falses p}` where `a = #{x | pred x}`, `b = #{x | ¬pred x}` — the fiber is `Fintype.piFinset (fun i => {x | pred x = p i})` (`Fintype.card_piFinset`), and `∏_i #{x | pred x = p i} = ∏_i (if p i then a else b) = a^{#trues}·b^{#falses}` (`Finset.prod_ite` + `Finset.prod_const`). **`bad_tuple_count_le`** (PROVED, `[propext, Classical.choice, Quot.sound]`): `#{g : Fin k → α | #{i | pred (g i)} ≤ m} ≤ 2^k·a^m·b^{k−m}` (for `b ≤ a`, `m ≤ k`) — `Finset.card_eq_sum_card_fiberwise` partitions the bad tuples by correctness pattern; each fiber is `≤ a^m·b^{k−m}` (`fiber_card`, then entry-209 `term_mono` since a bad pattern has `#trues ≤ m`, with `#falses = k − #trues` via `Finset.card_filter_add_card_filter_not`); and there are `≤ #{patterns} = 2^k` (`Fintype.card_fun` + `Fintype.card_bool` + `Fintype.card_fin`). Honest scope: proves the genuine **tuple-count ≤ binomial-tail reduction** completely — `Finset`/`Fintype` counting (`card_piFinset`, the fiberwise partition) reusing entry-209's `term_mono`. Combined with entry 209 this is the per-input bound over a supply with `a` correct, `b` wrong. What remains to reach the corrected `ChernoffPerInput`-over-supply: the **decay arithmetic** — for a `≥3/4` supply (`a ≥ 3b`) and `k = Ω(n)`, `2^k·a^m·b^{k−m}·2^n < (a+b)^k` (the `exp(−Ω(k))` step) — and the construction of an actual supply with the `≥3/4` property at every input (from the existing `acc0_approx_by_lowRankPredictor` at `3/4`). This proves the counting reduction, not the decay arithmetic. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FiberCount` |
| **The decay arithmetic — the full per-input concentration over a `3/4` supply (PROVED)** | Proves the decay that drives the entry-210 bound below the `2^{-n}` fraction the union bound needs, and **assembles the full per-input concentration** `2^n · #{bad tuples} < #tuples`. **`decay_core`** (PROVED): `2^n · 3^j < 4^j` for `j ≥ 3n+1` — split at `j = 3n`: `2^n·3^{3n} = (2·27)^n = 54^n ≤ 64^n = 4^{3n}` (`Nat.pow_le_pow_left`, `54 ≤ 64`) times `3^{j−3n} < 4^{j−3n}` (`Nat.pow_lt_pow_left`, strict since `j−3n ≥ 1`); via `4^j = 4^{3n}·4^{j−3n}` etc. **`decay_full`** (PROVED): `2^n · (2^{2j}·(3b)^j·b^{2j−j}) < (3b+b)^{2j}` for `b ≥ 1`, `j ≥ 3n+1` — i.e. `#inputs · (the entry-210 per-input bound at `a = 3b`, `k = 2j`, `m = j`) < #tuples`; reduces to `decay_core` by rewriting both sides as `(2^n·3^j)·(4^j·b^{2j})` and `4^j·(4^j·b^{2j})` and cancelling the positive common factor (`Nat.mul_lt_mul_right`). **`chernoff_per_input`** (PROVED, `[propext, Classical.choice, Quot.sound]`): for a `3/4` supply (`#correct = 3·#wrong`, `#wrong ≥ 1`) and `k = 2j` with `j ≥ 3n+1`, `2^n · #{g : Fin (2j) → α | #{i | pred (g i)} ≤ j} < (#correct + #wrong)^{2j}` — the per-input concentration `#inputs · #{bad} < #tuples`, assembling entry-210's `bad_tuple_count_le` (`#{bad} ≤ 2^{2j}·(3b)^j·b^j`, after `← mul_assoc`) with `decay_full` (via `Nat.mul_le_mul_left` then `decay_full`). Honest scope: proves the **decay arithmetic** and the **full per-input concentration over an (assumed) `3/4` supply** at `k = 2j = Θ(n)` (`j ≥ 3n+1`) — completely, in pure `Nat` arithmetic, combining entry 209 (`term_mono`), entry 210 (`bad_tuple_count_le`), and this decay. This is exactly the per-input bound the corrected (supply-restricted) `ChernoffPerInput` of entry 208 requires. The **last** residual of the entire BT depth-collapse is the **construction of a `3/4` supply**: that the RS approximants (`acc0_approx_by_lowRankPredictor`, which gives `3/4` agreement *globally*) can be arranged as a supply with `≥3/4` correct *at every input* (per-input vs. global agreement; pooling enough approximants). This proves the concentration over an *assumed* `3/4` supply, not the existence of the supply. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ChernoffDecay` |
| **The supply construction — a per-input `3/4` supply yields a tuple good at every input (PROVED)** | The amplification assembly: from a per-input `3/4` supply, a tuple whose majority is correct at *every* input. **Honest subtlety:** the repo's RS approximation `acc0_approx_by_lowRankPredictor` gives only *global* `3/4` (one function correct on `3/4` of inputs), which does **not** give a per-input `3/4` supply — a fixed function is wrong on a fixed `1/4` of inputs, and copies share that error set. The per-input property — at *every* input a `3/4` fraction of the family is correct — is the genuine probabilistic-polynomial-method content (per-point error `≤ 1/4` over the random polynomial), *stronger* than the formalised global statement, taken as the named socket. **`Uniform34 corr w := ∀ x, #{a | corr x a = true} = 3w ∧ #{a | corr x a = false} = w`** — the per-input `3/4`-supply socket (`corr x a` = "approximant `a` correct at input `x`"; so `≥3/4` correct at every input and `|α| = 4w`); the RS per-point guarantee. **`exists_good_tuple`** (PROVED, `[propext, Classical.choice, Quot.sound]`): `Uniform34 corr w → 1 ≤ w → 3n+1 ≤ j → ∃ g : Fin (2j) → α, ∀ x, ¬ (#{i | corr x (g i) = true}).card ≤ j` (majority correct at every input). Proof: the bad tuples (bad at *some* input) number `≤ ∑_x #{bad at x}` (union bound via `Finset.card_biUnion_le`) `≤ 2^n · M` (entry-210 `bad_tuple_count_le` per input, with `M = 2^{2j}·(3w)^j·w^j` *uniform* across inputs since the supply is uniform; `Fintype.card α = 4w` via `Finset.card_filter_add_card_filter_not`) `< #tuples = (4w)^{2j}` (entry-211 `decay_full`); since fewer than all tuples are bad, a tuple good at every input exists (`by_contra` + `Finset.filter_true_of_mem` + `card_univ` + `lt_irrefl`). Honest scope: proves the **amplification assembly** completely — that a per-input `3/4` supply yields a tuple correct-majority at every input — in pure `Nat`/`Finset` counting, combining entries 209–211 with a union bound and the probabilistic-method pigeonhole. This is the amplified `MajorityGoodFamily` witness the NW reconstruction needs (entry 206 then makes its majority an *exact* representation). What remains the named socket is **`Uniform34`** itself: the *existence* of a per-input `3/4` supply — the genuine RS per-point error bound (per-point `≤ 1/4` over the random polynomial), stronger than the repo's *global* `3/4`. This proves everything downstream of the supply, not the supply's existence (the irreducible RS analytic input). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SupplyConstruction` |
| **The Razborov–Smolensky per-point error bound — the detection involution (PROVED)** | The genuine algebraic heart of the RS per-point guarantee — the source of the `Uniform34` per-input `3/4`-supply socket (entry 212). RS detects a nonzero input vector by a random subset `S` testing `∑_{i∈S} x i ≠ 0` (the `OR`-gate approximant): if `x = 0` the sum is always `0` (no false positive); if some `x j ≠ 0`, a random `S` detects it with probability `≥ 1/2`. **`toggle j S := S △ {j}`** (`if j ∈ S then S.erase j else insert j S`), **`toggle_involutive`** (PROVED): an involution (`insert_erase`/`erase_insert`). **`subsetSum_toggle`** (PROVED): `∑_{i∈toggle j S} x i = ∑_{i∈S} x i + (if j ∈ S then −x j else x j)` (`Finset.sum_erase_eq_sub`/`Finset.sum_insert` + `abel`). **`detection_half`** (PROVED, `[propext, Classical.choice, Quot.sound]`): if `x j ≠ 0` then `2^s ≤ 2 · #{S | ∑_{i∈S} x i ≠ 0}` (detection probability `≥ 1/2`) — the involution `toggle j` maps each "missed" subset (`sum = 0`) to a "detected" one (`sum = ±x j ≠ 0`) injectively (`Finset.card_le_card_of_injOn`), so `#{miss} ≤ #{detect}`, and with `#{miss} + #{detect} = 2^s` (`Finset.card_filter_add_card_filter_not` + `Fintype.card_finset`) this gives `2·#{detect} ≥ 2^s`. **`detection_two_thirds_four`** (PROVED): `3·4^s ≤ 4 · #{(S₁,S₂) | ∑₁ ≠ 0 ∨ ∑₂ ≠ 0}` (two independent trials boost detection to `≥ 3/4`, per-point error `≤ 1/4`) — `#{both miss} = #{miss}²` (`Finset.card_product`), `#{miss} ≤ 2^{s−1}` (from `detection_half`), so `4·#{miss}² ≤ 4^s` and `#{detect} = 4^s − #{miss}² ≥ 3·4^{s−1}`. Honest scope: proves the genuine **per-point detection bound** — single-trial `≥ 1/2` (the `toggle` involution / pairing argument) and the two-trial boost to `≥ 3/4` — completely, in pure `AddCommGroup`/`Finset` arithmetic, *no measure theory*. This is exactly the per-point error `≤ 1/4` underlying the `Uniform34` per-input `3/4`-supply. What remains to fully discharge `Uniform34`: the **gate-to-supply packaging** — that this subset-sum test *is* the `OR`/`MOD`-gate `F_p`-approximant (the `(∑)^{p−1}` Fermat indicator; the `OR(x)=0` case always correct, "sum always `0`"), and that composing per-gate approximants across the constant-depth circuit yields a per-input `≥3/4` supply for the whole circuit (the RS composition). This proves the per-point detection bound (the analytic core); the gate-encoding and circuit-composition packaging remain. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RSPerPoint` |
| **The gate-encoding — the subset-sum test is the `OR`-gate Fermat indicator over `F_p` (PROVED)** | Encodes the abstract entry-213 detection as the actual Razborov–Smolensky `OR`-gate approximant `(∑_{i∈S} x i)^{p−1}` over `F_p`. **`fermat_ind`** (PROVED, `[propext, Classical.choice, Quot.sound]`): `y^{p−1} = if y = 0 then 0 else 1` over `ZMod p` (`p` prime) — Fermat's little theorem `ZMod.pow_card_sub_one_eq_one` for `y ≠ 0`, and `zero_pow` (`p − 1 ≥ 1`) for `y = 0`; so `y^{p−1}` is the indicator `[y ≠ 0]`. **`orApprox x S := (∑_{i∈S} x i)^{p−1}`** — the `OR`-approximant (an `F_p`-polynomial of degree `p − 1`). **`orApprox_eq_one_iff`** (PROVED): `orApprox x S = 1 ↔ ∑_{i∈S} x i ≠ 0` (by `fermat_ind`; the `sum = 0` branch uses `zero_ne_one`). **`orApprox_zero`** (PROVED): `(∀ i ∈ S, x i = 0) → orApprox x S = 0` — no false positive, so the `OR(x)=0` case of the gate is correct for *every* `S` (`Finset.sum_eq_zero` + `zero_pow`). **`orApprox_detection`** (PROVED): if `x j ≠ 0` (so `OR(x) = 1`), then `2^s ≤ 2 · #{S | orApprox x S = 1}` — a random subset makes the approximant output `1` with probability `≥ 1/2`, transporting the entry-213 detection bound to the actual gate (`Finset.filter_congr` with `orApprox_eq_one_iff`, then `detection_half`). Honest scope: proves the **single-gate encoding** completely — that the abstract subset-sum detection of entry 213 *is* the `OR`-gate `(∑)^{p−1}` Fermat-indicator approximant, with `OR=0` always correct and `OR=1` detected w.p. `≥ 1/2` — in pure `ZMod p`/`Finset` arithmetic (Fermat's little theorem). Combined with entries 209–213 this gives, *for a single `OR` gate*, a per-input `≥3/4` random approximant — the `Uniform34` ingredient at the gate level. What remains to fully discharge `Uniform34` for a *circuit* is the **RS composition**: composing per-gate approximants across the constant-depth `ACC⁰[p]` circuit (each `MOD`/`AND`/`OR` layer approximated, errors union-bounded over the gates) into a per-input `≥3/4` approximant for the whole circuit. This proves the single-gate encoding, not the circuit composition. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GateEncoding` |
| **Pointwise RS composition — single-gate per-point ⇒ whole-circuit per-input `3/4` supply (PROVED)** | The last BT-side structural bridge: lifts the single-`OR`-gate per-point `≥3/4` guarantee (entry 214) to a per-input `≥3/4` correct approximant for the *whole* constant-depth `ACC⁰[p]` circuit — exactly the `Uniform34` supply (entry 212) — via a union bound over the gates. **`Composes gateWrong circuitWrong := ∀ x σ, circuitWrong x σ → ∃ g, gateWrong g x σ`** — the composition socket: at input `x` under seed `σ`, if the circuit-approximant is wrong then *some* gate-approximant is wrong (equivalently all gates right ⇒ circuit right); the structural gate-substitution semantics. **`circuit_perpoint_3_4`** (PROVED, `[propext, Classical.choice, Quot.sound]`): given `Composes`, a per-gate per-point error bound `∀ g x, (univ.filter (fun σ => gateWrong g x σ)).card ≤ M`, and `4·(Fintype.card γ · M) ≤ Fintype.card Seed` (`γ` the gates, `G = #γ`; each gate's per-point error `≤ 1/(4G)`), then for every input `x`, `3·|Seed| ≤ 4·(univ.filter (fun σ => ¬ circuitWrong x σ)).card` — the circuit is correct on `≥ 3/4` of the seeds at every input. Proof: the circuit-wrong seeds inject into `⋃_g {σ | gate g wrong}` (`Composes`, `Finset.card_le_card` into `Finset.biUnion`), so `#{circuit wrong at x} ≤ ∑_g #{gate g wrong at x} ≤ G·M` (`Finset.card_biUnion_le` + `Finset.sum_le_sum` + `Finset.sum_const`); with `#{correct} + #{wrong} = |Seed|` (`Finset.card_filter_add_card_filter_not` + `Finset.card_univ`) and `4·G·M ≤ |Seed|`, `omega`. Honest scope: proves the **pointwise composition** completely — the union bound over the `G` gates that lifts the single-gate per-point `≥3/4` (entry 214) to a whole-circuit per-input `≥3/4` supply — in pure `Finset`/`Fintype` counting, no measure theory. The seed space with the circuit-correct predicate *is* a per-input `≥3/4` supply, the structural bridge to `Uniform34` (entry 212). What remains a named socket is **`Composes`**: that the gate-wise approximant is correct whenever all its gate-approximants are (the substitution/correctness of replacing each gate by its `F_p`-polynomial approximant) — the circuit-semantics step, cf. the entry-195 reconstruction correctness. (Also: entry-212 `Uniform34` used *exact* `3w`; this gives the genuine `≥3/4`, the honest RS output.) This proves the composition's counting heart, not the gate-substitution semantics. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RSPointwiseComposition` |
| **RS supply ⇒ exact majority representation (step-2 assembly, PROVED)** | Assembles the BT-side concentration/amplification pipeline (entries 209–215) into a single chain: a per-input `3/4` supply of approximant-functions yields, through the amplification (entry 212) and the majority-vote exactness (entry 206), an **exact** representation of `f` as a majority of `2j` approximants. The threading: supply elements are approximant *functions* via `ev : α → (Fin n → Bool) → Bool`; "`a` correct at `x`" is `ev a x = f x`. **`supply_to_exact_majority`** (PROVED, `[propext, Classical.choice, Quot.sound]`): given `Uniform34 (fun x a => decide (ev a x = f x)) w` (per-input `3/4` supply), `w ≥ 1`, `j ≥ 3n+1`, there is `g : Fin (2j) → ((Fin n → Bool) → Bool)` with `ACC0Amplification.MajVote g = f`. Proof: entry-212 `exists_good_tuple` (applied to `corr x a := decide (ev a x = f x)`) gives a tuple `g₀ : Fin (2j) → α` majority-correct at every input (`¬ #{i | decide (ev (g₀ i) x = f x)} ≤ j`); the count `#{i | ev (g₀ i) x = f x}` equals the `decide`-count (`Finset.filter_congr` + `decide_eq_true_eq`), so `2j < 2·#{i | ev (g₀ i) x = f x}` (`omega`), feeding entry-206 `maj_exact` to give `MajVote (ev ∘ g₀) = f`. Honest scope: proves the **supply ∘ amplification ∘ majority-exactness** assembly completely (pure `Finset` counting) — combined with the pointwise RS composition (entry 215, which produces the `Uniform34` supply) and the per-point/gate machinery (209–214), the BT-side amplification realised end to end: an `ACC⁰[p]` circuit `f` has an exact representation as a majority vote of `2j` low-degree (`SYM∘AND`-encodable) approximants. What this does **not** do: (i) collapse `MajVote g` of `2j` `SYM∘AND`s into a *single* quasipolynomial `SYM∘AND` (the `MAJ∘SYM∘AND` step — needed for the entry-203 `btQuasipolyCollapse`/depth-collapse, a separate structural step); (ii) it uses entry-212 `Uniform34` (*exact* `3w`) rather than entry-215's genuine `≥3/4` (the idealisation gap, a minor generalisation of `exists_good_tuple`). This proves the supply-to-exact-majority assembly, not the `MAJ∘SYM∘AND` collapse. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RSSupplyToExact` |
| **The `MAJ∘SYM∘AND` collapse — majority of `SYM∘AND`s is a `SYM∘AND`, but exponentially (PROVED)** | The structural collapse of the entry-216 majority-of-approximants into a single `SYM∘AND`, *with the honest size caveat that pins down where the majority-route and the classical CRT-route diverge*. **`IsSymAnd f := ∃ s w, HasSymAndFormFanIn f s w`** — `f` is a `SYM∘AND` of some size/fan-in. **`isSymAnd_combine`** (PROVED): `comb (f x) (g x)` is a `SYM∘AND` for any binary `comb`, if `f, g` are — the existing `hasSymAndFormFanIn_combine` (mixed-radix, size `s₁+(s₁+1)·s₂`), existentialised. **`isSymAnd_list`** (PROVED, `[propext, Classical.choice, Quot.sound]`): for any list `gs` of `SYM∘AND`s and any combiner `G : List Bool → Bool`, `fun x => G (gs.map (fun g => g x))` is a `SYM∘AND`. Proof by induction on `gs`: at the head `g`, `G (g x :: rest) = if g x then G(true::rest) else G(false::rest) = (g x ∧ A x) ∨ (¬ g x ∧ B x)` where `A := G(true::·)`, `B := G(false::·)` are `SYM∘AND`s by the inductive hypothesis (on `gs`), assembled by three binary combines (`isSymAnd_combine`); the equality is by `cases (g x)` + `simp`, after `List.map_cons`. `MAJ` is the special case `G = list-majority`, so the entry-216 `MajVote g` (a majority of `SYM∘AND`s) *is* a `SYM∘AND`. **Crucial honest caveat:** the size is **multiplicative** (the combine's `s₁+(s₁+1)·s₂`), hence **exponential in `gs.length`** — so the entry-216 majority of `2j = Θ(n)` approximants collapses to an exact `SYM∘AND` of *exponential* size, **not** quasipolynomial. **This is exactly where the RS-amplification (majority) route and the classical route diverge:** Beigel–Tarui's quasipolynomial *single* `SYM∘AND` is the **direct low-degree route** (entry 203 `btQuasipolyCollapse`: low degree ⇒ quasipolynomial `SYM∘AND`, fed by the RS degree bound of entry 204), *not* the collapse of a majority-of-approximants. Honest scope: proves the structural collapse *exists* completely (via the binary combine + `if-then-else` induction, pure `Finset`), and *documents* — does not hide — that the size is exponential, so this is the exact-but-exponential path, not the quasipolynomial one. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MajSymAnd` |
| **Step 3 via Route A — the quasipolynomial BT closure plugged into the Williams chain (PROVED)** | Assembles **Route A** — the *direct low-degree*, quasipolynomial path (the operative one for Williams' fast-`ACC⁰`-SAT, in contrast to the *exponential* majority route of entry 217) — into the BT-closure → `¬ NEXP` chain. **`routeA_quasipoly`** (PROVED): an `AC⁰[p]` `BoolCircuitSyntax` circuit `C` (every `MOD` modulus `p`, `4·#subcircuits ≤ p^t`, `1 ≤ n`, and the `SpanApproxToLowDegRep` span bridge) has `∃ m, HasSymAndFormFanIn (fun x => C.eval x) m (((p−1)t)^{C.depth}) ∧ m ≤ (((p−1)t)^{C.depth}+1)·n^{((p−1)t)^{C.depth}}` — a quasipolynomial `SYM∘AND` — by entry-203 `btDepthCollapse` (degree⇒quasipoly) applied to entry-204 `accToLowDeg_via_rs` (the RS degree-`((p−1)t)^{C.depth}` representation from the proved `acc0_approx_by_lowRankPredictor`). **`RouteAClosure p t`** (a `Prop`: every `AC⁰[p]` circuit, given its span bridge and size/`n` hypotheses, has a quasipolynomial `SYM∘AND`) / **`routeA_closure_proved`** (PROVED, by `routeA_quasipoly`). **`routeA_btClosure_to_NEXP`** (PROVED, `[propext, Classical.choice, Quot.sound]`): plugs the proved Route-A closure into the entry-166 `dynamicClosure_to_NEXP_not_ACC0` — with the Williams sockets `closure_to_quasi : RouteAClosure p t → BTQuasi`, `quasi_to_speed : BTQuasi → ACC0Speed`, `williams : ACC0Speed → NEXPC → Collapse`, and `hierarchy : ¬ Collapse` — `¬ NEXPC` follows. Honest scope: assembles the **quasipolynomial Route A** into the BT-closure → `¬ NEXP` chain — the genuinely operative path for the fast-SAT (vs the exponential majority route, entry 217). The route-A `SYM∘AND` is proved *modulo the `SpanApproxToLowDegRep` span bridge* (entry-204's residual, reduced by entries 205/207 to the count-mod-`p` multiplicity + the amplification), and the chain's remaining premises (`closure → BTQuasi`, `BTQuasi → ACC0SatSpeedup`, the Williams meta-theorem, the hierarchy) are the named **Williams-side** sockets, *not* BT content. This is a second, independent (`HasSymAndFormFanIn`-based) discharge of the BT-quasipoly closure, parallel to the *toAgree*-based `DynamicClosesAtBT_AC0p` (entry 179). This proves the route-A assembly and its chaining, not the span bridge or the Williams sockets. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RouteAClosure` |
| **Lazy diagonalization — the telescoping contradiction (PROVED), the simulation socketed** | The genuine logical heart of the lazy nondeterministic hierarchy, refining the entry-200 `DiagonalInNexp` socket. The naive complement-diagonal `¬ enum i w` is not obviously `NEXP`-decidable because nondeterministic classes are not known closed under complement; Cook's **lazy diagonalization** lazily copies `M_i` on the *next* input across a block and complements only **once**, at the boundary, which is nondeterministically affordable. **`lazy_telescope`** (PROVED): `(∀ k, k < len → M (a+k) = M (a+k+1)) → M a = M (a+len)` — the lazy equalities thread `M`'s value across the block (induction on `len`). **`lazy_diag_false`** (PROVED, `[propext, Quot.sound]`): if `M` decides its own lazy diagonal `L` — `∀ x, M x = L x`, with `L (a+k) = M (a+k+1)` for `k < len` (lazy copy on the block) and `L (a+len) = ¬ M a` (boundary complement) — then `False`: the lazy equalities telescope (`lazy_telescope`) to `M a = M (a+len)`, but the boundary gives `M (a+len) = ¬ M a`, so `M a = ¬ M a` (`Bool.not_ne_self`). *No machine decides its own lazy diagonal*, and the proof uses only the **single** boundary complement — which is why lazy diagonalization works for nondeterministic classes that lack closure under complement. Honest scope: proves the lazy-diagonalization **contradiction mechanism** completely (pure `Bool`/`ℕ` arithmetic) — the NTM-specific subtlety that entry-200's `DiagonalInNexp` socket hid (the *contradiction* is now proved). What remains the socket is the **simulation**: that the lazy diagonal `L` is decidable in the bigger nondeterministic time class `NEXP` (simulate `M_i` on the next input within the block, plus the one boundary complement, within the time bound) — now reduced to the *lazy-feasible* form (one complement, not closure under complement), the genuine remaining NTM-cost content. This proves the lazy contradiction, not the time-bounded simulation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LazyDiagonal` |
| **Physical TM transition tables — the clocked multi-step run from a deterministic table (PROVED)** | The time-bounded-computation substrate the machine-cost sockets rest on (the lazy-diagonalization *simulation* of entry 219, `ProvidesFastVerifiers` of entry 201, `ClockedInclusion`/`ConcreteHierarchy`). Entry 205 proved a worked *one-step* run; this proves the **clocked multi-step run** and its `reachIn` validity. A deterministic transition table is a total function `δ : ℕ × Bool → ℕ × Bool × Move` (`(state, read) → (state, write, move)`). **`detStep δ c := applyTrans c ((c.1, readSym c), δ (c.1, readSym c))`** — look up the rule for the current `(state, read)` and apply it (using the entry-205/`…ACC0ConcreteNTM` `applyTrans`, `readSym`, `CConfig`). **`detRun δ t := (detStep δ)^[t]`** — the clocked `t`-step run. **`detRun_add`** (PROVED): `detRun δ (a+b) c = detRun δ b (detRun δ a c)` — clocked additivity, via `Function.iterate_add_apply` (with `Nat.add_comm`). **`detNTM δ`** — the deterministic table as an abstract `NTM` (step relation `d = detStep δ c`, start state `0`, tape `= input`, accept iff state `1`), the bridge to the `…ACC0NTM` `reachIn` model. **`detRun_reachIn`** (PROVED, `[propext]`): `reachIn (detNTM δ) t c (detRun δ t c)` — the functional `t`-step run is reachable from `c` in exactly `t` steps of `detNTM δ`, by induction on `t` (`Function.iterate_succ_apply`). Honest scope: proves the **physical transition-table → clocked computation** realisation — a deterministic table drives a well-defined, clocked-additive `t`-step run that is a genuine `reachIn` computation in the `NTM` model — the time-bounded-computation substrate the machine-cost sockets reference. What this does **not** do: the *clocking/time-bound counting* (relating `detRun δ t` to a step-budget `≤ f(|x|)` and hence to the `NTIME(f)` classes), the *universal* table-driven simulation of one machine by another (the universal-machine cost), or nondeterministic-branching cost — those are the further machine-cost content the `NTIME`-level sockets still need. This proves the deterministic clocked run and its `reachIn` validity, not the time-class accounting. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TransitionTable` |
| **The Babai–Fortnow–Lund / IKW collapse — `NEXP = MIP` + guessable prover, structure exposed (PROVED glue)** | Opens up the deep Karp–Lipton inclusion (entry-202 `NexpSubsetMA_ofCircuits`: `NEXP ⊆ ACC⁰ ⟹ NEXP ⊆ MA`, the socketed direction) into its genuine **Babai–Fortnow–Lund / IKW** structure, and proves the composition. The collapse goes through interactive proofs: `NEXP = MIP` (Babai–Fortnow–Lund 1991, the multi-prover IP characterisation), and "circuits ⟹ the `MIP` prover's optimal strategy is a small circuit, which Arthur guesses, so `MIP ⊆ MA`". **`NexpEqMIP NEXP MIP := NEXP = MIP`** (the `MIP = NEXP` theorem; named socket). **`MIPSubsetMA_ofCircuits MIP ACC0 MA := MIP ⊆ ACC0 → MIP ⊆ MA`** (the guessable-prover collapse; named socket). **`nexpSubsetMA_via_MIP`** (PROVED): `NexpEqMIP NEXP MIP → MIPSubsetMA_ofCircuits MIP ACC0 MA → NexpSubsetMA_ofCircuits NEXP ACC0 MA`, by `fun hsub => heq ▸ guessable (heq ▸ hsub)` — a circuit hypothesis `hsub : NEXP ⊆ ACC⁰` becomes `MIP ⊆ ACC⁰` (rewrite along `NEXP = MIP`), yields `MIP ⊆ MA` (guessable), and rewrites back to `NEXP ⊆ MA`. **`karpLiptonCollapse_via_MIP`** (PROVED, `[propext, Quot.sound]`): `NexpEqMIP` + `MIPSubsetMA_ofCircuits` + the trivial `MASubsetNexp` ⇒ `KarpLiptonCollapse NEXP ACC0 MA`, via `nexpSubsetMA_via_MIP` and entry-202's antisymmetry (`karpLiptonCollapse_discharge`). Honest scope: **exposes the BFL-IKW structure** of the deep Karp–Lipton inclusion — that `NEXP ⊆ ACC⁰ ⟹ NEXP ⊆ MA` factors as `NEXP = MIP` composed with "circuits ⟹ guessable `MIP` prover ⟹ `MIP ⊆ MA`" — and proves the *composition* (rewriting along the class equality). The two genuine classical theorems remain named sub-sockets: `NexpEqMIP` (the `MIP = NEXP` theorem — the sum-check / multilinearity machinery of Babai–Fortnow–Lund) and `MIPSubsetMA_ofCircuits` (the guessable-prover collapse — a `P/poly` prover strategy turning the interactive proof Merlin–Arthur), each needing interactive-proof infrastructure absent here. This proves the structure and composition of the BFL-IKW collapse, not its two deep ingredients. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BFLCollapse` |
| **The derandomisation collapse — `MA = NP` via a PRG, structure exposed (PROVED glue)** | Opens the deep derandomisation direction (entry-202 `DerandMASubsetNP`: `Derandomisation ⟹ MA ⊆ NP`, the socketed direction) into its genuine **hardness–randomness** structure, parallel to the entry-221 BFL-IKW decomposition. An `MA` computation is `∃ Merlin message, Arthur's randomised check accepts`; the derandomisation supplies a pseudorandom generator (Nisan–Wigderson / Impagliazzo–Wigderson, from a hard function) that fools Arthur, so replacing his coins by the poly-many PRG seeds gives `MA ⊆ NP`. **`DerandGivesPRG Derand PRGExists := Derand → PRGExists`** (the hardness-to-PRG tradeoff; named socket). **`PRGCollapsesMAtoNP PRGExists MA NP := PRGExists → MA ⊆ NP`** (the PRG-seed-enumeration collapse; named socket). **`derandMASubsetNP_via_PRG`** (PROVED): `DerandGivesPRG Derand PRGExists → PRGCollapsesMAtoNP PRGExists MA NP → DerandMASubsetNP Derand MA NP`, by `fun hd => prg_collapses (derand_gives_prg hd)` (implication chaining). **`derandCollapsesMAtoNP_via_PRG`** (PROVED, `[propext, Quot.sound]`): `DerandGivesPRG` + `PRGCollapsesMAtoNP` + the trivial `NPSubsetMA` ⇒ `DerandCollapsesMAtoNP Derandomization MA NP` (`Derand ⟹ MA = NP`), via `derandMASubsetNP_via_PRG` and entry-202's antisymmetry (`derandCollapsesMAtoNP_discharge`). Honest scope: **exposes the hardness–randomness structure** of the deep derandomisation direction — `Derandomisation ⟹ MA ⊆ NP` factors as `Derandomisation ⟹ PRG` composed with `PRG ⟹ MA ⊆ NP` — and proves the *composition* (implication chaining). The two genuine classical ingredients remain named sub-sockets: `DerandGivesPRG` (the Nisan–Wigderson / Impagliazzo–Wigderson hardness-to-pseudorandomness tradeoff) and `PRGCollapsesMAtoNP` (replacing Arthur's randomness by PRG-seed enumeration), each requiring pseudorandomness infrastructure absent here. This proves the structure and composition, not its two deep ingredients. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DerandCollapse` |
| **MIP = NEXP — the sum-check round-reduction engine (PROVED), arithmetization socketed** | Opens the entry-221 `NexpEqMIP` socket (Babai–Fortnow–Lund 1991) and proves the genuine arithmetic engine of the BFL protocol — the sum-check round reduction — isolating the arithmetization and multi-prover soundness as named sub-sockets. `NEXP` membership arithmetizes into a claim `H = ∑_{b∈{0,1}^m} g(b)` about a low-degree polynomial `g`; the sum-check protocol verifies it in `m` rounds. Over a `CommRing R`: **`boolToR : Bool → R`** (`false↦0, true↦1`, the hypercube `{0,1}⊆R`), **`scSum g := ∑_{b:Fin m→Bool} g(boolToR∘b)`** (the hypercube sum = arithmetized `NEXP` claim), **`roundPoly g s := ∑_t g(Fin.cons s (boolToR∘t))`** (the round polynomial — first coord free at `s∈R`, rest summed). **`scSum_succ_eq`** (PROVED, `[propext, Classical.choice, Quot.sound]`): `scSum g = roundPoly g 0 + roundPoly g 1` — the verifier's per-round consistency check (`g₁(0)+g₁(1)=H`); proof reindexes `{0,1}^{m+1} ≃ {0,1}×{0,1}^m` (`consEquivBool`), splits the boolean head (`Fintype.sum_bool`), matches `boolToR false/true = 0/1` (`boolToR_cons`). **`roundPoly_eq_scSum_restrict`** (PROVED, rfl): `roundPoly g r = scSum (fun rest => g (Fin.cons r rest))` — after the challenge `r` the claim recurses to a fresh `m`-variable sum-check on `g(r,·)`. **`scSum_zero`** (PROVED): `scSum g = g Fin.elim0` — after `m` rounds the protocol terminates at a single point evaluation. **`nexpEqMIP_of_subset`** (PROVED, `[propext, Quot.sound]`): discharges the entry-221 `NexpEqMIP` socket from `NexpSubsetMIP` (deep) + `MipSubsetNexp` (simulation) by `Set.Subset.antisymm`. Honest scope: proves the **sum-check round reduction** completely over an arbitrary `CommRing` (the field case is operative) — the per-round consistency identity, the recursion to a smaller instance after the challenge, and base-case termination — pure finite-sum algebra, no measure theory; these three are the arithmetic engine the BFL `NEXP ⊆ MIP` direction runs on. What remain named sockets are the deep BFL ingredients: `NexpArithmetization` (certificate-to-low-degree-polynomial), `SumCheckSoundness` (Schwartz–Zippel on the low-degree round polynomials + the *multi-prover* multilinearity test that makes this `MIP` not single-prover `IP = PSPACE`), and `MipSubsetNexp` (the simulation — guess the exponential prover tables). This proves the sum-check arithmetic and the antisymmetry assembly of `NEXP = MIP`, not the arithmetization or soundness. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SumCheck` |
| **PRG collapse — the seed-enumeration mechanism, fooling residue isolated (PROVED)** | Workstream A, step 3 (residue internals): opening the most self-contained residue `PRGCollapsesMAtoNP` (`PRGExists → MA ⊆ NP`) and proving its seed-enumeration collapse mechanism. **`seedAccept V seeds x w := seeds.length < 2 · (seeds.filter (fun r => V x w r)).length`** (sub-bricks 1, 4) — the finite seed-majority NP verifier; **`instDecidableSeedAccept`** (PROVED) it is decidable (a `Nat` comparison over a finite count, a deterministic verifier step); **`seed_count_le`** (PROVED) the accepting-seed count is `≤ seeds.length` (finite enumeration). **`PRGFools probAccept V seeds := ∀ x w, probAccept x w ↔ seedAccept …`** (sub-bricks 2, 3) — completeness (`prob ≥ 2/3 ⇒ seed majority`) + soundness (`prob ≤ 1/3 ⇒ seed minority`) transfer, bundled as the count-level equivalence — the genuine pseudorandomness property. **`maLang_eq_npLang`** (PROVED) — `PRGFools ⟹ MALang probAccept = NPLang V seeds` (`exists_congr`): replacing true coins by the seed-majority changes neither side. **`maLang_mem_np`** (PROVED) — `PRGFools` + seed-language `∈ NP` ⟹ `MA` language `∈ NP`. **`prgCollapsesMAtoNP_of_realizes`** (PROVED) — discharges `PRGCollapsesMAtoNP` from the realized form `PRGRealizesCollapse` (every `MA` language is a `probAccept`-language with a fooling PRG whose seed-majority `NP` language is in `NP`). Honest scope: this proves the seed-enumeration collapse mechanism — replacing true coins by a finite, decidable seed-majority is sound *given* the PRG fools `V` — so `PRGCollapsesMAtoNP` is reduced to the genuine pseudorandomness residue `PRGFools` (the count-level completeness+soundness transfer), now a precise named statement. The deep residue that remains — a PRG from a hard function actually fools `V` — is the next target `HardFunction → PRGExists` (the NW/IW construction); the collapse glue around it is proved. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PRGCollapse` |
| **`HardFunction → PRGExists` — the NW reconstruction assembled end-to-end (PROVED glue)** | Workstream A, step 3 (final residue): assembling the Nisan–Wigderson reconstruction from its proved sub-pieces. Entry 222 proved the reconstruction-*contradiction* glue (`prgFools_of_hard` : `NWReconstruction D S → ¬S → ¬D`), reducing `HardFunction → PRGExists` to the abstract `NWReconstruction` socket; entries 190–196 separately proved the *concrete* sub-pieces (hybrid telescoping `hybrid_advantage`, Yao predict-from-distinguish identity `yaoPredictor_discharge`, reconstruction poly-size `reconstruction_socket_discharge`, hardness/circuit collision `hardnessExcludesCircuit_discharge`) — but each discharged its *own* socket in isolation, never chained, and the chain does not close on types (Yao outputs a probability-level predictor; reconstruction consumes a predictor-computes hypothesis and outputs a `size ≤ bound` claim; hardness consumes a concrete `Circ`). **`reconstruction_rebridge`** (PROVED) — pre/post-composing `Reconstruction P D C` to `Reconstruction P' D C'`, the type-level glue letting the Yao output feed the reconstruction input and the reconstruction output feed the hardness input. **`nwReconstruction_assembled`** (PROVED) — discharges `NWReconstruction (GlobalAdvantage f m ε) (HasCircuitOfSize fbool s)` by chaining `hybrid_advantage` → `yaoPredictor_discharge` → `bridgeYao` → `reconstruction_socket_discharge` (with the low-intersection design) → `bridgeRecon`. **`hardFn_to_prgExists_assembled`** (PROVED) — `HardFor fbool s → ¬ GlobalAdvantage f m ε`, i.e. `HardFunction → PRGExists`, via `prgFools_of_hard` on the assembled reconstruction. Honest scope: the **analytic core** of the NW/IW hardness-to-pseudorandomness tradeoff is now assembled — every combinatorial/analytic step is a proved theorem chained end-to-end. What remains are exactly two **named model-dependent bridges** — `bridgeYao` (the probability-level Yao predictor realised as a circuit, the residual `YaoCircuitEfficiency` socket) and `bridgeRecon` (the reconstructed predictor-plus-tables is a concrete `Circ` of size `≤ s` computing `f`, the residual `ReconstructionCorrectness` socket) — together with the **irreducible hardness** of the witness (`HardFor fbool s`, the circuit lower bound itself) and the **proved low-intersection design**. So `HardFunction → PRGExists` is no longer a monolithic residue. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NWReconstructionAssembly` |
| **NW-derandomisation — `HardFunction → MA ⊆ NP`, assembled (PROVED glue)** | Workstream A, step 2. Entry 307/313 took `derand : HardFunction → MA ⊆ NP` as a *hypothesis*; this discharges it via the Nisan–Wigderson hardness–randomness tradeoff. **`nw_hardFn_MA_subset_NP`** (PROVED) — from the hardness-to-PRG tradeoff (`hardToPRG : HardFunction → PRGExists`, NW/IW) and the PRG-collapse (`PRGCollapsesMAtoNP`, replace Arthur's coins by the poly-many PRG seeds, decide nondeterministically), `HardFunction → MA ⊆ NP`: chain `Derand ⟹ PRG ⟹ MA ⊆ NP` (`derandMASubsetNP_via_PRG`) at `Derand := HardFunction`. **`hardFnSeparation_fully_assembled`** (PROVED) — combining the Karp–Lipton MA inclusion (313) and this, entry 307's `HardFnSeparation` is discharged with **no** `karpLipton`/`derand` hypotheses — only the named residues `NexpEqMIP`, `MIPRealizedGuessable` (BFL/IKW), `HardFunction → PRGExists`, `PRGCollapsesMAtoNP` (NW/IW), the trivial `NP ⊆ MA ⊆ NEXP`, and the proved `NEXP ≠ NP`. Honest scope: this removes the NW-derandomisation piece of caveat 1 — `HardFunction → MA ⊆ NP` is no longer a monolithic socket but assembled (hardness ⇒ PRG ⇒ seed-enumeration collapse), with the chaining proved, reduced to two *named* classical residues: `DerandGivesPRG` (NW/IW hardness-to-pseudorandomness) and `PRGCollapsesMAtoNP` (PRG-seed enumeration). Both are *proven*-classical theorems, left as named sub-sockets; their full pseudorandomness internals (PRG-fooling details) are workstream-A step 3. With this, `HardFnSeparation` rests on purely named residues. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NWDerand` |
| **Karp–Lipton MA inclusion — `NEXP ⊆ ACC⁰ → NEXP ⊆ MA`, assembled (PROVED glue)** | Workstream A (close the classical Williams residues), step 1. Entry 307 took `karpLipton : NEXP ⊆ ACC⁰ → NEXP ⊆ MA` as a *hypothesis*; this discharges it, assembling the implication from the Babai–Fortnow–Lund decomposition. **`karp_lipton_NEXP_ACC_to_MA`** (PROVED) — from `NexpEqMIP` (`NEXP = MIP`, BFL) and `MIPRealizedGuessable` (under circuits the `MIP` prover is a small circuit, IKW), `NEXP ⊆ ACC⁰ → NEXP ⊆ MA`: the guessable-prover discharge (`mipSubsetMA_of_realized`, mechanism `mipLang_eq_maLang` — Merlin guesses the prover circuit `C`, Arthur runs the verifier `Ver x (proverOf C)`) gives `MIP ⊆ ACC⁰ → MIP ⊆ MA`, and the BFL composition (`nexpSubsetMA_via_MIP`) rewrites it through `NEXP = MIP` to the `NEXP`-level inclusion. **`hardFnSeparation_via_karpLipton`** (PROVED) — entry 307's `HardFnSeparation` with its `karpLipton` hypothesis **discharged**: now from `NexpEqMIP` + `MIPRealizedGuessable` + NW-derandomisation + the proved `NEXP ≠ NP`. Honest scope: this removes the Karp–Lipton piece of caveat 1 — `NEXP ⊆ ACC⁰ → NEXP ⊆ MA` is no longer a monolithic socket but assembled, with all glue and the Merlin-sends-circuit / Arthur-verifies mechanism proved, reduced to two *named* classical residues: `NexpEqMIP` (the BFL `NEXP = MIP` theorem — its sum-check arithmetic engine proved, entries 226–227) and `MIPRealizedGuessable` (the IKW small-prover under circuits — its collapse mechanism `mipLang_eq_maLang` proved). Those two are *proven*-classical theorems (BFL 1991, IKW), each a major formalisation, left as named sub-sockets. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0KarpLiptonMA` |
| **IKW easy-witness (target 3) — a hard function ⇒ the separation, decomposed (PROVED glue)** | The deepest Williams ingredient. `…ACC0IKWEasyWitness` reduced the easy-witness lemma to (1) `NoEasyWitnessHardFn` (no easy witness ⇒ a hard function) — *already discharged* for the concrete `Circ` predicates (entry 150) — and (2) `HardFnSeparation` (a hard function ⇒ the separation). This decomposes (2). **`hardFnSeparation_from_parts`** (PROVED) — `HardFnSeparation NEXP ACC0 HardFunction` from: Karp–Lipton (`NEXP ⊆ ACC⁰ → NEXP ⊆ MA`), NW-derandomisation (`HardFunction → MA ⊆ NP`), the trivial inclusions `NP ⊆ MA ⊆ NEXP`, and `NEXP ≠ NP` (entry 200, proved by diagonalisation). The chain: `HardFunction` + `NEXP ⊆ ACC⁰` give `NEXP ⊆ MA ⊆ NP` (Karp–Lipton then derandomisation), and `NP ⊆ MA ⊆ NEXP` gives `NP ⊆ NEXP`; antisymmetry yields `NEXP = NP`, contradicting `NEXP ≠ NP` — so `HardFunction → ¬ (NEXP ⊆ ACC⁰)`. **`ikw_from_parts`** (PROVED) — the full IKW easy-witness lemma assembled: sub-socket 1 (`NoEasyWitnessHardFn`, discharged) + the decomposition of sub-socket 2 ⇒ `EasyWitnessLemma`. Honest scope: this decomposes IKW's deepest remaining atom (`HardFnSeparation`) into the Karp–Lipton circuit-collapse + NW-derandomisation `MA ⊆ NP` + the *proved* `NEXP ≠ NP`, and proves the composition (inclusion-chasing + antisymmetry). So IKW now rests on exactly those two classical sub-sockets — both proven-classical theorems decomposed elsewhere in the corpus (entries 199/202/221/222/229), with `NEXP ≠ NP` proved (200) and `NoEasyWitnessHardFn` discharged (150) — not open obstructions (`NEXP ⊄ ACC⁰` is Williams 2011). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0IKWHardFnSeparation` |
| **NW guess-verify — verify the guessed witness inside the fast-SAT framework (PROVED)** | Target 2 of the Williams socket elimination (link 3, Nisan–Wigderson / guess-and-verify). The pipeline is already decomposed: the decider is correct (`…ACC0GuessVerify.gvDecider_eq`), the two phases compose additively in time (`…ACC0GuessVerifyTime.guess_verify_within`), and the collapse follows (`guessVerify_subset`). This connects the **verify phase to the fast-SAT**. **`nw_guess_verify_within_fast`** (PROVED) — guess a *small* witness-circuit code (reaching `mid` in `guessT ≤ 2^{n-k}` steps) and verify it via the fast-SAT (`acceptsFrom M mid verifyT`, `verifyT ≤ 2^{n-k}`, entry 291): by the proved two-phase composition, the decider accepts within `2 · 2^{n-k}` — the `2ⁿ/superpoly` fast budget (`= 2ⁿ / 2^{k-1}` for `k = ω(log n)`), `omega` discharging `guessT + verifyT ≤ 2·2^{n-k}`. **`verify_fastSat_savings`** (PROVED) — the verify phase's fast-SAT work `≤ 2^{n-k}` delivers the `2^k` savings over brute force `2ⁿ`: `2^k · work ≤ 2ⁿ` (entry-291 `fastSat_savings_of_work_le`) — the speedup that makes verification fast. Honest scope: this connects NW's verify phase to the fast-SAT — a guessed small `ACC⁰` witness is verified by the `ACC⁰`-SAT speedup in `≤ 2^{n-k}`, so the guess-and-verify decider runs in `2ⁿ/superpoly`. With the decider's correctness and the collapse already proved, the NW socket reduces to its two phase-realisation facts: the **guess** phase is poly (the IKW small-witness reduction — Williams link 1, separate target 3) and the **verify** circuit is `ACC⁰`-SAT-searchable (entry 291, via the YBT exact form) — both proven-classical ingredients, formalization engineering, not open obstructions. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NWGuessVerify` |
| **The universal step overhead `B` — one step's resource footprint is polynomially bounded (PROVED)** | The transition-table compile socket's logical side is done (entry 304); its physical side needs an overhead bound `B` — one logical universal step (decode → lookup → apply → re-encode) must cost only `B` of `U`'s own primitive steps. This proves the **resource footprint** of one step is bounded by a concrete `B = poly(M.length, |x| + k)`. **`stepOverhead mLen tapeBound := mLen + tapeBound + 1`** (scan + region + `O(1)`). **`head_bound_from_init`** (PROVED) — from the initial config `(0,0,x)`, the simulated head after `k` steps is `≤ k` (`reachIn_head_le` with start head `0`): the touched tape region is `≤ |x| + k`, a bounded region, not the whole tape. **`uStep_cost_bound`** (PROVED) — at any config `c` reached in `≤ k` steps, one universal step's footprint `(matchingRules M c.1 (readSym c)).length + (c.2.1 + 1)` (rules scanned + tape region up to the head) is `≤ stepOverhead M.length (|x| + k)`, from the rule-scan bound (`matchingRules_length_le`, `≤ M.length`) and the head bound (`≤ k`), closed by `omega`. Honest scope: this proves the per-step overhead bound `B = poly(M.length, |x| + k)` on the *resources* one universal step needs — rule scan and the bounded-head tape region — assembling the proved `RuleLookup` and `HeadLocation` contracts; exactly the `B` the compile socket asked for, at the resource level. What remains is `U`'s transition *rules* executing one resource-operation per primitive step (the decode/lookup/rewrite interpreter loop as `U`-rules) — classical Turing-machine engineering over the proved sub-machine contracts, not an open problem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalOverhead` |
| **Phase-internal refinement, completed — decode/re-encode (tape walk) and apply (`O(1)`) (PROVED)** | Entry 310 cell-refined the lookup phase; this grinds out the remaining three, same fold-as-small-steps pattern. **`walkStep`** / **`walkNTM`** — a tape-cell traversal: consume the head cell per step, folding via `g`. **`walk_reaches`** (PROVED) — from `(cells, acc)`, in exactly `cells.length` primitive steps, reach `([], cells.foldl g acc)` (induction on `cells`, `List.foldl_cons` per step): the cell-cost model for **decode** (reading the encoded tape) and **re-encode** (writing it) — `|tape|` primitive cell-steps (their correctness is the already-proved function-level `decodeSim_encodeSim`). **`applyStep`** / **`applyNTM`** — the apply micro-machine (a 3-step controller). **`apply_phase_celled`** (PROVED) — applying a rule is exactly **3** primitive field-updates (write the symbol, move the head, set the state) composing to `applyTrans c t`: `reachIn (applyNTM t) 3 (0, c) (3, applyTrans c t)` (the explicit three-step trace, `rfl` per step). Honest scope: with lookup (entry 310), **all four phases are now cell-refined to exact primitive step counts** — decode `|tape|`, lookup `M.length`, apply `3`, re-encode `|tape|`, together the per-`uEncStep` `stepOverhead` (entry 305). Pure mechanical list/automata bookkeeping, classical, not an open problem. The transition-table compile is fully ground from logical tracking down to primitive single-operation steps. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PhaseCellOpsRest` |
| **Phase-internal refinement — the lookup phase as primitive single-rule-comparison steps (PROVED)** | Entry 309 built `physU` at *phase* granularity and noted the only refinement left: expanding a phase macro-step into primitive single-cell operations. This does that for the representative non-trivial phase — **lookup** (rule scan). **`scanStep`** processes one rule per step: pop the head of the remaining-rules list, append it to the accumulator iff it matches. **`scanNTM`** is the machine. **`scan_reaches`** (PROVED) — from `(rules, acc)`, in exactly `rules.length` steps, the scanner reaches `([], acc ++ rules.filter p)` (induction on `rules`; `List.filter_cons` + append-assoc per step). **`scan_phase_celled`** (PROVED) — from `(M, [])`, in exactly `M.length` primitive single-rule comparisons, the scanner reaches `([], matchingRules M state sym)`: the lookup phase macro-step realised as `M.length` primitive operations, the exact step count matching the entry-305 resource bound (`≤ M.length`), now as a literal step count. Honest scope: this refines the lookup phase macro-step into primitive single-rule-comparison steps with the exact count `M.length` — the representative phase-internal refinement. Decode/re-encode (tape-cell traversal, `|tape|` single-cell steps) and apply (`O(1)`) follow the same fold-as-small-steps pattern. Pure mechanical list/automata bookkeeping, classical, not an open problem. With this, the transition-table compile is ground from logical tracking down to primitive single-operation steps. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ScanCellOps` |
| **The physical universal machine — `perStep` discharged by an explicit phased controller (PROVED)** | Entry 308 reduced the transition-table compile to two explicit hypotheses (`perStep`, `compose`/`refl0`); this discharges them with a concrete machine `physU`, an explicit four-phase controller genuinely distinct from the logical `uEncNTM`. **`PUConfig`** carries the phase: `tape s` (holding the encoded tape), `dec M c` (decoded), `loc M c t` (rule located), `app M d` (applied). **`physStep`** advances one phase per step, each move justified by a proved phase contract: `tape s →[decodeSim s = some (M,c)] dec M c →[t ∈ matchingRules M c.1 (readSym c)] loc M c t →[d = applyTrans c t] app M d →[u = encodeSim M d] tape u`. **`physU`** is the NTM. **`perStep_phys`** (PROVED) — `perStep` discharged: `uEncStep s u → reachIn physU 4 (tape s) (tape u)`, the four-phase trace laid explicitly, each step from `uEncStep`'s data via the decode / rule-lookup (`mem_matchingRules`) / apply / re-encode contracts. **`physU_tracks`** (PROVED) — the full run: `reachIn uEncNTM k s t → reachIn physU (k * 4) (tape s) (tape t)`, via entry-308's `physical_tracks_lift` with `physU`, `perStep_phys`, and `reachIn_add` for composition — **no remaining hypotheses**. Honest scope: this discharges entry-308's `perStep`/`compose`/`refl0` hypotheses with a concrete, explicit phased machine, proving `uEncStep` realized in 4 phase-steps and lifting to `4k` for the full run. `physU` is modelled at *phase* granularity (each macro-step is one phase); the per-phase *time* is the entry-305 `stepOverhead` bound, so the run is realized in `4k` macro-steps of `O(stepOverhead)` time. The only refinement left is expanding a phase macro-step into primitive single-cell tape operations — a phase-internal detail; the controller and its step-count/time bounds are proved. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PhysicalUniversalMachine` |
| **The physical universal interpreter loop — phases assembled, multi-step cost `k·B` (PROVED)** | Closing the transition-table compile socket's last mechanics. Entry 304 proved the logical tracking; entry 305 the per-step overhead `B`. This assembles the **interpreter loop** from the proved phase contracts and proves the **multi-step cost lift** `k·B`. **`physical_uStep_phases`** (PROVED) — one universal step on a genuine `M`-step `concreteStep M c d` via the four proved phases: **decode** (`decodeSim (encodeSim M c) = some (M, c)`), **lookup** (a matching rule `t ∈ matchingRules M c.1 (readSym c)` with `d = applyTrans c t` and scan cost `≤ M.length`, `lookup_phase`), **apply** (`applyTrans`), **re-encode** (`decodeSim (encodeSim M d) = some (M, d)`) — bundled with the scan-cost bound. **`physical_tracks_lift`** (PROVED) — the multi-step cost: given a physical realization `Realizes` that handles one `uEncStep` in `B` steps (`perStep`) and composes additively (`compose`, `refl0`), an entire encoded run `reachIn uEncNTM k s t` is realized in `k · B` physical steps (induction on `k`, accumulating `B` per step; `(k+1)·B = B + k·B` by `ring`). This is `physicalU_tracks_uEncNTM` with `C = 0`. Honest scope: the interpreter loop is assembled from the proved phase contracts and the multi-step cost lift `k·B` is proved. The two remaining last physical primitives, isolated as the hypotheses of `physical_tracks_lift`: (i) `perStep` — one `uEncStep` realized by the concrete machine `U` in `B = stepOverhead` primitive steps (the decode→lookup→apply→re-encode loop, one tape-operation per step, entry 305); (ii) `compose`/`refl0` — `U`'s reachability composes additively (standard). Both are classical Turing-machine engineering over the proved sub-machine contracts, not open problems. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PhysicalUniversalLoop` |
| **The universal run — the encoded universal machine tracks the simulated machine's full computation (PROVED)** | `…ACC0UniversalHStep` proved the *single*-step universal loop is faithful (decode → apply firing rule → re-encode advances the simulated machine by one genuine step and round-trips); this lifts it to the **multi-step run**. **`uEncStep s s' := ∃ M c t, decodeSim s = some (M, c) ∧ t ∈ M ∧ t.1 = (c.1, readSym c) ∧ s' = encodeSim M (applyTrans c t)`** — the encoded universal step (`U`'s logical step: decode → lookup firing rule → apply → re-encode); **`uEncNTM`** packages it as an NTM (tapes as configs). **`uEncStep_of_concreteStep`** (PROVED) — one `concreteStep M c d` lifts to one `uEncStep (encodeSim M c) (encodeSim M d)` (decode round-trips via `decodeSim_encodeSim`, same rule fires, re-encoding matches). **`uEncNTM_tracks`** (PROVED) — an entire `M`-computation lifts to an encoded run: `reachIn (toNTM M) k c c' → reachIn uEncNTM k (encodeSim M c) (encodeSim M c')` (induction on `k` via `uEncStep_of_concreteStep`) — the encoded universal machine simulates `M`'s **full** `k`-step computation, the multi-step counterpart of the single-step `hstep` contract. Honest scope: this proves the multi-step faithfulness of the encoded universal machine, assembling the single-step `hstep` contract over an arbitrary `M`-run — closing the *logical* side of the transition-table compile socket. What remains is the **physical realisation**: encoding the logical `uEncStep` (decode/lookup/rewrite/re-encode) as the explicit transition rules of a concrete `TMachine` `U` with a per-step overhead bound `B`, over the proved sub-machine contracts (`…ACC0UniversalDecode`, `…ACC0RuleLookup`, `…ACC0TapeRewrite`, …) — classical Turing-machine engineering, not an open problem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalRun` |
| **The lazy-diagonal decider assembly — complement-free hierarchy, one socket left (PROVED)** | Assembling the lazy-diagonal pieces into the nondeterministic time hierarchy, complement-free. **`lazy_diag_not_in_smaller`** (PROVED) — if the smaller class is enumerated (`Smaller ⊆ Set.range enum`), the lazy diagonal (which escapes the whole range, entry 302) is not in it: `lazyDiagLang enum ∉ Smaller`. **`lazy_time_hierarchy`** (PROVED) — given the enumeration of the smaller class and the decider socket `hbig : lazyDiagLang enum ∈ Bigger`, the bigger class is **not** contained in the smaller: `¬ (Bigger ⊆ Smaller)` (`fun hsub => lazy_diag_not_in_smaller henum (hsub hbig)`). Unlike the Cantor version (`…ACC0TimeHierarchyDiagonal`, which uses the *complement* diagonal and so needs co-nondeterminism), this rests on the **lazy** escape (one boundary complement per block, entry 302), so it is honest for nondeterministic classes. Honest scope: this assembles the lazy-diagonal hierarchy implication completely from the unconditional escape (302) and the decider-membership socket. The one remaining socket — `lazyDiagLang enum ∈ Bigger`, the lazy diagonal decided within the bigger time bound — is supported by the proved realization pieces: the **copy** positions by the universal simulation (296/297, overhead 1) of `enum i` on the next input, clocked to the bigger bound (298); the **boundary** position by the decidable complement (299). The only un-built part is compiling the routing into one transition-table `TMachine` (the `…ACC0UniversalHStep` physical line) — a proven-classical construction, formalization engineering, not an open obstruction (`NEXP ⊄ ACC⁰` is Williams 2011). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LazyDeciderAssembly` |
| **The lazy diagonal, concretely constructed — block bookkeeping, unconditional escape (PROVED)** | Entry 294 proved the lazy diagonal escapes the enumeration *given* a block layout; this constructs that layout concretely (the per-block bookkeeping the decider routes over) and discharges 294's hypotheses for an **unconditional** escape. **`lazyDiagLang enum n := if n % 2 = 0 then enum (n/2) (n+1) else ! enum (n/2) (n-1)`** — the simplest block layout: block `i = {2i, 2i+1}` (`block i = 2i`, `len i = 1`), copying `enum i` on the next input at the even position `2i` and complementing at the block start on the odd boundary `2i+1`. Every input lies in exactly one block at one position, so `lazyDiagLang` is total and the blocks tile `ℕ`. **`lazyDiagLang_escapes`** (PROVED) — **unconditional**: `lazyDiagLang enum ∉ Set.range enum` for *every* enumeration `enum` (entry 294's `lazy_diag_not_mem_range` with `block`/`len` discharged: the two lazy equations are arithmetic facts about `2i`, `2i+1`, closed by `omega` after `interval_cases k` on `k < 1`). Honest scope: this constructs the lazy diagonal's block bookkeeping concretely and proves the escape unconditionally — the structural skeleton of the lazy-diagonal decider (a total language tiling `ℕ` into copy/boundary positions per index, escaping every enumeration). What remains is the **decider machine** computing `lazyDiagLang` within the bigger time bound — copy positions via the universal simulation (296/297) of `enum i` on the next input, the boundary via the decidable complement (299), clocked (298) — physical engineering over this layout, proven-classical, not an open obstruction. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LazyDiagonalConstruction` |
| **Lazy-diagonal decider — the boundary complement realized: bounded acceptance is decidable (PROVED)** | The realization socket is down to the lazy-diagonal decider machine (entry 298); its hard case is the **boundary complement** (`D(boundary) = ¬ enum i (start)`). For a *nondeterministic* machine this is not free — except, as Cook's lazy diagonalization exploits, the start input is tiny, so the bounded computation is exhaustively explorable. This entry proves the foundation: **bounded acceptance in the concrete model is decidable**. **`succs`** / **`mem_succs`** (PROVED) — `concreteStep M` has finite one-step successors (`M` is a finite rule list): `d ∈ succs M c ↔ concreteStep M c d` (`List.mem_map`/`mem_filter`). **`reachExactly`** / **`mem_reachExactly`** (PROVED) — the finite list of `k`-step reachable configs enumerates `reachIn`: `d ∈ reachExactly M k c ↔ reachIn (toNTM M) k c d` (induction on `k`, `List.mem_flatMap`). **`decAccept`** / **`acceptsWithin_iff_decAccept`** (PROVED) — `acceptsWithin (toNTM M) x t` is the finite bounded search `∃ k ≤ t, ∃ c ∈ reachExactly M k (0,0,x), c.1 = 1`. **`instDecidableAcceptsWithin`** (PROVED) — hence `Decidable (acceptsWithin (toNTM M) x t)`. **`boundary_complement_decidable`** (PROVED) — `Decidable (¬ acceptsWithin (toNTM M) x t)`: complementing a bounded nondeterministic computation is a finite, computable operation. Honest scope: this is the crux of *why* lazy diagonalization's single boundary complement is affordable — the exhaustive exploration of the small bounded computation — now machine-checked over the transition-table model, the foundation of the lazy-diagonal decider's boundary case. The full decider still needs the per-block bookkeeping assembly (decode the block index, route copy-vs-boundary — copy via the universal simulation of 296/297, boundary via this decidability — with the clocking of 298) — physical engineering, proven-classical, not an open obstruction (`NEXP ⊄ ACC⁰` is Williams 2011). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundedAcceptanceDecidable` |
| **Realization wiring — clocking the universal machine to the bigger time bound (PROVED)** | The realization socket's universal machine is formalized (entries 296/297); the remaining *wiring* toward the hierarchy's `diag_in_big` is to clock the universal simulation to the **bigger** bound `f` (the diagonal must decide within `f` a language whose machines run within the smaller `g ≤ f`). This entry supplies the clocking — the genuinely-quantitative step — combining overhead-1 universal simulation with budget monotonicity (`acceptsWithin_mono`). **`universal_clocked_within`** (PROVED) — `univNTM ⟨M⟩` accepts `x` within any `t' ≥ t` for which `M` accepts `x` within `t` (rewrite by entry-296 `univ_simulates_exactly`, then `acceptsWithin_mono`): a `g`-time machine, simulated universally, fits any `f ≥ g`. **`uniformU_clocked_within`** (PROVED) — the same for the single fixed machine `uniformU decPair` on the encoded input `encPair ⟨M⟩ x` (entry-297 `uniformU_simulates_enc` + `acceptsWithin_mono`): the full "decode the code, simulate, clock to the bigger bound" for one universal machine. **`cNTIME_of_decider`** (PROVED) — a `TMachine` deciding `L` within `f` puts `L ∈ cNTIME f` (the membership packaging). **`diag_in_big_of_clocked_decider`** (PROVED) — the hierarchy's remaining socket (the diagonal `∈ cNTIME f`) reduces to a single `TMachine` deciding the diagonal within `f`; the clocking ensures such a decider's time bound is achievable. Honest scope: the realization wiring's quantitative core — the "clock to the bigger bound" step — is proved (free, given overhead 1). What remains for `diag_in_big` is *building* the lazy-diagonal decider machine: the per-block bookkeeping feeding each `enum i` its input with one boundary complement, compiled over the universal machine (pieces under development in `…ACC0UniversalHStep` and the decode/lookup/rewrite sub-machines) — a proven classical fact, formalization engineering, not an open obstruction (`NEXP ⊄ ACC⁰` is Williams 2011). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RealizationClocking` |
| **Decode-from-input uniformity — one fixed machine simulates every machine (PROVED)** | The last primitive of the universal-simulation socket (entry 296 left the *decode-from-input uniformity*): collapse the family `univNTM code` (code a Lean parameter) to a **single fixed machine** that reads the code from its input. Carry the code as *configuration data*: **`UConfig := ℕ × CConfig`** (decoded code, machine config), **`uStep p q := p.1 = q.1 ∧ univStep p.1 p.2 q.2`** (preserve code, apply `univStep code`), and **`uniformU dec`** — the single machine whose only parameter is the fixed, *machine-independent* parse `dec : List Bool → ℕ × List Bool` (the universal machine's own decoding logic), decoding the code at `init` and accepting on the machine state. **`uStep_preserves_code`** (PROVED) — the decoded code is preserved along every run (induction). **`uStep_reachIn`** (PROVED) — `uniformU`'s `k`-step reachability on code-carrying configs *equals* `univNTM code`'s, for every `k` (induction; the code is data, the step is `univStep code`): uniform, overhead 1. **`uniformU_simulates`** (PROVED) — if input `w` decodes to `(⟨M⟩, x)`, then `acceptsWithin (uniformU dec) w t ↔ acceptsWithin (toNTM M) x t` (combining `uStep_reachIn` + entry-296 `univ_simulates_exactly`): the single machine reads the code from input and simulates `M` exactly. **`decPair_encPair`** (PROVED) — a concrete encoding (`encPair`: `replicate code true ++ false :: x`; `decPair`: parse the leading `true`-run as the code) round-trips: `decPair (encPair code x) = (code, x)` (via `takeWhile`/`drop` inductions). **`uniformU_simulates_enc`** (PROVED) — self-contained: `uniformU decPair` on `encPair ⟨M⟩ x` accepts within `t` iff `M` accepts `x` within `t`. Honest scope: with entries 296+297 the universal NTM simulation is formalized — **one** uniform machine `uniformU decPair`, reading the code from its input, simulating *every* machine with overhead exactly 1: the universal TM (Turing 1936) over the transition-table model. The realization socket's universal-machine content is discharged. It does **not** by itself prove the time hierarchy — the remaining *wiring* (feed the diagonal its own code, clock to the bigger bound, the per-`x` block bookkeeping) plus the other two gate sockets (IKW easy-witness, NW guess-verify) are proven-classical formalization, not open obstructions. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DecodeUniformity` |
| **Formalizing the universal NTM simulation socket — overhead is exactly 1 (PROVED)** | The universal-simulation socket (the realization primitive: a universal machine simulating any `M` within a time bound) had its *interpreter* level proved (`…ACC0UniversalNTM`: `univStep ⟨M⟩ = concreteStep M` exact at the relation level; `enum_covers` proved), with the hierarchy reduced to one socket `diag_in_big` — the diagonal decidable within the bigger bound, "where the physical overhead matters." This entry supplies the **quantitative overhead bound: exactly 1**. **`univNTM code`** — the universal NTM at a machine code (Config `CConfig`, step `univStep code`, standard `init`/`accept`). **`univNTM_step_iff`** (PROVED) — at `code = ⟨M⟩`, `univNTM`'s step *is* `M`'s step (from `univStep_correct`). **`univNTM_reachIn`** (PROVED) — hence the `k`-step reachability of `univNTM ⟨M⟩` *equals* `M`'s, for every `k` (induction on `k` lifting the step equivalence): no per-step overhead. **`univ_simulates_exactly`** (PROVED) — `acceptsWithin (univNTM ⟨M⟩) x t ↔ acceptsWithin (toNTM M) x t`: the universal machine accepts `x` within `t` steps iff `M` does — the *same* time bound, no blowup (`init`/`accept` coincide; reachability matches by `univNTM_reachIn`). **`univ_simulation_no_time_blowup`** (PROVED) — restated as language equality at every budget `t`. Honest scope: the socket's feared "physical overhead" is **exactly 1** — free; simulating `M` for `t` steps takes exactly `t` steps. What remains of the universal-simulation socket is *not* an overhead bound but the **decode-from-input uniformity**: collapsing the family `univNTM` to a single fixed machine that reads the code `⟨M⟩` off its tape and runs as `univNTM ⟨M⟩` (the universal TM, Turing 1936) — a *proven* classical fact, formalization engineering, not an open obstruction (`NEXP ⊄ ACC⁰` is Williams 2011). The socket's quantitative heart is proved; one tape-decode primitive remains. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalSimulationOverhead` |
| **Williams internalized as the N-Frame algorithmic-counting branch (PROVED equivalence + socketed gate)** | Williams' `NEXP ⊄ ACC⁰` route, encoded directly as the N-Frame *algorithmic-counting branch* — no longer external commentary. The four N-Frame ingredients (all proved-structure from entries 290–294): *observer* = the integer gate-count `gateCount` (entry 291); *boundary* = its count-cell image; *compression* = `< 2^n` count cells (the YBT exact `SYM∘AND` form, entry 292); *escape* = characteristic-0 CRT universality (entries 290/291, the integer count carries every characteristic). **`NFrameWilliamsRoute := ∀ (n) (C : ACC0Circuit n), ∃ m mono top, eval C = symEval (monoAND ∘ mono) top ∧ m + 1 < 2^n`** and **`WilliamsFastSatRoute := ∀ n C, HasExactSymAndForm C`**. **`nframe_williams_route_equiv`** (PROVED, `Iff.rfl`) — `NFrameWilliamsRoute ↔ WilliamsFastSatRoute`: the N-Frame algorithmic-counting branch *is* the fast-SAT route, **definitionally** (the four ingredients repackage `HasExactSymAndForm` exactly). **`nframe_observer_characteristic_free`** (PROVED) — the escape: the integer count observer decides `MOD_M`-SAT for *every* modulus `M` against one count-cell image (entry 291), the char-0 universality that handles composite `MOD` where the single-field polynomial method is blocked (`no_common_char`, entries 280–289). **`nframe_fastSat_to_timeHierarchy`** (PROVED conditional) — the hard gate, encoded: the N-Frame route ⇒ (uniform realization socket) a speedup ⇒ (easy-witness/NW collapse socket) `NTIME f ⊆ NTIME g` ⇒ (nondeterministic time hierarchy socket, `¬ NTIME f ⊆ NTIME g`) `¬ (NEXP ⊆ ACC⁰)`, via `williams_concrete` (composition proved glue). **`nframe_hierarchy_diag_core`** (re-export) — the hierarchy subpiece's diagonalization core is proved (entry 294 lazy escape, `D ∉ range enum`), leaving only the realization primitive. Honest scope: Williams is now the N-Frame algorithmic-counting *route* — the counting twin of the blocked polynomial route, where the integer-count observer's char-0 universality escapes the composite barrier — proved equivalent to the fast-SAT route, feeding the contradiction whose hierarchy core is proved, with the deep gate honestly socketed to the named *proven* classical theorems (uniform realization, easy-witness/NW, nondeterministic time hierarchy — Williams 2011, being formalized, not open). The two N-Frame branches (polynomial: blocked; algorithmic-counting: Williams) are both encoded. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NFrameWilliamsRoute` |
| **Attacking the classical gate directly — the NTIME hierarchy's diagonalization logic is fully proved (PROVED)** | Attacking the single classical gate (the nondeterministic time hierarchy inside Williams' route) to its core. **Note: `NEXP ⊄ ACC⁰` is a *proven* theorem (Williams 2011)** — the gate is its *formalization*, not an open problem. The diagonalization logic was already decomposed deep: the lazy telescoping *contradiction* `lazy_diag_false` (entry 219, complement-free — only the single boundary flip, so valid for nondeterministic classes) and the `NEXP`-*placement* (`clocked_in_NEXP`, NTIME accounting) are proved; but entry 200's class-*escape* `diag_not_mem_range` used the *naive direct-complement* diagonal, **not** valid for nondeterministic classes. This entry supplies the complement-free upgrade. **`lazy_diag_escapes_enumeration`** (PROVED) — with `D` set up on each index `i`'s block to lazily copy `enum i` on the next input (`D(block i + k) = enum i (block i + k + 1)` for `k < len i`) and complement *only* at the boundary (`D(block i + len i) = ¬ enum i (block i)`), the proved lazy kernel `lazy_diag_false` gives `D ≠ enum i` for every `i` — using only the single boundary complement per block. **`lazy_diag_not_mem_range`** (PROVED) — hence `D ∉ Set.range enum`: the lazy diagonal escapes the *entire* enumeration of the smaller class, the complement-free analog of entry 200's Cantor escape, the form that genuinely applies to nondeterministic classes. Honest scope: this completes the *diagonalization logic* of the nondeterministic time hierarchy — contradiction (219), placement (accounting), and enumeration escape (here) all proved. What remains is **not** diagonalization but the **realization** primitive — a clocked NTM deciding `D` in the bigger time bound via universal simulation (`ClockedSimulation`), a *proven* classical fact (efficient universal NTM), formalization engineering rather than an open obstruction; likewise IKW easy-witness and NW guess-verify are proven classical lemmas. The gate is the formalization of Williams' **theorem** (2011), decomposed to model-substrate and classical-lemma atoms — *not* an open obstruction; the genuine open frontier (`P ≠ NP`) lies elsewhere. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LazyHierarchyEscape` |
| **Probing the realization socket — no new separation-strength obstacle; the route has one deep gate (PROVED)** | The realization socket (Williams link 2) was already split into S1–S4 (`…ACC0WilliamsRealizationSplit`): S1 encoding [routine], S2 cost bridge [arithmetic proved, `cell_count_savings`], S3 uniformity [standard], S4 time-hierarchy [separation-strength, self-audited `↔ NEXP⊄ACC⁰`]. This entry completes the picture by proving the one link that was only *gestured at*. **`cost_bridge_unlocked_by_quasipoly_ybt`** (PROVED, the missing link) — `proved_speedup_is_trivial_savings` had only the `k = 0` (factor-`>1`) savings and *described* the super-poly case as "supplied by the quasipoly exact form"; this proves it: a quasipolynomial cell bound `cells ≤ (D+1)·n^D + 1` (the content of the YBT *size* socket, entry 292) fitting `≤ 2^{n−k}` delivers the genuine `2^k` super-poly savings (`cell_count_savings ∘ le_trans`). So the realization cost bridge bites *exactly* when the (true-classical) YBT quasipoly-size wall is crossed — the two non-deep walls compose, no separation-strength content between them. **`realization_separation_strength_is_S4`** (re-export) — the realization socket's sole separation-strength piece is S4, `↔ NEXP⊄ACC⁰`. **`williams_route_single_separation_gate`** (PROVED) — the realization S4 socket and the downstream Williams socket have the *same* shape `(established hypothesis → NEXP⊄ACC⁰)`, each `↔ NEXP⊄ACC⁰` once its antecedent holds: so they are the *same* gate (the nondeterministic-time-hierarchy / Williams argument), not two separate deep gates. **`williams_route_socket_classification`** (PROVED bundle) — the full-route map: (a) the exact `SYM∘AND` form exists unconditionally (entry 292); (b) the YBT quasipoly size *unlocks* the realization cost bridge into super-poly savings; (c) given the routine sub-sockets the realization socket reduces to S4 (`routine_reduce_to_timeHierarchy`); (d) S4 `↔ NEXP⊄ACC⁰` once a uniform speedup holds. Honest scope: the realization socket adds **no** new separation-strength obstacle — routine parts grounded (S1 encoding, S3 uniformity), cost bridge S2 unlocked by the true-classical YBT size, and its sole deep piece S4 is the *same* gate as the Williams socket. So the entire algorithmic route (YBT + realization + Williams) has **one** irreducible separation-strength gate — the time-hierarchy / Williams algorithmic argument — with everything else proved, true-classical (YBT size), or routine. That single gate is `↔ NEXP ⊄ ACC⁰`, the honest open content. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0WilliamsSocketMap` |
| **Is the YBT socket separation-strength?  Answer: NO — the honest correction (PROVED)** | The test asked to confirm the YBT exact-normal-form socket is separation-strength; digging into the corpus, the answer is the **opposite**, and the discipline requires reporting it as found. The YBT socket is **not** separation-strength — it is a *known true classical theorem* (Beigel–Tarui) whose only open part is a quasipolynomial *size* bound on an *unconditionally existing* construction; the separation-strength lives downstream. **`exact_symAnd_form_exists`** (PROVED, re-export `acc0circuit_hasSymAndForm`) — every `ACC⁰` circuit equals a count gate over `symAndSize C` monomial-`AND`s, unconditionally, no socket. **`mod_form_characteristic_free`** (PROVED, re-export `hasSymAndForm_mod`) — a `MOD_q` gate has an exact form for *every* modulus `q` (composite included): the form is counting-based (`modQStatOn`, an integer weight), hence characteristic-free, so composite `MOD` is *no barrier to existence* (entry 290's escape inside the YBT reduction; the polynomial method's `no_common_char` barrier never touches the YBT form's existence). **`ybt_socket_is_size_bound`** (PROVED, re-export `acc0circuit_hasExactSymAndForm`) — once `symAndSize C + 1 < 2^n` the always-existing form *is* the socket `HasExactSymAndForm C`: the only open content is the quasipolynomial *size*, the true Beigel–Tarui theorem (hard combinatorics, not a hardness assumption). **`ybt_socket_yields_cell_search`** (PROVED) — the socket gives only a `< 2^n`-cell search, not a uniform algorithm. **`realization_is_separation_strength`, `williams_is_separation_strength`** (PROVED, re-export the self-audits) — once the cell speedup holds, the *realization* and *Williams* sockets are each *logically equivalent to* `NEXP ⊄ ACC⁰`: those carry the whole difficulty. **`ybt_socket_not_separation_strength`** (PROVED bundle) — the conclusion: the exact form exists unconditionally, the socket is just a size bound, and the separation-strength is the downstream realization socket. So even with YBT fully formalized, the separation would still need realization + Williams — which *are* separation-strength. Honest scope: this **corrects and answers** the question — YBT is the true/known combinatorially-hard front half, *not* separation-strength; the separation lives in the realization + Williams cash-out, already self-audited. Reporting this honestly rather than rubber-stamping "separation-strength" is the point. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0YBTSocketStrength` |
| **Digging into the `WilliamsFastSat` socket — the counting step is characteristic-universal (PROVED)** | Entry 290's escape made concrete *inside* the Williams fast-SAT (`…ACC0WilliamsFastSat`). The fast-SAT observable is `gateCount` — an integer count — and `observed_sat_iff` decides satisfiability by searching its image, which does **not** depend on the symmetric top. **`fastSat_universal_in_top`** (PROVED) — one count-cell image `image(gateCount g)` decides `symEval g h` for *every* symmetric top `h` (the image is independent of `h`). **`fastSat_decides_every_modulus`** (PROVED) — hence, for the `MOD_M` top and *every* modulus `M`, satisfiability is decided by the same image: `∃ c ∈ image, c % M = 0`. **`fastSat_mod6_via_crt`** (PROVED) — the `MOD₆` decision factors through the count's mod-2 *and* mod-3 residues simultaneously (`∃ c ∈ image, c % 2 = 0 ∧ c % 3 = 0`, via `omega`) — entry 290's CRT escape realised inside the algorithm, the integer count carrying both characteristics that no single field can. **`fastSat_cells_modulus_free`** (PROVED) — the `≤ m + 1` count-cell bound is independent of the modulus (`sym_count_card_le`). **`fastSat_modm_savings`** (PROVED) — the Williams savings `2^k · cells ≤ 2^n` hold for the `MOD_M` circuit for *every* `M` (instantiating `symAnd_williams_fastSat` at the `MOD_M` top): the modulus appears only in the top, irrelevant to the count, so the savings are modulus-agnostic. Honest scope: this proves the fast-SAT *counting/savings* step is characteristic-universal — one integer-count image serves all moduli — so the composite-`MOD` barrier is localised **entirely** to the open YBT exact-`SYM∘AND`-normal-form socket (link 1) and the uniform TM realisation socket (link 2), **not** the count search, which is exonerated. It is the concrete, fast-SAT-level form of entry 290's escape. It does **not** prove `NEXP ⊄ ACC⁰`: the YBT exact reduction for composite `MOD` and the uniform realisation remain the named separation-strength sockets (`…ACC0WilliamsFastSat`, `…ACC0WilliamsCashout`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FastSATCharacteristicUniversal` |
| **Why the algorithmic route escapes the composite barrier — integer counting carries all characteristics (PROVED)** | The honest pivot after the polynomial route was proved blocked (entries 280–289): Williams' `NEXP ⊄ ACC⁰` route is *algorithmic* (it counts satisfying assignments), and this isolates *why* that escapes the barrier. A count is an integer `c : ℤ` in characteristic 0; `ℤ` is the initial ring, mapping onto `ZMod m` for *every* `m`, so one integer carries all characteristics at once — exactly what no single field can do. **`countMod c m := decide (c % m = 0)`** — `MOD_m` read off the integer count. **`count_crt_mod6`** (PROVED, `omega`) — `c % 6 = 0 ↔ (c % 2 = 0 ∧ c % 3 = 0)`: one integer count realises `MOD₆`'s CRT decomposition via its mod-2 and mod-3 residues *simultaneously* — precisely what `no_common_char` forbids a single field. **`count_computes_all_moduli`** (PROVED, `rfl`) — the *same* count function computes `MOD_m` for every `m`. **`integer_carries_every_characteristic`** (PROVED) — `ℤ →+* ZMod m` exists for every `m` (`Int.castRingHom`): one integer inhabits every characteristic quotient. **`no_field_carries_two_primes`** (PROVED, re-export of `no_common_char`) — the contrast: a single field has only one prime characteristic. **`count_route_covers`** (PROVED) — for the count route the available characteristic set is `m.primeFactors` (reduce the integer mod each prime), so `CrossCharacteristic m m.primeFactors` is **false** for every `m ≠ 0`: the algorithmic observer is *never* characteristic-blocked, unlike the fixed-`{2,3}` polynomial method (entry 288). **`algorithmic_escape`** (PROVED) — bundles the three: integer counting (char 0, all primes) is not characteristic-bound, whereas the field-based polynomial method (one characteristic) is — the structural reason Williams went algorithmic. Honest scope: a machine-proved account of *why* the algorithmic/counting route escapes the composite-`ACC⁰` barrier that provably blocks the polynomial method — integer counting lives in characteristic 0 and carries every characteristic at once, sidestepping the `no_common_char` commitment. This isolates the structural *enabler* of Williams' route, tied to the typed invariant. It is **not** `NEXP ⊄ ACC⁰`: the deep `#SAT`-algorithm ⇒ lower-bound implication (the existing `…WilliamsFastSat` / Karp–Lipton / NW–IKW sockets) is not re-proved here. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AlgorithmicEscape` |
| **The semiring obstruction — the native composite escape fails even without subtraction (PROVED)** | Back to the open composite barrier (`NFRAME_TWO_ROUTES.md` §4): entry 300's obstruction used subtraction (`1 = 3 − 2`), so a natural escape candidate (case 4, "semiring / non-ring observers") is to drop subtraction. This **kills that candidate**. **`two_three_semiring_trivial`** (PROVED) — in any `AddCommMonoidWithOne R`, `(2 : R) = 0 → (3 : R) = 0 → (1 : R) = 0`, with **no subtraction**: `3 = 2 + 1` (`norm_num`), so `2 = 0` gives `3 = 0 + 1 = 1` (`zero_add`), hence `3 = 0 ⟹ 1 = 0`. **`no_nontrivial_semiring_both_native`** (PROVED) — no nontrivial `Semiring R` (where `Nontrivial ⟹ 1 ≠ 0`) is native to both `2` and `3`. Honest scope: this extends the Universal Native Characteristic Obstruction from commutative rings (entry 300) to all of `AddCommMonoidWithOne` — semirings, idempotent/tropical semirings, any structure with `+`, `0`, `1` — needing only `3 = 2 + 1`, not subtraction. So *no native additive structure whatsoever* can host both `MOD₂` and `MOD₃` natively (`2 = 0` and `3 = 0`) nontrivially: the semiring/non-ring native escape (§4 case 4) is eliminated. This does **not** close the composite barrier — the genuinely-open candidates remain *non-native* representations (no `p = 0` demanded), *staged observers without flattening*, and *probabilistic/approximate* composite representations (§4 cases 1–3). It narrows the frontier by removing the additive-structure escape. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SemiringObstruction` |
| **The universal characteristic obstruction — no nontrivial ring is native to two coprime moduli (PROVED)** | Pivoting back to the genuinely-open composite polynomial barrier (280–289): the no-go landscape (no common field, 280; product ring `ZMod 6` fails, 282) has a single **universal** root. **`two_three_native_trivial`** (PROVED) — any commutative ring with `(2 : R) = 0` and `(3 : R) = 0` has `(1 : R) = 0` (`linear_combination h3 - h2`, i.e. `1 = 3 - 2 = 0`). **`no_nontrivial_ring_both_native`** (PROVED) — hence no *nontrivial* commutative ring has both `2 = 0` and `3 = 0`: this subsumes the field no-go (280, `no_common_char`), the product-ring no-go (282, `ZMod 6` not a field / Fermat fails), and the tensor collapse `F₂ ⊗ F₃ = 0` — *every* candidate "combined characteristic" structure at once. **`coprime_native_trivial`** (PROVED) — the general form: for coprime `p, q`, any commutative ring with `(p : R) = 0` and `(q : R) = 0` has `(1 : R) = 0` (Bézout `u·p + v·q = 1` cast into `R` via `Int.castRingHom`). **`field_both_native_absurd`** (PROVED) — the field corollary (subsumes `no_common_char`). **`universal_native_characteristic_obstruction`** (PROVED, the stable named theorem) — the canonical reference: for coprime `p, q`, no nontrivial ring is native to both; **this blocks every native single-ring polynomial method for a composite modulus** (see `NFRAME_TWO_ROUTES.md`). Why this is the irreducible obstruction: the Razborov–Smolensky polynomial method represents `MOD_p` at low degree *only* where `p = 0` (the Fermat indicator `1 - x^(p-1)` computes `[count ≢ 0]` only when `y^(p-1) = 1` for `y ≠ 0`, i.e. in characteristic `p`); so a polynomial-method attack on `ACC⁰[6]` handling both `MOD₂` and `MOD₃` natively needs one ring with `2 = 0` *and* `3 = 0`, which is trivial. **Every native polynomial-method attack** (single field, product ring, tensor) fails for the one reason `1 = 3 - 2 = 0`. Connection to the counting escape (entry 290): integer counting lives in `ℤ` (characteristic 0, *neither* `2` nor `3` is `0`), reading residues via quotient maps `ℤ → ZMod m`, abandoning native Fermat low-degree — exactly how it sidesteps this obstruction. Honest scope: this is the universal algebraic root of the composite polynomial barrier, pinning *why* every native attack fails. It does **not** close the barrier — a *non-native* / non-Fermat representation (not committed to `p = 0`) is not excluded, which is the genuinely open frontier — but it sharpens it: a polynomial-method separation must abandon native low-degree `MOD` representation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalCharObstruction` |
| **Probing the composite lift — `MOD_5 ∉ ACC⁰[6]` does NOT reduce to the single-prime no-go (PROVED)** | The honest test of entry 288's open socket: can the composite case be *reduced* to the already-proved single-prime no-go `Layer4.mod_q_indicators_false` (`MOD_q ∉ AC⁰[p]`, `q ∤ p`)? The answer is **no**, proved. Model: **`NativelyLinearizes F p := CharP F p`** — a single field `F` linearises a `MOD_p` gate by the Fermat indicator `1 - x^(p-1)` (degree `p-1`) iff `F` has characteristic `p` (native); over any other characteristic `MOD_p` is high-degree (the single-prime no-go itself). **`SingleFieldReductionCovers F S := ∀ p ∈ S, NativelyLinearizes F p`**. **`single_prime_coverable`** (PROVED) — each singleton `{p}` is single-field-reducible (witness `ZMod p`, `ZMod.charP p`), so the proved single-prime no-go applies on every sub-class `AC⁰[p]`, giving `MOD_5 ∉ AC⁰[2]` and `MOD_5 ∉ AC⁰[3]` as genuine sub-class facts. **`acc6_reduction_needs_char_2_and_3`** (PROVED) — a single-field reduction covering `acc6 = {2,3}` forces `CharP F 2 ∧ CharP F 3` (the `MOD_2` gates need char 2, the `MOD_3` gates need char 3). **`no_single_field_covers_acc6`** (PROVED) — no field covers `{2,3}`: that needs `CharP F 2 ∧ CharP F 3`, refuted by `no_common_char` (entries 243, 280); so the joint class `AC⁰[{2,3}]` is *not* single-field-reducible. **`composite_lift_not_single_field_reducible`** (PROVED) — therefore the single-field method that proves the sub-class bounds provably **cannot combine** them into `MOD_5 ∉ ACC⁰[6]`; the obstruction is exactly the incompatible characteristics. **`cross_characteristic_witnesses_irreducibility`** (PROVED) — ties the obstruction to the typed invariant (entry 288): `CrossCharacteristic 5 acc6` holds precisely because the joint class is not single-field-reducible. Honest scope: this **answers the probe** — the composite `ACC⁰[6]` lower bound is *not* a reduction to the single-prime no-go; the single-prime tool covers each characteristic individually but provably cannot combine across incompatible characteristics (`no_common_char`). So the composite barrier is genuine and needs new (non-single-field) machinery — confirming entry 288's socket is the real open problem, not a gap a reduction could close. This is the same compositional cross-modulus blow-up flagged open in `JointModularBarrier`, now pinned to the typed invariant. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeReductionProbe` |
| **A characteristic-coupled (typed) boundary invariant — surviving the equality counterexample (PROVED classification + conditional)** | Entry 287 killed raw boundary size (equality breaks it); the fix is an invariant keyed to *characteristic type*, not size. **`CrossCharacteristic m S := ∃ p, p.Prime ∧ p ∣ m ∧ p ∉ S`** — the modulus `m` forces a prime characteristic outside the available set `S` (`ACC⁰[6]` ⇒ `S = acc6 = {2,3}`). Keyed to the prime factorisation, so it applies only to genuine `MOD` targets. **`mod5_cross_acc6`** / **`mod30_cross_acc6`** (PROVED) — `MOD_5`, `MOD_30` are cross-characteristic for `ACC⁰[6]` (prime `5 ∣ 5`, `5 ∣ 30`, `5 ∉ {2,3}`): the genuine obstruction, smallest unavailable-prime targets. **`mod6_not_cross_acc6`** (PROVED) — `MOD₆` is *not* cross for `ACC⁰[6]`: every prime factor of `6` is in `{2,3}` (`interval_cases` on `2 ≤ p ≤ 6` with `norm_num`/`decide`), so the invariant correctly leaves `MOD₆ ∈ ACC⁰[6]` unobstructed — no over-firing. **`mod6_cross_single_field`** (PROVED) — `MOD₆` is cross-characteristic for *every single* field `{p}` (`6 = 2·3` has two distinct prime factors, so `2 ∉ {p}` or `3 ∉ {p}`): the no-common-characteristic carry crossing of entries 280–283, now in the typed invariant — the single-field polynomial method cannot do `MOD₆`. **`equality_not_count_predicate`** (PROVED) — the discriminator that **beats entry 287**: there is no `g` with `[a = b] = g(wt a + wt b)` (the inputs `(10,01)` and `(10,10)` share total weight `2` but differ in equality), so equality is not a count/MOD predicate, carries no modulus, and the characteristic invariant *never flags it* — exactly the asymmetry raw boundary size could not see (equality has huge boundary yet is not characteristic-coupled). **`TypedCarryRefinementCrossing Computes S := ∀ m, CrossCharacteristic m S → ¬ Computes S m`** (named socket — the characteristic-coupled analog of entry-283 `CarryRefinementCrossing`, immune to the equality counterexample). **`typed_crossing_excludes_mod5`** (PROVED conditional, `[propext]`) — the typed crossing applied to the smallest unavailable prime gives `¬ Computes acc6 5` = `MOD_5 ∉ ACC⁰[6]`. **`typed_separation_chain`** (PROVED conditional) — typed crossing + the composite-component socket (`MOD_5 ∉ ACC⁰[6] → ACC0CompositeComponent`) ⟹ the composite-`ACC⁰` ingredient toward Williams (the typed analog of entry-278 `prime_route_to_ACC0Component`). Honest scope: a genuinely characteristic-coupled invariant that **provably distinguishes** equality (not flagged) and `MOD₆ ∈ ACC⁰[6]` (not flagged) from `MOD_5`/`MOD_30 ∉ ACC⁰[6]` (flagged) — the discrimination entry 287 proved raw boundary could not make. The classification is fully proved; the deep "cross-characteristic ⟹ not `ACC⁰[6]`-computable" implication is the open **Smolensky-composite** barrier (separation-strength; its single-prime instance `MOD_q ∉ AC⁰[p]` is already proved in this arc as `Layer4.mod_q_indicators_false`, but the lift to the composite class is open), kept as the honest socket and wired to Williams. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TypedBoundary` |
| **Attacking the boundary-boundedness socket directly — it is FALSE (PROVED no-go)** | The honest way to attack a separation-strength socket: push until it yields or breaks. The entry-286 socket `BoundedCompositionKeepsBoundaryBounded` — *does `ACC⁰[6]`-bounded composition keep the product-observer boundary polynomially bounded?* — **breaks**. **`product_observer_card_ge_of_rows_injective`** (PROVED) — for *any* two-party `f : A → A → Bool` whose communication-matrix rows `a ↦ (b ↦ f a b)` are pairwise distinct, every product observer (encoder `msg`, decoder `decode`, correct) has boundary `≥ #A` (correctness forces `msg` injective; the fooling-set argument for arbitrary `f`). **`eqValue_rows_injective`** (PROVED) — the equality function `eq(a,b) := [a = b]` has the **identity** communication matrix: row `a` is the indicator of `{a}`, so `#A` distinct rows. **`equality_product_observer_card_ge`** (PROVED) — equality needs boundary `≥ #A`. **`equality_boundary_two_pow`** (PROVED) — on `k`-bit halves (`A = Fin k → Bool`, `#A = 2^k` via `Fintype.card_fun`/`card_bool`/`card_fin`) equality needs boundary `≥ 2^k`: **exponential**. Equality is a depth-2 `AC⁰` circuit (`AND` of `XNOR`s), so `∈ AC⁰ ⊆ ACC⁰[6]` — a poly-size function with exponential communication boundary (set-disjointness, depth-2 `OR`-of-`AND`s, gives the same). **`equality_refutes_boundary_bounded`** (PROVED no-go, `[propext, Classical.choice, Quot.sound]`) — if equality is realizable (it is), any boundary budget bounding *all* realizable functions is `≥ #A`; **`boundary_socket_needs_exponential_budget`** — hence `≥ 2^k` on `k`-bit halves. So `BoundedCompositionKeepsBoundaryBounded` cannot hold with a polynomial budget: it is **false**, and entry-286's conditional separation, though a true implication, has an unsatisfiable hypothesis — the boundary route is dead. The decisive structural finding: communication boundary **anti-tracks** `ACC⁰[6]`-hardness — `MOD_M` is communication-*easy* (boundary `M`, `O(log M)` bits), equality/disjointness are communication-*hard* (`2^k`), yet **both** are in `AC⁰ ⊆ ACC⁰[6]`. Boundary size cannot be the separating resource; what separates is the *characteristic / CRT structure* (small for the native moduli `{2,3}`, large for a non-native prime — the field-incompatibility of entries 280–283) that raw communication cannot see. Attacking the socket head-on re-localizes the obstruction back onto characteristic mixing. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundaryBoundednessRefutation` |
| **Target changed to growth — CRT product boundary, frontier reduced to one socket (PROVED + conditional)** | Two field-free routes (284 streaming, 285 communication) converging on the *constant* `6` for fixed `MOD₆` was the signal to **change the target**: a fixed modulus has constant complexity and cannot witness a separation. The growth lives in the **CRT product** `M = ∏ mᵢ` (a family of moduli whose product grows). **`crt_product_observer_boundary_ge`** (PROVED) — for positive moduli `m₁,…,m_k`, any two-party product observer computing `MOD_(∏ mᵢ)` needs boundary `≥ ∏ mᵢ` (the entry-285 bound at `m = ∏ mᵢ`, with `NeZero (∏ mᵢ)` from `Finset.prod_pos`). **`crt_product_carry_card_ge`** (PROVED) — the streaming-model companion: a carry observer computing `MOD_(∏ mᵢ)` needs `≥ ∏ mᵢ` states (entry-284 bound at `m = ∏ mᵢ`). **`product_ge_two_pow`** (PROVED) — `∏ mᵢ ≥ 2^k` when each `mᵢ ≥ 2` (`Finset.prod_le_prod'` + `Finset.prod_const`). **`crt_product_observer_boundary_ge_two_pow`** (PROVED) — hence computing `MOD_(∏ mᵢ)` with all `mᵢ ≥ 2` needs `≥ 2^k` boundary states: **exponential in the number of CRT factors**, the unbounded growth fixed `MOD₆` lacks. By CRT the bound is tight — tracking the `k` factors *jointly* costs the *product* `∏ mᵢ`, not the sum `∑ mᵢ` (the carry-refinement crossing). **`growing_modulus_exceeds_budget`** (PROVED) — no fixed boundary budget `< ∏ mᵢ` suffices: the requirement climbs past any bound (in particular any polynomial, for a fast-growing product) — the precise sense in which the target now has growth. **`BoundedCompositionKeepsBoundaryBounded RealizableBy boundaryBudget`** (named socket — the **open**, separation-strength frontier): does `ACC⁰[6]`-bounded composition of resource `s` realize `MOD_M` only via product observers of boundary `≤ boundaryBudget s`? **`composite_ACC0_separation_from_socket`** (PROVED conditional, `[propext, Classical.choice, Quot.sound]`) — *given* that socket and a CRT product exceeding the budget at size `s` (`boundaryBudget s < ∏ mᵢ` — e.g. `boundaryBudget` polynomial, `∏ mᵢ` super-polynomial), `MOD_(∏ mᵢ)` is **not** realizable by a size-`s` `ACC⁰[6]` composition (the proved `≥ ∏ mᵢ` lower bound contradicts boundary `≤ boundaryBudget s < ∏ mᵢ`, via `omega`). Honest scope: the growing lower bound (`≥ ∏ mᵢ ≥ 2^k`) is genuine, field-free, machine-proved, and really *grows* — unlike fixed `MOD₆`. The proved growth discharges the composite separation **modulo exactly one named socket**: that `ACC⁰[6]`-bounded composition keeps the product-observer boundary polynomially bounded. Proving that socket (for a polynomial budget) *is* the composite `ACC⁰` separation — separation-strength, the boundary-invariant restatement of `CarryRefinementCrossing`, **not** proved here. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CarryBoundaryGrowth` |
| **Communication complexity — the boundary invariant, field-free route 2 (PROVED)** | The second characteristic-independent invariant for the `CarryRefinementCrossing` wall, in a different model (two-party communication) with a different tool (the communication-matrix / fooling-set technique) from entry 284's streaming Myhill–Nerode. **The model (field-free):** split the input across a cut — Alice holds residue `α`, Bob residue `β`; `MOD_m` decides `α + β ≡ 0 (mod m)`. The communication matrix is **`commValue m α β := decide (α + β = 0)`**; a *product observer* is a boundary alphabet `B`, encoder `msg : ZMod m → B` (Alice's cross-cut message), decoder `decode : B → ZMod m → Bool` (Bob's answer), correct when `decode (msg α) β = commValue m α β` — only residues and the boundary alphabet, no fields/polynomials. **`commValue_row_injective`** (PROVED) — the `m` rows of the `MOD_m` communication matrix are pairwise distinct: row `α` is `β ↦ [α + β = 0]`, firing only at `β = -α`, so distinct residues give distinct rows (the fooling-set / rank-`m` core; proof evaluates the rows at `β = -α` and uses `linear_combination` over `ZMod m`). **`mod_product_observer_boundary_ge`** (PROVED, `[propext, Classical.choice, Quot.sound]`) — a correct two-party product observer for `MOD_m` has `≥ m` boundary states: correctness forces `msg` to separate the `m` distinct rows, so `msg` is injective and `m = #(ZMod m) ≤ #B` (`Fintype.card_le_of_injective` + `ZMod.card`). **`mod6_product_observer_requires_large_boundary`** (PROVED) — `≥ 6` boundary states for `MOD₆`. **`mod6_boundary_six_achievable`** (PROVED) — `6` is **tight**: the identity protocol (Alice sends her residue mod 6, `B = ZMod 6`) computes `MOD₆` with exactly `6` boundary states. Honest scope — **same shape as entry 284:** a genuine field-free, two-party communication lower bound (`≥ m` boundary for `MOD_m`), proved by communication-matrix distinct-rows. But `6 = 2·3` is the **constant** CRT product, achievable — `MOD₆` is communication-*cheap* (`Θ(1)`), and tracking the CRT factors separately (`ZMod 2 × ZMod 3` boundary, `2·3 = 6` messages) matches the bound exactly, no blow-up. So the boundary invariant does **not** separate fixed-`MOD₆` composite; the separation needs a target whose communication boundary *grows* under bounded composition — the open `CarryRefinementCrossing` step. Both field-free routes (284 streaming, 285 communication) converge on the same verdict: the obstruction is growth under composition, not the size of any single boundary. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CommunicationComplexity` |
| **Carry-state complexity — the first field-free carry invariant (the frontier engaged, PROVED)** | The genuine attempt at the §5y open step: a **characteristic-independent** invariant for the `CarryRefinementCrossing` wall. **The model (field-free):** a *carry observer* is a finite state set `Q`, initial state `q₀`, increment map `δ : Q → Q` (read one more `1`-bit), and accept/output map — after Hamming weight `w` the carry state is `δ^[w] q₀`; no fields, no polynomials, only states and the increment map (exactly what entry 283 demanded). **`carry_observer_mod_card_ge`** (PROVED, `[propext, Classical.choice, Quot.sound]`) — a carry observer computing `MOD_m` (`acc (δ^[w] q₀) = [w ≡ 0 mod m]`) has `≥ m` states: the `m` residues are pairwise distinguishable — if `a < b < m` shared a state, appending `m − a` more `1`-bits would accept `a` (total weight `m`, `≡ 0`) but reject `b` (total `≡ b − a ≢ 0`); a field-free Myhill–Nerode argument on `δ` (via `Function.iterate_add_apply` + `Fintype.card_le_of_injective`). **`carry_observer_count_card_ge`** (PROVED) — a carry observer outputting the exact weight on `n` bits has `≥ n + 1` states: the carry *grows* with the count range (distinct weights → distinct outputs → distinct states). **`mod6_carry_state_ge_six`** (PROVED) — `≥ 6` carry states for `MOD₆`. **`mod_observer_too_small_fails`** (PROVED) — the trivial bounded case: `< m` states ⇒ cannot compute `MOD_m`. Honest scope — **where it bites, where it stops:** this is a genuine, field-free, machine-proved carry-state lower bound (`≥ m` for `MOD_m`, `≥ n+1` for the count) — the characteristic-independent kind the wall calls for. But for a *fixed* modulus the bound is the **constant** `6` (`= 2·3`, the CRT product) and is achievable — `MOD₆ ∈ ACC⁰[6]` — so carry-state complexity separates the *count* (growing) and growing moduli, but **not** fixed-`MOD₆` composite by itself. A composite separation requires a target whose carry-state complexity *grows* under bounded-observer composition; locating that target and proving the growth is the still-open `CarryRefinementCrossing` step. Real field-free progress on the named frontier, **not** a separation, not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CarryStateComplexity` |
| **The ACC anatomy frozen — `CarryRefinementCrossing` as the single composite target (roadmap steps 1+3, PROVED bundle)** | Freezes the result and names the one remaining theorem. **Frozen result:** the prime-`ACC⁰[p]` Razborov–Smolensky route is reconstructed and assembled from machine-proved parts (entries 264–279) modulo only the standard binomial tail and the committed `Circ` recursion; the composite obstruction is isolated as `CarryRefinementCrossing`, and the three natural field-based attacks on it are all proved to fail (entries 280–282). **`field_based_modes_all_fail`** (PROVED) — bundles the three composite no-gos: (1) *separate* — `∀ F field, ¬(CharP F 2 ∧ CharP F 3)` (no field native for both factors, entry 280); (2) *stage* — `∀ t d D₀ R observerDeg, observerDeg ≤ t^d·D₀ → R ≤ observerDeg → t^d·D₀ < R → False` (bounded degree cannot meet a strictly larger requirement, entry 281); (3) *carry* — `ZMod 6` has zero divisors and `(2 : ZMod 6)^{6-1} ≠ 1` (the carry ring is not a field and Fermat fails, entry 282). So *every* characteristic-committed (field-based) approach to `MOD₆` is refuted. **`CarryRefinementCrossing Mod6Computable FieldFreeUnboundedCarry`** (named socket — the real open `ACC⁰` theorem): *any bounded, field-compatible observer computing `MOD₆` is forced into field-free / unbounded-carry territory*. Proving it is the composite `ACC⁰` lower bound; by `field_based_modes_all_fail` its hypothesis cannot be met by any of the three field-based modes, so a proof needs a **characteristic-independent invariant** — a *new idea* (candidate directions: a ring/module invariant over `ZMod 6`, a communication/tensor-rank measure, a proof-complexity measure, an observer-holonomy/category invariant, or a direct carry-complexity lower bound). Honest scope: this freezes the anatomy and names the one open composite theorem, and proves that all three natural field-based attacks fail. It **does not** prove `CarryRefinementCrossing` (the composite lower bound) — that is genuinely open research, stated as the target, not faked. The step-5 first test (any bounded observer computing `MOD₆` must collapse to a non-native single field, excluded by 280/281, or use unbounded carry) is the open `MOD₆` instance. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CarryRefinementCrossing` |
| **The composite wall — product-field observers with explicit carry state fail (roadmap step 5 cont., the namesake NO-GO)** | The third honest composite no-go, whose failure mode is literally `CarryRefinementCrossing`'s namesake. The natural next idea after entries 280/281: **track both fields at once via an explicit carry state** — work over the product `ZMod 2 × ZMod 3 ≃ ZMod 6`, carrying the `MOD₂` and `MOD₃` residues together. This **proves why that fails**: the carry state is *not a field*. **`carry_state_has_zero_divisors`** (PROVED) — `ZMod 6` has zero divisors (`2 · 3 = 0` with `2 ≠ 0`, `3 ≠ 0`): the carry ring is not an integral domain. **`prodField_carry_has_zero_divisors`** (PROVED) — the same in product form (`(1,0)·(0,1) = 0` in `ZMod 2 × ZMod 3`, the CRT idempotents). **`carry_state_not_domain`** (PROVED) — hence `ZMod 6` is **not** an integral domain (not a field). **`fermat_fails_on_carry`** (PROVED) — `(2 : ZMod 6) ≠ 0` yet `(2 : ZMod 6)^{6-1} = 2 ≠ 1`: Fermat's little theorem `y^{p-1} = 1` (for `y ≠ 0`) — the *engine* of the degree-halving / Fermat indicator (entry 266) — is **false** over the carry ring, since `6` is not prime. **`product_field_carry_state_no_traction`** (PROVED) — packages both obstructions: the carry state is not a field *and* Fermat fails there, so the entire Smolensky polynomial method (entries 264–279, developed over fields `[Field F]` / `[Fact p.Prime]`, resting on the Fermat indicator) cannot be instantiated at the carry state. Honest scope: this proves the carry-state idea fails for the most literal reason — tracking both characteristics forces the product ring `ZMod 6`, which is not a field and where Fermat (the engine of the whole method) is false; the carry does *not* refine into a single field, the precise content of `CarryRefinementCrossing` (entry 238). It **does not** prove `MOD₆ ∉ ACC⁰[?]`; a genuine composite lower bound needs a method that works over such a ring or avoids fields — a *new idea*. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Mod6CarryState` |
| **The composite wall — bounded-depth staged observers for MOD₆ fail too (roadmap step 5 cont., proved NO-GO)** | The second honest composite no-go. The natural fix for entry-280's field-incompatibility is to *stage* the computation (compute `MOD₂` over `F₂`, `MOD₃` over `F₃`, combine — each stage bounded-depth); this **proves where staging fails**. **`staged_degree_bound`** (PROVED) — a depth-`d` staged observer's degree is `(·t)^[d] D₀ = t^d · D₀` (the per-stage growth `D ↦ t·D`, entry-273 `pow_depth_degree`): *bounded depth gives bounded degree*. **`mod6_staged_output_field_not_native`** (PROVED) — however many bounded-depth stages (over various intermediate fields) are composed, the *final output* lives over a single field `F` of one characteristic, which (entry 280) cannot be native for both `MOD₂` and `MOD₃`; staging does not manufacture a field native for both CRT factors. **`bounded_depth_staged_no_go`** (PROVED) — if the observer degree is `≤ t^d · D₀` (bounded depth), computing `MOD₆` forces degree `≥ R` (its non-native CRT factor requires degree `R` by the prime Smolensky bound, entries 275–279), while `t^d · D₀ < R` (the requirement grows with the input size, exceeding any fixed depth), then `False` (`omega`). Honest scope: this proves the *staged* approach fails for a definite reason — bounded depth caps the degree over the single output field, but `MOD₆`'s non-native CRT factor (forced by the field-incompatibility, entry 280) requires *unbounded* degree there, so staging cannot beat the carry-crossing wall (entry 238). The growing non-native degree requirement is the prime Smolensky bound (entries 275–279, resting on the standard binomial tail). It **does not** prove `MOD₆ ∉ ACC⁰[?]`; composite `ACC⁰` still needs a *characteristic-independent* method. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Mod6StagedObservers` |
| **The composite wall, smallest case — MOD₆ separated MOD₂/MOD₃ layers cross fields (roadmap step 5, proved NO-GO)** | The honest composite attack — **not** a claim of composite `ACC⁰`: take the smallest composite modulus `6 = 2·3` and the natural "separated layers" idea (handle the `MOD₂` and `MOD₃` parts independently), and **prove precisely where it fails**, pinning the `CarryRefinementCrossing` wall (entry 238) at `n = 6`. **`mod6_fires_iff_mod2_and_mod3`** (PROVED) — the CRT factorization: `MOD₆` fires (Hamming weight `≡ 0 mod 6`) iff *both* `MOD₂` and `MOD₃` fire (`6 ∣ #trues ↔ 2 ∣ #trues ∧ 3 ∣ #trues`, coprimality of 2,3 via entry-245 `modm_iff_modp_and_modq` + entry-270 `modpGate_fires_iff`); so a `MOD₆` gate genuinely separates into a `MOD₂` layer and a `MOD₃` layer. **`no_common_native_field`** (PROVED) — no field `F` has `CharP F 2 ∧ CharP F 3` (entry-243 `no_common_char` at `2 ≠ 3`): the field in which `MOD₂` is native (char 2) and the field in which `MOD₃` is native (char 3) are necessarily different. **`mod6_layers_cross_fields`** (PROVED) — therefore the separated `MOD₂`/`MOD₃` layers cannot both be native over a *single* field (each is native only in its own characteristic — `MOD₂` degree 1 over `F₂`, `MOD₃` degree 2 over `F₃`, entry 270); the prime Smolensky route (which commits to one fixed characteristic) does *not* lift to `MOD₆`. Honest scope: this proves a *negative structural fact* — the natural separated-layers reduction of `MOD₆` fails because its CRT factors require two different characteristics, which no single field provides — the precise shape of the `CarryRefinementCrossing` wall (entry 238) at `n = 6`. It **does not** prove `MOD₆ ∉ ACC⁰[?]` and **does not** circumvent the composite barrier; a genuine composite lower bound needs a *new idea* (a method not committed to one characteristic). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Mod6SeparatedLayers` |
| **Prime mechanical closure — `ApproximatorDegreeBound` discharged + the clean prime theorem (roadmap step 1) (PROVED)** | Discharges the genuinely-mechanical residual of the prime route (entry 278) and packages the clean prime theorem, **leaving exactly one frontier: composite carry-crossing (entry 238)**. **`approximator_times_complement_totalDegree`** (PROVED) — `P.totalDegree ≤ D`, `Q.totalDegree ≤ d` ⇒ `(P * Q).totalDegree ≤ D + d` (`MvPolynomial.totalDegree_mul`): **discharges `ApproximatorDegreeBound`** — the approximator (degree `≤ D`) times the complement monomial (degree `≤ |Sᶜ| ≤ n/2`, entry 277) has degree `≤ D + n/2`, the product-degree bookkeeping the degree-halving needed. **`prime_smolensky_route_closed`** (PROVED, depends on *no axioms*) — `CrossFieldCountHard` (established by entry-278's `no_small_approximator`) + the entry-261 bridge ⇒ `ACC0CompositeComponent`: the clean prime route to the ACC component, closed and wired. Honest scope: the genuinely-mechanical `ApproximatorDegreeBound` is now proved (it is just *degree of a product ≤ sum of degrees*); the prime route is packaged. The **binomial tail** (`lowDegreeDim n (n/2 + D) < 2ⁿ − E` for `D = O(√n)`) is *not* bookkeeping — it is the standard Chernoff/normal-tail estimate on `∑_{i ≤ n/2+D} C(n,i)`, kept as an honest numeric hypothesis (the textbook Smolensky parameter regime), with the weak form `lowDegreeDim n D < 2ⁿ` (`D < n`) already proved (entry-264 `lowDegreeDim_lt_two_pow`). The `Circ` recursion is the committed circuit arc's. After this the prime-`ACC⁰[p]` lower bound is assembled with every *mechanical* part proved; the single genuinely-open frontier is composite modulus — the `CarryRefinementCrossing` wall (entry 238), which needs a new idea and must not be bookkept through. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimeMechanicalClosure` |
| **The prime Smolensky lower bound, assembled + wired to the ACC component (roadmap steps 3–4) (PROVED)** | Assembles the prime-case Razborov–Smolensky lower bound from the proved pieces — the pigeonhole (entry 275), the degree-halving substitution (entry 276), and the full-product wiring (entry 277) — and wires the resulting hardness through the existing bridge to `ACC0CompositeComponent` (entry 261). **Prime case only; composite modulus stays the open `CarryRefinementCrossing` wall (entry 238), deliberately untouched.** **`smolensky_prime_goodset_bound`** (PROVED) — under `SmolenskyDegreeHalving (D') G` (every good-set point function is degree `≤ D'`, supplied by entries 276/277), `|G| ≤ lowDegreeDim n D'`: the good set is *bounded by the low-degree dimension* (contrapositive of entry-275's `smolensky_lower_bound_via_pigeonhole`). **`no_small_approximator`** (PROVED) — degree-halving + good set large (`2ⁿ − E ≤ |G|`, the small-bad-set output of the approximation machinery) + `lowDegreeDim n D' < 2ⁿ − E` (the binomial tail) ⇒ `False` (`omega` from `2ⁿ − E ≤ |G| ≤ lowDegreeDim n D' < 2ⁿ − E`): **no degree-`D'`, `≤ E`-error approximator of the symmetric (`MOD`/parity) target exists** — the prime Smolensky lower bound. **`prime_route_to_ACC0Component`** (PROVED, depends on *no axioms*) — `CrossFieldCountHard` + the entry-261 bridge `crossFieldHard_to_ACC0Component` ⇒ `ACC0CompositeComponent`, the component upstream of Williams `NEXP ⊄ ACC⁰`. Honest scope: the prime-case Smolensky lower bound is assembled from the proved pigeonhole/halving/wiring, and its hardness is wired to `ACC0CompositeComponent`. The remaining inputs are mechanical/standard: `ApproximatorDegreeBound` (entry 277, product-degree bookkeeping), the standard binomial tail `lowDegreeDim n (D+n/2) < 2ⁿ − E`, and the prime-case `CrossFieldCountHard` identification. **Composite modulus is the open wall** (entry 238). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SmolenskyPrime` |
| **The prime-case wiring — replacing the full product by the approximator on the good set (PROVED)** | Roadmap step 1 — the clean bridge from entry-276's degree-halving substitution into the proven approximation machinery (boosting, entry 267; `Approximable`, the committed arc), discharging the *agreement-input* of entry-276's `FullProductLowDegreeOnGoodSet` socket. **`fullProduct_replace_on_goodSet`** (PROVED) — given the approximator `P` agrees with the full product on the good set (`∀ x ∈ G, P x = ∏ᵢ yencₓ i`), then on `G` a monomial collapses: `∏_{i∈S} yencₓ i = P x · ∏_{i∈Sᶜ} yencₓ i` (substitute the agreement into entry-276's `smolensky_substitution`). So the hard full product is *replaced* by the degree-`D` approximator inside the degree-halving — a high-degree monomial becomes (approximator) × (complement monomial) on `G`. **`monomial_eq_approx_times_lowComplement`** (PROVED) — for `|S| > n/2`, on `G` the monomial equals `P x ·` (complement) *and* the complement has `2·|Sᶜ| < n` factors (entry 276): approximator × sub-half-degree complement. Socket: **`ApproximatorDegreeBound`** — `P` has degree `≤ D`, so `P x · ∏_{i∈Sᶜ} yencₓ i` (degree-`D` polynomial × degree-`|Sᶜ|` monomial) has degree `≤ D + |Sᶜ| ≤ D + n/2`, putting the good-set point functions in `lowDegreeSubmodule n (D + n/2)` — discharging `SmolenskyDegreeHalving` (entry 275/276); polynomial-degree bookkeeping. Honest scope: proves the wiring — the *agreement* of the approximator with the full product on the good set (the formerly-unspecified part of the socket) is exactly the approximation machinery's output (the `AC⁰[p]` approximator agreeing with the symmetric target off a small bad set, the good set being its complement), and it replaces the full product by the approximator inside the halving. The remaining `ApproximatorDegreeBound` is product-degree bookkeeping. Prime case = textbook Smolensky; **composite modulus is untouched** (the open `ACC⁰[composite]` wall, entry-238). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FullProductGoodSet` |
| **The Smolensky degree-halving — the prime-case substitution identity (textbook Smolensky) (PROVED)** | Attacks the `SmolenskyDegreeHalving` socket (entry 275), **prime case only, scoped to textbook Smolensky — composite untouched** (that is the `CarryRefinementCrossing` wall, entry 238). Proves the *algebraic core* of degree-halving over the `{±1}` encoding (values `y : Fin n → F` with `yᵢ² = 1`, the `p`-odd Smolensky setting where `±1` are distinct). **`smolensky_substitution`** (PROVED) — the substitution identity: `∏_{i∈S} yᵢ = (∏ᵢ yᵢ) · (∏_{i∈Sᶜ} yᵢ)`. The full product is `∏_S · ∏_{Sᶜ}` (`Finset.prod_mul_prod_compl`), and `(∏_{Sᶜ} yᵢ)² = ∏_{Sᶜ} yᵢ² = 1`, so the right side collapses to `∏_S` (proved by `linear_combination`, sidestepping the dependent-motive `rw` on the `univ` product). **`compl_card`** (PROVED) — `|Sᶜ| = n − |S|`. **`compl_card_lt_half`** (PROVED) — if `n < 2·|S|` (`|S| > n/2`) then `2·|Sᶜ| < n` (the complement is below the half-degree threshold). **`monomial_halving`** (PROVED) — the combined step: for `|S| > n/2`, the monomial `∏_{i∈S} yᵢ` equals `(∏ᵢ yᵢ) · (∏_{i∈Sᶜ} yᵢ)` with `2·|Sᶜ| < n` — *a high-degree monomial equals the full product times a sub-half-degree complement*. Socket: **`FullProductLowDegreeOnGoodSet`** — on the good set, the full product `∏ᵢ yᵢ` (the symmetric `MOD`-type function) equals a degree-`D` polynomial (the `AC⁰[p]` approximator); combined with `monomial_halving`, every monomial — hence every function — on the good set has degree `≤ n/2 + D`, which is `SmolenskyDegreeHalving` (entry 275). Honest scope: proves the algebraic core of the prime-case Smolensky degree-halving (the `{±1}` substitution + complement-counting that collapse a high-degree monomial to full-product × sub-half-degree complement); the remaining input — replacing the full product by the degree-`D` approximator on the good set — is the circuit-specific socket. Prime case = textbook Smolensky; **composite modulus is untouched** (the open `ACC⁰[composite]` wall, entry-238). Does **not** prove the lower bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SmolenskyDegreeHalving` |
| **The Smolensky pigeonhole — attacking the wall (step 4) via the entry-264 rank kernel (PROVED)** | Attacks the wall `SmolenskyNonNativeLowerBound` (the user's step 4). The Razborov–Smolensky non-native lower bound decomposes as **(counting/rank contradiction)** + **(degree-halving representation lemma)**; this file **proves the counting/rank half** by reusing the entry-264 rank kernel and entry-265 multilinear dimension, isolating the degree-halving as the single remaining socket. **`ptInd g := fun x => if x = g then 1 else 0`** — the point indicator at `g`. **`ptInd_linearIndependent`** (PROVED) — the point indicators over a finset `S` are linearly independent (`Fintype.linearIndependent_iff` + evaluation at a point: only the diagonal term survives). **`smolensky_pigeonhole`** (PROVED) — if every point indicator of `S` lies in a submodule `W` and `finrank W < |S|`, then `False`: the point indicators span a `|S|`-dimensional space (`finrank_span_eq_card`), which cannot embed in the smaller `W` (entry-264 `exists_notMem_of_finrank_lt`). **`smolensky_pigeonhole_lowDegree`** (PROVED) — instantiated at `W = lowDegreeSubmodule n D'` (finrank `= lowDegreeDim n D'`, entry 265): if every good-set point indicator is degree `≤ D'` and `lowDegreeDim n D' < |S|`, then `False`. **`smolensky_lower_bound_via_pigeonhole`** (PROVED) — given the degree-halving representation (`SmolenskyDegreeHalving`: good-set point functions are degree `≤ D'`) and a good set larger than the low-degree dimension (`lowDegreeDim n D' < |S|`), `False` follows. Socket: **`SmolenskyDegreeHalving`** — a degree-`D` approximator of the non-native target makes every good-set point indicator degree `≤ D'` (`D' = n/2 + D`): the Razborov–Smolensky representation lemma, using `MOD_q`'s algebra over `F_p`. The genuine remaining core; prime case is the classical Smolensky lemma, composite is the open `ACC⁰[composite]` wall (entry-238 `CarryRefinementCrossing`). Honest scope: proves the **counting/rank half** of the Smolensky lower bound — the pigeonhole that a large good set forces a high-degree point function — via the entry-264 rank kernel and entry-265 dimension; the remaining content is the single socket `SmolenskyDegreeHalving` (the degree-halving representation lemma). Does **not** prove the lower bound (the socket is the wall). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SmolenskyPigeonhole` |
| **The quantitative iteration — solving the size/depth/error recurrence in closed form (roadmap step 1) (PROVED)** | Solves the *iteration* (the user's step 1): turns entry-273's per-layer estimates into the full closed-form bound, built on top of entry 273 (not modifying the committed circuit arc). **`error_accumulation`** (PROVED) — the error closed form: if `E ≤ ∑ᵢ Eg i` (the union bound, the committed `error_union_bound`) and each gate is a `2^{-t}` fraction (`2^t · Eg i ≤ 2ⁿ`, entry 273), then **`2^t · E ≤ m · 2ⁿ`** (`Finset.mul_sum` + `Finset.sum_le_sum` + `Finset.sum_const`): the total error is a `m·2^{-t}` fraction. **`error_accumulation_size`** (PROVED) — with `m ≤ s` gates, `2^t · E ≤ s · 2ⁿ`: the total error is a `size·2^{-t}` fraction. **`quantitative_iteration_closed_form`** (PROVED) — the combined solver: a polynomial whose degree satisfies the `d`-fold recurrence `(·t)^[d] D₀` and an error union-bounded by `m` per-gate `2^{-t}` errors has degree `≤ t^d · D₀` (`pow_depth_degree`, entry 273) and `2^t · E ≤ m · 2ⁿ` (`error_accumulation`) — the closed-form solution of the size/depth/error recurrence. Socket: **`CircuitStructuralRecursion`** — the structural induction over the committed `Circ` datatype that produces, for a size-`s`, depth-`d` circuit, a polynomial of degree satisfying the `d`-fold recurrence and an error union-bounded by `s` per-gate `2^{-t}` errors (the quantitative refinement of the committed `approximable_exists`); it lives in the committed circuit arc, and feeding it to `quantitative_iteration_closed_form` discharges `QuantitativeDepthBound` (entry 272). Honest scope: solves the recurrence in closed form — the error accumulation `2^t·E ≤ size·2ⁿ` and (via entry 273) the degree `t^d·D₀` — completing the *analytic* content of the iteration; the structural `Circ` recursion feeding it is the committed arc's piece. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0QuantitativeIteration` |
| **The quantitative depth bound — clean per-gate error extraction + per-layer step (roadmap (A)) (PROVED)** | Roadmap step (A) — making the depth bound *quantitative*, built **on top of** the committed `or_step`/`and_step` (not modifying them). The committed steps give per-layer degree growth `D ↦ t·D` and error split `E ↦ k·E + Eg`, but leave the gate-boosting error `Eg` in the awkward form `(2^k)^t · Eg ≤ 2ⁿ · (2^{k-1})^t`; this file extracts the clean fraction. **`gate_error_le`** (PROVED) — `(card (Finset (Fin k)))^t · Eg ≤ N · (2^{k-1})^t` (with `1 ≤ k`) ⇒ **`2^t · Eg ≤ N`**: cancelling the common factor `2^{(k-1)t}` (`Nat.sub_one_mul`, `Nat.le_of_mul_le_mul_left`, `pow_pos`). So each `OR`/`AND` gate's boosting error is at most a `2^{-t}` fraction of the input space — *independent of the fan-in `k`*. **`card_input`** (PROVED) — `Fintype.card (Fin n → Bool) = 2ⁿ`. **`or_layer_quant`** / **`and_layer_quant`** (PROVED) — the clean per-layer quantitative step: `or_step`/`and_step` repackaged with the clean `2^t · Eg ≤ 2ⁿ` (degree `≤ t·D`, error `≤ k·E + Eg`). **`pow_depth_degree`** (PROVED) — the closed-form degree recurrence: the per-layer growth `D ↦ t·D` applied `d` times to `D₀` is `t^d · D₀` (`Function.iterate_succ_apply'` + `pow_succ`), so a depth-`d` circuit's approximant has total degree `≤ t^d · D₀` (the degree half of `QuantitativeDepthBound`, in closed form). Honest scope: assembles the *clean quantitative per-layer step* — the gate error is now the transparent `2^{-t}` fraction (`gate_error_le`), the `OR`/`AND` layer steps carry it, and the degree closed form `t^d·D₀` is proved. The remaining piece of `QuantitativeDepthBound` is the *structural iteration* over the committed `Circ` datatype (apply the layer step at each gate, accumulating degree `t^d` and error `size·2ⁿ·2^{-t}`) — this lives in the committed circuit arc (the quantitative refinement of `approximable_exists`) and is left to it. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0QuantitativeDepthBound` |
| **The polynomial-method bridge — `small AC⁰[p] observer ⇒ low-degree approximation` (PROVED bridge)** | Roadmap step 3 — a **bridge file** connecting the abstract `F_p` kernel (entries 264–271) and the concrete `F₂` circuit arc (Codex's `CircuitApprox`/`DepthInduction`) **without re-proving either**. **`LowDegreeApprox f D E`** — the common notion: an `F₂`-polynomial of total degree `≤ D` agreeing with the target `f` on all but `≤ E` inputs (matching Codex's `errCard`). **`approximable_iff_lowDegreeApprox`** (PROVED, `Iff.rfl`) — Codex's `Approximable C D E` *is* `LowDegreeApprox (boolToZMod ∘ Circ.eval C) D E`: the two arcs' notions coincide definitionally. **`small_AC0p_observer_implies_lowDegreeApprox`** (PROVED) — every `MOD`-free `AC⁰` circuit's function has a low-degree `F₂` approximant, re-exporting the committed `approximable_exists`: the "small observer ⇒ low-degree probabilistic approximation" direction. **`lowDegree_excludes_highDegree`** (PROVED) — the contradiction core: a function cannot both *have* a degree-`≤ D`, `≤ E`-error approximant and *require* more (`LowDegreeApprox f D E → ¬ LowDegreeApprox f D E → False`). **`no_lowDegree_observer_of_requiresHighDegree`** (PROVED) — a high-degree-requiring target admits no `LowDegreeApprox` at that bound. **`polynomial_method_contradiction`** (PROVED) — the assembled final step: `QuantitativeDepthBound f D E ∧ SmolenskyNonNativeLowerBound f D E → False`. Sockets: **`QuantitativeDepthBound`** — the size/depth → `(D,E)` refinement of the bridge (`D = (log s)^{O(d)}·(p-1)`, `E ≤ s·2^{-t}`), threading the committed `or_step`/`and_step` with the boosting parameter `t` (not yet assembled in either arc); **`SmolenskyNonNativeLowerBound`** — `¬ LowDegreeApprox f D E` for the non-native target (`MOD_q` over `F₂`, `q` odd) at the relevant small `(D,E)`: the Razborov–Smolensky high-degree requirement (mechanism = the proved `algExpander_forces_high_degree`, entry 264; composite `q` = the open `ACC⁰[composite]` wall, entry-238 `CarryRefinementCrossing`). Honest scope: bridges the two arcs at the interface level, re-exports the committed "small circuit ⇒ low-degree approximant" direction, and proves the contradiction skeleton; supplying the two quantitative sockets (the latter is the wall) completes the *prime*-`MOD` lower bound, with composite `MOD` the open barrier feeding `ACC0CompositeComponent` → Williams. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PolynomialMethodApproximation` |
| **The native/non-native bridge — split made theorem-level + `MOD_p` wired to the `PatternRich` socket (PROVED)** | The assembly step (the broad search being done): makes the native-easy/non-native-hard split *theorem-level* and connects entry-270 `MOD_p` to the `PatternRich`/cross-field lower-bound socket (entries 262/264) via the **dictator family** `gᵢ(x) := xᵢ`, whose cross-field count *is* `MOD_p`. **`modp_native_easy`** (PROVED) — over `F_p`, `MOD_p` has the exact degree-`(p-1)` representation (entry 270): the native side is easy. **`dictator_crossFieldCount_eq`** (PROVED, `rfl`) — `crossFieldCount q (dictator) x = #{true inputs} mod q`. **`modp_iff_dictator_crossFieldCount_zero`** (PROVED) — `MOD_p` fires iff the dictator cross-field count mod `p` is `0` (`modpGate_fires_iff` + `Nat.dvd_iff_mod_eq_zero`): `MOD_p` *is* the cross-field-count object of entry 251. **`dictator_meets_patternRich_socket`** (PROVED) — the dictator family is `AlgExpander` (entry 260) ∧ `PatternRich (2^s)` (entry 261): it satisfies the antecedent of `PatternRichCrossFieldLowerBound` (entry 262). **`native_nonnative_split`** (PROVED) — the theorem-level split: (native) the exact `F_p` representation, and (cross-field) the identification of `MOD_p` with the dictator cross-field count. Socket: **`NonNativeMODHard`** `= PatternRichCrossFieldLowerBound` — the central wall: the cross-field count of an `AlgExpander ∧ PatternRich` family mod `q` (`q ≠ p`) is hard (already reduced by entry 264 to the single `PolynomialMethodApproximation` ingredient, for which entries 264–270 supply the full proved RS analytic kernel; for composite `q` it is the open wall, entry-238 `CarryRefinementCrossing`). Honest scope: the native/non-native split is now theorem-level — native `MOD_p` is the proved exact degree-`(p-1)` `F_p` representation, and the non-native object is the proved identification of `MOD_p` with the dictator cross-field count (whose mod-`q` hardness is the named central socket and whose antecedent the dictator family provably satisfies). The general non-native lower bound is **not** proved (it is the wall). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NativeNonNativeBridge` |
| **The `MOD_p` gate — exact native degree-`(p-1)` representation + the native/non-native dichotomy (PROVED)** | The third `AC⁰[p]` gate — the counting gate `MOD_p`, the `SYM` of the Beigel–Tarui `SYM∘AND` form — completing the per-gate analytic kernel beyond `AND`/`OR` (entry 266). **`modpGate p x := decide (∑ᵢ (if xᵢ then 1 else 0 : ZMod p) = 0)`** — fires iff the number of `true` inputs is `≡ 0 mod p`. **`linSum_eq_count`** (PROVED) — `∑ᵢ (if xᵢ then 1 else 0) = ↑#{i : xᵢ}` in `F_p` (`Finset.sum_boole`): the clause sum is the cast Hamming weight. **`modp_native_repr`** (PROVED) — `1 − (∑ᵢ xᵢ)^{p-1} = if modpGate p x then 1 else 0` over `F_p`: the **exact** degree-`(p-1)` *native* polynomial computing `MOD_p`, **independent of the fan-in** (via the `fermat_indicator`, entry 266). So `MOD_p` is *easy over its own field* — constant native degree. **`modpGate_fires_iff`** (PROVED) — `modpGate p x ↔ p ∣ #{i : xᵢ}` (`ZMod.natCast_eq_zero_iff`): `MOD_p` is the divisibility of the Hamming weight, i.e. the `SYM` gate / cross-field-count object (entry 251). Socket: **`ModpNonNativeHardOverOtherField`** — over a field of characteristic `ℓ ≠ p`, `MOD_p` requires high degree (the Razborov–Smolensky obstruction; its dimension/rank mechanism is the proved `algExpander_forces_high_degree`, entry 264; for *composite* modulus it is the open `ACC⁰[composite]` wall, entry-238 `CarryRefinementCrossing`). Honest scope: proves the exact *native* (`F_p`) representation of `MOD_p` — degree `p-1`, fan-in-free — and identifies `MOD_p` with weight-divisibility (the `SYM`/cross-field object). The **dichotomy**: native is easy (constant degree); the *non-native* lower bound (`MOD_p` over `F_ℓ`) is the Razborov–Smolensky wall, left as the named socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModpGate` |
| **Independence — joint failure of `k` clauses `≤ 2^{-k}`, discharging `IndependentIntersectionBound` (PROVED)** | The last probabilistic ingredient of the single-gate low-degree approximation (entry 267), proved in the honest model where the `k` random subsets are *independent*: the **product space `∀ j, Y j`** of the `k` draws, where each clause's bad event is a *cylinder* (depends only on the `j`-th coordinate, `bad j = {y : y j ∈ B j}`). **`jointBad_card`** (PROVED) — the independence factorization: `|{y : ∀ j, y j ∈ B j}| = ∏ⱼ |B j|` (`Fintype.card_piFinset`), i.e. the joint bad event `⋂ⱼ bad j` is the product box and its cardinality factorizes. **`independent_intersection_bound`** (PROVED) — if each clause is bad on at most half its draws (`2·|B j| ≤ |Y j|`, the per-clause `1/2` of entry 268), then `2^k · |jointBad| ≤ ∏ⱼ |Y j|` (pull `2^k = ∏ⱼ 2` inside the product via `Finset.prod_mul_distrib` and compare factorwise via `Finset.prod_le_prod`). **`joint_error_le`** (PROVED) — equivalently `2^k · |jointBad| ≤ |∀ j, Y j|` (`Fintype.card_pi`): the `k` independent clauses fail *simultaneously* on at most a `2^{-k}` fraction of the randomness space. **`IndependentIntersectionBound` (entry 267) is discharged in the product-of-independent-draws model.** Honest scope: proves the independence factorization and the `2^{-k}` joint-failure bound — the only ingredient entry 267 still socketed. Composed with the proved per-clause `1/2` (entry 268, supplying `2·|B j| ≤ |Y j|`) and the proved boosting (`bad(boost) ⊆ ⋂ⱼ bad j`), it gives a single-gate degree-`k(p-1)` approximation with error `≤ 2^{-k}`. What remains is the *structural* lifting single-gate → circuit → observer (the `PolynomialMethodApproximation` glue). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Independence` |
| **Per-clause agreement over `F₂` — discharging `SingleSubsetAgreement` by the parity-toggle involution (PROVED)** | Discharges entry-267's per-clause `≥ 1/2` probabilistic socket over `F₂` — and there the bound is *exactly* `1/2`, proved by the parity-toggle involution. **`parF2 x S := ∑_{i∈S} xᵢ ∈ F₂`** (the Fermat clause sum at `p = 2`); **`tog j S := S △ {j}`** (toggle membership of `j`). **`par_tog`** (PROVED) — for `xⱼ = true`, `parF2 x (tog j S) = parF2 x S + 1`: toggling `j ∈ supp x` *flips* the clause parity (`Finset.add_sum_erase` / `Finset.sum_insert` + a `decide`d `F₂` identity). **`tog_tog`** / **`tog_injective`** (PROVED) — `tog j` is an involution (`Finset.insert_erase` / `Finset.erase_insert`), hence injective. **`singleSubsetAgreement_two`** (PROVED) — for any `x` with a `true` coordinate, `SingleSubsetAgreement 2 n x` holds: the toggle bijects `{S : par = 0} ↔ {S : par ≠ 0}` (the image-equals-filter set equality + `Finset.card_image_of_injective`), so the two parity fibers are *equinumerous* and sum to `2ⁿ` (`Finset.filter_card_add_filter_neg_card_eq_card`, `Fintype.card_finset`), giving `2ⁿ = 2·#{S : par ≠ 0}`, hence `2ⁿ ≤ 2·#{S : par ≠ 0}`. **The `SingleSubsetAgreement` socket of entry 267 is discharged over `F₂`** — the clause fires correctly on exactly half the subsets. Honest scope: proves the per-clause `≥ 1/2` (in fact `= 1/2`) agreement over `F₂`, one of the two probabilistic sockets of boosting; composed with the proved boosting (`boost_correct_off_iInter`) and the remaining `IndependentIntersectionBound` socket it would give the single-gate low-degree approximation with error `2^{-k}`. Does **not** prove the general-`F_p` bound or the independence socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SingleSubsetF2` |
| **Boosting — driving `k` clause indicators to error `2^{-k}` (the error-reduction core of Razborov–Smolensky) (PROVED)** | The second rung inside `PolynomialMethodApproximation` (entry 266 was the first). A single Fermat clause indicator approximates OR with *one-sided* error (always correct on the all-zero input; correct on a nonzero input for ≥ half the random subsets). **Boosting** combines `k` independent such indicators into one polynomial whose error set is the *intersection* of the individual ones — so with per-clause `≥ 1/2` agreement and independence the error drops to `2^{-k}`. **`boostPoly e x := 1 − ∏ⱼ (1 − eⱼ x)`** — the OR of the `k` approximators (degree `∑ⱼ deg eⱼ = k·(p-1)` for clause indicators). **`boostPoly_eq_one`** (PROVED) — some `eⱼ x = 1` ⇒ `boostPoly e x = 1` (the `j`-th factor `1 − 1 = 0`, product `0`). **`boostPoly_eq_zero`** (PROVED) — all `eⱼ x = 0` ⇒ `boostPoly e x = 0` (all factors `1`, product `1`). **`boost_correct`** (PROVED) — `boostPoly e x = tgt x` whenever the good event holds: for Boolean `tgt`, either `tgt x = 0` (one-sidedness `hone` forces all `eⱼ x = 0`, so `boost = 0 = tgt`) or `tgt x = 1` with some `eⱼ` firing (`hsome`, so `boost = 1 = tgt`). **`boost_correct_off_iInter`** (PROVED) — **the error-reduction core**: if each `eⱼ` is a one-sided approximator of `tgt` with error set `bad j`, then `boostPoly e` agrees with `tgt` on every input outside the *intersection* of the error sets (`∃ j, x ∉ bad j`), i.e. **`bad(boost) ⊆ ⋂ⱼ bad j`** — the combined error set is the intersection of the individual ones. Probabilistic sockets: **`SingleSubsetAgreement`** — the per-clause primitive (`#{S : ∑_{i∈S} xᵢ ≠ 0} ≥ 2ⁿ/2`, the `≥ 1/2`, exactly `1/2` over `F₂` by the parity-toggle involution); **`IndependentIntersectionBound`** — the independence product (`|⋂ⱼ bad j| ≤ ∏ⱼ |bad j| / 2^{n(k-1)}`). Composed with the proved boosting they give `|bad(boost)| ≤ 2ⁿ·2^{-k}`: a degree-`k(p-1)` approximation with error `2^{-k}`, the single-gate target of `RandomizedLowDegreeApproximation` (entry 266). Honest scope: the boosting *algebra* — the OR-of-approximators polynomial and the error-set intersection `bad(boost) ⊆ ⋂ⱼ bad j` — is fully proved; the two probabilistic ingredients (per-clause `≥ 1/2`, independence) are the named sockets. Does **not** prove those bounds. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Boosting` |
| **Polynomial approximation of a single AND gate — the base case of the Razborov–Smolensky method (PROVED)** | The first rung of the `PolynomialMethodApproximation` socket — how one gate is represented/approximated by a polynomial over `F`. Formalizes the genuinely-provable algebraic substrate and isolates the probabilistic boosting as the one named open ingredient. **`fermat_indicator`** (PROVED) — over `F_p` (`p` prime), `y^(p-1) = if y = 0 then 0 else 1` (Fermat's little theorem, `ZMod.pow_card_sub_one_eq_one` + `zero_pow`): the degree-`p-1` engine the whole polynomial method is built on (turns a *sum* of literals into a `0/1` value of degree `p-1`, independent of fan-in). **`andExact`** (PROVED) — `∏ᵢ (if xᵢ then 1 else 0) = if (∀ i, xᵢ) then 1 else 0`: the AND of `n` literals is the exact degree-`n` monomial. **`orExact`** (PROVED) — `1 − ∏ᵢ (1 − [xᵢ]) = if (∃ i, xᵢ) then 1 else 0`: the exact OR (De Morgan, degree `n`). **`clauseIndicator`** (PROVED) — `1 − (∑_{i∈S} [xᵢ])^(p-1) = if (∑_{i∈S} [xᵢ]) = 0 then 1 else 0`: the degree-`p-1` Fermat clause indicator detecting whether the `S`-sum vanishes mod `p`, a polynomial of degree `p-1` *regardless of `|S|`* — the RS building block. **`andIndicator_eq_mlMon`** / **`andIndicator_mem_lowDegree`** (PROVED) — the AND indicator *is* the monomial `mlMon univ` (entry 265), hence lies in `lowDegreeSubmodule n n`: the exact (degree-`n`) representation lands in the framework's degree-`≤ n` space. **`RandomizedLowDegreeApproximation F n D k`** (named socket) — a degree-`≤ D` polynomial (∈ `lowDegreeSubmodule n D`) agreeing with `AND_n` on all but `≤ k` inputs. **`and_exact_is_zeroError_approximation`** (PROVED) — the exact representation witnesses `RandomizedLowDegreeApproximation F n n 0` (`q = mlMon univ`, `bad = ∅`): the socket is non-vacuous at the exact endpoint `(D=n, k=0)`. Honest scope: the algebraic substrate of the per-gate polynomial method — Fermat indicator, exact AND/OR monomials, the degree-`p-1` clause indicator, the AND indicator's membership in the degree-`≤ n` submodule, and a proved zero-error degree-`n` approximation — is built. Does **not** prove the low-degree (`D ≪ n`) probabilistic approximation (random restriction + boosting), which is the open single-gate base case of `PolynomialMethodApproximation` (the open Razborov–Smolensky core). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AndGateApprox` |
| **The multilinear monomial basis over `F` — discharging the `LowDegreeDimensionIdentity` socket (PROVED)** | Builds the multilinear monomial basis over an arbitrary field `F` — the `F`-analogue of the in-arc `anf_involutive` (which did this over `F₂`) — and computes the degree-`≤ D` dimension exactly, **completely discharging the secondary `LowDegreeDimensionIdentity` socket** of entry 264. **The basis** — for `S : Finset (Fin n)`, the multilinear monomial `mlMon S x := ∏_{i∈S} (xᵢ : F)` over the Boolean cube (`true ↦ 1`, `false ↦ 0`), of degree `|S|`. **`mlMon_eval`** (PROVED) — the *triangular evaluation*: at `e_T(i) := [i ∈ T]`, `χ_S(e_T) = [S ⊆ T]` (the unitriangular subset/zeta matrix). **`mlMon_linearIndependent`** (PROVED) — the `2ⁿ` multilinear monomials are linearly independent over `F`, *a genuine general-field generalisation of `anf_involutive`*: a dependence `∑_S c_S χ_S = 0` evaluated at `e_T` gives `∑_{S ⊆ T} c_S = 0` for all `T` (triangular evaluation), and Möbius/strong induction on the subset lattice (`Finset.strongInductionOn` + `Finset.add_sum_erase`) forces every `c_T = 0`. **`card_subtype_card_le`** (PROVED) — `#{S : S.card ≤ D} = lowDegreeDim n D = ∑_{i≤D} C(n,i)` (partition the subsets by cardinality; `Finset.card_powersetCard`). **`lowDegreeSubmodule n D := span {χ_S : |S| ≤ D}`** and **`lowDegreeSubmodule_finrank`** (PROVED) — the degree-`≤ D` submodule has dimension exactly `lowDegreeDim n D` (`finrank_span_eq_card` on the linearly-independent sub-family + `card_subtype_card_le`). **`lowDegreeDimensionIdentity_discharged`** (PROVED) — therefore `LowDegreeDimensionIdentity (lowDegreeSubmodule n D) n D` holds: **the secondary socket of entry 264 is discharged**, no longer assumed. **`patternRichCrossFieldLowerBound_no_dimSocket`** (PROVED) — consequently, for Boolean gate families, instantiating entry-264's reduction at `W := lowDegreeSubmodule n D` eliminates the dimension socket: `PatternRichCrossFieldLowerBound` follows from the **single** remaining socket `PolynomialMethodApproximation`. Honest scope: the multilinear basis over a general field is built and the degree-`≤ D` dimension computed exactly, fully discharging `LowDegreeDimensionIdentity`. After this, the open target `PatternRichCrossFieldLowerBound` rests on the *single* socket `PolynomialMethodApproximation` — the probabilistic polynomial method (the genuine open Razborov–Smolensky core, entry-238 `CarryRefinementCrossing`). Does **not** prove that socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MultilinearBasis` |
| **Workstream B — the composite-barrier candidates collapse onto the single open socket (PROVED)** | The honest unification of `NFRAME_TWO_ROUTES.md` §4's surviving non-native candidates (1 char-0, 2 CRT/product, 4 probabilistic/approximate, 5 staged): **none is an *independent* escape.** **`exists_nontrivial_field_two_three_neZero`** (PROVED) — `∃` a nontrivial field with `(2:F)≠0 ∧ (3:F)≠0` (witness `ℚ`). The Universal Native Characteristic Obstruction (280/300/312) *requires* `2=0 ∧ 3=0`; both fail here, so the algebraic no-go is *silent* on the non-native candidates. **`native_obstruction_hypothesis_fails`** (PROVED) — in any field with `2≠0`, the conjunction `2=0 ∧ 3=0` is false. **`finrank_cube_fun`** (PROVED) — `finrank F ((Fin n → Bool) → F) = 2ⁿ` (ambient dimension, any field; `Module.finrank_pi` + `Fintype.card_fun`). **`lowDegreeSubmodule_ne_top`** (PROVED) — for `D < n` the degree-`≤ D` submodule is a *proper* subspace over **any** field: `finrank = lowDegreeDim n D < 2ⁿ = finrank` of the whole space (`lowDegreeSubmodule_finrank` + `lowDegreeDim_lt_two_pow` + `finrank_top`), so it cannot be `⊤`. *Crucially field-independent* — both inputs hold in every characteristic. **`exists_high_degree_over_any_field`** (PROVED) — hence over any field there is a function of true degree `> D`. **`charZero_no_low_degree`** (PROVED) — the `ℚ` (characteristic-0) instance: candidate 1 gains *nothing* dimensionally; the degree-`< n` space is proper over `ℚ` exactly as over `F_p`. Honest scope: this proves the native algebraic obstruction is inapplicable to the non-native candidates *and* that the dimension barrier is the same in every characteristic (char-0 included) — so the choice of ambient field/structure is not where a separation can come from; each candidate, over its field, reduces to the same single open socket `PolynomialMethodApproximation`. Does **not** prove that socket (= the composite barrier itself). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeCandidateUnification` |
| **Workstream B — why Razborov–Smolensky does not extend to composite: the foreign-gate obstruction (PROVED)** | Decomposes the single open socket `PolynomialMethodApproximation` to expose *why* it stalls at composite modulus — the natural-proofs-flavoured wall — without faking it. The polynomial method runs over a *single* field `K`. **`foreign_mod_no_lowDegree_approx`** (PROVED) — for distinct primes `q ∤ p`, over `K = F_{p^{q-1}}` (characteristic `p`), there is **no** family of degree-`≤ Δ` polynomials `g j` that tightly agree (`4q·|complement| ≤ 2ⁿ`) with the `q` residue indicators `modIndicator K q j` of `MOD_q` within the band-margin window `16·Δ² < 2m+3`. This is the proof-technique form of the foreign gate's hardness — the polynomial method cannot low-degree-approximate a `MOD_q` gate over a field of the wrong characteristic — proved by reconstructing a primitive `q`-th root of unity in `K` (`exists_primitiveRoot_galoisField`, available since `q ∤ p`) and invoking the proved general-`q` RS bound `Layer4.qary_full_contradiction` (the polynomial core of `mod_q_indicators_false`). **`composite_six_has_foreign_prime`** (PROVED) — for any prime `p`, `¬ 2 ∣ p ∨ ¬ 3 ∣ p`: a field of characteristic `p` is native for at most one of the two prime factors of `6` (the `2 ≠ 3` root of the native obstruction, in proof-technique form; `Nat.prime_dvd_prime_iff_eq` + `omega`). **`acc6_foreign_prime_exists`** (PROVED) — hence for `AC⁰[6]` over any single field there is a foreign prime `q ∈ {2,3}`, `q ∣ 6`, `¬ q ∣ p`: a `MOD_q` gate of the composite circuit foreign to `K`, whose RS approximation is impossible by `foreign_mod_no_lowDegree_approx`. Honest scope: this proves the obstruction to the *proof technique* — the polynomial-method approximation step cannot produce a low-degree approximant for a foreign `MOD` gate (the in-arc RS bound forbids it), and `AC⁰[6]` always has such a gate over any single field — the precise formal statement of *why RS does not extend to composite*. It is **not** a lower bound against `AC⁰[6]` (it does **not** show `MOD₅ ∉ AC⁰[6]`, the genuine open problem); it shows the single available technique stalls. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ForeignGateObstruction` |
| **Entry 319 — the counting observer realizes the Williams route (PROVED bridge)** | The precision the composite anatomy was missing: the composite barrier (280–318) blocks the *native polynomial* branch, but Williams' `NEXP ⊄ ACC⁰` route lives on the **counting-observer** branch — char-0 integer counts → CRT residue readout → fast-SAT `< 2ⁿ` compression → complement-safe lazy hierarchy. This file makes that branch *explicit N-Frame data* and proves it **is** the Williams route, removing the last ambiguity (a single bridge theorem, not "Williams special case"). Four ingredients, each grounded in a proved object: **`CharacteristicZeroCountingObserver`** — the integer count `gateCount g x = ∑ⱼ [g j x] : ℕ` (no characteristic, no `p = 0`) mediates evaluation for every top (`symEval g h x = h (gateCount g x)`); holds by `rfl` (`characteristicZeroCountingObserver_holds`). **`CRTResidueReadout`** — `MOD_M`-SAT read via the residue map `c % M` for every `M` (`Satisfiable (symEval g (modIndicator M)) ↔ ∃ c ∈ image (gateCount g), c % M = 0`), a readout of an integer statistic not a native polynomial; holds by `fastSat_decides_every_modulus` (entry 291). **`FastSATCompression`** — the `< 2ⁿ` count-cell exact `SYM∘AND` form, the Williams speedup object; `= WilliamsFastSatRoute` (`fastSATCompression_iff_williams`, `Iff.rfl`). **`LazyHierarchyContradiction`** — the complement-safe lazy diagonal escapes the enumeration (`D ∉ range enum`); holds by `lazy_diag_not_mem_range` (entry 294). **`countingObserver_to_williamsRoute`** (PROVED) — the requested shape: the four ingredients ⇒ `WilliamsFastSatRoute`. **`nframe_counting_branch_eq_williams`** (PROVED) — `NFrameCountingBranch ↔ WilliamsFastSatRoute`: the counting branch *is* the Williams route (ingredients 1/2/4 unconditional, 3 = the route), not merely analogous. **`nframe_counting_branch_derives_separation`** (PROVED conditional) — the full chain: the branch, through the named classical sockets (uniform realization, easy-witness/NW, nondeterministic time hierarchy), derives `¬ (NEXP ⊆ ACC⁰)` via `nframe_fastSat_to_timeHierarchy`. Honest scope: pins that the Williams route is the counting-observer branch (char-0 integer counts carry every characteristic ⇒ never characteristic-blocked), formally equal to `WilliamsFastSatRoute` — *not* the native polynomial branch the composite barrier blocks. The deep gate is proved *modulo* the named classical sockets, each a *proven* theorem (Williams 2011) being formalized. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CountingObserverWilliams` |
| **Entry 320 — the counting-branch separation packaged, all residues explicit (PROVED)** | Consolidates entry 319's chain into one clean theorem with the entire residue surface as a single inspectable hypothesis — the Williams/N-Frame route as clean as it can be without proving new lower bounds. **`WilliamsClassicalResidues ACC0 f g speedup`** — a `Prop` structure with exactly three named fields (the whole non-N-Frame input): `uniformRealization : NFrameWilliamsRoute → speedup` (the `< 2ⁿ` count-cell fast-SAT becomes a genuine uniform speedup; entries 304–311, ground to primitive TM ops); `easyWitnessNWCollapse : speedup → NEXP ⊆ ACC0 → NTIME f ⊆ NTIME g` (IKW easy-witness + Karp–Lipton + NW-derandomisation; entries 307/313/314/315/316); `nondetTimeHierarchy : ¬ (NTIME f ⊆ NTIME g)` (diagonalization core proved complement-free, entry 294). **`nframe_counting_branch_to_williams_separation`** (PROVED) — `WilliamsClassicalResidues … → NFrameCountingBranch → ¬ (NEXP ⊆ ACC0)`: the complete Williams/N-Frame route in one theorem, every assumption explicit (the N-Frame counting branch + the single residue bundle), via entry 319's `nframe_counting_branch_derives_separation`. **`williams_separation_from_residues`** (PROVED) — the same from any `WilliamsFastSatRoute` (route ⇒ separation form, branch supplied by `nframe_counting_branch_eq_williams`). Honest scope: a *packaging* entry — consolidates the proved chain and exposes the residue surface completely (one structure, three named fields). Does **not** prove the residues; names them (each a *proven* classical theorem, Williams 2011, being formalized). NOT a new `NEXP ⊄ ACC⁰`, and emphatically NOT `P ≠ NP` | `…ACC0CountingBranchSeparation` |
| **Entry 321 — `bridgeYao` discharged in the NW reconstruction assembly (PROVED)** | Discharges the upstream of entry 316's two model-dependent bridges. Entry 316's `bridgeYao : YaoNextBitPredictor f m ε → ComputesF` is exactly a `YaoCircuitEfficiency` socket; entry 194 (`…NisanWigdersonYaoCircuit`) built the explicit Yao predictor circuit `predictor D gidx := (x gidx) ⊕ ¬D` over a gate-level `Circ` syntax and proved `yaoCircuitEfficiency_discharge` — that it is a small circuit `SmallPredictor D gidx` (size `≤ size D + 3`, computing the guess-and-correct rule). **`bridgeYao_discharged`** (PROVED) — `YaoNextBitPredictor f m ε → SmallPredictor D gidx`, the bridge at `ComputesF := SmallPredictor D gidx`. **`nwReconstruction_bridgeYao_discharged`** (PROVED) — entry 316's `nwReconstruction_assembled` with `bridgeYao` supplied: `NWReconstruction (GlobalAdvantage f m ε) (HasCircuitOfSize fbool s)` from the design and **only `bridgeRecon`**. **`hardFn_to_prgExists_bridgeYao_discharged`** (PROVED) — entry 316's `HardFunction → PRGExists` (`HardFor fbool s → ¬ GlobalAdvantage f m ε`) with `bridgeYao` discharged; only `bridgeRecon`, the irreducible hardness, and the proved design remain. Honest scope: removes one of the two model-dependent bridges — the Yao predictor-circuit realisation is now a *proved* gate-level fact (entry 194's `Circ`/`SmallPredictor`) instantiated into the assembly. Honest caveat: `SmallPredictor` is a gate-count (`+3`, `D` referenced once) + semantics bridge; it does **not** formalise `ACC⁰` *membership* (depth/gate-type) of the distinguisher `D`. After this, `HardFunction → PRGExists` rests on the single remaining bridge `bridgeRecon` (entry 322). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NWReconstructionBridgeYao` |
| **Entry 322 — `bridgeRecon` constructed: the reconstructed circuit built syntactically (PROVED)** | The last model-dependent bridge of entry 316. Unlike `bridgeYao`, `bridgeRecon` cannot be supplied unconditionally — its output `HasCircuitOfSize fbool s` would directly contradict the hardness `HardFor fbool s = ¬ HasCircuitOfSize fbool s` — so it is genuinely load-bearing (the construction turning a distinguisher-derived predictor into a *small circuit for `f`*). It is therefore **built syntactically**: the reconstructed circuit is `csubst tables P` — the Yao predictor circuit `P` (entry 194/321) with the other blocks' hardwired tables substituted into its inputs (`Hardness.Circ` is `YaoCircuit.Circ`, the same inductive). **`csubst`** / **`subst_eval`** (PROVED) — circuit substitution and its semantic preservation `(csubst g P).eval x = P.eval (fun j => (g j).eval x)`. **`Circ.size_pos`** (PROVED) — every circuit has size `≥ 1`. **`subst_size_le`** (PROVED) — `(csubst g P).size ≤ P.size * M` when every sub-circuit has size `≤ M` (`M ≥ 1`): each leaf `var j` becomes `g j` (`≤ M`), each internal gate contributes `≤ M` (induction + `nlinarith`). **`reconstructed_computes`** (PROVED) — `csubst tables P` computes `fbool` from the composition correctness (entry 196's semantic content). **`reconstructed_hasCircuit`** (PROVED) — `HasCircuitOfSize fbool (P.size * M)`: the reconstructed circuit is concrete, computes `fbool`, and is size-bounded — the constructive core of `bridgeRecon`. **`bridgeRecon_constructed`** (PROVED) — the bridge as a concrete build. Honest scope: this **builds** the small circuit for `f` (`subst tables P`), proves its semantics (`subst_eval` + composition correctness) and bounds its size (`subst_size_le`) — `bridgeRecon` is no longer an abstract socket. Remaining genuine inputs: the table circuits realizing the hardwired blocks (size `≤ M ≈ 2^k`, the design — entry 194 bounded table size, `tabulate` proved lookup correctness) and the composition correctness. As in entry 321, this is a gate-count + semantics construction; it does **not** formalise `ACC⁰` *membership* (depth/gate-type). With both bridges built, the NW reconstruction's small-circuit-for-`f` is concrete; `HardFunction → PRGExists` rests on the irreducible hardness (`HardFor`, the circuit lower bound) and the design. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NWReconstructionBridgeRecon` |
| **Entry 323 — ACC⁰ depth / gate-type faithfulness for the circuit bridges (PROVED)** | Removes the honest caveat carried by entries 321/322 (the bridges captured size + semantics but not `ACC⁰` depth/gate-type). Equips `Circ` (the `YaoCircuit.Circ` model) with a **depth** measure and a **gate-type** accounting, and proves the Yao and reconstruction bridges are `ACC⁰[2]`-faithful. The basis `var/tru/fals/cnot/cand/cor/cxor` is exactly `{input, const, ¬, ∧, ∨, MOD₂}` = the `ACC⁰[2]` gates — the *only* modular gate is `MOD₂`, so no construction can introduce a foreign `MOD_q` (the gate-type concern of entry 318 cannot arise). **`depth`** — `var`/const `↦ 0`, `cnot ↦ 1 + ·`, binary gates `↦ 1 + max`. **`predictor_depth`** (PROVED) — `(predictor D gidx).depth = D.depth + 2`: the Yao bridge adds constant depth (a `NOT` then a `MOD₂`). **`csubst_depth_le`** (PROVED) — `(csubst g P).depth ≤ P.depth + dt` when every sub-circuit has depth `≤ dt`: the reconstruction bridge's depth is additive (predictor depth + table depth). **`predictor_preserves_acc0Depth`** / **`csubst_preserves_acc0Depth`** (PROVED) — bounded depth in ⇒ bounded depth out (`InACC0Depth d C := depth C ≤ d`). **`xorFree`** + **`csubst_xorFree`** (PROVED, axiom-free) — gate-type tracking: substitution preserves `XOR`-freeness (introduces no `MOD₂` beyond `P` and the tables). **`predictor_uses_mod2`** (PROVED) — the Yao bridge introduces exactly one `MOD₂`. Honest scope: this proves the bridges are `ACC⁰[2]`-faithful in **depth** (constant/additive overhead) and **gate type** (only `{¬,∧,∨,MOD₂}`, no foreign modulus) — the two constraints the 321/322 caveat flagged. The one remaining modelling choice is **fan-in**: `Circ` is a bounded-fan-in (formula) model, whereas full `ACC⁰` permits unbounded fan-in; an unbounded-fan-in model is a separate, larger formalisation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CircuitBridgeACC0Faithful` |
| **Entry 324 — the unbounded-fan-in ACC⁰ circuit model (PROVED)** | Closes the fan-in residual of entry 323 by building the genuine unbounded-fan-in `ACC⁰[m]` model. **`UCirc n`** — circuit over `n` inputs with `var`, `const`, `unot`, and the unbounded-fan-in gates `uand`/`uor`/`umod m` taking a *list* of children (one gate of any fan-in); `umod m cs` is the `MOD_m` gate (fires iff `#`accepting children `≡ 0 mod m`) — the genuine `ACC⁰[m]` basis. **`eval`, `size`, `depth`, `moduli`** — Boolean semantics, gate count, depth (`1 + foldr max` over children), and the `MOD`-modulus multiset (for `ACC⁰[m]` gate-type accounting). **`UCirc.induction`** — a custom induction principle (the `induction` tactic refuses nested inductives; built via size-bounded strong induction, `@[elab_as_elim]`, list-case IH over `∀ c ∈ cs`). **`uand_vars_depth`** (PROVED) — `depth (uand (L.map var)) = 1` for *any* list `L`: **unbounded fan-in, constant depth** — the defining `ACC⁰` property the binary `Circ` model could not express (`AND` of `n` inputs is one gate of depth `1`, not a depth-`log n` tree). **`usubst`, `usubst_eval`** (PROVED) — circuit substitution and its semantic preservation, the reconstruction wiring faithful to unbounded fan-in. **`usubst_depth_le`** (PROVED) — substitution depth is additive. **`IsACC0m`** + **`umod2_isACC0`** (PROVED) — gate-type accounting (`ACC⁰[m]` iff every `MOD` gate has modulus `m`). Honest scope: this builds the faithful unbounded-fan-in `ACC⁰[m]` model — the modelling choice entry 323 deferred — with semantics, depth, size, modulus accounting, the defining unbounded-fan-in/constant-depth property, and the substitution (wiring) lemmas. It does **not** by itself reprove the NW bridges over `UCirc` (that would re-run entries 321/322 in this model); it supplies the model and infrastructure they would use. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnboundedFanin` |
| **Entry 325 — the NW bridges ported onto the unbounded-fan-in model `UCirc` (PROVED)** | Ports the Yao predictor (entry 321) and the reconstruction (entry 322) onto the unbounded-fan-in model `UCirc` (entry 324), so the bridges are now faithful in depth, gate type, **and** unbounded fan-in. **`upredictor D gidx := ¬MOD₂([var gidx, ¬D])`** — the Yao predictor as a `MOD₂` gadget (`XOR(a,b) = ¬MOD₂([a,b])`). **`upredictor_eval`** (PROVED) — computes the guess-and-correct rule `if D then g else ¬g`. **`upredictor_depth`** (PROVED) — `= D.depth + 3`, constant depth overhead. **`upredictor_isACC0`** (PROVED) — `ACC⁰[2]` (its only `MOD` gate is the `MOD₂` it adds) when `D` is. **`UComputes` / `HasUCircuitOfDepth` / `HasUCircuitOfSize`** — circuit-computes-`f` and bounded depth/size predicates. **`usubst_size_le`** (PROVED) — `size (usubst g P) ≤ size P · M` (custom induction + `List.sum_le_sum` + `List.sum_map_mul_right`). **`ureconstructed_computes`** (PROVED) — `usubst tables P` computes `f` (via entry 324's `usubst_eval`). **`ureconstructed_hasDepth`** (PROVED) — the reconstruction is bounded-depth `≤ P.depth + dt` (via entry 324's `usubst_depth_le`): the depth-faithful, unbounded-fan-in reconstruction. **`ureconstructed_hasSize`** (PROVED) — bounded-size `≤ size P · M`. Honest scope: both NW bridges are now `ACC⁰`-faithful in all three dimensions (depth, gate type, fan-in) — the predictor an explicit constant-depth `MOD₂` gadget, the reconstruction `usubst` with proved semantics/depth/size. This still rests, as the NW argument always does, on the irreducible hardness of the witness function, the design, and the composition correctness of predictor and tables. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnboundedBridges` |
| **Entry 326 — the nondeterministic time-hierarchy residue, internal structure (PROVED)** | Opens the **nondeterministic time hierarchy** residue (`ConcreteHierarchy f g := ¬ (NTIME f ⊆ NTIME g)`) of entry 320's `WilliamsClassicalResidues` and proves its internal structure. **`langOf M g x := acceptsWithin M x (g |x|)`** — the language a machine decides under the `g`-clock. **`mem_NTIME_iff`** (PROVED) — `L ∈ NTIME g ↔ ∃ M, L = langOf M g`: the `NTIME`-class characterisation (`funext`/`propext`). **`not_mem_NTIME_of_no_machine`** (PROVED) — `(∀ M, D ≠ langOf M g) → D ∉ NTIME g`: a language differing from every `g`-clocked machine lies outside `NTIME g`. **`concreteHierarchy_of_diagonal`** (PROVED) — a diagonal language `D` in `NTIME f` (placement) that differs from every `g`-clocked machine (diagonalisation) witnesses `¬ (NTIME f ⊆ NTIME g)`: the diagonalisation assembly. **`lazy_diag_escape`** (re-export of entry 294's `lazy_diag_not_mem_range`) — the complement-safe lazy diagonalisation: a lazily-defined language escapes the entire enumeration without relying on closure under complement (which `NTIME` lacks) — the proved mechanism for the diagonalisation residue. Honest scope: proves the internal assembly of the hierarchy residue (`NTIME`-class characterisation + diagonalisation reduction are theorems), reducing `ConcreteHierarchy f g` to two precise machine residues — placement (`D ∈ NTIME f`, the clocked universal simulation of entries 296–311) and diagonalisation (the proved lazy diagonal of 294). Does **not** discharge placement: the abstract `NTM` has an arbitrary `Config` type so is not enumerable at this abstraction (enumerability belongs to the concrete model). Honest decomposition of the hierarchy socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NondetHierarchyInternals` |
| **Entry 327 — the concrete nondeterministic time hierarchy, complement-safe, on one residual (PROVED)** | Discharges the enumerability piece that entry 326 could not (the abstract `NTM` has an arbitrary `Config`); the concrete model has `enum_covers` (`cNTIME g ⊆ Set.range (enum g)` via `TMachine ≃ ℕ`, proved in `…ACC0UniversalNTM`). **Correctness point:** the existing `cTime_hierarchy` uses the *Cantor flip* diagonal (`diag_not_mem_range`), which is *unsound for nondeterministic time* — the flip requires complementing an `NTM` computation, and `NTIME` is not closed under complement, so its `diag_in_big` socket is unachievable. The honest hierarchy uses the **complement-safe lazy** diagonal (entry 302). **`place_of_decider`** (PROVED) — `D ∈ cNTIME f` from a routing `TMachine` deciding `D` within `f`. **`concrete_lazy_hierarchy`** (PROVED) — for any `D` with `D ∉ Set.range (enum g)` (the complement-safe diagonalisation, no flip) and `D ∈ cNTIME f` (placement), `¬ (cNTIME f ⊆ cNTIME g)`, composing the proved `enum_covers` with the escape. **`concrete_lazy_hierarchy_of_decider`** (PROVED) — the hierarchy from exactly two residues: the complement-safe escape (proved mechanism, entry 302) and a routing-decider `TMachine`. **`cNTIME_enumerable`** (re-export of the proved `enum_covers`). Honest scope: this lands the concrete nondeterministic time hierarchy on a single genuine residual — the **routing decider**: one low-level `TMachine` running the lazy-diagonal logic (block layout → clocked universal simulation of `enum`-machines, entries 296–298 → one decidable boundary complement, entry 299) within `f(|x|)`. Enumerability (`TMachine ≃ ℕ`), diagonalisation (complement-safe lazy, 302), clocking and overhead-1 universal simulation (296–298), and the decidable boundary complement (299) are all proved. The routing decider — the transition-table compilation of that routing — is the lone remaining construction, substantial formalisation engineering, **not built here and not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ConcreteLazyHierarchy` |
| **Entry 328 — the lazy-diagonal decision procedure, assembled from proved primitives (PROVED)** | Builds the total function the entry-327 routing decider must realise, and verifies — against the *proved* primitives — what it computes in each case. **`enumAccept g inp k m := decAccept (machineEquiv.symm k) (inp m) (g |inp m|)`** — the `g`-clocked decidable enumeration (total; `noncomputable` only because the enumeration `machineEquiv : TMachine ≃ ℕ` is classical, each value being the finite reachable-config search `decAccept`, entry 299). **`lazyDiagDecide g inp := lazyDiagLang (enumAccept g inp)`** — the lazy diagonal over it (entry 302): copy on even positions, complement on the odd boundary; block layout is the trivial `n % 2` / `n / 2`. **`lazyDiagDecide_copy`** (PROVED) — at even `n`, accepts iff `acceptsWithin (toNTM (machine n/2)) (inp (n+1)) (g …)`: a genuine *nondeterministic acceptance* (realised by the universal simulation of 296–298, in `NTIME`), via `acceptsWithin_iff_decAccept` (299). **`lazyDiagDecide_boundary`** (PROVED) — at odd `n`, accepts iff `¬ acceptsWithin (toNTM (machine n/2)) (inp (n−1)) (g …)`: the *decidable bounded complement* (entry 299) — the single complement lazy diagonalisation affords. **`lazyDiagDecide_escapes`** (PROVED) — the decision procedure escapes the enumeration (entry 302): the correct diagonal. Honest scope: this **builds and verifies the decision procedure** — grounding the copy case in a nondeterministic acceptance (universal simulation) and the boundary case in the decidable bounded complement (299), and confirming it escapes the enumeration. What remains for the entry-327 routing `TMachine` is purely the **timing-aware compilation**: realising this explicit total function as one transition table within `f(|x|)` (copy via the clocked universal simulator 296–298, boundary via the bounded search 299, routed by `n % 2` / `n / 2`). That transition-table assembly is the lone remaining construction, **not built here and not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LazyDiagDecider` |
| **Entry 329 — routing-machine assembly: sub-table monotonicity (PROVED)** | The first verified brick of the routing-`TMachine` build (the residual of entries 327/328): the sound mechanism for assembling the routing table from phase sub-machines, namely that a concrete machine's computation is preserved when its transition table is embedded in a larger one. **`concreteStep_mono`** (PROVED) — `M ⊆ M' → concreteStep M c d → concreteStep M' c d` (the step relation quantifies over `∃ t ∈ M`, and `t ∈ M ⊆ M'`). **`reachIn_mono`** (PROVED) — `M ⊆ M' →` every `k`-step run of `toNTM M` is a `k`-step run of `toNTM M'` (induction on `k`). **`acceptsWithin_mono_machine`** (PROVED) — bounded acceptance lifts (`toNTM M`, `toNTM M'` share init `(0,0,x)` and accept `state = 1`). **`acceptsWithin_union_left` / `acceptsWithin_union_right`** (PROVED) — acceptance by either part of a combined table `M₁ ++ M₂` lifts to the union (`List.subset_append_left/right`) — the two-phase assembly primitive. Axioms just `[propext]`. Honest scope: this proves the **assembly foundation** — the routing table is built by laying the phase sub-machines (decode, universal simulation 296–298, bounded complement 299) into one combined transition table, each phase's already-proved computation surviving by monotonicity. It does **not** yet build the routing table: that additionally needs the **control-flow wiring** (state-offsetting the phases, redirecting decode → dispatch → simulate/complement through the transition structure) and the **`f`-timing** analysis — the genuine remaining low-level engineering, **not built here and not faked**. The first verified brick of that build. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RoutingMachineComposition` |
| **Entry 330 — routing-machine wiring: state-offset preserves computation (PROVED)** | The second verified brick: to combine phases into one routing table they must occupy *disjoint state ranges*, and this proves that **offsetting a machine's states by a constant `s` preserves its entire computation**. **`shiftConfig s (state, head, tape) := (state + s, head, tape)`**, **`shiftTrans s`** adds `s` to a rule's read- and write-states, **`shiftMachine s M := M.map (shiftTrans s)`** (over the `TMTrans`/`CConfig` types — `Move` is not definitionally `ℕ`). **`readSym_shiftConfig`** (PROVED, `rfl`) — the read ignores the state. **`applyTrans_shiftTrans`** (PROVED, `rfl` via structure-eta) — `applyTrans (shiftConfig s c) (shiftTrans s t) = shiftConfig s (applyTrans c t)`: a step commutes with the offset (`readSym`/`moveHead`/`writeAt` touch only head and tape). **`concreteStep_shift`** (PROVED) — a step of `M` becomes a step of `shiftMachine s M` between shifted configs. **`reachIn_shift`** (PROVED) — a whole `k`-step run is preserved under the offset (induction on `k`). **`reachIn_in_combined`** (PROVED) — `M₂`'s run, offset by `s`, holds in the combined table `M₁ ++ shiftMachine s M₂` (via `reachIn_shift` + entry-329 monotonicity, `List.subset_append_right`): a phase placed on disjoint states keeps its run inside the routing table. Honest scope: with entry 329 this assembles *parallel* sub-tables on disjoint states (`reachIn_in_combined`). It does **not** yet do the **control-flow handoff** (redirecting phase 1's exit state to `s`, phase 2's shifted start, so the machine *sequences* the phases) or the **`f`-timing**, nor instantiate the phases with the universal simulator (296–298) and bounded complement (299) — the remaining low-level engineering, **not built here and not faked**. The second verified brick of the routing-machine build. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RoutingMachineWiring` |
| **Entry 331 — routing-machine sequencing: the control-flow handoff (PROVED)** | The third verified brick: the control flow that *sequences* the phases (entries 329/330 placed them in parallel on disjoint states; this wires phase 1's exit to phase 2's entry). **`handoffRules qexit s`** — the table `[(qexit, b) ↦ (s, b, stay)]` (both reads), jumping at exit state `qexit` to phase 2's shifted start state `s` (`Move := Fin 3`, `2` = stay). **`postHandoff qexit s c := applyTrans c ((qexit, readSym c), (s, readSym c, 2))`** — the resulting config (state `s`, head unchanged, read symbol written back). **`seqMachine M₁ M₂ qexit s := M₁ ++ handoffRules qexit s ++ shiftMachine s M₂`**. **`handoff_step`** (PROVED) — from any config in state `qexit`, `handoffRules qexit s` steps to `postHandoff` (`cases readSym` + `simp`; the read-state condition via `rw [hq]`). **`reachIn_seq`** (PROVED) — the end-to-end composition: phase-1 run (`a` steps of `M₁` to a state-`qexit` config) + the handoff (1 step) + phase-2 run (`b` steps of `shiftMachine s M₂` from `postHandoff`) ⟹ a run of `seqMachine` of length `a + 1 + b`, each phase lifted into the combined table by entry-329 monotonicity (`List.subset_append_left/right` + `.trans`) and the run concatenated by `reachIn_add`. Honest scope: this proves the **control-flow handoff** that sequences two phases into one transition table — the piece 329/330 left open. Together 329/330/331 give the routing machine's *control structure*, fully verified: parallel disjoint-state assembly plus exit → entry sequencing. What remains for the complete routing decider is instantiating phase 1 = decode/dispatch and phase 2 = the universal simulator (296–298) / bounded complement (299) as concrete tables, plus the **`f`-timing** bound on the total step count — the remaining low-level engineering, **not built here and not faked**. The third verified brick. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RoutingMachineSequencing` |
| **Entry 332 — phase instantiation: a concrete sequenced machine that accepts (PROVED)** | The fourth verified brick: instantiates the abstract control skeleton (entries 329/331) with *concrete* transition tables and proves a real end-to-end **accepting** run, validating the whole pipeline including the accept-state plumbing. **`demoRouting qexit s := handoffRules 0 qexit ++ handoffRules qexit s ++ handoffRules s 1`** — a concrete three-phase sequenced `TMachine` jumping `0 → qexit → s → 1` (each phase a proved `handoffRules` jump table). **`demoRouting_accepts`** (PROVED, axioms just `[propext]`) — `acceptsWithin (toNTM (demoRouting qexit s)) x 3` for every input `x`: the machine runs `(0,0,x) → (qexit,…) → (s,…) → (1,…)`, each step the proved `handoff_step` (331) lifted into the combined table by `concreteStep_mono` (329), reaching the **global accept state `1`** (`reachIn 3` as a nested step-chain, accept by `rfl`). Honest scope: this proves the routing control skeleton genuinely accepts concrete `TMachine`s and reaches the global accept state — the assembly (329) + sequencing (331) machinery works end-to-end on real tables. The phases are minimal jump tables, **not** the real decode / universal-simulation / bounded-complement phases. Instantiating *those* additionally requires bridging the universal simulator (entries 296–298, built as an *abstract* `NTM`, not a concrete `TMachine`) into a transition table, the bounded complement as a table, and the `f`-timing — the genuine remaining low-level construction, **not built here and not faked**. The fourth verified brick: the control skeleton, validated concretely. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RoutingMachinePhaseDemo` |
| **Entry 333 — the abstract-NTM → concrete-TMachine bridge: simulation transfers acceptance (PROVED)** | The fifth verified brick: the bridge mechanism resolving the gap entry 332 exposed (the universal simulator is an *abstract* `NTM`, the routing assembly runs on *concrete* `TMachine`s). **`Realizes A M φ cost`** (structure) — a concrete `TMachine` `M` step-simulates an abstract `NTM` `A` via config map `φ`: `step` (each `A.step c d` is a `cost`-step run of `toNTM M` from `φ c` to `φ d`), `accept` (`A.accept c → (φ c).1 = 1`), `init` (`φ (A.init x) = (toNTM M).init x`). **`reachIn_realize`** (PROVED) — a `k`-step abstract run becomes a `k * cost`-step concrete run (induction on `k`, concatenating per-step `cost`-blocks via `reachIn_add`; `(k+1)*cost = cost + k*cost` by `ring`). **`acceptsWithin_realize`** (PROVED) — `acceptsWithin A x t → acceptsWithin (toNTM M) x (t * cost)`: acceptance transferred with a `× cost` time blowup (`k*cost ≤ t*cost` by `gcongr`, init via `h.init`, accept via `h.accept`). Honest scope: this proves the **general abstract→concrete bridge** — any concrete `TMachine` step-simulating an abstract `NTM` transfers acceptance with controlled blowup. It reduces the routing decider's universal-simulation phase to a single named construction: exhibiting `Realizes physU M φ cost` for a concrete `TMachine` `M` — i.e. **building the universal Turing machine as an actual transition table** and proving it simulates the abstract universal NTM (296–298). That universal-TM table is a substantial classical construction, **not built here and not faked**. With the bridge proved, that table (plus the `f`-timing) is the lone remaining piece for the concrete universal-simulation phase. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AbstractConcreteBridge` |
| **Entry 334 — the universal-TM-table build, brick 1: `Realizes` interface inhabited + a verified traversal primitive (PROVED)** | Commits to the universal-TM construction (entry 333's named residual) with its first verified bricks. **`realizes_self`** (PROVED, axioms just `[propext]`) — `Realizes (toNTM M) M id 1`: every concrete `TMachine`, viewed as the abstract `NTM` `toNTM M`, is realized by itself (identity config map, one concrete step per abstract step). The entry-333 bridge interface is genuinely *inhabited* — concrete simulations of abstract machines exist, it is not a vacuous socket. **`moveHead_right`** (PROVED) — `moveHead h 1 = h + 1`, the right-move primitive. **`walkRight`** (the state-`0` loop that moves right on either symbol) and **`walkRight_run`** (PROVED) — from `(0,0,x)`, `walkRight` runs `k` steps to a configuration in state `0` with head at position `k`: the tape-traversal *scanner* a universal TM uses to walk the encoded table (induction on `k`, extending the run by one verified `walkRight` step `k → k+1` via `reachIn_add`). Honest scope: this is **brick 1** of the universal-TM-table build — the simulation interface shown inhabited and a genuine multi-step universal-TM component (the scanner) verified over the concrete model. It does **not** build the universal machine: the remaining bricks (a tape encoding of `(machine code, input)`, the rule-lookup scan matching `(state, symbol)` against the encoded table, the apply-step, the simulation loop, accept detection, assembled into one table `U` with `Realizes physU U φ cost` proved) are the substantial classical construction, undertaken as a real multi-brick effort, each a verified concrete component, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMBuild` |
| **Entry 335 — universal-TM-table build, brick 2: the tape encoding of `(machine, input)` (PROVED)** | The data representation the universal machine reads off its tape. `(TMachine × List Bool)` is `Encodable` (every component — `ℕ`, `Bool`, `Fin 3`, products, lists — is), and `List Bool` is denumerable. **`tapeEquiv`** — `List Bool ≃ ℕ` (`Denumerable.ofEncodableOfInfinite`). **`encodeTape M x := tapeEquiv.symm (Encodable.encode (M, x))`** / **`decodeTape t := Encodable.decode (tapeEquiv t)`** — write the pair to a tape / read it back. **`decodeTape_encodeTape`** (PROVED) — `decodeTape (encodeTape M x) = some (M, x)`: the round-trip, so the machine to simulate and its input are exactly recoverable from the encoded tape (`Equiv.apply_symm_apply` + `Encodable.encodek`). **`encodeTape_injective`** (PROVED) — distinct `(machine, input)` pairs get distinct tapes. Honest scope: **brick 2** — a verified injective, round-trip-decodable tape encoding of `(machine, input)`, the universal machine's input format, with the pair provably recoverable. It is the *data* foundation (recoverable); it does **not** yet give a *bit-by-bit scannable* format with a TM-level lookup procedure — that is brick 3 (the rule-lookup scan, walking the encoded table via the brick-1 traversal), followed by the apply-step, the simulation loop, accept detection, and assembling `U` with `Realizes physU U φ cost`. Those remain the substantial construction, built as verified bricks, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMEncoding` |
| **Entry 336 — universal-TM-table build, brick 3: the rule-lookup, sound and complete (PROVED)** | The heart of the simulation cycle: given the current `(state, symbol)`, find the matching transition and apply it. **`lookup M c := M.find? (· .1 = (c.1, readSym c))`** / **`applyLookup M c := (lookup M c).map (applyTrans c)`** — find the matching rule / find-and-apply. **`lookup_sound`** (PROVED, axioms just `[propext]`) — `lookup M c = some t → concreteStep M c (applyTrans c t)`: a found rule yields a genuine step (`List.find?_some` for the match + `List.mem_of_find?_eq_some` for membership + `of_decide_eq_true`). **`applyLookup_sound`** (PROVED) — `applyLookup M c = some d → concreteStep M c d`. **`lookup_complete`** (PROVED) — `concreteStep M c d → ∃ t, lookup M c = some t`: whenever a step exists, the lookup finds a matching rule (`List.find?_eq_none` in the contradiction case). Honest scope: this proves the **rule-lookup logic** — the matching-transition search is sound and complete w.r.t. `concreteStep`, exactly what the simulation cycle's lookup step computes. It does **not** build the bit-level scan loop that physically realises `find?` over the encoded transition table on the tape (walking the encoding with the brick-1 traversal, comparing each rule's encoded `(state, symbol)` to the current pair) — that realisation, then the apply-step, the simulation loop, accept detection, and `Realizes physU U φ cost`, remain the substantial construction, built as verified bricks, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMLookup` |
| **Entry 337 — universal-TM-table build, brick 4: the simulation loop, sound (PROVED)** | Iterates brick-3's verified lookup-and-apply step into the deterministic **simulation loop** and proves the iteration is a genuine run. **`simIter M`** — `0 ↦ some c`; `k+1 ↦ (applyLookup M c).bind (simIter M k)`: run `M` deterministically for `k` steps by repeatedly looking up and applying the matching rule (`none` if no rule ever matches). **`simIter_sound`** (PROVED) — `simIter M k c = some d → reachIn (toNTM M) k c d`: the deterministic lookup-and-apply iteration *is* a valid `k`-step run of the nondeterministic machine (induction on `k`, each step via brick-3's `applyLookup_sound`, `Option.bind_eq_some_iff`). **`simIter_acceptsWithin`** (PROVED) — a `k`-step simulation from `(0,0,x)` reaching an accept state (`d.1 = 1`) gives `acceptsWithin (toNTM M) x k`. Honest scope: this proves the **simulation loop is sound** — iterating the verified step is a genuine run, and an accepting simulation witnesses `acceptsWithin`. With bricks 1–3 this is the verified *logical* engine of the universal simulation cycle (traverse, look up, apply, repeat, detect accept), all at the config/rule level. What remains is the **bit-level realisation** — representing `simIter` as the run of one concrete `TMachine` `U` over the encoded tape, `Realizes physU U φ cost`, and the `f`-timing — built as verified bricks, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMLoop` |
| **Entry 338 — universal-TM-table build, brick 5: the scannable encoding layer (PROVED)** | Replaces brick 2's *opaque* `Encodable` tape with a concrete, **scannable** bit-grammar (intentionally dumb unary-with-separators: a nat `n` is `n` `true`s then a `false`), so a left-to-right scanner can consume it. **`encodeNatBits` / `decodeNatBits`** + **`decodeNatBits_encodeNatBits`** (PROVED) — `decodeNatBits (encodeNatBits n ++ rest) = (n, rest)`: scan one nat, leave the rest (`decodeNatBits` counts leading `true`s by structural recursion; induction + `encodeNatBits_succ`). **`encodeTransBits` / `decodeTransBits`** + **`decodeTransBits_encodeTransBits`** (PROVED) — scan one transition (its five fields `state, sym, state, sym, move`), leave the rest (the `Fin 3` move via `dif_pos t.2.2.2.isLt`). **`encodeMachineBits` / `decodeMachineBits`** + **`decodeMachineBits_encodeMachineBits`** (PROVED) — length-prefixed transition list; scan a whole table, leave the rest (`decodeTransListBits` + induction on the machine). Each round-trip `decode (encode x ++ rest) = some (x, rest)` is precisely the **cursor lemma** a scanner needs: it consumes exactly `x`'s encoding and stops, leaving `rest`. Honest scope: this builds the concrete scannable encoding with verified scan-and-leave-rest round-trips at every level (nat, transition, machine) — the traversal correctness a bit-level reader stands on; efficiency is irrelevant, scanability and correctness are the point. It does **not** yet build the scanner as a `TMachine`: the next bricks implement `lookup` (rule scan-and-match, 339) and `applyTrans` (340) over this format as concrete machine fragments, then assemble `U` and prove `Realizes physU U φ cost` (341) — built as verified bricks, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScannable` |
| **Entry 339 — universal-TM-table build, brick 6: bit-level rule lookup (PROVED)** | The rule lookup over the brick-338 scannable encoding, proved to coincide with the abstract `lookup` (336). **`bitLookup bits c := (decodeMachineBits bits).bind (lookup ·.1 c)`** — decode the encoded table from `bits` (the brick-338 scan) and find the matching rule. **`bitLookup_encodeMachineBits`** (PROVED) — `bitLookup (encodeMachineBits M) c = lookup M c`: on a correctly-encoded table the bit-level lookup *is* the abstract lookup (via the brick-338 machine round-trip). **`bitLookup_sound`** (PROVED) — `bitLookup (encodeMachineBits M) c = some t → concreteStep M c (applyTrans c t)`. **`bitLookup_complete`** (PROVED) — `concreteStep M c d → ∃ t, bitLookup (encodeMachineBits M) c = some t`. Honest scope: this implements the **rule lookup over the scannable encoding** and proves it equals the abstract `lookup` (so sound and complete w.r.t. `concreteStep`) — the lookup step of the simulation cycle, now operating on the encoded tape format. The "scan" is the brick-338 `decodeMachineBits` (left-to-right bit consumption); realising `decodeMachineBits`/the find as actual `TMachine` transitions (walking the bits with the brick-1 traversal) is folded into the assembly of `U`. Next: brick 7 (bit-level apply, 340), then assemble `U` and prove `Realizes physU U φ cost` (341) — built as verified bricks, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMBitLookup` |
| **Entry 340 — universal-TM-table build, brick 7: bit-level apply (PROVED)** | The apply-step over the scannable encoding, proved to equal the abstract `applyTrans`. A configuration `(state, head, tape)` is encoded with the brick-338 nat grammar plus a length-prefixed tape: **`encodeConfig c := encodeNatBits state ++ encodeNatBits head ++ encodeBoolList tape`** (`encodeBoolList l := encodeNatBits l.length ++ l`). **`decodeBoolList_encodeBoolList`** (PROVED) — length-prefixed bit-block round-trip (`decodeNatBits` + `take`/`drop`). **`decodeConfig_encodeConfig`** (PROVED) — `decodeConfig (encodeConfig c ++ rest) = (c, rest)`: scan a `(state, head, tape)` config, leave the rest. **`bitApply bits t := encodeConfig (applyTrans (decodeConfig bits).1 t)`** + **`bitApply_encodeConfig`** (PROVED) — `bitApply (encodeConfig c) t = encodeConfig (applyTrans c t)`: the bit-level apply *is* the abstract `applyTrans`, up to the config encoding. Honest scope: this implements the **apply-step over the scannable encoding** and proves it equals the abstract `applyTrans` — the config-update step of the simulation cycle, on the encoded tape format. With brick 6 (lookup) this gives both halves of one simulation step at the encoding level: scan-and-match the rule, then update the encoded config. What remains is assembling these into one concrete `TMachine` `U` whose transitions realise the decode/scan/update as actual tape moves, and proving `Realizes physU U φ cost` (+ the `f`-timing) — brick 8 (341), built as verified bricks, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMBitApply` |
| **Entry 341 — universal-TM-table build, brick 8: the encoded simulation run, correct (PROVED)** | Composes bricks 6–7 into one encoded simulation step, proves it equals the abstract `applyLookup`, and iterates it into the full encoded run, proving the entire deterministic simulation runs correctly on encoded bit-tapes. **`encodedStep Mbits cbits := (bitLookup Mbits (decodeConfig cbits).1).map (bitApply cbits)`** — decode the config, look up the rule in the encoded table, apply it, all on bit-tapes. **`decodeConfig_encodeConfig_fst`** (PROVED) — `(decodeConfig (encodeConfig c)).1 = c`. **`encodedStep_correct`** (PROVED) — `encodedStep (encodeMachineBits M) (encodeConfig c) = (applyLookup M c).map encodeConfig`: one encoded step equals the abstract step, re-encoded (via brick-6 `bitLookup_encodeMachineBits`, brick-7 `bitApply_encodeConfig`, `Option.map_map`). **`encodedRun`** (iterate `encodedStep`) + **`encodedRun_correct`** (PROVED) — `encodedRun (encodeMachineBits M) k (encodeConfig c) = (simIter M k c).map encodeConfig`: the entire `k`-step deterministic simulation runs correctly on encoded bit-tapes (induction on `k`). Honest scope: this is the **culmination at the encoding level** — the full universal simulation (`simIter`, brick 4) runs correctly entirely on the scannable bit-encoding (`encodedRun_correct`); config encoding, lookup, apply, and the whole loop are all verified equal to the abstract engine. The **one remaining residual** is purely the *function → transition* gap: realising `encodedStep` (a function on bit-lists) as the transitions of one concrete `TMachine` `U` (the `walkRight`-driven scans of bricks 1/5 wired into a transition table), which then gives `Realizes physU U φ cost` via the entry-333 bridge (plus the `f`-timing) — the genuine remaining low-level construction, **not built here and not faked** — but everything it must compute is now verified correct. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMEncodedRun` |
| **Entry 342 — universal-TM-table build, brick 9: emission ⇒ full universal simulation (PROVED)** | The final lifting (mirroring the entry-333 bridge, specialised to the verified encoded run): a per-macro-step emission obligation suffices to realise the *entire* simulation on a concrete `U`. **`EmitsEncodedStep U φ cost := ∀ Mbits cbits next, encodedStep Mbits cbits = some next → reachIn (toNTM U) cost (φ Mbits cbits) (φ Mbits next)`** — `U`, via the tape layout `φ`, realises one encoded lookup-and-apply in `cost` transitions. **`universalSim_of_emits`** (PROVED) — `EmitsEncodedStep` + the brick-8 `encodedStep_correct` ⟹ a whole `k`-step simulation `simIter M k c0 = some cf` is a `k * cost`-step run of `U` (`reachIn (toNTM U) (k*cost) (φ … (encodeConfig c0)) (φ … (encodeConfig cf))`), by induction on `k`: each `simIter` step is an `encodedStep` (brick 8), each `encodedStep` is `cost` `U`-steps (emission), composed by `reachIn_add`. Honest scope: this proves the final lifting — the per-macro-step emission obligation suffices to realise the entire universal simulation on `U` with a `× cost` blowup. So the **whole universal-TM construction is reduced to exactly one** named, irreducible obligation: build a concrete `U` (with layout `φ`) and prove `EmitsEncodedStep U φ cost` — that `U`'s transitions realise *one* encoded lookup-and-apply over the tape (the `walkRight`-driven scans of bricks 1/5 wired into a transition table). That single transition-table obligation is the genuine remaining low-level construction, **not built here and not faked** — but everything it feeds (the full simulation run, and acceptance via brick 4 + the entry-333 bridge) is proved. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMEmit` |
| **Entry 343 — universal-TM-table build, brick 10: emission ⇒ the universal machine decides (PROVED)** | Closes the conditional chain: the emission socket plus a natural accept-layout makes the universal machine `U` reach its accept state exactly when the simulated machine's run accepts. **`AcceptLayout φ := ∀ Mbits c, (φ Mbits (encodeConfig c)).1 = 1 ↔ c.1 = 1`** — `U`'s state is the accept state `1` iff the simulated config `c` is accepting (the obvious condition that `U`'s accept mirrors the simulated machine's). **`universalReachesAccept_of_emits`** (PROVED) — `EmitsEncodedStep` + `AcceptLayout` + a simulation `simIter M k c0 = some cf` reaching an accepting `cf` (`cf.1 = 1`) ⟹ `∃ d, reachIn (toNTM U) (k*cost) (φ … (encodeConfig c0)) d ∧ d.1 = 1`: `U` runs to a config in its accept state (via brick-9 `universalSim_of_emits` + the layout). Honest scope: this is the **final conditional theorem** — `U` decides what the simulated machine decides, *given* the emission socket (brick 9) and the natural accept-layout. So the entire Williams universal-simulation phase is reduced to **one** irreducible construction: building a concrete `U` (+ layout `φ`) satisfying `EmitsEncodedStep` and `AcceptLayout` (a verified universal-TM transition table), with *all* of its consequences (the full run, correct acceptance) proved. That single transition-table construction is the genuine remaining low-level work, **not built here and not faked**; everything it would deliver is verified. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMDecide` |
| **Entry 344 — universal-TM-table build, the transition table begins: `scanNat` (PROVED)** | Starts the *actual transition-table* construction (the `EmitsEncodedStep` socket). **`writeAt_getD`** (PROVED) — `(writeAt tape p w).getD q false = if q = p then w else tape.getD q false`: a write changes only the written cell (`getElem?_set_self`/`_ne`, `getElem?_append_left`/`_right`, `getElem?_replicate`). **`writeAt_getD_self`** (PROVED) — writing back the read symbol preserves *every* cell: `(writeAt tape p (tape.getD p false)).getD q false = tape.getD q false`, the non-destructive-scan invariant. **`scanNat := [((0,true),(0,true,1)), ((0,false),(1,false,1))]`** — in state `0`, on `true` move right and keep scanning, on `false` (separator) go to state `1` and move right. **`scanNat_step_true`** (PROVED) — at a `true` cell, `(0,j,tp) → (0,j+1,…)`, head advances, stays in state `0`. **`scanNat_step_false`** (PROVED) — at a `false` cell, `(0,j,tp) → (1,j+1,…)`, halts in state `1`. Honest scope: this is the **first verified fragment of the universal-TM transition table** — the write-back-preserves-tape invariant and `scanNat`'s single-step behaviour. It does **not** yet prove the full scan run (head reaches `n+1` after scanning `encodeNatBits n` — the next brick, a tape-invariant induction on `writeAt_getD_self`) nor `EmitsEncodedStep`. Building those, fragment by verified fragment, is the genuine remaining low-level construction, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanNat` |
| **Entry 345 — universal-TM-table build: the full `scanNat` run (PROVED)** | The verified cursor-advance over one encoded nat. **`encNat_getD_lt`** — `(encodeNatBits n ++ rest).getD j false = true` for `j < n` (the unary `true`s). **`encNat_getD_eq`** — `(encodeNatBits n ++ rest).getD n false = false` (the separator). **`scanNat_scan`** — the tape-invariant: after `j ≤ n` steps, state `0`, head `j`, tape contents preserved (induction on `j`, each step via `scanNat_step_true` + the write-back invariant `writeAt_getD_self`, entry 344). **`scanNat_run`** (PROVED) — `∃ tp, reachIn (toNTM scanNat) (n+1) (0, 0, encodeNatBits n ++ rest) (1, n+1, tp)`: the full scan consumes one encoded nat in `n+1` steps, halting in state `1` at head `n+1`. Honest scope: this proves the universal scanner's core primitive — advancing past one encoded nat — fully verified over the concrete `CConfig`/`TMTrans` model. Next fragments compose such scans (skip the five transition fields, scan the rule table for a match), then the apply, then `EmitsEncodedStep` — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanNatRun` |
| **Entry 346 — universal-TM-table build: the relocatable field scanner `scanNatFrom` (PROVED)** | Generalises `scanNat` (entries 344/345, fixed `0 → 1` and head-`0`) to the reusable primitive for chaining across an encoded transition's five fields (`nat, sym, nat, sym, nat`; entry 338). **`scanNatFrom s s'`** := `[((s,true),(s,true,→)), ((s,false),(s',false,→))]` — entry state `s` scans (`true` ↦ stay, move right), exits to `s'` at the `false` separator. **`scanNatFrom_step_true` / `scanNatFrom_step_false`** (PROVED) — single-step behaviours at an arbitrary head. **`scanNatFrom_scan`** (PROVED) — the relocatable tape-invariant: from `(s, h, tp)`, given the cells `h .. h+n-1` read `true`, after `j ≤ n` steps the machine is in state `s` at head `h+j`, tape contents preserved (induction on `j`; each step `scanNatFrom_step_true` + the write-back invariant `writeAt_getD_self`, entry 344, + `reachIn_add`). **`scanNatFrom_run`** (PROVED) — `∃ tp', reachIn (toNTM (scanNatFrom s s')) (n+1) (s, h, tp) (s', h+n+1, tp')` from an *abstract content hypothesis* (cells `h .. h+n-1` read `true`, cell `h+n` reads `false`): the scanner consumes exactly one encoded nat at offset `h`, between arbitrary entry/exit states, in `n+1` steps. Honest scope: this is the reusable field-scan primitive — relocatable in both head position and state numbering, parameterised over local content — so disjoint-state copies can be chained to skip the five fields of a transition. Next fragments wire those copies into one machine scanning a whole `encodeTransBits` layout, then scan-and-match the rule table, then the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanField` |
| **Entry 347 — universal-TM-table build: rule-list non-interference + sequential composition (PROVED)** | The wiring law for combining transition tables, so `scanNatFrom` copies can be chained into one machine. The key observation is structural: `concreteStep M c d` is an existential over rules `t ∈ M`, so enlarging `M` to a superset only *adds* rules — the witnessing rule of any step survives. Hence **no disjointness hypothesis is needed** for reachability to lift. **`concreteStep_append_left` / `concreteStep_append_right`** (PROVED) — a step of `M₁` (resp. `M₂`) is a step of `M₁ ++ M₂` (`List.mem_append`). **`reachIn_toNTM_mono`** (PROVED) — step monotonicity lifts to reachability: if every `concreteStep M` is a `concreteStep M'`, every `reachIn (toNTM M) k` run is a `reachIn (toNTM M') k` run (induction on `k`; the `0` case is configuration equality, the successor threads the step implication through the `∃ d`). **`reachIn_append_left` / `reachIn_append_right`** (PROVED) — a `k`-step run of a sub-table lifts verbatim to the union machine `M₁ ++ M₂`. **`reachIn_seq`** (PROVED) — sequential composition inside the union: a run of `M₁` from `c` to `d` (`a` steps) then a run of `M₂` from `d` to `e` (`b` steps) is a single run of `M₁ ++ M₂` from `c` to `e` in `a + b` steps (`reachIn_add` + the two lift lemmas). Minimal axioms (`reachIn_seq` is `[propext, Quot.sound]`, no `Classical.choice`). Distinct from entry-331's `ACC0RoutingMachineSequencing.reachIn_seq` (a state-renumbering handoff with an exit-state hypothesis); this is the rule-list union with no disjointness — complementary, not a duplicate. Honest scope: this is the wiring primitive that lets the relocatable field scanners be combined into one machine and run back-to-back. It does **not** yet instantiate it on the five fields of `encodeTransBits` (that threads each field's tape-preservation into the next field's content hypothesis — the next fragment), nor the rule-table scan-and-match, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCompose` |
| **Entry 348 — universal-TM-table build: preservation-tracking field run + the first two-field composition (PROVED)** | Closes the tape-preservation threading gap: `scanNatFrom_run` (346) returns `∃ tp'` but hides that `tp'` agrees with the input, yet the *next* field's content hypothesis is stated about that post-scan tape. **`scanNatFrom_run_pres`** (PROVED) — strengthens `scanNatFrom_run` to also expose `∀ q, tp'.getD q false = tp.getD q false`: the scan is non-destructive (`true` cells write back `true`, the separator writes back `false`). Proof reuses `scanNatFrom_scan`'s preservation to head `h+n`, then the closing `false` step's write-back is preserved via `writeAt_getD_self` (entry 344) since the read cell is `false`. **`scanTwoNats`** (PROVED) — the first genuine multi-field scan: given two consecutive encoded nats of lengths `n₁`, `n₂` at offsets `h` and `h+n₁+1`, the union machine `scanNatFrom 0 1 ++ scanNatFrom 1 2` runs `(n₁+1)+(n₂+1)` steps from `(0, h, tp)` to state `2` at head `h+n₁+1+n₂+1`. Field 2's content hypothesis is discharged on the post-field-1 tape via field 1's preservation (`rw hpres1`), then `reachIn_seq` (entry 347) composes the field-1 run (`0 → 1`) and field-2 run (`1 → 2`) inside the union machine; the shared mid-configuration `(1, h+n₁+1, tp1)` matches both, and the final head is defeq by left-associativity of `+`. Honest scope: this is the tape-preservation threading and the first two-field composition — the mechanism the full `encodeTransBits` traversal is built from. It does **not** yet handle the interleaved single *symbol* bits (`encodeTransBits = nat, sym, nat, sym, nat`; needs a one-bit scanner), nor the rule-table scan-and-match, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMFieldCompose` |
| **Entry 349 — universal-TM-table build: the one-bit scanner `scanBit` (PROVED)** | The missing piece for the single symbol bits interleaved in `encodeTransBits` (`nat, sym, nat, sym, nat`): the nat scanner `scanNatFrom` (346) handles the nat fields, `scanBit` handles the symbol bits. **`scanBit s s'`** := `[((s,true),(s',true,→)), ((s,false),(s',false,→))]` — unlike `scanNatFrom` (which loops in `s` until the separator), `scanBit` is a *single step*: it consumes one cell regardless of value, writes the symbol back, and exits to `s'` moving right. **`scanBit_step`** (PROVED) — the single-step behaviour at an arbitrary head, uniformly in the read symbol: `concreteStep (scanBit s s') (s, j, tp) (s', j+1, writeAt tp j (tp.getD j false))` (case on the read symbol; the `cases h :` form substitutes it in the goal, so the `applyTrans` component closes by `simp` and only the `readSym` component needs `h`). **`scanBit_run_pres`** (PROVED) — `∃ tp', reachIn (toNTM (scanBit s s')) 1 (s, h, tp) (s', h+1, tp') ∧ ∀ q, tp'.getD q false = tp.getD q false`: the one-step run consuming one symbol bit, preserving the tape (write-back non-destructive via `writeAt_getD_self`, entry 344). It matches the shape of `scanNatFrom_run_pres` (entry 348), so nat and bit scans compose under `reachIn_seq` (entry 347). Honest scope: this is the one-bit scanner with its preservation-tracking one-step run. It does **not** yet assemble the full five-field `encodeTransBits` scan (chaining `scanNatFrom`/`scanBit` runs), nor the rule-table scan-and-match, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanBit` |
| **Entry 350 — universal-TM-table build: the full five-field transition scan `scanTrans` (PROVED)** | The first complete traversal of one `encodeTransBits` record (layout `nat, sym, nat, sym, nat`), composing the verified field scanners. **`getD_append_shift`** (PROVED) — `(pre ++ X).getD (pre.length + i) false = X.getD i false`: reading past a prefix is reading the suffix. **`field_content`** (PROVED) — for `T = pre ++ (encodeNatBits n ++ rest)` with `pre.length = h`, the cells `h .. h+n-1` read `true` and cell `h+n` reads `false` (reusing the entry-345 `encodeNatBits` content lemmas). **`scanTrans`** (PROVED) — `∃ tp', reachIn (toNTM (scanNatFrom 0 1 ++ scanBit 1 2 ++ scanNatFrom 2 3 ++ scanBit 3 4 ++ scanNatFrom 4 5)) (rs+ws+mv+5) (0, 0, encodeTransBits t ++ rest) (5, rs+ws+mv+5, tp')`: the five-field machine scans a whole encoded transition in `|encodeTransBits t|` steps, ending in state `5` with the head exactly past the transition. The proof threads five field runs — the nat fields via `field_content` (their content transported to the current tape through accumulated preservation `pres_t2`/`pres_t4`) and the symbol bits via `scanBit_run_pres` (no content) — chained by `reachIn_seq` (entry 347, four times; the left-associated union matches the goal machine), with a final `convert ... using 1` discharging the step-count equality (`omega`) and the configuration triple (`Prod.mk.injEq`). Honest scope: this is the first whole-`encodeTransBits` scan. It does **not** yet scan-and-match the *rule table* (locate the transition whose `(state, read)` key matches the current configuration — the genuinely new decision step), nor apply the matched rule — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanTrans` |
| **Entry 351 — universal-TM-table build: the one-bit branch machine `branchBit` (PROVED)** | The first machine in the table build that does something other than scan forward — rule-table matching needs the ability to *branch* on what is read. **`branchBit s sTrue sFalse`** := `[((s,true),(sTrue,true,stay)), ((s,false),(sFalse,false,stay))]` — inspect the cell, write it back, *stay* (head unchanged), and route the control state to `sTrue` on `true` or `sFalse` on `false`. **`branchBit_step_true` / `branchBit_step_false`** (PROVED) — at `(s, j, tp)` reading `true` (resp. `false`) it steps to `(sTrue, j, …)` (resp. `(sFalse, j, …)`); the move `2` (stay) reduces `moveHead` to the head unchanged. **`branchBit_run_true` / `branchBit_run_false`** (PROVED) — the one-step branch run preserving both the tape and the head: reading `true` reaches `(sTrue, h, tp')`, reading `false` reaches `(sFalse, h, tp')`, with `tp'` agreeing with `tp` everywhere (write-back non-destructive via `writeAt_getD_self`, entry 344). Honest scope: this is the branch/decision atom for the rule table. It does **not** yet assemble an equality test between two encoded fields, nor the rule-table scan-and-match loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMBranch` |
| **Entry 352 — universal-TM-table build: the expected-bit checker `checkBit` (PROVED)** | `branchBit` (351) inspects and stays; verifying a *known bit pattern* needs to read a cell, *compare it to an expected constant* `b`, advance, and route on match/mismatch. **`checkBit b s sCont sFail`** := `[((s,b),(sCont,b,→)), ((s,!b),(sFail,!b,→))]` — read, write back, move right, go to `sCont` if the cell equalled `b` else `sFail`. **`checkBit_step_match` / `checkBit_step_fail`** (PROVED) — reading `b` advances to `(sCont, j+1, …)`, reading `!b` advances to `(sFail, j+1, …)`. **`checkBit_run_match` / `checkBit_run_fail`** (PROVED) — the one-step runs preserving the tape: a cell equal to `b` reaches `(sCont, h+1, tp')`, a cell equal to `!b` reaches `(sFail, h+1, tp')`, with `tp'` agreeing with `tp` everywhere. Chaining `checkBit b₁ s₀ s₁ F ++ checkBit b₂ s₁ s₂ F ++ …` (all failures funnelled to a common `F`) reaches the final continue-state iff every bit matched. Honest scope: this is the expected-bit checker — read, compare-to-constant, advance, branch. It does **not** yet chain checkers into a full pattern match, nor compare two *tape* regions (the two-pointer key match), nor the rule-table loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCheckBit` |
| **Entry 353 — universal-TM-table build: the fixed-pattern matcher `matchPattern` (PROVED)** | Chains a list of `checkBit`s (entry 352) into a machine that verifies a whole fixed bit pattern — the first composite control-flow machine of the matching half. **`matchPattern : List Bool → ℕ → ℕ → TMachine`** — `[] ↦ []`, `b :: bs ↦ checkBit b s (s+1) F ++ matchPattern bs (s+1) F`: state `s+i` checks pattern bit `i`, continuing to `s+i+1` or failing to the common state `F`. **`matchPattern_run_match`** (PROVED) — if the tape from offset `h` matches `pat` (`∀ i < pat.length, tp.getD (h+i) false = pat.getD i false`), then `∃ tp', reachIn (toNTM (matchPattern pat s F)) pat.length (s, h, tp) (s + pat.length, h + pat.length, tp') ∧ ∀ q, tp'.getD q false = tp.getD q false`: the matcher runs `pat.length` steps to the success state `s + pat.length` with the head past the pattern, preserving the tape. The proof is induction on `pat` (generalizing `s, h, tp`; `F` fixed): the head bit gives `hb` via `getD_cons_zero`, `checkBit_run_match` does bit 0, the tail's content hypothesis is discharged on the post-check tape via preservation + an index shift (`getD_cons_succ`), and `reachIn_seq` (entry 347) composes the two runs, with a final `convert ... using 1` bridging the `length_cons` arithmetic. Honest scope: this is the fixed-pattern matcher (constant pattern baked into the machine). It does **not** yet compare two *tape* regions (the two-pointer key match, both operands read from tape), nor the rule-table scan-and-match loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMMatchPattern` |
| **Entry 354 — universal-TM-table build: the leftward move `moveLeft` (PROVED)** | Every machine so far moves the head *right* (`scanNatFrom`, `scanBit`, `checkBit`; move `1`) or *stays* (`branchBit`; move `2`). The two-pointer key comparison needs the head to travel **left** to return between tape regions, so this brick adds the missing direction. **`moveLeft s s'`** := `[((s,true),(s',true,left)), ((s,false),(s',false,left))]` — read the cell, write it back, move the head left (move `0`, `h ↦ h-1`), go to `s'`, uniformly in the read symbol. **`moveLeft_step`** (PROVED) — at `(s, j, tp)` it steps to `(s', j-1, writeAt tp j (tp.getD j false))` (case on the read symbol; move `0` reduces `moveHead` to `j-1`). **`moveLeft_run_pres`** (PROVED) — `∃ tp', reachIn (toNTM (moveLeft s s')) 1 (s, h, tp) (s', h-1, tp') ∧ ∀ q, tp'.getD q false = tp.getD q false`: the one-step leftward run, tape preserved. Honest scope: this completes the set of head motions (right/stay/left) the universal table needs. It does **not** yet assemble the two-region key comparison (carry a bit in the control state, walk between regions, compare, return), nor the rule-table scan-and-match loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMMoveLeft` |
| **Entry 355 — universal-TM-table build: the two-cell comparison `compareAdjacent` (PROVED)** | The conceptual core of matching — the first machine to *read two tape cells and compare them* (the constant-pattern matchers 351–353 read only one operand from the tape). **`compareAdjacent s`** := `branchBit s (s+1) (s+2) ++ ((scanBit (s+1) (s+3) ++ checkBit true (s+3) (s+5) (s+6)) ++ (scanBit (s+2) (s+4) ++ checkBit false (s+4) (s+5) (s+6)))` — read cell `j` and carry its value (`branchBit`), advance (`scanBit`), check cell `j+1` equals the carried value (`checkBit`); reach the equal-state `s+5` iff they agree (`s+6` is the not-equal state). **`compareAdjacent_run_eq`** (PROVED) — if cells `j` and `j+1` hold the same symbol, `∃ tp', reachIn (toNTM (compareAdjacent s)) 3 (s, j, tp) (s+5, j+2, tp')`: the comparison runs three steps to the equal-state, head past both cells. Proof: `by_cases` on the carried bit; each arm chains `branchBit_run_true/false` (carry) + `scanBit_run_pres` (advance) + `checkBit_run_match` (compare, with cell `j+1`'s value transported through preservation and `heq`); the two control-flow arms live in one machine and are lifted into it by the non-interference law `reachIn_append_left`/`reachIn_append_right` (entry 347), then composed with the branch step via `reachIn_seq`. Honest scope: this is the carry-and-compare core, proved for the *equal ⇒ equal-state* direction on *adjacent* cells. It does **not** yet prove the not-equal direction, nor generalise to two *distant* regions (walking between them with a round-trip invariant), nor the rule-table loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCompareAdjacent` |
| **Entry 356 — universal-TM-table build: the not-equal branch of `compareAdjacent` (PROVED)** | Completes the two-cell equality decision: entry 355 handled equal ⇒ `s+5`; this handles differ ⇒ `s+6`. **`compareAdjacent_run_ne`** (PROVED) — if cells `j` and `j+1` differ (`tp.getD j false ≠ tp.getD (j+1) false`), then `∃ tp', reachIn (toNTM (compareAdjacent s)) 3 (s, j, tp) (s+6, j+2, tp')`: three steps to the not-equal state `s+6`. The proof mirrors `compareAdjacent_run_eq` but takes the *failing* `checkBit` branch (`checkBit_run_fail`): `by_cases` on the carried bit, the second cell's value derived as the opposite (via `h2 : … ≠ … := fun hc => hne (hb.trans hc.symm); simpa using h2`), and the two arms unified by `reachIn_append_left`/`reachIn_append_right` + `reachIn_seq`. With entry 355, `compareAdjacent` is now a *complete* equality decision: it reaches `s+5` exactly on a match and `s+6` exactly on a mismatch. Honest scope: complete for adjacent cells. It does **not** yet generalise to two *distant* regions (walking between them with a round-trip invariant), nor the rule-table loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCompareAdjacentNe` |
| **Entry 357 — universal-TM-table build: the fixed-distance walk `walkRightK` (PROVED)** | Entry-334's `walkRight` is a single-state self-loop (it stays in state `0`); to chain a walk *into* a later comparison we need one that ends in a *controlled distinct state*. **`walkRightK : ℕ → ℕ → TMachine`** — `0 ↦ []`, `k+1 ↦ scanBit s (s+1) ++ walkRightK k (s+1)`: one `scanBit` per cell, advancing the control state each step. **`walkRightK_run`** (PROVED) — `∃ tp', reachIn (toNTM (walkRightK k s)) k (s, h, tp) (s+k, h+k, tp') ∧ ∀ q, tp'.getD q false = tp.getD q false`: the walk runs `k` steps to state `s+k` at head `h+k`, preserving the tape. Proof: induction on `k` (generalizing `s, h, tp`), composing one `scanBit_run_pres` step with the inductive walk via `reachIn_seq`, with `convert ... using 1` bridging `k+1` vs `1+k` (not defeq, since `+` recurses on its second argument). Honest scope: the gap-traversal primitive ending in a controlled state — the piece a distant-region comparison chains between carrying a bit and comparing. It does **not** yet assemble the distant comparison (carry, walk, compare, round-trip invariant), nor the rule-table loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMWalkRightK` |
| **Entry 358 — universal-TM-table build: the distant-region comparison `compareDistant` (PROVED)** | The full-generality two-pointer match — `compareAdjacent` generalised to an arbitrary gap `m`. A finite-control machine compares cell `j` with the distant cell `j+m` by encoding cell `j` into the control state, traversing the gap on a per-value track, and comparing on arrival. **`compareDistant m s sT0 sF0 E N`** := `branchBit s sT0 sF0 ++ ((walkRightK m sT0 ++ checkBit true (sT0+m) E N) ++ (walkRightK m sF0 ++ checkBit false (sF0+m) E N))` — branch on cell `j` (carry), walk `m` on the matching track to region B, check cell `j+m` against the carried value, reach `E` on a match (`N` on a mismatch). **`compareDistant_run_eq`** (PROVED) — if cells `j` and `j+m` agree, `∃ tp', reachIn (toNTM (compareDistant m s sT0 sF0 E N)) (m+2) (s, j, tp) (E, j+m+1, tp')`: `m+2` steps to the equal-state `E`, head past region B. Proof: `by_cases` on the carried bit; `branchBit` (carry) + `walkRightK_run` (gap traversal, entry 357) + `checkBit_run_match` (compare, with cell `j+m`'s value transported through preservation and `heq`); the two tracks unified by `reachIn_append_left`/`reachIn_append_right` + `reachIn_seq`, with `convert ... using 1` bridging the `m+2` vs `1+(m+1)` count (the config closes by defeq). Honest scope: the distant equality test (equal ⇒ equal-state), the full-generality two-pointer comparison. It does **not** yet prove the not-equal direction at distance, nor the rule-table scan-and-match loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCompareDistant` |
| **Entry 359 — universal-TM-table build: the overwrite primitive `writeConst` (PROVED)** | Every machine so far *writes back what it read* (the scanners, checkers, and comparators are all tape-preserving). The *apply* phase — rewriting the encoded configuration with the matched rule's right-hand side — needs to *overwrite* a cell, so this brick adds that capability. **`writeConst b s s'`** := `[((s,true),(s',b,→)), ((s,false),(s',b,→))]` — read the cell (whatever it is), write the constant `b` in its place, move right, go to `s'`; *not* tape-preserving. **`writeConst_step`** (PROVED) — at `(s, j, tp)`, uniformly in the read symbol, it steps to `(s', j+1, writeAt tp j b)` (the head cell overwritten with `b`). **`writeConst_run`** (PROVED) — `reachIn (toNTM (writeConst b s s')) 1 (s, h, tp) (s', h+1, writeAt tp h b)`: the one-step overwrite run. The resulting cell content is `b` (and others unchanged) via `writeAt_getD` (entry 344). Honest scope: the overwrite atom — the first machine that changes a tape cell rather than restoring it. It does **not** yet copy a value between regions (carry-and-write, the dual of `compareDistant`), nor assemble the full apply, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMWriteConst` |
| **Entry 360 — universal-TM-table build: the inter-region bit copy `copyBit` (PROVED)** | The dual of `compareDistant` (entry 358): instead of reading a bit and *comparing* it against a distant cell, read it and *write* it into a distant cell. **`copyBit m s sT0 sF0 E`** := `branchBit s sT0 sF0 ++ ((walkRightK m sT0 ++ writeConst true (sT0+m) E) ++ (walkRightK m sF0 ++ writeConst false (sF0+m) E))` — carry cell `j` (`branchBit`), walk `m` cells to region B on the matching track (`walkRightK`), write the carried value at `j+m` (`writeConst`), converging to `E`. **`copyBit_run`** (PROVED) — `∃ tp', reachIn (toNTM (copyBit m s sT0 sF0 E)) (m+2) (s, j, tp) (E, j+m+1, tp') ∧ tp'.getD (j+m) false = tp.getD j false ∧ ∀ q, q ≠ j+m → tp'.getD q false = tp.getD q false`: the copy runs `m+2` steps to `E`, with cell `j+m` now holding cell `j`'s value and *every other cell unchanged* — a full specification. Proof: `by_cases` on the carried bit; `branchBit` (carry) + `walkRightK_run` (gap, entry 357) + `writeConst_run` (overwrite, entry 359, giving the exact tape `writeAt tp2 (j+m) b`); the content facts via `writeAt_getD` (entry 344, `if_pos`/`if_neg`) and accumulated preservation; tracks unified by the non-interference law. Honest scope: the inter-region bit copy — the apply's transplant operation, fully specified. It does **not** yet assemble the full apply (copy the matched rule's whole new state and symbol into the configuration), nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCopyBit` |
| **Entry 361 — universal-TM-table build: the constant block fill `fillConst` (PROVED)** | The write-side analog of `walkRightK` (entry 357): `writeConst` (entry 359) overwrites one cell, `fillConst` overwrites a whole block. **`fillConst (b : Bool) : ℕ → ℕ → TMachine`** — `0 ↦ []`, `L+1 ↦ writeConst b s (s+1) ++ fillConst b L (s+1)`: one `writeConst` per cell. **`fillConst_run`** (PROVED, full spec) — `∃ tp', reachIn (toNTM (fillConst b L s)) L (s, h, tp) (s+L, h+L, tp') ∧ (∀ i < L, tp'.getD (h+i) false = b) ∧ (∀ q < h, tp'.getD q false = tp.getD q false) ∧ (∀ q, h+L ≤ q → tp'.getD q false = tp.getD q false)`: runs `L` steps to state `s+L` at head `h+L`, with the block `[h, h+L)` set to `b` and every cell outside it unchanged. Proof: induction on `L` (generalizing `s, h, tp`), composing one `writeConst_run` step (giving the exact tape `writeAt tp h b`) with the inductive fill via `reachIn_seq`; the three content facts threaded via the inductive specs + `writeAt_getD` (`if_pos`/`if_neg`), with `convert ... using 1` bridging the count and the `s+(L+1)` vs `(s+1)+L` config (`Prod.mk.injEq` + `omega`). Honest scope: the constant block fill — overwriting a known-length region, fully specified. It does **not** yet assemble the full apply (transplant the matched rule's new state and symbol into the configuration, combining `copyBit`/`fillConst`), nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMFillConst` |
| **Entry 362 — universal-TM-table build: the fixed-distance left walk `walkLeftK` (PROVED)** | The left analog of `walkRightK` (entry 357) — the *return-trip* primitive: the apply's block copy must, after copying a source cell forward, walk the head back left to the next source cell. **`walkLeftK : ℕ → ℕ → TMachine`** — `0 ↦ []`, `k+1 ↦ moveLeft s (s+1) ++ walkLeftK k (s+1)`: one `moveLeft` per cell. **`walkLeftK_run`** (PROVED) — `∃ tp', reachIn (toNTM (walkLeftK k s)) k (s, h, tp) (s+k, h-k, tp') ∧ ∀ q, tp'.getD q false = tp.getD q false`: runs `k` steps to state `s+k` at head `h-k`, preserving the tape. Proof: induction on `k` (generalizing `s, h, tp`), composing one `moveLeft_run_pres` step with the inductive walk via `reachIn_seq`; `convert ... using 1` bridges the count and the `(s+1)+k` vs `s+(k+1)` / `(h-1)-k` vs `h-(k+1)` config (`omega` handles the `Nat` subtraction). Honest scope: the fixed-distance left walk, completing the `walkRightK`/`walkLeftK` motion pair. It does **not** yet assemble the block copy (copy a field cell-by-cell, `copyBit` forward then `walkLeftK` back), nor the full apply, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMWalkLeftK` |
| **Entry 363 — universal-TM-table build: the copy-and-return body `copyBitReturn` (PROVED)** | The first composite of the apply phase — the block-copy loop *body*. `copyBit` (entry 360) copies one cell across a gap but leaves the head past the destination; `copyBitReturn` adds the `walkLeftK` (entry 362) return so the head repositions for the next source cell. **`copyBitReturn m s sT0 sF0 E`** := `copyBit m s sT0 sF0 E ++ walkLeftK m E`. **`copyBitReturn_run`** (PROVED, full spec) — `∃ tp', reachIn (toNTM (copyBitReturn m s sT0 sF0 E)) (2*m+2) (s, j, tp) (E+m, j+1, tp') ∧ tp'.getD (j+m) false = tp.getD j false ∧ ∀ q, q ≠ j+m → tp'.getD q false = tp.getD q false`: runs `2m+2` steps to `(E+m, j+1, tp')`, with cell `j+m` now holding cell `j`'s value, every other cell unchanged, and the head back at the next source cell `j+1`. Proof: `copyBit_run` (copy + content) then `walkLeftK_run` (return, preserving the tape), composed by `reachIn_seq`, with `convert ... using 1` bridging the count `2m+2` vs `(m+2)+m` and the head `j+1` vs `(j+m+1)-m` (`omega`); the content facts transported through the `walkLeftK` preservation. The non-interference law (entry 347) means `reachIn_seq` needs no disjoint states. Honest scope: the copy-and-return body — one iteration of a block copy. It does **not** yet loop the body over a whole field, nor assemble the full apply, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCopyBitReturn` |
| **Entry 364 — universal-TM-table build: the single-base copy step `copyStep` (PROVED)** | `copyBitReturn` (entry 363) takes four state arguments; to *loop* it (chaining each iteration's exit into the next's entry) the internal states are fixed relative to a single base. **`copyStep m s`** := `copyBitReturn m s (s+1) (s+m+2) (s+2*m+3)` — carry tracks at `s+1`/`s+m+2`, converge at `s+2m+3`, return walk ending at `s+3m+3`. **`copyStep_run`** (PROVED, full spec) — `∃ tp', reachIn (toNTM (copyStep m s)) (2*m+2) (s, j, tp) (s+3*m+3, j+1, tp') ∧ tp'.getD (j+m) false = tp.getD j false ∧ ∀ q, q ≠ j+m → tp'.getD q false = tp.getD q false`: runs `2m+2` steps from the single entry state `s` to the single exit state `s+3m+3`, copying cell `j` to `j+m` and returning the head to `j+1`. Proof: `copyBitReturn_run` with the fixed states, rewriting its exit state `(s+2*m+3)+m` to `s+3*m+3` by `omega` (the machine unfolds definitionally, the count matches). Honest scope: the loopable copy unit (one entry, one exit). It does **not** yet loop over a field (the block copy, with its region-disjointness invariant), nor the full apply, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCopyStep` |
| **Entry 365 — universal-TM-table build: the block copy `copyBlock` (PROVED)** | Loops `copyStep` (entry 364) to transplant a whole field — the apply's new-state copy. **`copyBlock (m : ℕ) : ℕ → ℕ → TMachine`** — `0 ↦ []`, `L+1 ↦ copyStep m s ++ copyBlock m L (s + (3*m+3))` (state base shifts by `3m+3` per iteration). **`copyBlock_run`** (PROVED) — for **`L ≤ m`** (the region-disjointness invariant: destination `[j+m, j+m+L)` must not overlap source `[j, j+L)`, so each iteration reads the original source rather than an already-overwritten cell), `∃ tp', reachIn (toNTM (copyBlock m L s)) (L*(2*m+2)) (s, j, tp) (s + L*(3*m+3), j+L, tp') ∧ (∀ i < L, tp'.getD (j+m+i) false = tp.getD (j+i) false) ∧ (∀ q < j+m, tp'.getD q false = tp.getD q false)`: the destination block mirrors the source block, and every cell before the destination (including the whole source) is unchanged. Proof: induction on `L` (generalizing `s, j, tp`; the `L ≤ m` hypothesis auto-reverted), composing one `copyStep_run` with the inductive block via `reachIn_seq`; the `(L+1)*(2*m+2)` count is distributed by `add_mul`/`one_mul` *before* `omega` (which atomizes the nonlinear `L*(2*m+2)`); the copied-cells fact splits on `i = 0` (via the inductive "before" bound + `copyStep`'s target) vs `i = i'+1` (via the inductive copy + `copyStep`'s preservation, using `(j+1)+i' ≠ j+m` from `i' < L ≤ m-1`). Honest scope: the block copy — transplanting a contiguous field, with its region-disjointness invariant. It does **not** yet assemble the full apply (combine block copies and `writeConst`s to rewrite the configuration per the matched rule), nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCopyBlock` |
| **Entry 366 — universal-TM-table build: the head-neutral block copy `copyBlockReturn` (PROVED)** | `copyBlock` (entry 365) leaves the head advanced past the source (at `j+L`); since the apply performs several field operations, make the field copy *head-neutral* (the field-level analog of `copyBitReturn`, entry 363). **`copyBlockReturn m L s`** := `copyBlock m L s ++ walkLeftK L (s + L*(3*m+3))`. **`copyBlockReturn_run`** (PROVED) — for `L ≤ m`, `∃ tp', reachIn (toNTM (copyBlockReturn m L s)) (L*(2*m+2)+L) (s, j, tp) (s + L*(3*m+3) + L, j, tp') ∧ (∀ i < L, tp'.getD (j+m+i) false = tp.getD (j+i) false) ∧ (∀ q < j+m, tp'.getD q false = tp.getD q false)`: the destination block mirrors the source, everything before it is unchanged, and the head is back at the source start `j`. Proof: `copyBlock_run` then `walkLeftK_run` (return of `L`, preserving the tape), composed by `reachIn_seq`, with `convert ... using 1` for the head `(j+L)-L` vs `j` and the content transported through the `walkLeftK` preservation. Honest scope: the head-neutral field transplant, so apply-phase field operations compose from a common base. It does **not** yet assemble the full apply (rewrite the configuration's state and symbol per the matched rule), nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCopyBlockReturn` |
| **Entry 367 — universal-TM-table build: the relocatable record scanner `scanTransFrom` (PROVED)** | `scanTrans` (entry 350) scans one transition but with fixed states `0..5` at head `0`; the rule-table loop scans *successive* transitions, each at a different head and continuing the control state. **`scanTransFrom base`** := `scanNatFrom base (base+1) ++ scanBit (base+1) (base+2) ++ scanNatFrom (base+2) (base+3) ++ scanBit (base+3) (base+4) ++ scanNatFrom (base+4) (base+5)` (states `base..base+5`). **`scanTransFrom_run`** (PROVED) — `∃ tp', reachIn (toNTM (scanTransFrom base)) (rs+ws+mv+5) (base, pre.length, pre ++ encodeTransBits t ++ rest) (base+5, pre.length + (rs+ws+mv+5), tp')`: from state `base` at the start of a record (offset `pre.length`), scans the whole `encodeTransBits t` to state `base+5`, head past the record. Proof mirrors `scanTrans` exactly with state `0 → base` and head `0 → pre.length`, the `field_content` calls given the prefix `pre` (so field `i` sits at offset `pre.length + δᵢ`), composed by `reachIn_seq` with `convert ... using 1`. (A missing `open ... (TMachine)` initially cascaded into bogus downstream errors; adding it cleared them.) Honest scope: the relocatable record scanner, the loopable unit of the rule-table traversal. It does **not** yet loop over the whole table, nor the rule-table scan-and-match, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanTransFrom` |
| **Entry 368 — universal-TM-table build: the record scan preserves the tape (PROVED)** | `scanTransFrom_run` (entry 367) discards the fact that the scan is non-destructive; looping the scanner (the rule-table traversal) and interleaving it with key comparisons both need to *re-read* the tape after a scan. Every field scanner the record scanner is built from (`scanNatFrom_run_pres`, `scanBit_run_pres`) writes back what it reads, so the composed scan preserves every cell. **`scanTransFrom_run_pres`** (PROVED) — `scanTransFrom_run` (entry 367) *plus* `∀ q, tp'.getD q false = (pre ++ encodeTransBits t ++ rest).getD q false`: the record scan additionally preserving the tape. Proof: identical to `scanTransFrom_run` (same field runs, `reachIn_seq` chain, `convert`), with the final result tuple gaining the preservation conjunct — the per-field preservations `pres5 ∘ pres4 ∘ pres3 ∘ pres2 ∘ pres1` composed. Honest scope: strengthens the relocatable record scan with tape preservation, the property the rule-table loop and scan-and-match need to re-read across scans. It does **not** yet loop the scanner over the whole table, nor the rule-table scan-and-match, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanTransFromPres` |
| **Entry 369 — universal-TM-table build: in-bounds write-back is the identity `writeAt_id_of_lt` (PROVED)** | The scanners preserve the tape *content* (`getD` pointwise), but looping a scanner over the rule table needs the post-scan tape to equal the original *as a list* (so the inductive step can re-run the scanner on it). The foundation is that writing back the symbol a cell already holds, at an in-bounds position, leaves the tape exactly unchanged. **`writeAt_id_of_lt`** (PROVED) — `p < tape.length → writeAt tape p (tape.getD p false) = tape`. Proof: unfold `writeAt = (tape ++ replicate (p+1-len) false).set p w`; `p < len` makes the padding empty (`replicate_zero`, `append_nil`), reducing to `tape.set p w`; `w = tape.getD p false = tape[p]` (via `getElem?_eq_getElem`), and `tape.set p tape[p] = tape` by `List.set_getElem_self`. Honest scope: the in-bounds write-back identity — the foundation for list-level (not just content-level) scan preservation. It does **not** itself upgrade the scanners to list-preservation (that threads an in-bounds invariant through each step), nor build the table loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMWriteAtId` |
| **Entry 370 — universal-TM-table build: list-preserving field scanners (PROVED)** | Route A executed: by `writeAt_id_of_lt` (entry 369), a scanner that writes back exactly the symbol it reads leaves the tape *unchanged as a list* when the scanned positions are in bounds — not merely `getD`-equal. **`scanBit_run_eq`** (PROVED) — `h < tp.length → reachIn (toNTM (scanBit s s')) 1 (s, h, tp) (s', h+1, tp)`: one bit scan returning the *identical* tape (proof: `scanBit_step` + `rw writeAt_id_of_lt`). **`scanNatFrom_run_eq`** (PROVED) — given the content hypotheses and `h + n < tp.length` (the whole field, separator included, in bounds), `reachIn (toNTM (scanNatFrom s s')) (n+1) (s, h, tp) (s', h+n+1, tp)`: the nat-field scan returning the *identical* tape. Proof: a scan invariant (induction on `j`, each `scanNatFrom_step_true` rewriting `writeAt tp (h+j) true = tp` via `writeAt_id_of_lt` with `h+j < tp.length`), then the closing `scanNatFrom_step_false` rewriting `writeAt tp (h+n) false = tp`. Honest scope: the list-preserving field scanners (the scan returns the same list under an in-bounds hypothesis). They do **not** yet compose into a list-preserving record scanner, nor the table loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanListPres` |
| **Entry 371 — universal-TM-table build: the list-preserving record scanner `scanTransFrom_run_eq` (PROVED)** | Composes the list-preserving field scanners (entry 370) into a record scan that returns the tape *exactly*. **`scanTransFrom_run_eq`** (PROVED) — `reachIn (toNTM (scanTransFrom base)) (rs+ws+mv+5) (base, pre.length, pre ++ encodeTransBits t ++ rest) (base+5, pre.length + (rs+ws+mv+5), pre ++ encodeTransBits t ++ rest)`: the record scan returns the *identical* tape (no existential), head past the record. Proof: a length bound `pre.length + (rs+ws+mv+5) ≤ T.length` (so every field is in bounds, discharged by `omega`); `field_content` for the nat-field content; `scanNatFrom_run_eq`/`scanBit_run_eq` (entry 370) for the five fields — *all on the same `T`*, so there is no preservation-transport (unlike entries 367/368); composed by `reachIn_seq`. Honest scope: the list-preserving record scanner — the record scan leaves the tape exactly unchanged, the property the rule-table loop needs. It does **not** yet loop over the whole table, nor the rule-table scan-and-match, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanTransFromEq` |
| **Entry 372 — universal-TM-table build: the rule-table scan `scanTable` (PROVED)** | Loops the list-preserving record scanner (entry 371) over the whole transition list — the first traversal of the encoded rule table. **`encodeTransBits_length`** (PROVED) — `(encodeTransBits t).length = t.1.1 + t.2.1 + t.2.2.2.val + 5`. **`scanTable base : List TMTrans → TMachine`** — `[] ↦ []`, `t :: ts ↦ scanTransFrom base ++ scanTable (base+5) ts` (the machine is the same for any transition, so `t` is unused). **`scanTable_run`** (PROVED) — `reachIn (toNTM (scanTable base Ms)) (Ms.flatMap encodeTransBits).length (base, pre.length, pre ++ Ms.flatMap encodeTransBits ++ rest) (base + 5*Ms.length, pre.length + (Ms.flatMap encodeTransBits).length, pre ++ Ms.flatMap encodeTransBits ++ rest)`: scans the whole encoded transition list, returning the *identical* tape, head past the table. Proof: induction on `Ms` (generalizing `base, pre`); the cons step composes one `scanTransFrom_run_eq` (its tape left-associated to match) with the inductive `scanTable` run (its head aligned via `encodeTransBits_length`) by `reachIn_seq` — list-preservation means the two tapes are *exactly equal* (no transport), so the loop is a clean recursion; the goal is normalised by `simp` (`flatMap_cons`, `append_assoc`, lengths) and the count/state/head bridged by `omega`. Honest scope: the rule-table scan — traversing the whole encoded transition list list-preservingly. It does **not** yet *match* (compare each transition's key against the configuration), nor apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMScanTable` |
| **Entry 373 — universal-TM-table build: list-preserving branch/check runs (PROVED)** | Just as the table scan needed *list*-preserving scanners (entries 370–372), the rule-table scan-and-match needs list-preserving comparison primitives (the key comparison reads the tape, branches, and must leave it intact for the apply and the next comparison). `branchBit` and `checkBit` write back the symbol they read, so by `writeAt_id_of_lt` (entry 369) each is the identity on the list at an in-bounds head. **`branchBit_run_true_eq` / `branchBit_run_false_eq`** (PROVED) — at an in-bounds head, the read symbol forces the branch leaving the tape *identical* (`(sTrue, h, tp)` / `(sFalse, h, tp)`). **`checkBit_run_match_eq` / `checkBit_run_fail_eq`** (PROVED) — `h < tp.length`: a cell equal to `b` reaches `(sCont, h+1, tp)`, a cell equal to `!b` reaches `(sFail, h+1, tp)`, the tape *identical*. Each proof is the corresponding `_step` lemma with `writeAt tp h v = tp` rewritten via `writeAt_id_of_lt`. Honest scope: the list-preserving decision/comparison steps (the read-side mirror of entry 370). They do **not** yet assemble a list-preserving key comparison, nor the rule-table scan-and-match, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMBranchCheckEq` |
| **Entry 374 — universal-TM-table build: the list-preserving fixed-distance walk `walkRightK_run_eq` (PROVED)** | `walkRightK_run` (entry 357) only `getD`-preserves; the list-preserving `compareDistant` needs the walk to leave the tape *identical*. Since `walkRightK` is a fold of `scanBit`s and `scanBit_run_eq` (entry 370) is list-identity in bounds, the whole walk is. **`walkRightK_run_eq`** (PROVED) — `h + k ≤ tp.length → reachIn (toNTM (walkRightK k s)) k (s, h, tp) (s+k, h+k, tp)`: the fixed-distance right walk returning the *identical* tape. Proof: induction on `k`, composing `scanBit_run_eq` with the inductive walk via `reachIn_seq`. Honest scope: the list-preserving fixed-distance walk. It does **not** yet compose into a list-preserving `compareDistant`, nor the key comparison, nor the rule-table scan-and-match — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMWalkRightKEq` |
| **Entry 375 — universal-TM-table build: the list-preserving distant comparison (PROVED)** | `compareDistant` (entry 358) only `getD`-preserves; the rule-table scan-and-match needs the comparison to leave the tape *identical* (so the apply re-reads the original). Its three components (`branchBit` carry, `walkRightK` walk, `checkBit` compare) all now have list-preserving runs (entries 373/374) and run on the *same* tape. **`compareDistant_run_eq`** (PROVED) — cells `j`, `j+m` agree and `j+m < tp.length` ⇒ `reachIn (toNTM (compareDistant m s sT0 sF0 E N)) (m+2) (s, j, tp) (E, j+m+1, tp)`: reaches `E` leaving the tape *identical*. **`compareDistant_run_ne`** (PROVED) — cells differ and `j+m < tp.length` ⇒ reaches `(N, j+m+1, tp)`, tape identical. Each proof: `by_cases` on the carried bit; `branchBit_run_*_eq` + `walkRightK_run_eq` + `checkBit_run_match_eq`/`checkBit_run_fail_eq`, all on the same `tp`; the in-bounds obligations all `omega` from `j+m < tp.length`. Honest scope: the list-preserving distant comparison, both outcomes. It does **not** yet assemble the multi-bit key comparison (needs the concrete configuration layout), nor the rule-table scan-and-match loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCompareDistantEq` |
| **Entry 376 — universal-TM-table build: list-preserving left motions (PROVED)** | The comparison loop (comparing two unary fields cell by cell) returns the head left between iterations, and the return must leave the tape identical. `moveLeft` writes back what it reads, so it is the identity in bounds (`writeAt_id_of_lt`, entry 369), and `walkLeftK` (a fold of `moveLeft`s) inherits this. **`moveLeft_run_eq`** (PROVED) — `h < tp.length → reachIn (toNTM (moveLeft s s')) 1 (s, h, tp) (s', h-1, tp)`: one leftward step leaving the tape *identical*. **`walkLeftK_run_eq`** (PROVED) — `h < tp.length → reachIn (toNTM (walkLeftK k s)) k (s, h, tp) (s+k, h-k, tp)`: the fixed-distance left walk leaving the tape *identical* (all written positions `≤ h < tp.length`). Proofs mirror the rightward versions (entries 370/374). Honest scope: the list-preserving left motions, completing the list-preserving motion set (right by `walkRightK_run_eq`, left here). They do **not** yet assemble the compare-and-return loop body, nor the unary-field equality comparison, nor the rule-table scan-and-match — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMWalkLeftKEq` |
| **Entry 377 — universal-TM-table build: the compare-and-return body `compareBitReturn` (PROVED)** | Comparing two unary fields cell by cell needs a loop body that compares one pair and, *on a match*, returns the head to the next pair (*on a mismatch* halting at the not-equal state). **`compareBitReturn m s sT0 sF0 E N`** := `compareDistant m s sT0 sF0 E N ++ walkLeftK m E`. **`compareBitReturn_run_eq`** (PROVED) — equal cells and `j+m+1 < tp.length` ⇒ `reachIn ... (2*m+2) (s, j, tp) (E+m, j+1, tp)`: match, head returned to the next source cell, tape identical (`compareDistant_run_eq` then `walkLeftK_run_eq`, composed by `reachIn_seq`). **`compareBitReturn_run_ne`** (PROVED) — differing cells and `j+m < tp.length` ⇒ `reachIn ... (m+2) (s, j, tp) (N, j+m+1, tp)`: mismatch, halts at `N`, tape identical — the return walk, entered only at state `E`, is never run, so `compareDistant_run_ne` lifted via `reachIn_append_left` gives the result directly. Honest scope: the compare-and-return body, one iteration of a cell-by-cell field comparison. It does **not** yet loop over a field (the unary-field equality comparison, with its three-way continue/match/mismatch control flow), nor the rule-table scan-and-match — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCompareBitReturn` |
| **Entry 378 — universal-TM-table build: the three-way comparison step `compareStep3` (PROVED)** | Comparing two unary fields needs a *three-way* decision per cell pair: continue (both `true`), match-done (both `false` = aligned separators), or no-match (differ). The insight: `compareDistant`'s carry already splits on cell `j`'s value (true-track / false-track), so giving the two tracks *distinct* match-exits yields the three outcomes. **`compareStep3 m s sT0 sF0 cont matchSt noMatch`** := `branchBit s sT0 sF0 ++ ((walkRightK m sT0 ++ checkBit true (sT0+m) cont noMatch) ++ (walkRightK m sF0 ++ checkBit false (sF0+m) matchSt noMatch))`. **`compareStep3_run_cont`** (both `true` ⇒ `cont`), **`compareStep3_run_match`** (both `false` ⇒ `matchSt`), **`compareStep3_run_ne`** (differ ⇒ `noMatch`) — all PROVED, each `m+2` steps to the respective state at `j+m+1`, tape identical. Proofs are three instances of the entry-375 pattern (`branchBit_run_*_eq` + `walkRightK_run_eq` + `checkBit_run_match_eq`/`checkBit_run_fail_eq`, true-track lifted by `append_left` / false-track by `append_right`). Honest scope: the three-way comparison step — the loop body of a unary-field equality test. It does **not** yet loop it (the unary-field equality comparison), nor the rule-table scan-and-match — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCompareStep3` |
| **Entry 379 — universal-TM-table build: the unary-field equality comparison `compareUnary` (PROVED, match direction)** | The first *cyclic, data-dependent* loop of the build: loops the three-way step `compareStep3` (entry 378) into a comparison of two whole unary fields. **`compareUnary m sT0 sF0 sCont matchSt noMatch`** := `compareStep3 m (sCont+m) sT0 sF0 sCont matchSt noMatch ++ walkLeftK m sCont` — *cyclic*: the loop head is `sCont+m` (exactly where the return walk `walkLeftK m sCont` ends), so `compareStep3`'s `cont` exit, followed by the return, lands back at the loop head (one iteration per `true`-pair); the `matchSt` exit (aligned separators) leaves the loop "equal". State overlaps are harmless since `reachIn` is existential. **`compareUnary_run_match`** (PROVED) — two *equal* unary fields of common length `L` (region A at `j`, region B at `j+m`) drive the machine from the loop head `(sCont+m, j, tp)` to `(matchSt, j+L+m+1, tp)`, tape identical (`∃` step count). Proof by induction on `L`: the base (`L=0`) is `compareStep3_run_match` on the aligned separators (lifted by `reachIn_append_left`); the step does one `compareStep3_run_cont` + `walkLeftK_run_eq` return (composed by `reachIn_add` into one loop iteration `(sCont+m, j) → (sCont+m, j+1)`) then the inductive hypothesis at `j+1` (hypotheses shifted by an index rewrite). Honest scope: the unary-field equality comparison, *match* direction — the first genuine data-dependent loop. It does **not** yet prove the mismatch/length-difference directions, nor compose with the symbol-bit compare into the full key comparison, nor the rule-table scan-and-match — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCompareUnary` |
| **Entry 380 — universal-TM-table build: the unary comparison mismatch direction `compareUnary_run_ne` (PROVED)** | Complements entry 379's match outcome: if the two unary fields agree on a `true`-prefix of length `k` but the cells *differ* at position `k` (a genuine disagreement, including the unequal-length case where one field's separator meets the other's `true`), `compareUnary` reaches `noMatch`. **`compareUnary_run_ne`** (PROVED) — `(∀ i < k, both true at j+i, j+m+i)` and `tp.getD (j+k) false ≠ tp.getD (j+m+k) false` ⇒ `∃ N, reachIn (toNTM (compareUnary …)) N (sCont+m, j, tp) (noMatch, j+k+m+1, tp)`, tape identical. Proof: the same cyclic structure as the match case (induction on the agreeing-prefix length `k`), but the base step is a `compareStep3` *no-match* (the differing pair) instead of a *match*. With entry 379, `compareUnary` now decides unary-field equality: `matchSt` on equal fields, `noMatch` on a prefix-agreeing disagreement. Honest scope: the mismatch direction. It does **not** yet compose with the symbol-bit compare into the full key comparison, nor the rule-table scan-and-match — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMCompareUnaryNe` |
| **Entry 381 — universal-TM-table build: the full key match `keyMatch` (PROVED, match direction)** | A transition's key is `(state, readSym)` — a unary state field then a symbol bit (`encodeTransBits` layout). This composes the unary state-field equality `compareUnary` (entry 379) with the symbol-bit comparison `compareDistant` (entry 375). **`keyMatch m sT0 sF0 sCont matchSt noMatch bT0 bF0 fullMatch`** := `compareUnary … ++ walkLeftK m matchSt ++ compareDistant m (matchSt+m) bT0 bF0 fullMatch noMatch`. **`keyMatch_run_match`** (PROVED) — equal state fields (length `L`, config at `j`, rule at `j+m`) *and* equal symbol bits (config `j+L+1`, rule `(j+L+1)+m`) ⇒ `∃ N, reachIn … N (sCont+m, j, tp) (fullMatch, (j+L+1)+m+1, tp)`, tape identical. The proof exploits the layout: after the state compare the head lands at `j+L+m+1` — exactly the rule's symbol, `m` cells right of the config symbol — so a fixed `walkLeftK m` reposition lands on the config symbol and `compareDistant m` compares the two; the three phases are composed by `reachIn_seq` (the data-dependent mid configurations align), with all in-bounds obligations from `j+L+m+1 < tp.length`. Honest scope: the full key match, *match* direction. It does **not** yet handle the no-match directions, nor the rule-table scan-and-match loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMKeyMatch` |
| **Entry 382 — universal-TM-table build: the key match no-match directions (PROVED)** | The two ways a rule's key fails to match the configuration, both reaching `noMatch`. **`keyMatch_run_ne_state`** (PROVED) — state fields differ ⇒ `compareUnary` reaches `noMatch` directly (the symbol phase, entered only at `matchSt`, is never run; lifted into `keyMatch` by `reachIn_append_left` twice). **`keyMatch_run_ne_sym`** (PROVED) — state fields equal but symbol bits differ ⇒ `compareUnary` reaches `matchSt`, the head repositions (`walkLeftK`), and `compareDistant_run_ne` reaches `noMatch` (same three-phase `reachIn_seq` composition as `keyMatch_run_match`, with the symbol-compare's no-match branch). With entry 381, `keyMatch` is now a complete per-rule decision: `fullMatch` iff the rule's key equals the configuration's, `noMatch` otherwise. Honest scope: completes the per-rule key decision. It does **not** yet assemble the rule-table scan-and-match loop (iterating the key match over the rule list), nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTMKeyMatchNe` |
| **Entry 383 — universal-TM-table build: the 3-symbol (marker) tape model (PROVED)** | The rule-table loop must compare the configuration's key against *each* rule's key, but successive rules sit at *varying* distance — which the fixed-gap, 2-symbol (`Bool`) machines of entries 344–382 cannot do (one machine hardcodes one gap; a `Bool` tape has no spare *marker* to cross off compared cells). The standard fix is a richer alphabet. This brick founds a **3-symbol tape model** — `Sym3 = O/I/M` (blank/one/marker) — reusing the generic `NTM`/`reachIn` layer, with the same machine shape as the `Bool` model: **`CConfig3`**, **`TMTrans3`**, **`readSym3`**, **`writeAt3`**, **`applyTrans3`**, **`concreteStep3`**, **`toNTM3`** (its `init` maps the `Bool` input `true ↦ I`, `false ↦ O`). **`writeAt3_getD`** (PROVED) — a write changes only the written cell. **`writeAt3_id_of_lt`** (PROVED) — an in-bounds write-back is the list identity (the basis for list-preserving 3-symbol scans). Both are the `Bool` proofs (entries 344/369) ported intact with default `O`. The marker `M` will let a comparison cross off cells and shuttle between two regions at arbitrary distance. Honest scope: founds the 3-symbol model; the 39-entry `Bool` library (344–382) stays, this being a parallel track for the loop. It does **not** yet build any marker comparison, nor the rule-table loop, nor the apply — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Sym` |
| **Entry 384 — universal-TM-table build: composition over the 3-symbol model (PROVED)** | The wiring law `reachIn_seq` and the non-interference lemmas (entry 347) are stated for the `Bool` step relation; the marker-based machines are over the 3-symbol model (entry 383), so they need the same lemmas for `concreteStep3`. **`concreteStep3_append_left` / `concreteStep3_append_right`** (PROVED) — a step of `M₁` (resp. `M₂`) is a step of `M₁ ++ M₂` (`concreteStep3` is existential over rules, so no disjointness needed). **`reachIn_toNTM3_mono`** (PROVED) — step monotonicity lifts to reachability. **`reachIn_append_left3` / `reachIn_append_right3`** (PROVED) — a sub-table run lifts to the union. **`reachIn_seq3`** (PROVED) — sequential composition inside the union machine. The proofs are identical to entry 347 with `toNTM → toNTM3`, `concreteStep → concreteStep3`. Honest scope: ports the composition lemmas to the 3-symbol model. It does **not** yet build any marker comparison, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Compose` |
| **Entry 385 — universal-TM-table build: marker write and 3-symbol right move (PROVED)** | The first marker-track primitives over the 3-symbol model (entry 383). **`writeMark3 s s'`** := `[((s,O),(s',M,→)), ((s,I),(s',M,→)), ((s,M),(s',M,→))]` — overwrite the head cell with the marker `M` (the returnable anchor of the varying-distance comparison) and move right. **`writeMark3_step`** / **`writeMark3_run`** (PROVED) — `(s,j,tp) → (s', j+1, writeAt3 tp j M)` (case on the read symbol, pick the matching rule, all write `M`). **`moveRight3 s s'`** := writes back the read symbol and moves right (the Sym3 analog of `scanBit`). **`moveRight3_step`** (PROVED) and **`moveRight3_run_eq`** (PROVED) — `j < tp.length → reachIn (toNTM3 (moveRight3 s s')) 1 (s, j, tp) (s', j+1, tp)`: one rightward step leaving the tape *identical* (via `writeAt3_id_of_lt`). Honest scope: the marker anchor write and the preserving right shuttle. They do **not** yet assemble the marker-based comparison, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Move` |
| **Entry 386 — universal-TM-table build: left move and branch-on-marker (PROVED)** | Two more marker-track primitives: a tape-preserving left shuttle and a branch on the marker (the latter makes the varying distance irrelevant — a seek loop reads the cell and routes to "found" if it is `M` or "keep going" otherwise, so the head finds the anchor regardless of distance). **`moveLeft3 s s'`** — write back, move left. **`moveLeft3_run_eq`** (PROVED) — `j < tp.length → reachIn (toNTM3 (moveLeft3 s s')) 1 (s, j, tp) (s', j-1, tp)`, tape identical. **`branchMark3 s sFound sCont`** — at the head, *stay*; go to `sFound` if the cell is `M`, else `sCont`. **`branchMark3_run_mark`** (PROVED) — `readSym3 = M` and in bounds ⇒ `(sFound, j, tp)`. **`branchMark3_run_notmark`** (PROVED) — `readSym3 ≠ M` and in bounds ⇒ `(sCont, j, tp)` (case on the symbol; the `M` case contradicts the hypothesis). Both tape identical. Honest scope: the left shuttle and the marker branch — the components of a distance-agnostic seek. They do **not** yet build the seek loop, nor the marker comparison, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Mark` |
| **Entry 387 — universal-TM-table build: the seek-to-marker loop `seekMarkLeft` (PROVED)** | Assembles the marker branch (`branchMark3`) and the left shuttle (`moveLeft3`, entry 386) into the *distance-agnostic seek*: a cyclic machine that walks left until it reaches the marker `M`, *no matter how far*. This is the construction that makes the *varying gap* of the rule-loop irrelevant. **`seekMarkLeft s found cont`** := `branchMark3 s found cont ++ moveLeft3 cont s` — cyclic: the loop head `s` reads the cell; on the marker it exits to `found`, otherwise it moves one cell left (`moveLeft3 cont s`) and loops back to `s`. **`seekMarkLeft_run`** (PROVED) — if the marker is at position `p` and the `d` cells `p+1 … p+d` above it are all non-marker, the machine drives from `(s, p+d, tp)` to `(found, p, tp)` (`∃` step count), tape identical — by induction on the distance `d` (base = the marker found at `p`; step = one not-found iteration `(s, p+d+1) → (s, p+d)` then the IH). Honest scope: the seek-to-marker loop, the second cyclic-loop pattern, now over the 3-symbol model. It does **not** yet build the marker two-pointer comparison, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Seek` |
| **Entry 388 — universal-TM-table build: the rightward seek-to-marker `seekMarkRight` (PROVED)** | The mirror of `seekMarkLeft` (entry 387): a cyclic machine that walks *right* until it reaches the marker `M`, no matter how far (the marker comparison shuttles in both directions). **`seekMarkRight s found cont`** := `branchMark3 s found cont ++ moveRight3 cont s` (cyclic: `cont` returns to the loop head). **`seekMarkRight_run`** (PROVED) — if the marker is at `h+d` and the `d` cells `h … h+d-1` below it are non-marker, the machine drives from `(s, h, tp)` to `(found, h+d, tp)` (`∃` step count), tape identical — by induction on `d` (generalizing `h`, since the marker position `h+d` is `d`-dependent). Honest scope: the rightward seek, completing the distance-agnostic seek pair. It does **not** yet build the marker two-pointer comparison, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3SeekR` |
| **Entry 389 — universal-TM-table build: anchor-and-carry `markCarry3` (PROVED)** | The marker comparison begins each cell-pair by *anchoring* one operand: read the cell's bit, remember its value in the control state, and overwrite the cell with the marker `M`. **`markCarry3 s sO sI sM`** := `[((s,O),(sO,M,stay)), ((s,I),(sI,M,stay)), ((s,M),(sM,M,stay))]` — write `M`, *stay*, branch the control state on the original value (`sO`/`sI`/`sM`); not tape-preserving (the `O`/`I` value lives in the state, to be restored later). **`markCarry3_run_O`** (PROVED) — cell `O` ⇒ `reachIn (toNTM3 (markCarry3 s sO sI sM)) 1 (s, j, tp) (sO, j, writeAt3 tp j M)` (anchor laid, `O` remembered in `sO`). **`markCarry3_run_I`** (PROVED) — cell `I` ⇒ reaches `(sI, j, writeAt3 tp j M)`. Honest scope: the anchor-and-carry step. It does **not** yet shuttle to the other operand, compare, restore, or loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MarkCarry` |
| **Entry 390 — universal-TM-table build: the restore primitive `unmark3` (PROVED)** | The dual of `markCarry3` (entry 389): after anchoring a cell with `M`, carrying its bit, and shuttling away to compare, the machine must come back and *restore* the cell to its original value. **`unmark3 s s' w`** := `[((s,O),(s',w,stay)), ((s,I),(s',w,stay)), ((s,M),(s',w,stay))]` — overwrite the head cell with `w` (the carried value, supplied by the caller from which carry-track it is on) and stay. **`unmark3_step`** (PROVED) — at `(s, j, tp)` writes `w`: `(s', j, writeAt3 tp j w)`. **`unmark3_run`** (PROVED) — `reachIn (toNTM3 (unmark3 s s' w)) 1 (s, j, tp) (s', j, writeAt3 tp j w)`. Generalises `writeMark3` (write any `w`, stay). Honest scope: the restore phase. It does **not** yet assemble the shuttle-and-compare loop, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Unmark` |
| **Entry 391 — universal-TM-table build: double-write collapse `writeAt3_writeAt3` (PROVED)** | The marker comparison anchors a cell with `M` (overwriting its `O`/`I`) and later restores it; reasoning about that restore needs that writing twice at the *same* cell keeps only the last write. **`writeAt3_writeAt3`** (PROVED) — `writeAt3 (writeAt3 tape p a) p b = writeAt3 tape p b`. Proof: the outer write's padding is empty (the inner write already made the list reach `p+1`), so it reduces to `((tape ++ pad).set p a).set p b`, which `List.set_set` collapses to `(tape ++ pad).set p b = writeAt3 tape p b` (the paddings coincide). So anchor-then-restore (write `M`, then the original value) collapses to a single write of the original value — and if the original is already there, it is the identity (`writeAt3_id_of_lt`). Honest scope: the double-write collapse, the algebraic basis of the restore phase. It does **not** yet assemble the shuttle-and-compare loop, nor the rule-table loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3WriteWrite` |
| **Entry 392 — universal-TM-table build: the 3-symbol encoding `encodeNatBits3` (PROVED)** | Per the chosen path (migrate the encoding + `EmitsEncodedStep` to the `Sym3` marker model, then build the rule-loop/apply), the migration begins with the encoding. **`encodeNatBits3 n`** := `List.replicate n I ++ [O]` (a nat is `n` ones then a zero/separator). **`encodeNatBits3_length`** (PROVED) — `= n + 1`. **`encNat3_getD_lt`** (PROVED) — `j < n → (encodeNatBits3 n ++ rest).getD j O = I` (the ones). **`encNat3_getD_eq`** (PROVED) — the separator at `n` reads `O`. The `Sym3` ports of entries 338/345. Honest scope: founds the 3-symbol encoding, the basis for `Sym3` scanners and ultimately the `Sym3` universal machine. It does **not** yet build any `Sym3` scanner, nor the rule-loop, nor `EmitsEncodedStep` — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Encode` |
| **Entry 393 — universal-TM-table build: the 3-symbol transition encoding `encodeTransBits3` (PROVED)** | The simulated machine is a *Bool* TM, while the universal machine works on a *Sym3* tape (for markers), so the encoding maps a Bool transition onto `Sym3`: nat fields as `I`-runs (entry 392), symbol bits via `boolToSym3` (`true ↦ I`, `false ↦ O`). **`boolToSym3 b`** := `if b then I else O`. **`encodeTransBits3 t`** := `encodeNatBits3 (state) ++ boolToSym3 (read) :: (encodeNatBits3 (state') ++ boolToSym3 (write) :: encodeNatBits3 (move))`. **`encodeTransBits3_length`** (PROVED) — `= t.1.1 + t.2.1 + t.2.2.2.val + 5` (same as the Bool encoding). The `Sym3` port of entries 338/372. Honest scope: ports the transition encoding to the marker alphabet. It does **not** yet build any `Sym3` scanner, nor the rule-loop, nor `EmitsEncodedStep` — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3EncTrans` |
| **Entry 394 — universal-TM-table build: the 3-symbol machine encoding `encodeMachineBits3` (PROVED)** | The `Sym3` port of `encodeMachineBits` (entry 338): a machine `M` is encoded as its length (an `I`-run + separator) then its transitions concatenated. **`encodeMachineBits3 M`** := `encodeNatBits3 M.length ++ M.flatMap encodeTransBits3`. **`encodeMachineBits3_length`** (PROVED) — `= (M.length + 1) + (M.flatMap encodeTransBits3).length`. Honest scope: ports the machine encoding to the marker alphabet, completing the encoding layer's structure. It does **not** yet build any `Sym3` scanner, nor the rule-loop, nor `EmitsEncodedStep` — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3EncMachine` |
| **Entry 395 — universal-TM-table build: the 3-symbol field scanners `scanNatFrom3`/`scanBit3` (PROVED)** | The `Sym3` ports of the field scanners (entries 346/349) and their *list-preserving* runs (entry 370): the universal machine scans the marker-tape encoding (`I`-runs for nats, single cells for symbols), writing back what it reads so the tape is left identical in bounds (`writeAt3_id_of_lt`). **`scanNatFrom3_run_eq`** (PROVED) — content hypotheses + `h+n < tp.length` ⇒ `reachIn (n+1) (s,h,tp) (s',h+n+1,tp)`, tape identical. **`scanBit3_run_eq`** (PROVED) — `j < tp.length ⇒ reachIn 1 (s,j,tp) (s',j+1,tp)`, tape identical. Honest scope: ports the field scanners to the marker alphabet. It does **not** yet compose a record scanner nor the rule-loop — fragment by verified fragment, **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Scan` |
| **Entry 396 — universal-TM-table build: the 3-symbol field-content lemma `field_content3` (PROVED)** | The `Sym3` port of entry 350's tape-content helper. To run the field scanners on a record embedded in a larger tape, the rule-loop must know what the cells of an embedded unary field read. **`getD_append_shift3`** (PROVED) — `(pre ++ X).getD (pre.length + i) O = X.getD i O`. **`field_content3`** (PROVED) — `T = pre ++ (encodeNatBits3 n ++ rest)`, `pre.length = h` ⇒ `(∀ i < n, T.getD (h+i) O = I) ∧ T.getD (h+n) O = O`: exactly the hypotheses `scanNatFrom3_run_eq` consumes. Honest scope: the content lemma feeding the `Sym3` record scanner — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3FieldContent` |
| **Entry 397 — universal-TM-table build: the 3-symbol record scanner `scanTransFrom3` (PROVED)** | The `Sym3` port of the list-preserving record scanner (entry 371): composes the five field scanners into a relocatable scanner for one encoded transition on `T = pre ++ encodeTransBits3 t ++ rest`, returning `T` **exactly**. Because every field scan keeps the same tape `T` (the `_run_eq` versions), all five runs are on `T`, composed directly by `reachIn_seq3` with no transport; `field_content3` supplies each field's content and `scanBit3` handles the `boolToSym3` symbol cells. **`scanTransFrom3_run_eq`** (PROVED) — `reachIn (rs+ws+mv+5) (base, pre.length, T) (base+5, pre.length + (rs+ws+mv+5), T)`. Honest scope: the loopable record-scan unit of the rule-table traversal — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ScanTrans` |
| **Entry 398 — universal-TM-table build: the 3-symbol rule-table scan `scanTable3` (PROVED)** | The `Sym3` port of entry 372: loops the record scanner over the whole transition list. **`scanTable3 base Ms`** — `[] ↦ []`, `t :: ts ↦ scanTransFrom3 base ++ scanTable3 (base+5) ts`. **`scanTable3_run`** (PROVED) — `reachIn (Ms.flatMap encodeTransBits3).length (base, pre.length, pre ++ Ms.flatMap encodeTransBits3 ++ rest) (base + 5*Ms.length, pre.length + len, SAME tape)`, by induction on `Ms` generalizing `base`/`pre`; list-preservation makes the inductive step re-run the scanner on the *identical* tape — a clean recursion, no transport. Honest scope: traverses the encoded rule list list-preservingly; does **not** yet *match* (compare each transition's key against the configuration) nor apply — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ScanTable` |
| **Entry 399 — universal-TM-table build: the fixed-symbol bit test `testBit3` (PROVED — genuinely new, not a port)** | The match atom of the rule-loop, and the *first genuinely-new* marker-route construction: the comparison the rule-loop needs is *data against data* (config key vs rule key), exactly the wall the 2-symbol Bool route hit. The marker route's resolution: `markCarry3` (entry 389) reads one config-key bit **into the control-state lineage** (`sO` vs `sI`), so at the distant rule-key cell the comparison is no longer data-vs-data — it is a test against a **constant** `w` fixed by that lineage. **`testBit3 s sEq sNe w`** — on each symbol, write it back, *stay*, route to `sEq` if it equals `w`, else `sNe`. **`testBit3_run`** (PROVED) — `j < tp.length ⇒ reachIn 1 (s, j, tp) ((if readSym3 (s,j,tp) = w then sEq else sNe), j, tp)`: one step, head fixed, tape identical, control routed on `head = w`. Honest scope: a single-cell test against a constant — does **not** yet shuttle, compare a whole key, loop the match, nor apply — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3TestBit` |
| **Entry 400 — universal-TM-table build: the `Sym3` emission lifting `universalSim_of_emits3` (PROVED)** | Re-establishes the *final lifting* over the marker tape (port of entry 342). The abstract simulation layer — `encodedStep`, `simIter`, `encodeConfig`, `encodeMachineBits`, `encodedStep_correct` — is **symbol-agnostic** (pure functions on bit-lists), reused verbatim; only `U`, the layout `φ : List Bool → List Bool → CConfig3`, and `reachIn (toNTM3 U)` are `Sym3`-specific. **`EmitsEncodedStep3 U φ cost`** — the per-macro-step obligation over the marker machine. **`universalSim_of_emits3`** (PROVED) — emission + `encodedStep_correct` ⟹ `toNTM3 U` realises the full `simIter` simulation (`reachIn (toNTM3 U) (k*cost) …`), induction on `k` + `reachIn_add`. Reduces the whole `Sym3` construction to one obligation: concrete `U` + `φ` + `EmitsEncodedStep3`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Emit` |
| **Entry 401 — universal-TM-table build: the n-step right move `moveRightN3` (PROVED)** | The outbound leg of the marker shuttle. **`moveRightN3 s n`** — a corridor of `n` states (`0 ↦ []`, `n+1 ↦ moveRight3 s (s+1) ++ moveRightN3 (s+1) n`). **`moveRightN3_run`** (PROVED) — `j + n ≤ tp.length ⇒ reachIn n (s, j, tp) (s+n, j+n, tp)`, tape identical (each intermediate cell in bounds, so each write-back is the identity). Honest scope: the bounded move — does **not** yet compare nor restore — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MoveN` |
| **Entry 402 — universal-TM-table build: the marker-shuttle return-and-restore `probeTail3` (PROVED)** | The distance-independent half of the shuttle. **`probeTail3 sIn sFound sCont sOut b`** — `seekMarkLeft sIn sFound sCont ++ unmark3 sFound sOut b`. **`probeTail3_run`** (PROVED) — with the marker at `p`, no marker in `(p, p+d]`, and `p+d` in bounds: `∃ N, reachIn N (sIn, p+d, tp) (sOut, p, writeAt3 tp p b)` — `seekMarkLeft` returns to the anchor regardless of the outbound distance, `unmark3` restores the carried bit. Lift each via `reachIn_append_left3`/`right3`, chain `reachIn_add`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ProbeTail` |
| **Entry 403 — universal-TM-table build: the single-bit compare-after-mark `probe3` (PROVED — the crux of the marker route)** | The marker route's *distance-independent data-vs-data comparison*, the brick the 2-symbol route could not build. With the anchor `M` already at the config-key cell `p` (and the original bit `b` carried in the control state), `probe3` walks right `d` to the rule-key cell, tests it against `b`, returns distance-independently to the anchor, and restores it. **`probe3 s sEq sNe d b`** — `moveRightN3 s d ++ testBit3 (s+d) (s+d+1) (s+d+2) b ++ probeTail3 (s+d+1) (s+d+3) (s+d+4) sEq b ++ probeTail3 (s+d+2) (s+d+5) (s+d+6) sNe b`. **`probe3_run`** (PROVED) — anchor at `p`, no marker in `(p, p+d]`, `p+d` in bounds ⇒ `∃ N, reachIn N (s, p, tp) ((if tp.getD (p+d) O = b then sEq else sNe), p, writeAt3 tp p b)`. The machine is a *branch* (`testBit3` routes to one of two tails); each run traverses one — lift each sub-run to the union via the monotonicity lemmas, `by_cases` on the bit equality, chain `reachIn_add`. Honest scope: the single-bit compare — does **not** yet prepend `markCarry3`, loop over a key, nor the rule-table match — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Probe` |
| **Entry 404 — universal-TM-table build: the self-contained single-bit compare `bitCompareAtDist3` (PROVED)** | `probe3` with its own anchor management: `markCarry3` reads the config-key bit at `p` into the control-state lineage and lays the anchor `M`, then each lineage feeds the matching `probe3` (testing against `O` resp. `I`). **`bitCompareAtDist3 s sEq sNe d`** — `markCarry3 s (s+1) (s+d+8) 0 ++ probe3 (s+1) sEq sNe d O ++ probe3 (s+d+8) sEq sNe d I`. **`bitCompareAtDist3_run`** (PROVED) — for a bit cell (`tp.getD p O ∈ {O,I}`), `d ≥ 1`, no marker in `(p, p+d]`, `p`/`p+d` in bounds: `∃ N, reachIn N (s, p, tp) ((if tp.getD (p+d) O = tp.getD p O then sEq else sNe), p, tp)`, tape restored. Tape algebra: the marked tape `tp' = writeAt3 tp p M` agrees with `tp` off `p` (`writeAt3_getD`), and the result double-write collapses (`writeAt3_writeAt3`) back to `tp` (`writeAt3_id_of_lt`). Honest scope: a two-input bit comparison at arbitrary distance — does **not** yet loop over a key field nor the rule-table match — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3BitCompare` |
| **Entry 405 — universal-TM-table build: the three-way field-loop body `fieldStep3` (PROVED)** | `bitCompareAtDist3` refined into the genuine field-compare **loop body**: it gives the two *agreeing* cell-pair cases distinct control targets, which is what makes field termination decidable. Reads the config bit (anchoring via `markCarry3`), then probes the rule bit against `O` in the `O`-lineage and against `I` in the `I`-lineage, routing the lineages to *separate* targets. **`fieldStep3 s sCont sMatch sFail d`** — `markCarry3 s (s+1) (s+d+8) 0 ++ probe3 (s+1) sMatch sFail d O ++ probe3 (s+d+8) sCont sFail d I`. **`fieldStep3_run`** (PROVED) — for a bit cell (`tp.getD p O ∈ {O,I}`), `d ≥ 1`, no marker in `(p, p+d]`, `p`/`p+d` in bounds: `∃ N, reachIn N (s, p, tp) (end, p, tp)`, tape restored, where `end = if tp[p]=O then (if tp[p+d]=O then sMatch else sFail) else (if tp[p+d]=I then sCont else sFail)` — config-`O` (separator) ⇒ `sMatch` (rule `O`, fields ended together) / `sFail` (rule `I`, config shorter); config-`I` (a one) ⇒ `sCont` (rule `I`, both continue) / `sFail` (rule `O`, rule shorter). (`bitCompareAtDist3` is the case `sCont = sMatch`.) Proof mirrors entry 404 — `rcases` the config bit, route each lineage through the matching `probe3`, collapse the double-write back to `tp`, lift via `append_left3`/`right3` and `reachIn_add` — additionally reducing the outer `if` per lineage (`if_pos rfl` / `if_neg (by decide)`). Honest scope: one verified cell-pair step deciding continue / match / fail — does **not** yet advance the pointer, induct over the field, nor the rule-table match — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3FieldStep` |
| **Entry 406 — universal-TM-table build: the full unary field compare `fieldCompare3` (PROVED)** | The whole key-field compare — the field loop isolated as a clean theorem. Loops `fieldStep3` (entry 405) over a unary key field: it compares the config-key field (a unary nat `a` starting at `c`) against the rule-key field (a unary nat `b` starting at `c+d`, the *same* distance `d` away cell by cell) and routes to `sMatch` iff `a = b`. **`fieldCompare3 s sMatch sFail d`** — `0 ↦ []`, `L+1 ↦ fieldStep3 s (s+2d+15) sMatch sFail d ++ moveRight3 (s+2d+15) (s+2d+16) ++ fieldCompare3 (s+2d+16) sMatch sFail d L`: a corridor unrolled `L` times (a static budget on the field length), the continue target of each `fieldStep3` being the advance state, then a single `moveRight3` to the next config cell, then the next iteration (the rule pointer tracks automatically since `d` is constant). **`fieldCompare3_run`** (PROVED) — with **no marker anywhere on `tp`** (`hM`), `d ≥ 1`, `c+a+d < tp.length`, budget `min a b < L`, and the two fields' content (`a` ones then `O` at `c`; `b` ones then `O` at `c+d`): `∃ N, reachIn N (s, c, tp) ((if a = b then sMatch else sFail), c + min a b, tp)` — routes to `sMatch` iff the two unary fields are equal, head ending at `c + min a b`, tape restored. Proof by induction on the budget `L` (with `s,c,a,b` generalized): the `zero` case is vacuous (`min a b < 0`); the `succ` case runs one `fieldStep3` at `p=c` (all its bounds from `c+a+d<length`+`d≥1`; `hbit` from the config field by cases on `a`; `hno` from `hM`), then a four-way `Nat.eq_zero_or_pos` split on `a` then `b` — `a=0,b=0 ⇒ sMatch`, `a=0,b>0` and `a>0,b=0 ⇒ sFail` (terminating cases: reduce the `fieldStep3` end-state via `if_pos`/`if_neg`, lift to the full corridor via `append_left3` twice), and `a>0,b>0 ⇒` continue (reduce the end-state to the continue target, `moveRight3` advance, apply the IH at `(c+1, a-1, b-1)` with the *same* `d`, chaining via `reachIn_seq3` twice). Reducing `(a-1=b-1) ↔ (a=b)` inside the result `if` requires `simp only` (a `rw` fails the decidable-instance motive check). The head ends at `c + min a b` uniformly (terminating cases have `min = 0`; the continue case carries `(c+1) + min (a-1) (b-1) = c + min a b`). Honest scope: the genuine field loop — with it, the rule-table match loop becomes ordinary scanner/control engineering. Does **not** yet build the table loop, the apply, nor `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3FieldCompare` |
| **Entry 407 — universal-TM-table build: the single-record key matcher `recordKeyMatch3` (PROVED)** | The per-record "does *this* rule fire?" decision, composing the field loop (entry 406) and the single-bit compare (entry 404). A transition record is `encodeTransBits3 t = encodeNatBits3 state ++ boolToSym3 read :: (RHS)` (entry 393), so its *key* is a unary **state field** followed by one **symbol cell**; the configuration has the same shape. **`recordKeyMatch3 base recMatch recFail d L`** — `fieldCompare3 base M₀ recFail d L ++ moveRight3 M₀ (M₀+1) ++ bitCompareAtDist3 (M₀+1) recMatch recFail d`, where `M₀ = base + L*(2d+16)` is the first state past the field-compare corridor. **`recordKeyMatch3_run`** (PROVED) — with no marker anywhere on `tp`, `d ≥ 1`, budget `min a b < L`, bound `c+a+1+d < tp.length`, the two state fields' content (`a` ones then `O` at `c`; `b` ones then `O` at `c+d`), and the two symbol cells (`cs` at `c+a+1` with `cs ∈ {O,I}`, `rs` at `c+d+b+1`): `∃ N q, reachIn N (base, c, tp) ((if a = b ∧ rs = cs then recMatch else recFail), q, tp)` — routes to `recMatch` iff the two keys match, tape restored. **Key geometry**: once the state fields are equal (`a = b`) the two symbol cells sit at the *same* distance `d` (config at `c+a+1`, rule at `c+d+b+1 = (c+a+1)+d`), so the single-bit compare reuses the very same `d`. Proof: run `fieldCompare3`, then `by_cases a = b` — on match, `if_pos` collapses the head to `c+a`, a `moveRight3` lands on the symbol cell `c+a+1`, `bitCompareAtDist3` runs (its anchor-bit hypothesis comes from `cs ∈ {O,I}`; the rule cell `rs` need *not* be a valid bit, so that hypothesis is dropped — a strictly stronger statement), the symbol condition is rewritten to `rs = cs` via `simp only`, and the three pieces chain via `reachIn_seq3` twice; on mismatch, `if_neg` routes the field compare straight to `recFail`, lifted to the full machine via `append_left3` twice. The head position is left existential (`q = c+a+1` on the match path, `c + min a b` on the fail path). Honest scope: the per-record decision — does **not** yet loop over the whole rule table (scan to the next record on `recFail`), nor apply the matched rule — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3KeyMatch` |
| **Entry 408 — universal-TM-table build: the config-home reset `resetToHome3` (PROVED)** | The inter-record head reset the rule-table match loop needs. Each per-record matcher (`recordKeyMatch3`, entry 407) runs the marker shuttle out and back, leaving the head on the config side at a *data-dependent* position (`c + min(a,bᵢ)` on a state mismatch, `c+a+1` on the symbol stage). A data-dependent leftward move cannot be a fixed-length corridor, so returning to the config key start needs a **marker-based seek**. Assuming a persistent *home* marker at `home = c-1` (one cell left of the config key): **`resetToHome3 s found cont out`** — `seekMarkLeft s found cont ++ moveRight3 found out`. **`resetToHome3_run`** (PROVED) — with the home marker at `home` (`tp.getD home O = M`), no marker in `(home, home+d]`, and `home+d < tp.length`: `∃ N, reachIn N (s, home + d, tp) (out, home + 1, tp)` — from any head position `home+d` in the config window the machine returns to `c = home+1`, the tape identical. Proof: `seekMarkLeft_run` (entry 387, the distance-independent left seek to a marker) lands the head at `home`, then a `moveRight3` steps onto `home+1`, chained by `reachIn_seq3`. Honest scope: the reset primitive — note the loop that uses it must maintain the home marker, so the matcher's *global* no-marker hypothesis will need to become *windowed* (no marker only in the cells it shuttles through, leaving `c-1` free); that refactor and the `matchTable3` assembly are the genuine remaining construction — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ResetHome` |
| **Entry 409 — universal-TM-table build: the windowed matcher (PROVED)** | The hypothesis-windowing that lets the rule-table match loop (entry 410) keep a persistent *home* marker at `c-1`. Entries 406/407 assume **no marker anywhere** on the tape (`hM : ∀ j, tp.getD j O ≠ M`), which a home marker violates; this re-proves both runs with that hypothesis *windowed* to the cells the machine actually shuttles through. **`fieldCompare3_run_windowed`** (PROVED) — `fieldCompare3_run` (406) with `hM` replaced by `∀ j, c < j → j ≤ c+a+d → tp.getD j O ≠ M`, carried *inside* the `∀ L s c a b` because the window shifts `c → c+1` and shrinks as the recursion consumes the field. **`recordKeyMatch3_run_windowed`** (PROVED) — `recordKeyMatch3_run` (407) with `hM` replaced by `∀ j, c < j → j ≤ c+a+1+d → tp.getD j O ≠ M` (covering both the field-compare window `(c, c+a+d]` and the symbol-compare window `(c+a+1, c+a+1+d]`). These are the *same machines* `fieldCompare3` / `recordKeyMatch3` under *weaker* hypotheses, so the theorems are strictly stronger; the proofs mirror 406/407 verbatim except that `hno` is derived from the window rather than the global hypothesis, the IH is handed the shrunk window (`fun j hj1 hj2 => hWin j (by omega) (by omega)`), and the record matcher passes the field compare its sub-window (`fun j hj1 hj2 => hWin j hj1 (by omega)`). Design note: the window *must* sit inside the `∀` for the field compare (it depends on `c,a`, which the induction changes) but is an ordinary hypothesis for the record matcher (no induction). The record matcher's conclusion also bounds the fail/match head position (`c ≤ q ∧ q ≤ c+a+1`) — both the state-mismatch position `c+min a b` and the symbol-stage position `c+a+1` satisfy it — which the table loop (410) needs to discharge the reset's in-bounds and `home ≤ q` obligations. Honest scope: the hypothesis-windowing only — does **not** yet assemble the loop nor apply the matched rule — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3KeyMatchWin` |
| **Entry 410 — universal-TM-table build: the rule-table match loop `matchTable3` (PROVED)** | The full rule lookup, assembled from the windowed matcher (409) and the home reset (408). Walks the encoded transition list and, per record, decides whether its key matches the configuration key; on a mismatch it returns the head to the config home and tries the next record; on a match it exits to the global `recMatch` (apply) state. Records are modelled as descriptors `(d, b, rs)` — distance from the config key, record state length, read symbol. **`RecMatch a cs (d,b,rs)`** — `a = b ∧ rs = cs`. **`RecOK tp c a L (d,b,rs)`** — `1 ≤ d`, budget, in-bounds, plus the record's state field (`b` ones then `O` at `c+d`) and symbol cell (`rs` at `c+d+b+1`). **`matchTable3 recMatch L base recs`** — `[] ↦ []`; `(d,_,_)::rest ↦ recordKeyMatch3 base recMatch RF d L ++ resetToHome3 RF (RF+1) (RF+2) nextBase ++ matchTable3 recMatch L nextBase rest`, with `RF = base + L*(2d+16) + 2d + 16` (the matcher's fail state) and `nextBase = RF + 3`. **`matchTable3_run`** (PROVED) — with a home marker at `c-1` (`hmark`), no marker elsewhere (`hclean`), the config key content, every record `RecOK`, and **some** record matching the config key (`∃ rec ∈ recs, RecMatch a cs rec`): `∃ N q, reachIn N (base, c, tp) (recMatch, q, tp)` — the match-found state is reachable, tape restored. Proof by induction on the record list: the `nil` case is vacuous (no record can match the empty list); the `cons` case does `by_cases` on whether the head matches — a head match routes `recordKeyMatch3` straight to `recMatch` (lifted to the full machine via `append_left3` twice); a head mismatch routes it to the fail state at the bounded position `q`, `resetToHome3` returns the head home (its in-bounds/`home ≤ q` obligations discharged by the 409 `q`-bound), and the IH handles the tail (the `∃` moves there since the head is not the match), all chained by `reachIn_seq3` twice. Honest scope: the `∃`-direction — *some applicable rule ⇒ the match-found state is reachable*, which is the faithful lookup semantics for the non-deterministic `toNTM3` model. Does **not** yet prove the exhaustion direction (no applicable rule ⇒ a reject/halt state), nor *apply* the matched rule — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MatchTable` |
| **Entry 411 — universal-TM-table build: the single-cell copy `copyBitAtDist3` (PROVED)** | The foundational brick of the *apply* phase (which copies the matched rule's RHS into the configuration), the copy-side analogue of the single-bit compare (entry 404). The clean construction avoids markers entirely: *read the source bit without modifying it* (`readCarry3` writes the read symbol straight back, branching the control state on its value), walk right `d`, write the carried bit at the destination, then walk **back** the fixed `d` cells (`moveLeftN3`) — no seek needed, since the return distance is exactly `d`. **`moveLeftN3 s n`** — the `n`-step left-move corridor (mirror of `moveRightN3`); **`moveLeftN3_run`** (PROVED): `n ≤ j → j < tp.length → reachIn n (s, j, tp) (s+n, j-n, tp)`, tape identical. **`readCarry3 s sO sI`** — read the head cell, write it back, *stay*, branch to `sO`/`sI`; **`readCarry3_run_O`/`_I`** (PROVED): with `j < tp.length` the tape and head are unchanged (the write-back is the identity via `writeAt3_id_of_lt`) and control is routed on the bit. **`copyArm3 s sOut d b`** / **`copyArm3_run`** (PROVED) — one lineage: `tp[p] = b`, `d ≥ 1`, `p`/`p+d` in bounds ⇒ `∃ N, reachIn N (s, p, tp) (sOut, p, writeAt3 tp (p+d) b)`; the destination double-write collapse `writeAt3 (writeAt3 tp (p+d) b) p b = writeAt3 tp (p+d) b` is proved by `rwa [hp1] at hid` over `writeAt3_id_of_lt` (a direct `▸` rewrites the wrong, aliased `b`). **`copyBitAtDist3 s sOut d`** / **`copyBitAtDist3_run`** (PROVED) — `readCarry3 ++ copyArm3 (O) ++ copyArm3 (I)`: with `tp[p] ∈ {O,I}`, `d ≥ 1`, `p`/`p+d` in bounds: `∃ N, reachIn N (s, p, tp) (sOut, p, writeAt3 tp (p+d) (tp.getD p O))` — copies the source bit into cell `p+d`, head back at `p`, source unchanged (`rcases` the bit; the O-lineage lifts via `append_left3`, the I-lineage via `append_left3` twice + `append_right3` + `reachIn_add`). Honest scope: the single-cell copy — does **not** yet loop over a whole field (the new-state copy), nor write the symbol / move the simulated head, nor assemble `apply3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CopyBit` |
| **Entry 412 — universal-TM-table build: the unary field copy `copyField3` (PROVED)** | The new-state copy of the apply phase — loops the single-cell copy (411) over a whole unary field, the copy-side analogue of `fieldCompare3` (406). **`copyStep3 s sCont sDone d`** / **`copyStep3_run`** (PROVED) — refines `copyBitAtDist3` so the two source-bit lineages route to *distinct* targets: a one (`I`) ↦ `sCont` (continue), the separator (`O`) ↦ `sDone` (stop); copies the bit to `p+d`, head back at `p`, tape `= writeAt3 tp (p+d) (tp.getD p O)`. **`copyBlock tp c d m`** — the resulting tape (`tp` with cells `c … c+m` reproduced at `c+d … c+m+d`), *defined in the loop's order* (`0 ↦ writeAt3 tp (c+d) (tp.getD c O)`, `m+1 ↦ copyBlock (writeAt3 tp (c+d) (tp.getD c O)) (c+1) d m`) so the induction aligns with **no** write-commuting. **`copyField3 s sDone d L`** — the budget-`L` corridor `copyStep3 s (s+4d+5) sDone d ++ moveRight3 (s+4d+5) (s+4d+6) ++ copyField3 (s+4d+6) sDone d L`. **`copyField3_run`** (PROVED) — for a unary field (`m` ones then `O` at `c`), budget `m < L`, **disjointness `m < d`** (destination region beyond the source), and `c+m+d < tp.length`: `∃ N, reachIn N (s, c, tp) (sDone, c+m, copyBlock tp c d m)` — the field is reproduced at `c+d … c+m+d`, the source unchanged, head at `c+m`. The crucial difference from `fieldCompare3`: copying **modifies** the tape, so `tp` is *generalized inside the* `∀` and the recursion runs on `tp1 = writeAt3 tp (c+d) (tp.getD c O)`; the `copyBlock` fold is defeq to the recursive structure (`copyBlock tp1 (c+1) d m' = copyBlock tp c d (m'+1)`), and the field content of `tp1` is recovered by `writeAt3_getD` off-position (the writes at `c+d` are beyond the field since `m < d`). Compiled on the first try. Honest scope: the unary field copy, **rightward** (source left of destination) — does **not** yet write the symbol, move the simulated head, assemble `apply3`, nor provide a leftward variant — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CopyField` |
| **Entry 413 — universal-TM-table build: the configuration layout & symbol write `configEncode3` (PROVED)** | Pins the (previously deferred) simulated-machine configuration encoding and proves the apply phase's symbol write. The layout mirrors the rule key (entry 393): **`configEncode3 q sym rest = encodeNatBits3 q ++ boolToSym3 sym :: rest`** — current state (unary), then the current-symbol cell, then the rest of the simulated tape. On `pre ++ configEncode3 q sym rest` with `pre.length = c`, the state field is `c … c+q` (`q` ones then the `O` separator) and the symbol cell is at `c+q+1`, exactly the matcher's positions (`a = q`, symbol at `c+a+1`). **`configEncode3_content`** (PROVED) — the bridge to the matcher: `(∀ i < q, getD (c+i) = I) ∧ getD (c+q) = O ∧ getD (c+q+1) = boolToSym3 sym`, via `field_content3` (396) and `getD_append_shift3`. **`configEncode3_set_sym`** (PROVED) — the list surgery `writeAt3 (pre ++ configEncode3 q sym rest) (c+q+1) (boolToSym3 sym') = pre ++ configEncode3 q sym' rest`, proved with `writeAt3_eq_set` (an in-bounds write is a `List.set`) and a self-contained `set_at_boundary` (`(A ++ x :: l).set A.length y = A ++ y :: l`, by induction on `A`) applied at the index `(pre ++ encodeNatBits3 q).length`. **`writeSym3_run`** (PROVED) — with the head at the current-symbol cell `c+q+1`, one `unmark3` step gives `reachIn 1 (s, c+q+1, pre ++ configEncode3 q sym rest) (s', c+q+1, pre ++ configEncode3 q sym' rest)` — the symbol is rewritten `sym → sym'`, the rest untouched (after the matcher succeeds the head is already at this cell, so the write is a single step). Honest scope: pins the configuration layout and proves the symbol write — does **not** yet carry the write symbol from the matched rule, move the simulated head, nor assemble `apply3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Config` |
| **Entry 414 — universal-TM-table build: the leftward single-cell copy `copyBitAtDistLeft3` (PROVED)** | The mirror of the rightward copy (entry 411), the direction the apply phase needs: the configuration sits to the *left* of the rule table (the matcher placed the config key at `c`, the rule at `c+d`), so apply copies **right → left**. Read the source bit marker-free (`readCarry3`), walk **left** `d` cells (`moveLeftN3`), write the carried bit at `p-d`, then walk **back right** the fixed `d` cells (`moveRightN3`) — no seek, since the return distance is exactly `d`. **`copyArmLeft3 s sOut d b`** / **`copyArmLeft3_run`** (PROVED) — one lineage: `tp[p] = b`, `1 ≤ d`, `d ≤ p`, `p < tp.length` ⇒ `∃ N, reachIn N (s, p, tp) (sOut, p, writeAt3 tp (p-d) b)`. **`copyBitAtDistLeft3 s sOut d`** / **`copyBitAtDistLeft3_run`** (PROVED) — `readCarry3 ++ copyArmLeft3 (O) ++ copyArmLeft3 (I)`: with `tp[p] ∈ {O,I}`, `1 ≤ d`, `d ≤ p`, `p < tp.length`: `∃ N, reachIn N (s, p, tp) (sOut, p, writeAt3 tp (p-d) (tp.getD p O))` — copies the source bit into cell `p-d`, head back at `p`, source unchanged. An exact left/right swap of entry 411; compiled on the first try. Honest scope: the leftward single-cell copy — does **not** yet loop over a field, carry a specific rule field, nor assemble `apply3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CopyBitLeft` |
| **Entry 415 — universal-TM-table build: the leftward unary field copy `copyFieldLeft3` (PROVED)** | The new-state copy in the apply direction — loops the leftward single-cell copy (414) over a whole unary field, the leftward mirror of `copyField3` (412). **`copyStepLeft3 s sCont sDone d`** / **`copyStepLeft3_run`** (PROVED) — copy the source bit to `p-d`, routing `I ↦ sCont` / `O ↦ sDone`, head back at `p`, tape `= writeAt3 tp (p-d) (tp.getD p O)`. **`copyBlockLeft tp c d m`** — the resulting tape (`tp` with cells `c … c+m` reproduced at `c-d … c+m-d`), defined in the loop's order. **`copyFieldLeft3 s sDone d L`** — the budget-`L` corridor `copyStepLeft3 s (s+4d+5) sDone d ++ moveRight3 (s+4d+5) (s+4d+6) ++ copyFieldLeft3 (s+4d+6) sDone d L` (the *same* corridor shape as `copyField3` — the source head advances rightward, only the copy direction differs). **`copyFieldLeft3_run`** (PROVED) — for a unary field (`m` ones then `O` at `c`), `m < L`, `1 ≤ d`, `d ≤ c`, `c+m < tp.length`: `∃ N, reachIn N (s, c, tp) (sDone, c+m, copyBlockLeft tp c d m)`. **Simplification vs entry 412**: the leftward copy needs only `d ≤ c` (room to the left), *not* `m < d` — because the destination `c+i-d` is always *behind* the advancing source pointer `c+i`, so the writes never touch a cell that will still be read, and the recursion's field content (cells `c+1 … c+m`) is preserved for any `d` (the writes lie to the left of the remaining field). `tp` is generalized inside the `∀` (the copy modifies the tape); induction on `L`, cases on `m`, chained by `reachIn_seq3` twice. Compiled on the first try. Honest scope: the leftward unary field copy — does **not** yet carry the specific rule fields, move the simulated head, nor assemble `apply3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CopyFieldLeft` |
| **Entry 416 — universal-TM-table build: the unary head-pointer step `unaryInc3` / `unaryDec3` (PROVED)** | The simulated head move at the pointer level, under the chosen layout (`config = state | symCache | headPtr(unary) | tape`): a move updates the explicit unary head-position field by `±1`. With the field padded by reserved blank (`O`) cells, the update is a single cell flip after a field scan: **increment** `n → n+1` flips the separator `h+n` from `O` to `I` (the reserved-blank `h+n+1` becomes the new separator); **decrement** `n → n-1` flips the last one `h+n-1` from `I` to `O`. **`unaryInc3 s s'`** — `scanNatFrom3 s (s+1) ++ moveLeft3 (s+1) (s+2) ++ unmark3 (s+2) s' I`; **`unaryInc3_run`** (PROVED) — field `n` ones then `O` at `h`, `h+n+1 < tp.length` ⇒ `∃ N, reachIn N (s, h, tp) (s', h+n, writeAt3 tp (h+n) I)`. **`unaryDec3 s s'`** — scan then `moveLeft3` twice onto the last one then `unmark3 … O`; **`unaryDec3_run`** (PROVED) — for `n ≥ 1`: `∃ N, reachIn N (s, h, tp) (s', h+n-1, writeAt3 tp (h+n-1) O)`. **`unaryInc3_field` / `unaryDec3_field`** (PROVED) — content corollaries confirming the result is a unary field of length `n+1` (given the next cell is reserved blank) resp. `n-1`, by a `writeAt3_getD` case split. `scanNatFrom3` lands one past the separator and `moveLeft3` backs onto the target cell; chained by `reachIn_seq3`. Compiled on the first try. Honest scope: the unary head-pointer `±1` step — does **not** yet refresh the cached current symbol after the move, nor assemble `apply3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3UnaryStep` |
| **Entry 417 — universal-TM-table build: the distant-cell fetch `fetchBitFromDist3` (PROVED)** | The apply's *read-far / write-near* primitive — the mirror of the matcher's `probe3` (entry 403), which also reaches a distant cell but only *compares* it. Used to bring rule/tape values into the configuration (cache refresh, write-symbol carry). **`fetchArm3 s sOut d`** — `moveRightN3 s d ++ testBit3 (s+d) (s+d+1) (s+d+2) O ++ probeTail3 (s+d+1) (s+d+3) (s+d+4) sOut O ++ probeTail3 (s+d+2) (s+d+5) (s+d+6) sOut I`: like `probe3` but the two tails write the *read* value (`O` resp. `I`) instead of restoring a carried constant. **`fetchArm3_run`** (PROVED) — with the anchor `M` at `p`, no marker in `(p, p+d]`, `p+d < tp.length`, and `tp[p+d] ∈ {O,I}`: `∃ N, reachIn N (s, p, tp) (sOut, p, writeAt3 tp p (tp.getD (p+d) O))`; proof mirrors `probe3_run` (`set A/B/C/D`, lift each sub-run through the append, `by_cases tp[p+d]=O` with `if_pos`/`if_neg`, `probeTail3_run` per tail, chain `reachIn_add`; the `ne` case gets `=I` via `hfar.resolve_left`). **`fetchBitFromDist3 s sOut d`** — `markCarry3 s (s+1) (s+1) 0 ++ fetchArm3 (s+1) sOut d` (the `markCarry3` lays the anchor with both lineages going to the same state, since the local value is discarded). **`fetchBitFromDist3_run`** (PROVED) — with `tp[p] ∈ {O,I}`, `1 ≤ d`, no marker in `(p, p+d]`, `p+d < tp.length`, `tp[p+d] ∈ {O,I}`: `∃ N, reachIn N (s, p, tp) (sOut, p, writeAt3 tp p (tp.getD (p+d) O))` — cell `p ← tp[p+d]`, source unchanged; the marked-tape double-write collapses `writeAt3 (writeAt3 tp p M) p (tp[p+d]) = writeAt3 tp p (tp[p+d])` via `writeAt3_getD` (off-position) and `writeAt3_writeAt3`. Honest scope: the distant-cell fetch — does **not** yet drive the data-dependent walk to `tape[headPtr]` (the cache refresh), nor assemble `apply3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Fetch` |
| **Entry 418 — universal-TM-table build: the counter-driven walk `walkRightClearField3` (PROVED)** | The first genuine piece of unary head-pointer *data-dependent addressing*: walk the head right by an amount given by a unary tally, *consuming* it (vs `scanNatFrom3`, which walks a field non-destructively). **`walkStep3 s sCont sDone`** — `[((s,I),(sCont,O,right)), ((s,O),(sDone,O,stay)), ((s,M),(sDone,M,stay))]`: on a one, clear to `O`, move right, go `sCont`; on the separator, stay, go `sDone`. **`walkStep3_run_I`** (PROVED) — `tp[j]=I ⇒ reachIn 1 (s,j,tp) (sCont, j+1, writeAt3 tp j O)`; **`walkStep3_run_O`** (PROVED) — `tp[j]=O`, `j < tp.length ⇒ reachIn 1 (s,j,tp) (sDone, j, tp)` (writing `O` on an `O` is the identity via `h ▸ writeAt3_id_of_lt`). **`clearBlock tp h m`** — `tp` with cells `h … h+m-1` cleared to `O` (fold in loop order). **`walkRightClearField3 s sDone L`** — `L+1 ↦ walkStep3 s (s+1) sDone ++ walkRightClearField3 (s+1) sDone L` (stride 1 — the step itself moves right, so no separate advance). **`walkRightClearField3_run`** (PROVED) — for a unary tally (`m` ones then `O` at `h`), `m < L`, `h+m < tp.length`: `∃ N, reachIn N (s, h, tp) (sDone, h+m, clearBlock tp h m)` — head moves right by `m`, the tally cleared (induction on `L`, cases on `m`, `tp` generalized inside the `∀` since the walk modifies the tape, chained by `reachIn_seq3`). Compiled on the first try. Honest scope: a data-dependent walk component — full cache refresh must still relate the head-pointer count to the tape offset (`tape[m]` sits at `H+2m+1`); does **not** yet assemble that, nor `apply3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Walk` |
| **Entry 419 — universal-TM-table build: the marker-on-current-cell head move `headMoveRight3` / `headMoveLeft3` (PROVED)** | The simulated-head representation is switched to a **marker `M` on the current cell**. The decisive advantage over the explicit unary head-pointer (entries 416/418): a head *move* is now **local** — a swap of the marker with its neighbouring symbol — so it needs **no data-dependent walk**, and it *reuses the already-proven single-cell copies*. **`headMoveRight3 s sOut`** — `moveRight3 s (s+1) ++ copyBitAtDistLeft3 (s+1) (s+10) 1 ++ unmark3 (s+10) sOut M`: from `M` at `p`, step onto the neighbour `p+1`, carry its symbol `c = tp[p+1]` one cell left over the old marker (`copyBitAtDistLeft3` at distance 1, leaving `p ← c`), then write `M` at `p+1`. **`headMoveRight3_run`** (PROVED) — `tp[p+1] ∈ {O,I}`, `p+1 < tp.length` ⇒ `∃ N, reachIn N (s, p, tp) (sOut, p+1, writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M)`: the marker moves `p → p+1`, cell `p` receives the neighbour's symbol, head ends on the new marker. **`headMoveLeft3 s sOut`** — the mirror (`moveLeft3`, `copyBitAtDist3` at distance 1 carrying right, write `M` at `p-1`); **`headMoveLeft3_run`** (PROVED) — `tp[p-1] ∈ {O,I}`, `1 ≤ p`, `p < tp.length` ⇒ `∃ N, reachIn N (s, p, tp) (sOut, p-1, writeAt3 (writeAt3 tp p (tp.getD (p-1) O)) (p-1) M)`. Each proof chains `moveRight3`/`moveLeft3`, the proven copy (411/414), and `unmark3` via `reachIn_seq3`. The non-`M` cell sequence is preserved (`M` is a position marker between cells). Compiled on the first try. Honest scope: switches the head representation and proves the local moves — does **not** yet rework the matcher to read the current symbol at the marker, nor assemble `apply3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3HeadMove` |
| **Entry 420 — universal-TM-table build: read the current symbol at the marker `readHeadSym3` (PROVED)** | The bridge reconnecting the marker head representation (entry 419) to the matcher/apply: read the simulated current symbol distance-independently. The marker `M` marks the head; the current symbol is the cell *right after* `M`. **`readHeadSym3 s sO sI`** — `seekMarkRight s (s+1) (s+2) ++ moveRight3 (s+1) (s+3) ++ readCarry3 (s+3) sO sI`: seek right to `M` (entry 388, distance-independent), step onto the current cell, and branch on its value (entry 411). **`readHeadSym3_run`** (PROVED) — with the marker at `h+d` (no marker in `[h, h+d)`), `h+d`/`h+d+1` in bounds, and the current cell `h+d+1` a bit: `∃ N, reachIn N (s, h, tp) ((if tp.getD (h+d+1) O = O then sO else sI), h+d+1, tp)` — the head ends on the current cell, control routed on the current symbol, the tape identical. Proof: `seekMarkRight_run` + `moveRight3_run_eq` + `readCarry3_run_O`/`_I` (`rcases` the current bit, `if_pos`/`if_neg`), chained by `reachIn_seq3`; `readSym3 (s+3, h+d+1, tp) = tp.getD (h+d+1) O` by `rfl`. Compiled on the first try. No fixed-offset cache is needed — this is precisely why the marker representation is clean (contrast the unary head-pointer's data-dependent addressing, entries 416/418). Honest scope: the read-current-symbol bridge — does **not** yet rework the matcher to use it, nor assemble `apply3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ReadHead` |
| **Entry 421 — universal-TM-table build: the apply tape step `applyStep3` (PROVED)** | The tape transformation of one simulated step under the marker head representation: write the rule's symbol at the current cell, then move the head per the rule's direction (`M` at `q`, current cell `q+1`). **stay** — **`writeSymAtHead3 s sOut w`** = `moveRight3 s (s+1) ++ unmark3 (s+1) (s+2) w ++ moveLeft3 (s+2) sOut`; **`writeSymAtHead3_run`** (PROVED): `q+1 < tp.length ⇒ ∃ N, reachIn N (s, q, tp) (sOut, q, writeAt3 tp (q+1) w)` — write `w` at the current cell, head back at the marker. **right** — **`moveRightWrite3 s sOut w`** = `unmark3 s (s+1) w ++ moveRight3 (s+1) (s+2) ++ unmark3 (s+2) sOut M`; **`moveRightWrite3_run`** (PROVED): `q+1 < tp.length ⇒ ∃ N, reachIn N (s, q, tp) (sOut, q+1, writeAt3 (writeAt3 tp q w) (q+1) M)` — a *clean two-cell* result `q ← w`, `q+1 ← M` (TM-correct: the written symbol sits just left of the new head); the direct construction (write `w` at `q`, then `M` at `q+1`) avoids the `writeAt3`-commute nesting. **left** — **`moveLeftWrite3 s sOut w`** = `writeSymAtHead3 s (s+3) w ++ headMoveLeft3 (s+3) sOut`; **`moveLeftWrite3_run`** (PROVED): `1 ≤ q`, `q+1 < tp.length`, `tp[q-1] ∈ {O,I}` ⇒ `∃ N, reachIn N (s, q, tp) (sOut, q-1, writeAt3 (writeAt3 (writeAt3 tp (q+1) w) q (tp.getD (q-1) O)) (q-1) M)` (a three-cell update: `q-1 ← M`, `q ← old(q-1)`, `q+1 ← w`). All three chain the proven single-cell machines (`moveRight3`/`moveLeft3`/`unmark3`/`headMoveLeft3`) with `reachIn_seq3`. Honest scope: the apply tape step (symbol write + head move) — does **not** yet update the simulated *state* field, wire the write symbol / direction from the matched rule, nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Apply` |
| **Entry 422 — universal-TM-table build: the unary-field writer `writeUnaryFieldN3` (PROVED)** | The output dual of the field scanner (entry 395) and clearer (entry 418): *write* a unary field onto the tape — the primitive for emitting encoded fields toward `EmitsEncodedStep3`. **`writeIMove3 s s'`** — `[((s,_),(s',I,right))×3]`: write `I` at the head, move right; **`writeIMove3_run`** (PROVED) — `reachIn 1 (s, j, tp) (s', j+1, writeAt3 tp j I)`. **`writeFieldBlock tp j m`** — `tp` with cells `j … j+m-1` set to `I` and cell `j+m` set to `O` (fold in loop order). **`writeUnaryFieldN3 s sOut n`** — `0 ↦ unmark3 s sOut O`, `n+1 ↦ writeIMove3 s (s+1) ++ writeUnaryFieldN3 (s+1) sOut n`. **`writeUnaryFieldN3_run`** (PROVED) — `∃ N, reachIn N (s, j, tp) (sOut, j+n, writeFieldBlock tp j n)`: writes `encodeNatBits3 n` at `j`, head ending on the separator; induction on `n` generalizing `s`/`j`/`tp` (the writes modify the tape), chained by `reachIn_seq3` with the `(j+1)+n = j+(n+1)` rewrite. No length bound is needed (`writeAt3` extends, the head always advances). Compiled on the first try. Honest scope: writes a field of a *given* length — the simulated *state* update transfers a field whose length is read from the matched rule (a data-dependent copy, entry 415, after clearing the old field, entry 418); wiring that and `EmitsEncodedStep3` (cross-region addressing) is the remaining integration — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3WriteField` |
| **Entry 423 — universal-TM-table build: the field-then-bit emitter `writeFieldBit3` (PROVED)** | The first output-side *composition* for encoded structures: lay down `encodeNatBits3 n ++ [w]` (a unary field then a symbol cell) — the shape of a transition/config prefix (a state field then its read symbol, cf. `encodeTransBits3` entry 393, `configEncode3` entry 413). **`writeFieldBlock_length`** (PROVED) — `j+n < tp.length ⇒ (writeFieldBlock tp j n).length = tp.length`: the field writer (422) stays in bounds (induction on `n` generalizing `j`/`tp`, via `writeAt3_length_eq`). **`writeFieldBit3 s sOut n w`** — `writeUnaryFieldN3 s (s+n+1) n ++ moveRight3 (s+n+1) (s+n+2) ++ unmark3 (s+n+2) sOut w`. **`writeFieldBit3_run`** (PROVED) — `j+n < tp.length ⇒ ∃ N, reachIn N (s, j, tp) (sOut, j+n+1, writeAt3 (writeFieldBlock tp j n) (j+n+1) w)`: writes the unary field at `j`, then the symbol `w` at `j+n+1`, head on the symbol cell; chains the field writer (422) + `moveRight3` over the separator (using the length lemma) + `unmark3` via `reachIn_seq3`. Compiled on the first try. Honest scope: the field-then-bit emitter — does **not** yet emit a full encoded configuration, nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3WriteFieldBit` |
| **Entry 424 — universal-TM-table build: the writer emits the encoding `writeFieldBlock_eq_encode` (PROVED)** | Writer-correctness: the field writer's (entry 422) output is *exactly* the abstract encoding `encodeNatBits3` (entry 392) — the output-side link between the machine's tape and the encoding it must emit (toward `EmitsEncodedStep3`). **`writeFieldBlock_eq_encode`** (PROVED, a pure list lemma) — `n < X.length ⇒ writeFieldBlock (pre ++ X) pre.length n = pre ++ encodeNatBits3 n ++ X.drop (n+1)`: writing a unary field of length `n` at `pre.length` lays `encodeNatBits3 n` over the next `n+1` cells, prefix and tail intact. Proof: induction on `n` generalizing `pre`/`X`, `rcases X = x :: xs`, using `writeAt3_eq_set` (an in-bounds write is a `List.set`) + a self-contained `set_at_boundary` (`(A ++ x :: l).set A.length y = A ++ y :: l`); the successor step uses `encodeNatBits3 (n+1) = I :: encodeNatBits3 n` (via `List.replicate_succ`) and the reassociation `pre ++ (I :: xs) = (pre ++ [I]) ++ xs` before applying the IH at `(pre ++ [I], xs)`. **`writeUnaryFieldN3_emits`** (PROVED, the run corollary) — `∃ N, reachIn N (s, pre.length, pre ++ X) (sOut, pre.length + n, pre ++ encodeNatBits3 n ++ X.drop (n+1))`: running the writer emits the encoded field (entry 422's run + the surgery rewrite). Honest scope: the writer-emits-the-encoding correctness — does **not** yet emit a full encoded configuration, nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3WriteEncode` |
| **Entry 425 — universal-TM-table build: the emitter emits the encoded prefix `writeFieldBit3_emits` (PROVED)** | The field-then-bit emitter (entry 423) emits *exactly* the encoded prefix `encodeNatBits3 n ++ w :: rest` — the shape of a transition/config prefix (`encodeTransBits3` entry 393, `configEncode3` entry 413). **`writeFieldBit3_emits`** (PROVED) — `n+1 < X.length ⇒ ∃ N, reachIn N (s, pre.length, pre ++ X) (sOut, pre.length+n+1, pre ++ encodeNatBits3 n ++ w :: X.drop (n+2))`: running the emitter lays `encodeNatBits3 n ++ [w]` over the next `n+2` cells, prefix and tail intact. Proof: `writeFieldBit3_run` (423) + `writeFieldBlock_eq_encode` (424, the field part equals the encoding) + `List.drop_eq_getElem_cons` (to expose the symbol cell) + `writeAt3_cons_boundary` (the boundary write of the symbol, via `writeAt3_eq_set` + `set_at_boundary`); the index is rewritten to `(pre ++ encodeNatBits3 n).length` and back to avoid disturbing the head position. Honest scope: the emit-the-encoded-prefix correctness — does **not** yet emit a full encoded configuration (a second field for the next state, etc.), nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3EmitPrefix` |
| **Entry 426 — universal-TM-table build: the emitter emits a configuration `writeFieldBit3_emits_config` (PROVED)** | The clean payoff of the emit-side correctness chain: the field-then-bit emitter's output *is* the configuration encoding `configEncode3` (entry 413). **`writeFieldBit3_emits_config`** (PROVED) — `n+1 < X.length ⇒ ∃ N, reachIn N (s, pre.length, pre ++ X) (sOut, pre.length+n+1, pre ++ configEncode3 n sym (X.drop (n+2)))`: running the field-then-bit emitter with the symbol `w = boolToSym3 sym` emits exactly the encoded configuration `configEncode3 n sym _` (state field + current symbol + rest). A short corollary of entry 425: the emitted prefix `encodeNatBits3 n ++ boolToSym3 sym :: rest` is rewritten to `configEncode3 n sym rest` via `[configEncode3, List.append_assoc]`. The output side is now fully linked to the encoding (writer 422 → equals the encoding 424 → field-then-bit 423 → emits the prefix 425 → emits `configEncode3` 426). Honest scope: the emit-a-configuration correctness — does **not** yet connect the emitted config to the abstract simulated *step*, nor assemble `EmitsEncodedStep3` (which also needs the input/transfer side) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3EmitConfig` |
| **Entry 427 — universal-TM-table build: the transfer-side navigation `navigateToSource3` (PROVED)** | The clean addressing solution for the apply *transfer* (the recurring cross-region wall): reach a **data-dependent** rule position with no counter-driven walk. It combines `resetToHome3` (entry 408 — the distance-independent return to the config home `c` via the home marker, which absorbs the data-dependent "where is the head now") with a **fixed-offset** rightward move `moveRightN3 d'` (entry 401, with `d'` a *machine parameter*, exactly as the matcher took the inter-field distance `d`) reaching the rule source `c+d'`. **`navigateToSource3 s found cont mid d'`** — `resetToHome3 s found cont mid ++ moveRightN3 mid d'`. **`navigateToSource3_run`** (PROVED) — with the home marker at `home`, no marker in `(home, home+dq]`, `home+dq < tp.length`, `home+1+d' ≤ tp.length`: `∃ N, reachIn N (s, home+dq, tp) (mid+d', home+1+d', tp)` — from *any* config-window position `home+dq` the head ends at `home+1+d' = c+d'` (the rule source), the tape identical (chains `resetToHome3_run` + `moveRightN3_run` via `reachIn_seq3`). Compiled on the first try. This is how the transfer escapes the unary head-pointer's data-dependent-addressing wall — a home marker plus a parameterized offset, both proven. Honest scope: the transfer-side navigation — does **not** yet copy the rule field there (`copyFieldLeft3`, entry 415), nor assemble the full state transfer / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Navigate` |
| **Entry 428 — universal-TM-table build: the full field transfer `transferFieldLeft3` (PROVED)** | The cross-region field transfer end to end — the recurring wall, crossed: bring a unary field from the matched rule into the configuration. It composes the transfer-side navigation (`navigateToSource3`, entry 427 — reach the data-dependent rule source `c+d'` from anywhere in the config window) with the leftward field copy (`copyFieldLeft3`, entry 415 — copy the field at `c+d'` back to the config at `c`). **`transferFieldLeft3 s found cont mid d' sDone L`** — `navigateToSource3 s found cont mid d' ++ copyFieldLeft3 (mid+d') sDone d' L`. **`transferFieldLeft3_run`** (PROVED) — with the home marker at `home`, no marker in `(home, home+dq]`, budget `m < L`, `1 ≤ d'`, `home+1+d'+m < tp.length`, and the rule field (`m` ones then `O` at the source `home+1+d'`): `∃ N, reachIn N (s, home+dq, tp) (sDone, home+1+d'+m, copyBlockLeft tp (home+1+d') d' m)` — the rule field is copied into the config at `home+1`, from any starting config-window position. Proof: chains `navigateToSource3_run` (427) + `copyFieldLeft3_run` (415) via `reachIn_seq3`, bounds discharged by `omega`. Compiled on the first try. The transfer wall is now crossed with no counter-driven walk — a home marker, a parameterized offset, and a leftward copy, all proven. Honest scope: the full cross-region field transfer (navigate + copy) — does **not** yet clear the old config field before the copy (assumes a clean/sized destination), nor assemble the complete state update or `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Transfer` |
| **Entry 429 — universal-TM-table build: the clearer's content `clearBlock_getD_outside` / `_inside` (PROVED)** | The clearer-correctness lemmas — they characterise the field clearer's (`walkRightClearField3` / `clearBlock`, entry 418) effect, the dual of the writer-correctness lemma (`writeFieldBlock_eq_encode`, entry 424). Needed for the state update to clear the old config field before transfer (428) while preserving a *distant* rule field. **`clearBlock_getD_outside`** (PROVED) — `j < h ∨ h+m ≤ j ⇒ (clearBlock tp h m).getD j O = tp.getD j O`: cells outside the cleared region `[h, h+m)` are preserved. **`clearBlock_getD_inside`** (PROVED) — `h ≤ j → j < h+m ⇒ (clearBlock tp h m).getD j O = O`: cells inside become `O`. Both by induction on `m` generalizing `h`/`tp`; the successor peels `writeAt3 tp h O` via `writeAt3_getD` (`if_neg` outside / `if_pos` at `j=h`), and the inside `j=h` case reuses the outside lemma (`h` is outside `[h+1, h+1+m)`). The outside direction is exactly what shows a distant field (e.g. the rule's new-state field at `home+1+d'`, with `d' ≥ oldlen`) survives clearing the config field. Honest scope: the clearer-correctness content — does **not** by itself assemble the clear-then-transfer state update, nor `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ClearContent` |
| **Entry 430 — universal-TM-table build: the state-field update `clearStateField3` (PROVED)** | The complete simulated-state update of the apply phase: replace the configuration's old state field with the matched rule's new-state field, *handling the length change*. It clears the old field (`walkRightClearField3`, entry 418) then transfers the new one (`transferFieldLeft3`, entry 428). **`clearStateField3 s sMid found cont mid d' sDone L1 L2`** — `walkRightClearField3 s sMid L1 ++ transferFieldLeft3 sMid found cont mid d' sDone L2`. **`clearStateField3_run`** (PROVED) — with a home marker at `home`, no marker elsewhere, `oldlen < d'` (the rule field beyond the cleared region), budgets, bounds, the old config field (`oldlen` ones then `O` at `home+1`) and the rule field (`newlen` ones then `O` at `home+1+d'`): `∃ N, reachIn N (s, home+1, tp) (sDone, home+1+d'+newlen, copyBlockLeft (clearBlock tp (home+1) oldlen) (home+1+d') d' newlen)` — the config state field is cleared and replaced by a copy of the rule's new state. The two phases run on *different* tapes (the transfer runs on the cleared tape), so the cross-tape obligations — the home marker and the distant rule field survive clearing (`clearBlock_getD_outside`), and the cleared cells read `O` so the reset window is marker-free (`clearBlock_getD_inside`) — are discharged with the clearer-content lemmas (entry 429), plus a `clearBlock_length` (the clearer preserves length); chained by `reachIn_seq3` after aligning the intermediate head `home+(oldlen+1) = home+1+oldlen`. Compiled on the first try. Honest scope: the state-field update (clear + transfer) — does **not** yet sequence lookup → apply-tape-step → state-update into one step, nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3StateUpdate` |
| **Entry 431 — universal-TM-table build: navigation to the symbol cache `navigateToCache3` (PROVED)** | Rep-unification addressing: reconcile the matcher's fixed-offset *symbol cache* (the cell after the state field, `c+a+1`) with the marker head representation — after a head move the cache must be refreshed, which first means *reaching* the cache cell from anywhere. **`navigateToCache3 s found cont mid sOut`** — `resetToHome3 s found cont mid ++ scanNatFrom3 mid sOut`. **`navigateToCache3_run`** (PROVED) — with the home marker at `home`, no marker in `(home, home+dq]`, the state field (`a` ones then `O` at `home+1`), and bounds: `∃ N, reachIn N (s, home+dq, tp) (sOut, home+1+a+1, tp)` — from any config-window position the head ends on the cache cell `home+1+a+1 = c+a+1`, tape identical. The key: it reaches the *fixed-offset* cache by *scanning the data-length state field* (`scanNatFrom3`, entry 395, walks to the field's separator without needing `a` statically) after `resetToHome3` (entry 408); chained by `reachIn_seq3`. Compiled on the first try. Honest scope: the navigation to the symbol cache — does **not** yet refresh the cache (read the marker symbol and write it here), nor assemble the full step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3NavigateCache` |
| **Entry 432 — universal-TM-table build: skip a marker leftward `skipMarkLeft3` (PROVED)** | Resolves a genuine coherence conflict surfaced by the assembly: with a single marker symbol `M`, the *home* marker (config navigation, at `c-1`) and the simulated *head* marker (in the tape) are indistinguishable, so a leftward seek from inside the tape stops at the head marker, not the home marker — so `navigateToCache3` (431) can't be called from inside the tape. The single-symbol fix is to **skip** the head marker: seek to it, step one cell past it, so a *second* leftward seek reaches home. **`skipMarkLeft3 s found cont s'`** — `seekMarkLeft s found cont ++ moveLeft3 found s'`. **`skipMarkLeft3_run`** (PROVED) — with the marker at `p`, no marker in `(p, p+d]`, `p+d < tp.length`: `∃ N, reachIn N (s, p+d, tp) (s', p-1, tp)` — from any position right of the marker the head ends just left of it (`p-1`), tape identical (chains `seekMarkLeft_run` (387) + `moveLeft3_run_eq` (386) via `reachIn_seq3`). Compiled on the first try. This is the single-symbol tool to navigate past the head marker toward the home marker (`skipMarkLeft3` head-`M` → `resetToHome3` home-`M` → `scanNatFrom3` cache). Honest scope: the skip-a-marker primitive — does **not** by itself assemble the cache refresh or the full step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3SkipMark` |
| **Entry 433 — universal-TM-table build: the cache-refresh navigation `cacheRefreshNav3` (PROVED)** | The navigation half of the cache refresh: carry the head from *inside* the tape (right of the simulated head marker) to the cache cell, crossing the head marker — the two-marker conflict (entry 432), resolved end to end. **`cacheRefreshNav3 s found1 cont1 s2 found2 cont2 mid sOut`** — `skipMarkLeft3 s found1 cont1 s2 ++ navigateToCache3 s2 found2 cont2 mid sOut`. **`cacheRefreshNav3_run`** (PROVED) — with the head marker at `hm` (and the head at `hm+e` right of it), the home marker at `home < hm`, the state field (`a` ones then `O` at `home+1`), no other markers, and bounds: `∃ N, reachIn N (s, hm+e, tp) (sOut, home+1+a+1, tp)` — from inside the tape the head ends on the cache cell `c+a+1`, tape identical. Proof: `skipMarkLeft3_run` (432, skip the head marker to `hm-1`) then `navigateToCache3_run` (431, from `hm-1 = home+(hm-1-home)` seek past to the home marker and scan the state field to the cache); the intermediate position is aligned by rewriting `home+(hm-1-home) = hm-1`; chained by `reachIn_seq3`. Compiled on the first try. Honest scope: the cache-refresh navigation — does **not** yet *write* the read symbol into the cache, nor assemble the full step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CacheNav` |
| **Entry 434 — universal-TM-table build: navigate-and-write the cache `navigateAndWriteCache3` (PROVED)** | The write end of the cache refresh: after the cache-refresh navigation (`cacheRefreshNav3`, entry 433) carries the head from inside the tape to the cache cell, write a given symbol `w` there (`unmark3`, the write-and-stay primitive). **`navigateAndWriteCache3 s found1 cont1 s2 found2 cont2 mid sMidW sOut2 w`** — `cacheRefreshNav3 s found1 cont1 s2 found2 cont2 mid sMidW ++ unmark3 sMidW sOut2 w`. **`navigateAndWriteCache3_run`** (PROVED) — under the `cacheRefreshNav3` hypotheses: `∃ N, reachIn N (s, hm+e, tp) (sOut2, home+1+a+1, writeAt3 tp (home+1+a+1) w)` — from inside the tape, the head ends on the cache cell and the cache cell now holds `w` (chains `cacheRefreshNav3_run` (433) + `unmark3_run` via `reachIn_seq3`). Compiled on the first try. Honest scope: the symbol-parameterised cache-set — does **not** yet supply `w` from the marker symbol (the O/I branch of `readHeadSym3`, entry 420), nor assemble the full step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CacheWrite` |
| **Entry 435 — universal-TM-table build: the full cache refresh `cacheRefresh3` (PROVED)** | The rep-unification operation **cache := current symbol**, completing the matcher/marker reconciliation. It reads the symbol at the head marker (`readHeadSym3`, entry 420, routing the control to `sO`/`sI`), then on each lineage navigates to the cache and writes that symbol (`navigateAndWriteCache3`, entry 434). A **two-branch assembly**: the machine appends *both* lineage sub-machines after `readHeadSym3`; the relevant one runs and the other is never entered (lifted into the union by `reachIn_append_left3`/`reachIn_append_right3`). **`cacheRefresh3 s sO sI <7 O-internals> <7 I-internals> sFin`** — `readHeadSym3 s sO sI ++ (navigateAndWriteCache3 sO … O ++ navigateAndWriteCache3 sI … I)`. **`cacheRefresh3_run`** (PROVED) — head marker at `h+d` (no marker in `[h, h+d)`), current cell a bit, home marker at `home < h+d`, state field at `home+1`, bounds: `∃ N, reachIn N (s, h, tp) (sFin, home+1+a+1, writeAt3 tp (home+1+a+1) (tp.getD (h+d+1) O))` — the cache cell `c+a+1` is set to the current symbol, both lineages converging to `sFin`. Proof: `readHeadSym3_run` (420) routes to `sO`/`sI`; `rcases` the current bit; per branch `navigateAndWriteCache3_run` (434, with `hm=h+d`, `e=1`) lifted into the union (`append_left` for O / `append_right` for I) then `reachIn_seq3`; the read bit is rewritten to identify the written symbol with the current symbol; `hnoHead` (`k≤1`: the current cell isn't a marker) follows from `hcur`. Compiled on the first try. Honest scope: the full cache refresh — does **not** yet rework the matcher to call it, nor sequence read → lookup → apply → refresh into one step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CacheRefresh` |
| **Entry 436 — universal-TM-table build: advance the head right and return home `headRightReturnHome3` (PROVED)** | Top-level sequencing begins with the **return-to-home discipline** that makes phases compose (each phase ends at the canonical config home `c = home+1`). From the head marker: move it right (`headMoveRight3`, entry 419), skip the relocated marker (`skipMarkLeft3`, entry 432), reset to the config home (`resetToHome3`, entry 408). **`headRightReturnHome3 s s1 f1 c1 s2 f2 c2 out`** — `headMoveRight3 s s1 ++ skipMarkLeft3 s1 f1 c1 s2 ++ resetToHome3 s2 f2 c2 out`. **`headRightReturnHome3_run`** (PROVED) — with the home marker at `home`, the head marker at `p` (`home < p`), no other marker in `(home, p)`, the right neighbour `p+1` a bit, and `p+1 < tp.length`: `∃ N, reachIn N (s, p, tp) (out, home+1, writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M)` — the head marker has advanced to `p+1` and the head has returned to the config home, the tape being the moved tape. Proof: `headMoveRight3_run` (marker `→ p+1`, tape `tp'`) + `skipMarkLeft3_run` (skip the relocated marker at `p+1`, landing at `p`) + `resetToHome3_run` (`p → home+1`); `resetToHome3`'s no-marker window on `tp'` is discharged via `writeAt3_getD` (cells `< p` unchanged so `hclean` applies; cell `p` holds the neighbour symbol which is a bit `≠ M`) plus a length lemma; the intermediate position is aligned by `home+(p-home) = p`; chained by `reachIn_seq3`. Honest scope: the right head move with return-to-home — does **not** yet prepend the home-to-marker seek, branch on move direction, nor assemble the full step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3HeadRightHome` |
| **Entry 437 — universal-TM-table build: advance the head left and return home `headLeftReturnHome3` (PROVED)** | The symmetric companion of entry 436 — the left head move with the return-to-home discipline, the second canonical move phase. From the head marker: move it left (`headMoveLeft3`, entry 419), skip the relocated marker (`skipMarkLeft3`, entry 432), reset to the config home (`resetToHome3`, entry 408). **`headLeftReturnHome3 s s1 f1 c1 s2 f2 c2 out`** — `headMoveLeft3 s s1 ++ skipMarkLeft3 s1 f1 c1 s2 ++ resetToHome3 s2 f2 c2 out`. **`headLeftReturnHome3_run`** (PROVED) — with the home marker at `home`, the head marker at `p` (`home+2 ≤ p`), no other marker in `(home, p)`, the left neighbour `p-1` a bit, and `p < tp.length`: `∃ N, reachIn N (s, p, tp) (out, home+1, writeAt3 (writeAt3 tp p (tp.getD (p-1) O)) (p-1) M)` — the head marker has retreated to `p-1` and the head has returned to the config home, the tape being the moved tape. Proof: `headMoveLeft3_run` (marker `→ p-1`, tape `tp'`) + `skipMarkLeft3_run` (skip the relocated marker at `p-1`, landing at `p-2`) + `resetToHome3_run` (`p-2 → home+1`); the no-marker threading is *simpler* than entry 436 — the reset window `home+1 … p-2` lies entirely below `p-1`, all cells unchanged and in `(home, p)` so `hclean` applies directly (no neighbour-cell case); the positions are aligned by `p-1-1 = p-2` and `home+(p-2-home) = p-2`; chained by `reachIn_seq3`. Compiled on the first try. Honest scope: the left head move with return-to-home — does **not** yet branch on move direction, prepend the home-to-marker seek, nor assemble the full step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3HeadLeftHome` |
| **Entry 438 — universal-TM-table build: the move-direction dispatch `moveHeadDispatch3` (PROVED)** | The simulated head move goes left or right per the matched rule; by apply time that direction is carried in the *control state* (the matcher routes to a move-right or move-left state), so the honest realisation is a **dispatch** — one machine holding both canonical move phases (entries 436, 437), entered at `sR` for right or `sL` for left. The entry state selects the branch; the unused phase is never entered (lifted into the union by `reachIn_append_left3`/`reachIn_append_right3`). Both phases return to `home+1`, so either direction ends canonically. **`moveHeadDispatch3 <8 right states> <8 left states>`** — `headRightReturnHome3 … ++ headLeftReturnHome3 …`. **`moveHeadDispatch3_run_right`** (PROVED) — entered at the right-entry state (with the hypotheses of entry 436): `∃ N, reachIn N (sR, p, tp) (outR, home+1, writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M)` (right move). **`moveHeadDispatch3_run_left`** (PROVED) — entered at the left-entry state (with the hypotheses of entry 437): the left move (`…(p-1)…`). Each is the proven phase run lifted into the union via `reachIn_append_left3` (right) / `reachIn_append_right3` (left). Compiled on the first try. Honest scope: the move-direction dispatch — does **not** yet connect the entry state to the matched rule's move bit, prepend the home-to-marker seek, nor assemble the full step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MoveDispatch` |
| **Entry 439 — universal-TM-table build: the home-to-home right move `moveRightFromHome3` (PROVED)** | The first **home-to-home** phase (start *and* end at the config home `c = home+1`) — the unit of the master step sequence, which chains by `reachIn_seq3` with no positioning glue. From the config home, seek right to the head marker (`seekMarkRight`, entry 388), then advance the head right and return home (`headRightReturnHome3`, entry 436). **`moveRightFromHome3 sStart sFound sCont s1 f1 c1 s2 f2 c2 out`** — `seekMarkRight sStart sFound sCont ++ headRightReturnHome3 sFound s1 f1 c1 s2 f2 c2 out`. **`moveRightFromHome3_run`** (PROVED) — with the home marker at `home`, the head marker at `p` (`home < p`), no other marker in `(home, p)`, the right neighbour `p+1` a bit, and `p+1 < tp.length`: `∃ N, reachIn N (sStart, home+1, tp) (out, home+1, writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M)` — starting at the config home, the head marker advances `p → p+1` and the head returns to the config home. Proof: `seekMarkRight_run` (388, `c → p`, with `d = p-(home+1)`, the `hclear` derived from `hclean`, the endpoint rewritten via `(home+1)+(p-(home+1)) = p`) then `headRightReturnHome3_run` (436); chained by `reachIn_seq3`. Compiled on the first try. Honest scope: the home-to-home right move — does **not** yet do the symmetric left/dispatch home-to-home, chain multiple phases (which needs the moved tape's cleanliness), nor assemble the full step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MoveRightHome` |
| **Entry 440 — universal-TM-table build: the head move preserves the clean configuration `moveRight/Left_preserves_clean` (PROVED)** | The chaining-enabler: the marker invariant (home marker fixed, a single head marker, no marker strictly between) **persists** across a head move, with the head marker relocated — so one home-to-home phase's output meets the *next* phase's hypotheses (pure list lemmas, no `reachIn`). **`moveRight_preserves_clean`** (PROVED) — from `home < p`, the home marker, a clean window `(home, p)`, and `p+1` a bit: the moved tape `writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M` has the home marker at `home`, the head marker at `p+1`, and no marker in `(home, p+1)`. Proof by `writeAt3_getD` per component (home cell: two `if_neg` → `hmarkHome`; cell `p+1`: `if_pos` → `M`; window cells `j < p`: two `if_neg` → `hclean`; cell `j = p`: the written neighbour bit `≠ M` from `hc`). **`moveLeft_preserves_clean`** (PROVED) — the symmetric statement (head marker at `p-1`, no marker in `(home, p-1)`), simpler as the window lies below `p-1` so there is no neighbour-cell case. Compiled on the first try. Honest scope: the invariant-preservation lemmas — they do **not** by themselves chain the phases, nor assemble the full step / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MoveClean` |
| **Entry 441 — universal-TM-table build: chaining two home-to-home phases `twoMoveRight3` (PROVED)** | The first concrete **master-sequence** composition: two home-to-home right-move phases (entry 439) chained, the second phase's preconditions discharged from the first phase's output via the invariant-preservation lemma (entry 440). **`movedRight tp p`** — `writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M` (the tape after one right move). **`twoMoveRight3 <phase-1 states> <phase-2 states>`** — `moveRightFromHome3 … ++ moveRightFromHome3 …`. **`twoMoveRight3_run`** (PROVED) — with the home/head markers, a clean window `(home, p)`, the cells `p+1`,`p+2` bits, and bounds: `∃ N, reachIn N (a0, home+1, tp) (out2, home+1, movedRight (movedRight tp p) (p+1))` — from the config home, the head marker advances `p → p+1 → p+2` and the head returns home. Proof: `moveRightFromHome3_run` (439) for phase 1, then `moveRight_preserves_clean` (440) discharges phase 2's marker hypotheses (with the next-cell bit and length re-derived on `movedRight tp p`), then `moveRightFromHome3_run` again; chained by `reachIn_seq3`. Compiled on the first try. This is the chaining mechanism in miniature (the marker invariant carried across phases). Honest scope: the two-phase chain — does **not** generalise to arbitrary or heterogeneous phases, nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3TwoMove` |
| **Entry 442 — universal-TM-table build: the home-to-home left move `moveLeftFromHome3` (PROVED)** | The symmetric companion of entry 439: from the config home, seek right to the head marker (`seekMarkRight`, 388), then move the head left and return home (`headLeftReturnHome3`, 437). **`moveLeftFromHome3_run`** (PROVED) — home/head markers (`home+2 ≤ p`), clean window, left neighbour a bit, `p < tp.length`: `∃ N, reachIn N (sStart, home+1, tp) (out, home+1, writeAt3 (writeAt3 tp p (tp.getD (p-1) O)) (p-1) M)`. Chained by `reachIn_seq3`; compiled on the first try. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MoveLeftHome` |
| **Entry 443 — universal-TM-table build: the home-to-home move dispatch `moveFromHome3` (PROVED)** | The apply's head-move phase as a single home-to-home dispatch: both home-to-home move phases (entries 439, 442) under one machine, entered at `rS` for right or `lS` for left (direction carried in the control state). **`moveFromHome3 <10 right states> <10 left states>`** — `moveRightFromHome3 … ++ moveLeftFromHome3 …`. **`moveFromHome3_run_right`** / **`moveFromHome3_run_left`** (PROVED) — entered at the respective state, the home-to-home right/left move; each the proven phase lifted via `reachIn_append_left3`/`reachIn_append_right3`. Compiled on the first try. Honest scope: the move phase is now drop-in for either direction; the **single** remaining target is `EmitsEncodedStep3 U φ cost` for a concrete `U`/`φ` (entry 400's `universalSim_of_emits3` reduces the whole UTM to exactly that), which requires the matcher↔`bitLookup` and apply↔`bitApply` correspondence on a concrete layout — a large effort, **not faked / not socketed**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MoveFromHome` |
| **Entry 444 — universal-TM-table build: the home-to-home cache refresh `cacheRefreshHome3` (PROVED)** | The cache refresh (entry 435) made **home-to-home** by appending a reset (`resetToHome3`, 408) from the cache cell back to the config home. **`cacheRefreshHome3 <18 cache-refresh states> rf rc rout`** — `cacheRefresh3 … ++ resetToHome3 sFin rf rc rout`. **`cacheRefreshHome3_run`** (PROVED) — starting at the config home, with the head marker at `(home+1)+d`, the state field at `home+1`, and bounds: `∃ N, reachIn N (s, home+1, tp) (rout, home+1, writeAt3 tp (home+1+a+1) (tp.getD ((home+1)+d+1) O))` — the cache cell is set to the current symbol and the head returns to the config home; the result tape is a single `writeAt3`. Proof: `cacheRefresh3_run` (435, with `h = home+1`) then `resetToHome3_run` (408) on the result tape; the reset's no-marker window `(home, home+1+a+1]` is discharged by the state-field ones (`hco`), the separator (`hcsep`), and the cache cell (the current symbol, `≠ M` from `hcur`) via a `writeAt3_getD` case split; positions aligned by `home+(a+2) = home+1+a+1`; chained by `reachIn_seq3`. The head marker (further right than the cache) is outside the reset window, so untouched. Honest scope: one apply phase with a canonical endpoint — does **not** assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CacheRefreshHome` |
| **Entry 445 — universal-TM-table build: the leftward copier's content `copyBlockLeft_getD_outside/_inside` (PROVED)** | The content of `copyBlockLeft` (entry 415) in the disjoint case `m < d` (destination strictly left of source; holds for the state transfer as the rule field is far, `newlen < d'`). **`copyBlockLeft_getD_outside`** (PROVED) — `m < d → d ≤ c → (k < c-d ∨ c+m-d < k) → (copyBlockLeft tp c d m).getD k O = tp.getD k O`: cells outside the destination `[c-d, c+m-d]` (including the source and the home marker) are preserved. **`copyBlockLeft_getD_inside`** (PROVED) — `m < d → d ≤ c → c-d ≤ k → k ≤ c+m-d → (copyBlockLeft tp c d m).getD k O = tp.getD (k+d) O`: destination cells read the source. Both by induction on `m` (generalizing `tp`/`c`), the successor peeling `writeAt3` at `c-d` via `writeAt3_getD`; the inside boundary `k = c-d` reuses the outside lemma. So a marker-free source yields a marker-free destination. Honest scope: the copier-content lemmas — do **not** by themselves wrap the state update, nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CopyContent` |
| **Entry 446 — universal-TM-table build: the home-to-home state update `clearStateFieldHome3` (PROVED)** | The state update (entry 430) made **home-to-home** by appending a reset (`resetToHome3`, 408); the marker invariant is re-established on the transformed tape `copyBlockLeft (clearBlock tp (home+1) oldlen) (home+1+d') d' newlen` via the copier content (entry 445, disjoint case `newlen < d'`) and the clearer content (entry 429). **`clearStateFieldHome3 <state-update states> rf rc rout`** — `clearStateField3 … ++ resetToHome3 sDone rf rc rout`. **`clearStateFieldHome3_run`** (PROVED) — from the config home, with a single home marker, `oldlen < d'`, `newlen < d'`, budgets, bounds, the old and rule fields: `∃ N, reachIn N (s, home+1, tp) (rout, home+1, copyBlockLeft (clearBlock tp (home+1) oldlen) (home+1+d') d' newlen)` — the config state field is replaced by the rule's new state and the head returns to the config home. The no-marker reset window is discharged in three cases: the destination `[home+1, home+1+newlen]` reads the rule field (`copyBlockLeft_getD_inside`, `≠ M` by `hcnew`/`hcnewsep`), outside-and-cleared reads `O` (`clearBlock_getD_inside`), outside-and-uncleared reads `tp` (`clearBlock_getD_outside`, `≠ M` by `hclean`); plus `copyBlockLeft_length`/`clearBlock_length` for the length bound. Compiled on the first try. Honest scope: one apply phase with a canonical endpoint — does **not** assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3StateUpdateHome` |
| **Entry 447 — universal-TM-table build: the home-to-home symbol write `writeSymHome3` (PROVED)** | The last apply sub-phase made home-to-home: write the matched rule's symbol at the simulated current cell. **`writeSymHome3 <11 states> w`** — `seekMarkRight … ++ moveRight3 … ++ unmark3 … w ++ skipMarkLeft3 … ++ resetToHome3 …`. **`writeSymHome3_run`** (PROVED) — home/head markers (`home < p`), clean window, `w` a bit, `p+1 < tp.length`: `∃ N, reachIn N (a, home+1, tp) (out, home+1, writeAt3 tp (p+1) w)` — the current cell `p+1` is set to `w`, the head marker is unchanged, the head returns to the config home. A 5-piece chain (seek → step → write → skip-marker → reset); the skip/reset hypotheses on `writeAt3 tp (p+1) w` are discharged by `writeAt3_getD` (`p`, `home` ≠ `p+1`; window cells `< p`; `w ≠ M`); chained by `reachIn_seq3`. Compiled on the first try. With this, **all four apply sub-phases are home-to-home** (state-update 446, symbol-write 447, move 443, cache-refresh 444). Honest scope: the master apply chain still needs the marker-model reconciliation (the state update assumed no head marker; the others assume one) plus per-transition invariant threading — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3WriteSymHome` |
| **Entry 448 — universal-TM-table build: the windowed-clean state update `clearStateField3_runW`/`clearStateFieldHome3_runW` (PROVED)** | The marker-model reconciliation. The state update (entries 430, 446) used a *global* no-marker hypothesis (`∀ j ≠ home, ≠ M`) that forbids a head marker — incompatible with the symbol-write/move/cache-refresh phases (which carry a head `M`). The key observation: the update's only use of the global hypothesis was at the old field's separator cell, already pinned to `O` by `hcoldsep`. **`clearStateField3_runW`** (PROVED) — the state update with **no** global clean hypothesis. **`clearStateFieldHome3_runW`** (PROVED) — the home-to-home state update with a **windowed** clean hypothesis `∀ j, home < j → j ≤ home+1+d'+newlen → tp.getD j O ≠ M`, so a head marker beyond `home+1+d'+newlen` (in the simulated tape, right of the rule region) is permitted. Proof: a re-proof of entry 430 swapping `hclean → hcoldsep` at the separator, then `resetToHome3` with the windowed hypothesis for the gap/uncleared cells (3-way no-marker window: destination→rule field, cleared→`O`, uncleared→`hcleanW`); `copyBlockLeft_length`/`clearBlock_length` for the bound. Compiled on the first try. With this, **all four apply phases share one marker model** (home marker + head marker beyond the window). Honest scope: the reconciliation — does **not** yet chain the four into a master apply, nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3StateUpdateWindowed` |
| **Entry 449 — universal-TM-table build: the write-then-move apply core `applyWriteMoveRight3` (PROVED)** | The first heterogeneous **master-apply chain**: the core tape transformation of one simulated TM step (rightward) as one home-to-home machine. Under the marker-right-of-current-cell representation, `writeSymHome3` (447) puts `w` at the current cell `p+1` and `moveRightFromHome3` (439) carries that `w` to cell `p` (preserving it just left of the marker) while relocating the marker to `p+1` — correct "write then step right". **`applyWriteMoveRight3 <write states> mid <move states> w`** — `writeSymHome3 … mid w ++ moveRightFromHome3 mid …`. **`applyWriteMoveRight3_run`** (PROVED) — home/head markers, clean window, `w` a bit, `p+1 < tp.length`: `∃ N, reachIn N (a, home+1, tp) (outM, home+1, writeAt3 (writeAt3 (writeAt3 tp (p+1) w) p w) (p+1) M)`. The second phase's hypotheses are discharged from the first's output tape `writeAt3 tp (p+1) w` via `writeAt3_getD`; chained by `reachIn_seq3`. Compiled on the first try. Honest scope: the write+move core — does **not** yet add cache-refresh (more threading) or state-update (which hits the variable-length unary-field layout issue: changing the state length shifts the layout, needing a tape-shift op not yet built), nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ApplyWriteMove` |
| **Entry 450 — universal-TM-table build: the tape-side master apply `applyTapeRight3` (PROVED)** | The coherent tape-side master apply (rightward): write the rule's symbol, advance the head right, then refresh the symbol cache — **three home-to-home phases** in one machine, chaining the write-then-move core (entry 449) with the home-to-home cache refresh (entry 444), which reads the new current cell `p+2` after the move. **`applyTapeRight3 <write/move states> mid2 <cache states> w`** — `applyWriteMoveRight3 … mid2 w ++ cacheRefreshHome3 mid2 …`. **`applyTapeRight3_run`** (PROVED) — home/head markers, clean window, state field at `home+1` (length `a'`), `w` and cell `p+2` bits, the layout relation `home+1+a'+1 < p` (state field and cache strictly left of the head marker), `p+2 < tp.length`: `∃ N, reachIn N (a, home+1, tp) (rout, home+1, writeAt3 (writeAt3 (writeAt3 (writeAt3 tp (p+1) w) p w) (p+1) M) (home+1+a'+1) (tp.getD (p+2) O))`. The cache-refresh hypotheses are discharged on the post-move tape `T3` via three `getD` facts (`T3.getD (p+1) = M`, `T3.getD p = w`, all others `= tp`), the state field and cache being intact because they lie left of `p`; chained by `reachIn_seq3`. Compiled on the first try. Honest scope: the tape-side step (write+move+cache) — does **not** add the state-update phase (which needs a tape-shift for the variable-length unary state field), nor assemble `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ApplyTape` |
| **Entry 451 — universal-TM-table build: the tape-shift content primitive `copyBlockLeft_getD_*_gen`/`shiftLeftBlock_getD` (PROVED)** | The tape-shift primitive (leftward) the state update needs (the unary state field changes length on a state change, so the rest of the tape must move). The shift *machine* already exists — `copyBlockLeft` (the `copyFieldLeft3` fold, entry 415) with `d ≥ 1` reads each source cell before it is overwritten, so it performs a genuine left shift; only its *overlapping*-case content was missing (entry 445 covered only the disjoint `m < d`, and that hypothesis turned out to be never used). **`copyBlockLeft_getD_outside_gen`** / **`copyBlockLeft_getD_inside_gen`** (PROVED) — the content for *any* `d` (`d ≤ c`): outside the destination preserved, inside reads `tp.getD (k+d)`. **`shiftLeftBlock_getD`** (PROVED) — the shift specialisation: `(copyBlockLeft tp c d m).getD k = if c-d ≤ k ≤ c+m-d then tp.getD (k+d) else tp.getD k`. The proofs are entry 445's minus the `m < d` hypothesis (induction on `m` generalizing `tp`/`c`). Honest scope: the leftward tape-shift content — the machine is `copyFieldLeft3` (415), a rightward shift would need a `copyBlockRight`; does **not** yet assemble the state-update-with-shift nor `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3TapeShift` |
| **Entry 452 — universal-TM-table build: the rightward tape-shift content `copyBlockRight`/`shiftRightBlock_getD` (PROVED)** | The dual of entry 451 for the state-*growth* case: a **rightward** shift. The left-to-right copier `copyBlock` (412) clobbers on an overlapping right shift (it overwrites a source cell before reading it), so a genuine right shift copies **high-to-low**. **`copyBlockRight tp c d m`** — the high-to-low fold (write `c+m → c+m+d` first, recurse on the lower block). **`copyBlockRight_getD_outside`** / **`copyBlockRight_getD_inside`** (PROVED) — for `1 ≤ d`: outside the destination preserved, inside reads `tp.getD (k-d)`. **`shiftRightBlock_getD`** (PROVED) — the shift spec: `(copyBlockRight tp c d m).getD k = if c+d ≤ k ≤ c+m+d then tp.getD (k-d) else tp.getD k`. Induction on `m` generalizing `tp` (the highest cell `k = c+m+1+d` handled via the outside lemma). Honest scope: the **list-level** rightward shift content — the realising right-to-left machine (dual of `copyFieldLeft3`) is **not built here**; does **not** assemble the state-growth update nor `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3TapeShiftRight` |
| **Entry 453 — universal-TM-table build: the right-to-left shift machine `copyFieldRight3` (PROVED)** | The machine realising the rightward tape shift (entry 452); with it, the rightward shift is **complete** (machine 453 + content 452), the dual of the leftward (`copyFieldLeft3` 415 + entry 451). A right shift copies **high-to-low**, so the machine starts at the high cell, copies it rightward by `d` (`copyBitAtDist3`, 411), steps left (`moveLeft3`, 386), and recurses. **`copyFieldRight3 s sDone d n`** — `copyBitAtDist3 s (s+4d+5) d ++ moveLeft3 (s+4d+5)(s+4d+6) ++ copyFieldRight3 (s+4d+6) sDone d n` (base `copyBitAtDist3 s sDone d`). **`copyFieldRight3_run`** (PROVED) — `1 ≤ d`, the block `c … c+n` all bits, `c+n+d < tp.length`: `∃ N, reachIn N (s, c+n, tp) (sDone, c, copyBlockRight tp c d n)` — from the high cell, the block is shifted right by `d`, head ending at `c`. Proof by induction on `n`: `copyBitAtDist3_run` (copy current `→ +d`, head stays) + `moveLeft3_run_eq` + the IH; the per-step tape `writeAt3 tp (c+n+1+d) …` is exactly `copyBlockRight`'s recursion step (defeq), and the bit hypothesis propagates via `writeAt3_getD`. Compiled on the first try. Both tape-shift directions now have machine + content. Honest scope: does **not** assemble the state-update-with-shift nor `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ShiftRightMachine` |
| **Entry 454 — universal-TM-table build: the full state update `stateUpdateFull3` (PROVED)** | The complete simulated-state update handling the variable-length unary state field — **without shifting the marker-containing simulated tape**. The insight: a state-length change only invalidates the *cache* position (`navigateToCache3` scans the state field to its separator), so we **re-place the cache** instead of physically shifting: `clearStateFieldHome3` (448, write the new state field) then `cacheRefreshHome3` (444, read the current symbol from the head marker and write it at the *new* cache cell `home+1+newlen+1`). The old cache cell is harmless gap (the matcher never looks there; the simulated tape is found via the marker seek). So the tape-shift primitives (451–453) turn out **unnecessary** here (still reusable machinery). **`stateUpdateFull3 <state-update states> mid2 <cache states>`** — `clearStateFieldHome3 … mid2 ++ cacheRefreshHome3 mid2 …`. **`stateUpdateFull3_run`** (PROVED) — home marker, head marker at `hm` beyond the rule region (`home+1+d'+newlen < hm`), no other marker in `(home, hm)`, the old/new (rule) state fields, the current cell a bit, bounds: the config state field becomes the rule's new state and the cache is re-placed at the new boundary with the current symbol; head returns home. Proof: `clearStateFieldHome3_runW` (448) then `cacheRefreshHome3_run` (444, `d = hm-(home+1)`, `a = newlen`); the cache-refresh hypotheses are derived on `RT = copyBlockLeft (clearBlock …) …` via the content lemmas (451 general + 429) — `hRT_noM` (3-way: destination→rule field, cleared→`O`, else→`hclean`), the new state field reads the rule field, and far cells read `tp`; plus `copyBlockLeft_length`/`clearBlock_length`. Honest scope: the full state update — does **not** connect to the matched rule / `EmitsEncodedStep3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3StateUpdateFull` |
| **Entry 455 — universal-TM-table build: the matcher ↔ lookup correspondence `recMatch_iff_lookup`/`recMatch_iff_bitLookup` (PROVED)** | The crux bridge between the concrete matcher (`matchTable3`, 410) and the abstract bit-level lookup (`bitLookup`, = `lookup`), at the **condition level**. The matcher's match-existence hypothesis is `∃ rec ∈ recs, RecMatch a cs rec` with `RecMatch a cs (d,b,rs) := a = b ∧ rs = cs`; `lookup` matches a rule `r` when `r.1 = (c.1, readSym c)`. These are the **same condition**: encoding each rule `r` of `M` as a descriptor `(1, r.1.1, boolToSym3 r.1.2)`, the config state `c.1` plays the unary state length `a` and `boolToSym3 (readSym c)` plays `cs`, and `boolToSym3` is injective. **`recsOf M c`** — `M.map (fun r => (1, r.1.1, boolToSym3 r.1.2))`. **`boolToSym3_inj`** (PROVED). **`recMatch_iff_lookup`** (PROVED) — `(∃ rec ∈ recsOf M c, RecMatch c.1 (boolToSym3 (readSym c)) rec) ↔ (lookup M c).isSome`. **`recMatch_iff_bitLookup`** (PROVED) — the same with `bitLookup (encodeMachineBits M) c` (via `bitLookup = lookup`). Proof via `List.find?_isSome`, `List.mem_map`, `boolToSym3_inj`, and the `Prod` projections (`decide_eq_true_eq` for the Bool match). Combined with `matchTable3_run` (410): a rule existing in the encoded table implies the matcher reaches the match-found state. Honest scope: the **condition-level** correspondence (the match conditions coincide) — does **not** assemble the full layout-`φ` / run-level correspondence, which carries the **fixed-`cost` obstruction** of `EmitsEncodedStep3` (a fixed cost cannot bound scan time over unbounded tapes) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MatcherLookup` |
| **Entry 456 — universal-TM-table build: the existence-cost emission lifting `universalSimEx_of_emits3` (PROVED)** | The honest reformulation that dissolves the **fixed-`cost` obstruction** of entry 400. `EmitsEncodedStep3` demanded a single `cost` realising every encoded step — impossible for a real universal machine (per-step transitions grow with the encoded sizes). The fix is an **existence-cost** obligation: each step realised in *some* number of transitions. **`EmitsEncodedStepEx3 U φ`** — `∀ Mbits cbits next, encodedStep Mbits cbits = some next → ∃ N, reachIn (toNTM3 U) N (φ Mbits cbits) (φ Mbits next)`. **`universalSimEx_of_emits3`** (PROVED) — `EmitsEncodedStepEx3 U φ` ⟹ for any `M` and `simIter M k c0 = some cf`, `∃ N, reachIn (toNTM3 U) N (φ (encodeMachineBits M) (encodeConfig c0)) (φ … (encodeConfig cf))` — the whole `k`-step simulation reaches the encoded final config in some number of transitions (by induction on `k`: `encodedStep_correct` + per-step emission + `reachIn_add`; zero case `⟨0, rfl⟩`). Mirrors entry 400 but with `∃ N` instead of `k*cost`. The point: this is the spec the construction can *actually* discharge — every apply phase (entries 404–455) is exactly an `∃ N, reachIn N …` run — so the universal TM reduces to one *achievable* obligation (concrete `U`/`φ` + `EmitsEncodedStepEx3`, assembling the phases). The fixed-cost obstruction was an over-specification, not a real barrier. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3EmitEx` |
| **Entry 457 — universal-TM-table build: the config-region layout `homeConfigTape3` (PROVED)** | The first concrete artifact of the assembly layout `φ`: the **config region** — the home marker followed by the encoded configuration, with exactly the preconditions every phase consumes at `home = 0`, `c = 1`. **`homeConfigTape3 q sym rest`** — `Sym3.M :: configEncode3 q sym rest`. **`homeConfigTape3_content`** (PROVED) — the four invariants: `getD 0 = M` (home marker); `getD (1+i) = I` for `i < q` (state field); `getD (1+q) = O` (separator); `getD (1+q+1) = boolToSym3 sym` (cache). Follows from the prefix-parameterised `configEncode3_content` (entry 413) with prefix `[M]`. Honest scope: the config region only — does **not** add the rule table or the simulated tape's head marker, nor define the full `φ` / prove `EmitsEncodedStepEx3` (the large but obstruction-free remaining assembly, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3HomeLayout` |
| **Entry 458 — universal-TM-table build: the rule-record layout `record_RecOK` (PROVED)** | The rule-table region's per-record bridge: an encoded rule record satisfies the matcher's `RecOK` precondition (entry 410). A rule's key is encoded exactly as a configuration — `configEncode3 b rs rest` — so the matcher reads a record's `(state length, read symbol)` the same way it reads the config key. **`record_RecOK`** (PROVED) — with `pre.length = c + d`, `1 ≤ d`, `min a b < L`, and the bound: `RecOK (pre ++ configEncode3 b rs rest) c a L (d, b, boolToSym3 rs)` — the encoded record at offset `c+d` meets the per-record obligation with key `(b, boolToSym3 rs)`. The content fields are `configEncode3_content` (413) with prefix `pre` (rewriting `pre.length = c+d`). Honest scope: the per-record bridge only — does **not** lay out a *list* of records at cumulative offsets, place the simulated tape's head marker, nor define the full `φ` / prove `EmitsEncodedStepEx3` (large but obstruction-free, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3RecordLayout` |
| **Entry 459 — universal-TM-table build: the records-list cumulative layout `recordsTape3`/`records_RecOK` (PROVED)** | The rule table laid out contiguously (the cumulative-offset structure the matcher walks). Each record `(b,rs)` is `recordBlock (b,rs) = encodeNatBits3 b ++ [boolToSym3 rs]` (length `b+2`); the table is their concatenation, `recordsTape3 recs`. **`configEncode3_eq_recordBlock`** (PROVED) — `configEncode3 b rs X = recordBlock (b,rs) ++ X`. **`recordsTape3_split`** (PROVED) — `recordsTape3 (front ++ (b,rs)::back) = front.flatMap recordBlock ++ configEncode3 b rs (recordsTape3 back)` (induction on `front`; Prod eta is definitional). **`records_RecOK`** (PROVED) — with `(pre ++ front.flatMap recordBlock).length = c+d` and the bounds: `RecOK (pre ++ recordsTape3 (front ++ (b,rs)::back)) c a L (d, b, boolToSym3 rs)` — every record, at its cumulative offset, meets the matcher's per-record obligation (via the split + `record_RecOK`, 458). Honest scope: the rule-table layout — does **not** place the simulated tape's head marker, nor define the full `φ` / prove `EmitsEncodedStepEx3` (large but obstruction-free, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3RecordsLayout` |
| **Entry 460 — universal-TM-table build: the simulated-tape region `simTapeRegion` (PROVED)** | The sim-tape region of `φ`: the abstract TM tape (a `List Bool`) encoded as `Sym3` bits with the head marker `M` before the head cell (marker-right-of-current-cell rep). **`simTapeRegion tp h`** — `(tp.take h).map boolToSym3 ++ M :: (tp.drop h).map boolToSym3` (marker at `h`, current cell at `h+1`). **`mapBool_getElem?_bit`** (PROVED) — a bit-encoded cell reads `O` or `I`. **`simTapeRegion_marker`** (PROVED) — `h ≤ tp.length → getD h = M`. **`simTapeRegion_current`** (PROVED) — `getD (h+1) ∈ {O,I}` (current cell a bit). **`simTapeRegion_clean`** (PROVED) — `h ≤ tp.length → j ≠ h → getD j ≠ M` (no other marker). Proofs via `getD_eq_getElem?_getD` + `getElem?_append_left`/`_right` (arbitrary index + hypothesis) + `getElem?_cons_succ` + the bit helper. Honest scope: the sim-tape region — does **not** stitch config + rule table + sim-tape into the full `φ`, nor define `U` / prove `EmitsEncodedStepEx3` (large but obstruction-free, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3SimTapeLayout` |
| **Entry 461 — universal-TM-table build: the stitched full tape `fullTape3` (PROVED)** | The `φ` stitching of the config region and the rule table: **`fullTape3 a cs rules simtape`** = `M :: configEncode3 a cs (recordsTape3 rules ++ simtape)` (layout `home M | config key | rule table | sim-tape`). **`fullTape3_config`** (PROVED) — the config-key invariants (home, state field, cache), since it is definitionally `homeConfigTape3 …` (457). **`configEncode3_absorb_tail`** (PROVED) — `configEncode3 b rs X ++ Y = configEncode3 b rs (X ++ Y)`. **`fullTape3_record_RecOK`** (PROVED) — every rule record of `rules`, at offset `d = a+2 + (front.flatMap recordBlock).length`, satisfies the matcher's `RecOK` on the *full* tape at `c = 1` (via `recordsTape3_split` (459), the record absorbing the trailing sim-tape, and the per-record bridge (458)). The matcher side of `φ` is now assembled on one tape. Honest scope: does **not** expose the sim-tape head marker at its full-tape offset, define the bit-decoding `φ : List Bool → List Bool → CConfig3`, nor the windowed matcher / `U` / `EmitsEncodedStepEx3` (large but obstruction-free, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3FullLayout` |
| **Entry 462 — universal-TM-table build: the head marker at the full-tape offset `fullTape3_head_marker`/`_current` (PROVED)** | The apply-side stitching: with the sim-tape region (`simTapeRegion`, 460) placed as the trailing region of `fullTape3` (461), the head marker `M` sits at the full-tape offset `(cfgHead a cs ++ recordsTape3 rules).length + h` and the current cell at `+ h + 1` is a bit. **`fullTape3_head_marker`** (PROVED) — `h ≤ simTp.length → (fullTape3 a cs rules (simTapeRegion simTp h)).getD ((cfgHead ++ recordsTape3 rules).length + h) O = M`. **`fullTape3_head_current`** (PROVED) — the cell at `+ h + 1` reads `O` or `I`. Proofs: `fullTape3_eq_cfgHead` + `← append_assoc` + `getElem?_append_right` (with `Nat.le_add_right`) + `Nat.add_sub_cancel_left` + the local invariants (460). The sim-tape head is now located on the stitched tape. Honest scope: does **not** prove the config+rule region marker-free at full offsets (the matcher's windowed cleanliness), nor define the bit-decoding `φ` / `U` / `EmitsEncodedStepEx3` (large but obstruction-free, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3FullHead` |
| **Entry 463 — universal-TM-table build: the config+rule region is marker-free `fullTape3_matcher_clean` (PROVED)** | The matcher's windowed cleanliness on the stitched tape: every cell after the home marker and before the simulated tape is a bit (`O`/`I`), never `M` — the region is built only from `encodeNatBits3`/`boolToSym3`. **`allBits_getD_ne_M`** (PROVED) — an all-`O`/`I` list never reads `M`. **`encodeNatBits3_allBits`**/**`recordsTape3_allBits`**/**`configEncode3_allBits`** (PROVED) — the encodings are bit-only (via `mem_append`/`mem_cons` + `eq_of_mem_replicate` + `boolToSym3` cases). **`configRules_clean`** (PROVED) — `0 < j → (cfgHead a cs ++ recordsTape3 rules).getD j O ≠ M` (the region is `M :: configEncode3 …` with an all-bits tail). **`fullTape3_matcher_clean`** (PROVED) — `0 < j → j < (cfgHead ++ records).length → (fullTape3 …).getD j O ≠ M` (lifting the region cleanliness to the full tape via `getElem?_append_left`), so the matcher can walk the config+rules with the head marker beyond. Honest scope: does **not** re-prove `matchTable3` windowed, nor define `φ` / `U` / `EmitsEncodedStepEx3` (large but obstruction-free, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3RegionClean` |
| **Entry 464 — universal-TM-table build: the windowed rule-table match loop `matchTable3_run_windowed` (PROVED)** | The matcher (`matchTable3`, 410) re-proved with a **windowed** clean hypothesis — a head marker beyond a bound `hm` is permitted (the stitched-tape situation; entry 463 supplies the window). Replaces the global `hclean` (`∀ j ≠ c-1, ≠ M`) with `hcleanW : ∀ j, c-1 < j → j < hm → ≠ M` plus `hHead : ∀ rec ∈ recs, c+a+1+rec.1 < hm` (the records lie before the head marker). **`matchTable3_run_windowed`** (PROVED) — home marker, `hcleanW`, config key, every record `RecOK` and before `hm`, some record matching: `∃ N q, reachIn N (base, c, tp) (recMatch, q, tp)`. The proof mirrors entry 410; the two uses of the clean hypothesis (`recordKeyMatch3_run_windowed` (409) with window `≤ c+a+1+d`, and `resetToHome3` (408) with positions `≤ q ≤ c+a+1`) are discharged from `hcleanW` by `omega` (using `q ≤ c+a+1 < c+a+1+d < hm`). Compiled on the first try. So the matcher now runs on `fullTape3` with the window of entry 463. Honest scope: does **not** define `φ` / `U` / `EmitsEncodedStepEx3` (large but obstruction-free, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MatchTableWin` |
| **Entry 465 — universal-TM-table build: the bit-decoding layout `layoutPhi3` (PROVED)** | The concrete `φ : List Bool → List Bool → CConfig3` for `EmitsEncodedStepEx3` (456): decode the encoded config `cbits` (`decodeConfig`, the same decoder `encodedStep` uses) and machine `Mbits` (`decodeMachineBits`), and build the stitched tape (461) — home marker, config key (the config's state and current symbol), rule table (the decoded machine's keys `M.map (·.1)`), simulated tape (`simTapeRegion`). Control at `initState`, head at the config home `1`. **`layoutPhi3 initState Mbits cbits`** — `(initState, 1, fullTape3 …)`. **`layoutPhi3_wellformed`** (PROVED) — control `= initState`, head `= 1`, home marker at `0`. **`layoutPhi3_config`** (PROVED) — the config-key invariants on the decoded tape (`= fullTape3_config`). So `φ` is now concretely defined and well-formed. Honest scope: does **not** define the global `U`, nor prove `EmitsEncodedStepEx3 U layoutPhi3` (the end-to-end run, large but obstruction-free, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Phi` |
| **Entry 466 — universal-TM-table build: the lookup phase on the stitched tape `phiLookup_fullTape` (PROVED)** | The first step toward the global `U`: the rule-table matcher actually runs on the concrete `fullTape3`. Instantiates `matchTable3_run_windowed` (464) on `fullTape3 a csBool rules simtape` at `c = 1`, discharging every layout hypothesis from the proven invariants — the home marker and config key (`fullTape3_config`, 461), the windowed cleanliness (`fullTape3_matcher_clean`, 463, window bound `(cfgHead ++ recordsTape3 rules).length`, head marker beyond), and `boolToSym3 csBool ∈ {O,I}`. **`phiLookup_fullTape`** (PROVED) — given descriptors `recs` each `RecOK`, before the head marker, with one matching `(a, boolToSym3 csBool)`: `∃ N q, reachIn N (base, 1, fullTape3 …) (recMatch, q, fullTape3 …)` — the matcher reaches the match-found state. Compiled on the first try. The lookup phase is grounded on the concrete tape. Honest scope: does **not** construct the descriptor list, nor assemble the global `U` / `EmitsEncodedStepEx3` (large but obstruction-free, per entry 456) — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3PhiLookup` |
| **Entry 467 — universal-TM-table build: the descriptor list `descriptorsOf` / `descriptorsOf_match` (PROVED) + the fixed-`U` gap finding** | The matcher's record descriptors (cumulative offsets) for a rule-key list. **`descriptorsOf a rules`** — `descriptorsAux a 0 rules` (each record's distance = `a+2` plus the prior records' total length). **`descriptorsOf_match`** (PROVED) — `(∃ rec ∈ descriptorsOf a rules, RecMatch a (boolToSym3 csBool) rec) ↔ (∃ p ∈ rules, a = p.1 ∧ p.2 = csBool)` (the offset is irrelevant to `RecMatch`; induction + `boolToSym3` injectivity). **HONEST ARCHITECTURAL FINDING (not faked over):** `matchTable3` (entry 410) is *unrolled* — recursion over the record list, one transition block per record — so its machine **size grows with the rule count**. Hence `matchTable3` (and any `U` built from it) is **per-machine-compiled, NOT a fixed universal machine**. `EmitsEncodedStep(Ex)3` requires a *single fixed* `U` for all `Mbits`; a true fixed `U` needs a **generic tape-driven scan loop** (a fixed-size machine reading each record from the tape and looping), a separate construction not built in this arc. The 404–466 arc proves the unrolled-model lookup correctness on `φ`; the fixed-`U` requires the generic loop. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Descriptors` |
| **Entry 468 — generic scan loop: the unary-consume component `skipOnesRight` (PROVED)** | The first fixed-state component of the generic rule-table scan loop a *fixed* universal `U` needs (per the fixed-`U` finding, entry 467; the unrolled `matchTable3` is not fixed). Records are self-delimiting (a unary `I`-field, the separator `O`, a symbol), so a generic loop must consume a unary field with a *constant* number of states. **`branchOne3 s sCont sStop`** (3 rules, stay in place) — `I → sCont`, `O`/`M → sStop`; `branchOne3_run_one`/`branchOne3_run_stop` PROVED (mirroring `branchMark3`). **`skipOnesRight s stop cont`** = `branchOne3 s cont stop ++ moveRight3 cont s` (a fixed 3-state machine with a back-edge `cont → s`). **`skipOnesRight_run`** (PROVED) — with `h … h+d-1` all `I` and `h+d` non-`I`: `∃ N, reachIn N (s, h, tp) (stop, h+d, tp)`, tape identical — by induction on `d` (data), the machine fixed. Mirrors `seekMarkRight` (388). Compiled on the first try. This is the fixed-machine / data-dependent-steps pattern, now for unary fields. Honest scope: does **not** build the per-record key comparison, record-advance, table-end test, the full scan loop, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3SkipOnes` |
| **Entry 469 — generic scan loop: record-advance `advanceRecord3` (PROVED)** | The second fixed-state component. A record is a unary `I`-field (length `b`), the separator `O`, then a symbol — `b+2` cells. **`advanceRecord3 s m1 m2 stop cont`** = `skipOnesRight s m1 cont ++ moveRight3 m1 m2 ++ moveRight3 m2 stop`. **`advanceRecord3_run`** (PROVED) — with `h … h+b-1` all `I`, `h+b = O`, `h+b+1 < tp.length`: `∃ N, reachIn N (s, h, tp) (stop, h+b+2, tp)` — the head reaches the next record start, regardless of field length (`skipOnesRight` 468 + two `moveRight3`, chained by `reachIn_seq3`). Compiled on the first try. Honest scope: does **not** build the per-record key comparison, the table-end test, the full scan loop, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3AdvanceRecord` |
| **Entry 470 — generic scan loop: leftward unary-consume `skipOnesLeft` (PROVED)** | The leftward mirror of `skipOnesRight` (468), for the comparison shuttle. **`skipOnesLeft s stop cont`** = `branchOne3 s cont stop ++ moveLeft3 cont s` (fixed 3-state, back-edge). **`skipOnesLeft_run`** (PROVED) — with `p+1 … p+d` all `I`, `p` non-`I`, `p+d < len`: `∃ N, reachIn N (s, p+d, tp) (stop, p, tp)` — induction on `d`, machine fixed (mirroring `seekMarkLeft` 387). Compiled on the first try. The bidirectional unary-consume pair is complete. Honest scope: does **not** build the per-record key comparison, the table-end test, the full scan loop, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3SkipOnesL` |
| **Entry 471 — generic scan loop: the record-walk skeleton `skipRecordsToEnd` (PROVED)** | The architectural heart of the fixed-size loop: a *single fixed* machine walks the whole rule table to the terminator `M`. **`skipRecordsToEnd s done advE m1 m2 cont`** = `branchMark3 s done advE ++ advanceRecord3 advE m1 m2 s cont` (at each record start: `M → done` exit, else advance to the next record and loop via a back-edge to `s`). **`skipRecordsToEnd_run`** (PROVED) — for any `recs` and prefix `pre`, on `tp = pre ++ recordsTape3 recs ++ M :: tail`: `∃ N, reachIn N (s, pre.length, tp) (done, (pre ++ recordsTape3 recs).length, tp)` — **by induction on the record count**, machine fixed. Per-record content via `configEncode3_content` (413) + `configEncode3_absorb_tail` (461) + the `recordBlock` split (459); the branch and advance chained by `reachIn_add`. This proves the fixed-loop-over-records *works* — the backbone into which the comparison branch will be inserted. Honest scope: does **not** build the per-record key comparison (the 3-symbol relative shuttle), the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ScanLoop` |
| **Entry 472 — generic scan loop: the cursor-advance `markAdvance3` (PROVED)** | The core operation of the comparison shuttle (tape-preserving). A cursor is a marker `M` on a unary field; advancing it restores the marked cell to `I`, moves the marker one cell right, and detects whether the field continues or ends. **`markAdvance3 s smid sCont sEnd`** (3 rules) — at `M`: restore `I`, move right; then `I → ` re-mark (`→ sCont`), `O → ` stop (`→ sEnd`). **`markAdvance3_run_cont`** (PROVED) — `tp[j]=M`, `tp[j+1]=I` ⇒ `reachIn 2 (s,j,tp) (sCont, j+1, writeAt3 (writeAt3 tp j I) (j+1) M)`. **`markAdvance3_run_end`** (PROVED) — `tp[j]=M`, `tp[j+1]=O` ⇒ `reachIn 2 → (sEnd, j+1, writeAt3 (writeAt3 tp j I) (j+1) O)`. Two-step `concreteStep3` chains via `reachIn_add` + `writeAt3_getD`. The tape-preserving cursor step for unary comparison. Honest scope: does **not** build the two-field comparison loop, the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3MarkAdvance` |
| **Entry 473 — generic scan loop: the inter-cursor shuttle `crossRight`/`crossLeft` (PROVED)** | The head-transport of the comparison shuttle: cross from one cursor to its partner across the marker-free gap (step off the current cursor, then seek to the partner marker). **`crossRight s mid found cont`** = `moveRight3 s mid ++ seekMarkRight mid found cont`; **`crossLeft`** = `moveLeft3 ++ seekMarkLeft`. **`crossRight_run`** (PROVED) — cursor at `p`, partner `M` at `p+1+d`, cells `p+1 … p+d` marker-free ⇒ `∃ N, reachIn N (s,p,tp) (found, p+1+d, tp)`. **`crossLeft_run`** (PROVED) — cursor at `q+d+1`, partner `M` at `q`, cells `q+1 … q+d` marker-free ⇒ `∃ N, reachIn N (s,q+d+1,tp) (found, q, tp)`. Compositions of move + seek (388/387), chained by `reachIn_seq3`. Compiled on the first try. Honest scope: does **not** build the two-field comparison loop, the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3Cross` |
| **Entry 474 — generic scan loop: the half-tandem move `advanceCrossRight` (PROVED)** | Fuses cursor-advance (472) and inter-cursor cross (473): advance the cursor one cell within its field, then cross right to the partner cursor — the reusable iteration half of the two-cursor comparison loop. **`advanceCrossRight s smid sCont sEnd crMid crFound crCont`** = `markAdvance3 s smid sCont sEnd ++ crossRight sCont crMid crFound crCont`. **`advanceCrossRight_cont_run`** (PROVED) — cursor at `cp` (`M`), field continuing (`cp+1=I`), partner `M` at `cp+2+d`, gap `cp+2 … cp+1+d` marker-free ⇒ `∃ N, reachIn N (s,cp,tp) (crFound, cp+2+d, writeAt3 (writeAt3 tp cp I) (cp+1) M)`. `markAdvance3_run_cont` + `crossRight_run` chained by `reachIn_seq3`; marker-freedom on the modified tape via `writeAt3_getD`; a `writeAt3` length helper. Honest scope: does **not** build the leftward mirror, the full tandem loop, the four-way end-branch, the symbol compare, the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3AdvanceCross` |
| **Entry 475 — generic scan loop: the leftward half-tandem move `advanceCrossLeft` (PROVED)** | The mirror of 474: advance the right cursor one cell, then cross left back to the partner cursor. `advanceCrossRight` then `advanceCrossLeft` form one full tandem iteration (both cursors step right, head back on the left cursor). **`advanceCrossLeft s smid sCont sEnd crMid crFound crCont`** = `markAdvance3 s smid sCont sEnd ++ crossLeft sCont crMid crFound crCont`. **`advanceCrossLeft_cont_run`** (PROVED) — `1 ≤ d`, record cursor at `pc+d` (`M`), field continuing (`pc+d+1=I`), partner `M` at `pc`, gap `pc+1 … pc+d-1` marker-free ⇒ `∃ N, reachIn N (s,pc+d,tp) (crFound, pc, writeAt3 (writeAt3 tp (pc+d) I) (pc+d+1) M)`. `markAdvance3_run_cont` + `crossLeft_run` chained by `reachIn_seq3`; the `hclear` splits `k<d` (gap) / `k=d` (restored `I`). The half-tandem pair is complete. Honest scope: does **not** build the full tandem loop, the four-way end-branch, the symbol compare, the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3AdvanceCrossL` |
| **Entry 476 — generic scan loop: the comparison loop body `tandemStep3` (PROVED)** | One full tandem iteration fuses `advanceCrossRight` (474) and `advanceCrossLeft` (475): both unary-field cursors step right by one, the head returning to the config cursor — the constant-gap two-cursor walk comparing two unary fields cell-by-cell. **`tandemStep3 …`** (13 state params) = `advanceCrossRight s … crFound … ++ advanceCrossLeft crFound …`. **`tandemStep3_run`** (PROVED) — config cursor at `cp` (`M`, `cp+1=I`), record cursor at `cp+g` (`M`, `g ≥ 2`, `cp+g+1=I`), inter-cursor gap marker-free ⇒ `∃ N, reachIn N (s,cp,tp) (lcrFound, cp+1, tp₂)` with `tp₂` moving *both* cursors right one. 474 + 475 chained by `reachIn_seq3`; 475's hypotheses on the post-474 tape `tp1` discharged via `writeAt3_getD` (the partner `tp1[cp+1]=M` via `if_pos`). The comparison loop body is proven. Honest scope: does **not** iterate it (induction on `min(a,b)`), nor build the four-way end-branch, the symbol compare, the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3TandemStep` |
| **Entry 477 — generic scan loop: in-bounds tape-write algebra `writeAt3_eq_set`/`_overwrite`/`_comm` (PROVED)** | Reusable tape algebra for iterating the tandem comparison step (476, whose output is a nested `writeAt3`). In bounds `writeAt3` adds no padding and equals `List.set`. **`writeAt3_eq_set`** (PROVED) — `p < t.length → writeAt3 t p w = t.set p w`. **`writeAt3_overwrite`** (PROVED) — `p < t.length → writeAt3 (writeAt3 t p a) p b = writeAt3 t p b` (last write wins, `List.set_set`). **`writeAt3_comm`** (PROVED) — `p ≠ q → p,q < t.length → writeAt3 (writeAt3 t p a) q b = writeAt3 (writeAt3 t q b) p a` (`List.set_comm`). Unblocks the iteration's tape-tracking. Honest scope: does **not** iterate `tandemStep3`, nor build the four-way end-branch, the symbol compare, the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3WriteAlg` |
| **Entry 478 — generic scan loop: the canonical comparison tape `cursTape` cell lemmas (PROVED)** | The two-cursor comparison state in canonical form for the iteration. **`cursTape tp cp g i`** = `writeAt3 (writeAt3 tp (cp+i) M) (cp+g+i) M`. **`cursTape_cursorL`** (PROVED, `g≥1`) — `getD (cp+i) = M`. **`cursTape_cursorR`** (PROVED) — `getD (cp+g+i) = M`. **`cursTape_other`** (PROVED) — `x≠cp+i, x≠cp+g+i ⇒ getD x = tp.getD x`. **`cursTape_length`** (PROVED, in bounds) — `length = tp.length`. Via `writeAt3_getD` + `writeAt3_eq_set` (477). These feed `tandemStep3`'s hypotheses across the iteration. Honest scope: does **not** prove one canonical step (the nested-write collapse), iterate `tandemStep3`, nor build the four-way end-branch, the symbol compare, the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3CursTape` |
| **Entry 479 — generic scan loop: one canonical tandem step `tandemStepCanon_run` (PROVED)** | One full tandem iteration (476) maps the canonical tape `cursTape` (478) at step `i` to step `i+1` — both cursors advance one. The post-step six-layer `writeAt3` tape is shown `= cursTape … (i+1)` by **`List.ext_getElem`** (`getD` extensionality via `writeAt3_getD`, then `split_ifs <;> simp_all <;> omega`) — robust where the nested-`rw` normalization was fragile. **`tandemStepCanon_run`** (PROVED) — base field cells `I`, gap marker-free, in bounds ⇒ `∃ N, reachIn N (s, cp+i, cursTape tp cp g i) (lcrFound, cp+i+1, cursTape tp cp g (i+1))`. `tandemStep3_run` + the `cursTape` cell lemmas discharge the hypotheses; `ext_getElem` does the tape collapse; length via a `wlen` chain. The per-step collapse is done. Honest scope: does **not** iterate it (induction on the step count), nor build the four-way end-branch, the symbol compare, the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3TandemCanon` |
| **Entry 480 — generic scan loop: the iterated tandem comparison `tandemLoop` (PROVED)** | Closes the comparison loop with a back-edge (`lcrFound := s`): one tandem step (479) returns to the loop head `s`, so `d` iterations walk both cursors from step `0` to step `d`. **`tandemLoop …`** = `tandemStep3 … (lcrFound := s) …`. **`tandemLoop_run`** (PROVED) — for `d` iterations, with the base tape's two fields all `I` over `cp … cp+d` and `cp+g … cp+g+d`, the inter-field region marker-free, and in bounds: `∃ N, reachIn N (s, cp, cursTape tp cp g 0) (s, cp+d, cursTape tp cp g d)` — **by induction on `d`**, machine fixed (chaining `tandemStepCanon_run` via `reachIn_add`; hypotheses are monotone in `d`). Compiled on the first try. The distance-agnostic unary comparison loop is done. Honest scope: does **not** build the four-way end-branch (match vs mismatch at the field ends), the symbol compare, the match-or-advance branch, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3TandemLoop` |
| **Entry 481 — generic scan loop: the comparison match verdict `endMatch` (PROVED)** | The positive verdict (`a = b`): when both unary fields end at the same step, the comparison confirms a match and restores the tape. **`endMatch s mid1 cont1 e1 m2 sc2 mid3 matchSt recEnd`** = `markAdvance3 s mid1 cont1 e1 ++ seekMarkRight e1 m2 sc2 ++ markAdvance3 m2 mid3 recEnd matchSt` (config cursor onto its separator [end-case, removing it], seek to record cursor, record cursor onto its separator [end → `matchSt`]). **`endMatch_run`** (PROVED) — last field cells `I` (`cp+d`, `cp+g+d`), both separators `O`, gap marker-free, in bounds ⇒ `∃ N, reachIn N (s, cp+d, cursTape tp cp g d) (matchSt, cp+g+d+1, tp)` — the tape restored to base. `markAdvance3_run_end` ×2 (472) + `seekMarkRight_run` (388) chained by `reachIn_seq3`; final tape `= tp` via `List.ext_getElem` collapse. The comparison's `a = b` conclusion is done. Honest scope: does **not** build the mismatch branches, the symbol compare, the match-or-advance branch into the scan loop, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3EndMatch` |
| **Entry 482 — generic scan loop: a comparison mismatch verdict `endMismatchCR` (PROVED)** | The negative verdict where the config field ends but the record continues (`a < b`). **`eraseMark3 s fail`** = `[(s,M)→(fail,I,stay)]` (erase a cursor; `eraseMark3_run` PROVED). **`endMismatchCR …`** = `markAdvance3 (config) ++ seekMarkRight ++ markAdvance3 (record) ++ eraseMark3` (config cursor onto sep [end, removed], seek to record cursor, record cursor advances [cont, leaves a cursor], erase the leftover). **`endMismatchCR_run`** (PROVED) — config last `I` + sep `O`, record last `I` + *continues* (`I` ahead), gap marker-free, in bounds ⇒ `∃ N, reachIn N (s, cp+d, cursTape tp cp g d) (failSt, cp+g+d+1, tp)` — the tape restored. `markAdvance3_run_end` + `seekMarkRight_run` + `markAdvance3_run_cont` + `eraseMark3_run` chained by `reachIn_seq3` ×3; final tape `= tp` via `List.ext_getElem` (the `cp+g+d+1` write `M→I` overwrites). The `a < b` verdict is done. Honest scope: does **not** build the symmetric mismatch (`a > b`), the symbol compare, the match-or-advance into the scan loop, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3EndMismatch` |
| **Entry 483 — generic scan loop: the symmetric mismatch verdict `endMismatchRC` (PROVED)** | The third/final length-verdict: config field continues but record ends (`a > b`); the leftover cursor is the config one. **`endMismatchRC …`** = `markAdvance3 (config,cont) ++ crossRight ++ markAdvance3 (record,end) ++ seekMarkLeft ++ eraseMark3` (config advances, cross to the record cursor, record onto its sep [removed], seek *left* back to the config cursor, erase it). **`endMismatchRC_run`** (PROVED) — config last `I` + *continues*, record last `I` + sep `O`, gap marker-free, in bounds ⇒ `∃ N, reachIn N (s, cp+d, cursTape tp cp g d) (failSt, cp+d+1, tp)` — the tape restored. A 5-machine compose (`markAdvance3_run_cont` + `crossRight_run` + `markAdvance3_run_end` + `seekMarkLeft_run` + `eraseMark3_run`) chained by `reachIn_seq3` ×4; the `seekMarkLeft` cells via a trichotomy; final tape `= tp` via `List.ext_getElem`. **All three comparison verdicts (`=`, `<`, `>`) are now done, each restoring the tape.** Honest scope: does **not** build the symbol compare, the match-or-advance into the scan loop, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3EndMismatchRC` |
| **Entry 484 — generic scan loop: structural symbol read `readSymbolAt` (PROVED)** | Reach and read a record's one-cell symbol generically — the symbol-compare's read half (the verdict half is just `branchOne3`). **`readSymbolAt s m1 cont m2 sI sO`** = `skipOnesRight s m1 cont ++ moveRight3 m1 m2 ++ branchOne3 m2 sI sO` (consume the unary field to its terminator, step, branch on the symbol → `sI`/`sO`; no marker — the value is carried in the control state). **`readSymbolAt_run_I`** (PROVED) — `a` ones, sep `O`, symbol `I` ⇒ `∃ N, reachIn N (s, fs, tp) (sI, fs+a+1, tp)`. **`readSymbolAt_run_O`** (PROVED) — symbol `O` ⇒ reaches `sO`. `skipOnesRight_run` (468) + `moveRight3_run_eq` + `branchOne3_run_one`/`_stop` chained by `reachIn_seq3`. Honest scope: does **not** combine config+record symbol reads into the symbol compare, wire match-or-advance into the scan loop, build the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ReadSymbol` |
| **Entry 485 — generic scan loop: return to the config start `gotoConfigStart` (PROVED)** | Navigation glue: reposition the head at the config field start (cell `1`) via the home marker. **`gotoConfigStart s mid found cont`** = `seekMarkLeft s mid cont ++ moveRight3 mid found` (seek left to home `M` at `0`, step right to `1`). **`gotoConfigStart_run`** (PROVED) — home marker at `0`, cells `1 … p` marker-free, `p < len` ⇒ `∃ N, reachIn N (s, p, tp) (found, 1, tp)`. `seekMarkLeft_run` (387) + `moveRight3_run_eq` chained by `reachIn_seq3`. Compiled on the first try. Honest scope: does **not** combine config+record symbol reads (via this glue) into the symbol compare, wire match-or-advance into the scan loop, build the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3GotoConfig` |
| **Entry 486 — generic scan loop: read the config symbol `configSymbolVerdict` (PROVED)** | The config-side half of the symbol compare: from a record position, navigate to the config start (`gotoConfigStart`, 485) and read the config symbol (`readSymbolAt`, 484), routing on its value. **`configSymbolVerdict s gm gf gc rm rc rm2 sI sO`** = `gotoConfigStart s gm gf gc ++ readSymbolAt gf rm rc rm2 sI sO`. **`configSymbolVerdict_run_I`** (PROVED) — home marker at `0`, region clean to `p`, config field `ac` ones + sep `O` + symbol `I` ⇒ `∃ N, reachIn N (s, p, tp) (sI, 1+ac+1, tp)`. **`configSymbolVerdict_run_O`** (PROVED) — config symbol `O` ⇒ reaches `sO`. `gotoConfigStart_run` + `readSymbolAt_run_I`/`_O` chained by `reachIn_seq3`. Compiled on the first try. Honest scope: does **not** prefix the record-symbol read into the full symbol compare, wire match-or-advance into the scan loop, build the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3ConfigSymbol` |
| **Entry 487 — generic scan loop: the full symbol compare `symbolCompare` (PROVED)** | The complete one-cell symbol comparison of two records. **`symbolCompare …`** = `readSymbolAt (record) ++ configSymbolVerdict (rec=I: I↦match,O↦fail) ++ configSymbolVerdict (rec=O: I↦fail,O↦match)`. **`symbolCompare_run`** (PROVED) — record symbol `boolToSym3 rsym`, config `boolToSym3 csym` (with field/home/clean hypotheses) ⇒ `∃ N, reachIn N (s, fsR, tp) ((if rsym = csym then matchSt else failSt), 1+ac+1, tp)` — reaching `matchSt` iff the symbols are equal. Four cases (`rsym × csym`): `readSymbolAt_run_I`/`_O` (484) + `configSymbolVerdict_run_I`/`_O` (486) chained by `reachIn_seq3`/`reachIn_append_left3`; `boolToSym3` reductions via `rfl`, verdict via `if_pos`/`if_neg`. The symbol half of `RecMatch` is done (the state half is 480–483). Honest scope: does **not** combine state+symbol into the full record comparison, wire match-or-advance into the scan loop, build the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3SymbolCompare` |
| **Entry 488 — generic scan loop: cursor placement `placeCursor` (PROVED)** | Setup primitive: write a comparison cursor `M` onto a field cell (turns the real rule table into the canonical `cursTape` the comparison machines consume); the inverse of `eraseMark3` (482). **`placeCursor s s'`** = `[((s,I),(s',M,stay))]`. **`placeCursor_run`** (PROVED) — `tp.getD j O = I ⇒ reachIn 1 (s,j,tp) (s', j, writeAt3 tp j M)`. A one-step `concreteStep3`. `[propext]` (tightest axiom set yet). Honest scope: does **not** place both cursors and run the full record comparison, wire match-or-advance into the scan loop, build the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3PlaceCursor` |
| **Entry 489 — generic scan loop: the match-tail connector `endMatchTail` (PROVED)** | `endMatch` (481) minus its config step, stated generically over the tape — the reusable tail to attach after a config-end step in the branching comparison loop. **`endMatchTail s skf skc rm rc matchSt`** = `seekMarkRight s skf skc ++ markAdvance3 skf rm rc matchSt`. **`endMatchTail_run`** (PROVED) — head at `q`, record cursor `M` at `r ≥ q`, record separator `O` at `r+1`, span `[q, r)` marker-free, in bounds ⇒ `∃ N, reachIn N (s, q, t) (matchSt, r+1, writeAt3 (writeAt3 t r I) (r+1) O)` — the record cursor removed. `seekMarkRight_run` (388) + `markAdvance3_run_end` (472) chained by `reachIn_seq3`. Compiled on the first try. Honest scope: does **not** build the branching loop (tandem walk + verdict exits), the full record comparison, the scan-loop wiring, the generic apply, nor a fixed `U` / `EmitsEncodedStepEx3` — **not faked**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalTM3EndMatchTail` |
| **Brick A.1 — general composite-`MOD_m` CRT residue decomposition `modm_iff_residues` (PROVED)** | Pivot from the fixed-`U` plumbing to the actual Williams content (general YBT). The Beigel–Tarui integer/product-observer move, generalised from the tree's `MOD₆` to **arbitrary** modulus `m`. **`dvd_iff_primePow_residues`** (PROVED) — `m≠0 → (m∣k ↔ ∀ p∈m.primeFactors, p^{m.factorization p}∣k)` (via `Nat.factorization_le_iff_dvd` + `Prime.pow_dvd_iff_le_factorization` + `ordProj_dvd`). **`modm_iff_residues`** (PROVED) — `m≠0 → (modm m x ↔ ∀ p∈m.primeFactors, (hammingWeight x : ZMod (p^{m.factorization p}))=0)`: the general CRT residue decomposition (`ZMod.natCast_eq_zero_iff` per prime power). The CRT-reduction half of the single composite gate; the single-field RS method is *proven dead* for composite `m` (`CompositeBTTarget`), so this is the right object. Honest scope: does **not** prove the per-prime-power polylog-degree bound (Toda/RS symmetrization), the quasipoly `SYM∘AND` packaging, nor degree-additive depth composition — i.e. general YBT / `composite_BT_degree` remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeMODFactor` |
| **Brick A.2 — single prime-`MOD_p` gate as a degree-`(p-1)` `F_p` polynomial `modp_iff_fermat` (PROVED)** | The Razborov–Smolensky/Fermat seed, representing A.1's `e=1` residue terms. **`prime_pow_zero_iff`** (PROVED) — in `ZMod p`: `y=0 ↔ y^{p-1}=0` (`zero_pow` + `ZMod.pow_card_sub_one_eq_one`). **`hammingWeight_cast`** (PROVED) — `(hammingWeight x : ZMod p) = ∑ i, if x i then 1 else 0` (the *linear* form; `card_filter` + `Nat.cast_sum`). **`modp_iff_fermat`** (PROVED) — `(hammingWeight x : ZMod p)=0 ↔ (∑ i, if x i then (1:ZMod p) else 0)^{p-1}=0`: the exact degree-`(p-1)` `F_p` representation of one prime-`MOD` gate (a linear form raised to `p-1`). Compiled on the first try. A.1+A.2 together give a low-degree representation of every **squarefree** composite-`MOD` gate (`MOD₆`, `MOD₃₀`, …). Honest scope: does **not** prove the prime-power (`e≥2`) Toda lifting, a formal `MvPolynomial` degree-membership, the quasipoly `SYM∘AND` packaging, nor degree-additive depth composition — i.e. general YBT / `composite_BT_degree` remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimeMODLowDegree` |
| **Brick A.2b — squarefree composite-`MOD_m` as a product of degree-`(p-1)` `F_p` polynomials `modm_squarefree_fermat` (PROVED)** | Assembles A.1 (CRT) ∘ A.2 (Fermat) into one explicit theorem for an entire family of composite gates. **`squarefree_dvd_primeFactors`** (PROVED) — `m≠0, Squarefree m ⇒ (m∣k ↔ ∀p∈primeFactors, p∣k)` (via `dvd_iff_primePow_residues` (A.1) + `squarefree_iff_factorization_le_one`: `factorization p=1 ⇒ p^1=p`). **`modm_squarefree_fermat`** (PROVED) — `m≠0, Squarefree m ⇒ (modm m x ↔ ∀p∈primeFactors, (∑ i, if x i then (1:ZMod p) else 0)^{p-1}=0)`: the squarefree composite gate as a product of degree-`(p-1)` `F_p` polynomials (`natCast_eq_zero_iff` + `modp_iff_fermat` per prime). Compiled on the first try. The squarefree composite case (`MOD₆`, `MOD₃₀`, `MOD₁₀₅`, …) is now **fully low-degree-representable**. Honest scope: does **not** prove the prime-power (`e≥2`) Toda lifting, a formal `MvPolynomial` degree-membership, the quasipoly `SYM∘AND` packaging, nor degree-additive depth composition — i.e. general YBT / `composite_BT_degree` remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SquarefreeMOD` |
| **Brick A.2c — `MOD_p` indicator as an `MvPolynomial` of total degree `≤ p-1` `modp_iff_modpPoly` (PROVED)** | Makes the degree claim a *typed* theorem (Mathlib `MvPolynomial.totalDegree`). **`modpPoly p n`** = `(∑ᵢ Xᵢ)^{p-1} : MvPolynomial (Fin n) (ZMod p)`. **`modpPoly_totalDegree_le`** (PROVED) — `(modpPoly p n).totalDegree ≤ p-1` (`totalDegree_pow` + `totalDegree_finset_sum` + `totalDegree_X` [`Nontrivial` from `Fact p.Prime`] + `Nat.mul_one`). **`modpPoly_eval`** (PROVED) — `eval (fun i => if x i then 1 else 0) (modpPoly p n) = (∑ if x i then 1 else 0)^{p-1}` (`map_pow` + `map_sum` + `eval_X`). **`modp_iff_modpPoly`** (PROVED) — `(hammingWeight x : ZMod p)=0 ↔ eval … modpPoly = 0` (via A.2). Compiled on the first try. The degree bound is now a theorem, ready to feed the degree-additive composition. Honest scope: does **not** prove the prime-power (`e≥2`) Toda lifting, the quasipoly `SYM∘AND` packaging, nor degree-additive depth composition — i.e. general YBT / `composite_BT_degree` remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimeMODMvPoly` |
| **Brick D — quasipolynomial monomial count `degLeMonomials_card_le` (PROVED)** | The size guarantee of `SYM∘AND`: a degree-`≤ D` polynomial has at most `(n+1)^D` monomials = quasipoly for `D = polylog` (`< 2^n`). **`degLeMonomials n D`** = `univ.powerset.filter (·.card ≤ D)` (subsets of `Fin n` of size `≤ D` = `AND`-terms of fan-in `≤ D`). **`degLeMonomials_card_le`** (PROVED) — `(degLeMonomials n D).card ≤ (n+1)^D`, via a **surjection** from `Fin D → Option (Fin n)` (list a subset's `≤ D` elements via `toList[k]?`, pad with `none`); `card_le_card_of_surjOn` + `Fintype.card_fun` + `getElem?` membership. Compiled on the first try. The quasipoly heart of the YBT packaging. Honest scope: does **not** assemble the full symmetric-count `SYM∘AND` form, the prime-power (`e≥2`) Toda lifting, nor degree-additive depth composition — i.e. general YBT / `composite_BT_degree` remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MonomialCount` |
| **Brick D-assembly — a single `MOD_m` gate has an exact `SYM∘AND` form `modGate_hasExactSymAndForm` (PROVED)** | The full `SYM∘AND` assembly for a single gate (the easy case): `MOD_m` is a symmetric function of the weight = `gateCount` of the `n` singleton `AND`s (`monoAND {i} = xᵢ`). **`monoAND_singleton`** (PROVED) — `monoAND {i} x = x i`. **`gateCount_singletons`** (PROVED) — `gateCount (singletons) x = hammingWeight x`. **`modGate_eq_symEval`** (PROVED) — `(fun x => decide (m ∣ hammingWeight x)) = symEval (singletons) (fun c => decide (m ∣ c))`. **`modGate_hasExactSymAndForm`** (PROVED) — the `HasExactSymAndForm` *shape* for the `MOD_m` function: `∃ M mono h, modGate = symEval (monoAND∘mono) h ∧ M+1 < 2^n` (`M = n`, `n+1 < 2^n`). Honest point: a single gate is *trivially* `SYM∘AND` (degree-1, `n` terms); the YBT difficulty is **depth composition** (where degree → polylog and the count → Brick D's `(n+1)^D`), not the single gate. Honest scope: does **not** handle composed circuits (depth ≥ 2), the prime-power (`e≥2`) Toda lifting, degree-additive depth composition, nor the `ACC0Circuit eval` wrap — i.e. general YBT / `composite_BT_degree` remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModSymAndForm` |
| **Brick C — degree composes multiplicatively under substitution `totalDegree_bind₁_le` (PROVED)** | The genuine YBT depth-reduction core. The tree had proven naive composition over the *gate count* blows up doubly-exponentially (`ACC0Composition`); the *right* parameter is the **degree**, which composes multiplicatively. **`totalDegree_bind₁_le`** (PROVED) — `(∀i, (g i).totalDegree ≤ d) ⇒ (bind₁ g P).totalDegree ≤ P.totalDegree · d` (via `bind₁ = ∑ C·∏(g i)^{e i}`, `totalDegree_finsetSum_le` + `totalDegree_mul` + `totalDegree_finset_prod` + `totalDegree_pow` + `le_totalDegree`; `gcongr`). **`totalDegree_bind₁_comp_le`** (PROVED) — two layers ⇒ `≤ P.totalDegree · dh · dg`. So over constant depth `d`, degree stays `≤ D^d` (polylog), and Brick D bounds the monomials by `(n+1)^{D^d}` (quasipoly) — the degree control the gate-count blow-up missed. Honest scope: does **not** handle cross-prime composition (needs the product observer, A.1), the prime-power (`e≥2`) Toda lifting, nor wiring `degree^d` + Brick D + the `SYM∘AND` form into `composite_BT_degree` over an `ACC0Circuit` — i.e. general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DegreeCompose` |
| **Brick C-iterate — depth-`d` composition keeps degree `≤ D^d` `totalDegree_bind₁_iterate` (PROVED)** | The depth-degree wiring. **`totalDegree_bind₁_iterate`** (PROVED) — `(∀i, (g i).totalDegree ≤ D) ⇒ ((bind₁ g)^[d] P).totalDegree ≤ P.totalDegree · D^d` (induction on `d` over Brick C: `iterate_succ_apply'` + `pow_succ` + `gcongr`). For constant `d`, polylog `D` ⇒ degree polylog ⇒ Brick D caps monomials at `(n+1)^{D^d}` (quasipoly). **Cross-prime:** Brick C and this iterate hold over *any* `CommSemiring`, including the CRT product ring `∏ ZMod pᵢ` (A.1), so different-modulus composition needs **no new degree lemma**. Honest scope: does **not** prove the prime-power (`e≥2`) Toda lifting (the genuinely hard remaining classical theorem — *not* socketed), nor the final `ACC0Circuit`-level assembly of `degree^d` + Brick D + the `SYM∘AND` form into `composite_BT_degree` — i.e. general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DepthDegree` |
| **Brick A.3 — prime-power `MOD_{p^e}`: single gate done, composition obstruction honest (PROVED + honest)** | The prime-power case handled *without* faking. **`primePowerMod_iff_residue`** (PROVED) — `modm (p^e) x ↔ (hammingWeight x : ZMod (p^e))=0`. **`primePowerMod_hasExactSymAndForm`** (PROVED) — a single `MOD_{p^e}` gate is exact `SYM∘AND` (it's a symmetric function of the weight, so Brick D-assembly, which holds for any modulus, applies). With A.1 ⇒ **every** single composite-`MOD` gate (any modulus) is exact `SYM∘AND` — the single-gate YBT is closed. **Honest obstruction (NOT socketed):** the composition-level low-degree lift for `e ≥ 2` has *no* clean `F_p`/`ZMod (p^e)` representation (no Fermat in a non-field; RS-barrier-adjacent); BT uses a different integer/symmetric method, genuinely open and deliberately *not* faked. Honest scope: composition-level prime-power lift + final `ACC0Circuit` assembly = general YBT, remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerMOD` |
| **Brick C∘A.2c — same-prime constant-depth `MOD_p` circuit bounded degree `modp_composition_degree` (PROVED)** | A concrete assembly: the YBT degree machinery composing end-to-end on a real (restricted) circuit class. A depth-`(d+1)` uniform same-prime `MOD_p` circuit = `d` iterated substitutions of `modpPoly p n = (∑ᵢ Xᵢ)^{p-1}` (A.2c). **`modp_composition_degree`** (PROVED) — `((bind₁ (fun _ => modpPoly p n))^[d] (modpPoly p n)).totalDegree ≤ (p-1)^{d+1}` (Brick C-iterate at A.2c's per-gate degree; `calc` + `gcongr` + `pow_succ`). Constant `d, p` ⇒ constant degree ⇒ (post-multilinearization, Brick D) polynomial-size `SYM∘AND`. Compiled on the first try. No socket. Honest scope: does **not** include the multilinearization → Brick D step, AND/OR gates (exact degree = fan-in, needs the approximate RS polynomial), cross-prime nesting, prime-power composition, nor the `ACC0Circuit`-level `composite_BT_degree` — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModpCompositionDegree` |
| **Bridge — degree ⇒ quasipolynomial `AND`-term count `composed_andTerms_card_le` (PROVED)** | The multilinearization bridge connecting Brick C (degree) to Brick D (`AND`-term count). On Boolean inputs each monomial `∏Xᵢ^{eᵢ}` collapses to the `AND` of its variable-set `e.support` (`xᵢ^k=xᵢ`). **`support_card_le_totalDegree`** (PROVED) — `e ∈ P.support ⇒ e.support.card ≤ P.totalDegree` (`card_eq_sum_ones` + `sum_le_sum` + `le_totalDegree`). **`andTerms_card_le`** (PROVED) — `P.totalDegree ≤ D ⇒ (P.support.image (·.support)).card ≤ (n+1)^D` (image ⊆ `degLeMonomials`, `card_le_card` + Brick D). **`composed_andTerms_card_le`** (PROVED) — depth-`d` composition ⇒ `≤ (n+1)^{P.totalDegree·D^d}` `AND`-terms (the bridge ∘ C-iterate), quasipoly for constant `d` — the `SYM∘AND` size guarantee. Compiled on the first try. The degree(C) → `AND`-term-count(D) bridge is closed. Honest scope: does **not** include `AND`/`OR` approximate RS polynomials, cross-prime nesting, prime-power composition, nor the `ACC0Circuit`-level `composite_BT_degree` — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Multilinearize` |
| **Brick (AND) — the `AND` gate as an exact polynomial `∏ Xᵢ` `andPoly_eval` (PROVED)** | The exact `AND`/`OR` representation. **`andPoly S`** = `∏_{i∈S} Xᵢ`. **`andPoly_eval`** (PROVED) — `eval (fun i => if x i then 1 else 0) (andPoly S) = if (∀ i ∈ S, x i = true) then 1 else 0` (exactly `AND` of the bits in `S`; `map_prod` + `eval_X` + `prod_eq_one`/`prod_eq_zero`). **`andPoly_totalDegree_le`** (`[Nontrivial R]`, PROVED) — `(andPoly S).totalDegree ≤ S.card` (degree at most the fan-in). Compiled on the first try. Honest point: the exact `AND` degree is the fan-in; under composition it multiplies as `fan-in^depth` (not polylog), which is *why* YBT needs the **approximate** RS probabilistic polynomial for `AND`/`OR` — a genuinely different method, open and *not* faked. Honest scope: does **not** include the approximate `AND`/`OR` polynomial, cross-prime nesting, prime-power composition, nor `composite_BT_degree` — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AndPoly` |
| **Brick (OR-approx) — RS random-linear-form `OR` approximator `orApprox_eval_of_form_ne_zero` (PROVED, deterministic core)** | The genuine low-degree approximate `OR` over `F_p`. **`orApprox p n a`** = `(∑ᵢ C(aᵢ)·Xᵢ)^{p-1}`. **`orApprox_totalDegree_le`** (PROVED) — `totalDegree ≤ p-1`, *independent of fan-in*. **`orApprox_eval`** (PROVED) — `eval` at Boolean `x` = `(∑ᵢ aᵢ[xᵢ])^{p-1}`. **`orApprox_eval_allFalse`** (PROVED) — all-false ⇒ `eval=0` (= `OR`). **`orApprox_eval_of_form_ne_zero`** (PROVED) — `∑ aᵢ[xᵢ] ≠ 0 ⇒ eval=1` (= `OR`; Fermat). Compiled on the first try. A *single* random linear form computes `OR` **exactly off the bad set** `{x≠0 : ∑aᵢxᵢ=0}`, at degree `p-1` not fan-in — the deterministic skeleton of the RS probabilistic polynomial that makes `AND`/`OR` low-degree under composition (`AND` via De Morgan). Honest scope: does **not** include the probabilistic bad-set bound (`≤ 1/p`), `t`-fold amplification, existence of a good polynomial, cross-prime nesting, prime-power composition, nor `composite_BT_degree` — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0OrApprox` |
| **Brick (balancedness) — a nonzero linear form over `F_p` vanishes with probability `1/p` `card_linearForm_eq_zero` (PROVED)** | The genuine *probabilistic heart* of the RS `OR` approximator. **`card_linearForm_eq_zero`** (PROVED) — `x ≠ 0 ⇒ (univ.filter (fun a => ∑ᵢ xᵢ·aᵢ = 0)).card = p^{n-1}` (the zero-fiber is a `1/p` fraction). Proof via rank-nullity: the functional `L = ∑ᵢ xᵢ•proj i` is surjective (some `xⱼ≠0`, `Pi.single j (c·xⱼ⁻¹)` hits `c`), so `finrank range = 1`, `finrank ker = n-1` (`finrank_range_add_finrank_ker`), `card ker = p^{n-1}` (`card_eq_pow_finrank` + `ZMod.card`); the filter is the kernel (`ext` + `mem_ker`). Compiled clean. With `orApprox_eval_of_form_ne_zero`: a *single* random linear form computes `OR` with error `≤ 1/p`. Honest scope: does **not** include `t`-fold amplification (error `p^{-t}`, degree `t(p-1)`), existence of a globally-good polynomial, prime-power composition, nor `composite_BT_degree` — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Balancedness` |
| **Brick (amplify) — `t`-fold amplification shrinks the bad set to `p^{-t}` `card_allBad_eq` (PROVED)** | The amplification step of RS `OR`. **`card_allBad_eq`** (PROVED) — `x ≠ 0 ⇒ (univ.filter (fun a : Fin t → (Fin n → F_p) => ∀ j, ∑ᵢ xᵢ·(aⱼ)ᵢ = 0)).card = (p^{n-1})^t`: the all-bad `t`-tuples (every form vanishes) are the product of the single-form bad sets, hence `(single bad)^t` — a `p^{-t}` fraction of all `(p^n)^t` tuples. Proof: the filter `= Fintype.piFinset` (constant `univ.filter P`) via `ext` + `mem_piFinset`; then `Fintype.card_piFinset` (∏) + balancedness + `prod_const`. So `t` independent forms compute `OR` on each nonzero input with error `≤ p^{-t}` at degree `t(p-1)` — independence multiplies the error. Honest scope: does **not** include the union bound / existence of a globally-good polynomial (`2^n · p^{-t} < 1`), prime-power composition, nor `composite_BT_degree` — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Amplify` |
| **Brick (union bound) — existence of one globally-good random-form choice `exists_good_form` (PROVED)** | The probabilistic-method completion of RS `OR`. **`exists_good_form`** (PROVED) — `X` nonzero vectors, `X.card·(p^{n-1})^t < (p^n)^t ⇒ ∃ a : Fin t → (Fin n → F_p), ∀ x ∈ X, ∃ j, ∑ᵢ xᵢ·(aⱼ)ᵢ ≠ 0`: one tuple good on *every* test input at once. Proof: the bad set `= X.biUnion (bad_x)`, `card ≤ ∑ card(bad_x) = X.card·(p^{n-1})^t` (`card_biUnion_le` + amplify + `sum_const`) `< (p^n)^t =` `card univ` ⇒ bad `≠` univ (`eq_univ_iff_forall`) ⇒ `∃ a ∉` bad ⇒ good. Threshold `|X| < p^t` (e.g. `X` = nonzero Boolean inputs, `|X| ≤ 2^n`, `t` with `p^t > 2^n`) gives one *fixed* degree-`t(p-1)` polynomial correct on all inputs — the RS approximate `OR` *exists*. Honest scope: does **not** package it as a single `MvPolynomial` with degree certificate, prime-power composition, nor `composite_BT_degree` — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnionBound` |
| **Brick (OR package) — one degree-`t(p-1)` polynomial computing `OR` everywhere `exists_orPoly` (PROVED)** | Packages the full RS approximate-`OR`. **`orPoly a`** = `1 - ∏ⱼ(1 - orApprox aⱼ)`. **`orApprox_eval_gen`** (PROVED) — eval at general `F_p` input `= (∑ᵢ aᵢxᵢ)^{p-1}`. **`orPoly_totalDegree_le`** (PROVED) — `≤ t·(p-1)` (`totalDegree_sub`+`max_le`, `totalDegree_finset_prod`, `sum_const`). **`orPoly_eval_eq_one`** (PROVED) — `(∃j, ∑ᵢ(aⱼ)ᵢxᵢ≠0) ⇒ eval x = 1` (Fermat ⇒ zero factor ⇒ `prod_eq_zero`). **`orPoly_eval_zero`** (PROVED) — `eval 0 = 0`. **`exists_orPoly`** (PROVED) — `X` nonzero, `|X|·(p^{n-1})^t<(p^n)^t ⇒ ∃ P, totalDegree ≤ t(p-1) ∧ (∀x∈X, eval x P=1) ∧ eval 0 P=0` (via `exists_good_form`). Compiled on the first try. Taking `X` = nonzero inputs and `t` with `p^t > |X|`: one *fixed* degree-`t(p-1)` polynomial computes `OR` correctly everywhere — the fully-assembled RS approximate-`OR`. Honest scope: does **not** do prime-power `MOD_{p^e}` composition (A.3 obstruction — no clean `F_p` form, *not faked*) nor the `ACC0Circuit`-level `composite_BT_degree` — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0OrPolyPackage` |
| **Brick (circuit repr) — faithful polynomial representation of the `AND`/`OR`/`NOT` fragment `repr_eval` (PROVED)** | The model bridge `ACC0Circuit → MvPolynomial`. **`ModFree`** (no `mod` gates = `AC⁰`), **`repr`** (`const→C(0/1)`, `var→Xᵢ`, `not→1-c`, `and→a·b`, `or→a+b-a·b`), **`reprDeg`** (leaf measure). **`repr_eval`** (PROVED) — `ModFree C ⇒ eval (bv∘x) (repr C) = bv (ACC0Circuit.eval C x)` on Boolean inputs (induction; `bv_not`/`bv_and`/`bv_or` helpers; `map_sub/add/mul` + `eval_X`). **`repr_totalDegree_le`** (PROVED) — `(repr C).totalDegree ≤ reprDeg C`. The exact `circuit.eval = poly.eval` connection. Honest scope: `MOD` gates are *excluded* — `MOD_q` over `F_p` (`q≠p`) is *not* low-degree (RS), `q=p^e` (`e≥2`) has the A.3 obstruction, so a faithful single-field representation of the full circuit does *not* exist; quasipolynomial (approximate) degree and the full `composite_BT_degree` remain **open**, *not* faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CircuitRepr` |
| **Brick (approx wiring) — depth-`d` composition of approximate-`OR` keeps degree `≤ (t(p-1))^{d+1}` `orPoly_composition_degree` (PROVED)** | The degree side of the RS approximate-polynomial wiring through a circuit. **`orPoly_composition_degree`** (PROVED) — `((bind₁ (fun _ => orPoly p n t a))^[d] (orPoly p n t a)).totalDegree ≤ (t·(p-1))^{d+1}` (C-iterate at `orPoly`'s per-gate degree; `calc`+`gcongr`+`pow_succ`). The approximate analogue of `modp_composition_degree`: for `t` polylog and `d` constant, degree is polylog, hence `(n+1)^{(t(p-1))^{d+1}}` `AND`-terms (quasipoly) via the bridge. Honest scope: does **not** carry the per-gate *error* accounting through the recursion (the union over gates × inputs — that combinator is `exists_good_form`), `MOD`-gate handling, nor the full faithful RS-for-circuits `composite_BT_degree` — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0OrPolyComposition` |
| **Brick (AC⁰[p] repr) — recursive faithful representation of AC⁰[p] circuits over `F_p` `reprP_eval` (PROVED)** | The recursive faithful circuit representation (the RS setting). **`ModpOnly`** (every `mod` gate has modulus `p`), **`reprP`** (`const`/`var`/`not`/`and`/`or` + **`mod p S t → 1-(∑_{i∈S} Xᵢ - C(cast t))^{p-1}`**, the Fermat indicator), **`reprDegP`** (`mod→p-1`). **`reprP_eval`** (PROVED) — `ModpOnly p C ⇒ eval (bv∘x) (reprP p C) = bv (ACC0Circuit.eval C x)` *exactly* on all Booleans (induction; mod case: `weight_cast` + `by_cases (weightOn:ZMod p)=t` + Fermat `pow_card_sub_one_eq_one`/`zero_pow`). **`reprP_totalDegree_le`** (PROVED) — `≤ reprDegP`. Binary gates ⇒ exact, no approximation; constant depth ⇒ constant degree. This is the object RS runs on: `AC⁰[p]` is exactly low-degree over `F_p`, while `MOD_q` (`q≠p`) is *not* — the lower bound's content, not a gap. Honest scope: does **not** include prime-power composition, large-fan-in approximation wiring, nor the `composite_BT_degree` cash-out — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CircuitReprP` |
| **Brick (AC⁰[p] cash-out) — AC⁰[p] circuits use `≤ (n+1)^{reprDegP C}` `AND`-terms `reprP_andTerms_card_le` (PROVED)** | The genuine *size* cash-out of the proven bricks: `reprP` (AC⁰[p] representation) ∘ `andTerms_card_le` (degree→AND-term bridge). **`reprP_andTerms_card_le`** (PROVED) — `((reprP p C).support.image (·.support)).card ≤ (n+1)^{reprDegP p C}`. **`reprP_andTerms_lt`** (PROVED) — `(n+1)^{reprDegP p C}<2^n ⇒ count < 2^n` (searchable cell bound). Polynomial for constant depth, `< 2^n` under the degree budget — exactly the size quantity the Williams/Route-B counting step consumes, discharged with *real math* for `AC⁰[p]`. Honest scope: does **not** include the abstract conditional `mod6_composite_route_to_NEXP_not_ACC0` (`RSRep` = the open `composite_BT_degree` + `counting`/`williams`/`hierarchy`, P≠NP-strength), nor `MOD_q`(`q≠p`)/prime-power gates — general YBT and the full Williams cash-out remain **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ReprPAndTerms` |
| **Brick (SYM∘AND form) — AC⁰[p] value as an `F_p`-weighted sum over `AND`-gates `reprP_eq_sum_andTerms` (PROVED)** | The `SYM∘AND` form object built from `reprP`. **`andVal S x`** = `∏_{i∈S} bv(xᵢ)`; **`andVal_eq_bv_monoAND`** (PROVED) — `andVal S x = bv(monoAND S x)` (the `AND`-gate value). **`eval_eq_sum_andTerms`** (PROVED) — `eval (bv∘x) P = ∑ e∈support, coeff e P · andVal(e.support) x` (`MvPolynomial.eval_eq`; per-monomial `(bv xᵢ)^{eᵢ}=bv xᵢ`). **`reprP_eq_sum_andTerms`** (PROVED) — `ModpOnly p C ⇒ bv(eval C x) = ∑ e, coeff e (reprP p C)·andVal(e.support) x` (`reprP_eval` ∘ `eval_eq_sum_andTerms`). The `AC⁰[p]` circuit value is an `F_p`-linear (`SYM`) sum over `AND`-gates `monoAND(e.support)`, fan-in `≤ reprDegP C`, `≤ (n+1)^{reprDegP C}` terms — faithful on all Booleans = the `SYM∘AND` form. Honest scope: does **not** handle `MOD_q`(`q≠p`)/prime-power gates, nor the abstract Williams cash-out — general YBT remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymAndForm` |
| **Brick (count-symEval) — AC⁰[p] value as a count-based symmetric function `reprP_symEval` (PROVED)** | The count-based `symEval` repackaging. **`repCount P x`** = `∑ e∈P.support, (coeff e P).val * (if monoAND e.support x then 1 else 0)` (plain count of coefficient-replicated `AND`-gates). **`repCount_cast`** (PROVED) — `(repCount P x : ZMod p) = eval (bv∘x) P` (`Nat.cast_sum` + `ZMod.natCast_zmod_val` + `andVal_eq_bv_monoAND` + `eval_eq_sum_andTerms`). **`reprP_symEval`** (PROVED) — `ModpOnly p C ⇒ ACC0Circuit.eval C x = decide((repCount (reprP p C) x : ZMod p) = 1)`: the circuit value as `h(count)` with `h(c)=decide((c:ZMod p)=1)` (`cases eval C x` + `zero_ne_one`). The `AC⁰[p]` output depends only on *how many* replicated `AND`-gates accept, read mod `p` — the count-based `symEval` shape the tree's `symEval`/`HasExactSymAndForm` requires. Honest scope: uses `repCount` (the count statistic); the literal `Fin m → Finset` re-indexing is the next brick. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymEvalRepack` |
| **Brick (Fin m symEval) — the literal `HasExactSymAndForm` object for `AC⁰[p]` `reprP_hasSymAndForm` (PROVED)** | Realises `repCount` as the tree's literal `Fin m` `gateCount`. **`weightedCount_realizable`** (PROVED) — `∃ m mono, m=∑w ∧ ∀y, (∑ j:Fin m, if monoAND (mono j) y then 1 else 0) = ∑ e∈S, w e · [monoAND e.support y]` (replicate each `AND`-gate by its weight via `Idx=(e:↥S)×Fin(w e.val)`, `eqv:Fin(card)≃Idx`; `Equiv.sum_comp` + `univ_sigma_univ`/`sum_sigma` + `sum_const` + `sum_coe_sort`). **`reprP_hasSymAndForm`** (PROVED) — `ModpOnly p C`, `(∑(coeff).val)+1<2^n ⇒ ∃ m mono hh, ACC0Circuit.eval C = symEval (monoAND∘mono) hh ∧ m+1<2^n` (`hh c = decide((c:ZMod p)=1)`; `gateCount=repCount` via `weightedCount` + `reprP_symEval`). The exact `SYM∘AND` normal-form object, completed for `AC⁰[p]`. Honest scope: does **not** handle `MOD_q`(`q≠p`)/prime-power gates (RS/A.3 obstruction — no clean `F_p` object) nor the Williams cash-out (P≠NP-strength) — general YBT and `NEXP ⊄ ACC⁰` remain **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymEvalFinM` |
| **Brick (low-degree barrier) — RS barrier from the `AC⁰[p]` representation `not_acc0p_of_no_lowdeg_repr` (PROVED)** | Turns the representation into the lower-bound direction. **`acc0p_low_degree_repr`** (PROVED) — `ModpOnly p C ⇒ ∃ P, totalDegree ≤ reprDegP p C ∧ ∀x, eval(bv∘x) P = bv(eval C x)`. **`not_acc0p_of_no_lowdeg_repr`** (PROVED) — `f` has *no* degree-`≤D` `F_p` representation `⇒ ¬∃ C, ModpOnly p C ∧ reprDegP p C ≤ D ∧ eval C = f` (contrapositive of `reprP_eval`+`reprP_totalDegree_le`). The RS low-degree barrier: `AC⁰[p] ⊆ low-degree`, so anything not low-degree escapes `AC⁰[p]`; feeding the not-low-degree witness for `MOD_q`(`q≠p`) gives `MOD_q ∉ AC⁰[p]`. Honest scope: the witness itself (the RS lower bound — `hno` here, *not* re-proved) + `MOD_q`/prime-power gates inside circuits + the Williams cash-out remain **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LowDegreeBarrier` |
| **Brick (counting witness) — some function has no low-degree `F_p` representation `exists_no_lowdeg_repr` (PROVED)** | The Shannon/counting not-low-degree witness in the barrier's shape. **`degLeMonomials_card_lt`** (PROVED) — `D<n ⇒ (degLeMonomials n D).card < 2^n` (strict subset of powerset). **`exists_no_lowdeg_repr`** (PROVED) — `D<n ⇒ ∃ g, ¬∃ P, totalDegree ≤ D ∧ ∀x eval(bv∘x) P = g x` (every representable `g` lies in the image of `c ↦ ∑ c S · andVal S` over `degLeMonomials`, via `eval_eq_sum_andTerms` + `sum_fiberwise_of_maps_to`; `card image ≤ p^{|degLeMonomials|} < p^{2^n}` = card all functions). Feeds the low-degree barrier: some function `∉` bounded-degree `AC⁰[p]`. Honest scope: this is the **counting** (non-explicit) witness; the **explicit** `MOD_q` witness is the Razborov–Smolensky lower bound (deep analytic — tree's `Layer4.mod_q_indicators_false` / `Layer3Smolensky.parity_function_lower_bound` in `BoolCircuitSyntax` form), *not* re-proved or faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CountingWitness` |
| **Brick (parity witness) — an *explicit* function with no low-degree `F_p` representation `signWt_no_lowdeg_repr` (PROVED)** | The explicit Razborov–Smolensky witness (`MOD_2` in `±1` form, odd `p`). **`signWt p x`** = `∏ᵢ hsign(xᵢ)` (`hsign b = if b then -1 else 1` = `(-1)^{weight}`). **`signWt_sq`** (PROVED) — `signWt²=1`. **`signWt_toggle`** (PROVED) — `signWt(toggle i₀ x) = -signWt x`. **`Dsign_andVal_zero`** (PROVED) — `S≠univ ⇒ ∑ₓ signWt x · andVal S x = 0` (toggle-bit involution: `Equiv.sum_comp`, sign flips, `andVal` fixed ⇒ `∑=-∑ ⇒ ∑=0`, `2≠0`). **`Dsign_signWt_ne`** (PROVED) — `∑ₓ signWt² = (2^n:ZMod p) ≠ 0`. **`signWt_no_lowdeg_repr`** (PROVED) — `p≠2, D<n ⇒ ¬∃ P, P.totalDegree ≤ D ∧ ∀x eval(bv∘x)P = signWt x` (`Dsign` kills low-degree via `eval_eq_sum_andTerms` + `e.support≠univ`, but `Dsign(signWt)=2^n≠0`). The **explicit** witness (not just counting). Honest scope: general `MOD_q` (`q>2`) is the full RS bound (tree's `Layer4`); the Boolean barrier instance follows via the affine `signWt = 1−2·parity`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ParityWitness` |
| **Brick (parity barrier) — `MOD_2 ∉` bounded-degree `AC⁰[p]` for odd `p` `parityFn_not_acc0p` (PROVED)** | The concrete `q=2` Razborov–Smolensky separation in-framework. **`parityFn x`** = `decide(Odd (wt x))` (Boolean `MOD_2`). **`signWt_eq_pow`** (PROVED) — `signWt p x = (-1)^{wt x}`. **`signWt_affine`** (PROVED) — `signWt p x = 1 - 2·bv(parityFn x)`. **`parityFn_no_lowdeg`** (PROVED) — `p≠2, D<n ⇒ MOD_2` has no degree-`≤D` `F_p` representation (affine from `signWt_no_lowdeg`: a degree-`≤D` parity rep gives one of `signWt` via `Q = C 1 - C 2·P`). **`parityFn_not_acc0p`** (PROVED) — `p≠2, D<n ⇒ ¬∃ C, ModpOnly p C ∧ reprDegP p C ≤ D ∧ eval C = parityFn`: `MOD_2 ∉` bounded-degree `AC⁰[p]`. **Both RS halves assembled:** `AC⁰[p]` low-degree (`reprP`) + `MOD_2` not (this) ⇒ `MOD_2 ∉` bounded-degree `AC⁰[p]`. Honest scope: general `MOD_q` (`q>2`) = full RS (tree's `Layer4`); the Williams cash-out → `NEXP ⊄ ACC⁰` remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ParityBarrier` |
| **Brick (depth→degree) — `MOD_2 ∉` constant-*depth* `AC⁰[p]` for odd `p` `parity_not_acc0p_depth` (PROVED)** | The depth→degree blow-up upgrading bounded-degree to bounded-depth. **`reprDegP_le_depth`** (PROVED) — `reprDegP p C ≤ (p-1)·2^{depth C}` (binary `AND`/`OR` additive ⇒ doubles per level). **`parity_not_acc0p_depth`** (PROVED) — `p≠2, (p-1)·2^d < n ⇒ ¬∃ C, ModpOnly p C ∧ depth C ≤ d ∧ eval C = MOD_2`: a depth-`d` circuit has degree `≤ (p-1)2^d < n`, so the barrier kills `MOD_2`. **The standard constant-depth `q=2` Razborov–Smolensky separation** (explicit `n`-threshold; any size, since `reprDegP` is depth-only and `ACC0Circuit` is a formula). Honest scope: general `MOD_q` (`q>2`) = full RS (tree's `Layer4`); the Williams cash-out → `NEXP ⊄ ACC⁰` remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DepthDegreeBound` |
| **Brick (char witness) — the `MOD_q` character has no low-degree `F_p` representation `charWt_no_lowdeg_repr` (PROVED)** | The general-`q` witness. **`charWt ζ x`** = `∏ᵢ (if xᵢ then ζ else 1) = ζ^{weight}`. **`Dsign_charWt`** (PROVED) — `∑ₓ signWt x · charWt ζ x = (1−ζ)^n` (product factorisation `Finset.prod_univ_sum`; `∑_b (if b then -ζ else 1)=1−ζ`). **`Dsign_eval_zero`** (PROVED) — `deg P ≤ D<n ⇒ Dsign(eval P)=0`. **`charWt_no_lowdeg_repr`** (PROVED) — `p≠2, ζ≠1, D<n ⇒ ¬∃ P, totalDegree≤D ∧ ∀x eval(bv∘x)P=charWt ζ x` (`Dsign` kills low-degree but `Dsign(charWt)=(1−ζ)^n≠0`). Generalises the parity (`ζ=−1`) witness: every `MOD_q` character (`ζ` primitive `q`-th root, `q∣p−1`) is not low-degree. Honest scope: the *Boolean* `MOD_q` indicator `= (1/q)∑ⱼ charWt_{ζ^j}` — its degree needs `∑ⱼ (1−ζ^j)^n≠0` (the harder part); the Williams cash-out remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CharWitness` |
| **Brick (MOD_q indicator) — Boolean `MOD_q ∉` constant-depth `AC⁰[p]` when `Dsign(MOD_q)≠0` `modq_not_acc0p_depth` (PROVED, conditional)** | **`modqFn q x`** = `decide(q ∣ wt x)`. **`no_lowdeg_of_Dsign_ne`** (PROVED) — the general principle: `Dsign f ≠ 0, D<n ⇒ ¬∃ P deg≤D, eval(bv∘x)P=f x` (`Dsign` kills low-degree; generalises all witnesses). **`modqFn_no_lowdeg`** (PROVED) — `Dsign(bv∘MOD_q)≠0, D<n ⇒` `MOD_q` no degree-`≤D` rep. **`modq_not_acc0p_depth`** (PROVED) — `Dsign(bv∘MOD_q)≠0, (p-1)2^d<n ⇒ MOD_q ∉` depth-`d` `AC⁰[p]`. Honest scope: **conditional** on `Dsign(MOD_q)≠0` = the top coefficient `(1/q)∑ⱼ(1−ζ^j)^n` — always for `q=2`; for `q>2` it holds for many `n` (e.g. `n=1`, `MOD_q=¬`) but can vanish, where the unconditional bound is the full RS rank argument (tree's `Layer4`), *not* proved or faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModqWitness` |
| **Brick (MOD_q Fourier) — character decomposition of the Boolean `MOD_q` indicator `Dsign_modq` (PROVED)** | The discrete-Fourier structural identity. **`charWt_eq_pow`** (PROVED) — `charWt ζ x = ζ^{wt x}`. **`modq_fourier`** (PROVED) — `orderOf ζ = q, (q:F_p)≠0 ⇒ bv(MOD_q x) = q⁻¹ ∑_{j<q} charWt(ζ^j) x` (geometric sum `∑_j (ζ^{wt})^j = if q∣wt then q else 0`). **`Dsign_modq`** (PROVED) — `∑ₓ signWt x · bv(MOD_q x) = q⁻¹ ∑_{j<q}(1−ζ^j)^n` (Fourier + `Dsign_charWt`). This expresses the conditional-separation hypothesis `Dsign(MOD_q)≠0` as the explicit character sum `∑(1−ζ^j)^n`; a primitive `q`-th root `ζ` exists when `q∣p−1`. Honest scope: nonvanishing of `∑(1−ζ^j)^n` for all large `n` is the RS rank bound (tree's `Layer4`); the Williams cash-out remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModqFourier` |
| **Brick (MOD_q period) — *unconditional* `MOD_q ∉` constant-depth `AC⁰[p]` for `n ≡ 1 mod (p−1)` `modq_not_acc0p_depth_period` (PROVED)** | Discharges the conditional separation for an infinite family of arities. **`pow_eq_self_period`** (PROVED) — `x≠0, (p−1)∣(n−1), 1≤n ⇒ x^n = x` (Fermat). **`Dsign_modq_eq_one`** (PROVED) — `n≡1 mod(p−1) ⇒ Dsign(MOD_q)=1` (each `(1−ζ^j)^n=1−ζ^j`, `∑(1−ζ^j)=q`, `q⁻¹·q=1`). **`modq_not_acc0p_depth_period`** (PROVED) — `p≠2, q≥2, orderOf ζ=q, n≡1 mod(p−1), (p−1)2^d<n ⇒ MOD_q ∉` depth-`d` `AC⁰[p]` **unconditionally**. The genuine `q>2` Razborov–Smolensky separation for `n ≡ 1 mod (p−1)` (infinite arity family; `ζ` a primitive `q`-th root, `q∣p−1`). Honest scope: *all* large `n` (not just `n≡1 mod (p−1)`) is the full RS rank bound (tree's `Layer4`); the Williams cash-out remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModqPeriod` |
| **Brick (MOD_q infinite) — `MOD_q ∉` constant-depth `AC⁰[p]` for arbitrarily large arities `exists_large_arity_modq_not_acc0p` (PROVED)** | The asymptotic packaging. **`exists_large_arity_modq_not_acc0p`** (PROVED) — `∀ N, ∃ n > N, ¬∃ C : ACC0Circuit n, ModpOnly p C ∧ depth C ≤ d ∧ eval C = MOD_q` (pick `n = 1+(N+(p−1)2^d+1)(p−1)`, apply `modq_not_acc0p_depth_period`). The standard 'infinitely many `n`' `q>2` Razborov–Smolensky separation, unconditional & in-framework (`ζ` a primitive `q`-th root, `q∣p−1`, `p` odd). Honest scope: *all* large `n` (not just `n≡1 mod (p−1)`) is the full RS rank bound (tree's `Layer4`); the Williams cash-out remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModqInfinite` |
| **Brick (MOD_q infinite set) — the `MOD_q`-hard arities form an infinite set `modq_hard_arities_infinite` (PROVED)** | The `Set.Infinite` packaging. **`modq_hard_arities_infinite`** (PROVED) — `{n | ¬∃ C : ACC0Circuit n, ModpOnly p C ∧ depth C ≤ d ∧ eval C = MOD_q}.Infinite` (`Set.infinite_of_not_bddAbove` + `not_bddAbove_iff` + `exists_large_arity_modq_not_acc0p`). The cleanest standard form: the set of `MOD_q`-hard arities is infinite. Honest scope: *all* large `n` = full RS rank bound (tree's `Layer4`); the Williams cash-out remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModqInfiniteSet` |
| **Bridge (model translation) — `ACC0Circuit → BoolCircuitSyntax` eval/AC0p-preserving `toBoolSyntax_eval` (PROVED)** | Faithful translation to the tree's `Layer3/Layer4` model. **`toBoolSyntax`** (const/var/not/and/or/mod). **`count_support_eq_weight`** (PROVED) — count of accepting input-subcircuits over `S` = `weightOn S x`. **`toBoolSyntax_eval`** (PROVED, `[NeZero p]`) — `ModpOnly p C ⇒ BoolCircuitSyntax.eval (toBoolSyntax C) x = ACC0Circuit.eval C x` (mod case via `ZMod.natCast_eq_natCast_iff` + `decide_eq_decide`). **`toBoolSyntax_isAC0p`** (PROVED) — `ModpOnly p C ⇒ IsAC0pSyntax p (toBoolSyntax C)`. The model-bridge artifact connecting the two circuit formalizations. Honest scope: does **not** discharge `MOD_q` — `Layer4.mod_q_indicators_false` is a *joint residue-family size* bound (different shape); using it needs a residue-family construction + size bound, **not** done/faked. The Williams cash-out remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ToBoolSyntax` |
| **Bridge (Layer4 discharge) — `MOD_q` residue-family lower bound in the `ACC0Circuit` model `modq_indicators_false_acc0` (PROVED)** | Discharges `Layer4.mod_q_indicators_false` into this framework via `toBoolSyntax`. **`modq_indicators_false_acc0`** (PROVED) — `p,q` prime, `q∤p ⇒` no family `C : ℕ → ACC0Circuit (2m+1)` of `AC⁰[p]` (`ModpOnly`) circuits computing all `q` residue indicators `[weight≡j mod q]` within the RS depth/size window `⇒ False` (translate each via `toBoolSyntax`, apply `Layer4.mod_q_indicators_false`). The RS family lower bound now native to the `ACC0Circuit` framework. Honest scope: this is the **family** bound (Layer4's shape); a *single* `MOD_q ∉ AC⁰[p]` for all `n` needs the residue-family **construction** (build all `q` from one), separate/intricate, **not** done/faked; size/depth are on the translation (natural resource measures). The Williams cash-out remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Layer4Discharge` |
| **Bridge (residue-family construction) — `pinTrue` arity-extension eval-correct `pinTrue_residue_shift` (PROVED)** | The family-from-one operation. **`weight_extend`** (PROVED) — `weight(x⧺1ᵏ)=weight(x)+k`. **`weightOn_extend_split`** (PROVED) — `weightOn S (extend x)=weightOn (lowSupport S) x + highCard S` (`sum_bij'` reindex). **`pinTrue`** — `ACC0Circuit (n+k)→ACC0Circuit n` fixing the last `k` inputs to `true`. **`pinTrue_eval`** (PROVED) — `eval (pinTrue C) x = eval C (x⧺1ᵏ)`. **`pinTrue_modpOnly`**, **`pinTrue_depth`** (PROVED) — AC⁰[p] and depth preserved. **`pinTrue_residue_shift`** (PROVED) — a residue-`0` `AC⁰[p]` circuit at arity `n+k`, pinned, computes `[weight(x)+k ≡ 0 mod q]`, building any residue from one. The residue-family construction operation, eval/AC⁰[p]/depth correct. Honest scope: the full single-`MOD_q` all-`n` discharge via `modq_indicators_false_acc0` additionally needs the subcircuit-count (size) bound through `pinTrue` + a uniform residue-`0` family across arities — **not** done/faked. The Williams cash-out remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ResidueFamily` |
| **Bridge (size bound through `pinTrue`) — subcircuit count monotone under input-pinning `pinTrue_card_le` (PROVED)** | The quantitative step. **`subcircuitsList_map_input_length`** (PROVED) — subcircuit-list of input gates has length = index list. **`pinTrue_tbs_length_le`** (PROVED) — `(subcircuits (toBoolSyntax (pinTrue C))).length ≤ (subcircuits (toBoolSyntax C)).length` (mod case via `(lowSupport S).card ≤ S.card`). **`pinTrue_card_le`** (PROVED) — `(subcircuits (toBoolSyntax (pinTrue C))).toFinset.card ≤ (subcircuits (toBoolSyntax C)).length`. The size bound through `pinTrue`: a residue-`0` size bound transfers to every pinned residue indicator, feeding `modq_indicators_false_acc0`'s subcircuit-count hypothesis. Honest scope: the final single-`MOD_q` all-`n` discharge still needs the uniform residue-`0` family assembly across arities + arity/window bookkeeping (wiring). The Williams cash-out remains **open**. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PinSize` |
| **Bridge (final assembly) — `MOD_q ∉ AC⁰[p]` from a uniform residue-`0` family `modq_not_acc0p_uniform` (PROVED)** | The Layer4 route, assembled end to end. **`mod_shift`** (PROVED) — `j<q ⇒ ((w+(q−j))%q=0 ↔ w%q=j)` (via `ZMod`). **`tbs_depth_eq`** (PROVED) — `BoolCircuitSyntax.depth (toBoolSyntax C) = ACC0.depth C`. **`pinTrue_tbs_depth_le`** (PROVED) — depth non-increasing under `pinTrue`. **`modq_not_acc0p_uniform`** (PROVED) — `p,q` prime, `q∤p`, RS window `16((p−1)t)^d)²<2m+3 ⇒` no uniform `AC⁰[p]` family `D` (one per arity, `eval = residue-0`, `ModpOnly`, `depth (toBoolSyntax (D N))≤d`, `4q·(subcircuits (toBoolSyntax (D N))).length≤p^t`) computes `MOD_q ⇒ False`. Proof: `C j := pinTrue (D ((2m+1)+(q−j)))`; `pinTrue_residue_shift`+`mod_shift` ⇒ `eval=[weight%q=j]`; `pinTrue_modpOnly`/`pinTrue_tbs_depth_le`/`pinTrue_card_le` transfer the bounds; apply `modq_indicators_false_acc0`. The full Layer4 route to `MOD_q ∉ AC⁰[p]` (uniform RS resource regime). Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` is a different P≠NP-strength theorem, **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModqUniform` |
| **Bridge (size lower bound) — `MOD_q` needs super-polynomial `AC⁰[p]` size `modq_requires_large_size` (PROVED)** | The RS size lower bound. **`modq_requires_large_size`** (PROVED) — `p,q` prime, `q∤p`, any uniform `AC⁰[p]` family `D` computing `MOD_q` at depth `d`, any `t≥1 ⇒ ∃N, p^t < 4q·(subcircuits (toBoolSyntax (D N))).length` — so the size is not polynomially bounded (`t` arbitrary). Proof: contrapositive of `modq_not_acc0p_uniform`, choosing `m = 8((p−1)t)^d)²` (satisfies the RS window). `MOD_q ∉` poly-size constant-depth `AC⁰[p]` — the classical exponential size lower bound. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` is a different P≠NP-strength theorem (algorithmic method), **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModqSize` |
| **Bridge (concrete instantiation) — `MOD_3` needs super-poly `AC⁰[5]` size `mod3_requires_large_size_acc05` (PROVED)** | A concrete named separation. **`mod3_requires_large_size_acc05`** (PROVED) — `p=5, q=3` (`3∤5`), any uniform `AC⁰[5]` family `D` computing `MOD_3` at depth `d`, any `t≥1 ⇒ ∃N, 5^t < 12·(subcircuits (toBoolSyntax (D N))).length` — `MOD_3 ∉` poly-size constant-depth `AC⁰[5]`. Discharges the `Fact`-prime and `¬q∣p` conditions at concrete values: the abstract polynomial-method machinery yields named separations. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Mod3Acc5` |
| **Bridge (explicit exponential) — `MOD_q` size reaches `p^t` by a polynomial arity `modq_size_blowup` (PROVED)** | The explicit exponential size lower bound. **`modq_size_blowup`** (PROVED) — `p,q` prime, `q∤p`, uniform `AC⁰[p]` family `D` computing `MOD_q` at depth `d`, `t≥1 ⇒ ∃N ≤ 16((p−1)t)^d)²+1+q` (polynomial in `t`), `p^t < 4q·(subcircuits (toBoolSyntax (D N))).length` — i.e. `size(n) ≥ p^{Ω(n^{1/2d})}`. Proof: `by_contra`+`push_neg` (bounded size hyp) ⇒ re-run `modq_indicators_false_acc0` with `m=8(((p−1)t)^d)²`, residue family at arities `(2m+1)+(q−j) ≤ G`; size via `pinTrue_card_le` + bounded `hcon`. The size reaches `p^t` (exponential) by an arity polynomial in `t` — captures the `2^{n^{1/2d}}` blow-up exactly, no real-valued roots needed. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModqExp` |
| **Bridge (super-polynomial) — `MOD_q ∉` polynomial-size `AC⁰[p]` `modq_superpoly` (PROVED)** | The definitive super-polynomial separation. **`exp_beats_poly`** (PROVED) — `p≥2 ⇒ ∃t≥1, a·t^B < p^t` (exponential beats polynomial; `tendsto_pow_const_mul_const_pow_of_abs_lt_one`). **`modq_superpoly`** (PROVED) — `p,q` prime, `q∤p`, uniform `AC⁰[p]` family computing `MOD_q` at depth `d`, `∀c ⇒ ∃N, N^c < 4q·(subcircuits (toBoolSyntax (D N))).length` — the size beats every polynomial. Proof: `exp_beats_poly(K^c, 2dc)` + `modq_size_blowup` (`N≤G(t)≤K·t^{2d}`). `MOD_q ∉` polynomial-size constant-depth `AC⁰[p]` — the definitive polynomial-method separation. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Superpoly` |
| **Bridge (PARITY ∉ AC⁰) — the classic constant-depth lower bound `parity_not_ac0` (PROVED)** | The famous FSS/Håstad lower bound, via Razborov–Smolensky (`AC⁰ ⊆ AC⁰[p]`). **`ModFree`** — the `AC⁰` predicate (no `MOD` gates). **`modFree_modpOnly`** (PROVED) — `ModFree C ⇒ ModpOnly p C` for every `p`. **`parity_not_ac0`** (PROVED) — `n > 2^{d+1} ⇒` no depth-`d` `MOD`-free (`AC⁰`) circuit computes `PARITY` (a `MOD`-free PARITY circuit is `AC⁰[3]`, contradicting `MOD_2 ∉ AC⁰[3]`). `PARITY ∉ AC⁰` — the most famous constant-depth lower bound, as a clean RS corollary. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ParityAC0` |
| **Bridge (MOD_q ∉ AC⁰) — the `MOD`-counting lower bound in plain `AC⁰` `mod3_not_ac0` (PROVED)** | The `q>2` analogue of `PARITY ∉ AC⁰` (classic `MOD_m ∉ AC⁰`), via Razborov–Smolensky. **`modq_not_ac0_period`** (PROVED) — `p≠2` prime, primitive `q`-th root `ζ` in `F_p`, `n≡1 mod(p−1)`, `(p−1)2^d<n ⇒` no depth-`d` `AC⁰` circuit computes `MOD_q`. **`mod3_not_ac0`** (PROVED) — concrete `q=3`, `p=7`, `ζ=2`, `n≡1 mod 6 ⇒ MOD_3 ∉ AC⁰`. Honest scope: a fully general `MOD_q ∉ AC⁰` needs a prime `p≡1 mod q` (Dirichlet), supplied concretely here. The Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModqAC0` |
| **Bridge (AC⁰ ⊊ AC⁰[2]) — the constant-depth hierarchy is strict `ac0_strict_subset_acc02` (PROVED)** | A *class* separation (not just a function lower bound): `MOD` gates add power. **`mod2_cast_eq_one`** (PROVED) — `(n:ZMod 2)=1 ↔ Odd n`. **`eval_mod2_univ`** (PROVED) — the single gate `MOD_2` over all inputs computes `PARITY`. **`parity_mem_acc02`** (PROVED) — `PARITY ∈ AC⁰[2]` at depth `1`. **`ac0_strict_subset_acc02`** (PROVED) — `n>2^{d+1} ⇒ PARITY ∈ AC⁰[2] \ AC⁰`, witnessing `AC⁰ ⊊ AC⁰[2]`. The strict separation: a single `MOD_2` gate computes `PARITY` but no `AC⁰` circuit does. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0StrictSep` |
| **Bridge (AC⁰[2] ⊄ AC⁰[3]) — the prime modulus matters `acc02_not_subset_acc03` (PROVED)** | Separating two `MOD`-prime classes (the modulus is not interchangeable). **`acc02_not_subset_acc03`** (PROVED) — `n>2^{d+1} ⇒ PARITY ∈ AC⁰[2]` (one `MOD_2` gate) but `PARITY ∉ AC⁰[3]` (RS at odd prime `3`, all `n`), so `AC⁰[2] ⊄ AC⁰[3]`, hence `AC⁰[2] ≠ AC⁰[3]`. Honest scope: the reverse `AC⁰[3] ⊄ AC⁰[2]` needs `MOD_3 ∉ AC⁰[2]` (`p=2` has no primitive cube root in the polynomial-method machinery), not claimed. The Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimeSep` |
| **Bridge (AC⁰ ⊊ AC⁰[3]) — every prime modulus strictly extends `AC⁰` `ac0_strict_subset_acc03` (PROVED)** | The `q=3` companion to `AC⁰ ⊊ AC⁰[2]`. **`eval_mod3_univ`** (PROVED) — `mod 3 univ 0` computes `MOD_3`. **`mod3_mem_acc03`** (PROVED) — `MOD_3 ∈ AC⁰[3]` depth `1`. **`ac0_strict_subset_acc03`** (PROVED) — `n≡1 mod 6 ⇒ MOD_3 ∈ AC⁰[3] \ AC⁰`, witnessing `AC⁰ ⊊ AC⁰[3]`. The `MOD_3` gate strictly increases constant-depth power. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0StrictSep3` |
| **Bridge (AC⁰[3] ⊊ AC⁰[{2,3}]) — adding moduli strictly grows the class `acc03_strict_subset_acc023` (PROVED)** | The multi-modulus hierarchy grows. **`ModIn23`** — the `AC⁰[{2,3}]` predicate (`MOD` moduli `∈ {2,3}`). **`modpOnly3_modIn23`** (PROVED) — `AC⁰[3] ⊆ AC⁰[{2,3}]`. **`parity_mem_acc023`** (PROVED) — `PARITY ∈ AC⁰[{2,3}]` depth `1`. **`acc03_strict_subset_acc023`** (PROVED) — `n>2^{d+1} ⇒ PARITY ∈ AC⁰[{2,3}] \ AC⁰[3]`, so `AC⁰[3] ⊊ AC⁰[{2,3}]`. Granting a new prime strictly increases power. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MultiMod` |
| **Bridge (model upgrade) — PARITY needs super-poly size in *unbounded-fan-in* `AC⁰[p]` `parity_superpoly_ac0p` (PROVED)** | The real RS size lower bound in the genuine model. The earlier `ACC0Circuit` uses *binary* `AND`/`OR` (constant depth ⇒ ≤`2^d` leaves, a weaker model); this states the real bound in `BoolCircuitSyntax` (`List`-based *unbounded-fan-in* gates, the `Layer3`/`Layer4` model). **`parity_superpoly_ac0p`** (PROVED) — `IsAC0pSyntax p Cir`, depth `≤ d`, computes `PARITY`, window `8((p−1)t)^d)²≤m ⇒ p^t < 4·(subcircuits Cir).toFinset.card` (super-polynomial size). Proof: `Layer3.parity_function_lower_bound` + `Layer4.hmod_of_isAC0p`. The genuine Håstad/RS theorem in the real unbounded-fan-in model — upgrading the binary-fan-in `parity_not_ac0`. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnboundedParity` |
| **Bridge (PARITY ∉ poly-size AC⁰, real model) — Håstad via RS, unbounded fan-in `parity_superpoly_ac0` (PROVED)** | The canonical Furst–Saxe–Sipser / Håstad lower bound in the genuine model. **`isAC0_of_mem_subcircuits`** (PROVED) — `AC⁰` (no-`MOD`) inherited by subcircuits. **`hmod_of_isAC0`** (PROVED) — an `AC⁰` circuit has no `MOD` gates, so `hmod` is vacuous. **`parity_superpoly_ac0`** (PROVED) — `IsAC0Syntax Cir`, depth `≤ d`, computes `PARITY`, RS window `⇒ p^t < 4·(subcircuits Cir).toFinset.card` (any odd prime `p`) — super-polynomial size. The real FSS/Håstad theorem (via RS) in the unbounded-fan-in model. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnboundedAC0` |
| **Bridge (MOD_q ∉ AC⁰, real model) — residue-family no-go, unbounded fan-in `modq_indicators_false_ac0` (PROVED)** | The `q`-counting companion to `parity_superpoly_ac0`. **`isAC0_isAC0p`** (PROVED) — `IsAC0Syntax C → IsAC0pSyntax p C` (`AC⁰ ⊆ AC⁰[p]` syntactically; match-recursion, mod case vacuous). **`modq_indicators_false_ac0`** (PROVED) — `p,q` prime, `q∤p ⇒` no unbounded-fan-in `AC⁰` (`IsAC0Syntax`) family computes all `q` residue indicators `[weight≡j mod q]` in the RS window `⇒ False` (`isAC0_isAC0p` + `Layer4.mod_q_indicators_false`). The real `MOD_q` residue-family no-go for unbounded-fan-in `AC⁰`. Honest scope: a *single* `MOD_q ∉ AC⁰` for all `n` needs the residue construction; the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnboundedModq` |
| **Bridge (AC⁰ ⊊ AC⁰[2], real model) — strict separation, unbounded fan-in `ac0_strict_subset_acc02_unbounded` (PROVED)** | Class separation in the genuine model. **`parity_mem_acc02_unbounded`** (PROVED) — `PARITY ∈ AC⁰[2]` depth `1` (witness `toBoolSyntax (mod 2 univ 1)`, one unbounded-fan-in `MOD_2` gate; reuses `toBoolSyntax_isAC0p`/`tbs_depth_eq`/`toBoolSyntax_eval`+`eval_mod2_univ`). **`ac0_strict_subset_acc02_unbounded`** (PROVED) — RS window `⇒ PARITY` is a depth-`1` `AC⁰[2]` circuit ∧ every depth-`d` `AC⁰` circuit computing it has `3^t < 4·#{subcircuits}` (super-poly), so `AC⁰ ⊊ AC⁰[2]`. The strict separation in the real unbounded-fan-in model. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnboundedSep` |
| **Bridge (AC⁰[2] ⊄ AC⁰[3], real model) — the prime modulus matters, unbounded fan-in `acc02_not_subset_acc03_unbounded` (PROVED)** | Prime-modulus separation in the genuine model. **`acc02_not_subset_acc03_unbounded`** (PROVED) — RS window `⇒ PARITY` is a depth-`1` `AC⁰[2]` circuit (one `MOD_2` gate) ∧ every depth-`d` `AC⁰[3]` circuit computing it has `3^t < 4·#{subcircuits}` (super-poly via `parity_superpoly_ac0p` `p=3`), so `AC⁰[2] ⊄ AC⁰[3]`, `AC⁰[2] ≠ AC⁰[3]`. Honest scope: the reverse `AC⁰[3] ⊄ AC⁰[2]` needs `MOD_3 ∉ AC⁰[2]` (`p=2` no cube root), not claimed; the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnboundedPrimeSep` |
| **Bridge (MOD_q ∉ AC⁰[p], real model, single function) — uniform residue-0 no-go `modq_uniform_false_real` (PROVED)** | The single-`MOD_q` lower bound in unbounded fan-in (real-model analogue of `modq_not_acc0p_uniform`). **`modq_uniform_false_real`** (PROVED) — `p,q` prime, `q∤p`, uniform `AC⁰[p]` family `D : (N)→BoolCircuitSyntax N` computing `MOD_q` (residue-`0`) at every arity, `IsAC0pSyntax`, depth `≤ d`, padding-adjusted size `≤ p^t/(4q)`, RS window `⇒ False`. Proof: instantiate `Layer4.mod_q_family_false` with `D j = D ((2m+1)+(q−j))` (the repo's real-model residue construction `padTrue` + `padTrue_computes_indicator`). The single-`MOD_q` residue-`0` no-go for unbounded-fan-in `AC⁰[p]`, via the existing clean `Layer4` padding machinery. Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnboundedModqUniform` |
| **Bridge (MOD_q ∉ AC⁰, real model, single function) — uniform no-go, unbounded fan-in `modq_uniform_false_ac0_real` (PROVED)** | The single-`MOD_q` residue-`0` no-go for plain `AC⁰` (no `MOD`), real model. **`modq_uniform_false_ac0_real`** (PROVED) — `p,q` prime, `q∤p`, uniform `AC⁰` (`IsAC0Syntax`) family computing `MOD_q` (residue-`0`), bounded depth/size, RS window `⇒ False`. Proof: `isAC0_isAC0p` (`AC⁰ ⊆ AC⁰[p]`) + `modq_uniform_false_real`. Completes the real-model single-function `MOD_q` picture (no-`MOD` companion to `modq_uniform_false_real`, single-residue companion to `modq_indicators_false_ac0`). Honest scope: the Williams cash-out to `NEXP ⊄ ACC⁰` remains **open**/not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UnboundedModqAC0` |
| **Bridge (Williams via N-Frame, honest anatomy) — collapse socket IS separation-strength `nframe_collapse_socket_iff_separation` (PROVED)** | The honest Williams conditional anatomy (self-audit, mirroring the HM-socket-vacuous move). **`nframe_collapse_socket_iff_separation`** (PROVED) — given the proved-structure N-Frame counting route + uniform-realization socket + nondeterministic time hierarchy, the algorithmic-method collapse socket `↔ ¬(NEXP ⊆ ACC⁰)` (forward: full Williams composition; backward: `¬(NEXP⊆ACC⁰)` makes the collapse vacuous). So the route does **not** reduce the separation — the load-bearing socket **is** the separation. **`nexp_not_acc0_via_nframe`** (PROVED, conditional) — `¬(NEXP⊆ACC⁰)` from the YBT socket (`WilliamsFastSatRoute` = classical Beigel–Tarui, formalization-gap) + the algorithmic-method sockets. Honest conditional anatomy: open content named/classified (YBT = classical, **not** P≠NP-strength; collapse socket = proved equal to the separation). **No** fake unconditional `NEXP ⊄ ACC⁰`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NFrameWilliamsAnatomy` |
| **Bridge (hard math, Beigel–Tarui gate-degree rung) — Boolean gates are low-degree `gateBinary_degree` (PROVED)** | A genuine rung of the Beigel–Tarui hard combinatorics (the residual `hgu`/`hgb` of `compositeBT_representation`). **`gateUnary`/`gateBinary`** — exact multilinear interpolation of any unary/binary Boolean function. **`gateUnary_degree`** (PROVED) — `totalDegree ≤ 1`. **`gateBinary_degree`** (PROVED) — `totalDegree ≤ 2`. **`gateUnary_eval`/`gateBinary_eval`** (PROVED) — eval-correct on Boolean inputs. The `δ = 2` gate-degree input to Beigel–Tarui genuinely established for all Boolean (`AND/OR/NOT`) gates. Honest scope: the `MOD_m` gate's polylog (Toda) degree is the remaining hard combinatorics; unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength, not done. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GatePolyDegree` |
| **Bridge (hard math, Toda/RS MOD degree) — `MOD_p` is degree `p−1` over `F_p` `modpPoly_degree` (PROVED)** | The base rung of the composite-`MOD` Toda degree (the `MOD` gate's low-degree polynomial that Beigel–Tarui needs). **`modpPoly`** — `1 − (∑_{i∈S} X_i)^{p−1}` over `ZMod p`. **`modpPoly_degree`** (PROVED) — `totalDegree ≤ p−1`. **`modpPoly_eval`** (PROVED) — evaluates to the `MOD_p` residue-`0` indicator on Boolean inputs (Fermat: `a^{p−1}=0` if `a=0` else `1`). The exact low-degree (`p−1`) Toda/RS representation of the prime-modulus `MOD` gate — degree `O(1)` in fan-in, the input driving BT's quasipoly `SYM∘AND` count. Honest scope: composite `m` needs prime-power + CRT lifting (remaining rung); unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModpTodaPoly` |
| **Bridge (hard math, prime-power Toda lifting) — `MOD_{p^e}` root-of-unity representation `primePowerMod_charSum` (PROVED)** | The prime-power rung: where Fermat `F_p` fails (`e≥2`, RS barrier), the root-of-unity representation lifts. **`primePowerMod_charSum`** (PROVED) — over `F_ℓ` with a primitive `(p^e)`-th root `ζ` (`ℓ` prime, `p^e ∣ ℓ−1`, `ℓ≠p`), `bv(MOD_{p^e} x) = (p^e)⁻¹ ∑_{j<p^e} charWt(ζʲ) x` (instantiating the proved `modq_fourier` at `q = p^e`). **`mod4_charSum`** (PROVED) — concrete `MOD_4 = MOD_{2²}` over `F_5`, `ζ=2` (`orderOf 2 = 4`). The Toda integer/root-of-unity representation of the prime-power `MOD` gate — the lift the prime-power case genuinely needs (the doc's 'different representation'), where the low-degree `F_p` form does not exist. Honest scope: the `SYM∘AND` count consequence for composition = the separate Beigel–Tarui count argument; unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerToda` |
| **Bridge (hard math, SYM∘AND composition count for MOD_p circuits) — unconditional quasipoly count `modp_endToEnd` (PROVED)** | The `MOD_p` analogue of `endToEnd_BT`, made **unconditional**. **`substP`** — the `MOD_p`-circuit substitution (`una → MOD_p` gate `modPGate p=1−X^{p−1}`; `bin → gbP`). **`modp_endToEnd`** (PROVED) — odd prime `p`, `MOD_p`-circuit `c` of depth `d ⇒ (substP p c).totalDegree ≤ (p−1)^{d+1} ∧` monomial-support (`SYM∘AND` `AND`-term) count `≤ (n+1)^{(p−1)^{d+1}} ∧` sparse sum — quasipolynomial for constant depth. Proof: discharge `modP_circuit_representation` with `substP`, `gbP`. The unconditional `SYM∘AND` composition count for `MOD_p`-circuits, via the degree-`(p−1)` Toda/Fermat `MOD_p` gate. Honest scope: composite `MOD_m` CRT assembly + unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength, not done. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModpEndToEnd` |
| **Bridge (hard math, composite MOD_m CRT assembly) — squarefree `MOD_m` = AND of prime `MOD_p` `modm_squarefree_and` (PROVED)** | The CRT reduction of composite (squarefree) `MOD` to prime gates. **`squarefree_factorization_eq_one`** (PROVED) — squarefree `m ⇒ factorization p = 1` on prime factors. **`modm_squarefree_and`** (PROVED) — `m` squarefree `⇒ MOD_m(x) ↔ ∀ p ∈ m.primeFactors, (weight x : ZMod p) = 0` (composite gate = AND of prime `MOD_p` gates, via `modm_iff_residues` + `factorization=1` + `pow_one`). The CRT assembly carrying the per-prime Toda/Fermat low-degree + quasipoly `SYM∘AND` count (`modp_endToEnd`) to composite squarefree moduli. Honest scope: prime-power multiplicity `e≥2` (via the root-of-unity `primePowerMod_charSum`) + unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength, not done. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeMODCRT` |
| **Bridge (hard math, BT count ⇒ fast-SAT regime) — quasipoly `SYM∘AND` count drops below `2^n` `modp_fastsat_regime` (PROVED)** | The link from the BT count to the Williams fast-SAT ingredient. **`quasipoly_lt_exp`** (PROVED) — `∀ D, ∃ n≥1, (n+1)^D < 2^n` (via `exp_beats_poly`). **`modp_fastsat_regime`** (PROVED) — odd prime `p`, constant depth `d ⇒ ∃` arity `n`, every depth-`d` `MOD_p`-circuit has `SYM∘AND` `AND`-term count `< 2^n`. The BT count drops below `2^n` — the sub-exponential cell search = Williams' algorithmic fast-SAT ingredient, reached for constant-depth `MOD_p`-circuits. Honest scope: the realization/collapse sockets to `NEXP ⊄ ACC⁰` remain (collapse socket = P≠NP-strength, proved separation-equivalent). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModpFastSAT` |
| **Bridge (hard math, composite MOD_m prime-power multiplicity) — root-of-unity rep for any `m` `modm_charSum` (PROVED)** | The composite-with-multiplicity case via the factorization-independent root-of-unity representation. **`modm_charSum`** (PROVED) — any `m`, over `F_ℓ` with a primitive `m`-th root `ζ`, `bv(MOD_m x) = m⁻¹ ∑_{j<m} charWt(ζʲ) x` (`modq_fourier` at `q=m`, no dependence on factorization). **`mod12_charSum`** (PROVED) — concrete `MOD_{12} = MOD_{2²·3}` (multiplicity 2 on prime 2) over `F_13`, `ζ=2` (`orderOf 2 = 12`). Any `MOD_m` (any factorization, any multiplicity) has the exact root-of-unity representation. Honest scope: the `SYM∘AND` count for composition + unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeMultiplicity` |
| **Bridge (hard math, depth-2 prime-power SYM∘AND count) — `MOD_{p^e}∘AND` linear cell count `modpe_depth2_count` (PROVED)** | The depth-2 count via the symmetric structure (where the low-degree polynomial fails for `e≥2`). **`gateCount_le`** (PROVED) — count of satisfied gates `≤ k`. **`cells_card_le`** (PROVED) — count-cell image `≤ k+1`. **`modpe_depth2_count`** (PROVED) — `MOD_{p^e}(AND_1,…,AND_k)` has `≤ k+1` cells ∧ Satisfiable `↔ ∃` count-cell `c` with `p^e ∣ c`. The symmetric `MOD_{p^e}` top reduces SAT over the high-degree composition to a linear (`≤k+1`) cell search — the count the polynomial-degree route can't give for `e≥2`. Honest scope: deeper (depth≥3) compositions + unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModpeDepth2` |
| **Bridge (hard math, depth-3 prime-power composition) — cell count multiplies per layer `depth3_modpe_cells` (PROVED)** | The depth-3 recursion via the joint count vector. **`jointCells_card_le`** (PROVED) — the joint count vector of `t` count-gates (each `≤ m` gates) takes `≤ (m+1)^t` values. **`depth3_modpe_cells`** (PROVED) — depth-3 over `t` depth-2 `MOD_{p^e}∘AND` blocks (each `k` `AND`-gates) factors through the joint count vector `⇒ ≤ (k+1)^t` cells. The cell count multiplies per layer (`(k+1)^t`, polynomial for constant depth) — the symmetric-structure count surviving where the polynomial method fails for `e≥2`. Honest scope: arbitrary-depth nesting + unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModpeDepth3` |
| **Bridge (hard math, arbitrary-depth nesting recursion) — gate cells ≤ product over sub-circuits `cells_factor` (PROVED)** | The general inductive step for any depth. **`cells_compose`** (PROVED) — `t` functions each with `≤ C` cells `⇒ ≤ C^t` joint cells (abstract over value type). **`cells_factor`** (PROVED) — `F x = H(G_1 x,…,G_t x)`, each `G_i ≤ C` cells `⇒ (image F).card ≤ C^t`. The arbitrary-depth cell-count recursion: a gate's cells `≤` product over its sub-circuits' cells; applied at every node of a depth-`d` fan-in-`f` circuit `⇒ C^{f^d}` — quasipolynomial for constant depth, **field-independent** (so it survives the prime-power RS barrier). Generalizes `modpe_depth2_count` / `depth3_modpe_cells`. Honest scope: the concrete circuit-tree assembly + unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellRecursion` |
| **Bridge (hard math, circuit-tree assembly) — any count-tree cells `≤` product over the tree `ctree_cells_le` (PROVED)** | The end-to-end recursion over a concrete recursive datatype. **`cells_pair`** (PROVED) — joint of two functions `≤` product of cell counts. **`CTree`/`ceval`/`cbound`** — binary count-tree, eval, product-of-leaf-cells bound. **`ctree_cells_le`** (PROVED) — `(image (ceval t)).card ≤ cbound t` for every tree (structural induction, each node via `cells_pair`). Any binary count-tree of arbitrary depth has cell count `≤` the product of its leaf cells; with `MOD_{p^e}∘AND` leaves (`≤k+1` cells) `⇒` quasipolynomial for constant depth, field-independent (surviving the prime-power RS barrier). Honest scope: the unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellTree` |
| **Bridge (hard math, quantitative tree bound) — tree of prime-power blocks cell count `≤ (k+1)^{#leaves}` `modpe_tree_cells_le` (PROVED)** | The quantitative end-to-end output. **`cbound_le`** (PROVED) — `LeafBound C t ⇒ cbound t ≤ C^{leafCount t}`. **`ctree_cells_le_pow`** (PROVED) — `LeafBound C t ⇒ (image (ceval t)).card ≤ C^{leafCount t}`. **`isModpeTree_leafBound`** (PROVED) — a tree of `gateCount`-of-`k`-`AND` leaves is `(k+1)`-leaf-bounded (`cells_card_le`). **`modpe_tree_cells_le`** (PROVED) — tree of `MOD_{p^e}∘AND` blocks `⇒` cell count `≤ (k+1)^{#leaves}`. The quantitative end-to-end: arbitrary-depth nesting of prime-power `SYM∘AND` blocks stays quasipolynomial (`(k+1)^{#leaves}`, constant depth `⇒` poly `#leaves`), field-independent. Honest scope: the unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellTreeBound` |
| **Bridge (concrete↔abstract) — faithful `ACC0Circuit → CTree` translation `ceval_toCTree` (PROVED)** | Connects the abstract cell-count machinery to the concrete circuit model. **`toCTree`** — `ACC0Circuit → CTree` (`const`/`var`/`mod` → `{0,1}` leaves; `not`/`and`/`or` → `ℕ→ℕ→ℕ` nodes). **`ceval_toCTree`** (PROVED) — `ceval (toCTree C) x = if eval C x then 1 else 0` (eval-preservation, structural induction; `not`/`and`/`or` via standalone `Bool` helper lemmas to dodge the `if`-Decidable generalize issue). **`satisfiable_iff_toCTree`** (PROVED) — `(∃x, eval C x = true) ↔ ∃x, ceval (toCTree C) x = 1`. Every `ACC0Circuit` IS a count-tree, so `ctree_cells_le` applies to real circuits. Honest scope: naive Boolean leaves `⇒` loose `2^{#leaves}` bound; the quasipoly benefit needs `MOD∘AND`-block leaves (`modpe_tree_cells_le` regime). Unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CircuitToCellTree` |
| **Bridge (concrete↔abstract, symmetric leaf) — `ACC0Circuit` `MOD` gate has `≤ |S|+1` cells `modGate_satisfiable_iff` (PROVED)** | The honest refinement delivering on the flagged gap: the real `MOD` gate is a low-cell symmetric leaf. **`weightOn_le`** (PROVED) — `weightOn S x ≤ |S|`. **`weightOn_cells_le`** (PROVED) — `(image (weightOn S)).card ≤ |S|+1`. **`modGate_satisfiable_iff`** (PROVED) — `Satisfiable (eval (.mod q S t)) ↔ ∃ w ∈ image (weightOn S), (w:ZMod q)=t` — SAT over `≤|S|+1` weight-cells. The `MOD` gate over inputs is the genuine `MOD∘AND`-block leaf (AND-gates = single literals): linear (`≤|S|+1`) cells, fast-SAT, not `2^{|S|}` — the symmetric structure the polynomial method can't exploit for prime powers. Feeding such leaves through the count-tree (`modpe_tree_cells_le`) = the quasipoly regime. Honest scope: unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModGateCells` |
| **Bridge (concrete↔abstract payoff) — real `MOD`-layer circuit has `≤ (s+1)^t` cells `modLayer_cells_le` (PROVED)** | The payoff combining concrete symmetric leaf + abstract recursion. **`modLayer_cells_le`** (PROVED) — if `F x = H(weightOn S_1 x,…,weightOn S_t x)` with each `|S_i| ≤ s`, then `(image F).card ≤ (s+1)^t` (via `cells_factor` + `weightOn_cells_le`). **`modLayer_satisfiable_cells`** (PROVED) — such a Boolean circuit's cell bound ∧ SAT `↔ ∃ x, H(weights)=true`. A real depth-2 circuit (Boolean top `H` over `t` `MOD` gates, supports `≤ s`) has quasipolynomial cell count `(s+1)^t` — polynomial for constant fan-in/bounded supports `⇒` SAT = weight-cell search. The meaningful (non-trivial) cell bound for actual circuits. Honest scope: deeper interleaving + unconditional `NEXP ⊄ ACC⁰` = P≠NP-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModLayerCells` |
| **Bridge (concrete↔abstract, algorithmic payoff) — fast-SAT for a real `MOD`-layer circuit `modLayer_fastsat` (PROVED)** | The algorithmic speedup made concrete. **`modLayer_fastsat`** (PROVED) — for `F x = H(weightOn S_1 x,…,weightOn S_t x)` with `|S_i|≤s`: `(∃x, F x=true) ↔ ∃ w ∈ image(joint weights), H w = true`, AND the joint-weight image has `≤ (s+1)^t` cells. **`modLayer_beats_bruteforce`** (PROVED) — `(s+1)^t < 2^n ⇒` the cell search has `< 2^n` cells. SAT for a real `MOD`-layer circuit = search over `≤(s+1)^t` weight-cells (not `2^n` assignments) — beats brute force when `(s+1)^t < 2^n`; the Williams speedup made concrete via the field-independent symmetric cell count. Honest scope: realization/collapse sockets to `NEXP ⊄ ACC⁰` remain (collapse socket = P≠NP-strength). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ModLayerFastSAT` |
| **Attacking `PatternRichCrossFieldLowerBound` via the non-native degree route — the Smolensky counting engine, PROVED; the wall reduced to one analytic socket (PROVED)** | The direct attack on the single open target along its recommended Razborov–Smolensky route (entry-262 `NonNativeDegreeLowerBound`): factor the lower bound through *non-native polynomial degree over the field `F`*. The route has two halves — **(B)** richness ⇒ high non-native degree (Smolensky's *counting* argument), and **(A)** small resources ⇒ low non-native degree (the *polynomial-method* approximation of `AC⁰[p]`). This file **proves the entire (B) engine** — the dimension counting and rank pigeonhole that are the actual mechanism of Smolensky's bound — and reduces the target to the *single* named analytic socket (A). **`lowDegreeDim n D := ∑_{i≤D} C(n,i)`** — the dimension of the non-native degree-`≤ D` function space (number of multilinear monomials of degree `≤ D`). **`lowDegreeDim_lt_two_pow`** (PROVED) — `D < n ⇒ lowDegreeDim n D < 2^n`: the low-degree space is *strictly smaller* than the full function space (`Nat.sum_range_choose` + `Finset.sum_le_sum_of_subset`); the heart of why low-degree polynomials cannot compute rich functions. **`exists_notMem_of_finrank_lt`** (PROVED) — the rank kernel: a family spanning a space of dimension exceeding `finrank W` must contain a member outside `W` (`Submodule.span_le` + `Submodule.finrank_mono`). **`algExpander_forces_high_degree`** (PROVED) — the Smolensky **pigeonhole**: an `AlgExpander` family of `s` gates (rank `s` via `finrank_span_eq_card`) with `lowDegreeDim n D < s` must contain a gate of non-native degree `> D` (a gate outside the degree-`≤ D` submodule). *Too many independent gates cannot all be low-degree.* **`crossFieldCount_eq_firePattern_card_mod`** (PROVED, `rfl`) — the cross-field count *is* `|firing pattern| mod q` = `MOD_q` on the firing-pattern indicator, the function whose `AC⁰[p]` hardness is the in-arc `Layer4.mod_q_indicators_false`. Sockets: **`PolynomialMethodApproximation`** — the (A) half, the genuine open analytic core (the probabilistic polynomial method, `AC⁰[p] ⇒` low-degree approximation; concrete instances are the in-arc `Layer3.parity_function_lower_bound` and `Layer4.mod_q_indicators_false`); **`LowDegreeDimensionIdentity`** — the secondary socket (`finrank W = lowDegreeDim n D`, the multilinear `F`-Möbius/ANF basis count, cf. in-arc `anf_involutive` over `F₂`). **`nonNativeDegreeLowerBound_via_counting`** (PROVED) — assembles a concrete `NonNativeDegreeLowerBound` instance: the (B) half is *proved* by the counting engine, the (A) half is the polynomial-method socket. **`patternRichCrossFieldLowerBound_via_nonNativeDegree`** (PROVED) — feeds that through entry-262's `patternRich_lb_of_nonNativeDegree` to obtain `PatternRichCrossFieldLowerBound`, modulo the polynomial-method socket. Honest scope: the Smolensky counting + rank mechanism (the part that *makes the route work*) is now machine-proved as reusable kernels, and the open target is reduced to the *single* analytic socket `PolynomialMethodApproximation` (the open Razborov–Smolensky core, entry-238 `CarryRefinementCrossing`), plus the dimension identity. Does **not** prove the general lower bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NonNativeDegree` |
| **Reed–Muller / low-degree polynomial gates — a THIRD proved `AlgExpander` + `PatternRich` family, the one closest to Razborov–Smolensky (PROVED)** | The third hard-family candidate, obtained by the clean reduction *a low-degree polynomial gate is an affine gate in the monomial feature space*. **The family** — gate `i` is a degree-`≤ d` polynomial `Pᵢ` over `ZMod p`; it fires on `x` iff `Pᵢ(x) = bᵢ`. Writing `Pᵢ = ∑_m coeffᵢ,m · m` in the monomial basis and letting `φ(x)_m := m(x)` be the **monomial feature map** (`feat`, `m` over the degree-`≤ d` monomials `M`), `Pᵢ(x) = ⟨coeffᵢ, φ(x)⟩` — so `rmGate feat coeff b i x := decide (∑_m coeffᵢ,m · feat m x = bᵢ)` is an affine gate in feature space (`rmGate = varGate ∘ φ`); the Reed–Muller code `RM(d,n)` is the span of such `Pᵢ`. **`rmGate_degree_one_eq_varGate`** (PROVED) — the degree-1 features `feat m x = x_m` give `rmGate (fun m x => x m) a b = varGate a b`: the entry-262 varying-affine family is *exactly* the degree-1 special case. **Feature general position** — the hypothesis is that the polynomial-evaluation map `rmEval feat coeff : x ↦ (Pᵢ(x))ᵢ` is *surjective* (every value-vector attained — for Reed–Muller, non-degeneracy / large code distance). **`rmGate_patternImage_eq_univ`** (PROVED) — under feature general position every subset `T` is realized as a fire-pattern (solve `rmEval feat coeff x = (i ↦ if i∈T then bᵢ else bᵢ+1)`). **`rmGate_patternRich`** (PROVED) — hence `PatternRich (rmGate feat coeff b) (2^s)`. **`rmGate_coFiringRich`** (PROVED) — solving `rmEval feat coeff x = b` makes one input fire all `s` gates: `CoFiringRich (rmGate feat coeff b) s`. **`rmGate_algExpander`** (PROVED) — feature general position gives a private witness per gate, so the indicators are linearly independent over any field `F`: `AlgExpander`. **`rmGate_ACC0_chain`** (PROVED) — *given* the entry-262 lower-bound socket `PatternRichCrossFieldLowerBound` and the entry-261 bridge `crossFieldHard_to_ACC0Component`, the Reed–Muller family yields the `ACC⁰[composite]` component **with `AlgExpander` and `PatternRich` discharged here**. The RS attack invariant `NonNativeDegreeLowerBound` (non-native degree over `F_q` = Reed–Muller distance) applies verbatim. Honest scope: a third proved `AlgExpander`+`PatternRich`+`CoFiringRich` family, reduced to affine gates in feature space, with the degree-1 case shown to coincide with entry 262 and the chain to `ACC⁰[composite]` assembled with all family-specific hypotheses proved — only the two named sockets (`PatternRichCrossFieldLowerBound`/`NonNativeDegreeLowerBound`, Smolensky-strength entry-238 `CarryRefinementCrossing`; and the `crossFieldHard_to_ACC0Component` bridge) remain open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ReedMullerGates` |
| **Varying-direction affine hyperplanes — a SECOND proved `AlgExpander` + `PatternRich` family beyond dictator/`MOD_q`, with parallel as the degenerate easy subcase (PROVED)** | The program's second genuinely-different family satisfying *both* count-hardness invariants (entries 256–261), and the assembly of the chain to the `ACC⁰[composite]` component with all family-specific hypotheses discharged. **The family** — directions `a : Fin s → (Fin n → ZMod p)`, offsets `b`; gate `i` fires on `x` iff `⟨a i, x⟩ = b i` (`varGate a b i x := decide (∑ⱼ (a i j)·xⱼ = b i)`): each gate is the affine hyperplane `{x : ⟨a i, x⟩ = b i}` with its *own* direction `a i`, unlike the parallel family's one fixed all-ones direction. **General position** — the geometric hypothesis is that `dotEval a : x ↦ (⟨a i, x⟩)ᵢ` is *surjective* (every value-vector attained; for `n = s` exactly that the direction matrix is invertible). **`varyingAffine_patternImage_eq_univ`** (PROVED) — under general position *every* subset `T ⊆ Fin s` is realized as a fire-pattern: solve `dotEval a x = (i ↦ if i∈T then bᵢ else bᵢ+1)`; gate `i` fires iff `i ∈ T` (using `bᵢ+1 ≠ bᵢ`). **`varyingAffine_patternRich`** (PROVED) — hence `PatternRich (varGate a b) (2^s)`. **`varyingAffine_coFiringRich`** (PROVED) — solving `dotEval a x = b` makes a single input fire *all* `s` gates: `CoFiringRich (varGate a b) s` — the genuinely-new content beyond the parallel family (which never co-fires `≥ 2` gates). **`varyingAffine_algExpander`** (PROVED) — general position gives a private witness per gate (singleton-pattern solution), so the indicators are linearly independent over any field `F`: `AlgExpander`. **`parallel_pattern_image_ne_univ`** (PROVED, `s ≥ 2`) — the **degenerate easy subcase**: for the parallel family the all-fire pattern `univ` is *never* realized (no input co-fires `≥ 2` gates, entry 259), so its pattern image `≠ univ`; general position *fails* — the rank-deficient (`a i` all equal) degeneracy where the parallel fire-count is easy. Sockets: **`PatternRichCrossFieldLowerBound`** — *the single named missing theorem* (`AlgExpander → PatternRich (2^s) → CrossFieldCountHard`); **`NonNativeDegreeLowerBound`** — the Razborov–Smolensky **attack invariant**, factoring the target through *non-native polynomial degree over `F_q`* (`q ≠ p`), the route closest to RS; **`patternRich_lb_of_nonNativeDegree`** (PROVED) — the attack invariant *implies* the target (so attacking non-native degree is sound); **`varyingAffine_ACC0_chain`** (PROVED) — the payoff: *given* the lower-bound socket and the entry-261 bridge `crossFieldHard_to_ACC0Component`, the varying-affine family yields the `ACC⁰[composite]` component **with `AlgExpander` and `PatternRich` discharged here**. Honest scope: a second proved `AlgExpander`+`PatternRich`+`CoFiringRich` family under general position, parallel pinned as the degenerate subcase, and the chain to `ACC⁰[composite]` assembled with all family-specific hypotheses proved — only the two named sockets (`PatternRichCrossFieldLowerBound`/`NonNativeDegreeLowerBound`, Smolensky-strength entry-238 `CarryRefinementCrossing`; and the `crossFieldHard_to_ACC0Component` bridge) remain open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0VaryingAffinePatterns` |
| **Fire-pattern richness — strengthening co-firing to MANY distinct patterns, and the implication bridge (PROVED)** | Strengthens entry-260's existential `CoFiringRich` ("some input fires `≥ k` gates" — necessary but too weak) to pattern richness. **`PatternRich gates k := k ≤ #(Finset.image (firePattern gates) univ)`** — the firing map produces `≥ k` *distinct* fire-patterns. **`dictator_firePatternImage_eq_univ`** (PROVED) — the dictator/`MOD_q` family realizes *every* subset `T ⊆ Fin s` as a fire-pattern (the input `fun i => decide (i ∈ T)` has fire-pattern exactly `T`), so the pattern-image is all of `Finset (Fin s)`. **`dictator_patternRich`** (PROVED) — `PatternRich (fun i x => x i) (2^s)`: exponentially many distinct patterns (`Fintype.card_finset` + `Fintype.card_fin`). **`parallel_no_large_patterns`** (PROVED) — the parallel affine family has *no* fire-pattern of size `≥ 2` (all `≤ 1`, entry 259): no co-firing, no large patterns — it fails pattern richness in the strong sense, matching its easy fire-count. Sockets: **`PatternRichCountObstruction`** — the strengthened socket `AlgExpander gates → PatternRich gates → CrossFieldCountHard` (pattern richness, not just one co-fire); **`crossFieldHard_to_ACC0Component`** — the implication bridge `CrossFieldCountHard → ACC0CompositeComponent` (the cross-field count hardness *is* the `ACC⁰[composite]` lower-bound component, formally upstream of Williams `NEXP ⊄ ACC⁰`), with the proven instance the dictator/`MOD_q` family via the in-arc `Layer4.mod_q_indicators_false`. Honest scope: proves the strengthened separation (dictator/`MOD_q`: `2^s` patterns; parallel: none of size `≥ 2`) and states the implication bridge with its `MOD_q` proven instance. Does **not** prove the general count lower bound (Smolensky-strength) nor the general bridge. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FirePatternRichness` |
| **Co-firing richness — the corrected hardness invariant, separating parallel (easy) from dictator/MOD_q (hard) (PROVED)** | Defines the missing ingredient identified by the entry-259 negative (indicator rank not sufficient; need simultaneous firing). **`firePattern gates x := Finset.univ.filter (fun i => gates i x)`** — the set of gates firing on `x`. **`CoFiringRich gates k := ∃ x, k ≤ #(firePattern gates x)`** — some input fires `≥ k` gates simultaneously (the ingredient `AlgExpander` lacks). **`parallel_not_coFiringRich`** (PROVED) — the parallel affine family is *not* `CoFiringRich 2`: all its fire-patterns have size `≤ 1` (entry 259), so no input has `≥ 2` co-firing — exactly why its fire-count collapses to `{0,1}`. **`dictator_coFiringRich`** (PROVED) — the dictator family (gate `i` fires iff bit `i` set) *is* `CoFiringRich s`: the all-`true` input fires all `s` gates simultaneously. **`dictator_algExpander`** (PROVED) — the dictator family is also `AlgExpander` (private witnesses `wit i = the i-indicator`), so `AlgExpander` and `CoFiringRich` are *jointly satisfiable* — the corrected socket is non-vacuous. The corrected socket **`CoFiringCountObstruction`** := `AlgExpander gates → CoFiringRich gates → CrossFieldCountHard` (the entry-256/257 socket with the entry-259-required `CoFiringRich` added). Proven instance: the dictator family's mod-`q` fire-count is the Hamming weight mod `q` = **`MOD_q`** of the input — the canonical hard function whose `AC⁰[p]` hardness (`p ≠ q`) is the in-arc Razborov–Smolensky result `Layer4.mod_q_indicators_false` (entry 244). So the co-firing invariant exactly tracks the easy/hard split: parallel affine fails `CoFiringRich` and is count-easy; the dictator/`MOD_q` family satisfies both and is count-hard. Honest scope: proves the co-firing invariant separates the count-easy from the count-hard families and that `AlgExpander ∧ CoFiringRich` is non-vacuous with a proven hard instance (`MOD_q`); the *general* corrected socket is Smolensky-strength and not proved. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CoFiring` |
| **Affine hyperplane lower bound — the honest negative: the fire-count is EASY, refining the hard hypothesis (PROVED)** | Attacking the proposed `affineHyperplane_fireCount_modq_hard` for the entry-258 family reveals the opposite — an important finding. **`affineHyperplane_fireCount_le_one`** (PROVED) — for injective `targets`, `#{i : ∑ⱼ xⱼ = targets i} ≤ 1`: the parallel hyperplanes `{x : ∑ⱼ xⱼ = targets i}` are *disjoint* (any `x` has a single coordinate-sum value, so lies on at most one), so the fire-count is `≤ 1` (`Finset.card_le_one` + injectivity of `targets`). **`affineHyperplane_crossFieldCount_le_one`** (PROVED) — the mod-`q` fire-count of the parallel affine family is `≤ 1` (`Nat.mod_le` + the above): trivially computed by a 2-cell observer, so `affineHyperplane_fireCount_modq_hard` is *false* for this family. Key theorem-level finding: entry 258 proved this family is `AlgExpander` (full indicator rank), yet its fire-count is trivial — so **`AlgExpander` (indicator rank) is NOT sufficient for count-hardness**. The count lower bound needs *co-firing richness* — gates that fire *simultaneously* in many distinct patterns (overlapping, varying-direction) — which parallel/disjoint families lack (`≤ 1` fires). Consequently the naive bridge `AlgExpander ⇒ NEXP ⊄ ACC⁰` is **unsound** (this family is a counterexample); the genuine upstream condition is co-firing-rich count-hardness — the Razborov–Smolensky lower bound, proved in-arc for the (co-firing) `MOD_q` family (`Layer4.mod_q_indicators_false`). The corrected hard family is the *varying-direction* hyperplane family `{⟨aᵢ, x⟩ = bᵢ}` (inputs satisfy many simultaneously); the lower bound on it is the entry-256/257 socket with the corrected co-firing hypothesis. Honest scope: proves the parallel affine fire-count is easy (refuting the proposed target) and thereby the theorem-level fact that `AlgExpander` is not sufficient for count-hardness (co-firing is the missing ingredient). Does **not** prove the lower bound for the corrected co-firing family — the Smolensky-strength socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AffineHyperplaneLowerBound` |
| **Affine hyperplane indicators — a genuine non-trivial algebraically-expanding family (PROVED)** | The focused program's next concrete family beyond the trivial point/dictator gates (entry 257): affine hyperplane indicators. Over `F_p^{n+1}`, the parallel hyperplanes `{x : ∑ⱼ xⱼ = b}` (all-ones direction) partition the cube, so each input lies on exactly one — giving each gate a private witness, which forces linear independence (`AlgExpander`). **`private_witness_indep`** (PROVED, reusable tool) — if each gate `i` fires on a private witness `wit i` (`gates i (wit i) = true` and `gates j (wit i) = false` for `j ≠ i`), the gate indicators are linearly independent over `F` (evaluate the dependence `∑ⱼ cⱼ · gateInd j = 0` at `wit i`, collapsing to `c i = 0`; `Fintype.linearIndependent_iff` + `Finset.sum_eq_single`). **`affineHyperplane_algExpander`** (PROVED) — for any injective `targets : Fin s → ZMod p`, the affine hyperplane family `fun i x => decide (∑ⱼ xⱼ = targets i)` over `F_p^{n+1}` is `AlgExpander`: the private witness for gate `i` is `Pi.single 0 (targets i)` (coordinate-`0` set to `targets i`, rest `0`), which has coordinate-sum `targets i`, so it lies on hyperplane `i` and — by injectivity of `targets` — on no other. Honest scope: this proves a genuine non-trivial *affine* (non-dictator) algebraic-expander family has full indicator rank, via a reusable private-witness tool. It does **not** prove the count lower bound on it — that the mod-`q` fire-count of these affine gates needs superpolynomial resources — which is the entry-256/257 `AlgExpanderCountObstruction` socket, Smolensky-strength. Further families (random low-degree `F_p` polynomials, Reed–Muller) and the restricted lower bound remain. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AffineHyperplanes` |
| **Indicator rank — the algebraic-hardness invariant (stronger than treewidth), a concrete family, the RS link (PROVED)** | Makes the entry-256 algebraic-expansion invariant explicit and attacks it concretely (the focused program's invariant + family + RS-connection steps). **`gateRank gates := Module.finrank F (Submodule.span F (Set.range (gateInd gates)))`** — the dimension of the span of the gate-indicator functions over `F`, the algebraic-hardness geometry (treewidth governs DP; *rank* governs count-hardness). **`algExpander_gateRank_eq`** (PROVED) — `AlgExpander gates → gateRank gates = s`: algebraic expansion is exactly *full* indicator rank (`finrank_span_eq_card` + `Fintype.card_fin`). **`pointFamily_algExpander`** (PROVED) — the point/dictator family `fun i x => decide (x = i)` (gate `i` fires iff the input is `i`) has indicators equal to the standard basis `Pi.basisFun`/`Pi.single i 1`, hence linearly independent, so it satisfies `AlgExpander` (full rank `s`) — a concrete nontrivial algebraically-expanding family. The Razborov–Smolensky link: the count lower bound under `AlgExpander` (the entry-256 socket `AlgExpanderCountObstruction`) is *exactly* the Razborov–Smolensky statement — linearly-independent `F_p`-gate indicators ⇒ the mod-`q` fire-count needs superpolynomial resources — the `ACC⁰[composite]` lower-bound component, formally upstream of Williams `NEXP ⊄ ACC⁰`. The arc *proves* it for the `MOD_q` gate family (`Layer4.mod_q_indicators_false`, entry 244 — a proven restricted instance). Honest scope: proves the indicator-rank invariant (`AlgExpander = full rank`), exhibits a concrete full-rank algebraic-expander family, and links the count lower bound to Razborov–Smolensky (proven in-arc for `MOD_q`). It does **not** prove the general `AlgExpander` count lower bound (Smolensky-strength, the socket); the invariant is the algebraic-hardness geometry (rank, not treewidth), and a restricted bound for a non-trivial family beyond `MOD_q` is the next target. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0IndicatorRank` |
| **Algebraic expansion — the refined condition: gate-function independence, not cut-expansion (PROVED)** | Refines `ExpanderIncidence` into the correct hypothesis after the entry-255 negative (cut-expansion is necessary for DP-failure but not sufficient for count-hardness). The full-support degeneracy was about *redundant* gates, so the right condition is about the gate *functions*, not their variable supports. **`gateInd gates i := fun x => if gates i x then 1 else 0`** — the gate indicator as an `F`-valued function. **`AlgExpander gates := LinearIndependent F (gateInd gates)`** — the gate indicators are linearly independent over the field `F` (algebraic expansion = high gate-function rank). **`not_algExpander_of_duplicate`** (PROVED) — duplicate gates (`i ≠ j` with `gates i = gates j`) have equal indicators, hence are linearly dependent, so `¬ AlgExpander`: the entry-255 full-support / redundant-gate degeneracy is correctly excluded (via `LinearIndependent.injective`). **`algExpander_indicators_injective`** (PROVED) — `AlgExpander ⇒ Function.Injective (gateInd gates)`: distinct gates give distinct indicators, the genuine non-redundancy the degenerate family lacked. **`AlgExpanderCountObstruction`** (SOCKET — Smolensky-strength, a named conjecture, NOT proved) — `AlgExpander gates → ComputesCrossFieldCount → bound ≤ resources`: under *algebraic* expansion (gate-function independence over `F = ZMod q`), the mod-`q` fire-count needs superpolynomial resources; the entry-254 socket restated under the *correct* (algebraic) hypothesis. Honest finding: cut-expansion (treewidth / `ExpanderIncidence`) governs *DP* tractability exactly (251–253), but the genuine *count* lower bound needs **algebraic** expansion (gate-function linear independence), which rules out the redundant degeneracy. The two notions are genuinely different; this isolates the correct hypothesis. The lower bound under it is the open Smolensky-strength core (entry-238 `CarryRefinementCrossing`); not proved here. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AlgebraicExpansion` |
| **Special expander families — instantiating `ExpanderIncidence`, and an honest test of the socket (PROVED)** | The next step on the entry-254 bridge: instantiate `ExpanderIncidence` for concrete families and probe the socket. **`expander_of_pairwise_inter`** (PROVED) — `(∀ g g', g ≠ g' → d ≤ #(supp g ∩ supp g')) → ExpanderIncidence supp d`: if every two distinct gates share `≥ d` variables, every separator has size `≥ d` (a separator `S` with parts `A`, `B` contains `supp ga ∩ supp gb` for `ga ∈ A`, `gb ∈ B`, distinct since `A`, `B` disjoint, of size `≥ d`). **`fullSupport_expander`** (PROVED) — the all-variables family `fun _ => univ` satisfies `ExpanderIncidence (fun _ => univ) (Fintype.card V)`: no separator smaller than the whole variable set. Honest refinement (a genuine negative, surfaced): the full-support family is a separator-expander, yet its mod-`q` fire-count is plausibly *easy* — when all gates share the same support, the fire-count is a simple symmetric function of one shared sub-computation, with no genuine cross-field mixing. So **`ExpanderIncidence` alone does not imply the count obstruction**: separator-expansion is necessary for separator-DP failure but *not sufficient* for the count lower bound. This sharpens the bridge: the candidate invariant (separator-width / treewidth) governs *DP* tractability exactly, but the genuine *count* lower bound needs the algebraic / polynomial-method hardness — exactly what the entry-254 socket `ExpanderCountObstruction` captures and what Smolensky supplies. So the right hypothesis for the socket is combinatorial+algebraic expansion of *distinct, non-redundant* gates, not mere no-small-separator; isolating it and proving the bound is the open Smolensky-strength core. This file proves the `ExpanderIncidence` instantiations and the honest negative; it does **not** prove the count lower bound on any family (the socket). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ExpanderFamilies` |
| **Expander count obstruction — the conjectural bridge: DP-restricted bound proved, general bound socketed** | The negative side of the tractability dichotomy, formalized honestly with the split the user flagged (the general lower bound is Smolensky-strength). **`IsSeparator supp S`** — `S` separates the gate–variable incidence: the gates split into two nonempty parts whose variable supports meet only within `S`. **`ExpanderIncidence supp k`** — no small separator: every separator has size `≥ k`. **`ObserverBoundarySize := Finset.card`** — the observer's separator boundary size. **`dp_observer_needs_large_boundary`** (PROVED) — a separator-based observer whose boundary is a separator needs boundary `≥ k` on an expander (trivially: expander means every separator has size `≥ k`, so `hexp boundary hsep`). This is the *DP-restricted* lower bound — it bounds only observers that decompose along a separator. **`ExpanderCountObstruction`** (SOCKET — Smolensky-strength, a named conjecture `def`, NOT a proved theorem) — for *any* observer computing the mod-`q` fire-count (`ComputesCrossFieldCount`, abstract — covering algebraic / polynomial observers that need not decompose along a separator), `ExpanderIncidence supp k → ∀ boundary, ComputesCrossFieldCount boundary → k ≤ ObserverBoundarySize boundary`. The gap between this and the DP-restricted bound is exactly ruling out the clever algebraic shortcuts — equivalent in strength to the composite-`ACC⁰` lower bound; not proved. **`boundedSeparator_factorizes`** (PROVED, re-export of `ACC0TreewidthCount.separator_factor`) — the positive tractable side of the bridge (bounded separator ⇒ the count factors by the DP recurrence, entries 251–253). Honest finding: the DP-restricted lower bound is free (trivial by the definition of expander); the hard part is precisely ruling out *non-DP / algebraic* observers, which is the open Smolensky-strength `ExpanderCountObstruction` socket. The conjectural dichotomy: bounded separator/treewidth ⇒ tractable (proved); expander incidence ⇒ obstruction (socketed). Next step: instantiate `ExpanderIncidence` for special expander families and attempt the general bound there. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ExpanderCountObstruction` |
| **Treewidth count — separator conditioning: the bounded-treewidth DP core (PROVED)** | Pushes the tractable boundary of `CrossFieldCount` from block-diagonal (entry 252) to bounded treewidth, via its DP core: separator conditioning. **`separator_factor`** (PROVED) — for a separator `S` splitting the gates into parts `A`, `B` sharing only `S` (predicates `PA : S → A → Bool`, `PB : S → B → Bool`), `#{x : S×A×B | PA x.1 x.2.1 ∧ PB x.1 x.2.2} = ∑ s, #{a : PA s a} · #{b : PB s b}`: conditioning on the separator value makes the two sides independent, so the count factors (via `Fintype.sum_prod_type` + `Finset.sum_mul_sum` + `Finset.card_filter`). This is the variable-elimination / junction-tree recurrence. **`separator_factor_indep`** (PROVED) — the empty-separator special case: with no shared variables, `#{x : A×B | PA x.1 ∧ PB x.2} = #{a : PA a} · #{b : PB b}`, the block/disjoint product (entries 251/252) recovered as treewidth 0. Honest finding: iterating `separator_factor` over a tree decomposition of width `≤ w` gives the FPT-in-treewidth DP — each elimination conditions on a separator of size `≤ 2^w` (a `≤ 2^w`-fold sum) and recurses on the two sides, poly-time for bounded `w`. Disjoint (251) is treewidth 0; block-diagonal (252) is bounded treewidth with disconnected bags. The open case is **unbounded treewidth** — a connected / expander-like incidence graph with no small separators — where the count does not factor and the `mod-q` fire-count is the genuine cross-field mixing (Smolensky, entries 244/249/250) — the open `ACC⁰[composite]` core (entry-238 `CarryRefinementCrossing`). This file proves the separator-conditioning recurrence and its product specialisation; it does not formalize the full iterated junction-tree DP, nor resolve the unbounded-treewidth case (the exposed ACC wall). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TreewidthCount` |
| **Block-overlap count — the tractable fragment beyond disjoint: bounded local overlap factorizes (PROVED)** | Extends the entry-251 tractable boundary (disjoint, overlap rank 1) to **block-diagonal / bounded-local-overlap** families: when the input variables partition into independent blocks and each gate's support lies in one block, the count factors across blocks *even though gates may overlap arbitrarily within a block*. **`block_pattern_count`** (PROVED) — for independent variable blocks `Z : Fin m → Type` with an *arbitrary* per-block predicate `P b : Z b → Bool` (the within-block sub-computation, gates overlapping freely inside the block), `#{x : ∀ b, P b (x b)} = ∏ b, #{z : P b z}` — the count factors across blocks (`Fintype.card_piFinset`). **`block_count_le`** (PROVED) — bounded block size: `(∀ b, Fintype.card (Z b) ≤ W) → #{x : ∀ b, P b (x b)} ≤ W^m`, a product of `m` bounded factors (each block brute-forceable; the cross-field count is the convolution of the `m` per-block distributions, poly-time for bounded `W`). Honest finding: the tractability boundary is the gate–variable *incidence graph's block decomposition* — bounded *local* overlap (within a block arbitrary), not global disjointness. The disjoint case (entry 251) is blocks of size 1; this is bounded-size blocks. This refines the candidate invariant: the incidence graph decomposing into bounded-size connected components ⇒ fast `CrossFieldCount` (proved); a *connected / large-component* incidence graph (high global overlap, expander-like) is the open case, where the count does not factor and the `mod-q` fire-count is the genuine cross-field mixing (Smolensky, entries 244/249/250) — the open `ACC⁰[composite]` core (entry-238 `CarryRefinementCrossing`), not resolved here. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BlockOverlapCount` |
| **The cross-field count core — the exposed ACC wall, attacked directly; disjoint gates are tractable (PROVED)** | Every crossing route of the arc bottoms out at the same object, formalized directly here: `CrossFieldCount` = the number of `F_p`-gates firing on an input, taken **mod `q`** (`p ≠ q`). **`crossFieldCount q gates x := (Finset.univ.filter (fun i => gates i x)).card % q`**. **`crossFieldCount_lt`** (PROVED) — `< q`: the outer observer has `≤ q` cells (cheap; the difficulty is the per-cell *input* count, not the cell count). **`fireCount_eq_sum`** (PROVED) — the fire-count is `∑ᵢ [gate i fires]` (a sum of per-gate indicators). **`disjoint_pattern_count`** (PROVED, the tractable fragment) — for gates on *disjoint* supports (a product space `x : Fin s → Y` with gate `i` depending only on coordinate `i` via `gᵢ`), the number of inputs realising a given fire-pattern `b` factors as a product: `#{x : ∀ i, gᵢ (xᵢ) = bᵢ} = ∏ᵢ #{y : gᵢ y = bᵢ}` (via `Fintype.piFinset` + `Fintype.card_piFinset`). Independence: the cross-field count distribution is a convolution of the per-gate distributions, computable in poly time — **no cross-field mixing for disjoint supports**. Honest finding: the cross-field count is cheap for **disjoint / low-overlap** gate families (pattern-counts factor; convolution), so the obstruction is an *overlap* phenomenon. Candidate invariant: **low overlap / incidence rank ⇒ fast `CrossFieldCount`** (proved here for the disjoint case, overlap rank 1), **high overlap rank ⇒ Smolensky obstruction** (overlapping `F_p`-gates whose `mod-q` fire-count is the cross-field mixing, entries 244/249/250). The general dichotomy — a quasipoly observer (a crossing) *or* an exponential lower bound (the barrier) for arbitrary-overlap families — is the open `ACC⁰[composite]` core (entry-238 `CarryRefinementCrossing`); not resolved here. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CrossFieldCountCore` |
| **Staged fast-SAT without flattening — the outer stages for free; the obstruction relocates into the per-cell count (PROVED)** | The candidate crossing route (entries 246 + 249): a *staged* fast-SAT for the depth-2 mixed circuit (outer `MOD_3` of inner `MOD_2` outputs) that counts in stages, *without* flattening to a single `SYM∘AND` over one field.  The honest finding is decisive: staging the outer is free, but it relocates the obstruction rather than removing it. **`stagedSAT_outer_decomp`** (PROVED, **no axioms**) — `(∃ x, P x ∈ S) ↔ ∃ v ∈ S, ∃ x, P x = v`: SAT through the outer gate (where `P x` is the inner count it reads) is a *bounded disjunction* over the accepting residue-classes `S`; the outer `MOD_3` is handled by a `|S| ≤ 3`-way case split, **not** by composing it into a single polynomial — this is exactly "without flattening." **`staged_work_le`** (PROVED) — `(∀ v ∈ S, W v ≤ M) → ∑ v ∈ S, W v ≤ S.card * M`: the outer staging contributes only a `|S|`-fold (`≤` modulus, constant) factor to the total work, so the outer layer is cheap. Honest conclusion: each per-cell branch `∃ x, innerCount x = v` requires counting inputs `x` by `innerCount x = #(inner MOD_2 gates firing on x)` **mod 3** — counting `F_2`-gate fires mod 3 is the **cross-field mixing in counting form** (entries 244/249).  So staging *without* flattening does **not** bypass the wall; it relocates the cross-field mixing from a flattened polynomial into the per-cell inner count, where it is intrinsic.  This file proves the outer staging is a free bounded disjunction (constant factor) and locates the residual obstruction; whether the per-cell count (count `F_2`-fires mod 3 efficiently) is tractable is the open `ACC⁰[composite]` core (entry-238 `CarryRefinementCrossing`) — *not* proved tractable (a crossing) and *not* proved hard (a no-go, which would itself be the deep Smolensky-strength statement). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0StagedFastSAT` |
| **Depth-2 cross-modulus — outer MOD₃ fed by inner MOD₂: staged composition works; flattening is the wall (PROVED)** | The smallest depth-2 hard case (entry 248 localized the wall to depth-≥2 nesting): an outer `MOD_3` gate fed by inner `MOD_2` outputs.  Core question: can the product-observer states compose through the outer `MOD_3` *without* representing `MOD_2` over `F_3`?  **Answer: yes, at the staged level.** **`inner_mod2_deg_le_one`** (PROVED) — the inner `MOD_2` gate is degree `≤ 1` over `F_2` (entry-242 `modpIndicator`, `p = 2`). **`outer_mod3_deg_le_two`** (PROVED) — the outer `MOD_3` gate is degree `≤ 2` over `F_3` *in the inner bits* (entry-242, `p = 3`, `p-1 = 2`). **`outer_mod3_reads_inner_bits`** (PROVED) — the outer `F_3` polynomial evaluated on the inner output bits computes `[count of true inner bits ≡ 0 mod 3]` (entry-242 `modpIndicator_eval`): the outer gate *consumes the already-computed inner bits* via a degree-2 `F_3` polynomial, with **no** `MOD_2`-over-`F_3` arithmetization. So each layer is low-degree over its *own* field, and the product observer composes through the outer `MOD_3` reading the inner *bits* — the staged route works. Honest finding: the `MOD_2`-over-`F_3` collapse is forced *only* by **flattening** the depth-2 structure into a single `F_3` polynomial in the original inputs (substituting `bᵢ = MOD_2(inputs)`, composing degree `2 × deg_{F_3}(MOD_2)` with the high-degree parity `MOD_2` over `F_3`) — the Beigel–Tarui depth-reduction, Smolensky-blocked (entry 244), which is the original `ApproxToExactCount` wall. Whether a staged / multi-sorted fast-SAT (entry 246, whose counting budget composes) can consume the depth-2 staged observer *without* flattening — avoiding the single-field representation — is the open `ACC⁰[composite]` core (entry-238 `CarryRefinementCrossing`). Not built here. So: staged composition works (proved); flattening is the only blocked step. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Depth2CrossModulus` |
| **MOD₆ one-layer product-field mixing — one layer composes without collapse (PROVED); depth is the wall** | The smallest honest laboratory for cross-field circuit mixing (the irreducible core of `ACC0_ANATOMY.md`), on `MOD_6 = MOD_2 ∧ MOD_3`: can a circuit mixing `MOD_2` and `MOD_3` gates be tracked by a product-field observer (residues mod 2 *and* mod 3) without collapsing to `F_2` or `F_3`?  **`mixedOut comb k := comb (decide (k%2=0)) (decide (k%3=0))`** — a one-layer mixed `MOD_2`/`MOD_3` output (Boolean combiner of the two gate-output bits at count `k`). **`mixedOut_determined`** (PROVED, **no axioms**) — the output is determined by the product observer state `(count mod 2, count mod 3)`: equal residues mod 2 and mod 3 give equal output, **no collapse to a single field** (the gate outputs are bits, the combiner is a Boolean op touching no field arithmetic). **`mixedOut_determined_by_mod6`** (PROVED) — equivalently determined by `count mod 6` (the CRT product observer `ZMod 6 ≅ ZMod 2 × ZMod 3`, via `Nat.mod_mod_of_dvd`). **`mixedOut_cells_le_six`** (PROVED) — the one-layer mixed observer has `≤ 6` cells (bounded, no blow-up). Honest finding: **one layer** of mixed `MOD_2`/`MOD_3` gates feeding a Boolean combiner composes *without* collapse — a genuine partial crossing, tracked by the product observer (count mod 6). This **localizes the wall to depth ≥ 2**: a `MOD_q` gate *fed by* a non-native `MOD_p` output must arithmetize `MOD_p` over `F_q`, which is high-degree (Smolensky: `Layer3.parity_function_lower_bound` for `MOD_2` over `F_p`; `Layer4.mod_q_indicators_false` generally, entry 244). The depth-≥2 nested representation — composing the product observer *through* a non-native gate without collapsing — is **not** built here; it is the open `ACC⁰[composite]` core (entry-238 `CarryRefinementCrossing`). So the cross-field mixing obstruction lives specifically at depth nesting, not single-layer mixing. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ProductFieldMixing` |
| **Multi-sorted fast-SAT — the cell budget composes (PROVED); per-field gate representation is the blocked core** | The multi-sorted fast-SAT step (the deepest open question of the route), analysed honestly: the *counting budget* is proved fine, and building the per-field representations that would feed it is the blocked `ACC⁰[composite]` core. **`jointCells mcount := ∏ᵢ (mcountᵢ + 1)`** — the joint count-tuple space of a `k`-prime multi-sorted observer (counts over the product of per-prime ranges). **`jointCells_le_pow`** (PROVED) — `jointCells mcount ≤ (M+1)^k` when each `mcountᵢ ≤ M` (`Finset.prod_le_prod` + `prod_const` + `card_fin`): the cell budget composes multiplicatively and stays quasipolynomial for `M` quasipoly and `k` (the number of prime factors) constant — the counting side does not blow up (the product-structure analogue of entry 239). **`multiSorted_savings`** (PROVED) — `j ≤ n → jointCells ≤ 2^{n-j} → 2^j · jointCells ≤ 2^n` (`pow_add`): the joint cells meeting the budget give a `2^j` speedup, the same savings form as the single-field fast-SAT (`…ACC0WilliamsFastSat`). Honest finding: the multi-sorted fast-SAT *counting* is fine — it *would* run if the per-prime `SYM∘AND` representations existed. The blocked core (socket): the per-prime counting needs each circuit `MOD_m` gate realised as a low-degree `SYM∘AND` over a single `F_{pᵢ}`, which is impossible (`MOD_{pⱼ}`, `j ≠ i`, is not low-degree over `F_{pᵢ}` — Smolensky, entry 244), and the circuit's downstream gates *mix* the per-field computations; this cross-field mixing inside the circuit — not the final readout — is the actual `ACC⁰[composite]` barrier (entry-238 `CarryRefinementCrossing`). It is not built here. So the multi-sorted fast-SAT works *if* those representations exist; constructing them is the open, separation-strength wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MultiSortedFastSAT` |
| **The multi-sorted product-field observer — construction PROVED; feeding fast-SAT is the open socket** | The only remaining escape (roadmap step 4) after the single-field route was refuted by Smolensky (entry 244), analysed honestly: the product-field observer *construction* is proved, and feeding fast-SAT is named as the open socket (no crossing). **`modm_iff_modp_and_modq`** (PROVED) — for coprime `p, q`, `(p·q) ∣ k ↔ p ∣ k ∧ q ∣ k`: `MOD_{pq} = MOD_p ∧ MOD_q`, the product-field combiner is the `AND` of the per-prime residue-zero tests (CRT). **`prod_field_iso`** (PROVED) — `ZMod (p·q) ≃+* ZMod p × ZMod q` (Chinese Remainder): the observer lives in the product `∏ F_{pᵢ}` of per-prime fields. **`prodObs_bijective`** (PROVED) — the CRT residue tuple `k ↦ (k mod p, k mod q)` is a bijection: the product observer reads the count mod `p·q` faithfully, with no information loss (unlike a single small field, entry 239). Each per-prime component `MOD_{pᵢ}` is low-degree over its *own* field `F_{pᵢ}` (entry-242 `modpIndicator`; cited). Honest finding: the multi-sorted product-field observer *exists* and faithfully decomposes `MOD_m` into an `AND` of per-prime low-degree (over their own fields) indicators — it works *as an observer*. The open socket (step 5): whether it can feed Williams/BT fast-SAT — a quasipoly `SYM∘AND` — *without* collapsing to a single field (which Smolensky forbids, entry 244). The combiner is an `AND` of tests in *different-characteristic* fields; realising it as one low-degree polynomial over a common field would be the collapse (blocked); so the observer must stay genuinely product-sorted, and a fast-SAT machinery that consumes a multi-sorted observer is *not* constructed here. If yes → an `ACC⁰[m]` crossing; if no → a formal obstruction to the multi-sorted route. Open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MultiSortedObserver` |
| **Smolensky non-native MOD lower bound — connector: the field route is a NO-GO (PROVED, cites the arc's RS layer)** | Key finding: the Smolensky non-native MOD lower bound — the "open wall" named in entry 243 — is **already proved in this arc** (`Layer4.mod_q_indicators_false`: `MOD_q ∉ AC⁰[p]` for `q ∤ p`, sorry-free with clean axioms `[propext, Classical.choice, Quot.sound]`; `Layer3.parity_function_lower_bound`). This file **connects** it to the cross-field wall (it does **not** rebuild it). **`crossField_modq_fieldRoute_nogo`** (PROVED, delegates to `Layer4.mod_q_indicators_false`) — there is no family of `AC⁰[p]` circuits computing the `MOD_q` residue indicators within the Smolensky budget (for `q ∤ p`); i.e. `MOD_q` is not low-degree over `F_p` for `q ≠ p`. **`parity_nonnative_size_lb`** (PROVED, delegates to `Layer3.parity_function_lower_bound`) — the simplest non-native instance: an `AC⁰[p]` circuit (`p` odd) computing parity forces `p^t < 4 · #subcircuits`. Conclusion (the step-3 no-go): a single-field low-degree `F_p` representation of a non-native `MOD_q` is impossible, so `CarryRefinementCrossing` (entry 238) is **impossible via the single-field low-degree route** — the entry-243 "open wall" is, *for the field route*, closed against by the existing Razborov–Smolensky layer. The only possible escape is the **multi-sorted / product-field observer** in `∏_p F_p` (step 4) — separate low-degree components per prime + a symmetric combiner, not reducible to one `F_p` (entry-243 `no_common_char` forbids the collapse); whether it can feed fast-SAT without collapsing is the genuinely open next question, *not* settled here, and the Smolensky no-go does not apply to it. Honest scope: this file cites (does not reprove) the arc's Smolensky bound and draws the field-route no-go; it does not resolve the multi-sorted direction. Smolensky bounds `AC⁰[p]` (single prime), *not* `ACC⁰[m]` with the `MOD_m` gate available (Williams' open territory). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SmolenskyLowerBound` |
| **Cross-field combination — the structural root of the Smolensky wall (PROVED)** | The cross-field low-degree combination (the actual `ACC⁰[composite]` / Smolensky barrier), done honestly: the *structural root* of why it fails is proved; the deep degree lower bound is named and **not** proved, crossed, or faked. **`no_common_char`** (PROVED) — no field is simultaneously characteristic `p` and characteristic `q` for distinct `p, q` (via `CharP.eq` giving `p = q`); so the `MOD_p`-indicator (over `F_p`) and `MOD_q`-indicator (over `F_q`) cannot natively live in one field — the root incompatibility. **`self_prime_zero`** (PROVED) — `(p : ZMod p) = 0`. **`other_prime_unit`** (PROVED) — for distinct primes `p, q`, `(q : ZMod p) ≠ 0` (via `ZMod.natCast_eq_zero_iff` + `Nat.prime_dvd_prime_iff_eq`): in `F_p` the other prime is a *unit* while `p ↦ 0`, so the characteristic privileges its own prime (the Fermat indicator of entry 242 is native to mod-`p`, not mod-`q`). **`reduction_blind_to_other_prime`** (PROVED) — there are counts equal mod `p` but differing mod `q` (`0` and `p`: same mod `p` since `p ≡ 0`, different mod `q` since `q ∤ p`); so any decode factoring through `count mod p` cannot compute `MOD_q` — the naive single-residue cross-field route provably fails. Honest scope: these prove the **structural root** of the cross-field obstruction (incompatible characteristics, characteristic asymmetry, mod-`p` blindness to mod-`q`) — *why* the naive combinations fail. What remains is the genuine degree lower bound: `MOD_q` is not low-degree over `F_p` for `q ≠ p`, which is **Smolensky's deep theorem** (the native `MOD_p` is degree `≤ p-1` by entry 242; the non-native `MOD_q` at low degree is the open `ACC⁰[composite]` wall, entry-238 `CarryRefinementCrossing`). This file neither proves nor crosses that lower bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CrossFieldCombination` |
| **Layered-carry degree analysis — per-prime layers are low-degree; the composite combination is the wall (PROVED)** | The layered-carry degree analysis (the crossing/no-go question of entry 241), done honestly: the per-prime layer is proved cheap, and the composite combination is the named open wall — neither a crossing nor a faked no-go. **`boolToZMod`/`countSum x := ∑ boolToZMod (x_i)`** (the count mod `p`). **`linearCountPoly := ∑ X_i`**, **`linearCount_totalDegree_le_one`** (PROVED) — the count residue mod `p` (digit-0 layer) is realised by a *linear* polynomial; **`linearCount_eval`** (PROVED) — it evaluates to `countSum`. **`modpIndicatorPoly := 1 - (∑ X_i)^(p-1)`**, **`modpIndicator_totalDegree_le`** (PROVED) — the `MOD_p`-is-zero indicator has total degree `≤ p-1`, **independent of `n` and the count range `N`** (via `totalDegree_sub`/`totalDegree_pow`/`totalDegree_finset_sum`/`totalDegree_X`); **`modpIndicator_eval`** (PROVED) — it computes `[count ≡ 0 mod p]` (Fermat: `s^(p-1) = [s ≠ 0]`, so `1 - s^(p-1) = [s = 0]`). Honest finding: a *single prime layer* is low-degree (`≤ p-1`, independent of `n`) — so the layered route does **not** reintroduce entry-241's `Θ(N)` cost *within a single prime* (genuine good news for layers). What remains the **open wall**: for composite `m = ∏ pᵢ`, `MOD_m = ⋀ᵢ MOD_{pᵢ}` lives over *different* fields `F_{pᵢ}`; combining the per-prime low-degree indicators into *one* low-degree polynomial over a common field (or the higher p-adic carry digits for prime powers) is the open `ACC⁰[m]` barrier — Smolensky-strength, the entry-238 `CarryRefinementCrossing`. This file proves the per-prime good news and names the composite combination as the unresolved wall (if low-degree-realisable → an `ACC⁰[m]` crossing; if not → the formal Smolensky obstruction). It settles neither. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LayeredCarryDegree` |
| **Composite count degree — the formal obstruction: field decode costs Θ(N) (PROVED)** | The degree-complexity of exact composite count realisation, proving *why* the entry-240 field decode is too expensive. **`indicator_natDegree_ge`** (PROVED) — over a field, a nonzero polynomial vanishing at `N` distinct points has `natDegree ≥ N` (the `N` points are distinct roots, `#roots ≤ natDegree` via `Polynomial.card_roots'`). **`faithful_decode_degree_ge`** (PROVED, the obstruction) — a field decode `P` nonzero at one point `b` and `0` at `N` other distinct count-points `S` needs `natDegree ≥ N = S.card`. **`pointIndicator_natDegree_ge`** (PROVED) — the entry-240 prime point-indicator `pointIndicator p b` vanishes on the `p-1` field elements `≠ b`, so has degree `≥ p-1`. **`prime_indicator_natDegree_eq`** (PROVED, tightness) — `(pointIndicator p b).natDegree = p - 1` (lower bound here, upper bound entry-240): the prime field indicator has degree *exactly* `p-1 =` (field size) `− 1`. Degree-vs-faithful-range finding: a faithful field decode distinguishing counts `{0,…,N}` needs field `≥ N+1` (entry 239), and a single-count indicator there has degree exactly `N` — so the field-route decode degree is `Θ(N)`, linear in the count range, hence **not low-degree** (`polylog`/`n^{o(1)}`). This formally confirms the field readout cannot be the crossing. Honest scope: a genuine degree lower bound and tightness *for the single-field route*. It does **not** prove a lower bound against *layered* (base-`p` digit / multi-polynomial) carry realisations — the open question "can p-adic carry layers realise the exact composite count at degree `≪ N` while keeping quasipoly size?" is the entry-238 `CarryRefinementCrossing` crossing (if yes → a possible `ACC⁰[m]` crossing, if no → a formal degree obstruction), which this file does not settle. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeCountDegree` |
| **Carry observer realisation — the decode is realisable over a field; the count is the barrier (PROVED)** | Attacks the algebraic realisation of the carry observer (entry-238 `CarryRefinementCrossing`); the honest finding parallels entry 239 (size) — the count-*decode* is realisable over a field, which relocates the barrier precisely onto the exact *count* computation for composite modulus (entry 234). **`pointIndicator p a := 1 - (X - C a)^(p-1)`** — the Fermat indicator of `{a}` over a prime field. **`pointIndicator_eval`** (PROVED) — `eval y = if y = a then 1 else 0` (via `(y-a)^(p-1) = [y ≠ a]`, `ZMod.pow_card_sub_one_eq_one`). **`pointIndicator_natDegree_le`** (PROVED) — degree `≤ p-1`. **`decPoly`/`decPoly_eval`/`decPoly_natDegree_le`** (PROVED) — the decode polynomial `∑_{a : dec a} pointIndicator a` evaluates to `[dec y]` (exactly the `a=y` term fires, `Finset.sum_ite_eq`) and has degree `≤ p-1`. **`RealizesCarryObserver m D := ∀ dec : ZMod m → Bool, ∃ P, P.natDegree ≤ D ∧ ∀ y, P.eval y = if dec y then 1 else 0`**. **`prime_realizes`** (PROVED) — *prime field realises via Fermat*: `RealizesCarryObserver p (p-1)`, every decode over a prime field is a degree-`≤(p-1)` polynomial. **`faithful_of_size`** (PROVED, re-export entry 239) — `m ≥ N+1 → Faithful m N`. **`squarefree_crt_reduction`** (PROVED, supporting) — `ZMod(a·b) ≃+* ZMod a × ZMod b` for coprime `a,b` (entry-235), reducing squarefree realisation to per-prime decodes. **`primePow_no_power_indicator`** (PROVED, supporting obstruction) — over `ZMod (p^e)` (`e ≥ 2`) no power indicator exists (entry-235), so the Fermat/power route fails — the realisation needs the p-adic digit layers. Honest caveats (no overclaim): (i) the realised object is the *decode* (count → boolean), the *free symmetric part* of a `SYM∘AND` — the degree cost is in computing the *count* (the `AND` layer, entry 234), not here; (ii) the Fermat realisation has degree `p-1 =` (field size) `− 1`, and faithfulness needs field `≥ N+1` (entry 239), so a *faithful* single-field realisation has degree `~N`, **not** low-degree. So this confirms the obstruction is neither the observer's size (239) nor the decode (here), but the **exact, low-degree count computation for composite modulus** — entry-234 `ApproxToExactCount` / entry-238 `CarryRefinementCrossing`, the open `ACC⁰[m]` separation-strength wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CarryRealization` |
| **Carry observer size — quasipoly vs exponential, the answer (PROVED bounds)** | Defines the carry observer's state and size and settles the quasipoly-vs-exponential question for the *state count* — the honest finding being that the state count is **quasipolynomial, not exponential**, so the barrier is the algebraic realisation, not counting. **`CarryProfileState m k := (k : ZMod m)`** (the observer's state reading count `k`); **`Faithful m N`** (the modulus-`m` observer is injective on counts `{0,…,N}`); **`CarryObserverSize m N := #{ k mod m : k ∈ {0,…,N} }`** (distinct states occupied). **`faithful_iff_le`** (PROVED, centerpiece) — `Faithful m N ↔ N + 1 ≤ m`: a modulus-`m` observer recovers all counts `{0,…,N}` iff `m ≥ N+1` (forward by pigeonhole `Fintype.card_le_of_injective` + `ZMod.card`; backward since counts `< N+1 ≤ m` have distinct residues via `ZMod.val_cast_of_lt`). **`carryObserverSize_le_succ`** (PROVED) — `≤ N + 1`. **`carryObserverSize_le_modulus`** (PROVED) — `≤ m`: a *fixed* modulus caps the state count, so faithfulness on growing counts needs carry layers. **`exists_faithful`** (PROVED, the finding) — `Faithful (N+1) N`: a faithful observer on `{0,…,N}` exists with exactly `N+1` states, so the state count is *quasipolynomial* when `N` is (e.g. `N = #monomials`) — no exponential blow-up. **`field_faithful_iff`** (PROVED) — `Faithful p N ↔ N + 1 ≤ p`: trivial profile collapses to the field observer (faithful when `N < p`, no carry layer). **`primePow_faithful_iff`** (PROVED) — `Faithful (p^e) N ↔ N + 1 ≤ p^e`: a prime-power observer needs enough carry layers (`p^e ≥ N+1`, i.e. `e ≳ log_p N` digits). Honest scope: these bounds show the carry observer's *state count* is quasipolynomial (faithful with `N+1` states; a fixed modulus needs a larger field or carry layers, both quasipoly), **relocating the barrier**: the obstruction is not a counting blow-up but the **low-degree algebraic realisation** of the observer for composite `m` — the open entry-238 socket `CarryRefinementCrossing` / `ACC⁰[m]` separation-strength problem. This file does not realise it. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CarryObserverSize` |
| **The carry-refinement crossing condition — named socket (the open `ACC⁰[m]` wall, documented)** | Names and documents the *crossing condition* for the composite-`ACC⁰[m]` barrier (entries 234–237), with the surrounding facts proved so the socket sits faithfully between the proved characterisation and the open wall. ⚠️ This documents the open barrier; it does **not** cross it. **`fieldObs p k := (k : ZMod p)`** — the field count-observer (the satisfied-monomial count read mod `p`, what the weighted-`F_p` method provides). **`fieldObs_carry_blind`** (PROVED) — `fieldObs p (k + p) = fieldObs p k`: the field observer collapses counts differing by a carry-sized shift (`p ≡ 0`), the count-level form of entry-235 `field_observer_blind_to_carry`. **`fieldObs_not_injective`** (PROVED) — for `p ≥ 2`, `fieldObs p 0 = fieldObs p p` but `0 ≠ p`, so the field observer cannot recover the exact count and cannot be a carry-faithful observer. **`lowDegRep_of_observer`** (PROVED) — a count observer `obs` and decode `dec` through which `f` factors (`f x = dec (obs (saCount mono x))`), with a low-degree injective monomial family, *already* give `LowDegRep f D` (with `SYM` gate `dec ∘ obs`); so crossing reduces **exactly** to producing such an observer for composite `m`. **`CarryRefinementCrossing approxHyp f D := approxHyp → LowDegRep f D`** — the named socket: the composite-modulus RS approximant yields a `LowDegRep`, equivalently a *carry-faithful* count observer (surviving carry refinement, unlike the carry-blind `fieldObs`) realised by a low-degree injective monomial family exists. **Not proved** — establishing it for composite modulus is the open `ACC⁰[m]` separation-strength problem (entry-234 `ApproxToExactCount`), shown genuinely obstructed by the carry profile (entry 237). **`lowDegRep_of_crossing`** (PROVED conditional) — `CarryRefinementCrossing approxHyp f D → approxHyp → LowDegRep f D`: the socket, if established, discharges the barrier, confirming it is faithfully the crossing condition. Honest scope: the proved scaffolding pins down why the field observer cannot cross (carry-blindness) and what suffices (a count observer through which `f` factors with low-degree monomials), reducing the crossing to one named property — a carry-faithful low-degree count observer for composite `m` — which remains the open, separation-strength wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CarryCrossing` |
| **The CRT carry profile — composite-modulus observer from per-prime Kummer carries (PROVED)** | Assembles the entry-236 per-prime carry counts into a composite-modulus carry profile for `m = ∏ pᵢ^{eᵢ}`, conservatively. **`CarryProfileTrivial m n k := ∀ p ∈ m.primeFactors, carryCount p n k = 0`** — the profile is trivial when no base-`p` carry occurs at any prime layer of `m` (the joint per-prime Kummer data across `m`'s factorisation). **`carryProfileTrivial_iff_no_prime_dvd`** (PROVED) — `CarryProfileTrivial m n k ↔ ∀ p ∈ m.primeFactors, ¬ p ∣ C(n+k,k)` (the entry-236 field-observer link `carryCount_eq_zero_iff_not_dvd` at each prime factor, with `Fact p.Prime` from `Nat.mem_primeFactors`). **`carryProfileTrivial_iff_coprime`** (PROVED, `m ≠ 0`) — `CarryProfileTrivial m n k ↔ Nat.Coprime m (C(n+k,k))`: the composite-modulus observer sees the full count (trivial profile) iff `m` is coprime to the binomial, via `Nat.disjoint_primeFactors` (coprimality ⟺ disjoint prime-factor sets). **`carryProfile_mul`** (PROVED) — the CRT decomposition: for `a, b ≠ 0`, `CarryProfileTrivial (a·b) n k ↔ CarryProfileTrivial a n k ∧ CarryProfileTrivial b n k` (via `Nat.primeFactors_mul`); the composite carry profile is the *join* of the per-factor profiles, factoring through the modulus exactly as the entry-235 CRT residue observer splits `ZMod (a·b) ≃+* ZMod a × ZMod b`. Honest scope: these conservative fragments assemble the per-prime Kummer carries into a composite-modulus observer profile and characterise it (trivial ⟺ no prime layer divides the binomial ⟺ `m` coprime to it) with its CRT/multiplicative structure. This *characterises* which prime layers the mod-`m` observer is blind to; it does **not** cross the composite-`ACC⁰[m]` barrier (entry 234), which remains open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CRTCarryProfile` |
| **Kummer's carry count via factorization — quantitative carry seam (PROVED)** | Makes entry-235's carry seam *quantitative* via Kummer's theorem: the number of carries when adding `n` and `k` in base `p` is the `p`-adic valuation of the binomial `C(n+k, k)` — a genuine factorization invariant. Conservative: no global "obstructs exactification" claim. **`carryCount p n k := padicValNat p (Nat.choose (n+k) k)`**. **`carryCount_digits_identity`** (PROVED) — Kummer in digit-sum form: `(p-1)·carryCount p n k = S_p(k) + S_p(n) - S_p(n+k)` (each base-`p` carry reduces the digit sum by `p-1`); a re-export of Mathlib's `sub_one_mul_padicValNat_choose_eq_sub_sum_digits'`. **`carryCount_eq_zero_iff_digits`** (PROVED) — `carryCount p n k = 0 ↔ S_p(k)+S_p(n)-S_p(n+k) = 0` (no carry iff digit sums add without loss; via `p-1 ≠ 0` and `Nat.mul_eq_zero`). **`carryCount_eq_zero_iff_not_dvd`** (PROVED, the field-observer link) — `carryCount p n k = 0 ↔ ¬ p ∣ C(n+k,k)`: no carry iff the binomial is a `p`-adic unit, i.e. it survives mod `p` and the field/weighted-`F_p` observer sees the count contribution (via `padicValNat.eq_zero_of_not_dvd` and `one_le_padicValNat_of_dvd` with `Nat.choose_pos`). **`carryCount_pos_iff_dvd`** (PROVED) — `0 < carryCount p n k ↔ p ∣ C(n+k,k)`: a carry is present exactly when `p` divides the binomial, i.e. the count contribution is invisible to the mod-`p` observer — the *quantitative* refinement of entry-235 `field_observer_blind_to_carry`. Honest scope: these conservative, quantitative fragments measure the field observer's blindness by `carryCount` (a factorization invariant) and pinpoint a carry as exactly a mod-`p`-invisible binomial divisibility. They *characterise and quantify* the seam where the weighted-`F_p` and exact-unit-count observers diverge; they do **not** cross the composite-`ACC⁰[m]` barrier (entry 234), which remains open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0KummerCarry` |
| **Carry invariant — the p-adic seam at `ApproxToExactCount`, conservative fragments (PROVED)** | The N-Frame *invariant search* at the entry-234 composite-`ACC⁰` barrier, done conservatively: it formalises the **field power-indicator** `y^e = [y ≠ 0]` (the algebraic primitive turning a weighted "this `AND` is satisfied" bit into an *exact* `0/1` count contribution) and proves *safe fragment theorems* about where it exists and where carries force observer refinement — explicitly **not** a global "obstructs exactification" claim. **`PowerIndicator m := ∃ e, 1 ≤ e ∧ ∀ y : ZMod m, y^e = if y = 0 then 0 else 1`**. **`powerIndicator_of_prime`** (PROVED) — *MOD_p: no carry obstruction*: over a prime field the indicator exists with `e = p - 1` (`y^(p-1)=1` for `y≠0` by Fermat, `0^(p-1)=0`). **`not_powerIndicator_of_zeroDivisor`** (PROVED) — the obstruction primitive: a zero-divisor `z` (`z≠0`, `z·w=0`, `w≠0`) kills the indicator (it would force `z^e=1`, whence `w = z^{e-1}·(z·w) = 0`). **`not_powerIndicator_primePow`** (PROVED) — *MOD_{p^e} (e≥2): carry profile nontrivial*: over `ZMod (p^e)` the `ZMod`-level indicator fails since `p` is a zero-divisor (`p·p^{e-1}=p^e=0`, with `p, p^{e-1} < p^e`); RS recovers `p^e` only via the *extension field* `GF(p^e) ≠ ZMod (p^e)`, so the carry/field layer is genuinely needed. **`field_observer_blind_to_carry`** (PROVED) — *the field observer cannot distinguish a p-adic carry*: `k=0` and `k'=p` agree under the mod-`p` observer but differ under the refined mod-`p²` observer (`0≠p` in `ZMod(p²)` since `p<p²`); equivalently the unit-count observer changes under carry refinement. **`crt_residue_observer_suffices`** (PROVED) — *squarefree m: CRT residue observer suffices*: for coprime `a,b`, `ZMod(a·b) ≃+* ZMod a × ZMod b` (Chinese Remainder), so the residue profile determines the value with no carry layer (contrast prime-power). Honest scope: these conservative fragments *characterise* the seam where the weighted-`F_p` observer and the exact-unit-count observer must diverge — the N-Frame "boundary forces observer refinement" picture — but **do not cross** the barrier; nothing here forces exactification for composite `m`, which remains the open `ACC⁰[m]` barrier (entry 234). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CarryInvariant` |
| **RS approx→exact — the composite-`ACC⁰` barrier honestly separated (structural fragment PROVED)** | ⚠️ The highest false-closure-risk target, handled honestly. Entry 204 left `SpanApproxToLowDegRep` (a `3/4`-agreement `F_p`-span approximant of an `ACC⁰` circuit yields an *exact* `LowDegRep`) as one socket bundling the whole hard BT/RS content. This **separates** it into a genuinely-provable easy direction and the precise barrier-atom, proving only the former and naming the barrier loudly. **`ExactUnitCount p D f := ∃ (m) (mono : Fin m → Finset (Fin n)), Injective mono ∧ (∀ j, card ≤ D) ∧ ∀ x, boolToZMod p (f x) = (saCount mono x : ZMod p)`** — `f`'s `F_p` value is *exactly* (everywhere, not `3/4`) the count mod `p` of an injective degree-`≤D` monomial family (the unit-weight `SYM∘AND` target). **`lowDegRep_of_exactUnitCount`** (PROVED): `ExactUnitCount p D f → LowDegRep f D`, with the `SYM` gate `h k := decide ((k : ZMod p) = 1)` decoding the count mod `p` (`f x = true ↔ count ≡ 1`, `false ↔ count ≡ 0`, using `0 ≠ 1` in the field `ZMod p`) — the genuine *easy* direction of the Beigel–Tarui conversion. **`ApproxToExactCount approxHyp p D f := approxHyp → ExactUnitCount p D f`** — the barrier socket: the `3/4`-agreement weighted approximant yields an exact unit-coefficient count representation; it subsumes **(a)** amplification `3/4 → exact` and **(b)** the weighted → unit-coefficient conversion respecting `LowDegRep`'s *injective* family (a weighted `F_p`-sum is not the unweighted `saCount`, and monomials cannot be duplicated). **`lowDegRep_via_amplification`** (PROVED glue): `approxHyp → ApproxToExactCount approxHyp p D f → LowDegRep f D` — exhibiting `ApproxToExactCount` as the *sole* missing input between the proved RS approximant and the `LowDegRep` that the BT collapse (entry 203) consumes. Honest scope: proves the easy structural direction (exact unit-count ⟹ `LowDegRep`) and the factorisation. **`ApproxToExactCount` is the composite-`ACC⁰[m]` barrier** — for prime-power modulus the amplification is available (the polynomial method works), but for composite `m` it provably fails and is separation-strength. This file does **not** close it, and pushing the socket would be false closure. The structural span = weighted-count-of-`AND`s fact is the proved entry-207 `span_eval_weightedCount`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ApproxToExact` |
| **IKW small-prover — easy-witness failure yields a hard function (PROVED extraction)** | Entry 224 proved the guessable-prover collapse *mechanism* and left `GuessableProver` (under the circuit hypothesis an accepting prover strategy has a small-circuit description) as the named Impagliazzo–Kabanets–Wigderson easy-witness socket. This opens that socket as far as the discipline allows, proving the genuine **hardness extraction** at its core and surfacing the honest punchline: the easy-witness lemma's *other horn is a hard function*, so the IKW small-prover bottoms out at the same separation-strength atom as the circuit lower bound. **`IncompressibleAcceptingProver Ver proverOf x := ∃ p, Ver x p ∧ ∀ C, proverOf C ≠ p`** — an accepting prover strategy that equals *no* circuit's decoding (a hard, circuit-incompressible witness). **`not_easy_gives_incompressible`** (PROVED, **no axioms**): if `x` is accepted (`∃ p, Ver x p`) but no circuit gives an accepting prover (`¬ ∃ C, Ver x (proverOf C)`), then `IncompressibleAcceptingProver Ver proverOf x` — the accepting `p` cannot equal any circuit's decoding (else that circuit would be an accepting prover). The failure of easy-witness *is* hardness. **`EasyWitnessLemma circuitHyp := circuitHyp → ∀ x, ¬ IncompressibleAcceptingProver …`** — the IKW socket: the circuit hypothesis `NEXP ⊆ ACC⁰` rules out incompressible accepting strategies. **`guessableProver_of_easyWitness`** (PROVED): discharges the entry-224 `GuessableProver` from `EasyWitnessLemma` (an accepted `x` with no circuit prover would yield an incompressible accepting strategy, which the easy-witness lemma forbids). **`not_guessableProver_gives_incompressible`** (PROVED): `GuessableProver` *failure* yields an input with an incompressible accepting strategy — a hard, separation-strength function (the converse is false; the obstruction is "accepted but no circuit prover", which the extraction turns into hardness). Honest scope: proves the hardness extraction (no axioms) and the discharge logic. What remains the named socket is `EasyWitnessLemma` — that `NEXP ⊆ ACC⁰` *actually* rules out incompressible accepting witnesses, the IKW easy-witness theorem (via the Karp–Lipton collapse `NEXP ⊆ ACC⁰ ⟹ NEXP = MA`, entries 202/221, plus the witness-search hardness argument). Crucially, the obstruction it must defeat is the same separation-strength hard-function atom as step 5: the IKW small-prover does not reduce below the circuit lower bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0IKWSmallProver` |
| **Low-degree arithmetization — the multilinear extension (PROVED), discharging `LowDegArithmetization`** | Entry 226 proved arithmetization *computes the accepting count* but left `LowDegArithmetization` (the predicate has a *low-degree* polynomial agreeing with it on the hypercube) as a socket. This **constructs that polynomial explicitly** — the multilinear extension — and proves both halves. For `f : (Fin m → Bool) → Bool`, `mle f (x) = ∑_{a∈{0,1}^m} f(a)·∏_i χ(x_i,a_i)` with coordinate factor `χ(xi,ai) = xi` if `ai=true`, `1-xi` if `ai=false`. **`chi_boolToR`** (PROVED): `chi (boolToR bi) ai = [ai = bi]` (single-coordinate equality indicator, by cases). **`prod_chi_eq`** (PROVED): `∏_i chi (boolToR (b i)) (a i) = [a = b]` — the full equality indicator (`a=b`: all factors `1`; `a≠b`: a vanishing factor via `Finset.prod_eq_zero` at the differing coordinate). **`mleB_agrees`** (PROVED): `mleB f (boolToR∘b) = boolToR (f b)` — only the `a=b` term of the sum survives (`Finset.sum_eq_single`), giving `f(b)`; the MLE agrees with `f` on the hypercube. **`chiP_degree`** (PROVED, `[Nontrivial R]`): each coordinate factor `(chiP i ai).totalDegree ≤ 1` (`totalDegree_sub`/`totalDegree_X`). **`prod_chiP_degree`** (PROVED): each monomial `(∏_i chiP i (a i)).totalDegree ≤ m` (`totalDegree_finset_prod` + `m` degree-`≤1` factors). **`mleP_degree`** (PROVED): `(mleP f).totalDegree ≤ m` (`totalDegree_finset_sum` + `totalDegree_mul` + `totalDegree_C`) — the MLE is genuinely low-degree (multilinear), the property the entry-227 Schwartz–Zippel bound consumes. **`mleP_eval`** (PROVED): `eval x (mleP f) = mleB f x` — the `MvPolynomial` `mleP f` *is* the function-level MLE, so the *same* object is low-degree and agrees on the cube. **`mleP_arithmetizes`** (PROVED): discharges entry-226 `LowDegArithmetization f (eval · (mleP f))` with this concrete low-degree witness. Honest scope: proves the multilinear extension completely — agreement on the hypercube (discharging `LowDegArithmetization`) *and* total degree `≤ m` — tied by `mleP_eval`. No socket remains for the MLE itself. What this does not do: wire `f` to the *specific* `NEXP` certificate-acceptance predicate (`f` = the verifier of a `NEXP` machine, `m` polynomial in its running time) — the machine-level `NexpArithmetization` content. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Multilinear` |
| **ClockedSimulation cost — the universal lazy-diagonal simulation fits the NEXP budget (PROVED accounting)** | Proves the genuine cost-accounting half of the entry-223 `ClockedSimulation` socket, connecting the lazy-diagonalization substrate of entries 219 (lazy contradiction) / 220 (`detRun_reachIn` clocked run) / 223 (`clocked_in_NEXP` placement) into one tighter hierarchy theorem. **`sim_budget_fits`** (PROVED): exponent additivity `g + s ≤ F → 2^g · 2^s ≤ 2^F` (`pow_add` + `Nat.pow_le_pow_right`) — overhead `2^g` times simulated-steps `2^s` composes by adding exponents. **`exp_bound`** (PROVED, every `n`, `a ≥ 1`): `n + n^a + a ≤ n^(a+1) + (a+1)` (cases `n=0,1` explicitly; `n≥2` via `n^(a+1) = n·n^a ≥ 2n^a = n^a + n^a ≥ n^a + n`). **`nat_overhead_bound`** (PROVED): `2^n · 2^(n^a+a) ≤ 2^(n^(a+1)+(a+1))` — the universal simulator's per-step overhead `2^n` times the simulated machine's `NTIME(2^(n^a+a))` budget still fits the `NTIME(2^(n^(a+1)+(a+1)))` budget (the cost-accounting witness that the NEXP budget is large enough). **`detRun_accepts`** (PROVED, `[propext]`): a deterministic transition-table run (entry 220 `detRun_reachIn`) reaching accepting state `1` in `T` steps witnesses `acceptsWithin (detNTM δ) x T` (the bridge from 220's machine to 223's `acceptsWithin`). **`lazy_clockedSim`** (PROVED, `[propext]`): from `hbudget` (cost ≤ budget — the *proved* accounting), `hcomplete` (the sim accepts within its cost when `L x`), and `hsound` (no spurious acceptance within the budget), the clocked simulation `ClockedSimulation L (detNTM δ) c` holds; the **forward** direction `L x → acceptsWithin … (2^(|x|^c+c))` is *proved* from `hcomplete` + `hbudget` + `acceptsWithin_mono` — the cost accounting doing real work. **`lazy_universal_in_nexp`** (PROVED): composing with entry-223 `clocked_in_NEXP`, the lazy diagonal lives in `NEXP`. Honest scope: proves the cost accounting (the budget fits) and the completeness lifting (forward direction via monotone budget). What remain named sockets are the universal-simulation *correctness* facts the accounting cannot replace: `hcomplete` (the universal table actually accepts within its simulation cost when `L x`) and `hsound` (no spurious acceptance within the larger budget — the clocked-decider stability) — the decode → step → re-encode correctness of the universal machine (the `…ACC0UniversalHStep` contract). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ClockedSimulation` |
| **Arithmetization as counting — `scSum (arith) = #accepting` (PROVED), low-degree socketed** | Discharges the counting content of the entry-225 `NexpArithmetization` BFL socket. In BFL the `NEXP` claim is `H = ∑_{b∈{0,1}^m} g(b)` for an arithmetized predicate `g`; for `H` to mean "number of accepting certificates", `g` must agree with the boolean acceptance predicate `f` on the hypercube. **`scSum_indicator`** (PROVED): if `∀ b, g(boolToR∘b) = boolToR (f b)`, then `scSum g = (#{b | f b = true} : R)` — the hypercube sum is exactly the accepting count (substitute the agreement, rewrite `boolToR (f b)` as `if f b then 1 else 0`, sum via `Finset.sum_boole`). **`nexpArith_counting`** (PROVED): the arithmetized claim `scSum g` equals the accepting count given `LowDegArithmetization f g`. Honest scope: proves that arithmetization *computes the accepting count* — the meaning of "`H` = acceptance count" that makes the sum-check claim well-posed — on top of the entry-225 `scSum`. What remains the named socket is `LowDegArithmetization` (the multilinear-extension degree bound realising the agreement), needed so the round polynomials are low-degree for Schwartz–Zippel. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Arithmetization` |
| **Sum-check soundness — Schwartz–Zippel in one variable (PROVED), multilinearity socketed** | Discharges the root-bound core of the entry-225 `SumCheckSoundness` BFL socket — the per-round soundness amplifier that catches a cheating prover. Over a finite field `F`: **`agreement_le_degree`** (PROVED): for `p ≠ q`, the number of field points where `p(r) = q(r)` is `≤ (p-q).natDegree` (the agreement set injects into the roots of `p - q ≠ 0`; `mem_roots` + `Polynomial.card_roots'`). **`agreement_le_max`** (PROVED): distinct degree-`≤ d` polynomials agree at `≤ d` points (`natDegree_sub_le` + `max_le`). **`few_agreements`** (PROVED): if `d < |F|`, distinct degree-`≤ d` polynomials disagree somewhere — a separating challenge `r` with `p(r) ≠ q(r)` exists (so a random challenge from a large field catches a lying prover). Honest scope: proves the single-variable Schwartz–Zippel root bound completely. What remains the named socket is `SumCheckRoundSoundness` (that the round polynomials are *actually* degree `≤ d` — the multilinearity bound — and the multi-prover multilinearity test that makes the protocol `MIP` not single-prover `IP = PSPACE`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SchwartzZippel` |
| **MIP ⊆ NEXP — the prover table is the nondeterministic guess (PROVED mechanism)** | Discharges the entry-225 `MipSubsetNexp` simulation socket. An `MIP` acceptance `∃ prover, Ver x prover` is *already* a nondeterministic existential — a `NEXP` machine guesses the exponential-size prover table and verifies. **`NexpGuessLang Check := fun x => ∃ w, Check x w`** (the guess-and-check language). **`mipLang_eq_nexpGuess`** (PROVED, `rfl`, **no axioms**): `MIPLang Ver = NexpGuessLang Ver` — the existential over prover strategies *is* the `NEXP` guess. **`mipSubsetNexp_of_realized`** (PROVED, no axioms): discharges `MipSubsetNexp MIP NEXP` from `MIPRealizedInNexp` (every `MIP` language is an `MIPLang Ver` whose guess-language lands in `NEXP`). Honest scope: proves that the `MIP` existential *is* a nondeterministic guess and threads it into the socket. What remains the named socket is `MIPRealizedInNexp` (the verifier `Ver` is exp-time checkable given the prover table — the time-accounting). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MipSimulation` |
| **PRG ⟹ MA ⊆ NP — Arthur's coins replaced by seed enumeration (PROVED mechanism)** | Discharges the entry-222 `PRGCollapsesMAtoNP` socket. An `MA` computation is `∃ Merlin message, Arthur's randomised check accepts`; a PRG fooling Arthur lets the randomised check be replaced by a deterministic enumeration over the poly-many PRG seeds, giving `∃ Merlin, deterministic check` = `NP`. **`PRGFools Arthur ArthurDet := ∀ x w, Arthur x w ↔ ArthurDet x w`** (the fooling socket). **`maLang_eq_detLang`** (PROVED, `[propext, Quot.sound]`): `PRGFools Arthur ArthurDet → MALang Arthur = MALang ArthurDet` — the fooled deterministic check preserves the language. **`prgCollapses_of_realizes`** (PROVED): discharges `PRGCollapsesMAtoNP PRGExists MA NP` from `PRGRealizesNP` (the PRG provides, per `MA` language, a fooling deterministic check whose language lands in `NP`). Honest scope: proves that a fooling deterministic check preserves the language (the `MA → NP` collapse mechanism). What remains the named socket is `PRGFools` (that the PRG *actually* fools Arthur — the threshold over seeds matches the threshold over true coins). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SeedEnumeration` |
| **Derand ⟹ PRG — the Nisan–Wigderson reconstruction contradiction (PROVED glue)** | Discharges the entry-222 `DerandGivesPRG` socket via its genuine logical core. The NW generator (hard function `f` + low-intersection design, entries 191–196) fools small circuits *because* a distinguisher would reconstruct a small circuit for `f`, contradicting `f`'s hardness. **`NWReconstruction D S := D → S`** (a distinguisher yields a small circuit for `f`; entries 191–196 content, abstracted). **`prgFools_of_hard`** (PROVED, **no axioms**): `NWReconstruction D S → ¬ S → ¬ D` — reconstruction + hardness give "no distinguisher" (PRG fools), via `fun d => hard (recon d)`. **`derandGivesPRG_via_nw`** (PROVED, no axioms): discharges `DerandGivesPRG Derandomization (¬ Distinguisher)` from `DerandGivesHardFn` (a derandomisation supplies the reconstruction + the hard function). Honest scope: proves the reconstruction contradiction (the composition logic). What remain named sockets are `NWReconstruction` (the hybrid + Yao + reconstruction analysis) and the IW hard function supplied by the derandomisation (`DerandGivesHardFn`, the hardness-to-randomness tradeoff). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NWFromHardness` |
| **The guessable-prover collapse — Merlin guesses the prover circuit, Arthur verifies (mechanism PROVED)** | Opens the entry-221 `MIPSubsetMA_ofCircuits` socket (the second BFL-IKW ingredient of the Karp–Lipton chain: `MIP ⊆ ACC⁰ ⟹ MIP ⊆ MA`) into its genuine mechanism and proves the collapse mechanism, isolating the IKW small-prover content. An `MIP` computation accepts `x` iff *some* prover strategy makes the verifier accept; under circuits the optimal prover is itself a *small circuit*, so the existential over all provers collapses to one over small circuits (Merlin's guess). **`MIPLang Ver := fun x => ∃ p, Ver x p`** (the MIP language — `Ver` bundles the high-prob check; the ∃-over-provers is the `NEXP`-strength guess), **`MALang Arthur := fun x => ∃ w, Arthur x w`** (the MA language). **`GuessableProver Ver proverOf := ∀ x, (∃ p, Ver x p) → ∃ C, Ver x (proverOf C)`** — the IKW small-prover socket: every accepted input has an accepting prover *described by a small circuit*. **`mipLang_eq_maLang`** (PROVED, `[propext, Quot.sound]`): `GuessableProver Ver proverOf → MIPLang Ver = MALang (fun x C => Ver x (proverOf C))` — the MIP language *equals* the MA language "Merlin guesses the prover circuit `C`, Arthur evaluates the verifier on `proverOf C`". The two directions are visible: **backward** (`MA ⊆ MIP`) is FREE — a circuit-described prover `proverOf C` *is* a prover (`⟨proverOf C, hC⟩`); **forward** (`MIP ⊆ MA`, deep) is exactly the `GuessableProver` socket. **`mipLang_mem_ma`** (PROVED): with `GuessableProver` and the collapsed MA-language in `MA`, `MIPLang Ver ∈ MA`. **`MIPRealizedGuessable MIP ACC0 MA`** (the class-level realization socket: under `MIP ⊆ ACC⁰` every MIP language is an `MIPLang` with a guessable prover whose collapsed MA-language lands in `MA`). **`mipSubsetMA_of_realized`** (PROVED, `[propext, Quot.sound]`): `MIPRealizedGuessable MIP ACC0 MA → MIPSubsetMA_ofCircuits MIP ACC0 MA` — **discharges the entry-221 socket** by threading `mipLang_eq_maLang` through the realization. Honest scope: proves the **guessable-prover collapse mechanism** — that the MIP verifier's language *equals* the "guess-the-prover-circuit" MA language — completely, including the free backward inclusion (a circuit-prover is a prover) and the threading into the class-level `MIPSubsetMA_ofCircuits`. What remains the named socket is `GuessableProver` (bundled into `MIPRealizedGuessable`): that under circuits an accepting prover strategy *has a small-circuit description* — the Impagliazzo–Kabanets–Wigderson small-prover / easy-witness theorem, needing IKW machinery absent here. This proves the collapse mechanism and its composition, not the small-prover theorem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GuessableProver` |
| **NTIME class accounting — the lazy diagonal machine lives inside `NEXP` (placement PROVED)** | Completes the hierarchy side's **resource-boundary accounting**: turns "diagonal *mechanism* proved" (entry 219 `lazy_diag_false`) + "clocked *run* proved" (entry 220 `detRun_reachIn`) into "time-class *placement* proved". The `…ACC0NTM` definitions: `NTIME f = {L \| ∃ M, ∀ x, L x ↔ acceptsWithin M x (f \|x\|)}` and `NEXP = ⋃_c NTIME(2^{n^c+c})`. So a clocked machine `M` deciding `L` within `2^{\|x\|^c+c}` steps places `L ∈ NTIME(2^{n^c+c}) ⊆ NEXP` directly. **`accepts_within_budget`** (PROVED, `[propext]`): the cost-fits-budget step `acceptsWithin M x s → s ≤ t → acceptsWithin M x t` (the simulation cost `s` absorbed into the bigger budget `t`), `= acceptsWithin_mono`. **`ClockedSimulation L M c := ∀ x, L x ↔ acceptsWithin M x (2^{\|x\|^c+c})`** (the named socket: `M` decides `L` within the `NEXP` budget). **`clocked_in_NTIME`** (PROVED): `ClockedSimulation L M c → L ∈ NTIME(2^{n^c+c})`, `= ⟨M, h⟩`. **`clocked_in_NEXP`** (PROVED, `[propext]`): `ClockedSimulation L M c → L ∈ NEXP`, `= ⟨c, clocked_in_NTIME …⟩`. **`lazy_diagonal_in_nexp`** (PROVED, `[propext]`): the lazy diagonal `∈ NEXP` — the entry-200 `DiagonalInNexp` membership discharged from the clocked-machine model (entries 219–220), reduced to the `ClockedSimulation` socket. Honest scope: proves the **time-class placement** (a clocked machine deciding `L` within the `NEXP` budget puts `L ∈ NEXP`) completely, via the `NTIME`/`NEXP` definitions and `acceptsWithin_mono`. Combined with entry 219 (the lazy diagonal escapes any single machine) this is the hierarchy's `NEXP`-membership side, now from the clocked-machine model rather than assumed. What remains the named socket is `ClockedSimulation` for the lazy diagonal itself: that a clocked `NTM` *does* decide the lazy diagonal within the `NEXP` budget — the universal simulation of `M_i` on the next input across the block, plus the one boundary complement, clocked to `2^{\|x\|^c+c}` steps (building on 219–220) — now reduced to the *lazy-feasible* form (one complement, not closure under complement), the genuine remaining machine-cost content. This proves the placement, not the universal simulation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NTIMEAccounting` |
| **prime-power `MOD` — CRT divisibility generalises, the low-degree field gate does NOT (the obstruction)** | The honest boundary of the algebraic-observer route. PROVED (what generalises): **`primePower_coprime`** (distinct prime powers coprime), **`primePowerDvd_iff`** (`(p^a·q^b) ∣ s ↔ (p^a∣s ∧ q^b∣s)`, CRT), **`modPrimePower_observer_decides`** (`(s : ZMod (p^e)) = 0 ↔ p^e ∣ s`, ring residue observer). NOT provable (the obstruction, documented not faked): the low-degree *field* gate polynomial — `ZMod (p^e)` (`e≥2`) is not a field, `p` is a zero divisor, no Fermat-style indicator. This is exactly the `ACC⁰[composite]` barrier (polynomial method gives `MOD_p`/`AC⁰[p]` but `ACC⁰[m]` composite is open); the squarefree case worked by avoiding prime powers (distinct primes → product of fields). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerGate` |
| **the `AND`-layer assembly — the `AND`-count as a polynomial under the `MOD` gate** | Connects the bottom `AND` layer to the `MOD` gate polynomial. **`andCountPoly = ∑_j ∏_{i∈supports j} X_i`** (sum of `AND`-feature monomials). **`andCountPoly_eval`** (PROVED): `eval (boolVal∘x) andCountPoly = satCount` over `F_p` — the count of satisfied `AND`s (via `prod_boolVal_eq_andFeature` + `card_filter`/`Nat.cast_sum`). **`andCountPoly_degree`** (`≤ w` for `AND`-width `≤ w`). **`modPAndPoly = aeval ![andCountPoly] (modPGate p)`** (the `MOD_p∘AND` polynomial); **`modPAndPoly_degree`** (PROVED): degree `≤ (p−1)·w` (gate degree `p−1` × `AND`-count degree `w`, via `unaGate_degree`). Wires the `AND` features to the `MOD` gate; the composite-`MOD` pipeline is now `AND → count → MOD → compositeBT`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AndLayer` |
| **integration — the `MOD_p` gate polynomials feed `compositeBT_representation`** | Wires the composite-`MOD` algebraic observer into the composite-BT pipeline. **`modP_circuit_representation`** (PROVED): a `MOD_p`-gate circuit (unary gates `= modPGate p`, degree `≤ p−1` by `modPGate_degree`; binary gates degree `≤ p−1`), composed via `aeval`, gets the quasipolynomial low-degree sparse representation with `δ = p−1` — degree `≤ (p−1)^{depth+1}`, `≤ (n+1)^{(p−1)^{depth+1}}` features, sparse cube count — by instantiating `compositeBT_representation`. For constant depth `(p−1)^{O(1)}` is `polylog` ⇒ genuinely quasipolynomial. The `MOD_p` gate polynomial is a valid degree-`δ` gate; binary `AND`/`OR` kept as a degree-`≤ p−1` parameter. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeBTIntegration` |
| **CRT over a `Finset` of primes — arbitrary squarefree composite `MOD` gate** | Iterates two-prime CRT to a finset. **`prod_primes_dvd_iff`** (`(∏ p ∈ S, p) ∣ s ↔ ∀ p ∈ S, p ∣ s` for `S` a finset of primes; `Finset.prod_primes_dvd` backward + `dvd_prod_of_mem` forward). **`modGateProd_decides`** (PROVED): `(∀ p ∈ S, modPGate p fires at s mod p) ↔ (∏ p ∈ S, p) ∣ s` — the family of exact low-degree (`p−1`) prime gate polynomials over `∏ F_p` decides `MOD_m` for any squarefree `m = ∏ S`. Completes the squarefree composite case. Remaining: prime powers `p^e` (`ZMod p^e` not a field), `AND`-layer, `compositeBT_representation`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CRTFinsetGate` |
| **CRT generalisation — the prime-modulus gate polynomial, composite via coprime CRT** | Generalises `MOD₆`'s gate polys to arbitrary prime/squarefree modulus. **`modP_indicator`** (Fermat: `1 − a^{p−1} = [a=0]` over `F_p`). **`modPGate p = 1 − X^{p−1}`** (degree `p−1` over `F_p`, the general form of both `MOD₆` components — `p=2`: `1−X=1+X`, `p=3`: `1−X²`); **`modPGate_apply`/`modPGate_degree (≤ p−1)`/`modPGate_eq_one`**. **`modPGate_decides_dvd`** (`eval (s) = 1 ↔ p ∣ s` — computes `MOD_p`). **`modPQGate_decides`** (PROVED): for distinct primes `p ≠ q`, both gates fire iff `p·q ∣ s` (coprime CRT via `residue_observer_compose_coprime` + `Nat.coprime_primes`) — arbitrary squarefree two-prime composite. Remaining: prime powers `p^e`, `Finset`-product, `AND`-layer, feed into `compositeBT_representation`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CRTGatePolys` |
| **composite-`MOD` gate polynomials — the `MOD₆` exact low-degree local gate polynomial** | The first concrete composite-`MOD` gate polynomial, over the product-residue observer. **`mod6GateF2 = 1+X`** (degree 1 over `F₂`), **`mod6GateF3 = 1−X²`** (degree 2 over `F₃`); **`mod6GateF2_apply`/`mod6GateF3_apply`** (compute the residue indicators, via `mod2_indicator`/`mod3_indicator`); **`mod6GateF2_degree ≤ 1`/`mod6GateF3_degree ≤ 2`** (low-degree — valid degree-`δ` gate polys). **`mod6Gate_decides`** (PROVED): both components fire iff `6 ∣ s` — the gate computes `MOD₆` of the count exactly (via `mod6_decided_by_residue_pair`). The concrete composite-`MOD` gate polynomial for `MOD₆` (the open ACC⁰-side input, base case); remaining = CRT generalisation to arbitrary composite modulus + `AND`-layer + feed into `compositeBT_representation`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeGatePolys` |
| **the IKW easy-witness lemma — proof architecture (contrapositive glue proved, deep steps socketed)** | Decomposes the deepest Williams ingredient. **`NoEasyWitnessHardFn SmallWitnessCircuits HardFunction := ¬SmallWitnessCircuits → HardFunction`** (socket: no easy witnesses ⇒ a hard truth table). **`HardFnSeparation NEXP ACC0 HardFunction := HardFunction → ¬(NEXP⊆ACC0)`** (socket: hard function derandomises via Nisan–Wigderson + Karp–Lipton collapse ⇒ separation). **`easyWitness_from_parts`** (PROVED, contrapositive glue): the two ⇒ `EasyWitnessLemma` (assuming `NEXP⊆ACC⁰`, witnesses must be easy else (1)+(2) refute it; `Classical.byContradiction`). **`nexp_not_acc0_full`** (PROVED): IKW parts + guess-verify + `NondetTimeHierarchy` + speedup ⇒ `¬(NEXP⊆ACC⁰)`, reducing the separation to `{NoEasyWitnessHardFn, HardFnSeparation, GuessVerify, NondetTimeHierarchy}`. The two deep sub-lemmas (hard-fn extraction, derandomisation) are NOT proved. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0IKWEasyWitness` |
| **guess-and-verify budgets — the budget closure proved, verify machine socketed** | Closes the verify-time budget via the composite-BT feature count. **`budget_split`** (`guessT ≤ b → verifyT ≤ b → 2·b ≤ target → guessT + verifyT ≤ target`). **`guess_verify_fits`** (PROVED): the two physical phases (guess reaches `mid` in `≤ b` steps, verify accepts in `≤ b` steps) with `2·b ≤ target` give `acceptsWithin M x target`. Taking `b = (n+1)^{δ^{depth+1}}` (the composite-BT feature count from `compositeBT_representation`), the verify time is quasipolynomial — the cube sum over `≤ (n+1)^D` features, not `2^n`. The budget arithmetic + composition proved; remaining sockets = the two physical phase machines (guess `poly`, verify = enumerate features = fast `ACC⁰`-SAT). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GuessVerifyBudgets` |
| **composite Beigel–Tarui capstone — the quasipoly low-degree sparse representation, packaged** | Assembles the proved approximation machinery into one theorem. **`compositeBT_representation`** (PROVED): for a gate-polynomial assignment `Q` composed via `aeval` from degree-`≤ δ` gate polynomials, the circuit polynomial `Q c` has (1) total degree `≤ δ^{depth c+1}` (quasipoly for const depth, via `psubst_degree` + `aeval` degree discharge), (2) `≤ (n+1)^{δ^{depth c+1}}` distinct monomial-supports (via `support_mem_lowDeg` + `beigelTarui_monomial_count_le`), (3) sparse cube count `∑ₓ eval = ∑_{d} coeff·2^{n-|supp|}` (via `multilinear_cube_sum`). The quasipolynomial low-degree sparse `SYM∘AND` representation. Assumptions explicit: gate-poly degrees `≤ δ` (the `OR`/`MOD` probabilistic polys) + `aeval` composition. Error side = separate `circuit_error_bound`+`error_calibration`. The cleanest remaining ACC⁰-side packaging. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeBTCapstone` |
| **guess-and-verify, the time bound — two-phase time composition proved** | The decider's complexity is two phases (guess + verify), additive in time. **`acceptsFrom M start t`** (acceptance from a start config within budget `t`). **`acceptsWithin_compose`** (PROVED): `reachIn M guessT (init x) mid` + `acceptsFrom M mid verifyT` ⇒ `acceptsWithin M x (guessT+verifyT)` (via `reachIn_add` — the phases compose additively). **`guess_verify_within`** (PROVED): with `guessT+verifyT ≤ target`, accepts within `target` — the guess-and-verify decider fits the `NTIME[2ⁿ/superpoly]` budget once the two phase budgets do. The time composition is proved; the remaining sockets are the two phase budgets (guess `poly`, verify `2ⁿ/superpoly` via fast SAT — the actual fast-SAT/read-off content). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GuessVerifyTime` |
| **guess-and-verify — construction correctness proved, complexity socketed** | Williams' guess-and-verify ingredient, made precise at the verifier/language level. **`HasWitness L V := ∀x, L x ↔ ∃w, V x w`** (verifier characterisation); **`SmallWitnessRed V decode := ∀x, (∃w, V x w) → ∃cw, V x (decode cw)`** (IKW small-witness reduction); **`gvDecider V decode x := ∃cw, V x (decode cw)`** (guess a small circuit, verify). **`gvDecider_eq`** (PROVED): `gvDecider V decode x ↔ L x` — the decider decides exactly `L` (guessing only small witnesses loses nothing). **`guessVerify_subset`** (PROVED glue): if every `NTIME2n` language has such a verifier whose guess-and-verify decider lies in `NTIME2nFast`, then `NTIME2n ⊆ NTIME2nFast` (decider `= L`, inherits complexity). The construction's correctness is proved; the remaining socket is the decider's time bound (guess `poly` + verify `2ⁿ/superpoly` via fast SAT). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GuessVerify` |
| **the universal decode brick — recover `(machine, config)` from the tape** | Next brick of the physical universal machine: extract the simulation state from `U`'s tape. **`encodeSim M c := encodeTape ⟨M⟩ c`** (code + config on tape), **`decodeSim bs := (decodeTape bs).map (fun p => (machineEquiv.symm p.1, p.2))`** (parse to code+config, decode code to machine). **`decodeSim_encodeSim`** (PROVED): `decodeSim (encodeSim M c) = some (M, c)` — the round-trip recovers the machine and configuration (tape encoding + `machineEquiv`). **`decoded_machine_steps`** (PROVED): decode then step — tape decodes to `(M,c)` and the decoded machine reaches any `concreteStep` successor in one step (`reachIn_one`). The decode function + round-trip (the contract); physical scan/parse as `U`-transitions and the full decode→step→re-encode loop in `B` steps remain the construction. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalDecode` |
| **the physical single-step base — atomic-step correctness (first brick of the universal machine)** | Begins the physical `hstep` construction (option a). **`reachIn_one`** (PROVED): `reachIn (toNTM M) 1 c d ↔ concreteStep M c d` — one reachability step is exactly one transition (`simp [reachIn, exists_eq_right]` + `toNTM_step`). **`firing_rule_step`** (PROVED): `t ∈ M → t.1 = (c.1, readSym c) → reachIn (toNTM M) 1 c (applyTrans c t)` — a firing rule advances in one physical step. Genuine machine-transition content (the atomic base the physical simulation iterates). NOT the universal machine: the universal `U` decoding `M` from its own `encodeTape` and performing `M`'s step via its own transitions in `B` steps (assembling lookup/head/rewrite) is the large remaining construction. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PhysicalStep` |
| **the easy-witness collapse — proof architecture (glue proved, IKW + guess-verify socketed)** | Decomposes Williams' other deep socket `EasyWitnessCollapse`. **`EasyWitnessLemma NEXP ACC0 SmallWitnessCircuits := NEXP⊆ACC0 → SmallWitnessCircuits`** (socket: IKW easy-witness lemma). **`GuessVerify NTIME2n NTIME2nFast ACC0SatSpeedup SmallWitnessCircuits := SmallWitnessCircuits → ACC0SatSpeedup → NTIME2n⊆NTIME2nFast`** (socket: guess the small witness circuit, verify with fast SAT). **`easyWitnessCollapse_from_parts`** (PROVED, axiom-free): IKW ∘ guess-verify = `EasyWitnessCollapse`. **`nexp_not_acc0_from_witness_parts`** (PROVED, axiom-free): the two ingredients + `NondetTimeHierarchy` + speedup ⇒ `¬(NEXP⊆ACC⁰)`, reducing the separation to `{EasyWitnessLemma, GuessVerify, NondetTimeHierarchy}`. The two deep ingredients (circuit complexity + nondet simulation) are NOT proved. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0EasyWitness` |
| **the tape-rewrite sub-machine — write contract proved** | Third sub-machine of the physical universal machine: rewrite the simulated tape (`writeAt`). **`writeAt_getD_self`** (read-after-write: `(writeAt tape p w).getD p false = w`); **`writeAt_getD_ne`** (unchanged elsewhere: `q≠p → (writeAt tape p w).getD q false = tape.getD q false`, via `appendReplicate_getD` + `getElem?_set_ne`); **`writeAt_length`** (minimal extension: `= max tape.length (p+1)`); **`applyTrans_write`** (a rule writes its symbol at the old head: `(applyTrans c t).2.2.getD c.2.1 false = t.2.2.1`). The write contract the physical rewrite sub-machine must satisfy; realising it as `U`-transitions over `encodeTape` remains the socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TapeRewrite` |
| **the head-location sub-machine — head-movement locality proved** | Second sub-machine of the physical universal machine: locate the simulated head. The head is explicit in the config; the key property is locality. **`moveHead_le_succ`** (`moveHead h m ≤ h+1`, any move advances ≤1 cell). **`step_head_le`** (`concreteStep M c d → d.2.1 ≤ c.2.1+1`). **`reachIn_head_le`** (PROVED, induction): `reachIn (toNTM M) k c d → d.2.1 ≤ c.2.1 + k` — the simulated head stays within `head₀+k` after `k` steps, bounding the tape region the physical head-location sub-machine traverses (`O(k)` per step). The locality/region bound is proved; realising the navigation as `U`-transitions over the `encodeTape` layout remains the socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0HeadLocation` |
| **the rule-lookup sub-machine — specification proved (step = pick matching rule and apply)** | First sub-machine of the physical universal machine: find `M`'s transitions matching the simulated `(state, symbol)`. **`matchingRules M state sym := M.filter (·.1 = (state,sym))`**; **`mem_matchingRules`** (membership). **`concreteStep_iff_matching`** (PROVED): `concreteStep M c d ↔ ∃ t ∈ matchingRules M c.1 (readSym c), d = applyTrans c t` — the nondeterministic step IS selecting a matching rule and applying it (the lookup's correctness contract). **`matchingRules_length_le`** (the lookup scans `≤ M.length` rules — the cost bound). The lookup spec is proved correct against `concreteStep`; realising it as `U`-transitions scanning the encoded table on the tape remains the socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RuleLookup` |
| **the tape-layout encoding — faithful binary serialization of `(code, config)`** | The encoding the physical universal machine operates on. **`bitEquiv : List Bool ≃ ℕ`** (binary-string ↔ number, via `Denumerable`). **`encodeTape code c := bitEquiv.symm (Encodable.encode (code, c))`** (serialise machine code + simulated config to a binary tape), **`decodeTape bs := Encodable.decode (bitEquiv bs)`** (decoder). **`decodeTape_encodeTape`** (PROVED): `decodeTape (encodeTape code c) = some (code, c)` — the round-trip, so the tape determines the simulated config uniquely (faithful). **`encodeTape_inj`** (PROVED): same tape ⇒ same code and config. The faithful encoding for `hstep`; reading/rewriting it as `U`-transitions in one simulated step remains the socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TapeEncoding` |
| **the single-step simulation lemma — linear overhead accumulation; physical single step socketed** | Formalises the single-step simulation property of a physical universal machine and proves its consequences. Setup: `U : NTM`, encoding `enc : CConfig → U.Config`, per-step overhead `B`, single-step hypothesis `hstep : concreteStep M c d → reachIn U B (enc c) (enc d)`. **`sim_multi`** (PROVED): `reachIn (toNTM M) k c d → reachIn U (k·B) (enc c) (enc d)` (induction on `k`, splitting `(k+1)·B = B + k·B` via `reachIn_add`). **`sim_acceptsWithin`** (PROVED): `acceptsWithin (toNTM M) x t → acceptsWithin U x (t·B)` (overhead linear in steps), given `enc` carries initial/accept configs. The accumulation + overhead bound are proved; the single physical step (constructing `U`'s transition table to decode a rule, find the head, rewrite the simulated tape) is the remaining socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SimulationStep` |
| **universal interpreter simulation — `enum_covers` discharged; hierarchy down to one socket** | The universal simulation at the interpreter level (no physical overhead). **`univStep code := concreteStep (machineEquiv.symm code)`** with **`univStep_correct`** (`univStep ⟨M⟩ = concreteStep M` — one relation simulates any machine exactly). **`cNTIME f`** (concrete `NTIME` over encodable `TMachine`s), **`enum f k := langOf (machineEquiv.symm k) f`** (machine enumeration). **`enum_covers`** (PROVED): `cNTIME f ⊆ Set.range (enum f)` — every concrete-`NTIME` language equals `enum f ⟨M⟩`, **discharging a hierarchy socket** via `machineEquiv`. **`diag_not_mem_cNTIME`** (PROVED: diagonal `∉ cNTIME g`, Cantor + `enum_covers`). **`cTime_hierarchy`** (PROVED conditional): given the single remaining socket `diag_in_big` (`diagLang ∈ cNTIME f`), `¬(cNTIME f ⊆ cNTIME g)`. The interpreter sim has no overhead; `diag_in_big` still needs a physical universal machine with a verified overhead bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalNTM` |
| **concrete encodable NTM model — machines are enumerable** | The complexity-theory infrastructure the abstract model lacked. `Move := Fin 3`, `TMTrans := (ℕ×Bool)×(ℕ×Bool×Move)` (rule `(state,read)↦(state',write,move)`), `TMachine := List TMTrans` (nondet transition table), `CConfig := ℕ×ℕ×List Bool` (state,head,tape); `readSym`/`moveHead`/`writeAt`/`applyTrans`/`concreteStep` give the binary-tape semantics. **`machineEquiv : TMachine ≃ ℕ`** (PROVED, via `Encodable`+`Infinite`+`Denumerable.ofEncodableOfInfinite`) — machines are enumerable, the encode half the hierarchy `enum_covers` socket needs. **`toNTM`** bridges each concrete machine to an abstract `…ACC0NTM.NTM` (start `0`, accept state `1`), with `toNTM_step`/`_init`/`_accept`/`concrete_step_iff`. The encodable model + enumeration; the time-bounded universal simulator (to discharge `enum_covers`/`diag_in_big`) is the next step. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ConcreteNTM` |
| **the time-hierarchy diagonalisation — Cantor core proved, simulation/enumeration socketed** | The logical heart of the nondeterministic time hierarchy, machine-checked. **`idxEquiv`** (`List Bool ≃ ℕ` via `Denumerable.ofEncodableOfInfinite`). **`diagLang enum idx := fun w => ¬ enum (idx w) w`** (the diagonal language). **`diag_not_mem_range`** (PROVED, Cantor: `diagLang ∉ Set.range enum` — at `idx⁻¹ k`, `enum k = ¬ enum k`, absurd via `iff_not_self`). **`time_hierarchy_from_sockets`** (PROVED diagonal argument): given `enum_covers` (`NTIME g ⊆ range enum`, enumerability) and `diag_in_big` (`diagLang ∈ NTIME f`, simulatability in the bigger bound), `ConcreteHierarchy f g` holds. The diagonal ARGUMENT is fully proved; the two computational sockets (enumerability of `NTIME g`, diagonal decidable in `NTIME f`) need a string-encoded universal machine — a major project on top of `…ACC0NTM`. Does NOT prove the hierarchy outright. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TimeHierarchyDiagonal` |
| **NTM time-class library — foundation (model + `NTIME`/`NEXP` proved, hierarchy/simulation socketed)** | The concrete computation model the Williams sockets need. **`NTM`** (configuration type + nondeterministic `step` relation + `init` + `accept`); **`reachIn M k c c'`** (reachable in exactly `k` steps); **`acceptsWithin M x t`** (accepting config reached within `t` steps). Proved: **`reachIn_add`** (reachability composes), **`acceptsWithin_mono`** (more steps only help, axiom-free). **`NTIME f`** / **`NEXP`** (`NTIME(2^poly)`) complexity classes (`CClass = Set Lang`). **`williams_concrete`** (proved glue): the Williams meta-theorem instantiated at concrete `NTIME f`/`NTIME g` ⇒ `¬(NEXP⊆ACC0)`, given the concrete sockets. Deep SOCKETS over the model (NOT proved): **`ClockedInclusion`** (`f≤g ⇒ NTIME f ⊆ NTIME g`, clocking), **`ConcreteHierarchy`** (`¬(NTIME f ⊆ NTIME g)`, diagonalisation), universal simulation. A genuine foundation; the hierarchy/simulation are the deep content a full verified library would discharge (a separate major project). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NTM` |
| **Williams' meta-theorem — proof architecture (glue proved, deep ingredients socketed)** | Unpacks the monolithic `williams`/`hierarchy` sockets into Williams' two genuinely-deep ingredients over an explicit class framework (`Lang := List Bool → Prop`, `CClass := Set Lang`). **`EasyWitnessCollapse`** (socket: fast `ACC⁰`-SAT + `NEXP⊆ACC⁰` ⇒ `NTIME[2ⁿ]⊆NTIME[2ⁿ/superpoly]` via IKW easy witnesses), **`NondetTimeHierarchy`** (socket: `¬(NTIME[2ⁿ]⊆NTIME[2ⁿ/superpoly])`, diagonalisation). **`williams_meta_theorem`** (PROVED, axiom-free): the two ingredients + `ACC0SatSpeedup` ⇒ `¬(NEXP⊆ACC⁰)` by modus tollens. **`nexp_not_acc0_via_williams`** (proved conditional): RS representation → SAT speedup (counting socket) → separation. The glue is machine-checked; the two deep ingredients need a verified NTM time-class library and are NOT proved. NOT a proof of Williams' theorem; NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0WilliamsMetaTheorem` |
| **error calibration + Route-B complete capstone — end-to-end conditional `¬NEXP⊆ACC⁰`** | Closes the `ℕ` bookkeeping and packages the whole arc. `…ACC0ErrorCalibration`: **`per_gate_error`** (cancel seed relation `p^t·k=cardS`, `cardI=2^n` ⇒ `p^t·E ≤ 2^n`), **`error_calibration`** (`10·size ≤ p^t` ⇒ `10·(size·E) ≤ 2^n`), **`circuit_error_below_tenth`** (combined ⇒ circuit error `< 2^n/10`). `…ACC0RouteBComplete`: **`error_calibrated`** (re-export) and **`routeB_to_NEXP_not_ACC0`** (the end-to-end conditional — proved RS approximation side [degree `δ^{depth+1}`, error `< 2^n/10`, sparse read-off over `≤(n+1)^{δ^{depth+1}}` features] backs the RS representation; abstract `counting`/`williams`/`hierarchy` sockets give `¬NEXPHasACC0Circuits`; depends on NO axioms — pure conditional). A faithful machine-checked REDUCTION of `NEXP ⊄ ACC⁰` to Williams's SAT-speedup meta-theorem + nondeterministic time hierarchy, NOT a proof of those. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ErrorCalibration`, `…ACC0RouteBComplete` |
| **the error-averaging bridge — per-input seed error ⇒ a fixed good seed (probabilistic method)** | The error-side analogue of the composition-degree discharge. **`exists_below_average`** (`g : ι→ℕ` over nonempty finite `ι` ⇒ some `i` with `(card ι)·g i ≤ ∑ⱼ g j` — the argmin is at/below average; `exists_min_image` + `sum_const`/`sum_le_sum`). **`exists_good_seed`** (if every input is erred on by `≤ k` seeds, some seed errs on few inputs: `(card Seed)·#{x:err s x} ≤ (card Input)·k`; Fubini `∑_s #{x} = ∑_x #{s}` via `card_filter`+`sum_comm`, then `exists_below_average`). Converts the seed-space gate errors (`orPoly_error`/`amplifiedOrPoly_error`) into the fixed-seed input-space `ε = 2^n/p^t` consumed by `circuit_error_bound` (total `≤ size·ε`); with `p^t > 10·size` the circuit errs on `< 2^n/10` inputs. Both degree AND error interfaces now proved; only abstract `williams`/`hierarchy` remain. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ErrorAveraging` |
| **the composition-degree lemma — `totalDegree (aeval f p) ≤ totalDegree p · D` (discharges the per-gate hypothesis)** | Removes the per-gate degree-factor *hypothesis* of the low-degree substitution. **`aeval_totalDegree_le`** (`∀i,(f i).totalDegree ≤ D ⇒ (aeval f p).totalDegree ≤ p.totalDegree·D`; proof: expand `aeval f p = ∑_{d∈support} C(coeff)·∏_i (f i)^{d_i}` via `eval₂_eq`, each monomial image has degree `≤ (∑ d_i)·D = deg(d)·D ≤ totalDegree p·D` via `totalDegree_finset_prod`/`totalDegree_pow`/`le_totalDegree`, sup kept by `totalDegree_finset_sum`). **`unaGate_degree`/`binGate_degree`** (`(aeval ![A,B] G).totalDegree ≤ G.totalDegree·max A.totalDegree B.totalDegree` — the per-gate degree factor, now a THEOREM). Capstone **`psubst_degree_aeval_frontier`** (frontier): a substitution built by `aeval`-composition with degree-`≤δ` gate polys has `totalDegree ≤ δ^{depth+1}` UNCONDITIONALLY on the factor — only the local fact that each gate poly has degree `≤δ` (which `OR`/`MOD` satisfy). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AevalDegree` |
| **low-degree substitution — low degree survives constant-depth composition** | Fuses the probabilistic low-degree gate polynomials into the substitution: the degree analogue of `circuit_error_bound`. **`depth`** (circuit depth). **`psubst_degree`** (proved by structural induction): if every gate of a polynomial assignment `Q` raises total degree by `≤` factor `δ` (`totalDegree (Q gate) ≤ δ·max(child degrees)`, leaves `≤ δ`), then `totalDegree (Q c) ≤ δ^{depth c + 1}` — quasipolynomial for constant depth (the `OR` amplified poly has `δ=t(p-1)`, `MOD` has `δ=p-1`). **`psubst_features_lowDeg`** (every feature of `Q c` is a degree-`≤ δ^{depth c+1}` support in `lowDegMonomials`, via `support_mem_lowDeg` ⇒ `≤ (n+1)^{δ^{depth+1}}` features). With `circuit_error_bound` (error `≤ size·ε`) this is the low-degree, low-error substituted polynomial. Per-gate degree factor is the hypothesis (interface to the gate polys; Mathlib lacks a general `totalDegree`-of-composition lemma). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LowDegreeSubstitution` |
| **`MvPolynomial` re-modelling of circuit substitution — the circuit IS a polynomial; its accept count is sparse** | Fuses the substitution (`Bool`-valued) and multilinearisation (`MvPolynomial`) files. **`subst`** (each gate replaced by its exact multilinear `{0,1}` interpolation: unary `f(0)(1-A)+f(1)A`, binary the 4-term bilinear form, composed through the circuit). **`unary_interp_val`/`binary_interp_val`** (the interpolations are exact on `{0,1}`). **`subst_eval`** (`eval (boolVal∘x) (subst c) = boolVal (eval c x)` — the polynomial computes the circuit exactly on the cube). **`circuit_cube_count`** (`∑ₓ boolVal (eval c x) = ∑_{d∈(subst c).support} coeff_d·2^{n-|supp d|}` — the accept count IS the sparse cube count of the circuit's polynomial, fusing `subst_eval` with `multilinear_cube_sum`; the sub-`2^n` count for the actual circuit). **`circuit_features_lowDeg`** (every feature is a support of size `≤ totalDegree (subst c)`). Exact polynomial (high degree for wide gates); the low-degree version is the separate probabilistic content. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SubstitutionPoly` |
| **multilinearisation — a bounded-degree polynomial on the Boolean cube is a sparse `AND`-feature count** | Closes the read-off's input gap: over `{0,1}` every monomial collapses to its squarefree support (`x_i^k=x_i`) and `∏_{i∈S} x_i = monoAND_S`. **`boolVal`/`boolVal_pow`** (`boolVal b^k = boolVal b`, `k≥1`); **`prod_boolVal_eq_andFeature`** (the atom: `∏_{i∈S} boolVal(x i) = [monoAND_S x]` — a monomial IS an `AND` feature); **`eval_boolean_eq_sparse`** (`eval (boolVal∘x) Q = ∑_{d∈Q.support} coeff_d·[monoAND_{supp d} x]` — bounded poly evaluates on the cube to a sparse `AND`-feature sum, via `MvPolynomial.eval_eq`); **`multilinear_cube_sum`** (`∑ₓ eval (boolVal∘x) Q = ∑_{d∈Q.support} coeff_d·2^{n-|supp d|}` — closed sparse cube count, reusing `monoAND_cube_sum`); **`support_mem_lowDeg`** (`d∈Q.support`, `totalDegree Q ≤ D ⇒ supp d ∈ lowDegMonomials n D`, via `le_totalDegree` — features number `≤(n+1)^D`). Remaining: the modelling link identifying the circuit approximant with an `MvPolynomial`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Multilinearisation` |
| **the sparse-polynomial read-off — a low-degree sparse poly gives a sub-`2^n` cube count (wires the counting socket)** | Connects the circuit approximant to the existing counting machinery. **`sparsePoly 𝒮 c x := ∑_{S∈𝒮} c S · [monoAND_S x]`** (the sparse `SYM∘AND`/monomial form). **`sparsePoly_cube_sum`** (`∑ₓ sparsePoly = ∑_{S∈𝒮} c S · 2^{n-|S|}` — the cube sum is a SPARSE sum, no `2^n` enumeration; reuses `sparse_cube_sum`). **`sparse_features_le`** (`𝒮 ⊆ lowDegMonomials n D ⇒ 𝒮.card ≤ (n+1)^D`; reuses `beigelTarui_monomial_count_le`). **`sparse_readoff`** (the bridge: degree-`≤D` sparse poly's cube sum = sum of `≤(n+1)^D` feature weights — the sub-`2^n` `ACC⁰`-`SAT` counting input). **`sparse_readoff_to_NEXP_not_ACC0`** (given the sparse representation, counting+Williams ⇒ `¬NEXPHasACC0Circuits`). SOCKET: the multilinearisation turning the circuit-substitution approximant into this explicit sparse `monoAND` form. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SparsePolyReadoff` |
| **the circuit-substitution assembly — substitution error accumulates additively over gates (hybrid bound)** | The bookkeeping that turns per-gate probabilistic polynomials into a whole-circuit approximant. Circuit model **`Circ`** (`inp/cst/una/bin`; `AND`/`OR`/`MOD`/`NOT` are gate instances) with **`eval`** (true value), **`size`** (gate count), **`step`** (gate applied to children's approximants). **`circuit_error_bound`** (proved by structural induction, the hybrid argument): if every node's approximant `P c` agrees with `step P c` on all but `ε` inputs (the gate's own probabilistic-polynomial error), then `P c` agrees with `eval c` on all but `size c · ε` inputs — i.e. `#{x : P c x ≠ eval c x} ≤ size c · ε`. At each gate the error ⊆ local-error ∪ propagated-children-errors (`Finset.card_le_card` + `card_union_le`). Calibrated with `ε=2^n/(10·size)` (amplification + `error_choice`) ⇒ total error `<2^n/10`. SOCKET: the polynomial→`SYM∘AND` read-off (sparse cube-sum / level counts) = `…ACC0BTSizeRecurrence.QuasipolyApproxCompression`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CircuitSubstitution` |
| **Razborov–Smolensky amplification — the product error bound `(1/p)^t`** | Boosts the single-form `OR`-polynomial error `1/p` to sub-constant via `t` independent forms. **`amplified_form_balance`** (`v≠0` ⇒ `p^t·#{R : ∀ s, ∑ᵢ R s i vᵢ = 0} = p^{m·t}` — all `t` independent forms vanish on `v` for an exact `(1/p)^t` fraction; proof: joint vanishing set = `t`-fold `Fintype.piFinset` product of the single-form set, card `(single)^t`, then `p^t·(single)^t = (p·single)^t = (p^m)^t = p^{m·t}` via `linear_form_balance` + `mul_pow`/`pow_mul`). The amplified polynomial: **`amplifiedOrPoly`** (`1 − ∏ₛ(1 − orPoly v (R s))`, degree `t·(p-1)`), **`amplifiedOrPoly_eq_zero_iff`** (vanishes iff every form vanishes — `Finset.prod_eq_zero`/`prod_eq_one`), **`amplifiedOrPoly_zero`** (exact on all-zero), **`amplifiedOrPoly_error`** (`p^t·#{R : amplifiedOrPoly v R = 0} = p^{m·t}` — error exactly `(1/p)^t`). With `t=O(log_p s)` + `error_union_bound`, a size-`s` circuit's total error `<1/10`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ProbabilisticAmplification` |
| **the `MOD₆` probabilistic polynomial — exact `MOD` indicators + the Razborov–Smolensky `OR` core (random linear form)** | Attacks the probabilistic-polynomial wall and the analysis splits: the `MOD` gates need NO probabilism (they are exactly low-degree), all probabilism is in the `AND`/`OR` layer. **`mod2_indicator`** (`1+a` over `F₂`, deg 1), **`mod3_indicator`** (`1−a²` over `F₃`, deg 2, Fermat), **`mod6_indicator_product`** (`MOD₆` = both exact indicators fire over `F₂×F₃`). The RS core PROVED: **`linear_form_balance`** (`p·#{r : ∑rᵢvᵢ=0}=p^m` for `v≠0` — the random `F_p`-linear form vanishes on a fixed nonzero vector with probability exactly `1/p`, via additive equidistribution of the dot-product hom's fibres). The `OR` probabilistic polynomial: **`orPoly_eq`** (`(∑rᵢvᵢ)^{p-1}` is a 0/1 indicator, Fermat), **`orPoly_zero`** (exact on the all-zero input), **`orPoly_error`** (error exactly `1/p` on nonzero inputs — degree `p-1`, the genuine RS `OR` probabilistic polynomial). SOCKET: **`AmplifiedErrorSocket`** (boost `1/p`→`(1/p)^t` by `t` independent forms + quasipoly assembly = the remaining bookkeeping). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Mod6ProbabilisticPolynomial` |
| **the Beigel–Tarui size recurrence — exact fold is exponential in fan-in; probabilistic-collapse socket** | Pins down why exact mixed-radix is insufficient and stages the probabilistic replacement. **`btComposeSize l r := l·(r+1)+r`** with **`btComposeSize_eq`** (= the proved `miniBT_collapse_size`); **`iterSize b k`** (fold a fan-in-`(k+1)` gate); **`iterSize_ge`** (`b·(b+1)^k ≤ iterSize b k` — EXPONENTIAL in fan-in, so a single poly-fan-in `ACC⁰` gate blows up exponentially under exact collapse). Bookkeeping PROVED: **`error_union_bound`** (`s` gates of error `≤ ε` ⇒ total `≤ s·ε`), **`error_choice`** (`ε=1/(10s)` ⇒ total `=1/10`). SOCKETS (open): **`ApproxSymAndRep`** (`ε`-approximate bounded-size rep), **`QuasipolyApproxCompression`** (joint ⇒ quasipoly approximate single-count — the probabilistic replacement for exact mixed-radix), **`MOD6ProbabilisticApprox`** (first concrete target: `MOD₆` probabilistic polynomial), **`probabilistic_route_to_NEXP_not_ACC0`** (quasipoly approx rep over const depth ⇒ `¬NEXP⊆ACC⁰`, conditional). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BTSizeRecurrence` |
| **restricted mini-Beigel–Tarui: the two-count collapse is PROVED exactly (the wall is size, not impossibility)** | Corrects the earlier guess. The achievable `AND`-counts are closed under addition (append layers) and integer scaling (replicate gates), so a mixed-radix single count `c⋆ = (t₂+1)·c₁ + c₂` recovers the pair (`c₂ ≤ t₂ < t₂+1` ⇒ `c₁ = c⋆/(t₂+1)`, `c₂ = c⋆%(t₂+1)`). **`satCountF_sumElim`** / **`satCountF_replicate`** / **`satCountF_le_card`** (the count algebra); **`miniBT_two_count_collapse`** (joint two-count ⇒ single `SYM∘AND`); **`miniBTCollapse_holds : MiniBTCollapse n`** (discharges the `…ACC0SymAndComposition` socket — it was provable, not open); **`hasSymAndRep_and`** / **`_or`** (∴ `SYM∘AND` unconditionally closed under AND/OR). **`miniBT_collapse_size`** (`t₁·(t₂+1)+t₂` gates — the collapse is MULTIPLICATIVE in size, compounding to a tower over depth = the real Beigel–Tarui wall, kept quasipoly only via probabilistic polynomials). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MiniBTTwoCount` |
| **the `SYM∘AND` composition lemma — what composes (proved) and the one wall (socketed)** | Attacks the mini-Beigel–Tarui composition question honestly, splitting proved from open. `HasSymAndRep F := ∃ supports sym, ∀x, F x = sym (satCount supports x)` (grounded by `mod6AndCircuit_hasSymAndRep`). PROVED composes (no count-dimension rise): **`hasSymAndRep_not`** (NOT, same layer); **`hasSymAndRep_and_sharedLayer`** / **`_or_`** (shared-layer AND/OR stay single-count); **`residue_observer_compose_coprime`** (`a*b ∣ m ↔ a∣m ∧ b∣m` for coprime `a,b` — general product-residue/mixed-modulus CRT composition). PROVED the dimension rise: **`hasBinarySymRep_and`** / **`_or_`** (cross-layer AND/OR → joint TWO-count representation `HasBinarySymRep`). THE WALL (NOT proved): **`MiniBTCollapse`** (every joint two-count rep collapses to a single `SYM∘AND` — the BT degree reduction; exact form too strong, real theorem is the quasipoly relaxation via probabilistic polynomials). Conditional bridges: **`symAndRep_closed_under_and_of_miniBT`** / **`_or_`** (`MiniBTCollapse` ⟹ closure under AND/OR). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymAndComposition` |
| **depth-2 `MOD₆∘AND` — exact `SYM∘AND` representation via the residue-pair observer (Route B first composite case)** | The first real composite-`MOD` representation. `satCount supports x` = # satisfied bottom monomial-`AND`s; `mod6AndCircuit = 6 ∣ satCount`. **`mod6_depth2_symAnd_repr`** (the circuit IS `mod6Sym ∘ satCount` — the exact `SYM∘AND` factorization, `Iff.rfl`); **`mod6_depth2_residue_pair`** (accepts iff `(satCount mod 2=0 ∧ satCount mod 3=0)` — the CRT product observer reads it, via `mod6_decided_by_residue_pair`); **`mod6_depth2_symmetric`** (output depends only on `satCount` — the genuine `SYM` invariance); **`mod6_bottom_count_le_quasipoly`** (distinct bottom `AND`s of support `≤ D` number `≤ (n+1)^D`, Beigel–Tarui — quasipoly size). Bridge socket: **`depth2_to_full_via_composition`** (proved base case + open mini-BT composition lemma ⇒ full `ACC⁰` `SYM∘AND` rep; the composition/blow-up is the named open content). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Mod6SymAndDepth2` |
| **the composite-`MOD` Beigel–Tarui target — `MOD₆`: single-field RS fails, integer/product observer replaces it** | Isolates the one open ACC⁰ wall at the smallest composite obstruction. **`mod6_iff_mod2_and_mod3`** (`6∣m ↔ 2∣m ∧ 3∣m`); **`no_injective_ringHom_zmod6_to_prime_field`** / **`field_polynomial_projection_fails_for_MOD6`** (NO injective ring hom `ZMod 6 → ZMod p` for any prime `p` — the zero-divisor `2·3=0` of `ZMod 6` can't survive in a field, so the single-field RS span cannot carry both `MOD₂` and `MOD₃`); **`compositeResidueObserver`** + **`compositeResidueObserver_injective`** (the CRT product observer `ZMod 6 ≃+* ZMod 2 × ZMod 3` — the faithful integer/multi-prime replacement, a product ring not itself a field); **`mod6_decided_by_residue_pair`** (`6∣m ↔ (m mod 2=0 ∧ m mod 3=0)`); **`mod6_symAnd_residue_pair`** (restricted `SYM` representation: `MOD₆` of the Hamming weight = the residue-pair conjunction). OPEN socket: **`mod6_composite_route_to_NEXP_not_ACC0`** (quasipoly composite `SYM∘AND` representation surviving `ACC⁰` composition = full YBT, named hypothesis ⇒ `¬NEXP⊆ACC⁰`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeBTTarget` |
| **the tri-aspect boundary — one `Ω`, three projections (incidence refuted, polynomial viable, composite open)** | Turns "tri-aspect monism" into a theorem map: a single boundary object `TriAspectBoundary = (supports, deg)` induces three *derived* observers (projections), not three independent hacks. **`incidence_projection_fails`** (singleton `Ω` ⇒ `¬IncidenceCollapses` — the membership-rank projection never collapses, reusing the rank-shrink no-go); **`polynomial_projection_succeeds_for_AC0p`** (`deg < n`, `2≠0` in `ZMod p` ⇒ `algebraicProjectionSucceeds`: holonomy parity escapes the degree-`≤deg` evaluation span = the RS core — the effective-dimension projection is the working one); **`cost_projection_quasipoly`** (`|lowDegMonomials n deg| ≤ (n+1)^deg`, Beigel–Tarui); **`tri_aspect_redirect`** (on ONE singleton `Ω`: incidence fails AND algebraic succeeds — N-Frame redirects to Route B); **`composite_projection_socket`** (the one open projection: composite-`MOD` algebraic representation ⇒ `¬NEXPHasACC0Circuits`, named conditional). NOT vacuous: projections are real functions of `Ω`, positive theorems discharge to real prior theorems. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TriAspectBoundary` |
| **the rank-shrink wall is real — `NFrameRankShrink` false for the membership-rank observer** | The honest attack on Route A's wall: tested whether `NFrameRankShrink` is true — it is NOT. **`rankCellCollapse_implies_collision`** (`RankCellCollapse ⇒` two live coords share a pattern — collapse NEEDS a collision); **`not_rankCellCollapse_of_injOn`** (injective patterns ⇒ `¬RankCellCollapse`); **`singleton_supports_injective`** (`supports j = {j}` ⇒ patterns `= Pi.single`, injective); **`singleton_supports_no_rank_collapse`** (`∀ L, ¬RankCellCollapse (singletons) L` — a concrete `ACC⁰`-realizable family where the rank observer NEVER collapses, refuting the socket). CONCLUSION: rank-shrink needs pattern collisions, which adversarial supports defeat (matching the cell-count characterization + `MOD` no-go); the rank that DOES separate is the polynomial-method effective dimension (`V_D`, Route B), not the observer incidence rank. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankShrinkWall` |
| **the ACC⁰ frontier — two final conditional theorems, two named walls** | The 'what remains' map: the whole programme reduces to TWO deep open theorems, everything downstream proved. **Route A (N-Frame):** `NFrameRankShrink sys` (∀ predictor: `∃ L, 2^{cellRank} < \|L\|`) — the open rank-shrink socket; **`rank_shrink_gives_acc0_lower_bound`** (`NFrameRankShrink ⇒ ACC0HolonomyLowerBound`, via the proved rank-cell bridge). Open = force `cellRank < log₂\|L\|` for wide overlapping `MOD` = rank analogue of the `MOD` wall. **Route B (Williams/Beigel–Tarui):** **`composite_route_to_NEXP_not_ACC0`** (`(composite_BT_degree : RSRep) + counting + williams + hierarchy ⇒ ¬NEXPHasACC0Circuits`, via `williams_cashout_skeleton`). Open = composite-modulus Yao/Beigel–Tarui degree theorem; count side + `AC⁰[p]` degree + cash-out proved. Both are CONDITIONALS; the sockets are the genuine open `NP/NEXP`-strength content, named. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankRouteFrontier` |
| **random restriction yields a `RankCellCollapse` witness** | Exposes the intermediate witness from the probabilistic rank route. **`exists_rankCellCollapse_whp`** (`2^a ≤ b ∧ Pr[cellRank≥a]+Pr[\|L\|≤b]<1 ⇒ ∃ L, RankCellCollapse supports L` — the two-event method: some `L` is simultaneously low-rank `cellRank<a` and large `\|L\|>b≥2^a`, so `2^{cellRank}<\|L\|`); **`randomRestriction_rank_collapse_low_correlation`** (⇒ low corr via the rank-cell bridge). Packages the probabilistic rank route into the `RankCellCollapse`/observer-state-space API; rank analogue of the cell-count whp, keyed on the sharper cell-rank tail. Open: the feasibility `Pr[cellRank≥a]` small for wide overlapping `MOD` = the rank-shrink balance. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RandomRestrictionRankCollapse` |
| **bounded support footprint ⇒ low observer rank** | The honest intersection/support-dimension rank bound. A coordinate outside `footprint = ⋃_j supports j` lies in no support ⇒ zero cell pattern, so the rank is bounded by the footprint size, INDEPENDENT of gate count. **`footprint`**; **`cellPatternVec_eq_zero_of_not_mem_footprint`**; **`cellRank_le_footprint`** (`cellRank ≤ \|footprint\|`; `cellSpan ⊆ span` of footprint patterns, `Submodule.finrank_mono` + `finrank_span_finset_le_card` + `card_image_le`); **`footprint_rank_cell_collapse`** / **`footprint_low_correlation`** (`2^{\|footprint\|} < \|L\| ⇒ RankCellCollapse ⇒` low corr). A `MOD` layer reading `t` coordinates has rank `≤ t` regardless of gate count. NOTE: pairwise small intersection does NOT bound rank (disjoint = full rank); the genuine bound is the total footprint. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FootprintRank` |
| **block-diagonal MOD rank-shrink — `k` gates in `d` blocks ⇒ observer rank `≤ d`** | A structured-`MOD` rank-shrink fragment. A wide layer of `k` `MOD` gates in `d` blocks, each block's gates sharing a support (`supports j = center (blk j)`), has observer rank `≤ d` — a genuine shrink (`d ≪ k`; survivor count `k`, linear rank only `d`). **`BlockEqualSupports`**; **`blockEqual_clustered`** (it's `ClusteredSupports … d 0`; `Finset.sum_eq_single`); **`blockEqual_cellRank_le`** (`cellRank ≤ d` via `clustered_supports_low_rank`); **`blockEqual_rank_cell_collapse`** / **`blockEqual_low_correlation`** (`2^d < \|L\| ⇒ RankCellCollapse ⇒` low corr). Does NOT force low rank for general wide overlapping `MOD` under restriction = the open rank-shrink wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BlockRankCollapse` |
| **rank-cell collapse — the linear observer state space is exactly `2^rank`** | Upgrades the observer route to the right linear-algebraic invariant. **`observer_state_space_card`** (`Nat.card (cellSpan supports L) = 2^{cellRank supports L}` — the `F₂` observer state space has EXACTLY `2^rank` states, via `Module.card_eq_pow_finrank` + `ZMod.card`; sharper than `2^{survivingCount}`); **`RankCellCollapse`** (`2^{cellRank} < \|L\|`); **`cellPatternCount_le_observer_states`** (realized cells `≤ \|cellSpan\| = 2^rank`); **`rank_cell_collapse_implies_low_holonomy_correlation`** (`RankCellCollapse ⇒ LowHolonomyCorrelation`, directly, bypassing survivor count); **`rankCellCollapse_implies_cellCountCollapse`**. The bridge + fragments + composition were already in the corpus; this crystallizes the exact state-space identity. Open: rank-shrink under restriction for wide overlapping `MOD` (`cellRank < log₂\|L\|`) = the rank analogue of the `MOD` wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankCellCollapse` |
| **Beigel–Tarui sparsity (count half) — `SYM∘AND` monomial count is quasipolynomial** | The count input to the counting socket. **`sum_choose_le_quasipoly`** (`∑_{i≤D} C(n,i) ≤ (n+1)^D`, via `Nat.choose_le_pow`: `C(n,i)≤n^i≤n^i·C(D,i)`, summed `= (n+1)^D` by `add_pow`); **`beigelTarui_monomial_count_le`** (`(lowDegMonomials n D).card ≤ (n+1)^D` — a degree-`≤D` `SYM∘AND` representation is `(n+1)^D`-sparse, quasipolynomial for `D = polylog`). HONEST: COUNT half only. DEGREE half: `AC⁰[p]` PROVED in RS layer (`D=((p-1)t)^depth`) ⇒ `AC⁰[p]` is `(n+1)^D`-sparse; general composite `ACC⁰` degree = the DEEP OPEN Yao/Beigel–Tarui symmetric-representation construction (Williams), NOT established. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BeigelTaruiSparsity` |
| **binomial inversion — recover the level-counts `N_t` from the moments `B_d`** | Inverts the unit-triangular `B_d = Σ_t N_t·C(t,d)`. **`binom_orthogonality`** (`Σ_{d≤K} (-1)^{d-s} C(d,s) C(t,d) = δ_{s,t}`, via `Nat.choose_mul` subset-of-subset + `Int.alternating_sum_range_choose`; factor `C(t,s)`, reindex `sum_Ico_eq_sum_range`, alternating row-sum); **`binomial_inversion`** (`N_s = Σ_d (-1)^{d-s} C(d,s) B_d` over `ℤ`); **`levelCount_eq_inversion`** (`(N_s:ℤ) = Σ_d (-1)^{d-s} C(d,s)·(∑_x C(andCount x,d))`). Closes the algebraic half: the `N_t` are an EXPLICIT alternating combination of kernel-computable moments. Remaining: Beigel–Tarui `#monomials` bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BinomialInversion` |
| **per-level counts from binomial moments — the `N_t` bridge** | Connects the `SYM∘AND` level-counts `N_t = #{x : exactly t fire}` to the kernel-computable sums via binomial moments. **`levelCount_sum`** (`∑_{t≤k} N_t = 2ⁿ`, `card_eq_sum_card_fiberwise` + `card_pi`); **`binomial_moment_eq_sum_levels`** (`∑_x C(andCount x, d) = ∑_{t≤k} N_t·C(t,d)`, fiberwise over `andCount`). The matrix `C(t,d)` is unit-upper-triangular, so the `k+1` moments determine the `k+1` level-counts by inclusion–exclusion; each moment is a sparse cube-sum (`e_d` of the `AND`-indicators), computable by the kernel — hence so are the `N_t`. HONEST: the moments↔levels bridge is proved; the explicit inversion `N_t = ∑_d (-1)^{d-t} C(d,t) B_d` + the `e_d`-sparsity identity + Beigel–Tarui bound remain. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LevelCounts` |
| **the SYM-layer reduction — `SYM∘AND` SAT/count reduces to the `k+1` level-counts** | The symmetric top layer of Williams' counting socket. A `SYM∘AND` circuit is `sym(∑_j AND_j(x))`; the `AND`-layer fires `0..k` gates, so the cube partitions into `k+1` levels `N_t = #{x : exactly t fire}`. **`andCount`/`symAndEval`/`levelCount`**; **`andCount_le`** (`≤ k` fire); **`symAnd_satCount_eq_sum_levels`** (`#SAT = Σ_{t≤k} [sym t]·N_t`, via `card_eq_sum_card_fiberwise`); **`symAnd_sat_iff`** (`∃x` accepting `⟺ ∃ t≤k, sym t ∧ N_t ≠ 0` — decision over `k+1` levels, not `2ⁿ`). The symmetric layer collapses `SYM∘AND` SAT to `k+1` level-counts. HONEST: the reduction THROUGH the symmetric layer is proved; computing each `N_t` (fed by the sparse kernel) + the Beigel–Tarui quasipoly `#monomials` bound remain open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymLayerReduction` |
| **the sparse-counting kernel — fast cube-sum over a `SYM∘AND` representation** | The genuinely-provable algorithmic kernel of Williams' counting socket. A monomial-`AND` over `S` is satisfied by exactly `2^{n−|S|}` inputs, so a sparse combination's cube-sum is a closed form over its coefficients (no `2ⁿ`-enumeration). **`monoAND_cube_count`** (`\|{x : monoAND S x}\| = 2^{n−|S|}`, via `piFinset` + `card_univ_diff`); **`monoAND_cube_sum`** (`∑_x [∏_{i∈S} x_i] = 2^{n−|S|}` over any comm ring); **`sparse_cube_sum`** (`∑_x Σ_{S∈𝒮} c_S·[∏_{i∈S} x_i] = Σ_{S∈𝒮} c_S·2^{n−|S|}`, computable in `\|𝒮\|` ops). WHY the representation gives a counting speedup: `#monomials < 2ⁿ` ⇒ count from coefficients alone. HONEST: the counting PRIMITIVE, not the full socket — the `SYM` top layer (sum→SAT) and the Beigel–Tarui quasipoly `#monomials` bound for `ACC⁰` are the remaining algorithmic inputs, open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SparseCounting` |
| **control shrinkage under leaf restriction — the provable half + the barrier** | Attacks the open rung `random_restriction_makes_control_shallow`. **`leafFree`/`restrictedControlEval`**; **`restrictedControlEval_eq_of_agree`** (restricted control depends only on free leaves); **`control_restriction_shallow`** (the `ρ`-restricted control is a decision tree of depth `≤ |leafFree ρ|` — observer state count shrinks to `2^|free|`); **`control_restriction_searchable`** (the `ρ`-restricted composite is SAT-searchable `< 2ⁿ` once `2^{|leafFree ρ|} < 2^n`, via `acc0_over_mod_searchable`). BARRIER (honest): searches the RESTRICTED composite (fixed leaves ignore the oracle); lifting to the original needs the leaf restriction realized by an `x`-restriction, and a `MOD` leaf is `x`-constant only when its whole support is fixed (`parity_constant_iff_support_fully_fixed`, no absorbing value) — for wide/overlapping `MOD` a restriction rarely fully-fixes supports, so depth doesn't drop below `n`. Same `MOD` wall, localized to the control layer. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ControlShrinkage` |
| **boundary-observer control — the boundary selects the observer (dynamic N-frame unification)** | Formalizes "observer is not a fixed mechanism; the boundary/context selects it", routing the proved routes by gate boundary, each grounded in a proved fact. **`GateKind`** (andOr/mod q/symmetric), **`BoundaryContext`** (absorbing/linearResidual/polynomialSpan/countingState), **`boundarySelect`**; **`boundarySelect_andOr_iff_absorbing`** (absorbing ⟺ AND/OR); **`andOr_boundary_absorbing`** (AND has absorbing value); **`mod_boundary_not_absorbing`** (MOD constant iff support fully fixed); **`mod_observer_reduces_to_membership`** (linearResidual = membership, bounded); **`mod_polynomial_observer_separates`** (polynomialSpan separates the MOD target); **`counting_state_cashes_out`** (countingState → Williams cash-out). Unification/routing layer, NOT new hard content. The countingState observer is realised concretely in `…ACC0OracleControl` (`oracle_control_over_mod_searchable`: few-MOD fragment SAT-searchable `< 2ⁿ`); open rung `random_restriction_makes_control_shallow` = the state-shrinkage. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundaryObserverControl` |
| **Williams cash-out from the polynomial method (CONDITIONAL, representation half discharged)** | Williams' `ACC⁰`-SAT algorithm = representation (sparse `SYM∘AND`) + counting (sub-`2ⁿ`). **`RSMonoANDRepresentation`** / **`rsMonoANDRepresentation_proved`** — the representation half (1) DISCHARGED by the polynomial method (re-export of `lowDegPolyEval_mem_monoAND_span`: degree-`≤D` poly eval ∈ monoAND span). **`williams_cashout_skeleton`** — the Williams skeleton with representation factored out. **`williams_cashout_from_polynomial`** — same chain with `hrep` SUPPLIED by the polynomial method, leaving only the **counting** socket (rep ⇒ sub-`2ⁿ` SAT, Beigel–Tarui/Williams algorithmic heart), the **Williams** collapse, and the **time hierarchy** as inputs. Proves the IMPLICATION, NOT the separation; faithful account of which inputs close `NEXP ⊄ ACC⁰` and which the polynomial method supplies. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0WilliamsCashoutFromPolynomial` |
| **PIVOT to the polynomial method — exact effective-dimension separation** | Lands the polynomial method on the SAME target the observer route could not collapse (`fParity univ`). **`lowDegree_poly_ne_holonomy_parity`** (NEW): a polynomial of total degree `< n` cannot equal the holonomy parity `x ↦ ∏ᵢ pmOne(xᵢ)` on the cube — its evaluation `∈ V_D` (`eval_mem_lowDegSpan`), which the holonomy parity escapes (`holonomy_parity_not_lowDegEval`, effective dim `≥ n`). The DIMENSION analogue of the observer's swap-invariance — and unlike the observer route it bites on `MOD`. **`holonomy_parity_escapes_lowDegSpan`** (re-export); **`observer_route_no_escape_for_mod`** (re-export: the ceiling bypassed). Quantitative `AC⁰[p]` size `2^{Ω(n^{1/2d})}` = `…Layer3NFrameParityRS.nframe_parity_target_size_lower_bound`. Honest `F_p` ceiling: real `PARITY/MOD_p ∉ AC⁰[p]`; composite/general = Williams. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PivotToPolynomialMethod` |
| **MOD gate semantics + residual observer reduces to membership (the ACC⁰ battlefield)** | Grounds the `MOD` no-go in gate SEMANTICS and proves the residual observer can't escape. Part A: **`effVal`/`parityVal`/`ParityConstant`**; **`parity_constant_iff_support_fully_fixed`** (a parity gate is constant under `ρ` IFF support entirely fixed — forward = the no-absorbing-value statement); **`and_constant_of_absorbing`** (contrast: `AND` deactivated by one fixed-`false` input — `AND` has an absorbing value, parity has none = why `AC⁰` switches, `ACC⁰` doesn't). Part B: **`residualSignature`** (free summand iff free ∧ in support); **`residual_eq_membership_of_free`** (free coord: residual signature = membership pattern); **`residual_merge_iff_sameCell`** (free coords merge under residual observer IFF share membership cell); **`residualPatternCount_eq_membership_on_free`** (residual count = membership count); **`residual_no_escape_in_hardRegime`**. CONCLUSION: a coordinate's affine contribution to a linear gate IS its membership ⇒ residual observer = membership observer on free coords ⇒ inherits the hard-regime ceiling ⇒ the ENTIRE observer/merging programme is membership-bounded for `MOD` ⇒ `ACC⁰` needs the polynomial method. NEGATIVE/localization result. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MODResidualObserver` |
| **MOD no-go — variable-fixing merging is INERT for symmetric gates** | A `MOD` gate has NO absorbing value (constant iff support ENTIRELY fixed), so **`MODGateActive ρ supports j := ∃ i ∈ supports j, ρ i = none`** (active iff it reads a free input). **`mod_separating_gate_active`** (a gate separating two FREE coords contains one of them ⇒ always active ⇒ can never be deactivated); **`modRefined_eq_membership_of_free`** (free coord: `MOD`-refined pattern = membership pattern — no gate drops out); **`mod_refined_merge_iff_sameCell`** (two free coords share a `MOD`-refined cell IFF a membership cell — NO merging gain); **`mod_no_collapse_in_hardRegime`** (distinct free coords never merge). Proves the `AND`/`OR` absorbing-value merging that powers the refined route is inert for `MOD` — exactly why `AC⁰` switches but `ACC⁰` does not. Pins the obstruction to `MOD`'s lack of an absorbing value. A NEGATIVE/localization result, NOT an `ACC⁰` lower bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MODNoGo` |
| **richer variable-fixing observer model — restriction CAN merge** | The model the membership ceiling demanded. `Restriction n := Fin n → Option Bool` (`none`=free); `AND`-of-positive-literal gates become CONSTANT when an input is fixed-`false`, so a gate drops out even while reading a free coord — the merge membership couldn't do. **`Restriction`/`freeSet`/`GateActive`** (no fixed-false input ∧ has free input)/**`refinedCellPatternVec`** (membership over ACTIVE gates only)/**`refinedCellPatternCount`/`RefinedCellCollapse`**; **`exists_sameRefinedCell_of_collapse`** (pigeonhole); **`refined_merge_of_inactive_separators`** (THE new power: coords separated only by inactive gates merge); **`refined_strictly_beats_membership`** (concrete `n=3,k=1`, gate `{0,2}`, fix coord 2 false: `¬ CellCountCollapse` membership ∧ `RefinedCellCollapse` refined — strict gain over the proved ceiling). **`refinedWeightVec`** (support-count on active gates, 0 on inactive); **`refined_mem_of_eq`** (equal refined patterns ⇒ membership agrees on active gates); **`refinedWeightVec_pairSwap`** (refined-same-cell swap preserves the statistic); **`RefinedLowCorrelation`** / **`refinedCellCollapse_implies_refinedLowCorrelation`** (the CORRELATION BRIDGE, via the generic `low_correlation_of_pres`). OPEN (the genuine ACC⁰ wall): `MOD`/symmetric gates have NO absorbing value (constant only when support fully fixed) ⇒ this AND/OR-style merging is weak for `MOD` (AC⁰ switches, ACC⁰ doesn't). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RefinedObserverModel` |
| **EXACT characterization of the cell-count socket — false in the hard regime** | `cellPatternVec` is `L`-INDEPENDENT, so two live coords collapse iff their FULL patterns are equal, and any separating gate contains one of them so always SURVIVES ⇒ a restriction CANNOT merge patterns (only drops coords). **`globalCellCount_le_n`** (`≤ n`); **`forcesLowCellCount_iff_global_lt`** (`FullACC0ForcesLowCellCount ↔ globalCellCount < n`); **`forcesLowCellCount_iff_not_injective`** (`↔ ¬ Injective (cellPatternVec supports)`); **`not_forcesLowCellCount_of_hardRegime`** (`HardRegime univ ⇒ ¬ FullACC0ForcesLowCellCount`, since hard ⇒ `globalCellCount = n` = injective). HONEST CEILING: the route fires EXACTLY when two coords already share a global pattern (`2^k<n` pigeonhole / structured fragments force it); FALSE in the hard regime. `StructuredOverlappingMOD ⇒ collapse` is a FALSEHOOD for genuinely-hard families. Restriction-merging needs a RICHER model (fixing vars ⇒ gates constant). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountCharacterization` |
| **sunflower (common-core) supports — wide overlap, `≤ k+2` cells** | `supports j = core ∪ petal j` with pairwise-disjoint petals: every pair OVERLAPS in the core (NOT laminar — `supports i ∩ supports j = core ≠ ∅`, neither nested). **`SunflowerSupports`**; **`sunflower_suppSet_core/_petal/_empty`** (patterns are `univ`, `{j₀}`, or `∅`); **`sunflower_cellPatternCount_le`** (`≤ k+2`; image `⊆ insert univ (insert ∅ (univ.image singleton))`); **`sunflower_low_correlation`** (`k+2 < \|L\| ⇒` low corr, regardless of rank/survivors). Genuine wide-overlap-⇒-few-patterns fragment outside laminar/low-rank. General overlapping `MOD` need not be sunflower-structured (can shatter) = the wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SunflowerCellCount` |
| **hard-regime reduction — exactly where the open lemma lives** | **`HardRegime supports L`** := `\|L\| ≤ 2^{survivingCount L} ∧ \|L\| ≤ globalCellCount` (both general collapse bounds fail). **`collapse_of_not_hardRegime`** (outside ⇒ `CellCountCollapse`, via survivor route ∨ global route); **`full_of_hardRegime_resolved`** (`HardRegime univ → Full ⇒ Full`: the hard regime is the ONLY obstruction); **`hardRegime_univ_of`** (the realistic regime `n ≤ 2^{surv univ}`, `n ≤ globalCellCount` IS hard). A reduction isolating the open content (`cellPatternCount L < \|L\|` while both bounds fail = the merged-count concentration on surviving gates), not a resolution. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountHardRegime` |
| **direct cell-count concentration — not via `2^{survivors}`** | `cellPatternVec` is INDEPENDENT of `L` (ranges over all gates), so cell count = distinct patterns among live coords, monotone in `L`, bounded by restriction-independent `globalCellCount := cellPatternCount supports univ ≤ 2^k`. **`cellPatternCount_le_card`** (`≤ \|L\|`); **`cellPatternCount_mono`** / **`cellPatternCount_le_global`** / **`globalCellCount_le_two_pow_gates`**; **`Pr_cellPatternCount_ge_le_size_tail`** (`Pr[≥a] ≤ Pr[\|L\|≥a]`, no `2^surv`); **`Pr_cellPatternCount_ge_eq_zero_of_global_lt`**; **`expected_cellPatternCount_le_global`** (`Exp ≤ globalCellCount`, direct first moment); **`low_global_cellCount_collapse`** / **`low_global_forces_lowCellCount`** / **`few_gates_forces_lowCellCount`** (deterministic: `globalCellCount<\|L\|`, resp `<n`, resp `2^k<n` ⇒ collapse, NO probability). HONEST WALL: `globalCellCount` is restriction-independent, generically `=n` for `k≫log n`; the restriction's real power is MERGING patterns among surviving gates (`cellPatternCount L ≪ globalCellCount`), and bounding that merged count below both `2^surv` and `\|L\|`/`globalCellCount` is the open structural-concentration content. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DirectCellConcentration` |
| **random restriction in cell-count language — first moment, Markov, sharp whp, the precise wall** | The cell-count analogue of the rank random-restriction/whp files, on `ACC0ForcesLowCellCount`. **`cellPatternCount_le_two_pow_cellRank`/`_survivingCount`** (monotone bounds); **`expected_cellPatternCount_le_expected_pow_survivors`** (`Exp[cellPatternCount] ≤ Exp[2^surv]`); **`Pr_cellPatternCount_ge_le_markov`** (`Pr[≥a] ≤ Exp/a`); **`Pr_cellPatternCount_ge_le_pow_survivor`** (proxy `≤ Exp[2^surv]/a`); **`randomRestriction_forces_low_cellCount`** (`Exp ≤ B < a ⇒ ∃ L, cellPatternCount < a`); **`cellCount_predictor_fails_whp`** (two-event balance, needs only `a ≤ b` — SHARPER than rank's `2^a ≤ b`); **`cellCount_whp_subsumes_rank`** (rank feasibility `2^a≤b` ⇒ cell-count fires — broadest whp); **`expected_cellPatternCount_le_of_bound`** + **laminar (`≤k+1`)** + **bounded-distinct (`≤2ᵈ`)** + **block-product (`≤cᵐ`)** discharges. HONEST WALL: first moment closes only for deterministic-bound structures; general wide overlapping `MOD` has only the exp-weak `Exp[2^surv]` handle — closing the gap needs structural concentration on the distinct-pattern count = the open switching lemma. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RandomRestrictionCellCount` |
| **integer-polynomial CRT decode (the exact *back half* of Beigel–Tarui)** | The integer count polynomial is `gateCount g x` (`…ACC0SymmetricObserver`: the exact `ℕ`-valued count of accepting sub-gates). For a pairwise-coprime family `qs : List ℕ` with `M = qs.prod`: **`count_crt_iff`** (`gateCount g x ≡ t [MOD qs.prod] ↔ ∀ i, gateCount g x ≡ t [MOD qs.get i]`, the Chinese Remainder Theorem for the count, via `Nat.modEq_list_prod_iff`); **`modCount_factors_through_resVec`** (the exact `MOD_{qs.prod}` count decision `decide (gateCount ≡ t [MOD qs.prod])` equals a fixed `G (countResVec qs g x)` where `countResVec qs g x i = (gateCount g x : ZMod (qs.get i))` — CRT collapses the modulus, `ZMod.natCast_eq_natCast_iff` turns each `≡ t [MOD q_i]` into a residue equation); **`modCount_observed`** (so the decision is `ObservedBy` the residue vector); **`countResVec_card_le`** (`|image(countResVec)| ≤ qs.prod`, landing in `∏_i ZMod (qs.get i)` of card `∏_i qs.get i = qs.prod` via `List.prod_ofFn`/`List.ofFn_get` + `ZMod.card`); **`count_crt_sat_speedup`** (`qs.prod < 2^n ⇒` SAT of the decision decided by a `< 2^n` residue-cell search); **`mod6_count_crt_speedup`** (the concrete `M = 6 = 2·3` split, `[2,3]`, searchable in `≤ 6` cells). This is the Beigel–Tarui CRT decode generalised from the fixed `mod6_eq_mod2_and_mod3` (`…Layer3MixedModulus`) to an arbitrary squarefree `M = ∏ q_i`, and attached to the **count polynomial** rather than one `MOD`-gate's support count. Honest: this is the **exact back half only** — CRT decodes from the *exact* count `gateCount`; it does **NOT** turn *approximate* RS residues (each correct on `1-ε` over its own prime field) into an exact decision (the CRT combination is correct only where all approximants are, still approximate). So it does *not* close the approximate/exact gap; that gap is the *front half* — **Wall 1**, `ACC⁰ → exact low-degree integer polynomial across depth` (`MixedACCDepthReductionSocket`/`HasExactSymAndForm`). A `< 2^n` cell count is still not a uniform algorithm (Wall 2) | `…ACC0IntegerPolynomialCRT` |
| **toy *exact-and-quasipolynomial* `SYM∘AND` (depth-2 bounded-fan-in DNF)** | The single restricted fragment where *exact* and *quasipolynomial* hold **simultaneously** — the property the general `ACC⁰` wall (Wall 1) provably cannot have. For a depth-2 `OR ∘ AND_w` DNF (`mono : Fin m → Finset (Fin n)` injective, `∀ j, (mono j).card ≤ w`): **`dnf_exact_symAnd`** (`(fun x => decide (∃ j, monoAND (mono j) x = true)) = symEval (fun j x => monoAND (mono j) x) (fun k => decide (1 ≤ k))` — the DNF **is** the symmetric count gate `[count ≥ 1]` over its bottom `AND`s, *exactly*, via `or_exact_sym`); **`dnf_bottom_count_le`** (`m ≤ ∑_{i≤w} C(n,i)` — distinct bottom `AND`s of fan-in `≤ w` are distinct degree-`≤w` monomials, `n^{O(w)}`, quasipoly for `w = polylog n`, via `monomial_count_le`); **`dnf_exact_quasipoly_searchable`** (**both at once**: the exact `SYM∘AND` identity *and*, once `(∑_{i≤w} C(n,i)) + 1 < 2^n`, SAT-searchable in `< 2^n` cells via `or_exact_searchable`, since `m + 1 ≤ (∑_{i≤w}C(n,i)) + 1 < 2^n`). Why the wall lifts *here* and exactly here: a bottom `AND` of fan-in `≤ w` (`monoAND`) **is** the degree-`≤w` monomial — a *genuine* `AND` gate, no approximation, so bounded fan-in keeps the degree bounded *exactly*; the top `OR` is *exactly* `[count ≥ 1]`. Honest: this holds **only** for the depth-2 bounded-fan-in `DNF` fragment. It does not extend to *unbounded* fan-in `AND` (degree = fan-in, up to `n`), the `MOD` layer (needs the RS *approximation* for low degree), or *arbitrary depth* (degree multiplies) — those are exactly where exactness forces exponential size (the front half of Beigel–Tarui, Wall 1). Still the cell/observer model; `< 2^n` cells is not a uniform algorithm (Wall 2) | `…ACC0ExactQuasipolyDepth2` |
| **exact-quasipoly fragment + one `MOD` top (`MOD_M ∘ AND_w`)** | Pushes the exact-quasipoly fragment one step toward the YBT normal form: replace the `OR` top with a `MOD_M` top (a genuine `SYM ∘ AND_w` with a modular count top), `M = qs.prod` over pairwise-coprime `qs`. **`modAnd_bottom_count_le`** (bottom `AND_w` quasipoly: `m ≤ ∑_{i≤w} C(n,i)`, `monomial_count_le`); **`modAnd_exact_observed`** (the `MOD_M ∘ AND_w` decision `modCountDecision qs t (monoAND ∘ mono)` factors *exactly* through `countResVec` — the top `MOD_M` is CRT-decoded on the integer count `gateCount`, `…ACC0IntegerPolynomialCRT`, **no approximation**); **`modAnd_exact_quasipoly_searchable`** (both: `m ≤ ∑_{i≤w}C(n,i)` *and* `∃ G`, SAT-searchable in `< 2^n` residue cells once `M = qs.prod < 2^n` — the modular top *compresses* the count to `M` classes, independent of the bottom count); **`mod6_and_exact_quasipoly_searchable`** (`M = 6 = 2·3` concrete, `[2,3]`). Both layers stay **exact**: the bottom `AND` of fan-in `≤ w` *is* the degree-`≤w` monomial (genuine gate), and `MOD` is *exactly* symmetric — it needs no RS approximation, unlike a low-degree polynomial over a single field. So the exact-and-quasipolynomial fragment *does* extend to one `MOD` layer. Honest — exactly where it stops: this is still depth 2 over a **bounded-fan-in** bottom. It does not reach the general YBT normal form (arbitrary `ACC⁰` bottom: unbounded fan-in / interleaved `MOD` / arbitrary depth), where exactness forces exponential size — the front half, **Wall 1**. The exact `MOD` top buys nothing there; it is the *bottom* `AND_w → exact low-degree polynomial across depth` that breaks. Cell/observer model; `< 2^n` cells is not a uniform algorithm (Wall 2) | `…ACC0ExactQuasipolyModTop` |
| **depth-3 exact composition over `MOD_M ∘ AND_w` (the "across depth" clause of Wall 1)** | Tests directly whether exactness survives *one composition step*: a top gate over `k` middle `MOD_M ∘ AND_w` gates (depth 3, bounded fan-in below). **`depth3_middle_exact`** (each middle `MOD_M ∘ AND_w` gate is decoded *exactly* by its count-residue vector — CRT, no approximation, `modCount_factors_through_resVec`); **`depth3_modAnd_compose_searchable`** (both: every middle bottom layer is quasipolynomial `m ≤ ∑_{d≤w} C(n,d)` via `monomial_count_le`, *and* the depth-3 circuit `top ∘ (MOD_M ∘ AND_w)^k` is SAT-searchable in `< 2^n` cells once `2^k < 2^n`, observed *exactly* by the `k`-bit middle-output vector via `exact_depth_composes`). **Answer: exactness survives the extra composition step — at the *product* cost `2^k`.** Honest — the cost *is* the wall: `2^k` is quasipolynomial only for `k = polylog`, exponential for unbounded top fan-in; and pushing the exact intermediate residues up instead multiplies the per-gate `M` cells to `M^k` — the same product blow-up. The general YBT normal form needs an *arbitrary* `ACC⁰` bottom across *arbitrary* depth, where this product is genuinely exponential — the front half, **Wall 1** (it is the *bottom* `AND_w → exact low-degree polynomial across depth` that breaks, not the symmetric top, which composes exactly here). Cell/observer model; `< 2^n` cells is not a uniform algorithm (Wall 2) | `…ACC0ExactQuasipolyDepth3` |
| **the anti-product trick — a *symmetric* top avoids the `2^k` blow-up** | The smallest case where the depth-3 *product* wall is genuinely avoided. Entry `…ACC0ExactQuasipolyDepth3`'s *arbitrary* top costs `2^k` because it reads the full `k`-bit middle-output vector; a *symmetric* top (a count / `MOD` / threshold over the `k` middle gates) reads only the **count** of accepting middle gates — `k+1` states. **`antiproduct_count_card_le`** (`(image (gateCount (middleGate …))).card ≤ k + 1`, via `sym_count_card_le` — the product `2^k` collapses to linear); **`antiproduct_sym_modAnd_searchable`** (every middle bottom layer quasipolynomial `m ≤ ∑_{d≤w}C(n,d)` *and* the depth-3 circuit with a *symmetric* top `symEval (middleGate qs mono t) h` is SAT-searchable in `≤ k+1` cells once `k+1 < 2^n`, via `sym_searchable`). So two symmetric layers — an outer `SYM` over `k` inner `MOD_M ∘ AND_w` gates (themselves `SYM ∘ AND`) — compose with *linear* `k+1` observer state, not the product `2^k`/`M^k`. Honest — why this genuine trick is still not YBT: (1) it needs the circuit *pre-grouped* as a single symmetric layer over `k` nice middle gates (reducing arbitrary `ACC⁰` to that shape is the open problem); (2) the count of accepting middle gates is symmetric in the *middle outputs*, not the *inputs*, so it cannot be iterated to flatten arbitrary depth; (3) the bottom `AND` degree must still stay polylog (Wall 1). This *is* the YBT `SYM` top being tractable (`…ACC0SymmetricObserver`: a symmetric function is observed by its count, `m+1` not `2^m`), made concrete one layer up — it avoids the product at *one* symmetric layer; reducing arbitrary `ACC⁰` to one `SYM ∘ AND` of quasipoly width with polylog bottom degree remains the front half, Wall 1. Cell/observer model; `< 2^n` cells is not a uniform algorithm (Wall 2) | `…ACC0AntiProductSym` |
| **bottom-clause no-go — exact unbounded-fan-in `OR`/`AND` needs full degree = fan-in** | Attacks Wall 1's *bottom clause* head-on at the gate level and proves the obstruction is **real** (the exact counterpart to the proved *approximate* side `toAgree_totalDegree_le`). Over `F₂` every Boolean function has a *unique* multilinear (ANF) representation via the subset-sum transform `anf` (`…MajorityAlgebraicImmunity`, `anf_involutive : anf (anf g) = g`). Computing the **top ANF coefficient** of the unbounded-fan-in gates: **`anf_andFn_univ`** (`anf andFn univ = 1` — `AND` is literally the single full monomial); **`anf_orFn_univ`** (`anf orFn univ = 1` for `n ≥ 1` — `OR`'s full monomial is present, via the parity of the `2ⁿ−1` nonempty subsets, computed with `Finset.sum_boole` + `filter_ne'`/`card_erase_of_mem` + `((2ⁿ−1 : ℕ) : ZMod 2) = 1`); **`or_exact_degree_full`**/**`and_exact_degree_full`** (`∃ S, |S| = n ∧ anf · S ≠ 0` — a nonzero coefficient at the full set, so exact degree `= n`); **`or_not_anf_degree_lt`**/**`and_not_anf_degree_lt`** (the no-go: `¬ ∀ S, n ≤ |S| → anf · S = 0`, i.e. **no exact `F₂` representation of degree `< n`**). So an exact unbounded-fan-in `OR`/`AND` has `F₂` degree exactly `n` = the fan-in, hence its exact monomial-`AND` (`SYM∘AND` bottom) representation needs `2^n` monomials — *exponential*, not quasipolynomial. This is the precise reason the bottom clause is hard: the **naive route — one exact low-degree polynomial per `ACC⁰` gate — is impossible** for unbounded fan-in (exactness forces degree = fan-in). The only escape is the *approximate* low-degree polynomial (RS `toAgree`, degree polylog, agrees `1-ε`) converted back to an *exact* decision by the symmetric/count top, keeping degree polylog — exactly the irreducible Beigel–Tarui analytic core, **Wall 1**. This file proves the gate-level exact-vs-approximate gap that makes that conversion *necessary*; it does **not** perform the conversion (does not cross Wall 1). Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0ExactDegreeNoGo` |
| **the BT approximate→exact conversion — socket + the proven decoding mechanism** | The route left after the no-go: recover the *exact* value from several *approximate* low-degree representations by a **symmetric/count decoder**. (1) The socket **`ApproxToExactSymmetricDecode D r f`** := `∃ g decoder, (∀ i, IsLowDegreeGate D (g i)) ∧ f = symEval g decoder` — the missing Beigel–Tarui theorem made *explicit*, where `IsLowDegreeGate D g := ∃ mono, (∀ j, (mono j).card ≤ D) ∧ g = symEval (monoAND ∘ mono) h`. **Crucially non-trivial only via the low-degree clause**: without it, `r` copies of `f` satisfy it (`f = symEval (const f) decoder`); *with* it, the socket holding for an arbitrary `ACC⁰` `f` (with `r` quasipoly, `D` polylog) **is** Wall 1. **`socket_searchable`** (the socket ⇒ `f` is the exact symmetric decode of `r` gates ⇒ SAT-searchable in `≤ r+1` cells once `r+1 < 2^n`, via `sym_searchable`). (2) The decoding mechanism **`majority_decode`** (TOY, *proved*): if at every point a strict majority of the `r` approximants agree with `f` (`∀ x, r < 2·#{i : g i x = f x}`), then `f x = decide (r < 2·gateCount g x)` *exactly* — error-corrected threshold-count decoding (proof: `gateCount = #(=1)` via `sum_boole`, `#(=1)+#(=0)=r` via `card_filter_add_card_filter_not`, then `omega` per `f x` case); **`majority_decode_symmetric`** (`f = symEval g (fun k => decide (r < 2k))` — `f` is *exactly* a symmetric threshold-count of the approximants); **`majority_decode_gives_socket`** (low-degree **and** majority-correct approximants ⇒ the socket). Honest decomposition: the *decoding* half is genuinely proved (symmetric threshold count recovers exact `f` under controlled error); the **open** part is producing `r =` quasipoly **low-degree** approximants that are majority-correct at *every* one of the `2^n` points — the irreducible Beigel–Tarui/Yao probabilistic construction, **Wall 1**, which this file does **not** perform. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0ApproxToExactDecode` |
| **the balancedness seed of the RS/BT probabilistic `OR` (attacking the construction)** | Attacks the open construction (Wall 1) at its genuine analytic atom — the random `F₂` linear form, the building block of the Razborov–Smolensky / Beigel–Tarui probabilistic polynomial. **`pv v S := ∑_{i∈S} (v_i : ZMod 2)`** (the form `L_S(v) = ⊕_{i∈S} v_i`); **`tog j S := S △ {j}`** with **`tog_tog`** (involution) and **`pv_tog`** (toggling a coordinate `j` with `v_j = 1` flips the form's parity: `pv v (tog j S) = pv v S + 1`); **`pv_false`** (on the all-`0` input the form is `0` for every `S` — *one-sided*: a random form never gives a false positive when `OR = 0`); **`pv_balanced`** — *the seed*: for `v` with some bit set, exactly `2^{m-1}` of the `2^m` subsets give form value `1`, i.e. **a uniformly random linear form predicts `OR = 1` correctly with probability exactly `1/2`** (proved by the parity-flipping toggle involution pairing the parity-`0` and parity-`1` subsets via `Finset.card_bij'`, then `card filter + card filter¬ = 2^m` and `2^m = 2·2^{m-1}`). This is the genuine analytic reason a random parity is an unbiased `OR`-predictor — the atom on which the whole BT construction rests. Honest scope — what remains (not faked): **(1) degree-`t` boosting** (`OR` of `t` independent forms is correct with probability `1 - 2^{-t}`, `> 1/2` for `t ≥ 2` — a product/independence count over `pv_balanced`); **(2) quasipoly sampling** (Chernoff + a union bound over the `2^n` inputs to extract `r = O(n)` boosted forms majority-correct *everywhere*, giving the quasipoly low-degree family the socket needs). Plus a basis caveat: these parity forms are low *polynomial* degree but high *monomial-`AND`* degree, whereas `IsLowDegreeGate` is monomial-`AND`-based — the `AND`/`XOR` translation is part of the RS argument. Steps (1)–(2) + that bridge are the remaining Beigel–Tarui analytic work, **Wall 1**. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0ProbabilisticForm` |
| **degree-`t` boosting — the full `t`-tuple form family is majority-correct everywhere** | Step (1) above, done: the `OR` of `t` independent forms `q_{(S_1,…,S_t)}(v) = ⋁_k L_{S_k}(v)` is wrong only when *all* `t` forms vanish. **`pv_zero_card`** (for `v ≠ 0`, exactly `2^{m-1}` subsets give form `0` — complement of `pv_balanced`); **`boost_wrong_card`** (the all-zero `t`-tuples number `(2^{m-1})^t`, by coordinatewise independence: the filter `{σ | ∀ k, pv v (σ k) = 0}` is `Fintype.piFinset (const (filter (=0)))`, card `= (2^{m-1})^t` via `Fintype.card_piFinset_const`); **`boost_total`** (`(2^m)^t` tuples via `Fintype.card_fun`/`card_finset`); **`boost_correct_card`** (correct tuples `= (2^m)^t - (2^{m-1})^t`, the complement); **`boost_predict_zero`** (on the all-`0` input the boosted predictor fires for *no* tuple — one-sided, no false positive); **`boost_majority_nonzero`** (for `t ≥ 2` and `v ≠ 0`, `total < 2 · correct` — a *strict* majority of `t`-tuples are correct, since `(2^m)^t = 2^t·(2^{m-1})^t > 2·(2^{m-1})^t`). So **the full family of `t`-tuples (`t ≥ 2`) is majority-correct at every input** (all tuples on `v = 0`, a `1 - 2^{-t} > 1/2` fraction on `v ≠ 0`) — this **discharges the "majority-correct" clause** of the socket `ApproxToExactSymmetricDecode` for `OR`: a family of low-(polynomial-)degree gates majority-correct everywhere genuinely exists. Honest — the sole remaining gap: the family *size* is `(2^m)^t`, *exponential*, whereas the socket needs `r =` quasipolynomial. Shrinking it (Chernoff concentration + a union bound over the `2^n` inputs to sample `r = O(n)` boosted forms keeping majority-correctness) is the probabilistic-method *sampling* step — that, plus the parity-vs-`monoAND` basis bridge, is the last of the Beigel–Tarui front half, **Wall 1**, not faked. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0ProbabilisticBoost` |
| **self-contained finite Chernoff (the sampling concentration engine, route B)** | The last gate of the BT front half needs concentration; Mathlib's Chernoff/Hoeffding (`ProbabilityTheory.measure_sum_ge_le_of_iIndepFun` etc.) are measure-theoretic, so this proves a **purely combinatorial** Chernoff lower-tail bound staying in `Finset`/`ℕ`/`ℝ` — no measure theory, no `exp`. **`finite_chernoff_lower`**: for any real `c ≥ 1`, sampling `σ : Fin r → P` with a good set `G ⊆ P`, the *minority-good* samples are bounded — `(#{σ | 2·#{i | σ i ∈ G} ≤ r} : ℝ) ≤ ((|G|/c) + c·(|P| − |G|))^r`. Proof = the classical Markov-on-a-product (Chernoff) argument in finite form: bound the count by `∑_σ c^r·d^{good σ}` (`d = (c²)⁻¹`, each minority term `≥ 1` via `pow_le_pow_right₀`), then the engine **`∑_{σ:Fin r→P} ∏_i w(σ i) = (∑_x w x)^r`** (`Finset.sum_prod_piFinset` + `Fintype.piFinset_univ` — the finite analogue of independence `E[∏] = ∏ E`), collapsing to `(c·(|G|·d + |P|−|G|))^r = (|G|/c + c(|P|−|G|))^r`. **`finite_chernoff_majority`**: the clean `c = 2`, good-fraction-`≥ 3/4` case (`3|P| ≤ 4|G|`) gives `≤ (7|P|/8)^r` — *geometric* decay, base `7/8 < 1` relative to `|P|`, exactly what the union bound consumes. Honest scope: this is the concentration **engine**, self-contained and proved. The finished sampling step still needs **(1)** instantiating `P` as the boosted form-tuples, `G` as the per-input correct set (`|G| ≥ 3|P|/4` from `…ACC0ProbabilisticBoost`); **(2)** the **union bound** over the `2^n` inputs (`∑_v #{bad at v} ≤ 2^n·(7|P|/8)^r < |P|^r` for `r = O(n)` ⇒ a sample correct everywhere by pigeonhole); plus the parity-vs-`monoAND` basis bridge — the remaining Wall 1 assembly, routine given this bound but not done here, not faked. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0FiniteChernoff` |
| **union-bound sampling existence (abstract — closes the sampling gate)** | The probabilistic-method existence argument on top of the finite Chernoff, finite and self-contained. **`exists_sample_majority_correct_all`** — abstract over input set `X`, predictor pool `P`, good sets `Good : X → Finset P`: given `∀ x, 3·|P| ≤ 4·|Good x|` (every input's good set has density `≥ 3/4`), `0 < |P|`, and `(|X| : ℝ)·(7/8)^r < 1`, there is a *single* sample `σ : Fin r → P` with `∀ x, r < 2·#{i | σ i ∈ Good x}` — a strict majority of the `r` coordinates land in `Good x`, **for every input `x` simultaneously**. Proof = union bound + pigeonhole: the global bad set `B = {σ | ∃ x, minority-good at x}` is `⊆ ⋃_x {minority-good at x}`, so `|B| ≤ ∑_x |minority-good at x| ≤ ∑_x (7|P|/8)^r = |X|·(7|P|/8)^r` (`card_biUnion_le` + `finite_chernoff_majority`); then `|X|·(7|P|/8)^r = |X|·(7/8)^r·|P|^r < |P|^r` (from the hypothesis, `mul_lt_mul_of_pos_right`), so `|B| < |P|^r = |{all samples}|`, hence `B ≠ univ` and a non-bad `σ` exists (`by_contra` + `card_le_card`). Deliberately **abstract** — not yet tied to parity forms. Intended instantiation (next step): `X = Fin m → Bool`, `P =` the boosted `t`-tuples of parity forms (`…ACC0ProbabilisticBoost`), `Good x =` the boosted forms correct on `x` (density `≥ 3/4` for `t ≥ 2` by `boost_majority_nonzero`); with `|X| = 2^m` the bound `2^m·(7/8)^r < 1` holds at `r = O(m)`, yielding the quasipolynomial-size form family majority-correct everywhere — exactly the open clause of the socket `ApproxToExactSymmetricDecode`. After that, the parity-vs-`monoAND` basis bridge is the final piece of the Beigel–Tarui front half, **Wall 1**. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0SamplingExistence` |
| **instantiation to parity forms — the quasipolynomial majority-correct family EXISTS** | Plugs the boosted parity forms into the abstract union-bound lemma, closing the sampling gate *concretely*. `X = Fin m → Bool` (`|X| = 2^m`), `P = Fin t → Finset (Fin m)` (boosted `t`-tuples), **`boostCorrect v σ := (∃ k, pv v (σ k) = 1) ↔ (∃ i, v i = true)`** (the boosted `OR`-prediction matches `OR(v)`), **`boostGood v := univ.filter (boostCorrect v)`**. **`card_pool`** (`|P| = (2^m)^t`); **`pv_all_false`** (form vanishes on the all-`0` input); **`boostGood_card_ge`** — *every input's good set has density `≥ 3/4`*: `3·|P| ≤ 4·|boostGood v|` for `t ≥ 2` (nonzero `v`: `boostGood v` = the firing tuples, card `(2^m)^t − (2^{m-1})^t` by `boost_correct_card`, and `4·(2^{m-1})^t ≤ (2^m)^t` since `2^t ≥ 4`; zero input: every tuple correct, `boostGood v = univ`); **`exists_quasipoly_majority_correct_forms`** — for `t ≥ 2` and `2^m·(7/8)^r < 1` (so `r = O(m)`), `∃ σ : Fin r → (Fin t → Finset (Fin m)), ∀ v, r < 2·#{i | σ i ∈ boostGood v}` — a single sample of `r = O(m)` boosted parity forms is **majority-correct at every one of the `2^m` inputs**, by `exists_sample_majority_correct_all`. This is exactly the quasipolynomial majority-correct family the socket `ApproxToExactSymmetricDecode` needs — **its open clause, now discharged for the parity-form construction**. The **one remaining piece** of the Beigel–Tarui front half is the *basis bridge*: these forms are low *polynomial* (`F₂`) degree but high *monomial-`AND`* degree, while the socket's `IsLowDegreeGate` is monomial-`AND`-based (the `AND`/`XOR` RS translation) — that last step is **Wall 1**, not done here. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0SamplingForms` |
| **the basis bridge — parity forms ARE low monomial-`AND` degree** | Resolves the `F₂`-poly-vs-`monoAND` mismatch. **Framing decision: the correct low-degree notion is `F₂`-polynomial total degree.** A linear form `L_S = ∑_{i∈S} X_i` has *polynomial* degree `1` — it is the *sum*, not the *product*, regardless of how large `S` is. So the boosted predictor `q_σ = 1 − ∏_k (1 − L_{σ k})` is a polynomial of total degree `≤ t`, and a degree-`≤t` `F₂` polynomial's Boolean-cube evaluation lies in the span of monomial-`AND`s of fan-in `≤ t`. There was no real mismatch — the "wide set" lives in the *linear* (degree-1) part, and only `t` forms multiply. **`linForm`** (`∑_{i∈S} X_i`), **`boostPoly`** (`1 − ∏_k (1 − L_{σ k})`), **`linForm_totalDegree_le`** (`≤ 1`, via `totalDegree_finsetSum_le` + `totalDegree_X`), **`boostPoly_totalDegree_le`** (`≤ t` — the crux, via `totalDegree_sub` + `totalDegree_finset_prod` + `oneSubLin_totalDegree_le`); **`eval_linForm`** (`eval (boolToZMod 2 ∘ v) (linForm S) = pv v S`), **`eval_boostPoly_eq`** (`= boolToZMod 2 (boostPredict σ v)` — the polynomial genuinely computes `⋁_k L_{σ k}`, by cases on `∃ k, pv = 1` with `prod_eq_zero`/`prod_eq_one`); **`boostPredict_mem_monoAND_span`** — *the bridge*: `(fun v => boolToZMod 2 (boostPredict σ v)) ∈ Submodule.span (ZMod 2) (range of degree-≤t monomial-`AND` indicators)`, via `lowDegPolyEval_mem_monoAND_span 2 t (boostPoly σ)`. So the probabilistic-method parity-form family (`…ACC0SamplingForms`) genuinely lives in the low-monomial-`AND`-degree world the socket wants. Honest scope: this resolves the bridge **for the `OR`-gate construction** (turning span membership into the exact `symEval`-`IsLowDegreeGate` packaging is the proved coefficient-duplication step, `…ACC0PolyToSymAnd`). What it does **not** do: the *full* Beigel–Tarui front half is the construction for an *arbitrary `ACC⁰` circuit across constant depth* (`(log s)^{O(d)}` degree, `MOD` layers, depth composition) — the single unbounded-fan-in `OR` handled here is one gate; the depth composition over all of `ACC⁰` remains **Wall 1**. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0BasisBridge` |
| **the `AND`-gate BT front half, via `OR`-duality** | `AND(v) = ¬ OR(¬v)`, so the `OR` construction dualizes directly. The linear form `L_S = ∑_{i∈S} X_i` becomes the **affine** form `A_S = ∑_{i∈S}(1 + X_i)` (which evaluates to `L_S(¬v)` on the negated input — `eval_affForm`), and `boostPoly` becomes `andPoly = ∏_k (1 − A_{σ k})`. Since `1 + X_i` is *still degree 1*, `AND` inherits `OR`'s exact degree profile. **`affForm_totalDegree_le`** (`≤ 1`), **`andPoly_totalDegree_le`** (`≤ t`); **`eval_andPoly_eq`** (`= boolToZMod 2 (andPredict σ v)`, by cases with `prod_eq_one`/`prod_eq_zero`); **`andPredict_eq_not_boostPredict`** — the duality `andPredict σ v = ¬ boostPredict σ (¬v)` (via `(∀ k, pv (¬v) (σ k) = 0) ↔ ¬ ∃ k, pv (¬v) (σ k) = 1` + `decide_not`), so the `OR` family's majority-correctness (`…ACC0SamplingForms`) transfers to `AND` under the input-negation bijection; **`andPredict_mem_monoAND_span`** — the `AND` basis bridge: the boosted `AND` predictor's `F₂`-embedding lies in the span of the degree-`≤t` monomial-`AND` indicators, via `lowDegPolyEval_mem_monoAND_span 2 t (andPoly σ)`. So the unbounded-fan-in `AND` gate has low-monomial-`AND`-degree majority-correct approximants too — the **second of the three `ACC⁰` gate types** (after `OR`; `MOD` next). Honest scope: this is the `AND`-gate front half; the `MOD` gate and the **depth composition** of these approximants through a constant-depth circuit (degree `(log s)^{O(d)}`) plus assembly into `SYM∘AND` are the rest of the Beigel–Tarui/Yao front half, **Wall 1**. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0AndBasisBridge` |
| **one-layer composition of BT approximants — degree multiplies by `t`, not exponentially** | The Razborov–Smolensky depth-composition step. The basis bridge built the boosted `OR` out of the input variables `X_i`; composing through a layer is the *same* construction with the `X_i` replaced by the `k` **subgate approximant polynomials** `P_j`. **`compLinForm P S = ∑_{j∈S} P_j`** (the linear form over subgate *outputs*), **`compPoly P σ = 1 − ∏_l (1 − ∑_{j∈σ l} P_j)`** (the boosted `OR` over the subgates). **`compLinForm_totalDegree_le`** (`≤ d` if each `P_j` has degree `≤ d`) and **`compPoly_totalDegree_le`** — *the crux*: `(compPoly P σ).totalDegree ≤ t · d`, i.e. **the degree multiplies by the boosting parameter `t` per layer**, so across constant depth `D` the degree is `≤ t^D · (base) = (log s)^{O(D)}` — polynomial-in-`log`, *not* exponential (the Razborov–Smolensky degree-composition phenomenon). **`eval_compPoly`** (the composed polynomial evaluates to the boosted `OR` over the subgate values `eval … P_j`); **`compPoly_eval_mem_monoAND_span`** (the composed approximant lies in the degree-`≤ t·d` monomial-`AND` span, via `lowDegPolyEval_mem_monoAND_span 2 (t*d)`). The basis bridge (`…ACC0BasisBridge`) is the special case `P_j = X_j`, `d = 1` (so `t·d = t`). Honest scope: this is **one `OR`-layer** of the depth composition (subgate degree `d` → composed degree `t·d`, staying low). The `AND`-layer is the affine dual, `MOD` its own case; iterating through a whole constant-depth `ACC⁰` circuit — tracking *error accumulation* across layers (each boosted approximant is only majority-correct, errors compound), discharging `MOD`, and assembling the final `SYM∘AND` — is the rest of the Beigel–Tarui/Yao front half, **Wall 1**. This file supplies the degree-composition step, not the full inductive assembly. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0LayerCompose` |
| **across-depth error accumulation — composed error grows *linearly* in the gate count** | The *error* counterpart to `…ACC0LayerCompose`'s degree composition; together they are the RS depth composition. The composite approximant errs at `x` only if *some* gate errs at `x` (correctness composes: where every gate is correct, the composite is correct), so the composite error set is contained in the union of per-gate error sets. **`error_union_bound`** (`(univ.biUnion Err).card ≤ Fintype.card G * e` when each `(Err g).card ≤ e`, via `card_biUnion_le` + `sum_le_sum` + `sum_const`); **`composite_error_subset`** (`{x | comp x ≠ target x} ⊆ ⋃_g Err g`, given `hcomp`: comp correct wherever no gate errs — `by_contra` + `push_neg`); **`composite_error_bound`** (`|composite error| ≤ Fintype.card G * e`); **`exists_majority_correct_composite`** — if `2·(Fintype.card G * e) < Fintype.card X` (total error `< 1/2`), then `Fintype.card X < 2·|{x | comp x = target x}|` (the composite agrees with the target on a *strict majority* of inputs; via `card_filter_add_card_filter_not` + `omega`). **Linear-in-`s` error (not exponential) is exactly why constant depth works**: a depth-`d`, size-`s` circuit has `s` gates, total error `≤ s·e`, so taking the per-gate error `e < |X|/(2s)` — achievable with the boosting at polylog degree — keeps the whole-circuit composite *majority-correct*. Combined with `…ACC0LayerCompose` (degree `(log s)^{O(d)}`, polylog), this is the Razborov–Smolensky depth composition: linear error, polylog degree. Honest scope: this is the abstract union-bound error-accumulation mechanism over arbitrary per-gate error sets. The full RS composition also needs discharging `hcomp` for the actual gate substitution (per-point composition), the per-gate boosting that makes `e` small (`…ACC0ProbabilisticBoost`, `e = 2^{-t}·|X|`), and the `MOD` layer; iterating into the `ACC⁰ → SYM∘AND` normal form is the rest of the Beigel–Tarui/Yao front half, **Wall 1**. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0ErrorAccumulation` |
| **per-point composition — the composed polynomial computes the gate of the subgate values** | Discharges the `hcomp` hypothesis of `…ACC0ErrorAccumulation` for an `OR` layer. **`boost_formula`** (`1 − ∏_l (1 − pv w (σ l)) = boolToZMod 2 (boostPredict σ w)`, both sides `= eval (boostPoly σ)` at `w`); **`eval_compPoly_of_subgates`** — if each subgate poly `P_j` computes its Boolean subgate `h_j` at `x` (`eval (P_j) x = boolToZMod 2 (h_j x)`), then `eval (compPoly P σ) x = boolToZMod 2 (boostPredict σ (fun j => h_j x))`: the composed polynomial computes the boosted `OR` of the subgate *values*. This is the bridge tying the *degree* composition (`…ACC0LayerCompose`) and *error* composition (`…ACC0ErrorAccumulation`) to the actual Boolean computation. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0CompositionCorrect` |
| **the `MOD₂` gate is *exactly* degree 1 (error 0)** | Where `OR`/`AND` have no exact low-degree polynomial (`…ACC0ExactDegreeNoGo`) and need the probabilistic boosting, `MOD₂` (parity) over `F₂` simply **is** the linear form `⊕_i x_i = ∑_i X_i`. **`mod2_exact_eval`** (`eval (linForm univ) x = boolToZMod 2 (parityBool x)` — exact, no approximation), **`mod2_totalDegree_le`** (`≤ 1`), **`mod2_mem_monoAND_span`** (in the degree-`≤1` monomial-`AND` span, exactly). So `MOD₂` composes with degree `d = 1`, error `e = 0`. Honest scope — and the real barrier: this is `MOD₂` only (`AC⁰[2]`). The Razborov–Smolensky polynomial method works over `F_p` for **prime-power** moduli; for **composite** `m` with two distinct prime factors (e.g. `MOD₆`) there is *no* known low-degree representation over any single field — exactly why `ACC⁰` lower bounds are hard and why the only known result (`NEXP ⊄ ACC⁰`, Williams) uses a different, algorithmic method. The `MOD` part genuinely stops at prime-power moduli; composite-`MOD` `ACC⁰` is the open barrier, **Wall 1** at its strongest. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0Mod2Exact` |
| **the `MOD_p` gate is *exactly* degree `p−1` over `F_p` (Fermat) — the `AC⁰[p]` ingredient** | The prime-`p` generalisation of `…ACC0Mod2Exact`. By **Fermat's little theorem** (`ZMod.pow_card_sub_one_eq_one`: `a^{p−1}=1` for `a≠0`, `0^{p−1}=0`), over `F_p` the count-`≡0` indicator is `MOD_p(x) = [∑_{i∈S} x_i ≡ 0 (mod p)] = 1 − (∑_{i∈S} X_i)^{p−1}` *exactly*. **`modpPoly`/`modpBool`** (the `F_p` polynomial and the gate), **`modp_exact_eval`** (`eval (modpPoly p S) x = boolToZMod p (modpBool p S x)` — exact, error 0, via Fermat + `set s`/case-split on `s=0`), **`modpPoly_totalDegree_le`** (`≤ p−1`, via `totalDegree_pow` on the degree-1 linear form), **`modp_mem_monoAND_span`** (in the degree-`≤(p−1)` monomial-`AND` span over `F_p`, via `lowDegPolyEval_mem_monoAND_span` at prime `p`). So a `MOD_p` gate composes into the RS machinery with degree `p−1`, error `0`; prime powers reduce to `F_{p^k}` by the same argument, giving the full `AC⁰[p^k]` picture. Honest scope — the barrier: this is **prime** `p` only. For composite `m=p·q`, Fermat pins the exponent to the chosen field's order (`p−1`, not `m−1`), so `a^{m−1}` is no `{0,1}`-indicator over `F_p` and fails symmetrically over `F_q` — **no single field sees `MOD_m` low-degree** (`mod6_eq_mod2_and_mod3`, `modq_residue_image_not_subspace`). That is why `NEXP ⊄ ACC⁰` needed Williams' algorithmic method. Classical `AC⁰[p]`-level; nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0ModPExact` |
| **a single small-error boosted form, by averaging — the per-gate error input** | Error accumulation (`…ACC0ErrorAccumulation`) needs each gate to have a *single* approximant with a small error set; the boosting (`…ACC0ProbabilisticBoost`) gives a majority-correct *family*. This extracts a single good form by averaging (first moment). **`errSet σ = {w | (∃ j, w_j) ∧ ∀ l, L_{σ l}(w) = 0}`** (the boosted-`OR` error, one-sided) with **`errSet_eq`** (it equals the actual `boostPredict`-vs-`OR` error, via one-sidedness `pv = 1 → ∃ j, w_j` and `(∀l, pv=0) ↔ ¬∃l, pv=1`); **`sum_errSet_card_le`** (`∑_σ |errSet σ| ≤ 2^k · (2^{k-1})^t` — the Fubini double-count `∑_σ |errSet σ| = ∑_w #{σ : σ errs on w}`, each nonzero `w` err'd by exactly `(2^{k-1})^t` forms via `boost_wrong_card`); **`exists_small_errSet`** (averaging + pigeonhole via `card_nsmul_le_sum` + `exists_min_image`: `∃ σ, (2^k)^t · |errSet σ| ≤ 2^k · (2^{k-1})^t`, i.e. error fraction `≤ 2^{-t}`). This is the per-gate small-error form that feeds `…ACC0ErrorAccumulation`. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0SmallErrorForm` |
| **circuit datatype + approximation invariant + base cases (the depth-induction scaffolding)** | Sets up the datatype and invariant for the Razborov–Smolensky depth induction. **`Circ n`** — an *unbounded-fan-in* Boolean circuit (`inp`/`const`/`not`/`or : List (Circ n)`/`and : List (Circ n)`; unbounded fan-in is the whole point of the polynomial method); **`Circ.eval`** and **`Circ.size`** (the `or`/`and` recursions over the `List` use `cs.attach` + a `sizeOf` termination argument with a two-pass `decreasing_by`). **`errCard P C`** — the number of inputs on which the `F₂` polynomial `P` disagrees with the circuit `C`'s value (under the `boolToZMod` embedding); **`Approximable C D E := ∃ P, P.totalDegree ≤ D ∧ errCard P C ≤ E`** — the approximation invariant. **`errCard_eq_zero_of_exact`** (exact computation ⇒ error 0). Base cases PROVEN: **`approximable_inp`** (`P = X i`, `D=1, E=0` — the variable is computed exactly), **`approximable_const`** (`P = C (boolToZMod 2 b)`, `D=0, E=0`), **`approximable_not`** (`P ↦ 1 − P`, preserves `D` and `E` — `1 − ·` is an `F₂` bijection flipping the value, proved via `(∀ u v : ZMod 2, (1 − u ≠ 1 − v) ↔ (u ≠ v))`). Honest scope — the inductive step that remains: for `OR`/`AND`, "subcircuits `Approximable` with degree `≤ D`, errors `≤ E_i` ⇒ `Circ.or cs` `Approximable` with degree `≤ t·D`, error `≤ (∑ E_i) + 2^{-t}·2^n`" — degree from `…ACC0LayerCompose`, the error split from per-point composition (`…ACC0CompositionCorrect`) + union bound (`…ACC0ErrorAccumulation`) over subgate errors plus the gate's own boosting error (`…ACC0SmallErrorForm`, input-space variant). Assembling that step, iterating to `degree ≤ t^d` / `error ≤ size·2^{-t}`, and handling `MOD` (prime-power only — composite `MOD` is the genuine open barrier) is the rest of the Beigel–Tarui/Yao front half, **Wall 1**. This file is the scaffolding (datatype, invariant, base cases). Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0CircuitApprox` |
| **input-space small-error averaging — the per-gate error for the circuit induction** | `…ACC0SmallErrorForm` averaged the boosted error over the *subgate-output* space `Fin k → Bool`; but in the circuit induction the gate's inputs are the *circuit* inputs `x` and the subgate values are `v(x) = (c_1(x), …, c_k(x))`, with `x ↦ v(x)` **not** measure-preserving — so this averages over the *input* space directly, for an arbitrary value map `v : X → (Fin k → Bool)`. **`boostError_iff`** (the boosted `OR`-predictor errs at output vector `w` iff `(∃ j, w_j) ∧ ∀ l, L_{σ l}(w) = 0`, via one-sidedness `pv = 1 → ∃ j`); **`errSetV v σ`** (the inputs where the form errs on `v(x)`) with **`errSetV_eq`** (it equals the actual `boostPredict (v x) ≠ OR (v x)` error set); **`vanish_card_le`** (for fixed `w`, `≤ (2^{k-1})^t` forms vanish on it, via `boost_wrong_card`); **`sum_errSetV_card_le`** (`∑_σ |errSetV v σ| ≤ |X|·(2^{k-1})^t` — Fubini `∑_σ |{x : errs}| = ∑_x |{σ : errs on v(x)}|`, each input contributing `≤ (2^{k-1})^t`); **`exists_small_errSetV`** (averaging + pigeonhole: `∃ σ, (2^k)^t · |errSetV v σ| ≤ |X|·(2^{k-1})^t`, i.e. the form errs on `≤ 2^{-t}` fraction of *inputs*). This is the per-gate small-error form *over the circuit inputs* — the input needed for the `OR`/`AND` inductive step of `…ACC0CircuitApprox`; it generalizes `…ACC0SmallErrorForm` (the case `X = Fin k → Bool`, `v = id`). Honest scope: plugging it into the inductive step (gate boosting error over inputs) + per-point composition (`…ACC0CompositionCorrect`) + union bound (`…ACC0ErrorAccumulation`) gives one layer; iterating it, and handling `MOD` (prime-power only — composite `MOD` is the genuine open barrier), is the rest of the front half, **Wall 1**. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0InputSmallError` |
| **the `OR`/`AND` inductive step, assembled** | Assembles one `OR`/`AND` layer of the Razborov–Smolensky depth induction from all the pieces. `perr P f` = the error set of polynomial `P` against Boolean function `f`. **`or_step`** — for subgates `h_i` with degree-`≤D`, error-`≤E` approximants `P_i`, there exist `Q` (`= compPoly P σ`) and a gate-error budget `Eg` with `Q.totalDegree ≤ t·D`, `(perr Q (OR-of-h)).card ≤ k·E + Eg`, and `(2^k)^t·Eg ≤ 2^n·(2^{k-1})^t` (gate error `≤ 2^{-t}` fraction). **The crux is the error containment** `perr(compPoly P σ, OR(h)) ⊆ (⋃_i perr(P_i, h_i)) ∪ errSetV v σ`: at any input where *every* subgate approximant is correct (so `eval (compPoly) = boolToZMod (boostPredict (v x))` by `eval_compPoly_of_subgates`) *and* the gate's boosting is correct on `v(x)` (so `boostPredict (v x) = OR(v x)` by `errSetV_eq`), the composed polynomial computes the gate exactly — hence it errs only via a subgate error or the gate's own boosting error. Counting (`card_union_le` + `card_biUnion_le` + `∑_i E = k·E`) gives `≤ k·E + |errSetV v σ|`, and `exists_small_errSetV` (input-space averaging) makes the gate term `≤ 2^{-t}` fraction; degree is `compPoly_totalDegree_le`. **`and_step`** — the dual, `AND = ¬OR¬`: apply `or_step` to the negated subgates `¬h_i` (with approximants `1 − P_i`, whose error sets are unchanged by `perr_not_eq`) and negate the result, using `andTarget_eq` (`andTarget w = !(orTarget (¬w))`); same degree and gate-error bounds. Honest scope: this is one `OR`/`AND` layer over a `Fin k`-indexed family — the genuine content of the inductive step. Threading it through the `Circ` datatype (`…ACC0CircuitApprox`'s `List`-to-`Fin` packaging), iterating to the depth-`d` bounds (`degree ≤ t^d`, `error ≤ size·2^{-t}`), and handling `MOD` (prime-power only — composite `MOD` is the genuine open barrier) is the rest of the Beigel–Tarui/Yao front half, **Wall 1**. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0OrStep` |
| **the depth induction — every `MOD`-free circuit is approximable (the `AC⁰` case, closed)** | Closes the Razborov–Smolensky depth induction for the `MOD`-free (`AC⁰`) fragment, end to end. **`or_eval_bridge`** / **`and_eval_bridge`** — the `List`-to-`Fin` packaging: `Circ.eval x (or cs) = orTarget (fun i : Fin cs.length => Circ.eval x (cs.get i))` (and the `and`/`andTarget` dual), via `List.any_eq_true`/`all_eq_true` + `List.mem_iff_get` + `Bool.eq_iff_iff`. `errCard_eq_perr` (definitional). **`approximable_exists`** — `∀ C : Circ n, ∃ D E, Approximable C D E`: *every* `MOD`-free circuit has a low-degree `F₂` approximant, by well-founded recursion on `sizeOf` (the `or`/`and` recursive calls decrease by `List.sizeOf_lt_of_mem (List.get_mem cs i)`), with base cases `approximable_inp`/`approximable_const`/`approximable_not` and the inductive step `or_step`/`and_step` — bridged from the `List` fan-in to the `Fin`-indexed family using `choose` (for the per-subgate approximants) and `Finset.sup` (for the uniform degree/error bounds). **Honest scope — this is classical and not new:** the result is `AC⁰`/`AC⁰[2]`-level (the corpus's Tier 1/2: `PARITY ∉ AC⁰`, `MOD ∉ AC⁰[p]`), a clean from-scratch Razborov–Smolensky construction, **not new mathematics and not progress toward `P ≠ NP`**. The bounds are existential (`∃ D E`); the quantitative `degree ≤ t^depth`, `error ≤ size·2^{-t}` follow by tracking `sup`/`size` through the same recursion. Extending to `MOD` works only for **prime-power** moduli (`mod2_mem_monoAND_span` is the `MOD₂` case); **composite `MOD_m` has no low-degree representation over any single field** — the genuine `ACC⁰` barrier, **Wall 1**, which the polynomial method *cannot* cross (the reason `NEXP ⊄ ACC⁰` required Williams' algorithmic method, not the polynomial method). Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0DepthInduction` |
| **quantitative degree bound — every `MOD`-free circuit is approximable at degree `≤ t^depth`** | Sharpens the `AC⁰` depth induction to the Razborov–Smolensky *degree* form. **`cdepth`** (circuit depth: `OR`/`AND` add a layer, `NOT` free, leaves `0`); **`le_foldr_max`** (list element `≤ foldr max`); **`cdepth_or_get_lt`**/**`cdepth_and_get_lt`** (a subcircuit's depth is *strictly less* than its `OR`/`AND` parent's, via `le_foldr_max` + `omega`); **`Approximable.mono_deg`** (weaken the degree bound). **`approximable_quant`** — for `t ≥ 1`, `∀ C : Circ n, ∃ E, Approximable C (t ^ cdepth C) E`: every `MOD`-free circuit has an `F₂` approximant of total degree `≤ t^depth`. By well-founded recursion: base cases via `simp [cdepth, pow_zero]` / `mono_deg`; the `OR`/`AND` step applies `or_step`/`and_step` with the *uniform* subgate-degree bound `D = t^(depth − 1)` (each subgate degree `≤ t^cdepth(child) ≤ t^(depth−1)` by `Nat.pow_le_pow_right` + child-depth `<` parent-depth) and then `t · t^(depth−1) = t^depth` (`← pow_succ'` + `congr` + `omega`). So each `OR`/`AND` layer multiplies the degree by `t`, giving `t^depth = (log s)^{O(d)}` for `t = polylog`, constant depth — the RS degree bound. Honest scope: still **classical** `AC⁰`/`AC⁰[2]`-level (Tier 1/2), not new mathematics, not `P ≠ NP`; the *error* bound stays existential (accumulates `≤ k·E + 2^{-t}` per layer, `…ACC0OrStep`); composite `MOD_m` is the genuine barrier, **Wall 1**. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0QuantDegree` |
| **quantitative error bound — approximant at degree `≤ t^depth` AND error `≤ size·2^{-t}` (the finished `AC⁰` package)** | Completes the quantitative Razborov–Smolensky package for the `MOD`-free (`AC⁰`) fragment. **`gate_bound`** — a boosted form whose error is `≤ 2^{-t}` of the inputs: `∃ σ, 2^t·|errSetV v σ| ≤ 2^n` (cancels the `(2^{k-1})^t` factor for `k ≥ 1` via `Nat.le_of_mul_le_mul_right`; the empty-gate `k = 0` case has `errSetV = ∅`, `Fin 0` empty). **`or_step_quant`/`and_step_quant`** — the `OR`/`AND` step in *sum* form: degree `≤ t·D`, error `≤ (∑ subgate errors) + Eg`, and the gate bound `2^t·Eg ≤ 2^n`. `sum_get_eq` — the `Fin`↔`List` sum bridge (`(cs.attach.map (g ∘ ·.1)).sum = ∑ i, g (cs.get i)`, via `List.ofFn`/`map_ofFn`/`sum_ofFn`). **`approximable_full`** — for `t ≥ 1`, `∀ C : Circ n, ∃ Q, Q.totalDegree ≤ t^cdepth C ∧ 2^t·(perr Q ⟦C⟧).card ≤ size C · 2^n`: every `MOD`-free circuit has an `F₂` approximant of **degree `≤ t^depth` *and* error `≤ size·2^{-t}`** (count form `2^t·error ≤ size·2^n`). By well-founded recursion: each `OR`/`AND` gate contributes a `2^{-t}`-fraction boosting error (`gate_bound`) and errors accumulate *additively* across the `size` gates (union bound, `or_step_quant`'s `∑`); the degree multiplies by `t` per layer. So the error stays `≤ size·2^{-t}` — for `t = log(2·size)` and constant depth, degree `(log s)^{O(d)}` (quasipoly) and error `< 1/2` (the function is exactly computed on a majority). Honest scope: this **finishes the quantitative `AC⁰` RS construction** (degree `t^depth`, error `size·2^{-t}`) — still **classical** (`AC⁰`/`AC⁰[2]`-level, Tier 1/2), **not** new mathematics, **not** `P ≠ NP`. `MOD` extends only to prime-power moduli (`MOD₂` done); composite `MOD_m` is the genuine `ACC⁰` barrier, **Wall 1**, which the polynomial method cannot cross. Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP` | `…ACC0QuantError` |

#### The exact-quasipoly fragment boundary (the four probes, as one localized result)

The four entries above (`…ACC0IntegerPolynomialCRT`, `…ACC0ExactQuasipolyDepth2`, `…ACC0ExactQuasipolyModTop`,
`…ACC0ExactQuasipolyDepth3`) are not four separate facts — they are **one result probed from four sides**, mapping
exactly the boundary of where an *exact-and-quasipolynomial* `SYM∘AND` is achievable, and isolating the single clause
that fails in general (Wall 1).  Read together:

| probe | fragment | exact? | size / cell cost | what it isolates |
|---|---|---|---|---|
| **back half** (`…IntegerPolynomialCRT`) | a `MOD_M` *top* on the integer count, `M = ∏ q_i` | **exact** (CRT, no approx) | `M` residue cells | the *symmetric/modular top* is exactly decodable — for free |
| **top kind** (`…ExactQuasipolyDepth2`) | `OR ∘ AND_w` (DNF) | **exact** | `≤ ∑_{i≤w}C(n,i)` bottom | a *bounded-fan-in* `AND` *is* a degree-`≤w` monomial, exactly |
| **one MOD layer** (`…ExactQuasipolyModTop`) | `MOD_M ∘ AND_w` | **exact** | bottom quasipoly, top `M` cells | the modular top *composes onto* the exact bottom, still exact |
| **one depth step** (`…ExactQuasipolyDepth3`) | `top ∘ (MOD_M ∘ AND_w)^k` | **exact** | **product** `2^k` (or `M^k`) | composition survives exactly — at a *product* cost |

The invariant across all four: **exactness is never the obstruction.**  Every symmetric top (`OR`, `MOD`, threshold)
is *exactly* a count/CRT-decoded gate, and every *bounded-fan-in* `AND` is *exactly* a bounded-degree monomial — no
Razborov–Smolensky approximation is needed anywhere in this fragment.  What is bounded — and the only thing that must
be bounded — is the **fan-in / degree of the bottom `AND` layer**, which controls both the monomial count
(`∑_{i≤w}C(n,i)`, quasipoly for `w = polylog`) and the composition cost (`2^k`/`M^k`, quasipoly for `k = polylog`).

So the fragment boundary is sharp and one-dimensional: the exact-and-quasipolynomial `SYM∘AND` holds **iff the bottom
`AND` degree stays polylog**, and the symmetric/modular structure above it is irrelevant to exactness.  The general
Yao–Beigel–Tarui normal form is precisely the case where the bottom degree is *not* given bounded — an arbitrary
`ACC⁰` subcircuit (unbounded fan-in / interleaved `MOD` / arbitrary depth) whose *exact* low-degree representation
across depth is the open analytic core.  The four probes thus localize **Wall 1 entirely to the bottom**:
`ACC⁰ → exact low-degree integer polynomial across depth`.  Nothing in this fragment is `NEXP ⊄ ACC⁰` or `P ≠ NP`;
it is the precise map of how far exactness reaches before that one clause stops it.

#### The two remaining YBT walls (what is *not* proved, and exactly why)

The YBT cash-out lane above is **fully assembled** in the cell/observer model: every link from the `SYM∘AND` normal
form onward is proved sorry-free (`circuit → poly` exact via Layer3 `toPoly`; `poly → SYM∘AND`-span given degree via
`acc0p_circuit_in_monoAND_span`; degree discharged for the approximant via `acc0p_toAgree_in_monoAND_span`;
coefficient-duplication count-mod-`p` via `weightedGateCount_cast_eq`; `SYM` count observer via `sym_searchable`;
exact gate symmetry via `…ACC0SymmetricExact`; exact depth composition via `exact_depth_composes`; additive degree
via `…ACC0AdditiveDegree`). What remains are **two named walls**, each *not* a missing lemma but a known
research-grade theorem, documented here so the boundary is explicit and never silently crossed.

**Wall 1 — the exact–quasipoly tension (the Beigel–Tarui analytic core).**  This is now a *theorem*, not a
suspicion: `exact_depth_composes` together with `…ACC0AdditiveDegree`/`…ACC0ToAgreeDegree` pins the obstruction
exactly.  There are two ways to compose an `ACC⁰` circuit into a searchable normal form, and they are mutually
exclusive with the tools in hand:

* **Exact composition** (`exact_depth_composes`, `…ACC0SymmetricExact`): the gates `AND`/`OR`/`MOD` *are* exact
  symmetric count functions, so they compose at any depth with **no approximation** — but the boundary is the
  *product* `2^k` (or `∏ᵢ|Sᵢ|`, `depth_compose_searchable`), **exponential** in the width.  Exact ⇒ exponential.
* **Quasipolynomial composition** (`toAgree_totalDegree_le`, `…ACC0AdditiveDegree`): polynomial degree composes
  *additively* (`toPoly_andGate_totalDegree_le`: `AND` degree = sum, not product, of input degrees), so a
  constant-depth circuit has polylog degree `((p-1)t)^{depth}` and hence only **quasipolynomially** many
  monomial-`AND`s (`∑_{i≤D}C(n,i)`).  But the polylog-degree representation is the Razborov–Smolensky **approximant**
  (`toAgree`), which agrees with the circuit on only a `1-ε` fraction (`…ACC0ApproxConsequence` shows an approximate
  `SYM∘AND` bounds the *solution count*, not SAT).  Quasipoly ⇒ approximate.

  Having **both at once** — an *exact* `SYM∘AND` of *quasipolynomial* size — is the Beigel–Tarui construction: an
  exact integer-valued polynomial of polylog degree whose value is decoded by the symmetric top gate via CRT
  (`MOD`-counting modulo several primes simultaneously).  That construction is the irreducible analytic core of the
  Yao–Beigel–Tarui normal-form theorem.  It is a *known classical result* but is **NOT** formalized here, and it
  cannot be obtained by gluing the two proved halves (each provably gives up the property the other needs).  Socketed
  as `MixedACCDepthReductionSocket` / `HasExactSymAndForm`; the socket holding for arbitrary `ACC⁰` with `m`
  quasipolynomial **is** the YBT theorem.  *The CRT-decode **back half** is now formalized* (`…ACC0IntegerPolynomialCRT`):
  given the *exact* integer count polynomial, the exact `MOD_M` decision is recovered from its residues modulo the
  prime factors of `M` (`count_crt_iff`, `modCount_factors_through_resVec`).  This sharpens the wall to the **front
  half** alone — producing the *exact* low-degree integer polynomial from an `ACC⁰` circuit across depth — since CRT
  decodes only from an *exact* count and cannot upgrade approximate RS residues to an exact decision.

**Wall 2 — Williams uniform realization (the algorithmic / separation-strength step).**  Even granted an exact
quasipoly `SYM∘AND` normal form (Wall 1), a *small cell count is not a uniform algorithm*.  Turning the
normal-form/observer structure into the actual `2^{n-n^ε}`-time `ACC⁰`-SAT algorithm — and running it through
Williams' connection to get `NEXP ⊄ ACC⁰` — is the `UniformWilliamsRealizationSocket` step
(`…ACC0WilliamsCashout`, `…WilliamsNEXP_ACC0`, `…NFrameACC0Speedup`).  This is **separation-strength** and is
deliberately **untouched** (per the HAL guidance: do not attack the uniform-realization cash-out, which is
equivalent to the separation itself).  The self-audit theorems `williams_socket_iff_separation` /
`realization_socket_iff_separation` prove each cash-out socket is *logically equivalent to the separation* — so they
reduce nothing; they honestly mark where the `NEXP ⊄ ACC⁰`-strength content lives.

**Net honest position.**  Everything reducible in the cell/observer model is discharged sorry-free with clean axioms
`[propext, Classical.choice, Quot.sound]`.  The lane bottoms out at exactly these two named walls — Wall 1 (the
Beigel–Tarui exact-quasipoly normal form, a known but unformalized classical theorem) and Wall 2 (Williams uniform
realization, separation-strength).  Nothing here is, or claims to be, `NEXP ⊄ ACC⁰` or `P ≠ NP`.

**Strongest proved statement.** *Bounded‑overlap depth‑2 `MOD`‑bottom ACC⁰ circuits fail to correlate with the
holonomy parity after a random restriction, under an explicit feasibility inequality* (`Pr(survivors ≥ a) +
Pr(live ≤ b) < 1` with `2^a ≤ b`).  **The one remaining wall:** unbounded support reuse / high‑overlap
depth‑reduction (the higher‑moment Håstad switching), where the survivor count can reach `~log₂` of the live count
and the two tails cannot both be driven below `1`.  Everything reachable by the moment method, pigeonhole, bounded‑
overlap double‑counting, and union bounds is proved sorry‑free with clean axioms; that single tension is the genuine
`NP ⊄ ACC⁰`‑strength residue.

**Depth reduction toward deeper circuits (`…ACC0DepthReduction`).**  The deterministic backbone for lifting past
depth 2 is proved: `support` and `eval_eq_of_agreeOn` (**locality** — a circuit of any depth reads only its
support, by induction); `eval_const_of_support_disjoint` (**killed ⇒ constant** — a subcircuit whose support is
disjoint from the live set is constant across live completions, the deterministic version of the collapse a
restriction produces); `modSupports` (**MOD‑support extraction** for arbitrary depth — the support family the
switching machinery acts on); and `reduction_bridge` (**transfer** — any circuit extensionally equal to a depth‑2
`MOD`‑bottom `C'` inherits `C'`'s correlation failure after a low‑survivor restriction).  The two layers of the
remaining wall are named as one property: `Depth2Reducible` (a circuit *is* extensionally depth‑2 `MOD`‑bottom) and
`HastadDepthReduction` (a poly‑size constant‑depth circuit, after a random restriction, becomes `Depth2Reducible`
with bounded‑overlap survivors).  Granted `HastadDepthReduction`, `reduction_bridge` discharges the rest — so the
**entire remaining `NP ⊄ ACC⁰` difficulty is isolated to that single named property**: the random restriction
simultaneously collapsing the AND/OR depth to a `MOD`‑bottom survivor *and* keeping its overlap bounded.

**The star family — a concrete miniature of the high‑overlap wall (`…ACCOverlapStar`).**  To see *where* high
overlap hurts, the star family `S_j = core ∪ petal_j` (shared `core`, pairwise‑disjoint `petals`) concentrates all
overlap in the core: `star_inter` proves `S_i ∩ S_j = core` for `i ≠ j`.  Consequences, all proved: `star_cov_le`
(`Cov(X_{S_i}, X_{S_j}) ≤ |core|·p` off‑diagonal) and `star_variance_le` — **`Var[X] ≤ k·s·p + k²·|core|·p`**, the
*quadratic‑in‑`k`* core blowup that the second moment cannot absorb when `|core|` is large (the miniature of the
higher‑moment wall).  But `core_surgery` shows it is **removable by a restriction**: with the core dead (disjoint
from the live set), every live coordinate lies in *at most one* star support — overlap collapses to `1`, so the
disjoint pipeline (`Var ≤ k·s·p`) fires.  **Verdict: for star families high overlap is bad only while the shared
core stays live**; a depth‑reduction restriction that kills the core defeats it.  This locates the genuine wall
precisely — it is *not* concentrated‑in‑a‑small‑core overlap (solvable by core surgery) but overlap that no small
killed set can disjointify, which is where the real Håstad difficulty lives.

**The spread‑overlap probe — the genuine Håstad regime (`…ACCOverlapDesign`).**  Testing the real enemy: a *design
family* with bounded but spread pairwise intersections.  The decisive finding, all proved: `design_variance_le`
gives `Var[X] ≤ k·s·p + k²·λ·p` under `|S_i ∩ S_j| ≤ λ` — **the same bound shape as the star**, so the second
moment *cannot* distinguish concentrated from spread overlap.  What separates them is the **surgery cost**, the size
of `overlapCoords = {v : v lies in ≥ 2 supports}`: `disjointify_requires_kill` proves any live set on which the
supports are disjoint must have *every* overlap coordinate dead (`Disjoint overlapCoords L`), so
`card_overlapCoords_le_compl` gives `|overlapCoords| ≤ #dead` — disjointifying costs killing `≥ |overlapCoords|`
coordinates.  And `star_overlapCoords_subset_core` proves the star is the small‑`overlapCoords` case
(`overlapCoords ⊆ core`), surgically removable; a spread design with *large* `overlapCoords` is not — killing few
coordinates cannot disjointify, and killing many destroys the live set the cell bridge needs.  **Net: the variance
bound does not see the wall; the explicit quantity that does is `|overlapCoords|`.**  The remaining `NP ⊄ ACC⁰`
difficulty is now pinned to one concrete object — a support family with *small pairwise intersections yet large
`overlapCoords`* (spread overlap) — exactly the Håstad regime, where no small killed set disjointifies.

**Core decomposition — the dichotomy made sharp (`…ACCCoreDecomposition`).**  The star surgery generalizes to
*every* family.  The canonical core is `overlapCoords`, and: `kill_overlap_gives_disjointOnLive` (converse of
`disjointify_requires_kill`) gives the exact characterization `disjointOnLive supports L ↔ Disjoint (overlapCoords)
L` (`disjointOnLive_iff`); `restricted_pairwise_disjoint` proves that after killing the core the restricted family
`supports j ∩ L` is *genuinely pairwise disjoint*; and `core_decomposition_variance` then gives the *clean disjoint
bound* `Var[X] ≤ k·s·p` for the restricted family — **the `k²·λ` overlap term is gone**.  So the surgery cost to
remove all overlap is exactly `|overlapCoords|`, and the dichotomy is sharp: **`|overlapCoords|` small ⇒ cheap to
kill ⇒ reduces to disjoint (`Var ≤ ksp`, the easy case, e.g. the star); `|overlapCoords|` large ⇒ no small kill
disjointifies (the hard spread/Håstad case).**  The entire remaining `NP ⊄ ACC⁰` difficulty is now pinned to one
explicit cardinality — `|overlapCoords|` of the bottom‑gate support family — and the whole easy side of the
dichotomy is proved sorry‑free.

**The restriction tree — recursive depth reduction (`…ACCRestrictionTree`).**  The deterministic descent toward the
depth‑2 bridge is mechanized.  A restriction is a partial assignment `ρ : Fin n → Option Bool` (`Agrees` = input
respects ρ's fixings); restrictions accumulate as a *list* with `AgreesAll` the conjunction, so composing steps is
list concatenation.  The one‑step Håstad step is named `RestrictionTreeSwitch` (every depth‑`≥3` circuit, under some
restriction, equals a strictly‑smaller‑depth circuit on agreeing inputs).  Proved: `reduces_to_depth2` —
**granted one‑step switching, every circuit reduces to a depth‑`≤2` circuit** over an accumulated restriction list
(`AgreesAll ρs x → eval C x = eval C' x`, `depth C' ≤ 2`), by well‑founded recursion on depth (axioms `[propext,
Quot.sound]` only — constructive).  Each step strictly decreases depth, so iteration terminates at `2`; the
depth‑2 survivor then feeds the depth‑2 `MOD`‑bottom bridge and the correlation/pigeonhole machinery.  So the entire
depth‑reduction *structure* — the restriction tree's descent — is mechanized; the lone unproved input is the single
per‑layer switching `RestrictionTreeSwitch`, the Håstad lemma.

**The Williams cash‑out — the whole arc as one conditional (`…ACCWilliamsCashout`).**  The programme is assembled
into a single top‑level theorem in the contrapositive (Williams) shape.  `williams_correlation_cashout` proves: a
small ACC⁰ predictor correlating with the holonomy parity ⇒ (`reduces_to_depth2`) it reduces to a depth‑`≤2`
survivor ⇒ that survivor fails to correlate (`hfail`, the proved support‑extraction + pigeonhole machinery) ⇒ by
transfer of correlation under restriction (`htransfer`), contradiction — hence **no small ACC⁰ predictor correlates
with the holonomy parity**, conditional on the named gaps.  The cash‑out *logic* is fully proved (axioms `[propext,
Quot.sound]`, driven by the proved descent); the load‑bearing inputs are exactly the two `NP ⊄ ACC⁰`‑strength walls
— the per‑layer Håstad switching `RestrictionTreeSwitch` and the subcube‑transfer `htransfer` — plus the proved
survivor‑failure machinery (`hfail`).  So the entire reduction is now **one explicit theorem with the irreducible
difficulty isolated to those named hypotheses**, the honest culmination of the restricted ACC⁰ lower‑bound machine.

**A real switching step, deterministically (`…ACCDepth3Switch`).**  One concrete instance of the
`RestrictionTreeSwitch` wall is now *discharged with no probabilistic hypothesis*, on a depth-3 fragment: a
**CNF of `MOD` gates** (AND of clauses, each clause an OR of `MOD` gates — depth 3 AND/OR/`MOD`).  Proved:
`modGate_eval_eq_of_agreeOn` (a `MOD` gate depends only on its support); `agree_on_fixed` (inputs respecting a
restriction that fixes `S` agree on `S`); `evalClause_true_of_mem` / `evalCNF_cons_of_clause_true` (a true disjunct
satisfies its clause, a satisfied clause drops from the AND); and `switch_step` — **the real switching atom**: if a
restriction fixes a gate `G ∈ c`'s support (bounded fan-in: `≤ s` coordinates) and forces `G` true, then on the
restricted cube `evalCNF (c :: cnf) = evalCNF cnf` — the clause drops.  All constructive (axioms `[propext,
Quot.sound]`).  Iterating it over the clauses reduces the CNF to depth `≤ 2`, feeding the depth-2 bridge.  So the
restriction tree's atom is **no longer assumed** — it is proved for this fragment via support fixing and `MOD`
locality; only the *simultaneous / with-high-probability* control (forcing one gate per clause at once while
keeping coordinates live) remains the named Håstad difficulty.

**Williams' `NEXP ⊄ ACC⁰` scaffold (`…WilliamsNEXP_ACC0`).**  `NEXP ⊄ ACC⁰` is a *theorem* (Williams 2011), so its
algorithmic‑method skeleton is mechanizable with the hard ingredients named.  Proved (pure logic, **no axioms**):
`acc0_sat_speedup_implies_NEXP_not_ACC0` — a nontrivial ACC⁰‑circuit‑SAT algorithm (`ACC0SatSpeedup`) together with
Williams' core implication (`williams`: speedup + `NEXP ⊆ ACC⁰` ⇒ nondeterministic‑hierarchy collapse) and the time
hierarchy (`hierarchy`: no collapse) gives `¬ NEXPHasACC0Circuits`, i.e. `NEXP ⊄ ACC⁰`.  And `nframe_williams_cashout`
— the N‑frame route: *if* the holonomy/correlation structure yields the speedup (`NFrameGivesACC0SatSpeedup` via a
named `bridge`), then `NEXP ⊄ ACC⁰`.  **Honest scope:** this is the *scaffold* of a known theorem — the implication
structure, mechanized — conditional on the two real inputs (`williams`/`ACC0SatSpeedup` = Williams' SAT‑speedup
algorithm; `hierarchy` = the nondeterministic time hierarchy).  The N‑frame supplies the geometric route and the
hard target, but **not** the SAT speedup itself: proving `NFrameGivesACC0SatSpeedup` (N‑frame ⇒ nontrivial
ACC⁰‑SAT) is the genuine open Williams bridge.  It does not prove `NP ⊄ ACC⁰` or `P ≠ NP`; it states exactly which
known inputs close `NEXP ⊄ ACC⁰` and where the N‑frame machinery would attach.

**The N‑frame → ACC⁰‑SAT speedup kernel (`…NFrameACC0Speedup`).**  The structural half of the open Williams bridge
(`NFrameGivesACC0SatSpeedup`) is discharged honestly.  Proved: `sat_iff_image` — a predicate factoring through a
statistic is satisfiable iff some *achieved* statistic value is accepted (search the image, not the domain); and
`sat_depth2_reduces` — **a depth‑2 `MOD`‑bottom circuit's satisfiability reduces to a search over its `weightVec`
image** (via the proved support extraction `eval_factors`).  So SAT over the `2^n` cube collapses to a search over
the cell/residue space of the bottom support family — whose size is the speedup parameter (`≤ 2^{#surviving}` after
a low‑survivor restriction, far below `2^{#live}`).  This is the N‑frame's genuine contribution to the SAT speedup,
powered by the corpus's support‑extraction and cell/survivor analysis.  **Honest scope:** this is the *search‑space
collapse* half only; converting it to a `2^{n − n^ε}` *time bound* (restriction‑tree branching over killed
coordinates + a time model) is the named algorithmic gap — the genuine Williams content.  So
`NFrameGivesACC0SatSpeedup` is *partially* discharged (structural kernel proved; time accounting named), and the
companion boundary/action speedup (`…NFrameSpeedupBridge`: `action < 2^n ⇒` beats brute force) gives the dual,
DP‑based form.  Nothing here proves `NEXP ⊄ ACC⁰`, `NP ⊄ ACC⁰`, or `P ≠ NP`.

**A time-cost model for the ACC⁰‑SAT speedup (`…ACC0SatTimeCost`).**  The structural kernel becomes a genuine
*model-relative time bound*.  In the natural cost model where deciding SAT means enumerating the achievable cells,
the cost is `|image(weightVec)|`, and: `weightVec_le` (each cell coordinate `≤ n`) + `imageSearchCost_le`
(`|image(weightVec)| ≤ (n+1)^k` — the cell vectors live in `(Fin k → {0,…,n})`) + `imageSearch_beats_bruteforce`
(`(n+1)^k < 2^n ⇒ |image| < 2^n`) give `nframe_acc0_sat_timebound` — **a depth‑2 `MOD`‑bottom circuit's SAT is
decided by a cell search of cost `< 2^n` in the small‑gate regime** (`k = o(n/log n)`).  All proved
(`Fintype.piFinset` cardinality + `card_le_card`).  **Honest scope:** this is a *search‑count* model (cost = cells
enumerated, the dominant term), and the regime `(n+1)^k < 2^n` is where the single base‑case search alone beats
brute force, *no branching*.  For larger gate counts one branches over killed coordinates (restriction tree),
giving `2^{#killed} ·` cell‑cost with the survivor count the parameter — that combination is the remaining named
accounting toward the full `2^{n−n^ε}` bound.  So the speedup is now a *real model‑relative time bound* in the
small‑gate regime; it is **not** the full ACC⁰‑SAT theorem, and proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**The branched bound — past the small‑gate regime (`…ACC0SatBranched`).**  The unbranched bound needed few *gates*;
branching needs only few *survivors*.  Branch over a killed coordinate set; on each branch the killed gates are
constant (locality, `eval_const_of_support_disjoint`), so the per‑branch cell search is over the survivor count `r`,
not `k`.  Proved: `branched_cost_le` (branched cost `≤ 2^{#killed}·(n+1)^r`, via `imageSearchCost_le` on the
survivor family); `branched_regime` (the arithmetic: `#killed + #live = n` and `(n+1)^r < 2^{#live}` ⇒
`2^{#killed}·(n+1)^r < 2^n`, via `mul_lt_mul_of_pos_left` + `pow_add`); and `branched_beats_bruteforce` — **the
branched cell search beats brute force in the few‑survivor regime**.  Since the restriction tree / core
decomposition is exactly what makes `r` small, this extends the model‑relative time bound *past* the small‑gate
`(n+1)^k` regime to the few‑survivor regime.  **Honest scope:** still the cell‑search model; the branch enumeration
correctness (SAT = OR over the `2^{#killed}` branches) is the standard branch‑and‑restrict decomposition (the cost
interpretation), and survivor‑constancy per branch is the proved locality.  Not a full Turing‑machine
`2^{n−n^ε}` analysis; proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**A minimal time-complexity model (`…ACC0SatMachine`).**  The abstract cell cost becomes a *real timed algorithm*.
A `TimedDecision` is a result paired with a step count; `cellSearch C` enumerates the achievable cells, checks
`cellPredicate` on each, and accepts if any does, with `steps` = the number of cells examined.  Proved:
`eval_eq_cellPredicate` (the circuit value at `x` is `cellPredicate` at its cell, by `rfl` — support extraction at
the value level); `decideSAT_correct` (**the algorithm decides SAT**: `(cellSearch C).result = true ↔ Satisfiable
C.eval`); `cellSearch_steps_eq_checks` (**`steps` is the operation count** — equals the length of the enumerated cell
list); and `cellSearch_steps_le` / `cellSearch_beats_bruteforce` (**the time bound in the model**: `steps ≤ (n+1)^k`,
and `< 2^n` in the small-gate regime).  So "time" is now the step count of a defined algorithm whose correctness is
proved — closing the gap from "abstract cell cost" to "machine time" *for this cost model*.  **Honest scope:** a
unit‑cost cell‑check model (one step per cell, each check `O(k)`), not a Turing‑machine simulation; `cellSearch` is
`noncomputable` only because the enumeration order is choice‑supplied (irrelevant to result/steps).  Still not the
full `2^{n−n^ε}` ACC⁰‑SAT theorem; proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**An operational step machine (`…ACC0SatStepMachine`).**  The `steps` field becomes the transition count of a real
interpreter.  A `MachineState` is `(acc, todo)`; `step` consumes one cell, OR‑ing its `cellPredicate` into `acc`;
`runFor t` runs `t` steps.  Proved: `foldl_or_eq_any` (OR‑accumulation `= List.any`); `runFor_length` (running
`#todo` steps drains the list and folds the accumulator); `machine_decides` — **the machine computes SAT**: after
`#cells` steps its `acc` is `true` iff the circuit is satisfiable; and `machine_steps_le` / `machine_beats_bruteforce`
— **the machine halts in `≤ (n+1)^k` steps, `< 2^n` in the small‑gate regime**.  So "time" is now the number of
`step` transitions of a defined machine with proved correctness — operational, not a field.  **Honest scope:** a
*list‑processing step machine* (one cell per transition), **not** a Turing machine with tape/head/input encoding,
and no `n^ε` accounting; it grounds machine time for the cell‑search cost model.  The full Turing‑machine
`2^{n-n^ε}` ACC⁰‑SAT analysis remains the named gap (the genuine Williams content).  Proves nothing about
`NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**The faithful elementary‑operation count (`…ACC0SatOpCount`).**  The finest the cell‑search cost model goes:
checking a cell evaluates `k` gate residues, so the true elementary operation count is
`totalOps = k·#cells = k·|image(weightVec)|`.  Proved: `totalOps_eq_gates_mul_steps` (`= k·(cellSearch steps)`);
`totalOps_le` (`≤ k·(n+1)^k`); `totalOps_beats_bruteforce` (`k·(n+1)^k < 2^n ⇒ totalOps < 2^n`).  So *every*
elementary gate‑residue operation is counted and the total is sub‑`2^n` for `k = o(n/log n)`.  This is the endpoint
of the operational‑cost refinement: a real algorithm whose every elementary op is counted and proved below brute
force in the small‑gate regime.  The full Turing‑machine model (tape/head/input encoding + the genuine
`2^{n-n^ε}` ACC⁰‑SAT algorithm) is the remaining Williams content — a research‑grade complexity formalization beyond
this corpus.  Proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**Survivor → cell count: the speedup tied back to the core machinery (`…ACC0SatSurvivorCells`).**  The cell bound is
refined from *all* `k` gates to the **active** (non-empty) gates.  An empty support gives a constant‑`0` `weightVec`
coordinate (`weightVec_eq_zero_of_empty`), so the cell vector is determined by its active coordinates; proved
`image_card_le_active` — **`|image(weightVec)| ≤ (n+1)^{#active}`** (an `InjOn` of the cell image into
`(active → {0..n})` plus `Fintype.piFinset` cardinality), hence `cells_le_active` (`cellSearch` steps
`≤ (n+1)^{#active}`).  After a restriction the killed gates become empty on the live set, so `#active = #surviving`
— the *same* survivor parameter the correlation/restriction machinery (`…ACCSwitchingPipeline`,
`…ACCCoreDecomposition`) controls.  So the SAT‑speedup is now **directly tied back to the survivor machinery**:
*few survivors ⇒ few active gates ⇒ few cells ⇒ fast search* (`(n+1)^{#surviving}`), closing the loop between the
speedup and the core correlation analysis.  A real, completable theorem; still the cell‑search model, not the full
Turing‑machine `2^{n-n^ε}` analysis, and proving nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**Capstone: the survivor‑parameterized speedup (`…ACC0SatSpeedupCapstone`).**  The whole sub‑arc folds into one
statement.  `survivor_sat_speedup` — **if `(n+1)^{#active} < 2^n`, then `cellSearch C` correctly decides SAT *and*
runs in `< 2^n` steps** (correctness `∧` time bound, combining `decideSAT_correct` with `cells_le_active`).  Since
`#active = #surviving` after a restriction, this is exactly *few surviving gates ⇒ a correct SAT decision below
brute force*, driven directly by the survivor machinery (`…ACCSwitchingPipeline`, `…ACCCoreDecomposition`,
`…ACCRestrictionTree`).  This is the headline of the cell‑search speedup, in the parameter the corpus controls — a
real, correct, timed algorithm beating brute force when few gates survive.  Still the cell‑search model, not the
full Turing‑machine `2^{n-n^ε}` theorem; proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**Restriction ⇒ active, branch correctness, and the master bridge (`…ACC0SatRestrictionActive`,
`…ACC0SatBranchCorrect`, `…NFrameACC0Master`).**  Three moves finish assembling the N‑frame → ACC⁰‑SAT speedup.
*(i) Restriction ⇒ active:* after restricting to live set `L`, the restricted family `S_j ∩ L` is active at `j`
iff `S_j` survives, so `activeSupports_restrict_card` gives `#active(S·∩L) = survivingCount`, and
`cells_restrict_le_surviving` gives **`#cells(S·∩L) ≤ (n+1)^{survivingCount}`** — the cell cost is governed by the
*surviving* count the restriction/switching/core‑decomposition machinery controls.  *(ii) Branch correctness:*
`sat_branch_decompose` — `(∃ x, f x = true) ↔ ∃ b, branchSAT f K b`, the proved branch‑and‑restrict decomposition.
*(iii) Master bridge:* `nframe_gives_acc0_sat_speedup` assembles the pipeline into one visible theorem —
**a restriction with `(n+1)^{survivingCount} < 2^n` makes the restricted cell search beat brute force** — with the
single remaining socket `NFrameGivesACC0SatSpeedupSocket` (a restriction leaving few surviving gates), discharged by
`speedup_of_socket`.  So the **entire pipeline is one theorem with the open content isolated to exactly one socket**:
*the restriction/switching/core‑decomposition machinery guarantees few active surviving gates while leaving enough
live variables* — the heart of the ACC⁰ push.  Still the cell‑search model, not the full Turing‑machine
`2^{n-n^ε}` theorem; proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**Attacking the socket from `exists_low_survival` (`…ACC0SatSocketReduction`).**  The master socket
`NFrameGivesACC0SatSpeedupSocket` (`∃ L, (n+1)^{survivingCount} < 2^n`) is now **discharged from the probabilistic
existence theorem**.  `socket_of_expectation`: if the *expected* surviving‑gate count is `≤ r` (with
`(n+1)^r < 2^n`), then by `exists_low_survival` some restriction `L` has `survivingCount L < r+1`, i.e. `≤ r`, so
`(n+1)^{survivingCount L} ≤ (n+1)^r < 2^n` — the socket.  `speedup_of_expectation` chains this to the master, so
**expected survivors `≤ r` (with `(n+1)^r < 2^n`) ⇒ a restriction whose cell search beats brute force**, with no
combinatorial socket left.  This reduces the open content to a single *standard* quantity: the measure expectation
`Exp p (survivingCount) ≤ r`.  By the first moment that expectation is `≤ k·s·p`; the only remaining input is the
routine identification `Exp p (survivingCount) = ∑_j Pr(S_j survives)` (linearity of the discrete expectation over
the support indicators) with `Pr(S_j killed) = (1-p)^{|S_j|}` (the per‑support marginal) — standard facts about the
`p`‑biased measure.  Still the cell‑search model; proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**Collapsing the socket to the bare regime (`…ACC0SatFirstMoment`).**  The expectation hypothesis `hE` is now
**discharged**, by proving the two measure facts in full.  *(i) Per‑support marginal:* `prDisjoint` —
`Pr p (Disjoint S ·) = (1-p)^{|S|}`: the event `Disjoint S L` is `L ⊆ Sᶜ`; factoring
`weight p L = (1-p)^{|S|}·p^{|L|}(1-p)^{|Sᶜ|-|L|}` over `L ⊆ Sᶜ` and summing with `biased_sum_one Sᶜ` gives
`(1-p)^{|S|}`.  *(ii) Linearity:* `exp_sum` — `Exp p (∑_j g_j) = ∑_j Exp p (g_j)`; writing `survivingCount` as a sum
of survival indicators (each `= 1` minus the kill indicator, so `exp_indicator_eq_survProb` gives
`Exp = 1-(1-p)^{|S|} = survProb`) yields **`exp_survivingCount_eq`: `Exp p (survivingCount) = expectedSurviving`**,
and with `expectedSurviving_le` (Bernoulli) **`exp_survivingCount_le`: `Exp p (survivingCount) ≤ k·s·p`**.  Composing
with `socket_of_expectation` gives **`socket_of_regime`**: the socket holds from the *bare arithmetic regime*
`k·s·p ≤ r` and `(n+1)^r < 2^n` — no probabilistic hypothesis remaining — and `speedup_of_regime` chains it to the
master speedup.  This is exactly the *first‑moment* regime (heavy restriction, `p` small); beating it (constant `p`
while collapsing high‑fan‑in gates) is the higher‑moment Håstad content, the genuine `NP ⊄ ACC⁰` frontier.  Still
the cell‑search model; proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**The second moment: variance identification and Chebyshev concentration (`…ACC0SatSecondMoment`).**  The variance
file computed the *abstract* `∑_{j,l} cov ≤ d·k·s·p`; the Chebyshev file gave `Pr((g−Eg)²≥t²) ≤ Exp((g−Eg)²)/t²`.
The missing link — the **identification of the measure variance `Exp((survivingCount−E)²)` with `∑_{j,l} cov`** —
is now proved in full.  Route through the **kill** indicators `kind S L = 1[S∩L=∅]`: the product
`kind A · kind B = 1[Disjoint(A∪B)]` is a *single* disjointness event, so `exp_kind_mul`:
`Exp(kind A·kind B) = (1-p)^{|A∪B|} = killProb(A∪B)` directly via `prDisjoint` (no inclusion–exclusion).  Since
`cov` is *defined* as `killProb(S∪T)−killProb S·killProb T`, expanding `Exp(K²)−(EK)²` (`exp_sub_sq`) for the kill
count gives `kill_variance_eq`: `∑_{j,l} cov = variance` term‑by‑term; and `survivingCount = k−K` (so equal centred
squares) yields **`survivingCount_variance_eq`: `Exp((survivingCount−E)²) = variance p supports`**.  Feeding this and
`variance_boundedOverlap_le` into `chebyshev_of_variance_le` gives **`survivingCount_concentration`:
`Pr(|survivingCount−E|"≥"t) ≤ d·k·s·p/t²`**, and the complement (`pr_compl`) gives **`most_restrictions_good`:
`Pr(within t of mean) ≥ 1 − d·k·s·p/t²`** — at *constant* `p` almost every restriction is good.  **Honest scope:**
this is a high‑probability (robustness) upgrade of first‑moment *existence*, **not** a lower existence threshold —
`min survivingCount ≤ E` always, so existence at `r ≈ k·s·p` was already free; concentration controls the *measure*
of good restrictions, not a smaller witness.  Pushing the threshold past constant `p` needs the **exponential**
switching tail `Pr(survivingCount≥t) ≤ (O(p·s))^t`, beyond the second moment — the genuine `NP ⊄ ACC⁰` content, and
exactly the overlap term `cov_nonneg` exposes.  Still the cell‑search model; proves nothing about `NEXP/NP ⊄ ACC⁰`
or `P ≠ NP`.

**The exponential tail: factorial‑moment method and the independent `(s·p)^t` tail (`…ACC0SatExpTail`).**  Pushing
to the exponential tail `Pr(survivingCount ≥ t) ≤ (…)^t`.  *(i) Factorial‑moment method:* `pr_ge_le_factorial_moment`
— `Pr(t ≤ X) ≤ Exp(C(X,t))` (the binomial coefficient `C(X,t) ≥ 1 ⟺ X ≥ t`, Markov on it).  *(ii) Elementary‑
symmetric identity:* `factorial_moment_eq` — `Exp(C(survivingCount,t)) = ∑_{|T|=t} Pr(all of T survive)` (`C(X,t)`
counts `t`‑subsets of the surviving set), giving `pr_ge_le_sum_jointSurv`: `Pr(t ≤ survivingCount) ≤ ∑_{|T|=t}
Pr(all of T survive)`.  *(iii) Independence (pairwise‑disjoint supports):* `exp_prod_kind_disjoint` — the product of
kill indicators is a *single* disjointness event of the disjoint union (card `∑|S_j|`), so `Exp(∏ kind) = ∏ killProb`;
the multilinear expansion `∏(1−kind) = ∑_{U}∏_U(−kind)` lifts this to **survival independence**
`jointSurv_disjoint_eq_prod`: `Pr(all of T survive) = ∏_{j∈T} survProb`.  *(iv) The tail:* `exp_tail_disjoint` —
**`Pr(t ≤ survivingCount) ≤ C(k,t)·(s·p)^t`** for pairwise‑disjoint supports of fan‑in `≤ s`.  **Honest scope:**
the `(s·p)^t` decay is genuine but (a) **requires independence** (`cov_disjoint`) and (b) carries the **clause‑count
factor `C(k,t)`**.  For *overlapping* supports it is *false*: `cov_nonneg` (positive correlation) gives
`Pr(all survive) ≥ ∏ survProb`, so no `(s·p)^t` bound holds — the precise wall.  Håstad's clause‑count‑free
`(5·p·w)^t` tail evades both defects but is about **decision‑tree depth** via the encoding/canonical‑labelling
argument (the `…Switching*` arc), not gate survival; that clause‑count‑free tail is the genuine `NP ⊄ ACC⁰` content
and remains open.  Still the cell‑search model; proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

**Connecting the switching decoder arc to a probability tail (`…Depth3SwitchingProbTail`).**  The decoder arc already
proves the *weighted* switching bound `tight_descent_switching_prob`: `∑_{Bad} pweight ≤ (2p/(1-p))^s·(2w)^s·∑_{Short}
pweight` — the `(2w)^s` from the proved injective path‑label encoding (`card_pathLabels`, `reconstructionCorrect_
fullpath`), the `(2p/(1-p))^s` from the per‑restriction weight gain.  The missing link to an actual **probability
tail** was that `pweight` is a *distribution*.  `pweight_total` proves it (`∑_ρ pweight p ρ = 1`, via
`Finset.prod_univ_sum` and the per‑coordinate mass `p+(1-p)/2+(1-p)/2=1`), so `∑_{Short} pweight ≤ 1` (`pweight_le_
one`), turning the weighted bound into **`hastad_switching_prob_tail`: `∑_{σ∈Bad} pweight p σ ≤ (4·p·w/(1-p))^s`** —
the `p`‑biased probability that a width‑`w` DNF/CNF needs canonical decision‑tree depth `≥ s` decays as `(O(p·w))^s`
**with NO dependence on the number of clauses**.  `hastad_switching_prob_tail_fullpath` discharges the
reconstruction invariant via the proved `reconstructionCorrect_fullpath`, so the tail is unconditional given the
concrete deepest‑descent hypotheses on `Bad`.  This is exactly the clause‑count‑free decay the gate‑survival tail
(`…ACC0SatExpTail`) provably *could not* achieve (its intrinsic `C(k,t)` factor) — the genuine switching‑lemma
phenomenon, now a probability statement.  **Honest scope:** depth‑3 DNF/CNF model, canonical deepest‑descent tree;
the full `NEXP ⊄ ACC⁰` programme needs this iterated across depth, composed with the MOD‑gate switching step
(`ACCDepth3Switch.switch_step`) and the Williams method — separate work.  Proves nothing about `NEXP/NP ⊄ ACC⁰` or
`P ≠ NP` on its own.

**Depth reduction whp — the complement of the tail (`…Depth3SwitchingDepthReduction`).**  Since `pweight` is a
distribution (`pweight_total`), the switching tail's complement carries the rest of the mass: `switching_depth_
reduction` proves `1 − (4pw/(1-p))^s ≤ ∑_{σ∈Badᶜ} pweight σ`, and `depth_collapse_mass_ge` gives the explicit form —
the restrictions whose canonical decision tree has depth `< s` carry mass `≥ 1 − (4pw/(1-p))^s`.  A depth-`<s`
decision tree is a width-`<s` DNF *and* CNF, so on a `1 − (4pw/(1-p))^s` fraction of restrictions the **width-`w`
DNF depth-collapses to a depth-`≤2` (width-`<s`) circuit** — the single‑layer `depth 3 → depth 2` reduction as a
probability statement.  `switching_depth_reduction_fullpath` discharges reconstruction via
`reconstructionCorrect_fullpath`.  **Honest scope / `switch_step`:** this is the depth reduction *in the DNF model*
(`canonicalDT`, `pweight`); `ACCDepth3Switch.switch_step` is the *deterministic* atom of the sibling **`MOD` model**
(a forced `MOD` gate drops its CNF clause) — the same "a clause collapses" phenomenon in the modular layer.  A
*literal* cross‑model composition needs a bridge identifying `canonicalDT` depth with `MOD`‑gate forcing; that bridge
is **not** done here (not faked).  The full programme iterates this across layers and composes with the `MOD` step and
the Williams method.  Proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP` on its own.

**The cross‑model bridge — and it is a NO‑GO (`…ACCSwitchingModBridge`).**  Building the bridge between the switching
depth reduction (DNF model: collapses by fixing only `~s` of `n` coordinates, leaving the rest *free*) and
`switch_step` (`MOD` model: a clause drops only when its gate is *forced*, which by locality needs the gate's
**entire support fixed**) resolves to the **Razborov–Smolensky vs. switching wall**, formalised.  Algebraic core:
`weightOn_update_add`/`modQStatOn_update` show a support‑coordinate flip shifts the `MOD` statistic by `±1`, so
`modQStatOn_flip_ne` — **the `MOD` statistic is fully sensitive** (`q ≥ 2`): every support‑coordinate flip changes it.
Hence `mod_statistic_live_on_free_cube`: the statistic is non‑constant on *any* restriction cube leaving a support
coordinate free; and for parity `mod_gate_parity_nonconstant`: the **gate value** is non‑constant there, so the gate
is neither forced true nor false and `switch_step` cannot fire.  Contrast: a width‑`w` DNF depth‑collapses with `~s`
coordinates fixed (`depth_collapse_mass_ge`), but a `MOD` gate of support `s` stays live until *all* `s` coordinates
are fixed.  That gap is exactly **why switching reduces `AC⁰` depth but provably does not reduce `MOD`/`ACC⁰` depth**
— the two models do not compose into a switching‑driven `ACC⁰` lower bound (which would contradict the known
hardness).  This is the *correct* mathematical answer to the cross‑model question: genuine `ACC⁰` lower bounds need a
different tool on the `MOD` layer (polynomial approximation / Razborov–Smolensky, the Williams method), not switching.
Proves nothing about `NEXP/NP ⊄ ACC⁰` or `P ≠ NP`.

## 5. The exact missing theorem — where everything converges

Every route terminates at the **same** wall, in five equivalent forms (all `= CookLevinFrontierHyp = P ≠ NP`):

| form | statement | provable half | open half |
|---|---|---|---|
| decision‑holonomy | every correct SAT trajectory has super‑poly decision time | — | the time bound |
| `AdaptiveResidualNonCollapse` | every cheap adaptive decomposition keeps `2^{Ω(n)}` outcomes | one fixed decomposition | the `min` over **all** |
| `DimensionGapHard` | `d_res(SAT) − d_obs ≥ Ω(n)` for *every* poly observer | a fixed observer carries debt | the `min` over **all** |
| global SPDP bridge | every poly‑time observer has poly SPDP rank | restricted `K` | all of `P` |
| Book‑1 CEW (A1) | every poly‑time SAT decider has polylog contextual width | syntactic surrogate; both unweighted invariants (dichotomy); all *fixed*‑order projections (locality no‑go); expander/PAC amplifiers can't rescue fixed order | `ScalingSPDPBridge` = `PolyTimeLowGodelSPDP`: at scaling order, **realized** feature count poly for `P` (not a counting fact) |
| `(Goldreich)InversionHardness` | the family resists fast inversion (OWF / local‑PRG security) | the four classes of §2 | all poly inverters |

In every form the **provable half is geometric/pointwise** (a fixed low‑`d_obs` observer, a fixed class) and the
**open half is universal** (the quantifier over *all* poly observers / *all* cheap decompositions / *all* poly
inverters). The gap theorem `distinguishability_debt_not_time_lower_bound` proves *why* the debt machinery — an
*action/space* bound — cannot by itself cross from the pointwise to the universal: that crossing is exactly
`P ≠ NP`.

**Bottom line.** The optimal predicate is identified and proved optimal; it unconditionally defeats every
restricted inverter class the programme expresses; the binding pair (low‑degree ∧ `AC⁰[p]`) is realized on that
single predicate with no hypothesis; the complementarity that blocked this is resolved; the false routes are
retired. The wall was then pushed across all three axes to its exact ceiling: **space** is settled (boundary
exactly `r`), the **time** axis is proved unreachable by the space machinery, the **decision** axis has an
unconditional (but non‑explicit) lower bound, and the **explicit** residue is isolated as the single `InNP`
conjunct — which the natural‑proofs barrier proves counting cannot supply, leaving the Nečiporuk `n²/log n`
frontier as the strongest explicit, non‑natural bound reachable. The N‑Frame / Book‑1 CEW route was audited to the
same wall — its A1 (bounded contextual width for all `P`) is the lone `P ≠ NP`‑strength field — and a proved
dichotomy shows *no unweighted contextual invariant* can discharge it: additive ones are too small to separate,
multiplicative/rank ones are achieved by easy functions, so only a *time‑structure‑coupled projection* (the
assumed‑not‑derived SPDP bridge) could.  The projected‑rank arc then probed that opening to its end: a proved
locality no‑go (`…AffineIndicatorCollapse`) shows **every *fixed*‑order projection — low‑degree and SPDP alike —
collapses the high‑degree affine/Tseitin residual to polynomial rank**, and expander/PAC amplifiers were proved
unable to rescue fixed order (they amplify *visibility*, not *invisibility*).  The Gödel‑hierarchy tower
(`…GodelHierarchySPDPScaling`) does raise visibility with level — but its feature budget at the scaling level is
*vacuous* (`…GodelFeatureCount`: the ceiling is exponential), so the surviving direction is one named theorem,
`ScalingSPDPBridge` / `PolyTimeLowGodelSPDP`: that poly‑time observers keep a polynomial number of *realized*
features at scaling order — A1 is provably **not a counting fact**, exactly the SPDP‑rank lower bound. The separation is reduced to one named statement, shown equivalent to
`P ≠ NP` in six forms and shown *unreachable* by these methods. Everything reducible is discharged. The one
irreducible step — an explicit, non‑natural decision lower bound above `n²/log n`, equivalently a contextual
invariant low *because of* the poly‑time structure, equivalently universal quantification over all polynomial‑time
machines — is `P ≠ NP` itself, and nothing here claims to take it.
