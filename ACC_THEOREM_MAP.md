# ACC⁰ theorem map — at-a-glance status

Consolidated status of the ACC⁰ lower-bound programme (pall-lean, PathB). Companion to `ACC_ROADMAP.md`
(narrative) and `WHAT_IS_PROVED.md` (full ledger). Updated 2026-06-18.

## Razborov–Smolensky route — frozen dependency graph (entries 264–283)

```
PRIME route (machine-assembled, modulo ⟨binomial tail⟩ + ⟨Codex Circ recursion⟩):

  lowDegreeDim_lt_two_pow ─┐                          (264 counting engine)
  exists_notMem_of_finrank_lt ─┐                      (264 rank kernel)
  mlMon_linearIndependent ──── lowDegreeSubmodule_finrank   (265 multilinear basis)
        │                              │
        └──────────┬───────────────────┘
                   ▼
        smolensky_pigeonhole ───────────────────────► smolensky_prime_goodset_bound   (275 → 278)
                                                              ▲
  fermat_indicator (266) ── smolensky_substitution (276) ── fullProduct_replace_on_goodSet (277)
        │                          │                              │  (+ approximator_times_complement_totalDegree, 279)
        └── modp_native_repr (270) ┴── degree-halving ────────────┘
                   │
        boost_correct_off_iInter (267) + joint_error_le (269) + singleSubsetAgreement_two (268)
                   │  (= the approximation machinery: small AC⁰ ⇒ low-degree, large good set)
                   ▼
        no_small_approximator (278) ──► prime_smolensky_route_closed (279)
                   │                          │
        small_AC0p_observer_implies_lowDegreeApprox (272, bridges Codex Approximable)
                   ▼
        prime_route_to_ACC0Component (278) ──► ACC0CompositeComponent ──► (Williams) ──► NEXP ⊄ ACC⁰

COMPOSITE wall (the one open object):  CarryRefinementCrossing  (238 ≡ 283)
  three natural field-based attacks ALL PROVED TO FAIL (field_based_modes_all_fail, 283):
    • separate  → no common native field        (mod6_layers_cross_fields, 280)
    • stage     → bounded degree < growing req.  (bounded_depth_staged_no_go, 281)
    • carry     → product ring not a field, Fermat fails  (…carry_state_no_traction, 282)
  ⇒ needs a CHARACTERISTIC-INDEPENDENT invariant (open research; not faked)

  FIELD-FREE invariants — genuine attempts at the char-independent obstruction:
   route 1 (carry-state complexity, 284, streaming / Myhill–Nerode):
    • carry_observer_mod_card_ge   : MOD_m needs ≥ m carry states
    • carry_observer_count_card_ge : exact count needs ≥ n+1 states (carry grows with count range)
   route 2 (communication complexity, 285, two-party / fooling-set):
    • commValue_row_injective              : MOD_m comm-matrix has m distinct rows
    • mod_product_observer_boundary_ge     : MOD_m needs ≥ m cross-cut boundary states
    • mod6_boundary_six_achievable         : 6 is tight (identity protocol)
    ⇒ BOTH routes: for FIXED MOD₆ the bound is the constant 6 (= 2·3, achievable: MOD₆ ∈ ACC⁰[6])
       so they separate count/growing-modulus but NOT fixed-MOD₆ composite;
       the separation needs a target whose carry/boundary GROWS under bounded composition (still open)

  TARGET CHANGED TO GROWTH — CRT product (286, the actual frontier):
    • crt_product_observer_boundary_ge       : MOD_(∏ mᵢ) needs boundary ≥ ∏ mᵢ   (grows)
    • crt_product_carry_card_ge              : same in streaming carry model
    • crt_product_observer_boundary_ge_two_pow : mᵢ≥2 ⇒ boundary ≥ 2^k  (EXPONENTIAL in #factors)
    • growing_modulus_exceeds_budget         : no fixed budget < ∏ mᵢ suffices (genuine growth)
    REDUCED TO ONE SOCKET:
      BoundedCompositionKeepsBoundaryBounded (open): does ACC⁰[6]-bounded composition keep boundary ≤ poly?
      composite_ACC0_separation_from_socket (PROVED conditional): socket + boundaryBudget s < ∏ mᵢ
        ⇒ MOD_(∏ mᵢ) ∉ size-s ACC⁰[6].  Proving the socket (poly budget) = the composite separation.

  SOCKET ATTACKED DIRECTLY ⇒ FALSE (287, proved no-go):
    • equality eq(a,b)=[a=b] ∈ AC⁰⊆ACC⁰[6] (depth-2 AND-of-XNOR) has IDENTITY comm-matrix
      ⇒ 2^k distinct rows ⇒ equality_boundary_two_pow: boundary ≥ 2^k (EXPONENTIAL)
    • equality_refutes_boundary_bounded: any budget bounding all realizable f is ≥ #A = 2^k
      ⇒ BoundedCompositionKeepsBoundaryBounded is FALSE for poly budget; the boundary route is DEAD
    FINDING: comm boundary ANTI-tracks ACC⁰[6]-hardness — MOD_M comm-EASY (boundary M, O(log M)),
      equality/disjointness comm-HARD (2^k), BOTH ∈ AC⁰[6]. Separating resource is NOT boundary size
      but the characteristic/CRT structure (280-283), which raw communication cannot see.

  CHARACTERISTIC-COUPLED (TYPED) INVARIANT — survives the equality counterexample (288):
    CrossCharacteristic m S := ∃ p prime, p∣m ∧ p∉S    (keyed to prime factors, NOT boundary size)
    classification (all PROVED):
      • equality_not_count_predicate : equality carries NO modulus ⇒ NEVER flagged (beats 287)
      • mod5_cross_acc6 / mod30_cross_acc6 : 5∉{2,3} ⇒ MOD_5, MOD_30 obstructed for ACC⁰[6]
      • mod6_not_cross_acc6 : factors {2,3}⊆{2,3} ⇒ MOD₆ NOT flagged (correctly: MOD₆∈ACC⁰[6])
      • mod6_cross_single_field : ∀ prime p, MOD₆ cross {p}  (carry crossing over a single field)
    TypedCarryRefinementCrossing Computes S := ∀ m, CrossCharacteristic m S → ¬ Computes S m
      typed_crossing_excludes_mod5 (PROVED cond.): ⇒ MOD_5 ∉ ACC⁰[6]
      typed_separation_chain (PROVED cond.): + component socket ⇒ composite-ACC⁰ ingredient → Williams
    DEEP socket (open, separation-strength): "cross-characteristic ⇒ ¬ACC⁰[6]-computable" = Smolensky-composite
      (single-prime instance MOD_q∉AC⁰[p] already PROVED = Layer4.mod_q_indicators_false; composite lift open)

  PROBE — does the composite lift REDUCE to the single-prime no-go? ANSWER: NO (289, proved):
    NativelyLinearizes F p := CharP F p   (Fermat indicator native ⇔ char p)
    SingleFieldReductionCovers F S := ∀ p∈S, NativelyLinearizes F p
    • single_prime_coverable : each {p} coverable (ZMod p) ⇒ single-prime no-go gives MOD_5∉AC⁰[2], ∉AC⁰[3]
    • acc6_reduction_needs_char_2_and_3 : covering {2,3} forces CharP F 2 ∧ CharP F 3
    • no_single_field_covers_acc6 : ¬∃ such field (no_common_char) ⇒ AC⁰[{2,3}] NOT single-field-reducible
    • composite_lift_not_single_field_reducible : sub-classes coverable BUT joint class is not
    VERDICT: single-prime method works prime-by-prime, provably CANNOT combine at the joint class;
      obstruction = exactly no_common_char (280). Composite barrier GENUINE, not a missing reduction.

  UNIVERSAL CHARACTERISTIC OBSTRUCTION — no nontrivial RING is native to two coprime moduli (300, proved):
    • two_three_native_trivial : [CommRing R] (2:R)=0 → (3:R)=0 → (1:R)=0   (linear_combination, 1 = 3-2)
    • no_nontrivial_ring_both_native : [Nontrivial] ⇒ False  — subsumes field(280)+product-ring(282)+tensor collapse
    • coprime_native_trivial : coprime p q → (p:R)=0 → (q:R)=0 → (1:R)=0  (Bézout u·p+v·q=1 cast into R)
    • field_both_native_absurd : subsumes no_common_char
    ⇒ THE algebraic root: Fermat-native polynomial method needs p=0 to low-degree-represent MOD_p; NO nontrivial ring
      has two coprime moduli =0, so every native polynomial attack (field/product/tensor) fails for one reason 1=3-2=0.
      Counting escape (290) avoids it via ℤ [char 0, no p=0]. SHARPENS open frontier: a polynomial-method separation
      must use a NON-native (non-Fermat, not p=0) representation. Composite barrier still OPEN; this pins its root.

  SEMIRING OBSTRUCTION — the native escape fails even WITHOUT subtraction (312, proved):
    • two_three_semiring_trivial : [AddCommMonoidWithOne R] (2:R)=0 → (3:R)=0 → (1:R)=0  (3=2+1, 2=0 ⟹ 3=1; NO subtraction)
    • no_nontrivial_semiring_both_native : [Semiring][Nontrivial] ⇒ False
    ⇒ extends entry-300 (CommRing, used 1=3-2) to ALL AddCommMonoidWithOne — semirings, tropical/idempotent, any +,0,1.
      KILLS NFRAME_TWO_ROUTES §4 candidate 4 (semiring/non-ring native observers). Obstruction needs only additive
      numeral structure. Open candidates remaining: non-native (1), staged-no-flatten (2), probabilistic/approximate (3).

  PIVOT — why the ALGORITHMIC route escapes the barrier (290, proved):
    counting = integer count c∈ℤ (characteristic 0), reads MOD_m off ONE integer for EVERY m
    • count_crt_mod6 : c%6=0 ↔ (c%2=0 ∧ c%3=0)   — one integer carries char-2 AND char-3 at once
    • integer_carries_every_characteristic : ℤ →+* ZMod m for all m (ℤ initial, char 0)
    • count_route_covers : ¬ CrossCharacteristic m m.primeFactors — count route NEVER char-blocked
    • no_field_carries_two_primes : but no FIELD has two prime chars (no_common_char) — the contrast
    ⇒ counting (char 0, carries all primes) SIDESTEPS the field barrier that kills polynomials (280-289)
      = structural reason Williams went ALGORITHMIC. Deep #SAT⇒LB machinery stays the existing socket.

  DUG INTO WilliamsFastSat socket — the counting step is CHARACTERISTIC-UNIVERSAL (291, proved):
    fast-SAT observable = gateCount (an INTEGER); decision searches image(gateCount g), indep of the top h
    • fastSat_universal_in_top : ONE count-cell image decides symEval g h for EVERY h (observed_sat_iff)
    • fastSat_decides_every_modulus : ⇒ decides MOD_M-SAT for EVERY M (∃ c∈image, c%M=0), same image
    • fastSat_mod6_via_crt : MOD₆ decision factors through c%2=0 ∧ c%3=0 (290's CRT, inside fast-SAT)
    • fastSat_cells_modulus_free : cell bound ≤ m+1 indep of modulus
    • fastSat_modm_savings : Williams savings 2^k·cells ≤ 2^n hold for EVERY M (symAnd_williams_fastSat at MOD_M)
    ⇒ composite-MOD barrier is NOT in counting/savings (modulus-agnostic, exonerated) — it lives in the
      YBT exact-normal-form socket (link 1) + uniform TM realization (link 2).

  IS THE YBT SOCKET SEPARATION-STRENGTH? ANSWER: NO (292, proved — honest correction):
    • exact_symAnd_form_exists : EVERY ACC⁰ circuit has an exact SYM∘AND form UNCONDITIONALLY (acc0circuit_hasSymAndForm)
    • mod_form_characteristic_free : MOD_q form exists for EVERY q (counting-based modQStatOn, composite no barrier — 290)
    • ybt_socket_is_size_bound : HasExactSymAndForm = just symAndSize C + 1 < 2^n firing (quasipoly SIZE = Beigel–Tarui)
    • ybt_socket_yields_cell_search : socket ⇒ only a < 2^n CELL search, NOT a uniform algorithm
    • realization_is_separation_strength / williams_is_separation_strength : the DOWNSTREAM sockets are ↔ NEXP⊄ACC⁰
    VERDICT: YBT is NOT separation-strength — it's a TRUE classical theorem (existence proved; quasipoly size = hard
      combinatorics, not a hardness assumption). The separation-strength lives in realization + Williams (links 2,3).
      Composite-MOD never even blocks YBT EXISTENCE (counting/char-free); only the SIZE is the open Beigel–Tarui work.

  PROBED the REALIZATION socket — it adds NO new separation-strength obstacle; route has ONE deep gate (293, proved):
    realization already split S1-S4 (WilliamsRealizationSplit): S1 encoding [routine], S2 cost-bridge [arith proved],
      S3 uniformity [standard], S4 time-hierarchy [separation-strength, ↔ NEXP⊄ACC⁰ self-audited].
    • cost_bridge_unlocked_by_quasipoly_ybt (NEW proved): quasipoly cell bound (D+1)·n^D+1 ≤ 2^{n-k} ⇒ 2^k·cells ≤ 2^n
      (super-poly savings) — the cost bridge bites EXACTLY when the YBT quasipoly-size wall (292) is crossed; the link
      proved_speedup_is_trivial_savings only gestured at (k=0), now proved for k=Ω(n).
    • williams_route_single_separation_gate: S4 and the Williams socket are the SAME shape (X→sep), each ↔ sep ⇒ ONE gate
    • williams_route_socket_classification: full-route map (YBT exists uncond + cost-bridge unlocked + reduce to S4 + S4↔sep)
    VERDICT: entire algorithmic route (YBT+realization+Williams) has ONE irreducible separation-strength gate = the
      nondeterministic-time-hierarchy / Williams argument; all else proved / true-classical (YBT size) / routine.

  ATTACKED that gate directly — the NTIME hierarchy's diagonalization logic is FULLY PROVED (294):
    NB: NEXP⊄ACC⁰ is a PROVEN theorem (Williams 2011) — the gate is its FORMALIZATION, not an open problem.
    • Cantor escape (200): diag_not_mem_range [naive direct complement — NOT valid for nondet classes]
    • lazy telescoping CONTRADICTION (219): lazy_diag_false [complement-free, single boundary flip]
    • NEXP PLACEMENT (NTIMEAccounting): clocked_in_NEXP, lazy_diagonal_in_nexp
    • lazy ENUMERATION ESCAPE (294, NEW): lazy_diag_not_mem_range = complement-free upgrade of 200, the form
      that applies to nondeterministic classes (built on lazy_diag_false; ∀ i D ≠ enum i ⇒ D ∉ range enum)
    ⇒ diagonalization logic of the hierarchy COMPLETE (contradiction + placement + escape all proved).
    REMAINING = the REALIZATION primitive (ClockedSimulation: clocked NTM decides D in bigger bound via universal
      sim) + IKW easy-witness + NW guess-verify — all PROVEN classical facts (formalization, not open obstruction).

  WILLIAMS INTERNALIZED as the N-Frame algorithmic-counting branch (295, proved):
    NFrameWilliamsRoute = ∀ ACC⁰ circuit, ∃ SYM∘AND form (m+1 < 2^n):
      observer = integer gateCount | boundary = count-cell image | compression = <2^n cells | escape = char-0 CRT univ
    • nframe_williams_route_equiv : NFrameWilliamsRoute ↔ WilliamsFastSatRoute  (Iff.rfl — the route IS fast-SAT, defeq)
    • nframe_observer_characteristic_free : integer count decides MOD_M-SAT for EVERY M (the char-0 escape, 290/291)
    • nframe_fastSat_to_timeHierarchy (PROVED cond.): route → speedup → (easy-witness/NW collapse + NTIME hierarchy)
        ⇒ ¬(NEXP ⊆ ACC⁰), via williams_concrete (composition proved; deep ingredients = named classical sockets)
    • nframe_hierarchy_diag_core : the hierarchy subpiece's diagonalization core PROVED (294 lazy escape)
    ⇒ Williams is now the N-Frame algorithmic-counting ROUTE (not external commentary); gate honestly socketed to the
      PROVEN classical theorems (uniform realization, easy-witness/NW, NTIME hierarchy) — Williams 2011, being formalized.

  FORMALIZED the universal NTM simulation socket — overhead is EXACTLY 1 (296, proved):
    already present: ACC0UniversalNTM.univStep ⟨M⟩ = concreteStep M (interpreter, exact at relation level);
      enum_covers PROVED (enumerability); hierarchy reduced to one socket diag_in_big (physical sim within bigger bound).
    NEW (ACC0UniversalSimulationOverhead): univNTM code (universal NTM at a code, step = univStep code):
      • univNTM_step_iff : at ⟨M⟩, univNTM's step = M's step
      • univNTM_reachIn : k-step reachability of univNTM ⟨M⟩ = M's, ∀ k  (no per-step overhead, induction)
      • univ_simulates_exactly : acceptsWithin (univNTM ⟨M⟩) x t ↔ acceptsWithin (toNTM M) x t  (SAME t, overhead = 1)
    ⇒ the socket's feared "physical overhead" is EXACTLY 1 (free, no time blowup); what remains = ONLY the
      decode-from-input uniformity (collapse univNTM family to one fixed tape-reading machine = the universal TM,
      Turing 1936) — a PROVEN classical fact, formalization not obstruction.

  DECODE-FROM-INPUT UNIFORMITY — one fixed machine simulates EVERY machine (297, proved):
    UConfig := ℕ × CConfig (code carried as config DATA); uStep (preserves code, applies univStep code);
    uniformU dec = ONE fixed NTM (only param = M-independent parse dec), decodes code at init, accepts on state.
    • uStep_preserves_code : decoded code preserved along every run (induction)
    • uStep_reachIn : reachIn (uniformU dec) k (code,c)(code,c') ↔ reachIn (univNTM code) k c c'  (uniform, overhead 1)
    • uniformU_simulates : dec w = (⟨M⟩,x) ⇒ acceptsWithin (uniformU dec) w t ↔ acceptsWithin (toNTM M) x t (via 296)
    • decPair_encPair : concrete encoding round-trips (replicate-unary code + false sep + input; takeWhile/drop inductions)
    • uniformU_simulates_enc : SELF-CONTAINED — uniformU decPair on encPair ⟨M⟩ x simulates M exactly
    ⇒ universal NTM simulation FULLY FORMALIZED (one uniform machine + overhead 1) = the universal TM (Turing 1936)
      over the transition-table model. Remaining for full hierarchy: feed the diagonal its own code + clock to bigger
      bound + per-x block bookkeeping (wiring) — plus IKW + NW (the other two gate sockets). All proven-classical.

  REALIZATION WIRING — clock the universal machine to the bigger bound (298, proved):
    • universal_clocked_within : univNTM ⟨M⟩ accepts x within any t' ≥ t (296 overhead-1 + acceptsWithin_mono)
    • uniformU_clocked_within : SAME for the single machine uniformU decPair on encoded input (297 + mono)
      ⇒ a g-time machine fits any f ≥ g: "clock to the bigger bound" is FREE given overhead 1
    • cNTIME_of_decider / diag_in_big_of_clocked_decider : diag_in_big ⇐ ONE socket = a TMachine deciding the
      (lazy) diagonal within f. Clocking proves the time bound achievable; remaining = BUILDING that decider.
    ⇒ realization wiring's quantitative core PROVED; remaining = the lazy-diagonal decider machine (per-block
      bookkeeping + boundary complement, compiled over the universal machine — physical, parallel to UniversalHStep).

  LAZY-DIAGONAL DECIDER — boundary complement realized: bounded acceptance is DECIDABLE (299, proved):
    concreteStep M has FINITE branching (M a finite rule list) ⇒ reachable configs are finitely enumerable:
    • succs / mem_succs : d ∈ succs M c ↔ concreteStep M c d (finite one-step successors)
    • reachExactly / mem_reachExactly : d ∈ reachExactly M k c ↔ reachIn (toNTM M) k c d (finite k-step reach)
    • acceptsWithin_iff_decAccept : acceptsWithin = decAccept (a bounded finite search)
    • instDecidableAcceptsWithin : Decidable (acceptsWithin (toNTM M) x t)
    • boundary_complement_decidable : Decidable (¬ acceptsWithin ...) ⇒ the boundary complement is COMPUTABLE
    ⇒ the crux of WHY lazy diagonalization's single boundary complement is affordable (exhaustive search of the small
      bounded computation) — machine-checked. Remaining for the full decider: per-block bookkeeping assembly
      (decode index, route copy[=universal sim 296/297]-vs-boundary[=this], + clocking 298) — physical engineering.

  LAZY-DIAGONAL CONSTRUCTION — concrete block bookkeeping, unconditional escape (302, proved):
    lazyDiagLang enum n := if n%2=0 then enum (n/2) (n+1) else !enum (n/2) (n-1)  [block i={2i,2i+1}, len 1]
    • lazyDiagLang_escapes : ∉ Set.range enum UNCONDITIONALLY (294's escape, block layout discharged by omega)
    ⇒ the structural skeleton of the lazy-diagonal decider: total language tiling ℕ into copy(even)/boundary(odd)
      positions per index, escaping every enumeration. Remaining = the decider MACHINE (compute lazyDiagLang within f:
      copy via universal sim 296/297, boundary via decidable complement 299, clocked 298) — physical engineering.

  UNIVERSAL RUN — multi-step hstep faithfulness, encoded machine tracks full computation (304, proved):
    uEncStep s s' := ∃ M c t, decodeSim s = some(M,c) ∧ t∈M ∧ t.1=(c.1,readSym c) ∧ s'=encodeSim M (applyTrans c t)
      [U's logical step: decode→lookup firing rule→apply→re-encode]; uEncNTM = the encoded universal NTM.
    • uEncStep_of_concreteStep : one M-step ⇒ one encoded universal step
    • uEncNTM_tracks : reachIn (toNTM M) k c c' ⇒ reachIn uEncNTM k (encodeSim M c)(encodeSim M c') — FULL run, induction
    ⇒ lifts UniversalHStep's SINGLE-step hstep contract to the MULTI-step run: the encoded universal machine
      faithfully simulates M's entire k-step computation. Closes the LOGICAL side of the compile socket; remaining =
      physical realisation of uEncStep as concrete TMachine rules with per-step overhead B (decode/lookup/rewrite sub-machines).

  UNIVERSAL OVERHEAD — the per-step bound B is poly (305, proved):
    stepOverhead mLen tapeBound := mLen + tapeBound + 1  [scan + region + O(1)]
    • head_bound_from_init : reachIn (toNTM M) k (0,0,x) c ⇒ c.2.1 ≤ k (bounded head region, from reachIn_head_le)
    • uStep_cost_bound : one step's footprint (matchingRules.length + (head+1)) ≤ stepOverhead M.length (|x|+k)
      [from matchingRules_length_le (scan ≤ M.length) + head bound (≤k)]
    ⇒ the concrete per-step overhead B = poly(M.length, |x|+k) on one universal step's RESOURCES, from the proved
      RuleLookup + HeadLocation contracts. Remaining = U's rules executing one resource-op per primitive step
      (the interpreter loop) — classical engineering. So compile socket = LOGICAL [304] + RESOURCE-B [305] DONE;
      only the U-rule-table-executes-one-op-per-step realisation remains.

  PHYSICAL UNIVERSAL INTERPRETER LOOP — phases assembled + multi-step cost k·B (308, proved):
    • physical_uStep_phases : one universal step via the 4 PROVED phases — decode (decodeSim_encodeSim) + lookup
      (matching rule fires, scan ≤ M.length, lookup_phase) + apply (applyTrans) + re-encode (round-trip).
    • physical_tracks_lift : reachIn uEncNTM k s t ⇒ Realizes (enc s)(enc t)(k·B), given perStep (one uEncStep in B) +
      compose (additive) + refl0; induction on k accumulating B. = physicalU_tracks_uEncNTM (C=0).
    ⇒ the interpreter loop ASSEMBLED + the multi-step cost lift k·B PROVED. Last two physical primitives isolated as
      hypotheses: perStep [one uEncStep = B=stepOverhead primitive steps, the decode→lookup→apply→encode loop, 305] +
      compose/refl0 [U's reachability composes additively, standard]. Classical TM engineering, not open.

  PHYSICAL UNIVERSAL MACHINE — perStep DISCHARGED by an explicit phased controller (309, proved):
    PUConfig (tape s | dec M c | loc M c t | app M d); physStep advances one phase per step, each move = a phase contract:
      tape s →[decodeSim s=some(M,c)] dec M c →[t∈matchingRules] loc M c t →[d=applyTrans c t] app M d →[u=encodeSim M d] tape u
    • perStep_phys : uEncStep s u → reachIn physU 4 (tape s)(tape u) — the 4-phase trace, from the proved contracts
    • physU_tracks : reachIn uEncNTM k s t → reachIn physU (k*4)(tape s)(tape t) — via physical_tracks_lift with physU,
      perStep_phys + reachIn_add (compose), NO REMAINING HYPOTHESES.
    ⇒ entry-308's perStep/compose/refl0 hypotheses DISCHARGED with a concrete machine. Transition-table compile now:
      logical[304]+resource-B[305]+phase-assembly[308]+lift[308]+perStep-discharge[309] ALL PROVED. physU at phase
      granularity (B=4 macro-steps), per-phase time = stepOverhead [305]; only refinement left = phase→primitive-cell-ops
      (phase-internal detail). The U-rule-table loop is GROUND OUT to a concrete machine with proved step/time bounds.

  PHASE-INTERNAL CELL-OP REFINEMENT — lookup phase as primitive single-rule steps (310, proved):
    scanStep (one rule per step: pop head, append iff matches); scanNTM.
    • scan_reaches : from (rules, acc), in rules.length steps reach ([], acc ++ rules.filter p) (induction on rules)
    • scan_phase_celled : from (M,[]), in EXACTLY M.length primitive single-rule comparisons, reach
      ([], matchingRules M state sym) — the lookup macro-step expanded to primitive ops, exact count = the 305 bound.
    ⇒ the representative phase macro-step (lookup) is refined to primitive cell-ops with exact step count M.length.

  ALL FOUR PHASES CELL-REFINED — decode/re-encode (tape walk) + apply (O(1)) (311, proved):
    • walk_reaches : generic tape-cell traversal — from (cells, acc) in cells.length steps reach ([], cells.foldl g acc)
      = the decode (read) / re-encode (write) cell-cost model (|tape| primitive steps; correctness = decodeSim_encodeSim).
    • apply_phase_celled : reachIn (applyNTM t) 3 (0,c)(3, applyTrans c t) — apply = 3 primitive field-updates
      (write symbol → move head → set state), composing exactly to applyTrans.
    ⇒ ALL FOUR phases now cell-refined to EXACT primitive step counts: decode |tape|, lookup M.length [310], apply 3,
      re-encode |tape| — together the per-uEncStep stepOverhead [305]. The transition-table compile is ground from
      logical tracking [304] ALL THE WAY DOWN to primitive single-operation steps. Pure mechanical bookkeeping.

  NW GUESS-VERIFY (target 2, Williams link 3) — verify inside the fast-SAT framework (306, proved):
    already proved: gvDecider_eq (correctness), guess_verify_within (two-phase time), guessVerify_subset (collapse).
    • nw_guess_verify_within_fast : guess small (guessT ≤ 2^{n-k}) + verify via fast-SAT (verifyT ≤ 2^{n-k})
        ⇒ decider accepts within 2·2^{n-k} = 2ⁿ/superpoly (via guess_verify_within composition)
    • verify_fastSat_savings : verify work ≤ 2^{n-k} ⇒ 2^k·work ≤ 2ⁿ (the entry-291 speedup margin)
    ⇒ NW's verify phase CONNECTED to the fast-SAT: a guessed small ACC⁰ witness is verified by the ACC⁰-SAT speedup
      in ≤ 2^{n-k}, so the guess-verify decider runs in 2ⁿ/superpoly. Socket reduced to the two phase-realisation
      facts: guess poly [IKW small-witness = target 3] + verify ACC⁰-SAT-searchable [291, YBT] — proven-classical.

  WORKSTREAM A — KARP–LIPTON MA INCLUSION assembled (313, proved): NEXP⊆ACC⁰→NEXP⊆MA
    • karp_lipton_NEXP_ACC_to_MA : NexpEqMIP (NEXP=MIP) + MIPRealizedGuessable (IKW small prover) ⇒ NEXP⊆ACC⁰→NEXP⊆MA
      (= nexpSubsetMA_via_MIP ∘ mipSubsetMA_of_realized; Merlin sends prover circuit, Arthur verifies = mipLang_eq_maLang).
    • hardFnSeparation_via_karpLipton : DISCHARGES entry-307's karpLipton hypothesis with the above.
    ⇒ removes the Karp–Lipton piece of caveat 1: NEXP⊆ACC⁰→NEXP⊆MA is no longer a monolithic socket — reduced to two
      NAMED classical residues [NexpEqMIP = BFL NEXP=MIP, sum-check engine proved 226-227; MIPRealizedGuessable = IKW
      small prover, collapse mechanism mipLang_eq_maLang proved], glue + Merlin/Arthur mechanism PROVED.

  WORKSTREAM A step 2 — NW-DERANDOMISATION HardFunction→MA⊆NP assembled (314, proved):
    • nw_hardFn_MA_subset_NP : (HardFunction→PRGExists)[NW hardness-to-PRG] + PRGCollapsesMAtoNP[seed enum] ⇒
      HardFunction→MA⊆NP (= derandMASubsetNP_via_PRG at Derand:=HardFunction).
    • hardFnSeparation_fully_assembled : HardFnSeparation from PURELY named residues — NexpEqMIP + MIPRealizedGuessable
      (Karp-Lipton 313) + (HardFunction→PRGExists) + PRGCollapsesMAtoNP (derand) + NP⊆MA⊆NEXP + NEXP≠NP[200 proved].
    ⇒ BOTH entry-307 hypotheses (karpLipton, derand) DISCHARGED. IKW chain now rests on purely named proven-classical
      residues. Caveat-1 pieces done: Karp-Lipton [313] + NW-derand MA⊆NP [314]; remaining = PRG-fooling internals
      (DerandGivesPRG/PRGCollapsesMAtoNP + NexpEqMIP/MIPRealizedGuessable arithmetization) = workstream-A step 3.

  WORKSTREAM A step 3 — PRG-COLLAPSE SEED-ENUMERATION mechanism (315, proved):
    • seedAccept V seeds x w := seeds.length < 2·#{r∈seeds | V x w r} [finite seed-majority NP verifier], DECIDABLE
      (instDecidableSeedAccept), count bounded (seed_count_le). Sub-bricks 1+4.
    • PRGFools probAccept V seeds := ∀ x w, probAccept x w ↔ seedAccept ... [completeness+soundness transfer]. Sub-bricks 2+3.
    • maLang_eq_npLang : PRGFools ⇒ MALang probAccept = NPLang V seeds (collapse mechanism, exists_congr).
    • prgCollapsesMAtoNP_of_realizes : PRGRealizesCollapse ⇒ PRGCollapsesMAtoNP.
    ⇒ opens the PRGCollapsesMAtoNP residue: seed-enumeration mechanism PROVED + decidable seed verifier; reduces it to
      the genuine pseudorandomness residue PRGFools. Next: HardFunction→PRGExists (the NW/IW PRG construction).

  WORKSTREAM A step 3 (FINAL residue) — HardFunction→PRGExists: the NW reconstruction ASSEMBLED end-to-end (316, proved):
    222 proved the reconstruction-contradiction glue (prgFools_of_hard : NWReconstruction D S → ¬S → ¬D), leaving the
    ABSTRACT NWReconstruction socket. 190–196 separately proved the CONCRETE sub-pieces, each discharging its OWN socket
    in isolation; they were never chained, and the chain does NOT close on types (Yao outputs a probability-level
    predictor; recon consumes a predictor-computes hyp & outputs size≤bound; hardness consumes a concrete Circ).
    THIS entry chains them through nw_hybrid_no_distinguisher:
      GlobalAdvantage --[hybrid_advantage PROVED]--> AdjacentAdvantage --[yaoPredictor_discharge PROVED]-->
      YaoNextBitPredictor --[bridgeYao = YaoCircuitEfficiency]--> ComputesF
      --[reconstruction_socket_discharge PROVED + ReconDesign]--> SmallCircuitForFAt(size≤bound)
      --[bridgeRecon = ReconstructionCorrectness]--> HasCircuitOfSize fbool s.
    • reconstruction_rebridge : pre/post-compose Reconstruction P D C with pre:P'→P, post:C→C' ⇒ Reconstruction P' D C'
      (the type-level glue matching Yao-output→recon-input and recon-output→hardness-input).
    • nwReconstruction_assembled : discharges NWReconstruction (GlobalAdvantage) (HasCircuitOfSize fbool s) from the
      proved sub-pieces + the two bridges + the design.
    • hardFn_to_prgExists_assembled : HardFor fbool s → ¬GlobalAdvantage f m ε  (= HardFunction→PRGExists), via
      prgFools_of_hard ∘ nwReconstruction_assembled.
    ⇒ HardFunction→PRGExists is no longer monolithic: ANALYTIC CORE assembled (hybrid telescoping, Yao identity, poly-size
      accounting, hardness collision all PROVED & chained); remaining = two NAMED model-dependent circuit-realisation
      bridges (bridgeYao=YaoCircuitEfficiency, bridgeRecon=ReconstructionCorrectness) + irreducible hardness HardFor +
      proved low-intersection design. Caveat-1 step 3 complete at the assembly level. NOT NEXP⊄ACC⁰, NOT P≠NP.

  IKW EASY-WITNESS (target 3, the deepest) — sub-socket 2 decomposed (307, proved):
    IKW EasyWitnessLemma ⟸ (1) NoEasyWitnessHardFn [DISCHARGED 150, the witness w IS the hard fn] ∧ (2) HardFnSeparation.
    • hardFnSeparation_from_parts : HardFnSeparation ⟸ KarpLipton(NEXP⊆ACC⁰→NEXP⊆MA) + derand(HardFn→MA⊆NP)
        + NP⊆MA⊆NEXP + NEXP≠NP[200 PROVED]: chain ⇒ NEXP⊆NP ∧ NP⊆NEXP ⇒ NEXP=NP, contra NEXP≠NP (antisymm)
    • ikw_from_parts : NoEasyWitnessHardFn + decomposition ⇒ full EasyWitnessLemma
    ⇒ IKW now rests on exactly TWO classical sub-sockets: Karp–Lipton circuit-collapse + NW-derandomisation MA⊆NP,
      both decomposed elsewhere [199/202/221/222/229]; with NEXP≠NP PROVED [200] and NoEasyWitnessHardFn discharged [150].

  LAZY-DECIDER ASSEMBLY — complement-free hierarchy, ONE socket left (303, proved):
    • lazy_diag_not_in_smaller : Smaller ⊆ range enum ⇒ lazyDiagLang enum ∉ Smaller (from 302 escape)
    • lazy_time_hierarchy : henum + hbig(lazyDiagLang ∈ Bigger) ⇒ ¬(Bigger ⊆ Smaller) — the NTIME hierarchy on the
      LAZY (one-boundary-complement) escape, HONEST for nondet classes (no co-nondeterminism, unlike Cantor version)
    ⇒ the lazy-diagonal hierarchy implication is ASSEMBLED; the ONE remaining socket (lazyDiagLang ∈ Bigger = the lazy
      diagonal decided in the bigger bound) is supported by copy[universal sim 296/297, clocked 298] + boundary[decidable
      complement 299]; lacks ONLY the transition-table TMachine compilation of the routing (UniversalHStep line) —
      proven-classical physical engineering, NOT an open obstruction.
```

## Tier 1 — AC⁰ (switching method): **DONE & FENCED**

| result | theorem | file |
|---|---|---|
| clause-count-free switching tail | `hastad_switching_prob_tail` (`∑_Bad pweight ≤ (4pw/(1-p))^s`) | `…Depth3SwitchingProbTail` |
| depth collapse whp | `depth_collapse_mass_ge` (width-`w` DNF → depth ≤ 2 on `1-(4pw/(1-p))^s`) | `…Depth3SwitchingDepthReduction` |
| **switching cannot cross MOD** | `mod_gate_parity_nonconstant` (MOD gate non-constant on any free-support cube) | `…ACCSwitchingModBridge` |

⇒ Switching kills AC⁰ depth; provably does **not** reduce MOD/ACC⁰ depth.

## Tier 2 — AC⁰[p] (polynomial / Razborov–Smolensky, `p` prime): **DONE & FENCED**

| result | theorem | file |
|---|---|---|
| PARITY ∉ AC⁰[p] | `parity_function_lower_bound` (`2^{Ω(n^{1/2d})}`) | `…Layer3Smolensky` |
| N-frame target = parity | `fParity_univ_eq_parity`, `nframe_parity_target_size_lower_bound` | `…Layer3NFrameParityRS` |
| socket 1: target escapes `V_D` | `holonomy_parity_not_lowDegEval` | `…Layer3LowDegHolonomy` |
| socket 2: AC⁰[p] ⊆ low-rank | `acc0_approx_by_lowRankPredictor`, `eval_mem_lowDegSpan` | `…Layer3ACC0LowRank` |
| socket 3: modulus boundary | `fermat_indicator`, `modp_eval_mem_lowDegSpan` (`MOD_p` ∈ `V_{p-1}` over `F_p`) | `…Layer3ModulusBoundary` |
| MOD_q ∉ AC⁰[p], `q≠p` prime | `mod_q_indicators_false` (Smolensky) | `…Layer4ModqChar` |

⇒ Polynomial method linearises exactly `MOD_p` gates over `F_p`; the lower bound is real.

## Tier 3 — general ACC⁰ (mixed / composite MOD): **OPEN — the genuine frontier**

The wall is precisely **mixed modulus**: no single prime field linearises gates of different moduli.

| structural fact | theorem | file |
|---|---|---|
| `F_p` statistic = count mod p | `fp_statistic_eq_count` | `…Layer3MixedModulus` |
| **MOD_6 = MOD_2 ∧ MOD_3** (CRT) | `mod6_eq_mod2_and_mod3` | `…Layer3MixedModulus` |
| MOD_2 low-degree over `F_2` | `mod2_detector_lowdeg_F2` (∈ `V_1`) | `…Layer3MixedModulus` |
| MOD_3 low-degree over `F_3` | `mod3_detector_lowdeg_F3` (∈ `V_2`) | `…Layer3MixedModulus` |
| hybrid observer | `MixedModulusStratifiedObserverSocket` (named OPEN socket) | `…Layer3MixedModulus` |
| **mixed-modulus SAT speedup** | `mod_circuit_sat_speedup` / `mod6_circuit_residue_speedup`: `∏q_j<2^n` ⇒ `<2^n` residue cells | `…ACC0ModResidueSpeedup` |
| **operational residue machine** | `residueSearch` (timed algorithm); `residueSearch_decides`, `residueSearch_beats_bruteforce` (`steps≤∏q_j`, `<2^n`) | `…ACC0ResidueMachine` |
| **branched residue cost** | `branched_residue_beats_bruteforce` / `…_mod6_…`: `2^killed·∏surviving q_j < 2^n` past the `∏q_j<2^n` base regime | `…ACC0BranchedResidue` |
| **restriction ⇒ few survivors** | `residue_cells_le_surviving_moduli`: `\|cells(C↾L)\| ≤ ∏_{surviving} q_j` (discharges step 2's per-branch bound) | `…ACC0ResidueRestriction` |
| **depth iteration (socket)** | `acc0_depth_reduction_speedup`: `MixedACCDepthReductionSocket` (Yao–Beigel–Tarui depth-2 normal form, OPEN) ⇒ residue search decides `Satisfiable(eval C)` in `<2^n` | `…ACC0ResidueDepthReduction` |
| **Williams cash-out (interface)** | `residue_cashout_bundled`: depth socket + `UniformWilliamsRealizationSocket` ⇒ `NEXP⊄ACC⁰`; self-audit `*_iff_separation` (sockets ⟺ separation) | `…ACC0WilliamsCashout` |
| **residue-observer algebra** | `ObservedBy`, `observed_top_pi` (composition law), `.and`/`.or`/`.comp`, cell-count bounds | `…ACC0ResidueObserver` |
| **toy Beigel–Tarui (attacking the wall)** | `toy_bounded_bottom_searchable`: SYM-of-`AND_w` (fan-in `≤w` bottom) searchable in `<2^n` — depth-reduction socket **discharged for the bounded-bottom fragment** | `…ACC0BeigelTaruiToy` |
| **depth-3 MIXED fragment** | `depth_mixed_searchable`: arbitrary top over a bottom **mixing** `MOD_q` + bounded-`AND_w` gates is searchable in `<2^n` when `∏(per-gate states)<2^n` — socket **discharged for the mixed-bottom fragment** | `…ACC0Depth3Mixed` |
| **support normal form (syntax-level)** | `acc0_observed_by_projection`: ANY `ACC0Circuit` observed by projection to its `support` (`eval_depends_on_support`, induction); `acc0_junta_searchable` (junta ⇒ `<2^n`); `acc0_top_over_subcircuits_searchable` (top over ACC⁰ subcircuits jointly reading `<n` vars ⇒ `<2^n`, union support) — `ACC⁰⊆mixed-bottom` chipped at circuit syntax | `…ACC0SupportNormalForm` |
| **depth-3 MOD normalization (residue gain)** | `and_of_mods_searchable`: syntactic `MOD_{q₁}∧MOD_{q₂}` ⇒ `Depth2ModCircuit`, residue-searchable `<2^n` when `q₁·q₂<2^n` **regardless of support** (junta gives nothing here); `mod_bottom_circuit_searchable` (arbitrary top over MOD family) | `…ACC0Depth3ModNormalize` |
| **AC⁰-over-MOD normalization (general)** | `modComb_normalizes`: ANY AND/OR/NOT combination of a MOD-gate family `= Depth2ModCircuit.eval ⟨gates, combTop C⟩` (factors through MOD outputs, induction); `modComb_searchable` (residue-searchable `<2^n` when `∏q_i<2^n`) | `…ACC0ModCombNormalize` |
| **observer extracted from raw syntax** | `acc0_residueObserved`: by induction on `ACC0Circuit`, ANY circuit (positive MOD moduli) is `ObservedBy` a product statistic of state count `≤ stateBound C` (occurrence product: `q` per MOD, `2` per var, mult. through `∧`/`∨`) — `MOD` leaf→`modGate_observedBy`, `var`→coord projection, gates→`.comp`/`.and`/`.or`; `acc0_modcircuit_searchable` (`stateBound C<2^n` ⇒ `<2^n` cells). Bottom *derived*, not assumed | `…ACC0ExtractObserver` |
| **deduplicated state bound (collapses occurrence overcount)** | `acc0_dedupObserved`: by induction, `eval C` observed by `(mod-residue, ONE projection to `varSupp C`)` — `MOD`-internal vars absorbed into residue (`modOcc C`, no `2` cost), `var`-leaves counted by *deduplicated* union support (the `∧`/`∨` arms share a single projection onto `varSupp a ∪ varSupp b`, recovered by restriction); `acc0_dedup_searchable` (`modOcc C·2^\|varSupp C\| < 2^n` ⇒ `<2^n` cells). Strictly tighter than `stateBound` (vars once, not per occurrence) | `…ACC0DedupShrink` |
| **syntactic restriction + boundary shrinkage** | `subst1 C i v` (substitute `v` for var `i`; `MOD` gate `mod q S t ↦ mod q (S.erase i)(t-[i∈S]·v)`); `eval_subst1` (`= eval C (update x i v)`, induction); `varSupp_subst1_subset`/`modOcc_subst1_le`/`ModsPos_subst1` (boundary monotone under restriction); **`varSupp_subst1_card_lt`** (restricting a *read* var strictly shrinks the dedup boundary); **`sat_branch_subst1`** (SAT branches over the two restrictions of any var). The restriction algebra for shrinkage — NOT a switching lemma (one branch halves each boundary, no asymptotic gain alone) | `…ACC0RestrictShrink` |
| **random-restriction collapse of the dedup boundary** | `restrList` (multi-var restriction = iterated `subst1`); `eval_restrList`/`varSupp_restrList`/`modOcc_restrList_le`/`ModsPos_restrList` (lifts from single-var lemmas); `fixedListOf Lᶜ β`/`varSupp_restr_compl_subset` (fixing `L`'s complement ⇒ `varSupp ⊆ varSupp C ∩ L`); **`exp_liveBoundary_eq`** (`Exp_p \|varSupp C ∩ L\| = p·\|varSupp C\|` — proportional collapse, via `exp_sum`+`exp_indicator_eq_survProb` on the existing `p`-biased measure); `exists_small_liveBoundary` (Markov+prob. method); **`dedup_restriction_collapses`** (∃ live `L`: ∀ fixed-assignment `β`, restricted circuit's dedup boundary `< a ≈ p·\|varSupp C\|`). Honest: proportional (first-moment) collapse, NOT switching — no SAT speedup alone (branching over `2^{#fixed}` offsets) | `…ACC0RandomRestrictShrink` |
| **function-level collapse: decision-tree observer** | **`dt_observed`**: `BoolDecisionTree.eval T` is `ObservedBy` a statistic of cell-count `≤ 2^{T.depth}` (induction; query node uses the *sum*-typed leaf observer `S_low ⊕ S_high` = additive leaf count, so the bound is `2^depth` not `2^{#queried vars}`) — the *function's* leaf boundary, INDEPENDENT of syntactic support; `dt_searchable`/`function_collapse_of_dt` (a function computed by a shallow DT, `2^depth<2^n`, is SAT-searchable `<2^n`). Cash-out converting decision-tree depth (what switching reduces) into the observer boundary. AC⁰ layer only (MOD no-go) | `…ACC0DecisionTreeObserver` |
| **`DTree → BoolDecisionTree` bridge (switching's canonical tree, observed)** | `toBoolDT` (constructor-for-constructor map) + `toBoolDT_eval`/`toBoolDT_depth` (semantics + depth preserved, one-line inductions); `dtree_observed`/`dtree_searchable` (the observer cash-out transferred to `Depth3.DTree`); **`canonicalDTree_observed`/`canonicalDTree_searchable`**: the switching arc's `canonicalDTree` (width-`w` DNF, fuel `F`, depth `≤ F·w` by `canonicalDTree_depth_le`) has a `≤ 2^{F·w}`-cell observer boundary, SAT-searchable `<2^n` when `2^{F·w}<2^n`. The representation step flagged separate is now DONE — function-level collapse composes end-to-end with switching's canonical tree. (Caveats: `eval(canonicalDTree)=restricted DNF` is cited soundness; MOD layer excluded) | `…ACC0DTreeBridge` |
| **AC⁰ + MOD composition across the no-go** | `dt_oracle_observed` (a depth-`d` DT over Boolean *oracle*-subfunctions `h : Fin m → (Fin n→Bool)→Bool` has a `≤ 2^d` boundary — sum-typed leaf, branch on `h i x`); **`acc0_over_mod_observed`/`acc0_over_mod_searchable`**: `x ↦ T(g₁(x),…,g_m(x))` (`T` depth-`d` DT top, `gⱼ` MOD gates) is `ObservedBy` a `≤ 2^d` boundary / SAT-searchable `<2^n` when `2^d<2^n` — MOD gates QUERIED as oracles along each path, never collapsed (respects the no-go), boundary independent of moduli & vars read. Honest: this is the *deterministic* composition law; supplying a shallow `T` over the gate outputs is the OPEN part — gate outputs `gⱼ(x)` are determined by `x` (not independent), so switching's restriction can't collapse the top over them = the no-go's deeper form. Composes ONE AC⁰-over-MOD level; iterating through MOD layers stays blocked | `…ACC0ModComposition` |
| **oracle-control syntax → DT observer** | `OracleControl` (Boolean control circuit over `m` oracle leaves) + `controlEval`; `treeOf`/`treeOf_eval`/`treeOf_depth_le` + `fullTree` (complete query tree: ANY `f` on `m` bits → depth-`≤m` DT computing it); **`control_to_decision_tree`** (every control reduces to a depth-`≤m` DT); **`oracle_control_dt_searchable`** (control computed by depth-`d` DT ⇒ AC⁰-over-MOD composite SAT-searchable `<2^n` when `2^d<2^n`); **`oracle_control_over_mod_searchable`** (unconditional fragment: control over `m` MOD oracles searchable below brute force when `2^m<2^n`, i.e. `<≈n` MOD gates, ANY control structure). Deterministic control→DT (depth `m` in general) | `…ACC0OracleControl` |
| **step 3 (DNF fragment): random restriction makes the control shallow** | **`dnf_control_switching_searchable`**: for a DNF control `D` over `k` oracle positions (width `≤w`, `≤M` clauses, switching union-bound `(2p/(1-p))(2wM)<1` & `r^s/(1-r)<1`) and MOD gates with `s≤n`, **∃ position-restriction `ρ`** s.t. `(canonicalDT D F ρ)` is shallow (depth `<s`) and `x ↦ (canonicalDT D F ρ).eval(gate-outputs)` is SAT-searchable `<2^n`. The collapse is the PROVED fully-discharged switching lemma `Depth3.exists_shallow_all_tight_uncond` (no reconstruction socket — only width/clause-count + union bound); `canonicalDT` returns a `BoolDecisionTree` directly so it composes with `acc0_over_mod_searchable` with no bridge. HONEST: `ρ` restricts the oracle POSITIONS, not `x` — the position/`x` gap (oracle outputs determined-not-independent = the no-go's deeper form) is NOT crossed; this is the control-position-level collapse composed with oracles, not an `x`-level circuit speedup | `…ACC0DNFControlSwitching` |
| **position → `x` bridge: realizing oracle restrictions** | `Extends`/`RealizableByInputRestriction` (∃ input restriction `σ` forcing every fixed gate to its `ρ`-value on all completions); `modGate_eval_congr` (MOD value depends only on support); **`realizable_of_disjoint`** (POSITIVE fragment: pairwise-disjoint supports + per-gate achievability ⇒ realizable — set each fixed gate's support independently via `Classical.choose`, disjointness ⇒ no conflict); **`position_restriction_not_always_realizable`** (NO-GO: two MOD₂ gates same support `{0}` targets `0`/`1`, both forced `true` ⇒ `0=1` in `ZMod 2`, impossible). So the bridge is TRUE on the disjoint fragment, FALSE in general — must stay fragment-restricted/a socket. HONEST: solves the *forcing* half (fix determined gates); full composition needs free gates to stay free under `σ` (disjoint supports it, not assembled) | `…ACC0OracleRestrictionRealization` |
| **disjoint-fragment `x`-level speedup (exact reduction)** | **`gate_vector_realizable`** (disjoint supports + each gate both-achievable ⇒ the gate-output vector is SURJECTIVE: `∀ y, ∃ x, ∀ j, (gⱼ).eval x = y j`, via `realizable_of_disjoint` at `ρ=some∘y`); **`disjoint_fragment_sat_iff`** (the EXACT reduction `Satisfiable (x ↦ C(g₁(x),…,g_k(x))) ↔ Satisfiable C` over `Fin k → Bool`); **`disjoint_fragment_speedup`** (`f`-SAT over `Fin n` ↔ search over `Fin k → Bool`, card `2^k < 2^n`). The `n`-variable circuit collapses EXACTLY to the `k`-variable control — a genuine `x`-level speedup. Disjointness is load-bearing: the no-go breaks surjectivity for overlapping gates, so the reduction fails there | `…ACC0DisjointFragmentSpeedup` |
| **parity (MOD₂) constraint realization: realizable ⟺ F₂ system consistent** | **`realizable_iff_achievable`** (arbitrary MOD, no disjointness needed: `RealizableByInputRestriction ρ gate ↔ ∃ x, ∀ j b, ρ j = some b → (gⱼ).eval x = b` — disjointness only *constructs* the input, the no-go is when none exists); `parityGate`/`zmod2_ne_iff`/**`parity_force_linear`** (forcing a parity gate = the F₂ equation `parity Sⱼ = (tⱼ if b else tⱼ+1)`); **`parity_realizable_iff_consistent`** (realizable ⟺ the F₂ parity system has a Boolean solution); **`control_sat_iff_reachable_image`** (arbitrary MOD: SAT ⟺ search the *reachable* gate-output image, not pretended-independent `2^k`); `parityVector_xor`/`parity_reachable_zero_mem`/**`parity_reachable_add_mem`** (the reachable parity image is an F₂-SUBSPACE — closed under `0`,`+` via xor-additivity — so `\|image\| = 2^rank`, `2^k` only in the disjoint/full-rank case). Replaces "disjoint-only" with linear-algebraic compatibility | `…ACC0ParityConstraintRealization` |
| **exact `2^rank` cardinality of the reachable parity image** | `parityLinMap S : (Fin n→ZMod 2) →ₗ[ZMod 2] (Fin k→ZMod 2)` (F₂-linear realization of the parity statistic); `parityVector_eq_linMap`; **`range_parityVector`** (reachable image `= ↑(LinearMap.range parityLinMap)` — Boolean-input embedding surjective via `zmod2_ite`); `parity_subspace_card` (`Fintype.card ↥(range) = 2^finrank` via `Module.card_eq_pow_finrank`+`ZMod.card`); **`parity_reachable_card`**: `Nat.card (Set.range (parityVector S)) = 2 ^ finrank (ZMod 2) (range parityLinMap)`. The parity-gate observer boundary is EXACTLY `2^rank` (`2^k` iff the gates are independent / full rank). Quantitative closure of the reachable-image story (MOD₂); general MOD_q is 0/1-feasibility over ZMod q | `…ACC0ParityRankCardinality` |
| **rank-based `x`-level speedup (parity)** | `image_parityVector_card` (`\|univ.image (parityVector S)\| = 2^rank`, via `Set.toFinset_range`+`Set.toFinset_card`+`parity_reachable_card`); **`parity_rank_speedup`**: a parity-gate control's `f`-SAT over `Fin n` ⟺ search the reachable gate-output image (gate vector = relabel ∘ parityVector), whose card is `< 2^n` once `2^rank < 2^n` (i.e. `rank < n`). Strictly stronger than disjoint `2^k`: `rank ≤ k`, strict under linear dependence — a parity control whose gates span an `<n`-dim row space is sub-`2^n` searchable EVEN with overlapping supports | `…ACC0ParityRankSpeedup` |
| **general `MOD_q` realization = 0/1-feasibility** | `modGate_eval_true_iff`/`modGate_eval_false_iff` (forcing a `MOD_q` gate is the count `=`/`≠` target constraint); **`modq_realizable_iff_feasible`** (realizable ⟺ ∃ Boolean `x` meeting the equality/disequality count system — the general-`q` analogue of `parity_realizable_iff_consistent`, now a 0/1-INTEGER feasibility problem NOT linear algebra); `modQStatOn_singleton`, **`modq_residue_image_not_subspace`** (THE OBSTRUCTION proved: for `MOD₃` the reachable residues `{0,1}⊆ZMod 3` are NOT closed under `+` (`1+1=2` unreachable), so the `2^rank` subspace structure is MOD₂-specific). The `x`-level speedup itself is `q`-independent (`≤2^k` gate-output bits, via `oracle_control_over_mod_searchable`); `MOD_q` only changes *which* patterns are reachable (the 0/1-feasibility region — no closed-form card for `q≥3`) | `…ACC0ModQFeasibility` |
| **mixed moduli `MOD₆ = MOD₂ ∧ MOD₃` via CRT** | **`mod6_eval_true_iff`** (CRT split: `(⟨6,S,t⟩).eval x = true ↔ count_S(x) ≡ t.val [MOD 2] ∧ ≡ [MOD 3]`, via `Nat.modEq_and_modEq_iff_modEq_mul` `2⊥3` + `ZMod.natCast_eq_natCast_iff`); **`mod6_realizable_iff_mixed`** (a forced-true `MOD₆` family realizable ⟺ `∃ x` solving the mod-2 system AND the mod-3 system simultaneously). Combines the F₂-linear/rank story with the F₃-feasibility story, COUPLED through the shared `x` — neither rank nor feasibility alone suffices, no product/closed-form shortcut. (Forced-true case; general `ρ` makes each gate a conjunction/disjunction of the CRT parts) | `…ACC0Mod6Mixed` |
| **mixed moduli `MOD₆`, general `ρ` (true ∧, false ∨)** | **`mod6_eval_false_iff`** (forcing false negates the CRT conjunction → DISJUNCTION `¬(count ≡ t mod 2) ∨ ¬(count ≡ t mod 3)`, via `Bool.eq_false_iff`+`mod6_eval_true_iff`+`not_and_or`); **`mod6_realizable_iff_general`** (arbitrary `ρ`: realizable ⟺ `∃ x` meeting per-gate the conjunction of CRT congruences (forced-true) / disjunction of CRT disequalities (forced-false), coupled through `x`). The fully-general mixed-modulus realization = a mixed CSP over the Boolean cube (linear mod-2 ∧ feasibility mod-3 atoms, conjoined/disjoined per gate) — no rank or product shortcut | `…ACC0Mod6MixedGeneral` |
| **YBT `SYM`-top count observer (the tractable heart of `SYM∘AND`)** | `gateCount`/`symEval` (count of accepting sub-gates; a symmetric = count-function of them); `gateCount_le` (`≤ m`); **`sym_observed`** (`symEval g h` `ObservedBy` the count); **`sym_count_card_le`** (count boundary `≤ m+1` — the symmetric top collapses `2^m` → `m+1`); **`sym_searchable`** (`SYM∘(m gates)` SAT-searchable `<2^n` once `m+1<2^n`; at the YBT form `m`=quasipoly, `<2^n`). Proves WHY the YBT `SYM` top is cheap (count boundary), for arbitrary sub-gates. The YBT REDUCTION (arbitrary ACC⁰ → `SYM∘AND`, `m` quasipoly) is NOT proved — the open structural wall, socketed as `MixedACCDepthReductionSocket` | `…ACC0SymmetricObserver` |
| **low-degree polynomial → `SYM∘AND` (YBT chip)** | `monoAND` (monomial `AND` gate, fan-in `\|S\|`); `lowDegSubsets`/**`lowDegSubsets_card`** (degree-`≤D` monomials count `= ∑_{i≤D} C(n,i)`, via `card_biUnion` over `powersetCard`); **`monomial_count_le`** (distinct degree-`≤D` monomials ⇒ `≤ ∑_{i≤D} C(n,i)` bottom gates); **`lowDegreePoly_searchable`** (a `SYM` gate over `m` monomial-`AND`s is SAT-searchable `<2^n` once `m+1<2^n` — the poly value is `gateCount` over its monomial-`AND`s, so `sym_searchable` applies). The polynomial→`SYM∘AND` cash-out (0/1-coeff multilinear; general coeffs duplicate gates). The hard direction — arbitrary ACC⁰ *is* such a low-degree poly (RS approximation) — is NOT proved (the wall) | `…ACC0PolyToSymAnd` |
| **RS low-degree span → `SYM∘AND` bottom layer** | **`squarefreeEvalMonomial_eq_monoAND`** (the RS generator `∏_{i∈S} boolToZMod p (xᵢ) = if monoAND S x then (1:ZMod p) else 0` — the low-degree-span generators ARE the monomial-`AND` gates); **`lowDegPolyEval_mem_monoAND_span`** (lifts Layer3's `eval_mem_lowDegSpan` through the bridge: a degree-`≤D` polynomial's cube eval ∈ `ZMod p`-span of the monomial-`AND` indicators — the `SYM∘AND` bottom, `≤∑_{i≤D}C(n,i)` gates). Connects the RS machinery to the `SYM∘AND` world exactly. Honest gaps: RS is *approximate* (`acc0_approx_by_lowRankPredictor`, agrees on `1-ε`) so this is an *approximate* `SYM∘AND`; the `ZMod p`-linear top → count-mod-`p` `SYM` needs gate duplication; the exact `AC⁰[p]→SYM∘AND` across depth is the wall | `…ACC0RSToSymAnd` |
| **weighted-count `SYM` observer (closes the `ZMod p`-linear → count gap)** | `weightedGateCount c g x := ∑ⱼ cⱼ·[gⱼ accepts]` (= count over the multiset with gate `j` duplicated `cⱼ` times) + `weightedSymEval`; `weightedGateCount_le` (`≤ ∑ⱼ cⱼ`); **`weightedSym_observed`/`weightedSym_count_card_le`/`weightedSym_searchable`** (weighted `SYM` observer, boundary `≤ ∑ⱼcⱼ+1` not `2^m`, searchable `<2^n` once `∑cⱼ+1<2^n`); **`weightedGateCount_cast_eq`** (over `ZMod p` with `cⱼ=(coeffⱼ).val`, the weighted count's residue = the polynomial value `∑ⱼ coeffⱼ·monoAND_j` — the coefficient-duplication identity). Closes the gap exactly: a `ZMod p`-combination of monomial-`AND`s IS a weighted-count `SYM∘AND`. (RS still approximate; exact reduction still the wall) | `…ACC0WeightedSymAnd` |
| **approximate vs exact (what RS approximation buys)** | `disagreeSet f g` (inputs where `f≠g`); **`sat_card_le_of_disagree`** (`\|{f=true}\| ≤ \|{g=true}\| + \|disagreeSet\|` — an approximating `g` bounds `f`'s *solution count*, not SAT); **`exact_sat_iff`** (`disagreeSet = ∅` ⇒ `Satisfiable f ↔ Satisfiable g` — only EXACT agreement transfers SAT). Pins down why the cash-out needs the exact `SYM∘AND`: approximation gives only a sparsity/correlation bound; exactness gives the SAT equivalence | `…ACC0ApproxConsequence` |
| **YBT exact normal-form socket (the structural wall, conditional cash-out)** | `HasExactSymAndForm C` (socket: `eval C = symEval (monomial-ANDs) h` with `m+1<2^n`); **`ybt_socket_searchable`** (socket ⇒ `Satisfiable (eval C)` decided by `<2^n`-cell search, via `lowDegreePoly_searchable`). The socket for arbitrary `ACC⁰` (`m` quasipoly) IS the YBT theorem — a known classical result, NOT formalized (the structural wall, cf. `MixedACCDepthReductionSocket`). Needs the EXACT form (approximate only bounds solution count) | `…ACC0YBTSocket` |
| **YBT reduction core: `AC⁰[p]` circuit → `SYM∘AND`-bottom span (conditional on degree)** | **`acc0p_circuit_in_monoAND_span`**: `IsAC0Syntax c` + `(toPoly p c).totalDegree ≤ D` ⇒ `(x ↦ boolToZMod p (c.eval x)) ∈ ZMod p`-span of degree-`≤D` monomial-`AND` indicators. Wires Layer3's EXACT circuit→poly reduction (`toPoly_eval_AC0`) into the `SYM∘AND` bottom via `squarefreeEvalMonomial_eq_monoAND`/`lowDegPolyEval_mem_monoAND_span`. **The degree bound IS the wall**: `toPoly` is EXACT but high-degree (unbounded fan-in `∧` = product of inputs ⇒ degree grows with fan-in); polylog degree is the *approximate* RS (`toAgree`), so the exact polylog form for arbitrary `AC⁰[p]` is open. (Span→explicit combination + `weightedSym_searchable` = remaining mechanical glue) | `…ACC0YBTReduction` |
| **`toAgree` approximant in the bounded `SYM∘AND` span (degree bound DISCHARGED)** | **`acc0p_toAgree_in_monoAND_span`**: `(x ↦ eval (embed x) (toAgree p t R C)) ∈ ZMod p`-span of degree-`≤((p-1)t)^{depth}` monomial-`AND` indicators — **UNCONDITIONAL** (the degree bound is *proven* by Layer3's `toAgree_totalDegree_le`, not assumed, unlike the exact `toPoly`). So the RS approximant is genuinely a bounded `SYM∘AND`: polylog degree (const depth, polylog `t`) ⇒ quasipoly gates `∑_{i≤D}C(n,i)` — the YBT size, for the approximant. Remaining gap is purely approximate-vs-exact (`toAgree` agrees `1-ε`, bounds solution count not SAT, per `…ACC0ApproxConsequence`); the exact form is the wall | `…ACC0ToAgreeDegree` |
| **symmetric-count exact trick (`AND`/`OR` are exactly symmetric, no ε)** | `gateCount_eq_card_iff` (`gateCount g x = m ↔ ∀ j, gⱼ x` — `AND` = count hits `m`); `gateCount_pos_iff` (`1 ≤ gateCount g x ↔ ∃ j, gⱼ x` — `OR` = count positive); **`and_exact_sym`/`or_exact_sym`** (`AND`/`OR` over `m` gates EQUAL `symEval g [·=m]`/`symEval g [1≤·]` — EXACT, no approximation); **`and_exact_searchable`/`or_exact_searchable`** (hence SAT-searchable `≤ m+1` cells, exactly). The Beigel–Tarui exact trick at the GATE level: `AND`/`OR`/`MOD` need no RS approximation — they are exactly symmetric count functions, the exactness from the `SYM` top reading the exact count. Closes the approximate/exact gap per-gate; depth composition (keeping size quasipoly) is the remaining structural step | `…ACC0SymmetricExact` |
| **depth composition law (top over observed subcircuits → searchable, product boundary)** | **`depth_compose_searchable`**: a top over `k` subcircuits each `ObservedBy` a statistic with `≤\|Sᵢ\|` cells, with `∏ᵢ\|Sᵢ\| < 2^n`, is SAT-searchable in `< 2^n` (joint statistic = product, via `observed_top_pi`+`observed_sat_iff`+`observed_pi_cellCount_le`). The depth-composition law — composes at any depth. Honest: the boundary is the **product** `∏ᵢ\|Sᵢ\|` (EXPONENTIAL in width); the YBT quasipoly control replaces this multiplicative blow-up by the *additive* polynomial-degree composition (`…ACC0AdditiveDegree`/`…ACC0ToAgreeDegree`, approximate) — the exact quasipoly depth-composition is the wall | `…ACC0DepthCompose` |
| **additive degree control (`AND` degree = sum of input degrees)** | `toPolyList_eq_map`; **`toPoly_andGate_totalDegree_le`** (`totalDegree (toPoly (AND cs)) ≤ ∑_{c∈cs} totalDegree (toPoly c)` — the gate degree is the SUM, not product, of input degrees, via `MvPolynomial.totalDegree_list_prod`); **`toPoly_andGate_totalDegree_le_of_bounded`** (fan-in `≤w`, inputs `≤D` ⇒ `AND` degree `≤ w·D` — one layer multiplies degree by `≤` fan-in, via `List.sum_le_card_nsmul`). The additivity that makes the polynomial method quasipoly: degree adds across `∧`, so depth-`d` ⇒ polylog degree (`((p-1)t)^depth`) ⇒ quasipoly monomial-`AND`s — vs the observer's exponential product (`…ACC0DepthCompose`). Within-layer additive; exact polylog-across-depth is the wall | `…ACC0AdditiveDegree` |
| **exact depth composition (Bool specialization, exponential boundary)** | **`exact_depth_composes`**: for `sub : Fin k → (Fin n → Bool) → Bool`, any `top`, once `2^k < 2^n`: `(fun x => top (fun i => subᵢ x))` is SAT-searchable in `< 2^n` with the subcircuit-output vector as the cell statistic (boundary `2^k`). The exact (no-approximation) side of composition — the `…ACC0SymmetricExact` exact gates compose at any depth, but with the **exponential** `2^k` product boundary. Specializes `depth_compose_searchable` at `S = fun _ => Bool` (`Fintype.card_bool`); makes the exact-vs-quasipoly tension precise: exact ⇒ exponential boundary, quasipoly ⇒ additive degree but only *approximate* — having *both* (exact `SYM∘AND` of quasipoly size) is the Beigel–Tarui ℤ-polynomial+CRT wall | `…ACC0ExactCompose` |
| **integer-polynomial CRT decode (exact BACK half of Beigel–Tarui)** | **`count_crt_iff`** (CRT for the integer count `gateCount`: `≡t [MOD qs.prod] ↔ ∀i ≡t [MOD qs.get i]` for pairwise-coprime `qs`, via `Nat.modEq_list_prod_iff`); **`modCount_factors_through_resVec`** (the exact `MOD_{qs.prod}` count decision `= G(countResVec)`, factoring through the residue vector `i↦(gateCount : ZMod (qs.get i))`, via CRT + `ZMod.natCast_eq_natCast_iff`); **`countResVec_card_le`** (`≤ qs.prod`); **`count_crt_sat_speedup`** (`qs.prod<2^n ⇒ <2^n` residue cells); **`mod6_count_crt_speedup`** (`[2,3]` concrete). Generalizes `mod6_eq_mod2_and_mod3` to arbitrary squarefree `M=∏qᵢ` and attaches it to the **count polynomial** (not one MOD-gate's support count). Honest: the EXACT back half only — CRT decodes from the EXACT count; it does **NOT** turn approximate RS residues into an exact decision, so it does not close the approximate/exact gap = Wall 1 (front half: `ACC⁰→exact low-degree ℤ-poly across depth`) | `…ACC0IntegerPolynomialCRT` |
| **toy exact-AND-quasipoly `SYM∘AND` (depth-2 bounded-fan-in DNF)** | The one restricted fragment where **exact and quasipolynomial hold at once** (what general Wall 1 can't have). For a depth-2 `OR∘AND_w` DNF with distinct bottom monomials of fan-in `≤w`: **`dnf_exact_symAnd`** (`= symEval (monoAND∘mono) [1≤·]` **exactly**, via `or_exact_sym` — no approximation); **`dnf_bottom_count_le`** (`m ≤ ∑_{i≤w}C(n,i)` distinct degree-`≤w` monomials, quasipoly `n^{O(w)}`, via `monomial_count_le`); **`dnf_exact_quasipoly_searchable`** (both at once + SAT-searchable in `<2^n` once `(∑_{i≤w}C(n,i))+1<2^n`). Bounded fan-in `w` keeps degree bounded **exactly** (`monoAND` is a genuine `AND`, not an approximant). Honest: holds ONLY for this fragment — the general wall is the loss of any of {bounded fan-in, no MOD layer, bounded depth}; unbounded fan-in/MOD/depth force exact⇒exponential (Wall 1) | `…ACC0ExactQuasipolyDepth2` |
| **exact-quasipoly fragment + one `MOD` top (`MOD_M ∘ AND_w`)** | Extends the fragment toward YBT by replacing `OR` with a `MOD_M` top — both layers still EXACT, no RS approximation: **`modAnd_bottom_count_le`** (bottom `AND_w` quasipoly, `m≤∑_{i≤w}C(n,i)`); **`modAnd_exact_observed`** (top `MOD_M` decoded EXACTLY by the count-residue vector via CRT, `…ACC0IntegerPolynomialCRT`); **`modAnd_exact_quasipoly_searchable`** (bottom quasipoly ∧ searchable `<2^n` once `M=qs.prod<2^n` — the modular top compresses the count to `M` cells); **`mod6_and_exact_quasipoly_searchable`** (`[2,3]` concrete). `MOD` is *exactly* symmetric so the top needs no approximation. Honest: stops at depth-2 bounded-fan-in bottom; general YBT needs arbitrary `ACC⁰` bottom where exact⇒exponential = Wall 1 (the exact `MOD` top buys nothing — it's the *bottom* `AND_w→exact low-deg poly across depth` that breaks) | `…ACC0ExactQuasipolyModTop` |
| **depth-3 exact composition over `MOD_M ∘ AND_w` (the "across depth" clause)** | Tests whether exactness survives ONE composition step: a top over `k` middle `MOD_M∘AND_w` gates. **`depth3_middle_exact`** (each middle gate CRT-decoded EXACTLY by its residue vector); **`depth3_modAnd_compose_searchable`** (every middle bottom quasipoly `m≤∑_{d≤w}C(n,d)` ∧ depth-3 circuit searchable `<2^n` once `2^k<2^n`, via `exact_depth_composes` k-bit middle-output observer). ANSWER: exactness survives, at the **product** cost `2^k` (quasipoly only for `k=polylog`). Honest: this is exactly why the general construction breaks — as top fan-in/depth grow the exact boundary multiplies to exponential = Wall 1 (the bottom degree-across-depth; the exact top buys nothing) | `…ACC0ExactQuasipolyDepth3` |
| **anti-product trick: symmetric top avoids the `2^k` blow-up** | The smallest case where the depth-3 product wall is genuinely AVOIDED. A *symmetric* top over `k` middle `MOD_M∘AND_w` gates reads only the COUNT of accepting middle gates: **`antiproduct_count_card_le`** (`≤ k+1` cells, NOT `2^k`); **`antiproduct_sym_modAnd_searchable`** (bottom quasipoly ∧ `≤k+1` cells ∧ searchable `<2^n` once `k+1<2^n`). Two symmetric layers (outer `SYM` over inner `MOD∘AND_w`=`SYM∘AND`) compose with LINEAR observer state, not the product `2^k`/`M^k`. Honest: genuine anti-product but NOT YBT — needs pre-grouping as one `SYM` layer, can't iterate (symmetric in middle outputs not inputs), bottom degree still Wall 1. This is the YBT `SYM` top being tractable (= `sym_count_card_le`, `m+1` not `2^m`) one layer up | `…ACC0AntiProductSym` |
| **bottom-clause no-go: exact unbounded-fan-in `OR`/`AND` = full degree** | Attacks Wall 1's bottom clause directly and proves the obstruction is REAL. Over `F₂` the ANF is unique (`anf_involutive`): **`anf_andFn_univ`**/**`anf_orFn_univ`** (top ANF coeff of `AND`/`OR` `= 1` — for `OR` via parity of the `2ⁿ−1` nonempty subsets); **`or/and_exact_degree_full`** (`∃ S, |S|=n ∧ anf≠0`); **`or/and_not_anf_degree_lt`** (NO exact `F₂` representation of degree `<n`). So exact unbounded-fan-in `OR`/`AND` has degree = fan-in `n` ⇒ `2^n` monomials ⇒ exponential. Rules out the naive route (single exact low-degree poly per gate); the only escape is approximate→exact symmetric-top conversion = the irreducible Beigel–Tarui core (Wall 1, NOT crossed). Exact counterpart to the proved *approximate* side (`toAgree_totalDegree_le`) | `…ACC0ExactDegreeNoGo` |
| **BT approximate→exact conversion: socket + proven decoding** | The only route left after the no-go. **`ApproxToExactSymmetricDecode`** (the missing BT theorem made explicit: `f` = exact symmetric/count decode of `r` `IsLowDegreeGate D` gates; non-trivial ONLY via the low-degree clause — `r` copies of `f` satisfy it otherwise — so this for arbitrary `ACC⁰` = Wall 1); **`socket_searchable`** (socket ⇒ `≤r+1` cells); **`majority_decode`** (TOY, PROVED: strict-majority-correct at every point ⇒ `f x = decide(r<2·gateCount)` EXACTLY — error-corrected threshold decoding); **`majority_decode_symmetric`** (`f = symEval g [>r/2]`); **`majority_decode_gives_socket`** (low-degree + majority-correct ⇒ socket). Honest: the DECODING half is proven; the open part = low-degree approximants majority-correct everywhere for `ACC⁰` = the BT/Yao construction = Wall 1 (not faked) | `…ACC0ApproxToExactDecode` |
| **balancedness seed of the RS/BT probabilistic `OR`** | Attacking the construction at its atom: the balancedness of nonzero `F₂` linear forms. **`pv`** (the form `⊕_{i∈S}v_i`), **`tog`**/`tog_tog` (toggle involution `S△{j}`), **`pv_tog`** (toggling a `v_j=1` coord flips parity), **`pv_false`** (form `=0` on all-`0` input ⇒ one-sided, no false positive for `OR`), **`pv_balanced`** (THE SEED: for `v≠0` exactly `2^{m-1}` of `2^m` subsets give form `=1` — a random parity predicts `OR=1` w.p. exactly `1/2`; proved by the parity-flipping involution + `card_bij'`). Honest: this is the atom; degree-`t` boosting (`1-2^{-t}`) + quasipoly sampling (Chernoff+union) remain socketed = Wall 1. Basis caveat: parity forms are low *poly*-degree but high *monoAND*-degree vs `IsLowDegreeGate` | `…ACC0ProbabilisticForm` |
| **degree-`t` boosting: full `t`-tuple family majority-correct everywhere** | The boosting step over the atom. `OR` of `t` independent forms is wrong only when all `t` vanish: **`pv_zero_card`** (`#{=0}=2^{m-1}`), **`boost_wrong_card`** (all-zero tuples `=(2^{m-1})^t`, via `card_piFinset` independence), **`boost_total`** (`(2^m)^t`), **`boost_correct_card`** (`(2^m)^t-(2^{m-1})^t`), **`boost_predict_zero`** (one-sided: never fires on all-`0` input), **`boost_majority_nonzero`** (`t≥2, v≠0 ⇒` strict majority correct, via `2^t>2`). So the FULL `t`-tuple family (`t≥2`) is majority-correct at EVERY input — **discharges the "majority-correct" clause of the socket for `OR`**. Sole remaining gap: family size `(2^m)^t` exponential vs quasipoly = the Chernoff+union *sampling* (Wall 1) | `…ACC0ProbabilisticBoost` |
| **self-contained finite Chernoff (sampling concentration engine)** | Route B: a purely combinatorial Chernoff — no measure theory, no `exp`, in `Finset`/`ℕ`/`ℝ`. **`finite_chernoff_lower`** (for `c≥1`: `#{σ:Fin r→P | 2·#{i|σ i∈G}≤r} ≤ (|G|/c + c(|P|-|G|))^r` — Markov-on-a-product, engine `∑_σ∏_i f(σ i)=(∑_x f x)^r` via `Finset.sum_prod_piFinset`, the finite `E[∏]=∏E`); **`finite_chernoff_majority`** (`c=2`, good-fraction `≥3/4` ⇒ `≤(7|P|/8)^r`, geometric base `7/8<1`). Honest: the concentration engine; the union bound over `2^n` inputs + instantiation to the boosted forms + basis bridge remain = Wall 1 assembly (routine given this) | `…ACC0FiniteChernoff` |
| **union-bound sampling existence (abstract, closes the sampling gate)** | The probabilistic-method existence argument, finite + self-contained. **`exists_sample_majority_correct_all`**: abstract over input set `X`, pool `P`, good sets `Good : X → Finset P`; given `∀ x, 3|P|≤4|Good x|` (density `≥3/4`), `0<|P|`, `|X|·(7/8)^r<1` ⇒ `∃ σ : Fin r → P, ∀ x, r < 2·#{i | σ i ∈ Good x}` (majority-correct at EVERY input). Proof: union the per-input minority-good sample sets (`card_biUnion_le`), bound each by `(7|P|/8)^r` (`finite_chernoff_majority`), bad samples `< |P|^r` total ⇒ good sample exists (pigeonhole). Next: instantiate `X=Fin m→Bool`, `P=`boosted `t`-tuples, `Good x=`correct forms (density from `boost_majority_nonzero`, `|X|=2^m ⇒ r=O(m)`); then basis bridge | `…ACC0SamplingExistence` |
| **instantiation to parity forms: quasipoly majority-correct family EXISTS** | Plugs the boosted parity forms into the abstract lemma. `X=Fin m→Bool`, `P=Fin t→Finset(Fin m)`, **`boostGood v`**=tuples correct on `v`. **`card_pool`** (`|P|=(2^m)^t`); **`boostGood_card_ge`** (`∀ v, 3|P|≤4|boostGood v|`, density `≥3/4` for `t≥2` — nonzero via `boost_correct_card`+`4·(2^{m-1})^t≤(2^m)^t`, zero-input all-correct via `pv_all_false`); **`exists_quasipoly_majority_correct_forms`** (`t≥2, 2^m·(7/8)^r<1 ⇒ ∃ σ:Fin r→(Fin t→Finset(Fin m)), ∀ v, r<2·#{i|σ i∈boostGood v}` — `r=O(m)` boosted forms majority-correct at EVERY input). **The socket's open clause, discharged for the parity-form construction.** Remaining: the basis bridge (parity low poly-deg vs `monoAND`-deg) = Wall 1 | `…ACC0SamplingForms` |
| **the basis bridge: parity forms ARE low `monoAND` degree (`F₂`-poly framing)** | FRAMING: low-degree = `F₂`-poly total degree. A linear form `L_S=∑_{i∈S}X_i` has POLY degree 1 (it's the SUM, not product) regardless of `|S|`; so `boostPoly σ=1-∏_k(1-L_{σk})` has degree `≤t`, and a degree-`≤t` `F₂` poly's cube-eval ∈ fan-in-`≤t` monoAND span. No mismatch — the wide set is in the linear (deg-1) part. **`linForm_totalDegree_le`** (`≤1`), **`boostPoly_totalDegree_le`** (`≤t`, the crux), **`eval_linForm`**(`=pv`), **`eval_boostPoly_eq`** (`=boolToZMod 2 (boostPredict)`), **`boostPredict_mem_monoAND_span`** (THE BRIDGE: boosted predictor's `F₂`-embedding ∈ span of degree-`≤t` monoAND indicators, via `lowDegPolyEval_mem_monoAND_span`). Resolves the bridge for the OR-gate construction. Honest: full BT front half = arbitrary `ACC⁰` across depth (this OR is one gate) = remaining Wall 1 | `…ACC0BasisBridge` |
| **AND-gate BT front half (via OR-duality)** | `AND(v)=¬OR(¬v)`. Dualizes OR: linear form `∑X_i` → affine form **`affForm`**`=∑(1+X_i)` (evaluates to `L_S(¬v)`, still degree 1), `boostPoly` → **`andPoly`**`=∏_k(1-A_{σk})`. **`affForm_totalDegree_le`** (`≤1`), **`andPoly_totalDegree_le`** (`≤t`), **`eval_andPoly_eq`** (`=boolToZMod 2 (andPredict)`), **`andPredict_eq_not_boostPredict`** (duality: `andPredict σ v=¬boostPredict σ (¬v)` — OR's majority-correctness transfers via input-negation bijection), **`andPredict_mem_monoAND_span`** (AND basis bridge: AND predictor's `F₂`-embedding ∈ span of degree-`≤t` monoAND indicators). 2nd of 3 `ACC⁰` gate types (after OR; MOD next). Remaining: MOD + depth composition = Wall 1 | `…ACC0AndBasisBridge` |
| **one-layer composition of BT approximants (degree multiplies by `t`)** | The depth-composition step: replace input vars `X_i` by the `k` subgate approximant polynomials `P_j` (each degree `≤d`). **`compLinForm`**`=∑_{j∈S}P_j` (linear form over OUTPUTS), **`compPoly`**`=1-∏_l(1-compLinForm(σl))` (boosted OR over subgates). **`compLinForm_totalDegree_le`** (`≤d`), **`compPoly_totalDegree_le`** (`≤t·d` — THE CRUX: degree `×t` per layer, so across depth `D` it's `t^D=(log s)^{O(D)}`, polynomial not exponential), **`eval_compPoly`** (= boosted OR over subgate values), **`compPoly_eval_mem_monoAND_span`** (composed approximant ∈ degree-`≤t·d` monoAND span). Basis bridge = `P_j=X_j, d=1` special case. Honest: one OR-layer; full depth induction (error accumulation, MOD layer, SYM∘AND assembly) = Wall 1 | `…ACC0LayerCompose` |
| **across-depth error accumulation (linear in gate count)** | The error counterpart to `…ACC0LayerCompose`'s degree composition. Composite errs only where SOME gate errs (correctness composes), so composite error ⊆ union of per-gate errors. **`error_union_bound`** (`|⋃_g Err g| ≤ #gates·e` via `card_biUnion_le`), **`composite_error_subset`** (comp-error ⊆ gate-error union, given comp correct off the union), **`composite_error_bound`** (`|comp error| ≤ #gates·e`), **`exists_majority_correct_composite`** (`2·#gates·e<|X| ⇒ |X|<2·|correct|` strict majority). LINEAR-in-`s` error (not exponential) is WHY constant depth works: per-gate `e<|X|/(2s)` keeps composite majority-correct. With entry 51 (polylog degree) = the RS depth composition. Honest: abstract union-bound; full assembly (per-point composition + boosting `e=2^{-t}|X|` + MOD + SYM∘AND) = Wall 1 | `…ACC0ErrorAccumulation` |
| **per-point composition (discharges `hcomp`)** | **`boost_formula`** (`1-∏(1-pv w)=boolToZMod(boostPredict σ w)`), **`eval_compPoly_of_subgates`** (if `P_j` compute `h_j` at `x`, `compPoly P σ` computes the boosted OR of subgate values `(h_1 x,...,h_k x)`). The bridge from degree composition (`…ACC0LayerCompose`) + error accumulation (`…ACC0ErrorAccumulation`) to actual Boolean computation | `…ACC0CompositionCorrect` |
| **`MOD₂` is exactly degree 1 (error 0)** | Unlike OR/AND (no exact low-degree, `…ACC0ExactDegreeNoGo`), `MOD₂`=parity IS the linear form. **`mod2_exact_eval`** (`eval (linForm univ)=boolToZMod(parityBool)`), **`mod2_mem_monoAND_span`** (degree-`≤1`, exact). Honest: `MOD₂` only (=`AC⁰[2]`); composite `MOD_m` has NO low-degree rep over any field (RS works for prime-power only) — the genuine `ACC⁰` barrier (why Williams needed a different method) | `…ACC0Mod2Exact` |
| **`MOD_p` is exactly degree `p−1` over `F_p` (Fermat) — `AC⁰[p]` ingredient** | Prime-`p` generalisation of `…ACC0Mod2Exact`. By Fermat (`ZMod.pow_card_sub_one_eq_one`): `MOD_p(x)=[∑_{i∈S}x_i≡0 mod p]=1−(∑X_i)^{p−1}` exactly. **`modpPoly`/`modpBool`**, **`modp_exact_eval`** (exact, error 0, via `set s` + case-split `s=0`/`zero_pow`/Fermat), **`modpPoly_totalDegree_le`** (`≤p−1`, `totalDegree_pow`), **`modp_mem_monoAND_span`** (degree-`≤(p−1)` monoAND span over `F_p`, via `lowDegPolyEval_mem_monoAND_span` at prime `p`). Composes with degree `p−1`, error 0; prime powers → `F_{p^k}` ⇒ full `AC⁰[p^k]`. Honest: prime only; composite `m=p·q` pins Fermat exponent to `p−1≠m−1` ⇒ no single field (`mod6_eq_mod2_and_mod3`, `modq_residue_image_not_subspace`). Classical `AC⁰[p]`; NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0ModPExact` |
| **YBT exact normal-form socket: exact SYM∘AND closure (mixed-radix)** | Discharges the EXISTENCE half of `HasExactSymAndForm` (`…ACC0YBTSocket`) via Beigel–Tarui base-`(m+1)` counting. `HasSymAndForm f s` (SYM count gate over ≤s monomial-ANDs, size-tracked); `saCount_sum_elim` (disjoint union → additive count), `saCount_prod_const`/`fin_sum_const_nat` (replicate r× → r·count); base `hasSymAndForm_const`/`_monoAND`/`_var`/`_mod` (MOD_q = symmetric fn of support literals, exact, size |S|); `hasSymAndForm_not`; **`hasSymAndForm_combine`** (closed under ANY binary Bool combiner: merged count `C=c1+(m1+1)c2`, decode mod/div `(m1+1)`, size `s1+(s1+1)s2`) → `_and`/`_or`; **`acc0circuit_hasSymAndForm`** (EVERY ACC0Circuit has exact SYM∘AND form, structural recursion, size `symAndSize C`); **`acc0circuit_hasExactSymAndForm`** (reindex to Fin m via `Fintype.equivFin` ⇒ the socket, when `symAndSize C+1<2^n`). Lean gotchas: Fintype instance diamond → `letI` (not haveI, for zeta-defeq) + re-derive hfe/hcard at ambient; `Finset.sum_const` won't HO-match a nested sum under a binder → `fin_sum_const_nat` (induction) applied with the constant given EXPLICITLY. Honest: EXACTNESS proved; SIZE is the wall — `symAndSize` multiplicative at AND/OR ⇒ exponential, socket fires only when `<2^n` (not poly-size). Quasipoly-exact (true YBT) needs additive degree (`…ACC0AdditiveDegree`), only approximate (`…ACC0ApproxConsequence`), prime-power MOD only. Converts the socket to the sharp residue 'exact+quasipoly at once'. NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0YBTExactCompose` |
| **split UniformWilliamsRealizationSocket into sub-sockets** | Same surgery as YBT: factor the monolith `(∀ n, MixedACCResidueSatSpeedup n) → NEXPnotACC0` (`…ACC0WilliamsCashout`) into a named chain, prove composition+self-audit, ground the routine arithmetic. **`EncodingSocket`** (S1, routine: cell search → machine), **`CostBridgeSocket`** (S2, routine: cell count <2^n → runtime 2^{n-n^ε}), **`UniformitySocket`** (S3, standard: uniform family), **`TimeHierarchySocket`** (S4, deep: Williams NTIME hierarchy method). **`realization_socket_factors`** (S1∧S2∧S3∧S4 ⇒ the monolith); **`routine_reduce_to_timeHierarchy`** (given S1,S2,S3 ⇒ socket reduces to S4 alone); **`timeHierarchy_socket_iff_separation`** (self-audit: S4 ⟺ NEXP⊄ACC⁰, NO axioms/pure logic — S4 carries all difficulty); **`cell_count_savings`** (cost-bridge ARITHMETIC core proved via SpeedupMargin: steps ≤2^{n-k} ⇒ 2^k·steps ≤2^n); **`proved_speedup_is_trivial_savings`** (the proved steps<2^n is only k=0 trivial savings; super-poly k=n^ε needs quasipoly exact form ⇒ links back to `…ACC0YBTExactCompose` size wall). Honest: S1+S2-arith routine/grounded, S3 standard, S4 = separation-strength. NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0WilliamsRealizationSplit` |
| **ACC⁰ frontier summary: dependency graph as Lean theorems** | Readable/referee-proof re-export of the boundary map. `ac0_approximation_quantitative` (=approximable_full), `modp_exact_low_degree` (=modp_exact_eval), `exact_symAnd_decoding` (=acc0circuit_hasSymAndForm), `fastSat_quantitative` (=symAnd_williams_fastSat .2.2), `timeHierarchy_is_the_separation` (=timeHierarchy_socket_iff_separation). **`williams_route_reduces_to_two_sockets`** (HEADLINE): the whole route is ONE conditional theorem — given (A) the exact-quasipoly SYM∘AND / depth-reduction socket + routine S1,S2,S3 + (B) the time-hierarchy socket ⇒ NEXPnotACC0 (composes residue_cashout_bundled + routine_reduce_to_timeHierarchy). Only non-routine inputs = the two named walls. NOT `NEXP⊄ACC⁰`, NOT `P≠NP`  **Now also folds in the N-Frame route**: `nframe_bridge` (collapse ⇒ low correlation, proved), `nframe_collapse_composes` (lift through a layer), `nframe_unconditional_disjoint` (discharged fragment), `nframe_lower_bound` (holonomy lower bound from ONE socket — N-Frame analogue of the Williams two-socket reduction, sharper since the bridge is proved).  **Also folds in the rank route** (the sharpened N-Frame route): `rank_bridge` (`2^{cellRank} < |L| ⇒` low correlation), `rank_subsumes_survivor` (`cellRank ≤ survivingCount`), `rank_fragment_equal_supports` (equal supports, any `k`, unconditional), `rank_random_restriction` (`p`-biased low-rank live set), `rank_whp` (two-event intersection on the rank tail, subsumes the survivor whp route) — open: `ACC0ForcesLowCellRank` = the rank-flavoured switching lemma. | `…ACC0FrontierSummary` |
| **restricted YBT: bounded-leaf ACC⁰ has exact QUASIPOLY SYM∘AND** | Genuine restricted progress on the size wall via a multiplicative identity. psize/leafCount/maxBase; **`symAndSize_succ_eq_psize`** (symAndSize C + 1 = psize C = PRODUCT of leaf base sizes: const 1/var 2/not id/and·or product/mod |S|+1; induction+ring); **`psize_le`** (psize C ≤ maxBase C ^ leafCount C); **`restricted_acc0_has_exact_symAnd`** (psize C<2^n ⇒ HasExactSymAndForm C); **`restricted_acc0_quasipoly`** (leafCount≤ℓ ∧ maxBase≤B ∧ B^ℓ<2^n ⇒ HasExactSymAndForm C; ℓ=O(log n),B=poly ⇒ B^ℓ=2^{O(log²n)} quasipoly<2^n ⇒ bounded-leaf poly-MOD-support ACC⁰ has exact quasipoly SYM∘AND). Honest: quasipoly only for LOGARITHMIC leaf count; full ACC⁰ (poly leaves) = B^poly exponential = the wall unchanged. Real fragment. NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0RestrictedYBT` |
| **bounded-overlap MOD: small support footprint ⇒ exact SYM∘AND** | Sharp footprint criterion via the same psize identity. **`baseSum`** (total support footprint: mod→|S|, var→1, const→0, not→id, and/or→sum); `succ_le_two_pow` (k+1≤2^k, inline induction); **`psize_le_two_pow_baseSum`** (psize C ≤ 2^(baseSum C), since base b ≤ 2^{b-1}); **`acc0_exact_of_baseSum_lt`** (baseSum C < n ⇒ HasExactSymAndForm C — SHARP since baseSum = log₂ psize); **`andOfModList`/`baseSum_andOfModList`** (depth-2 AND of MOD_q list, footprint = ∑|S_i|); **`boundedOverlap_mod_exact`** (∑|S_i| < n ⇒ exact SYM∘AND); disjoint chain `disjoint_foldr_union`+`sum_card_eq_card_union`+`sum_card_le_of_pairwise_disjoint`+**`disjoint_mod_footprint_le`** (pairwise-disjoint supports ⇒ footprint ≤ n); **`disjoint_mod_exact`** (disjoint MOD not covering all n vars ⇒ exact form). Lean: `List.mem_cons_self` now nullary → `List.mem_cons.mpr (Or.inl rfl)`; map_map conversion via `simp [List.map_map]`. Honest: sharp for mixed-radix but caps at <n (disjoint/bounded-overlap MOD = footprint <n); general ACC⁰ reads all vars many times (footprint≫n) untouched. NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0BoundedOverlapMOD` |
| **restricted Williams speedup: small footprint ⇒ super-poly savings** | Cashes restricted exact-form (entries 64,65) into a real Williams-style speedup. **`restricted_acc0_searchable`** (every ACC0Circuit: SAT decided by count-cell search, cells ≤ psize C; via acc0circuit_hasSymAndForm + observed_sat_iff + saCount_le_card image⊆range + symAndSize_succ_eq_psize); **`restricted_williams_speedup`** (baseSum C + k ≤ n ⇒ SAT in ≤2^(n-k) cells, savings 2^k·cells≤2^n via psize_le_two_pow_baseSum + SpeedupMargin.savings_ge_of_work_le); **`restricted_savings_by_footprint`** (parametric: savings exponent = EXACTLY n - baseSum C; footprint=polylog ⇒ 2^(n-polylog) super-poly); **`boundedOverlap_mod_williams_speedup`**/**`disjoint_mod_williams_speedup`** (AND-of-MOD_q, bounded total support / pairwise-disjoint |⋃Sᵢ|+k≤n ⇒ savings 2^k). Lean: annotate lambdas `(fun g : Finset (Fin n) × ZMod q => g.1)` in signatures since `.card`/`∪` don't pin element type; letI for HasSymAndForm instance. Honest: REAL super-poly-savings ACC⁰-SAT in count-cell model, ONLY footprint<n fragments; feeds realization split as concrete fastSat instance; does NOT discharge TimeHierarchySocket or full-ACC⁰ size wall. NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0RestrictedWilliamsSpeedup` |
| **bounded-depth fragment: depth d ⇒ ≤2^d leaves ⇒ poly exact SYM∘AND + speedup** | Controls restricted-YBT by DEPTH (vs leaf count/footprint), same psize lever. **`leafCount_le_two_pow_depth`** (leafCount C ≤ 2^(depth C); induction, and/or via 2^m+2^m=2^(1+m) [pow_add+pow_one+ring]); **`psize_le_of_depth`** (depth≤d ∧ maxBase≤B ⇒ psize ≤ B^(2^d), poly for const d/poly B); **`bounded_depth_exact`** (B^(2^d)<2^n ⇒ HasExactSymAndForm); **`restricted_williams_speedup_of_psize`** (psize≤2^(n-k) ⇒ savings 2^k, psize-parametric — generalizes the baseSum version); **`bounded_depth_williams_speedup`** (depth≤d ∧ maxBase≤B ∧ B^(2^d)≤2^(n-k) ⇒ SAT in ≤2^(n-k) cells, savings 2^k; const d/poly B ⇒ B^(2^d)=2^(O(log n)) ⇒ k=n-O(log n) super-poly). Honest: depth is BINARY (and/or 2-ary) so depth-2=≤4 leaves (small balanced); classic unbounded-fan-in depth-2 = deep binary chain (linear leaves) covered by leaf-count/footprint fragments instead. Complementary fragment. NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0BoundedDepth` |
| **uniformity bookkeeping: restricted constructions lift to uniform families** | Makes the STRUCTURAL half of UniformitySocket concrete. **`ACC0Family`** := (n:ℕ)→ACC0Circuit n (single function = manifestly uniform); **`UniformFootprint`/`UniformLeaf`/`UniformBase`/`UniformDepth`** (bound = single ℕ→ℕ). **`uniform_symAndSize_bound`** (uniform SYM∘AND translation: symAndSize(F n)+1 ≤ 2^(b n) — exact-form size bounded by ONE function of n; via symAndSize_succ_eq_psize + psize_le_two_pow_baseSum); **`uniform_exact_of_footprint`/`uniform_exact_of_depth`** (whole family has exact SYM∘AND forms, pointwise acc0_exact_of_baseSum_lt / bounded_depth_exact); **`uniform_williams_speedup`/`uniform_depth_williams_speedup`** (whole family gets count-cell speedup, uniform savings exponent k:ℕ→ℕ; pointwise restricted_williams_speedup / bounded_depth_williams_speedup). Honest: STRUCTURAL uniformity (one function/recursion over all n) discharges that half of S3; does NOT define TMs ⇒ NOT machine uniformity (poly-time TM emitting cells) = stays socket open content (EncodingSocket/CostBridgeSocket). NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0UniformFamily` |
| **N-Frame cell-collapse route: bridge proved, restriction lemma socketed** | States the N-Frame route to a full-ACC⁰ correlation bound, isolating the single hard statement. **`CellCollapse`** supports L := 2^(survivingCount supports L) < L.card; **`LowHolonomyCorrelation`** := predictor g∘weightVec agrees with fParity D ≤ half on off-diagonal. **`cell_collapse_implies_low_holonomy_correlation`** = THE BRIDGE, PROVED (re-export of `ACCSwitchingPipeline.predictor_fails_of_survivors`). **`FullACC0ForcesCellCollapse`** = the HARD open socket (N-Frame switching/restriction lemma = same content as `RestrictionTreeSwitch`, NP⊄ACC⁰-strength). **`cell_collapse_of_survival`** (PROVED reduction: <m survivors on |L|≥2^m ⇒ collapse; open content = "<log₂|L| survivors on large L"). **`nframe_route`** (PROVED composition: socket ⇒ low correlation). Honest: probabilistic half proved (`exists_low_survival`); quantitative switching bound for full ACC⁰ open, blocked for naive leaf-switching by the proved MOD no-go. Pins the target; does NOT prove it. NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0CellCollapseRoute` |
| **quantitative survival bound for bounded fragments — N-Frame route closed UNCONDITIONALLY** | Discharges the `FullACC0ForcesCellCollapse` socket for two bounded fragments, making the N-Frame route unconditional there. **`survivingCount_le_card`** (≤ k supports survive any live set, `card_filter_le`). **`bounded_gate_forces_cell_collapse`** (`2^k < n ⇒` collapse; live set `univ`, `2^survivors ≤ 2^k < n`). **`survivingCount_eq_zero_of_disjoint`** + **`small_footprint_forces_cell_collapse`** (footprint `+ 2 ≤ n ⇒` collapse; live set = complement of supports' union, `0` survivors, `|L| = n−|⋃| ≥ 2`; via `compl_eq_univ_sdiff`+`card_compl`). **`bounded_gate_low_holonomy_correlation`** / **`small_footprint_low_holonomy_correlation`** (compose with the proved bridge `nframe_route`: predictor does NOT correlate with the holonomy parity — UNCONDITIONAL, no socket). Honest: bounded fragments only (`k < log₂ n` gates, or footprint `≤ n−2`); full `ACC⁰` has poly-many wide supports ⇒ `survivingCount` large on any live set ⇒ collapse fails = the wall, unchanged. Real unconditional low-correlation bound FOR THE FRAGMENTS. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundedSurvival` |
| **bounded-overlap (disjoint-support) cell collapse — N-Frame route discharged again** | The bounded-overlap regime at its clean extreme (pairwise-disjoint supports, overlap 0); works even when supports cover all `n` variables (unlike small-footprint). **`disjoint_supports_forces_cellCollapse`** — pairwise-disjoint supports + some `|S_{j₀}| ≥ 3` ⇒ `FullACC0ForcesCellCollapse` (live set `S_{j₀}`; only it meets itself so `survivingCount = 1`, `2^1 = 2 < |S_{j₀}|`; survivor filter `= {j₀}` via `not_disjoint_iff` + `card_pos`). **`disjoint_supports_low_holonomy_correlation`** — compose with the proved bridge (`nframe_route`): predictor does NOT correlate with the holonomy parity, unconditionally, no socket. Honest: overlap-0 (disjoint) only; degree-`d` overlap (`d ≥ 2`) is NOT forced into collapse by the overlap bound alone (`≤ d·|L|` supports meet any `L` ⇒ `2^{d·|L|} < |L|` impossible) — `d ≥ 2` needs a probabilistic restriction (second-moment cell bridge, socketed in `bounded_overlap_acc0_low_correlation_whp`), the frontier. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DisjointCollapse` |
| **the N-Frame ACC⁰ lower bound, stated beside Williams — one socket, bridge proved** | Packages the whole N-Frame route as ONE conditional theorem (Williams = two sockets; N-Frame = one, since collapse ⇒ low correlation is already proved). `PredictorClass ι n := ι → Σ k, Fin k → Finset (Fin n)`; **`NFrameCellCollapse sys`** = the hard open socket (every predictor's supports collapse on some live set); **`ACC0HolonomyLowerBound sys tops`** = the conclusion (every predictor fails to correlate with some holonomy parity). **`nframe_acc_lower_bound`** (HEADLINE, proved): `NFrameCellCollapse sys ⇒ ACC0HolonomyLowerBound sys tops` (one-socket reduction via `nframe_route` pointwise; N-Frame analogue of `williams_route_reduces_to_two_sockets`). **`nframe_lower_bound_disjoint`** — the socket DISCHARGED for disjoint-support classes ⇒ UNCONDITIONAL N-Frame lower bound (conditional not vacuous). Honest: the socket is the open `NP ⊄ ACC⁰`-strength content — discharged for bounded fragments, FALSE for arbitrary dense overlapping supports (the wall); genuine open statement = cell collapse for real `ACC⁰` under restriction (the switching lemma). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NFrameLowerBound` |
| **`collapse_lifts_through_layer` — cell collapse composes under a survivor budget** | The composition direction of the N-Frame route: stacking a layer appends its supports (`Fin.append`). **`survivingCount_append`** — survivor counts ADD across the layer (`survivingCount (append supp₁ supp₂) L = survivingCount supp₁ L + survivingCount supp₂ L`; `finSumFinEquiv` + `Fintype.sum_sum_type` + `Fin.append_left/right` + `card_filter`). **`collapse_lifts_through_layer`** (`2^{s₁+s₂} < |L| ⇒ CellCollapse (append) L`). **`collapse_lifts_of_budget`** (controlled-parameter form: layer survivors `≤ δ`, base slack `2^{s₁+δ} < |L|` ⇒ lift). **`full_collapse_lifts`** (packaged as `FullACC0ForcesCellCollapse (append …)`). Honest: collapse composes ADDITIVELY in survivors (multiplicatively in cell count), so it lifts only when the new layer adds FEW survivors (budget `δ`, `s₁+δ < log₂|L|`); a wide `MOD` layer surviving on most of `L` blows the budget = the wall. Supplying the small budget for a real `ACC⁰` layer under restriction = the switching lemma (blocked for leaf-switching by the proved `MOD` no-go). Proves collapse composes + lifts under budget; does NOT supply the budget for full `ACC⁰`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CollapseLift` |
| **rank-based cell collapse — the sharper collapse (rank, not survivor count)** | For overlapping supports many gates survive but induce few distinct cells; the cell count is governed by the `F₂`-RANK of the support incidence. **`incidenceVec`/`survivorIncidence`/`supportRank`** (`F₂` incidence vectors of surviving supports + dim of their span, `Set.finrank`). **`supportRank_le_survivingCount`** (rank ≤ #generators, via `finrank_span_finset_le_card` + `card_image_le`). `RankCellCollapse supports L := 2^{supportRank} < |L|` (sharp). **`cell_collapse_implies_rank_collapse`** (`CellCollapse ⇒ RankCellCollapse` — correct direction; rank ≤ survivors ⇒ rank collapse is WEAKER / more achievable; the rank→cell direction is the flip, false in general). **`FullACC0ForcesRankCollapse`** (weaker socket) + **`full_cell_collapse_implies_full_rank_collapse`** (survivor socket ⇒ rank socket). Honest: the rank route WEAKENS the open socket and is the correct cell count for `MOD₂`/overlapping supports (`#parity cells = 2^rank = parity_reachable_card`, `…ACC0ParityRankCardinality`). Genuine next target = sharp bridge `RankCellCollapse ⇒ low correlation` (pigeonhole with `#cells ≤ 2^rank`), NOT from composing the survivor bridge. Forcing `supportRank < log₂|L|` for real `ACC⁰` = the rank-flavoured switching lemma, open `NP ⊄ ACC⁰`-strength. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankCollapseRoute` |
| **the sharp rank bridge: 2^rank < |L| ⇒ low holonomy correlation** | A GENUINE theorem (no socket) — the correlation bridge with the sharp RANK cell count, not the crude survivor count. `cellPatternVec supports v := fun j => if v∈supports j then 1 else 0` (column of incidence); **`sameCell_iff_pattern`** (`SameCell ⟺ cellPatternVec v = cellPatternVec w`); `cellSpan`/`cellRank` (F₂-span of cell patterns over L + its dim); **`cellPattern_image_card_le`** (#distinct cell patterns `≤ 2^cellRank` — patterns ⊆ span, `|span| = 2^finrank` via `Module.card_eq_pow_finrank` + `ZMod.card`, the `parity_reachable_card` technique); **`exists_sameCell_pair_of_rank`** (`2^cellRank < |L|` ⇒ two live coords share a cell, pigeonhole); **`rank_collapse_low_correlation`** (THE SHARP BRIDGE: `2^cellRank < |L| ⇒ LowHolonomyCorrelation`, via `CellWitness` + the proved `cellWitness_gives_low_correlation`). Strictly sharper than the survivor bridge: `cellRank` (column rank) `≤ survivingCount` (row count), far smaller for overlapping supports. Self-contained, no socket. Open content unchanged: FORCING `cellRank < log₂|L|` for real `ACC⁰` under restriction = the rank-flavoured switching lemma (`NP ⊄ ACC⁰`-strength). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP`  **Cleanup (proved): the rank bridge strictly subsumes the survivor bridge** — `cellPatternVec_mem_survivorSpan` (a cell pattern lies in the survivor-singles span, vanishing off survivors), `cellRank_le_survivingCount` (column rank ≤ row count, via `Submodule.finrank_mono` + `finrank_span_finset_le_card`), `survivor_collapse_implies_rank_collapse` (`2^survivingCount < |L| ⇒ 2^cellRank < |L|`). The full hierarchy is now machine-checked: few survivors ⇒ low rank ⇒ low cell count ⇒ same-cell witness ⇒ low holonomy correlation. | `…ACC0RankBridge` |
| **low-rank fragment of `acc0_restriction_forces_low_cellRank` — rank wins, survivors lose** | Discharges the rank socket for low-rank systems the survivor route cannot touch. `ACC0ForcesLowCellRank supports := ∃ L, 2^{cellRank} < |L|`; **`low_cellRank_low_correlation`** (socket ⇒ low correlation, ▸ the proved rank bridge); **`bounded_cellRank_univ_forces`** (`2^{cellRank univ} < n ⇒` socket); **`equal_supports_cellRank_le_one`** (equal supports ⇒ `cellRank ≤ 1`, patterns ∈ span{𝟙}, INDEPENDENT of gate count `k`); **`equal_supports_low_correlation`** (equal supports, any `k`, `n ≥ 3` ⇒ predictor fails to correlate, UNCONDITIONAL). Punchline: equal supports = all `k` gates survive (`survivingCount = k`, survivor route powerless), yet rank `≤ 1` so the rank route still gives the bound — proof the rank reformulation strictly strengthens. Honest: a fragment; general low-rank under restriction = open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LowRankFragment` |
| **random restriction toward low cell rank — probabilistic stepping stones** | Transfers the `p`-biased survivor concentration to cell RANK via pointwise `cellRank ≤ survivingCount`. **`expected_cellRank_le_expected_survivors`** (`Exp p (cellRank) ≤ Exp p (survivingCount)`, monotone expectation); **`randomRestriction_forces_low_cellRank`** (expected survivors `≤ B < a ⇒ ∃ L, cellRank < a`, via `exists_low_survival` + `cellRank ≤ survivingCount`). Honest: stepping stones — give `cellRank < a` (real threshold); the socket needs `2^{cellRank} < |L|` (`cellRank < log₂|L|` with `|L|` large simultaneously) = the open rank switching lemma. Supplies the probabilistic inputs (bounded-overlap `E[survivors]` bound ⇒ small `E[cellRank]` + low-rank `L`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RandomRestrictionRank` |
| **bounded-fan-in instantiation: low-cell-rank live set + the exact |L|-balance gap** | Instantiates the probabilistic rank machinery with the proved first-moment bound `exp_survivingCount_le` (`Exp p (survivingCount) ≤ k·s·p`). **`boundedFanin_forces_low_cellRank`** (fan-in `≤ s`, `k·s·p < a` ⇒ `∃ L, cellRank supports L < a`); **`heavy_restriction_forces_rank_zero`** (`k·s·p < 1` ⇒ `∃ L, cellRank = 0`). Honest: supplies the LOW-RANK half of the socket unconditionally for bounded fan-in (`cellRank < a`, real); does NOT close `2^{cellRank} < |L|` — the gap is precise, the Markov live set is NOT guaranteed large (`L = ∅` has `survivingCount = 0`, `cellRank = 0`, but `|L| = 0`, so socket fails). Need `L` simultaneously low-rank AND large (`|L| > 2^{cellRank}`) — the heavy-vs-light restriction tension, requiring two-sided concentration (lower-tail on `|L|`, not yet in corpus) + the structural rank input for general `ACC⁰` = the open rank-flavoured switching lemma. Pins exactly that balance. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundedOverlapRank` |
| **the rank whp route — two-event intersection with the rank tail (subsumes survivor whp)** | The rank version of `bounded_overlap_predictor_fails_whp`, with strictly weaker feasibility. **`Pr_mono`** (`E ⊆ F ⇒ Pr E ≤ Pr F`, `sum_le_sum_of_subset_of_nonneg`); **`rank_predictor_fails_whp`** (`2^a ≤ b ∧ Pr[cellRank ≥ a] + Pr[|L| ≤ b] < 1 ⇒ LowHolonomyCorrelation`, via `exists_both_of_pr_add_lt_one` ⟶ `2^{cellRank} < |L|` ⟶ sharp rank bridge); **`rank_feasibility_le_survivor`** (`Pr[cellRank ≥ a] + Pr[|L| ≤ b] ≤ Pr[survivingCount ≥ a] + Pr[|L| ≤ b]`, since `{cellRank ≥ a} ⊆ {survivingCount ≥ a}`); **`rank_whp_of_survivor_feasible`** (survivor feasibility `< 1` ⇒ rank route fires — STRICTLY SUBSUMES the survivor whp theorem). The two-event intersection (low-rank ∧ large-`L`) now uses the *rank* tail. Honest: the feasibility `Pr[cellRank ≥ a] + Pr[|L| ≤ b] < 1` with `2^a ≤ b` is still the open balance for poly-many overlapping gates (bounding `Pr[cellRank ≥ a]` = the structural rank input). Sharpest whp reduction; does not close it. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankWhp` |
| **structured case of ACC0ForcesLowCellRank — bounded distinct supports** | Discharges the rank socket for ≤ `d` distinct supports, regardless of gate count `k`. `classVec supports T := fun j => if supports j = T then 1 else 0` (support-class indicator); **`cellPatternVec_mem_classSpan`** (cell pattern ∈ span of the `≤ d` class indicators — constant on each support-class, `∑_T (v∈T)·classVec T` via `sum_eq_single_of_mem`); **`cellRank_le_distinct`** (`cellRank ≤ |image supports|`); **`bounded_distinct_forces_low_cellRank`** (`|image| ≤ d ∧ 2^d < n ⇒ ACC0ForcesLowCellRank`); **`bounded_distinct_low_correlation`** (⇒ predictor fails, unconditional, any `k`). Generalizes equal supports (`d=1`); survivor route powerless (`survivingCount = k`), rank route wins (`cellRank ≤ d`). Honest: a fragment — general `ACC⁰` has poly-many *distinct* wide supports (`d` large, `2^d > n`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundedDistinctRank` |
| **low-dimensional support-span ⇒ low cell rank — the natural next N-Frame theorem** | Generalizes bounded-distinct by bounding cell rank via the `F₂`-DIMENSION of the support indicators. `SupportsInRankSpan supports r := ∃ B c, ∀ j i, (i ∈ supports j) = ∑_m c j m · B m i` (indicators are `F₂`-combos of `r` fixed vectors); **`cellRank_le_of_span`** (`SupportsInRankSpan r ⇒ cellRank ≤ r`; `cellPatternVec v = ∑_m (B m v)·(c_·m)` ∈ span of `r` coefficient vectors — the column-rank `≤` row-rank bound made CONSTRUCTIVE via explicit coefficients, no abstract row=col rank); **`supportSpan_forces_low_cellRank`** (`SupportsInRankSpan r ∧ 2^r < n ⇒ ACC0ForcesLowCellRank`); **`supportSpan_low_correlation`** (⇒ predictor fails, unconditional, any `k`). Sharpest structured rank fragment: caps `cellRank` by the support-family *dimension*, subsuming bounded-distinct (`r ≤ #distinct`) and equal supports (`r = 1`). Honest: a fragment — general `ACC⁰` indicators span high dimension (`r ~ poly`, `2^r > n`); bounding support-span dim under restriction = the open rank switching lemma. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SupportSpanRank` |
| **rank growth through layers — `cellRank` is subadditive under `append`** | The depth-direction rank composition law. `embedFst`/`embedSnd` (`F₂`-linear inclusions `Fin k_i → Fin(k₁+k₂)` extending by zero; `map_add'`/`map_smul'` via `Fin.addCases` + `Fin.append_left/right`); `cellPatternVec_append_eq` (append pattern = `embedFst p₁ + embedSnd p₂`); **`cellRank_append_le`** (`cellRank (append supp₁ supp₂) L ≤ cellRank supp₁ L + cellRank supp₂ L`; `cellSpan(append) ≤ map embedFst cellSpan₁ ⊔ map embedSnd cellSpan₂`, `finrank_sup_add_finrank_inf_eq` + `Submodule.finrank_map_le`); **`rank_collapse_lifts_of_rank_budget`** (`2^{r₁+r₂} < |L| ∧ cellRank₁ ≤ r₁ ∧ cellRank₂ ≤ r₂ ⇒ 2^{cellRank(append)} < |L|`). Turns depth composition into RANK-budget accounting (vs survivor-budget): a layer adding only `r₂` observer degrees of freedom keeps composite rank `≤ r₁+r₂` — the sharpened analogue of the survivor `collapse_lifts_through_layer`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP`  **(also re-exported in `…ACC0FrontierSummary` as `rank_append_subadditive` / `rank_collapse_lifts_budget`).** | `…ACC0RankComposition` |
| **direct cell-count collapse route — the sharpest observer collapse (cells, not rank)** | The collapse keyed on the actual number of observer cells, strictly generalizing the rank route. `cellPatternImage`/`cellPatternCount`; `CellCountCollapse supports L := cellPatternCount < |L|`. **`exists_sameCell_pair_of_count`** (bare pigeonhole, no rank bound); **`cellCountCollapse_implies_low_correlation`** (`CellCountCollapse ⇒ LowHolonomyCorrelation`); **`rank_collapse_implies_cellCount_collapse`** (`2^{cellRank} < |L| ⇒ CellCountCollapse`, since `cellPatternCount ≤ 2^{cellRank}` — SUBSUMES the rank route). Hierarchy: few survivors ⇒ low rank ⇒ few cells ⇒ same-cell witness ⇒ low correlation. Open target sharpens to `ACC0ForcesLowCellCount`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountRoute` |
| **chain (nested) supports — few cells even at full rank** | The class the rank route misses: nested supports `S_1 ⊆ … ⊆ S_k` have FULL rank but `≤ k+1` cell patterns. `ChainSupports`/`suppSet`; **`suppSet_comparable`** (chain membership sets pairwise comparable, by_contra + chain comparability); `oneSet_eq_suppSet`/`cellPatternVec_eq_of_suppSet` (pattern↔membership dictionary); **`chain_cellPatternCount_le`** (`ChainSupports ⇒ cellPatternCount ≤ k+1`; card map injective on comparable patterns via `card_le_card_of_injOn` + `eq_of_subset_of_card_le`); **`chain_low_correlation`** (`ChainSupports ∧ k+1 < |L| ⇒ LowHolonomyCorrelation`, regardless of rank). The honest correction: for nested/laminar, cell COUNT (not rank) is the right observer invariant. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ChainCellCount` |
| **clustered supports — `cellRank ≤ d + r` (corollary of support-span)** | Clustered = `d` cluster centers + `r`-dim variation: `(i∈supports j) = ∑ cc·center + ∑ cv·V`. **`ClusteredSupports`**; **`clustered_supports_in_span`** (⇒ `SupportsInRankSpan supports (d+r)`; `B = append center V`, `c = append cc cv`, `finSumFinEquiv` sum-split + `Fin.append_left/right`); **`clustered_supports_low_rank`** (`cellRank ≤ d+r` via `cellRank_le_of_span`); **`clustered_low_correlation`** (`2^{d+r} < n ⇒` low correlation, any `k`). Caps observer rank by `#clusters + variation rank`; subsumes bounded-distinct (`r=0`) and equal supports (`d=1,r=0`). A cheap genuine corollary. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ClusteredRank` |
| **cell-count composition — `cellPatternCount` submultiplicative under `append`** | The cell-count analogue of rank composition. **`cellPatternVec_append`** (append pattern = concatenation `Fin.append (cPV A v)(cPV B v)`); **`cellPatternCount_append_le`** (`cellPatternCount (append A B) L ≤ cellPatternCount A L * cellPatternCount B L`; concatenation injective ⇒ `image ⊆ (img_A ×ˢ img_B).image append`, `card_image_le` + `card_product`); **`cellCount_collapse_lifts`** / **`cellCount_collapse_of_budget`** (`c₁·c₂ < |L| ⇒ CellCountCollapse (append A B) L`). Depth composition MULTIPLIES cell counts (vs additive survivor, subadditive rank). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountComposition` |
| **full cell-count switching socket — `FullACC0ForcesLowCellCount ⇒ holonomy LB`** | The SHARPEST (weakest, most achievable) N-Frame switching socket. **`FullACC0ForcesLowCellCount supports := ∃ L, cellPatternCount supports L < \|L\|`** (the open socket); **`nframe_cellcount_route`** (socket ⇒ `LowHolonomyCorrelation`, proved bridge); **`cellCollapse_implies_lowCellCount`** (survivor socket ⇒ cell-count socket, since `cellPatternCount ≤ 2^cellRank ≤ 2^survivingCount` — it is the WEAKEST socket); **`NFrameLowCellCount`** / **`nframe_cellcount_lower_bound`** (over a predictor class, socket ⇒ `ACC0HolonomyLowerBound`); **`nframe_lowCellCount_of_cellCollapse`** (survivor class-socket ⇒ cell-count class-socket — NOT vacuous, every disjoint/bounded discharge carries over). The official final target. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountSwitching` |
| **low VC dimension ⇒ few observer cells (Sauer–Shelah)** | The cell patterns ARE the gate-membership sets `{suppSet v : v ∈ L} ⊆ 2^(Fin k)`, so cell count = set-family size, bounded by Sauer–Shelah. The common generalization of bounded-distinct/clustered/laminar — *few observer patterns, not low rank* (the N-Frame observer idea). **`cellVCdim supports L`** := `(L.image (suppSet supports)).vcDim`; **`patternFamily_card`** (`\|{suppSet v}\| = cellPatternCount`, one-set bijection + `card_image_of_injOn`); **`lowVC_cellPatternCount_le`** (`cellVCdim ≤ d ⇒ cellPatternCount ≤ ∑_{i≤d} C(k,i)`, Mathlib Pajor `card_le_card_shatterer` + Sauer–Shelah `card_shatterer_le_sum_vcDim`); **`lowVC_cellCountCollapse`** / **`lowVC_low_correlation`** (`∑_{i≤d} C(k,i) < \|L\| ⇒` collapse ⇒ low corr). Converts `ACC0ForcesLowCellCount` to "bound observer VC under a restriction" for wide overlapping `MOD`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0VCCellCount` |
| **laminar (nested-or-disjoint) supports — `≤ k+1` cells** | Generalizes chains: any two supports nested OR disjoint, still `≤ k+1` cells despite possibly full rank. **`LaminarSupports`**; **`chain_isLaminar`** (chains ⊂ laminar); **`laminar_suppSet_eq`** (for `⊆`-minimal-card `j∈suppSet v`, `suppSet v = {i : supports j ⊆ supports i}` the up-set); **`laminar_cellPatternCount_le`** (every pattern is an up-set indicator or `∅`, `≤ k+1`; `card_le_card_of_injOn` into `insert ∅ (univ.image U)`, `exists_min_image`); **`laminar_low_correlation`** (`k+1 < \|L\|` ⇒ low correlation, regardless of rank). A genuine strengthening — the cardinality-injection trick of chains fails (laminar `suppSet`s not pairwise comparable). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LaminarCellCount` |
| **block-product supports — cell count is the PRODUCT of per-block counts** | `m` blocks of `b` gates (`supports : Fin m → Fin b → Finset (Fin n)`); cell count multiplies. **`blockCellCount`** / **`flatSupports`** (flattened to `Fin (m·b)` via `finProdFinEquiv`); **`blockCellCount_le_prod`** (`≤ ∏ i, cellPatternCount (supports i) L`; tuple map ↪ `Fintype.piFinset`, `Fintype.card_piFinset`); **`blockCellCount_le_pow`** (each block `≤ c` ⇒ `≤ cᵐ`); **`cellPatternCount_flat_eq`** (flattening = gate relabelling, `finProdFinEquiv` bijection ⇒ same cells); **`block_product_low_correlation`** (`∏ < \|L\|` ⇒ low correlation). The `m`-fold generalization of `cellPatternCount_append_le`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BlockProductCellCount` |
| **`e_d`-sparsity identity — each binomial moment is a sparse `d`-subset sum** | `C(∑_j b_j, d) = Σ_{|T|=d} ∏_{j∈T} b_j` for `0/1` values (`b_j ∈ {0,1}`). **`boolean_esymm`** (`S = {j:b_j=1}`; `∑b=\|S\|`; `∏_T = [T⊆S]`; `Σ_{|T|=d}[T⊆S] = \|powersetCard d S\| = C(\|S\|,d)`); **`andCount_choose_eq`** (applied to AND-indicators: `C(andCount x,d) = Σ_{\|T\|=d} ∏_{j∈T}[AND_j(x)]`). Makes each moment a kernel-computable sparse sum. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ElementarySymmetric` |
| **the prime-power obstruction, made precise — `MOD_{p^e}` is not a function of the mod-`p` residue** | Pins down *why* the field-indicator route stops at prime powers. **`modPrimePower_not_function_of_modP`** (PROVED): `0` and `p` share the mod-`p` residue (`(0:ZMod p)=(p:ZMod p)=0`) yet `MOD_{p^e}` accepts `0` (`p^e∣0`) and rejects `p` (`¬p^e∣p`, since `p<p^e` for `e≥2`) — so `MOD_{p^e}` is not determined by the mod-`p` residue, hence no `F_p` polynomial in the prime-`p` residue (any degree) computes it. **`modPrimePower_needs_ring`** (the deciding observer lives in the ring `ZMod(p^e)`, not a low-degree field poly). The honest `ACC⁰[composite]` boundary: a low-degree-field prime-power gate would be the open lower bound; obstruction recorded, not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerObstruction` |
| **prime-power observer candidates — refute the field family, confirm the richer (filtered ring / valuation) observers** | Tests the candidate richer observers the tri-aspect boundary could select. **`charP_field_observer_fails`** (PROVED, REFUTES the whole char-`p` field family `F_p, F_{p^k}, …`): in *any* field `F` of characteristic `p`, the count's image depends only on the mod-`p` residue (`(0:F)=(p:F)=0`) yet `MOD_{p^e}` separates `0` and `p` (`e≥2`) — so no characteristic-`p` field computes `MOD_{p^e}` (generalises the `F_p` obstruction to the entire field family). **`ringPrimePower_observer_decides`** (PROVED, candidate #1): the ring `ZMod(p^e)` (characteristic `p^e`, *not* a field, has zero divisors) sees the mod-`p^e` residue and decides `MOD_{p^e}` (`(s:ZMod(p^e))=0 ↔ p^e∣s`). **`valuation_observer_decides`** (PROVED, candidate #2): the `p`-adic valuation decides it — `p^e∣x ↔ e ≤ v_p(x)` (`x≠0`, via `Nat.Prime.pow_dvd_iff_le_factorization`); `MOD_{p^e}(x)⟺v_p(x)≥e`. Honest dichotomy: the field family is OUT, the filtered-ring/valuation observers are IN *information-theoretically*; the open crux is whether any richer observer admits a quasipolynomial low-degree sparse representation (over `ZMod(p^e)` zero divisors break Fermat; valuation is a threshold, not an obvious low-degree poly). N-Frame reading: the boundary must select a FILTERED RING observer, not a field one. Obstruction sharpened, not faked. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerObserverCandidates` |
| **candidate #3 — the layered residue tower `x ↦ (x mod p, …, x mod p^e)`** | Analyses the filtration `ZMod p ← ZMod p² ← … ← ZMod p^e`. **`tower_level_decides`** (PROVED, graded ladder): rung `i` decides `MOD_{p^i}` (`(s:ZMod(p^i))=0 ↔ p^i∣s`) — the tower IS the ladder `{MOD_{p^i}}_{i=1}^e`. **`tower_projection_compatible`** (PROVED, filtration): rung `i` is the ring projection of rung `e` — `ZMod.castHom (h:p^i∣p^e) (ZMod(p^i)) : ZMod(p^e)→+*ZMod(p^i)` commutes with the residue (`map_natCast`), so the top rung determines the whole tower ⇒ the tower is information-EQUIVALENT to candidate #1 (`ZMod p^e`), a *presentation* not a richer observer. **`tower_truncation_insufficient`** (PROVED, depth-`e` required): `0` and `p^{e-1}` agree on every rung `1≤i≤e-1` (both `≡0 mod p^i`, via `pow_dvd_pow`) yet `MOD_{p^e}` accepts `0` and rejects `p^{e-1}` (`¬p^e∣p^{e-1}` since `e-1<e`) — so truncating below level `e` loses `MOD_{p^e}`; the field observer is exactly the failing case `e=1`. Honest: the tower exposes the graded ladder and localises the depth requirement, but does NOT escape the low-degree wall (its top rung is still the zero-divisor ring). No quasipoly sparse rep claimed. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerTowerObserver` |
| **candidate #4 — the Witt-vector / `p`-adic digit observer `x = a₀ + p·a₁ + p²·a₂ + …`** | Records `x` by its base-`p` digits `aᵢ := digit p i x = (x/pⁱ) mod p` rather than residues. **`lowest_digit_eq_mod_p`** (PROVED): `digit p 0 s = s mod p` — the characteristic-`p` field observer sees *only* the lowest digit `a₀`. **`digit_observer_decides`** (PROVED, full digit-peeling induction on `e`, NOT the residue shortcut): `p^e ∣ s ↔ ∀ i < e, digit p i s = 0` — the digit observer decides `MOD_{p^e}` exactly via vanishing of the lowest `e` digits `a₀ … a_{e-1}` (induction peels one factor of `p`: base `p^0∣s`; step uses `s%p=0 ⇒ s=p·s'`, `digit p (i+1) (p·s') = digit p i s'` via `Nat.mul_div_mul_left`, then IH). **`field_digit_insufficient`** (PROVED): `0` and `p` share `a₀ = 0` (`p mod p = 0`) yet `MOD_{p^e}` separates them (`e≥2`) — the lowest digit alone is insufficient; digits `a₁ … a_{e-1}` are needed. Honest: the digit observer is information-EQUIVALENT to `ZMod(p^e)` / the tower (digits ↔ residue determine each other), so it does NOT escape the low-degree wall; its value is exposing the explicit Witt/digit decomposition and localising the field obstruction to the lowest digit. No quasipoly sparse rep claimed. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerDigitObserver` |
| **Route 1 — non-field low-degree theory: why the prime-power indicator resists a low-degree representation** | Tests the open composite-MOD crux honestly: can a non-field observer admit a low-degree representation? Two bounding facts. **`field_root_card_le_natDegree`** (PROVED): over a field, a nonzero polynomial vanishing on a finset of size `k` has `natDegree ≥ k` (distinct roots inject into `g.roots`, `card_roots'`). **`modPrimePower_field_indicator_high_degree`** (PROVED): a char-`0` field polynomial vanishing on `{1,…,p^e-1}` (the `p^e-1` consecutive non-multiples `MOD_{p^e}` must reject) has `natDegree ≥ p^e-1` — exponential in `e`, so the FIELD route cannot give polylog degree (it gives `MOD_p` only by collapsing mod `p` to `{0..p-1}`, degree `p-1`; reaching `p^e` needs reducing mod `p^e`, leaving the category of fields). **`ring_root_count_fails`** (PROVED): over `ZMod(p^e)` (`e≥2`), `C(p^{e-1})·X` has `natDegree ≤ 1` yet two distinct roots `0 ≠ p` (since `p^{e-1}·p = p^e = 0`) — the field root bound is FALSE over the ring, so the degree-bounding machinery has no analogue on the non-field observer. VERDICT: the field route is quantitatively degree-blocked, and the non-field escape has no root-counting obstruction to exploit — which is exactly why the low-degree question is genuinely OPEN (the actual `ACC⁰[composite]` wall, not glue). Neither a low-degree non-field representation nor its impossibility is claimed (either = the open lower bound). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NonFieldObserverTheory` |
| **Route 1 — valuation / `p`-adic sparse theory: the sparse top-structure of `MOD_{p^e}` and the carry barrier** | Isolates *where the cost lives* in the valuation route. **`modPrimePower_eq_and_of_downshift_modP`** (PROVED): `p^e ∣ s ↔ ∀ i < e, p ∣ (s / p^i)` — `MOD_{p^e}` is an `e`-fold AND of `MOD_p` tests on the down-shifted counts `s, s/p, …, s/p^{e-1}` (sparse top, `e` conjuncts; equiv. to the digit decomposition via `digit_observer_decides`). **`downshift_conjunct_decided_by_lowdegree_gate`** (PROVED): each conjunct `p ∣ (s/p^i)` is decided by the degree-`(p-1)` Fermat gate `modPGate p` at the down-shifted count — the modular arithmetic of each test costs only degree `p-1`, no blow-up. **`downshift_breaks_additivity`** (PROVED): `(p-1)/p + (p-1)/p ≠ (2(p-1))/p` for `p≥2` (LHS `=0`, RHS `=1`) — the down-shift `s ↦ s/p` is NOT additive, so it destroys the linear structure of the count `s = ∑xᵢ` that makes `MOD_p` cheap (the cheap polynomial is degree `p-1` in the linear form `∑Xᵢ`); after one down-shift the argument is no longer a linear form, so the higher conjuncts can't reuse the mechanism. VERDICT: the top structure is sparse (`e`-fold AND) and each conjunct's arithmetic is low-degree; the entire residual difficulty is a sparse representation of the down-shift/carry `s/p^i` in the input bits — the carry, not the modulus. Sharper target, not the breakthrough; no quasipoly bit-representation is constructed (= the open LB). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ValuationSparseTheory` |
| **Route 1 — carry sparse theory: the structure of `⌊s/p⌋` and the carry boundary** | Dissects the down-shift `s ↦ ⌊s/p⌋` (the object Route 1's wall reduced to). **`downshift_add_carry_identity`** (PROVED): `⌊(a+b)/p⌋ = ⌊a/p⌋ + ⌊b/p⌋ + ⌊(a%p+b%p)/p⌋` — the non-additivity defect is *exactly* the carry term. **`carry_le_one`** (PROVED): `⌊(a%p+b%p)/p⌋ ≤ 1` (residues `<p` ⇒ sum `<2p`) — the entire obstruction is one bit per addition. **`downshift_additive_no_carry`** (PROVED, restricted positive): `a%p+b%p < p ⇒ ⌊(a+b)/p⌋ = ⌊a/p⌋+⌊b/p⌋` — in a no-carry regime the down-shifted count stays a linear form, so the `MOD_p` test on it is again cheap and the sparse rep goes through (the fragment where Route 1 succeeds). **`carry_not_function_of_downshifts`** (PROVED, obstruction): `(0,0)` and `(p-1,1)` have identical down-shifts `(0,0)` yet carries `0` and `1` — so a sparse theory of `⌊s/p⌋` must track the low residues `a%p,b%p`, not just the down-shifts; the carry couples the residue boundary nonlinearly into the next `p`-adic level. Dynamic-boundary reading: `MOD_p→MOD_{p^e}` forces the observer to refine field-residue→carry-aware `p`-adic filtration. Genuine structure theory of the carry, NOT a representation of it; the general (carry-present) regime is the open `ACC⁰[composite]` LB. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CarrySparseTheory` |
| **Route 1 — count carry symmetric structure: `⌊s/p⌋` as a sum of threshold gates** | For the `ACC⁰` count `s = ∑ᵢxᵢ`, the down-shift tower `s, ⌊s/p⌋, ⌊s/p²⌋, …` are all functions of the *single* `s`, so the residue-coupling obstruction of the general `a+b` carry dissolves. **`count_carry_symmetric`** (PROVED): equal-weight inputs give equal carry `⌊(∑xₖ)/p^i⌋` — the count carry is a *symmetric* function of `s` alone (contrast `…CarrySparseTheory.carry_not_function_of_downshifts`). **`count_carry_threshold_card`** (PROVED): `#{j ∈ [1,N] : j·p ≤ s} = min N ⌊s/p⌋` (`j·p ≤ s ↔ j ≤ ⌊s/p⌋` via `Nat.le_div_iff_mul_le`). **`count_carry_eq_sum_thresholds`** (PROVED): `⌊s/p⌋ = ∑_{j∈[1,N]} [j·p ≤ s]` for `⌊s/p⌋ ≤ N` — the count carry is a sum of `⌊n/p⌋` threshold (Majority-flavoured) indicators `[s ≥ j·p]`, i.e. a `SYM` form (exactly the Beigel–Tarui top structure). **`count_carry_range_le`** (PROVED): `⌊s/p⌋ ≤ ⌊n/p⌋` for `s ≤ n` — small range, far coarser than the count. VERDICT: the count carry has the symmetric/`SYM` top structure the route wants (sum of bounded-range thresholds), but this re-localises the open difficulty to the *low-degree* (polynomial-method) handle on a threshold `[s ≥ j·p]` of the count — the Majority-flavoured hard symmetric functions. Does NOT prove the thresholds low-degree (= the open LB, composed over the tower) nor that they aren't. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CountCarrySymmetric` |
| **dynamic observer selection — the boundary forces observer refinement (+ BT-closure socket)** | Formalises the N-Frame dynamic-boundary principle for the `MOD`/`ACC⁰` ladder. Abstraction: an observer `O : ℕ → α` reads the count; **`Sufficient O f`** (`∀ x y, O x = O y → (f x ↔ f y)`) = `O` carries the minimal state to evaluate `f`; **`Finer O₁ O₀`** = `O₁` distinguishes at least as much. Concrete observers `resObs p` (residue mod `p`), `padicObs p e` (down-shift residue tower), decision `modPow p e` (`MOD_{p^e}`). PROVED: **`resObs_insufficient_for_modPow`** (residue observer NOT sufficient for `MOD_{p^e}`, `e≥2`: `0,p` share residue, `MOD_{p^e}` separates ⇒ refinement *forced*); **`padicObs_sufficient_for_modPow`** (`p`-adic carry observer IS sufficient, via the down-shift AND decomposition); **`padicObs_finer_than_resObs`** (`p`-adic refines residue, `i=0` component); **`observer_refines_modp_to_padic`** (the three together = the boundary refines `MOD_p`→`p`-adic for `MOD_{p^e}`); **`carry_observer_eq_threshold_observer`** (carry observer factors through a Hamming-threshold observer, re-export of `…CountCarrySymmetric`). SOCKET (the ACC wall, UNPROVED): dynamic refinement closes at a quasipoly-size BT/`SYM∘AND` observer; **`dynamic_boundary_to_acc0_sat_speedup`** is the pure-glue (modus ponens, NO axioms) cash-out `DynamicClosesAtBT → BTHasQuasipolySparse → Speedup` with all premises socketed. Discharging the socket = the open `ACC⁰[composite]` LB. The ladder is proved up to the threshold observer; BT-as-fixed-point is quarantined. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DynamicObserverSelection` |
| **the BT-closure frontier — the final conditional chain `dynamic closure ⇒ NEXP ⊄ ACC⁰`** | Freezes the capstone (entry 165) and states the whole reduction as one theorem ending at the codebase's actual separation statement. **`dynamicClosure_to_NEXP_not_ACC0`** (PROVED as glue, depends on **NO axioms**): `DynamicClosesAtBT → BTHasQuasipolySparse → ACC0SatSpeedup → ¬ NEXPHasACC0Circuits`, composed through the proved Route-B/Williams reduction (`…ACC0RouteBComplete.routeB_to_NEXP_not_ACC0`) — the chain is `hClosure` + `closure_to_quasipoly` + `quasipoly_to_speedup` (= `counting`) + `williams` + `hierarchy (¬Collapse)`. **`observer_ladder_proved`** (re-export): the proved foundation the chain rests on (`…DynamicObserverSelection.observer_refines_modp_to_padic` — residue insufficient, `p`-adic sufficient + finer). The chain is valid and machine-checked; its two load-bearing premises `DynamicClosesAtBT` (refinement closes at a BT observer) and `closure_to_quasipoly` (it is quasipoly-size) are UNPROVED sockets that together *are* the open `ACC⁰[composite]` LB (= the Beigel–Tarui/Yao quasipoly `SYM∘AND` representation). Everything around the wall is proved (the observer-refinement ladder + the Route-B/Williams reduction); the wall itself is socketed. Terminal scaffolding, not a proof. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BTClosureFrontier` |
| **threshold/BT closure — splitting `DynamicClosesAtBT` into 2 proved size-factors + the depth-reduction socket** | IMPORTANT framing: `DynamicClosesAtBT` is the **Beigel–Tarui/Yao theorem** (quasipoly `SYM∘AND` for all `ACC⁰`), and `NEXP ⊄ ACC⁰` (Williams 2011) is also a **proven** classical theorem — the wall here is the *size of the Lean formalisation*, NOT mathematical openness, and nothing in this arc is a new separation or `P ≠ NP`. The BT size factors as (linear `SYM` top) × (quasipoly bounded-fan-in feature layer), held by fan-in preservation. **`symmetric_observer_state_le`** (PROVED): a symmetric observer (function of the count `s∈{0..n}`) has `≤ n+1` states — the `SYM` top is linear-state, not the bottleneck. **`bounded_fanin_feature_count_le`** (PROVED, re-export of `beigelTarui_monomial_count_le`): `≤ (n+1)^D` AND-features of fan-in `≤ D` — quasipoly for `D=polylog`. **`FanInStaysPolylog D w := w ≤ D`** — the remaining socket: the BT depth reduction (fan-in preserved under `ACC⁰` composition); a theorem, not open, but the large remaining formalisation; NOT proved. **`quasipoly_BT_observer_of_fanin_preservation`** (PROVED glue): `FanInStaysPolylog D w ⇒ (lowDegMonomials n w).card ≤ (n+1)^D` (quasipoly). So the two size-factors of BT are proved; the wall is refined to the single concrete lemma `FanInStaysPolylog`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ThresholdBTClosure` |
| **fan-in depth recurrence — the depth induction reduces `FanInStaysPolylog` to one per-layer merge** | Does the "induct over circuit depth" step of the BT depth reduction. **`fanInAtDepth b factor d := factor^d · b`** with **`fanInAtDepth_succ`** (`= factor · fanInAtDepth b factor d`, the per-layer recurrence). **`fanin_bounded_by_recurrence`** (PROVED): if `layerFanIn 0 ≤ b` and each layer satisfies `layerFanIn (d+1) ≤ factor · layerFanIn d` (the per-layer merge), then `layerFanIn d ≤ fanInAtDepth b factor d` (induction on `d`). **`fanInAtDepth_le`** (PROVED): `factor ≤ L`, `d ≤ D`, `1 ≤ L` ⇒ `fanInAtDepth b factor d ≤ L^D · b` — polylog for constant depth `D` and polylog `L, b` (constant depth is essential). **`quasipoly_feature_count_of_layer_merge`** (PROVED, chains to entry 167): the per-layer merge + constant depth ⇒ bottom AND-feature count `≤ (n+1)^{L^D·b}` (quasipoly). The single socketed premise is `hstep` (per-layer merge: `layerFanIn (k+1) ≤ factor · layerFanIn k`) — the genuine Beigel–Tarui mixed-radix merge (composing children's `SYM∘AND` forms multiplies bottom fan-in by only a polylog factor); a theorem, not open, large to formalise over a concrete `ACC⁰` gate datatype; NOT proved. The depth induction + constant-depth arithmetic are proved; `FanInStaysPolylog` is reduced to exactly the per-layer merge. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FanInRecurrence` |
| **the per-layer merge over the concrete `ACC0Circuit` datatype — fan-in tracking** | Carries out the per-layer merge over the concrete `ACC0Circuit` datatype (`…ACC0CircuitModel`), refining `HasSymAndForm` (which tracks only the AND *count*) to **`HasSymAndFormFanIn f s w`** (count `≤ s`, every monomial-`AND` fan-in `≤ w`). PROVED: base cases **`…_const`** (`0,0`), **`…_monoAND`** (`1, S.card`), **`…_var`** (`1,1`), **`…_mod`** (`S.card, 1` — single-literal `AND`s), **`…_not`** (preserves), and the merge **`hasSymAndFormFanIn_combine`**: any binary combiner gives fan-in **`max w₁ w₂` (no growth)** and count `s₁+(s₁+1)·s₂` (the merged monomials are exactly the children's, via `Sum.elim` + replication — `saCount_sum_elim`/`saCount_prod_const`), corollaries `…_and`/`…_or`. **`acc0circuit_hasSymAndFormFanIn`** — every `ACC0Circuit` has such a form with count `symAndSize C` and fan-in `fanInBound C`; **`fanInBound_le_one`** — `fanInBound C ≤ 1`. KEY honest finding: in the *exact* `SYM∘AND` representation the bottom `AND`s are single literals, so fan-in is `≤ 1` — **fan-in is NOT the Beigel–Tarui bottleneck**; the **count** (`symAndSize`, multiplicative `s₁+(s₁+1)·s₂` ⇒ exponential) is. This concretely discharges the fan-in side of `FanInStaysPolylog` (factor `1`), and re-localises the wall to the quasipoly *count* bound (the polynomial-method/additive-degree content, `…ACC0AdditiveDegree`, approximate only / prime-power `MOD` — the recognised wall). NOT a new separation, NOT open (BT/Williams proven), NOT `P ≠ NP` | `…ACC0SymAndFanIn` |
| **the additive count bound — degree-governed count is quasipoly; the exact gate-count blows up** | Makes the two count regimes explicit (the wall re-localised by entry 169 to the *count*). **`count_quasipoly_of_degree_bound`** (PROVED): `cnt ≤ (n+1)^{δ^e}`, `δ≤L`, `e≤E` ⇒ `cnt ≤ (n+1)^{L^E}` — quasipoly (`n^{polylog}`) for polylog `L` and *constant* `E`; the count is governed by the **degree exponent** `δ^e`, not the gate count. **`compositeBT_count_quasipoly`** (PROVED): applies it to the `(n+1)^{δ^{depth+1}}` monomial-support bound of `…ACC0CompositeBTCapstone.compositeBT_representation` — for `δ≤L` and `depth c ≤ D` the monomial count is `≤ (n+1)^{L^{D+1}}` (quasipoly). **`symAndSize_andTree_ge`** (PROVED, the contrast): a depth-`d` balanced `AND`-tree (`andTree`) has exact gate-count `symAndSize ≥ 2^d` — the multiplicative recurrence `sₐ+(sₐ+1)·s_b` blows up at least exponentially, so the *exact* form is NOT quasipoly and the degree route is essential. Honest scope: the degree-governed (additive) count bound is quasipoly and proved (on the proved `compositeBT_representation`); the exact count blows up. The remaining gap is the gate-poly instantiation of `compositeBT_representation` for an actual `ACC⁰` circuit, which over a field exists only for prime-power `MOD` (the documented composite-`MOD` obstruction). BT/Williams proven classically ⇒ formalisation, not open. NOT a new separation, NOT `P ≠ NP` | `…ACC0AdditiveCountBound` |
| **the composite-`MOD` field-gate instantiation — squarefree works low-degree, prime-power is the obstruction** | Resolves the remaining gap object (the low-degree field gate for composite `MOD`) exactly along the proved dichotomy. **`squarefree_modGate_all_lowdegree`** (PROVED): each per-prime gate `modPGate p` (`p ∈ S`) has degree `≤ p−1`. **`squarefree_field_gate_instantiation`** (PROVED): for distinct primes `S`, the low-degree (`≤ p−1`) per-prime Fermat gate family `{modPGate p}` over `∏_{p∈S} F_p` decides `MOD_{∏S}` (`⟨degree bound, modGateProd_decides⟩`) — the low-degree field gate the BT/polynomial-method route needs, *here* for squarefree composite modulus. **`primepower_field_gate_obstruction`** (PROVED, re-affirm of `…ACC0PrimePowerObstruction`): for `p^e` (`e≥2`), `0` and `p` share the mod-`p` residue yet `MOD_{p^e}` separates them, so no `F_p` field gate (any degree) computes it; `ZMod(p^e)` is not a field — the documented obstruction, NOT faked. Net: the composite-`MOD` field gate **exists and is low-degree for squarefree** and **provably does not exist over a field for prime-power**, splitting the field-gate route exactly at squarefree vs prime-power. The full Beigel–Tarui theorem handles `p^e` by a non-low-degree-field mixed-radix `SYM∘AND` argument (proven classically, to formalise). The only mathematically hard object — a low-degree field indicator for prime-power `MOD` — is proved *not* to exist. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeMODFieldGate` |
| **the end-to-end Beigel–Tarui instantiation — exact circuit polynomial fed into `compositeBT_representation`** | Completes the concrete pipeline `circuit → polynomial → degree + quasipoly count + sparse sum`. **`guP`/`gbP`** — the exact unary/binary multilinear gate polynomials; **`guP_deg`/`gbP_deg`** (PROVED) — degree `≤ 2` (via `totalDegree_add`/`_mul`/`_neg`, `oneSubX_deg`, `termBd`). **`subst_una_eq`/`subst_bin_eq`** (PROVED): the exact circuit polynomial `subst` (`…ACC0SubstitutionPoly`) composes through `guP`/`gbP` via `aeval` (the hypotheses `compositeBT_representation` needs). **`endToEnd_BT`** (PROVED): instantiating `compositeBT_representation` with `Q = subst`, `δ = 2`, for every `Circ` `c` the exact polynomial `subst c` (a) computes the circuit on the cube (`subst_eval`), (b) has degree `≤ 2^{depth+1}`, (c) has `≤ (n+1)^{2^{depth+1}}` distinct monomial-supports (quasipoly for bounded depth), (d) admits the sparse cube-sum read-off. A genuine, concrete, *exact* end-to-end instantiation; the squarefree composite-`MOD` field gate (entry 171) slots into the same pipeline at `δ = max(2, p−1)`. Honest: being the *exact* representation, the count is quasipoly only for bounded depth (the exact route's known limitation, entry 170); the quasipoly count for all poly-size circuits needs the *approximate* low-degree gates (probabilistic polynomial method). BT/Williams proven classically ⇒ formalisation, not open. NOT a new separation, NOT `P ≠ NP` | `…ACC0EndToEndBT` |
| **the approximate Beigel–Tarui instantiation — polylog degree, quasipoly count (the RS route)** | Step (a): upgrades the exact bounded-depth pipeline (entry 172) to the genuine BT-style quasipoly route via the Razborov–Smolensky probabilistic approximant. **`toApprox_monomial_count_le`** (PROVED): the concrete RS approximant `…Layer3.toApprox p t R C` (`∨`/`∧` via `genOrApprox`, `MOD_p` via the Fermat indicator; degree `≤ ((p−1)·t)^{depth}` by the proved `toApprox_totalDegree_le`) has monomial-support count `≤ (n+1)^{((p−1)·t)^{depth}}` (degree-governed, via `support_mem_lowDeg` + `beigelTarui_monomial_count_le`). **`approx_endToEnd_BT`** (PROVED): for `(p−1)·t ≤ L` and `depth ≤ D`, the approximant has total degree `≤ L^D` *and* monomial-support count `≤ (n+1)^{L^D}` (via the entry-170 `count_quasipoly_of_degree_bound` + `Nat.pow_le_pow_left/right`). For a polylog RS error parameter `(p−1)·t = polylog` (`L = polylog`) and *constant* depth `D`, this is **degree polylog and count quasipolynomial for every constant-depth circuit** — the BT-style low-degree sparse bound, no longer bounded-depth-only. Honest scope: this is the degree+quasipoly-count half, fully assembled from proved RS pieces; the **bounded-error** half is the existing RS probabilistic machinery (`…Layer3.orApprox_error_rate`, `…ACC0CircuitSubstitution.circuit_error_bound`, `…Layer3.exists_form_few_errors`), cited and *not* faked into the theorem. Prime-`p` (`AC⁰[p]`) route; squarefree composite runs it per prime over `∏ F_p` (entry 171), prime-power `MOD` is the proven field obstruction (step (b)). BT/Williams proven classically ⇒ formalisation, not open. NOT a new separation, NOT `P ≠ NP` | `…ACC0ApproxBTInstantiation` |
| **prime-power mixed-radix `SYM∘AND` — the non-field route that clears the field obstruction (step b)** | The full Beigel–Tarui handling of prime-power `MOD`: *not* by a low-degree field polynomial (which entry 171 proved cannot exist for `p^e`) but by the **symmetric/mixed-radix** route — a `MOD_q` gate of *any* modulus is exactly a symmetric function of its support literals. **`modPrimePower_symAndForm`** (PROVED): `MOD_{p^e}` has an exact `SYM∘AND` form of size `|S|` (`hasSymAndForm_mod` at `q = p^e`); **`modPrimePower_symAndFanIn`** — fan-in `1` (single-literal `AND`s, no field polynomial). **`primePower_mixed_radix_combine`** (PROVED): a `MOD_{p^e}` gate combines with any other `SYM∘AND` form under any binary combiner via the base-`(|S|+1)` mixed-radix merge (`hasSymAndForm_combine`), size `|S|+(|S|+1)·s₂`. **`primePower_sym_clears_field_obstruction`** (PROVED): the contrast — `MOD_{p^e}` has an exact `SYM∘AND` form *and* (`e ≥ 2`) no `F_p` field gate computes it (`0`, `p` share the mod-`p` residue, `MOD_{p^e}` separates them). So the entry-171 field obstruction is *field-specific*: the symmetric/mixed-radix `SYM∘AND` route handles `MOD_{p^e}` exactly, which is how BT sidesteps it. Honest: this is the *exact* representation (size `|S|`, multiplicative under composition); the *quasipoly* count for prime-power `MOD` circuits is the full BT mixed-radix size analysis (proven classically, to formalise) combined with the step-(a) approximate `AC⁰` count control. BT/Williams proven classically ⇒ formalisation, not open. NOT a new separation, NOT `P ≠ NP` | `…ACC0PrimePowerMixedRadix` |
| **step (3) — the honest partial discharge of `DynamicClosesAtBT`** | Bundles the two PROVED BT ingredients and reduces `DynamicClosesAtBT` (entry 166's socket) to a single residual. **`ACComponent` / `acComponent_proved`** (PROVED, from entry 173): the AC⁰ Razborov–Smolensky approximant has polylog degree (`≤ L^D`) and quasipoly count (`≤ (n+1)^{L^D}`). **`MODComponent` / `modComponent_proved`** (PROVED, from entry 174): a `MOD_{p^e}` gate has an exact `SYM∘AND` form of size `|S|` (no field polynomial). **`dynamicClosure_partial_discharge`** (PROVED as glue): `DynamicClosesAtBT` from `hSize` (the residual *BT size analysis* socket combining (a),(b) into one quasipoly-size `SYM∘AND` for arbitrary `ACC⁰`) applied to the proved components — two of three ingredients proved, `hSize` the sole remaining socket. **`partialDischarge_to_NEXP`** (PROVED as glue): chains it to `¬ NEXPHasACC0Circuits` via entry 166 (`dynamicClosure_to_NEXP_not_ACC0`), with the residual `hSize` + the Williams sockets (`closure_to_quasi`, `quasi_to_speed`, `williams`, `hierarchy`). HONEST PARTIAL: this is **not** a proof of `DynamicClosesAtBT` or `NEXP ⊄ ACC⁰` — the residual `hSize` is the full Beigel–Tarui size theorem (proven classically, large to formalise), left as the one named socket; the proved (a),(b) are genuinely discharged. User's 3-step plan (a + b + discharge) complete as a partial discharge. NOT a new separation, NOT `P ≠ NP` | `…ACC0DynamicClosureDischarge` |
| **the Beigel–Tarui size theorem over a concrete datatype (prime-`p` route)** | Discharges the *size* content of the entry-175 residual `hSize`, concretely. **`btSymAndSize p P`** — the `SYM∘AND` size of a representation = number of distinct `AND`-features (`(P.support.image (·.support)).card`). **`QuasipolyBTSize` / `quasipolyBTSize_proved`** (PROVED, via entry 173 `approx_endToEnd_BT`): for any `BoolCircuitSyntax` circuit `C`, the RS approximant `toApprox p t R C` has `SYM∘AND` size `btSymAndSize ≤ (n+1)^{L^D}` for `(p−1)·t ≤ L`, `depth ≤ D` — quasipolynomial (`n^{polylog}`) for polylog `L` (the RS error parameter) and *constant* depth `D`. This is the BT size theorem for the AC⁰[p]/prime route, over the concrete datatype: the `SYM∘AND` size is genuinely quasipolynomial. Honest: discharges the **size half** of `hSize` for the prime-`p` route; the remaining BT residuals (both proven classically, large to formalise) are (1) **correctness/error** — that the size-`btSymAndSize` `SYM∘AND` actually computes/`1/poly`-approximates the circuit (the RS *agreement* content, `…Layer3` correlation + `circuit_error_bound`), and (2) **composite/prime-power modulus** — the prime-`p` approximant computes only `MOD_p`; composite runs per prime over `∏ F_p` (entry 171) and prime-power uses the exact mixed-radix `SYM∘AND` (entry 174) whose quasipoly size is the BT mixed-radix analysis. NOT full `DynamicClosesAtBT`; size half proved. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BTSizeTheorem` |
| **the RS agreement/error bound — the correctness half, assembled (the complete RS BT representation)** | Discharges the *correctness/error* half (entry-176 residual (1)) for the AC⁰[p] route, by assembling the repo's proved agreement machinery. **`rs_agreement_BT`** (PROVED): for an AC⁰[p] circuit `C` (every `MOD` gate has `q = p`), there is a form choice `ω` such that the Razborov–Smolensky approximant `…Layer3.toAgree p t (oracleOf p t C ω) C` *simultaneously* has **(a)** total degree `≤ ((p−1)·t)^{depth}` (`toAgree_totalDegree_le`), **(b)** `SYM∘AND` size `≤ (n+1)^{((p−1)·t)^{depth}}` (`support_mem_lowDeg` + `beigelTarui_monomial_count_le`), and **(c)** bounded error `· p^t ≤ (#subcircuits)·2^n` (`…Layer3.composed_error_le`, the full RS agreement guarantee, error rate `≤ (#subcircuits)·p^{-t}`). So for AC⁰[p] circuits the RS approximant has a **complete** BT representation — low degree, quasipoly size, *and* small error, together, all proved. Honest: this discharges residual (1) (correctness) for the prime route; the sole remaining residual is (2) **composite/prime-power modulus** — `composed_error_le`'s `hmod` covers only `q = p`; squarefree composite runs per prime over `∏ F_p` (entry 171), prime-power via the exact mixed-radix `SYM∘AND` (entry 174). NOT full `DynamicClosesAtBT` (composite modulus remains), NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RSAgreementBound` |
| **composite mixed-radix quasipoly-size — combining a constant number of forms stays quasipoly** | Discharges the *size* content of the composite residual (2): a squarefree composite `MOD_m = ⋀ MOD_{pᵢ}` (CRT, entry 171) combines the per-prime `SYM∘AND` forms via the mixed-radix merge, and for a fixed `m` (constantly-many prime factors) with per-prime quasipoly size the result stays quasipoly. **`hasSymAndForm_mono`** (PROVED): `HasSymAndForm` monotone in the size bound. **`mrBound Q` / `mrBound_eq` / `mrBound_le_pow`** (PROVED): the mixed-radix size recurrence `mrBound Q (k+1) = Q + (Q+1)·mrBound Q k` has closed form `mrBound Q k = (Q+1)^k − 1 ≤ (Q+1)^k`. **`andAll` / `hasSymAndForm_andAll`** (PROVED): the `AND` of a list of `SYM∘AND` forms (each size `≤ Q`) is a `SYM∘AND` form of size `≤ mrBound Q (length)` (iterated `hasSymAndForm_combine`). **`mixedRadix_quasipoly_size`** (PROVED): the `AND` of `forms` (each `≤ Q`) has `SYM∘AND` size `≤ (Q+1)^{forms.length}` — `(quasipoly)^{constant} = quasipoly` for a constant number of forms (the `ω(m)` prime factors of fixed `m`) and quasipoly `Q`, *not* exponential because the number of moduli is fixed. Combined with entry 177 (per-prime correctness), the composite `ACC⁰` BT size is assembled. Honest: this is the size analysis (combining a constant number of quasipoly forms); it does not itself run the per-prime RS approximation over each `F_{pᵢ}` (entry 177 per prime) and assumes `m` fixed (constant prime-factor count). BT/Williams proven classically ⇒ formalisation, not open. NOT a new separation, NOT `P ≠ NP` | `…ACC0CompositeMixedRadixSize` |
| **the final integration — `DynamicClosesAtBT` PROVED for the AC⁰[p] route, chained to `¬ NEXP`** | The culmination of the BT-closure arc (166–178): turns `DynamicClosesAtBT` from a socket into a *proved theorem* for the AC⁰[p] route. **`DynamicClosesAtBT_AC0p`** (concrete def): every AC⁰[p] `BoolCircuitSyntax` circuit has a complete Beigel–Tarui representation (∃ form `ω`: polylog degree `≤ ((p−1)·t)^{depth}` ∧ quasipoly `SYM∘AND` size `≤ (n+1)^{((p−1)·t)^{depth}}` ∧ bounded error `· p^t ≤ (#subcircuits)·2^n`). **`dynamicClosesAtBT_AC0p_proved`** (PROVED): it holds universally, directly from `rs_agreement_BT` (entry 177) — so `DynamicClosesAtBT` is a *theorem*, not a socket, for AC⁰[p]. **`acc0p_BTclosure_to_NEXP`** (PROVED): plugging the proved closure into the entry-166 chain (`dynamicClosure_to_NEXP_not_ACC0`), `¬ NEXPHasACC0Circuits` follows from the **Williams sockets alone** (`closure_to_quasi`, `quasi_to_speed`, `williams`, `hierarchy`) — the entire Beigel–Tarui (closure) side is discharged for AC⁰[p]; only the Williams meta-theorem sockets (themselves `NEXP ⊄ ACC⁰`-strength classical theorems, not BT content) remain. Honest: for AC⁰[p] the BT closure is genuinely proved; for arbitrary `ACC⁰` (composite) the closure is assembled from the proved CRT (171) + prime-power mixed-radix (174) + per-prime rep (177) + mixed-radix size (178), with cross-route bookkeeping remaining. BT/Williams proven classically ⇒ formalisation, not open. NOT a new separation, NOT `NEXP ⊄ ACC⁰` (Williams sockets remain), NOT `P ≠ NP` | `…ACC0DynamicClosesAtBTComplete` |
| **composite cross-route bookkeeping — the squarefree-composite BT closure, assembled** | Assembles the composite closure from proved per-route pieces, stating the two halves. **`composite_count_decomposes`** (PROVED, entry 171): `(∏ p∈S, p) ∣ cnt ↔ ∀ p∈S, p ∣ cnt` — the squarefree composite `MOD_{∏S}` *is* `⋀_{p∈S} MOD_p`. **`composite_decide_decomposes`** (PROVED): the `decide`/Boolean form — the composite `MOD_{∏S}` indicator on the count equals the conjunction of the per-prime indicators. **`composite_quasipoly_size`** (PROVED, entry 178): the `AND` (`andAll`) of the per-prime `SYM∘AND` forms (each of size `≤ Q`, from the per-prime exact form entry 174 / RS rep entry 177) has `SYM∘AND` size `≤ (Q+1)^{|forms|}` — quasipolynomial for a *constant* number of prime factors. Together: the composite `MOD_{∏S}` decomposes (CRT) into `|S|` per-prime tests, and their `AND` is quasipoly-size — so the composite gate has a quasipoly BT representation built from the proved AC⁰[pᵢ] pieces over the distinct primes. Honest: this is the cross-route *bookkeeping* (the two assembled halves: CRT decomposition + mixed-radix size); it does not itself run the per-prime RS approximation of a whole composite circuit over each `F_{pᵢ}` (entry 177 per prime, then combined here), with `m` fixed (constant prime-factor count). BT/Williams proven classically ⇒ formalisation, not open. NOT a new separation, NOT `P ≠ NP` | `…ACC0CompositeCrossRoute` |
| **the physical universal `hstep` — decode → step → re-encode loop correctness, assembled** | The Williams-side mountain: `U` simulates `M` from `M`'s code on its own tape. The sub-machine contracts are proved (tape encode/decode `…ACC0TapeEncoding`/`…ACC0UniversalDecode`, rule lookup `…ACC0RuleLookup`, head `…ACC0HeadLocation`, rewrite `…ACC0TapeRewrite`, atomic step `…ACC0PhysicalStep`); this file assembles them into the `hstep` loop's correctness. **`univSimStep M c t`** — the encoded one-step transition (decode `encodeSim M c` → apply firing rule `t` → re-encode `encodeSim M (applyTrans c t)`). **`univ_hstep_correct`** (PROVED): for a firing rule `t` at `c` (`t∈M`, `t.1=(c.1, readSym c)`), the loop is faithful — (a) `decodeSim (encodeSim M c) = some (M, c)` (decode), (b) `reachIn (toNTM M) 1 c (applyTrans c t)` (a genuine one-step `M`-transition), (c) `decodeSim (univSimStep M c t) = some (M, applyTrans c t)` (re-encode round-trips). **`univ_hstep_concreteStep`** (PROVED): the re-encoded config is reached by `concreteStep M`. Honest: the `hstep` **correctness contract** (decode → step → re-encode is faithful and round-trips) is proved by assembling the sub-machine contracts; what remains is realising `univSimStep` as `U`'s *own* transition rules with the explicit step-overhead bound `B` (the `hstep` *machine*, vs. its correctness contract) — classical TM engineering, not an open problem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalHStep` |
| **the physical `hstep` realization with explicit overhead `B`** | Turns the entry-181 semantic `hstep` contract into a step-bounded simulation lemma. The universal step runs in four phases over `U`'s own transitions: decode (`c0→c1`), rule-lookup (`c1→c2`), tape-rewrite (`c2→c3`), re-encode (`c3→c4`). **`hstep_realized_with_overhead`** (PROVED): given the four per-phase reachabilities (each phase a run of `U`'s own transitions within its bound `bDecode`/`bLookup`/`bRewrite`/`bReencode`), `U` reaches `c4` from `c0` within `B = bDecode + bLookup + bRewrite + bReencode` steps — composed from the proved additive `…ACC0NTM.reachIn_add`. **`hstep_exists_overhead`** (PROVED): hence `∃ B, reachIn U B c0 c4` with the explicit `B` the phase sum — `U` performs one simulated `M`-step in `B` of its own steps. Honest: this proves the **overhead composition** (the explicit `B` is the additive sum of the four phase costs, composition proved via `reachIn_add`); the four per-phase reachabilities are taken as hypotheses — they are the remaining construction (building `U`'s actual transition table so each phase is a real `U`-transition run with its step count; the proved sub-machine *contracts* specify *what* each phase computes, realising them as transitions is the residual). Classical TM engineering, not an open problem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalHStepMachine` |
| **the universal rewrite phase — a concrete `reachIn` realizing the tape write** | Discharges the first of entry 182's four per-phase reachabilities with a *concrete machine* and an *actual* `reachIn`. **`rewriteRule c q' w m`** / **`rewriteMachine`** — a one-rule machine firing at `c` (write `w`, move `m`, go to `q'`). **`rewrite_phase`** (PROVED): `reachIn (toNTM (rewriteMachine c q' w m)) 1 c (applyTrans c (rewriteRule c q' w m))` (the write in **one** physical step, via `firing_rule_step`) **and** `(applyTrans c …).2.2.getD c.2.1 false = w` (read-after-write: the rewritten head holds `w`, via `…ACC0TapeRewrite.applyTrans_write`). So the rewrite phase of entry 182 is realized as `reachIn U 1 c (rewritten c)` for a concrete `U` with explicit `bRewrite = 1` and the tape content certified. Honest: this realizes the rewrite phase's *core operation* (write/move/state) as an actual transition of a concrete machine with a proved step count and tape-content guarantee; embedding the gadget into the *universal* `U` so it rewrites the *encoded* simulated tape at the encoded head position is the remaining step (the decode/lookup/re-encode phases, which touch the layout, are heavier). Classical TM construction, not an open problem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalRewritePhase` |
| **the universal lookup phase — the rule scan finds the firing rule, with scan-cost bound** | Discharges the second of entry 182's four per-phase reachabilities (the *lookup* phase) using the proved rule-lookup contract (`…ACC0RuleLookup`). **`lookup_fires`** (PROVED): a rule found by the scan fires in one step — `t ∈ matchingRules M c.1 (readSym c) → reachIn (toNTM M) 1 c (applyTrans c t)` (via `mem_matchingRules` + `firing_rule_step`). **`lookup_phase`** (PROVED): when a step `concreteStep M c d` exists, the table scan `matchingRules M c.1 (readSym c)` contains a rule `t` with (a) `reachIn (toNTM M) 1 c (applyTrans c t)` (it fires), (b) `d = applyTrans c t` (reaches the intended config), and (c) `(matchingRules M c.1 (readSym c)).length ≤ M.length` (the scan-cost bound, `matchingRules_length_le`). So the lookup phase of entry 182 is realized as an actual reachability with the scan-cost bound `bLookup ≤ M.length`. Honest: realizes the lookup's *core operation* (scan finds firing rule, it advances in one step, scan `≤ M.length`); it does not realise the scan as the *universal* `U` walking the *encoded* table on its own tape (splicing the scan into `U`'s transition table over the `encodeTape` layout, with the per-rule step count, is the remaining step). Classical TM construction, not an open problem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalLookupPhase` |
| **the universal decode phase — faithful, unambiguous recovery of `(M,c)` from the tape** | Discharges the third of entry 182's four per-phase reachabilities (the *decode* phase), the first that touches the tape *layout*, using the proved tape-encoding contracts (`…ACC0TapeEncoding`: `decodeTape_encodeTape`, `encodeTape_inj`) + the decode round-trip (`…ACC0UniversalDecode`). **`encodeSim_inj`** (PROVED): the simulation-tape encoding is injective — `encodeSim M c = encodeSim M' c' → M = M' ∧ c = c'` (via `encodeTape_inj` + `machineEquiv.injective`). **`decode_phase`** (PROVED): for a step `concreteStep M c d`, (a) `decodeSim (encodeSim M c) = some (M, c)` (round-trip recovery), (b) `reachIn (toNTM M) 1 c d` (the decoded machine steps, `decoded_machine_steps`), and (c) the recovery is unambiguous (`∀ M' c', encodeSim M c = encodeSim M' c' → M = M' ∧ c = c'`). So the decode phase of entry 182 is realized as a faithful, invertible recovery wired to the simulated step. Honest: proves the decode's correctness contract (faithful + unambiguous, connected to the step); it does not realise the parse as the universal `U` *scanning* the `encodeTape` bit-layout with its own transitions and a per-symbol step count (the `bDecode` realisation over the layout is the remaining step). Classical TM construction, not an open problem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalDecodePhase` |
| **the universal re-encode phase — write the post-step state back, completing the four phases** | Fourth and last of entry 182's per-phase realizations (rewrite 183, lookup 184, decode 185). Re-encode is the inverse of decode, so the same round-trip + injectivity contracts cover it. **`reencode_phase`** (PROVED): the post-step tape is canonical — `decodeSim (encodeSim M d) = some (M, d)` (round-trips) and `∀ M' d', encodeSim M d = encodeSim M' d' → M = M' ∧ d = d'` (unique encoding, `encodeSim_inj`). **`universal_step_complete`** (PROVED): all four phases assembled — for a firing rule `t` at `c`, the universal step takes `encodeSim M c` (decodes to `(M,c)`) through a genuine one-step `M`-transition `reachIn (toNTM M) 1 c (applyTrans c t)` to the re-encoded `encodeSim M (applyTrans c t)` (decodes to `(M, applyTrans c t)`, the unique such tape) — the complete decode → step → re-encode cycle, faithful and round-tripping. So all four per-phase correctness realizations are done, and the full tape-level universal step is assembled. Honest: what remains (as for the other phases) is realising the re-encode/parse as `U`'s *own* transitions writing the `encodeTape` bit-layout with explicit per-symbol step counts; combined with entry 182's overhead composition, that gives the fully physical `hstep` machine with its `B` bound. Classical TM construction, not an open problem. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalReencodePhase` |
| **the fast-SAT verifier — a correct, quasipoly-work SAT decider from the `SYM∘AND` representation** | Williams mountain 2/3: turn the `SYM∘AND` (BT closure output) into a SAT decider that beats brute force. **`fast_sat_verifier`** (PROVED): for a degree-`≤D` injective monomial-`AND` family with symmetric top `h`, SAT is decided by the **count-cell image** — `Satisfiable (symEval …) ↔ ∃ c ∈ image (gateCount …), h c = true` (correctness, `…ACC0ResidueObserver.observed_sat_iff`) — and the verifier's work, that image's size, is `≤ (D+1)·n^D + 1` (quasipoly for polylog `D`, unconditional in any speedup parameter `k`, via `sym_count_card_le` + `fastSatWork_le_of_degree` + `lowDegMonomialCount_le_pow`). **`fast_sat_beats_bruteforce`** (PROVED): if that quasipoly work `< 2^n` (as for polylog `D`), the verifier examines `< 2^n` count-cells — strictly faster than brute force. Packages the verifier's *runtime* (quasipoly work) + the beats-brute-force consequence on top of `…ACC0WilliamsFastSat`'s savings/correctness. Honest: it does not turn this into a *uniform* machine-realised `ACC⁰`-SAT algorithm within a time bound (the `UniformWilliamsRealizationSocket`, `NEXP ⊄ ACC⁰`-strength), nor supply the `SYM∘AND` representation (the BT closure, entry 179). Williams 2011 proven classically ⇒ formalisation, not open. NOT a new separation, NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FastSATVerifier` |
| **the Williams contradiction — IKW/NW + fast SAT ⇒ `NEXP ⊄ ACC⁰`, the final capstone** | Williams mountain 3/3, the final assembly. **`williams_contradiction`** (PROVED as glue, depends on **NO axioms**): `¬ (NEXP ⊆ ACC⁰)` from the BT closure `hClosure : DynamicClosesAtBT` (proved for AC⁰[p], entry 179) bridged to the `ACC⁰`-SAT speedup by `closure_to_speedup` (the BT → fast-SAT route, entries 179/187 + uniform realization), the IKW easy-witness lemma `ew`, guess-and-verify / NW-derandomisation `gv`, and the nondeterministic time hierarchy `hierarchy` — composing `…ACC0EasyWitness.nexp_not_acc0_from_witness_parts` (the proved Williams glue) with the closure-supplied speedup. Reduces `NEXP ⊄ ACC⁰` to a precise set: the BT closure (proved AC⁰[p]) + four genuinely-deep classical sockets (`closure_to_speedup` which subsumes the separation-strength `UniformWilliamsRealizationSocket`, IKW, NW/guess-verify, the time hierarchy). The four sockets are *proven classical theorems* (Williams 2011, IKW, Nisan–Wigderson, the time hierarchy), each a major formalisation, left named. The composition is proved glue. `NEXP ⊄ ACC⁰` is itself already Williams' theorem; this is its formalisation *architecture* with the deep ingredients socketed, not a new separation or `P ≠ NP` | `…ACC0WilliamsContradiction` |
| **IKW refined — exposing the Nisan–Wigderson derandomisation socket** | Refines the entry-150 IKW socket `HardFnSeparation` (hard fn ⇒ `¬(NEXP⊆ACC⁰)`), which bundled NW + Karp–Lipton, into two distinct named sub-sockets. **`NWDerandomization`** (the NW socket): a hard function yields a derandomisation (PRG defeating `ACC⁰`, Nisan–Wigderson / Impagliazzo–Wigderson). **`DerandKarpLiptonSeparation`** (the collapse socket): derandomisation ⇒ `¬(NEXP⊆ACC⁰)` (Karp–Lipton). **`hardFnSeparation_from_NW`** (PROVED, **no axioms**): `NWDerandomization` + `DerandKarpLiptonSeparation` ⇒ `HardFnSeparation` (`fun hf => kl (nw hf)`). **`easyWitness_from_NW`** (PROVED): the IKW lemma from the *refined* ingredients (`NoEasyWitnessHardFn` + NW + Karp–Lipton ⇒ `EasyWitnessLemma`), composing `hardFnSeparation_from_NW` into the entry-150 contrapositive. So the IKW chain now names **NW** explicitly; the three classical pieces (no-easy-witness ⇒ hardness, NW, Karp–Lipton) are named sub-sockets, each a proven classical theorem (IKW 2002, Nisan–Wigderson, Karp–Lipton) needing circuit-complexity/pseudorandomness infrastructure. Refines the architecture; does not prove IKW or NW. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0IKWNisanWigderson` |
| **the Nisan–Wigderson generator — combinatorial design (proved) + hybrid argument (socketed)** | Decomposes the entry-189 NW socket (`NWDerandomization`) into its two classical components — a combinatorial design and the hybrid argument — proving the design base case and the glue. **`disjoint_design`** (PROVED): a genuine NW design (the disjoint base case) — for any `m, k`, the `m` blocks `Ico (i·k) (i·k+k)` each have size `k` (`Nat.card_Ico`) and are pairwise disjoint (`Finset.disjoint_left` + the block ordering `i·k+k ≤ j·k` for `i<j`, via `calc … (i+1)·k …` + `omega`). A real design with pairwise intersection `0`. **`NWGeneratorFromDesign`** — the hybrid-argument socket: a design + a hard function ⇒ the derandomisation (the generator `G(y) = (f(y|_{Sᵢ}))ᵢ` fools `ACC⁰`, since a distinguisher gives a small circuit for `f`). **`nwDerandomization_from_design`** (PROVED, **no axioms**): design + hybrid ⇒ `NWDerandomization`. So NW decomposes into a *proved* combinatorial design (disjoint base case) + the hybrid argument (socket). Honest: the disjoint design is real but inefficient (seed `m·k`); the genuine NW generator needs the low-intersection designs (seed `O(k²/log m)`) and the hybrid analysis — classical results (Nisan–Wigderson 1994) needing circuit-complexity infrastructure, left as the socket. Builds the base case + decomposition; does not prove NW. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonGenerator` |
| **the efficient Nisan–Wigderson design — low intersection from polynomial graphs (PROVED)** | The quantitative refinement of entry 190's disjoint base case: the genuine *efficient* NW design and its low-intersection property, fully proved. Over a finite field `F`, the graphs of the `|F|^k` distinct degree-`<k` polynomials are `|F|^k` sets, each of size `|F|`, with **pairwise intersection `< k`** — the optimal NW design (seed `|F|² = O(k²/log m)` for `m = |F|^k` sets). **`poly_agree_le_natDegree`** (PROVED): distinct `p ≠ q` agree (`p(a)=q(a)`) on ≤ `(p−q).natDegree` points — the agreement set is the root set of the nonzero `p−q`, so the entry-161 field root-count bound `field_root_card_le_natDegree` applies (`eval_sub` + `sub_self`). **`poly_agree_lt_of_degree_lt`** (PROVED): with `deg p, deg q < k`, agreement `< k` (`natDegree_sub_le` + `omega`). **`nwSet p := univ.image (a ↦ (a, p(a)))`** (the graph), **`nwSet_card`** (PROVED): size `|F|` (`card_image_of_injective`, recover `a` from first coord). **`nwSet_inter_le`** (PROVED): graph intersection ≤ agreement count (the intersection injects into the agreement filter via `card_image_le`). **`nw_low_intersection_design`** (PROVED, **no axioms**): the capstone — distinct degree-`<k` polys give graphs of size `|F|` with pairwise intersection `< k`. So the combinatorial *heart* of the efficient NW design is proved from first principles. Honest: this proves the **design** (the low-intersection property), not the **generator** — the hybrid argument (design + hard fn ⇒ a generator fooling `ACC⁰`) remains the entry-189/190 socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonLowIntersection` |
| **the Nisan–Wigderson hybrid argument — the telescoping heart (PROVED) + the reduction sockets** | Opens the entry-190 `NWGeneratorFromDesign` socket (design + hard fn ⇒ generator fooling `ACC⁰`) and proves its genuine analytic/combinatorial **heart**. The NW hybrid argument: a distinguisher with *global* advantage `ε` between the generator (hybrid `0`) and uniform (hybrid `m`) ⟹ telescoping ⟹ an *adjacent* hybrid pair with advantage `≥ε/m` ⟹ Yao ⟹ a next-bit predictor for `f` ⟹ reconstruction (hardwiring the other `m−1` blocks as size-`≤2^k` tables, by low intersection) ⟹ a small circuit for `f` ⟹ contradiction with hardness. **`hybrid_advantage`** (PROVED): `0<ε`, `ε≤|f m−f 0|` ⇒ `∃ i<m, ε/m ≤ |f(i+1)−f i|` — via telescoping `∑_{i<m}(f(i+1)−f i)=f m−f 0` (`Finset.sum_range_sub`), `|∑|≤∑|·|` (`Finset.abs_sum_le_sum_abs`), and `sum_lt_sum_of_nonempty`: if every gap `<ε/m` then `∑<m·(ε/m)=ε≤|f m−f 0|`, ⊥ (the `m=0` case forces `|f 0−f 0|=0<ε`, impossible). **`low_intersection_table_card`** (PROVED): a block sharing `r<k` bits is a Boolean fn of `r` bits, truth table `2^r≤2^k` (`Fintype.card_fun`+`Nat.pow_le_pow_right`) — the entry-191 low-intersection payoff: per-block hardwiring cost `≤2^k`. **`nw_hybrid_no_distinguisher`** (PROVED): telescoping + Yao + reconstruction + hardness + design ⇒ a global advantage is `False`. **`nwGeneratorFromDesign_via_hybrid`** (PROVED, **no axioms beyond the standard three**): discharges the entry-190 `NWGeneratorFromDesign` socket — a design + hard fn give `¬GlobalAdvantage` (no `ε`-distinguisher). Honest: proves the hybrid **engine** (telescoping) + the low-intersection payoff + the composition glue; the two model-dependent steps — **`YaoPredictor`** (Yao distinguisher↔predictor) and **`Reconstruction`** (predictor+design⇒small circuit), plus **`HardnessExcludesCircuit`** — remain named sub-sockets (classical, need circuit-complexity/probability infra). Proves the engine + decomposition, not the full generator. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonHybrid` |
| **Yao's distinguisher↔predictor — the predict-from-distinguish identity (PROVED)** | Discharges the first entry-192 sub-socket (`YaoPredictor`: adjacent advantage ⇒ next-bit predictor, Yao 1982) by proving its genuine quantitative **heart**. Construction: a distinguisher `D` accepts the real-bit hybrid w.p. `a` and the wrong-bit case w.p. `c` (random-bit hybrid: `(a+c)/2`); the predictor guesses bit `g`, runs `D`, outputs `g` if `D` accepts else `1−g`, succeeding w.p. `a/2+(1−c)/2 = 1/2+(a−c)/2`. **`predictSuccess a c := a/2+(1−c)/2`**, **`predictSuccessFlip a c := (1−a)/2+c/2`**, **`hybridGap a c := a−(a+c)/2`** (`= (a−c)/2`, the distinguishing advantage). **`yao_success_identity`** / **`yao_success_flip_identity`** (PROVED, `ring`): success `= 1/2 ± hybridGap`. **`yao_best_predictor`** (PROVED): `max (predictSuccess) (predictSuccessFlip) = 1/2 + |hybridGap a c|` (`rcases le_total 0 gap`, `abs_of_nonneg`/`abs_of_nonpos`, `max_eq_left`/`max_eq_right`) — the better convention removes the sign. **`yao_predictor_from_advantage`** (PROVED): `δ ≤ |hybridGap a c| ⇒ 1/2+δ ≤ max(…)`. **`hybridGap_realise`** (PROVED): `|hybridGap y (2x−y)| = |y−x|` — any gap is realised by explicit acceptance probs. **`YaoNextBitPredictor f m ε`** := `∃ i<m, ∃ a c, |f(i+1)−f i|=|hybridGap a c| ∧ 1/2+ε/m ≤ max(predictSuccess,predictSuccessFlip)`. **`yaoPredictor_discharge`** (PROVED, **no axioms beyond the standard three**): `YaoPredictor (AdjacentAdvantage f m ε) (YaoNextBitPredictor f m ε)` — an adjacent advantage `ε/m≤|f(i+1)−f i|` gives block `i` with acceptance probs `a=f(i+1)`, `c=2·f i−f(i+1)` realising the gap, predictor success `≥1/2+ε/m`. Honest: proves Yao's **arithmetic engine** (the success identity) + advantage bookkeeping + discharges the entry-192 `YaoPredictor` socket for the concrete success predicate; the residual **`YaoCircuitEfficiency`** socket (the predictor is a *small circuit*, since `D` is bounded-size `ACC⁰`) is model-dependent, named not proved. Proves the probability identity, not the circuit-level reduction. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonYao` |
| **the NW reconstruction step — the hardwiring size accounting (PROVED)** | Discharges the second entry-192 sub-socket (`Reconstruction`: next-bit predictor + low-intersection design ⇒ small circuit for `f`) by proving its quantitative **size accounting**. The reconstruction wires the predictor to hardwired copies of the other `m−1` design blocks; by the low-intersection property (entry 191), each shares `r_j < k` seed bits, so (by entry-192 `low_intersection_table_card`) is a Boolean fn of `r_j` bits, hardwirable as a table of `2^{r_j} ≤ 2^k` entries. **`reconstruction_table_sum_le`** (PROVED): `∑_j Fintype.card (Fin (r_j)→Bool) ≤ numOther·2^k` when `∀ j, r_j<k` (`Finset.sum_le_sum` via `low_intersection_table_card`, then `Finset.sum_const`+`card_univ`+`card_fin`). **`reconstruction_total_size_le`** (PROVED): reconstructed size `predictorSize + ∑_j 2^{r_j} ≤ predictorSize + numOther·2^k` (`Nat.add_le_add_left`). **`reconstruction_poly`** (PROVED): if `2^k ≤ B` (NW regime `k=O(log m)`), size `≤ predictorSize + numOther·B`, polynomial (`gcongr`) — *why* the low-intersection design (intersection `<k`, `k` log) keeps the derandomisation efficient. **`ReconNextBit`**/**`ReconDesign numOther k r := ∀ j, r_j<k`** (the design carrier = the entry-191 low-intersection property)/**`SmallCircuitForFAt ComputesF actualSize bound := ComputesF ∧ actualSize ≤ bound`**. **`reconstruction_socket_discharge`** (PROVED, **no axioms beyond the standard three**): `Reconstruction (ReconNextBit …) (ReconDesign numOther k r) (SmallCircuitForFAt …)` — the predictor + design + correctness ⇒ a small circuit for `f` (computes `f`, size within the proved bound), `fun hComputes hr => ⟨hComputes, reconstruction_total_size_le …⟩`. Honest: proves the reconstruction's **size engine** (the hardwiring cost accounting, reusing entry-191/192 low intersection) + discharges the entry-192 `Reconstruction` socket for the concrete size-carrying predicate; the residual **`ReconstructionCorrectness`** socket (the assembled circuit *computes `f`*) is model-dependent, named not proved. Proves the size accounting, not the circuit-correctness construction. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonReconstruction` |
| **NW reconstruction correctness — the assembled circuit computes `f` (PROVED)** | Discharges the residual `ReconstructionCorrectness` socket of entry 194 by building a minimal **Boolean-function semantics** and proving the assembled circuit computes `f`. Model: seed `s:S` restricted to block `j` via `restrict j : S→I`; block `j` hardwired as `table j : I→Bool` reproducing its true value `blockVal j`; predictor `pred : (Fin numOther→Bool)→Bool` combines into `target : S→Bool` (`f`'s next bit). **`assembledOutput restrict table pred s := pred (fun j => table j (restrict j s))`** — the reconstructed circuit. **`tabulate h := h`**, **`tabulate_apply`** (PROVED, `rfl`): lookup is semantically exact (entry 192/194 bound the *size* `2^r≤2^k`; this is the *correctness*). **`reconstruction_computes`** (PROVED, **`[Quot.sound]` only**): `htable` (tables correct on the restricted seed) + `hpred` (predictor correct on the *true* block values — the Yao guarantee) ⇒ `assembledOutput … s = target s` ∀ `s`; proof `funext (fun j => htable j s)` makes the predictor's argument the true block-value vector, then `hpred s`. **`PredictorCorrect`**/**`TablesCorrect`**/**`AssembledComputesF`** — the carriers. **`reconstructionCorrectness_discharge`** (PROVED): `ReconstructionCorrectness (PredictorCorrect …) (TablesCorrect …) (AssembledComputesF …)` — discharges the entry-194 socket (`fun hpred htable s => reconstruction_computes …`). **`reconstruction_computes_tabulated`** (PROVED): with canonical tables `table j = tabulate (blockVal j)`, table correctness is definitional (`fun _ _ => rfl`), so a correct predictor alone gives a correct assembled circuit. Honest: builds the standard *semantic* circuit model (circuit ≡ its Boolean fn on a finite domain) and proves reconstruction correctness from two inputs — exact lookup tables (proved) + predictor correctness (the Yao guarantee). With entry 194's size bound, the reconstruction step is now **both size-bounded and correct**. Does not formalise gate-level syntax / `ACC⁰` membership (other sockets). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonReconCorrect` |
| **Yao predictor circuit efficiency — the predictor is a small circuit (PROVED)** | Discharges the residual `YaoCircuitEfficiency` socket of entry 193 (the predictor derived from the distinguisher is a *small circuit*) by building a minimal **gate-level Boolean circuit syntax** — exactly the gate-count content the entry-195 semantic model omits. **`Circ n`** (basis `var, ⊤, ⊥, ¬(cnot), ∧(cand), ∨(cor), ⊕(cxor)`), **`Circ.size`** (gate count), **`Circ.eval`** (Boolean semantics). **`predictor D gidx := cxor (var gidx) (cnot D)`** — outputs `g ⊕ ¬D` = (`g` if `D` accepts, `¬g` otherwise), the entry-193 guess-and-correct rule, referencing `D` once. **`yao_predictor_size`** (PROVED, `[propext]`): `size (predictor D gidx) = size D + 3` (exactly three added gates, `simp [size]; ring`). **`yao_predictor_eval`** (PROVED, **depends on NO axioms**): `eval (predictor D gidx) x = if D.eval x then x gidx else !(x gidx)` (`cases` on `D.eval x` and `x gidx`, `rfl`). **`SmallPredictor D gidx`** := `∃ P, P.size ≤ D.size+3 ∧ ∀ x, P.eval x = if D.eval x then x gidx else !(x gidx)`. **`smallPredictor_exists`** (PROVED): witness `predictor D gidx` (size `+3`, computes the rule). **`yaoCircuitEfficiency_discharge`** (PROVED, `[propext]`): `YaoCircuitEfficiency YaoNextBit (SmallPredictor D gidx)` — discharges the entry-193 socket (`fun _ => smallPredictor_exists D gidx`). Honest: builds a genuine syntactic circuit model with explicit gate count and proves the Yao step's additive `+3` size overhead + the predictor's correctness — the gate-count fact entry 195's semantic model omits. The `cxor` gate is `MOD₂ ∈ ACC⁰` so the predictor stays in `D`'s class. Tree model, `D` referenced once (no blow-up). Does not formalise `ACC⁰` *membership* of `D` (depth/gate-type), only the additive overhead of the Yao step. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonYaoCircuit` |
| **HardnessExcludesCircuit — the definitional link + the reconstruction collision (PROVED)** | Discharges the third entry-192 sub-socket (`HardnessExcludesCircuit`: a hard fn has no small circuit). **CRITICAL HONESTY:** this is the *definitional* link, NOT a circuit lower bound. "Hard against size `s`" *means* "no size-`≤s` circuit computes `f`", so `HardnessExcludesCircuit` is provable precisely as the unfolding of that definition. It does **NOT** prove any function hard — that is the genuine circuit lower bound the whole anatomy rests on (`NoEasyWitnessHardFn` / easy-witness, of separation strength), the irreducible assumption. Over the entry-196 `Circ` model: **`Computes C f := ∀ x, C.eval x = f x`**, **`HasCircuitOfSize f s := ∃ C, C.size ≤ s ∧ Computes C f`**, **`HardFor f s := ¬ HasCircuitOfSize f s`** (the *definition* of hardness). **`hardFor_excludes`** (PROVED, **NO axioms**): `HardFor f s → HasCircuitOfSize f s → False` (literally `hard hc`). **`hardness_collision`** (PROVED, **NO axioms**): a concrete `C` with `C.size ≤ s` computing `f` contradicts `HardFor f s` — the form in which the NW reconstruction (entries 194–196: reconstructed circuit size `≤ predictorSize+numOther·2^k`, correct) collides with the witness fn's hardness, the use of `HardnessExcludesCircuit` in the hybrid argument. **`hardnessExcludesCircuit_discharge`** (PROVED, **NO axioms**): `HardnessExcludesCircuit (HardFor f s) (HasCircuitOfSize f s)` = `fun hard hc => hard hc`. Honest: closes the socket's *definitional* content + the reconstruction collision; the hardness of the witness fn (`HardFor f s` for the actual witness) remains the irreducible `NoEasyWitnessHardFn` assumption, NOT proved here and not provable by these methods. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonHardness` |
| **NoEasyWitnessHardFn — the easy-witness↔hardness identification + extraction (PROVED)** | Discharges the entry-150 `NoEasyWitnessHardFn` socket (`¬SmallWitnessCircuits → HardFunction`). **CRITICAL HONESTY:** this is the *extraction* (easy half), NOT the easy-witness lemma. A NEXP witness is a truth-table string `w`; "easy" = a small circuit *describes* `w`. In the truth-table representation the function `w : (Fin n→Bool)→Bool` is the *same object*, so "small circuit describes the string" = "small circuit *computes* the function". **`EasyWitness w s := HasCircuitOfSize w s`** (over the entry-196/197 `Circ` model). **`noEasyWitness_iff_hard`** (PROVED, **NO axioms**): `¬ EasyWitness w s ↔ HardFor w s` by `rfl` — "no easy witness" and "the witness is a hard function" are the same statement. **`ExistsHardFunction n s := ∃ f, HardFor f s`**. **`noEasyWitness_extracts_hard`** (PROVED, **NO axioms**): `¬ EasyWitness w s → ExistsHardFunction n s` via `⟨w, h⟩` — the witness `w` *is* the hard function. **`noEasyWitnessHardFn_discharge`** (PROVED, **NO axioms**): `NoEasyWitnessHardFn (EasyWitness w s) (ExistsHardFunction n s)` = `fun h => ⟨w, h⟩` — discharges the entry-150 socket. Honest: proves only the identification/extraction (a witness lacking a small circuit *is* a hard function); does NOT prove the easy-witness lemma nor that any NEXP language lacks easy witnesses — that the hypothesis `¬EasyWitness` ever holds is the genuine circuit lower bound, of separation strength, NOT discharged here. Closing this socket isolates the irreducible Williams-side assumption to a single statement — *a NEXP language without easy witnesses exists* = a hard function exists = the separation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NisanWigdersonEasyWitness` |
| **DerandKarpLiptonSeparation — the Karp–Lipton collapse chain (composition glue PROVED)** | Decomposes the entry-189 `DerandKarpLiptonSeparation` socket (`Derandomization → ¬(NEXP ⊆ ACC⁰)`) into the classical Karp–Lipton collapse chain and proves the composition glue. **`KarpLiptonCollapse NEXP ACC0 MA := NEXP ⊆ ACC0 → NEXP = MA`** — the Babai–Fortnow–Lund / IKW collapse (if `NEXP` has small circuits the doubly-exponential guess-verify collapses it into `MA`); named socket. **`DerandCollapsesMAtoNP Derandomization MA NP := Derandomization → MA = NP`** — a PRG simulates Arthur's coins nondeterministically; named socket. **`NexpNeqNp NEXP NP := NEXP ≠ NP`** — the nondeterministic time hierarchy; named socket. **`derandKarpLipton_discharge`** (PROVED, **NO axioms**): the three ⇒ `DerandKarpLiptonSeparation NEXP ACC0 Derandomization`, by `fun hd hsub => hier ((kl hsub).trans (derand hd))` — a derandomisation `hd` + circuits `hsub:NEXP⊆ACC⁰` give `NEXP=MA` (KL) and `MA=NP` (derand), so `NEXP=NP` by transitivity, contradicting `NEXP≠NP`. Honest: proves the *logical composition* of the Karp–Lipton route; the three ingredients (KL/BFL collapse, derandomisation collapse, hierarchy) remain named classical sub-sockets needing complexity-theory infrastructure (guess-verify, hardness–randomness, diagonalisation) absent here. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0KarpLiptonCollapse` |
| **NexpNeqNp / nondet time hierarchy — the diagonalization kernel (PROVED)** | Discharges the entry-199 `NexpNeqNp` sub-socket (`NEXP ≠ NP`) — the one classically **unconditional** Williams ingredient (Cook / Seiferas–Fischer–Meyer nondeterministic time hierarchy) — by proving its genuine heart, **Cantor diagonalization**. **`enc n := List.replicate n true`**, **`enc_injective`** (PROVED, by length). **`diag enum w := ∀ i, enc i = w → ¬ enum i w`** — the diagonal language flipping machine `i` on its own encoding. **`diag_eval`** (PROVED): `diag enum (enc i) ↔ ¬ enum i (enc i)` (the `∀` collapses by injectivity). **`diag_ne`** (PROVED, `[propext]`): `diag enum ≠ enum i` ∀`i` — else `congrFun` gives `enum i (enc i) ↔ ¬ enum i (enc i)`, killed by `iff_not_self`. **`diag_not_mem_range`** (PROVED, `[propext]`): `diag enum ∉ Set.range enum` — the diagonal escapes the whole enumeration. **`NPEnumerable NP := ∃ enum, NP = Set.range enum`** (NP = range of a clocked-NTM enumeration; socket), **`DiagonalInNexp NEXP NP := ∀ enum, NP = range enum → diag enum ∈ NEXP`** (the diagonal is NEXP-decidable; socket, hiding the NTM lazy-diagonalization). **`nexpNeqNp_discharge`** (PROVED, `[propext]`): `NPEnumerable NP → DiagonalInNexp NEXP NP → NexpNeqNp NEXP NP` — were `NEXP = NP = range enum`, the diagonal (in NEXP) would lie in `range enum`, contradicting `diag_not_mem_range`. Honest: proves the genuine unconditional **diagonalization kernel** (`diag ∉ range enum`) + the discharge; the two model sockets (`NPEnumerable`, `DiagonalInNexp`) make it apply to NTMs — and `DiagonalInNexp` hides the genuinely NTM-specific subtlety (nondet classes not known closed under complement ⇒ the real theorem uses Cook's *lazy* diagonalization, not the direct complement `¬enum i w` used here at set level). Set-theoretic diagonalization proved, not the lazy-diagonalization simulation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NondetTimeHierarchy` |
| **GuessVerify discharge — language-level collapse + entry-187 fast-SAT time bound (PROVED)** | Connects three completed pieces: the proved language-level guess-and-verify collapse (`…ACC0GuessVerify`: `gvDecider_eq`, `guessVerify_subset`), the abstract entry-150 `GuessVerify` socket (`SmallWitnessCircuits → ACC0SatSpeedup → NTIME[2ⁿ] ⊆ NTIME[2ⁿ/superpoly]`), and the entry-187 fast-`ACC⁰`-SAT verifier. **`ProvidesFastVerifiers NTIME2n NTIME2nFast ACC0SatSpeedup SmallWitnessCircuits`** := small witnesses + speedup ⇒ ∀ `L ∈ NTIME2n`, a verifier with the small-witness reduction whose `gvDecider` is in the fast class (named socket, the NTM-cost placement). **`guessVerify_discharge`** (PROVED, `[propext, Quot.sound]`): `ProvidesFastVerifiers ⇒ GuessVerify NTIME2n NTIME2nFast ACC0SatSpeedup SmallWitnessCircuits`, via the *proved* `guessVerify_subset` (`fun hsw hspeed => guessVerify_subset _ _ (prov hsw hspeed)`) — discharges the entry-150 socket from the language-level collapse. **`guess_verify_beats_bruteforce_abstract`** (PROVED): `verifyWork ≤ (D+1)·n^D+1`, `guessCost + ((D+1)·n^D+1) < 2ⁿ` ⇒ `guessCost + verifyWork < 2ⁿ`. **`guess_verify_beats_bruteforce`** (PROVED): the concrete tie-in — with verify work the entry-187 `fast_sat_verifier` count-cell image (`≤ (D+1)·n^D+1`), `guessCost + (verify work) < 2ⁿ`: guess the small circuit, verify with fast `ACC⁰`-SAT, beat `2ⁿ`. Honest: discharges the entry-150 socket from the proved language-level collapse + proves the concrete time bound (why `ProvidesFastVerifiers` should hold); the `NTM`-cost placement (`ProvidesFastVerifiers`) is the remaining socket. Does NOT overwrite the existing `…ACC0GuessVerify` (refines it). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GuessVerifyDischarge` |
| **Collapse ingredients — `DerandCollapsesMAtoNP` + `KarpLiptonCollapse` (antisymmetry glue PROVED)** | Opens the two *class-equality* entry-199 sub-sockets into their two-inclusion structure and proves the antisymmetry glue. `DerandCollapsesMAtoNP` (`MA = NP`): **`DerandMASubsetNP`** (`Derand → MA ⊆ NP`, derandomisation) + **`NPSubsetMA`** (`NP ⊆ MA`, trivial). **`derandCollapsesMAtoNP_discharge`** (PROVED, `[propext, Quot.sound]`): `fun hd => Set.Subset.antisymm (fwd hd) bwd`. `KarpLiptonCollapse` (`NEXP = MA`): **`NexpSubsetMA_ofCircuits`** (`NEXP ⊆ ACC0 → NEXP ⊆ MA`, the deep BFL collapse) + **`MASubsetNexp`** (`MA ⊆ NEXP`, trivial). **`karpLiptonCollapse_discharge`** (PROVED): `fun hsub => Set.Subset.antisymm (deep hsub) triv`. Honest: proves the antisymmetry structure discharging both sockets; the deep inclusions (`MA ⊆ NP` via derand, `NEXP ⊆ MA` via BFL) stay named sub-sockets (classical, need complexity infra). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CollapseIngredients` |
| **Hierarchy witness model — `NPEnumerable` + `DiagonalInNexp` discharged end-to-end (PROVED)** | Shows the two entry-200 model sockets are non-vacuous and the diagonalization discharge fires end-to-end in an explicit model. **`npEnumerable_range`** (PROVED, NO axioms): `NPEnumerable (Set.range enum)` = `⟨enum, rfl⟩`. **`diagonalInNexp_univ`** (PROVED): `DiagonalInNexp Set.univ (Set.range enum)` (every diagonal ∈ univ). **`witness_nexpNeqNp`** (PROVED, `[propext]`): `NexpNeqNp Set.univ (Set.range enum)` via entry-200 `nexpNeqNp_discharge` — yields the *genuine true* `Set.univ ≠ Set.range enum` (the diagonal escapes the enumeration, `diag_not_mem_range`). Honest: a witness/consistency model (NP = enumeration, NEXP = all languages), NOT the real NEXP/NP; demonstrates the diagonalization kernel bites and the socket interface is satisfiable. Does not prove the actual `NEXP ≠ NP`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0HierarchyWitness` |
| **Concrete transition-table run — a worked table-driven machine (PROVED)** | Realizes the existing transition-table apparatus (`…ACC0ConcreteNTM`: `TMTrans`, `applyTrans`, `concreteStep`, `toNTM`) on a worked example. **`flipTable := [((0,false),(1,true,stay))]`** — one rule. **`flip_applyTrans`** (PROVED, `decide`): `applyTrans (0,0,[false]) rule = (1,0,[true])` (exercises `writeAt`/`moveHead`). **`flip_step`** (PROVED): `concreteStep flipTable (0,0,[false]) (1,0,[true])`. **`flip_reachIn`** (PROVED): `reachIn (toNTM flipTable) 1 …`. **`flip_accepts`** (PROVED): `acceptsWithin (toNTM flipTable) [false] 1`. Honest: a worked realization confirming the transition-table semantics (`readSym`/`writeAt`/`moveHead`/`applyTrans`/`concreteStep`/`reachIn`/`acceptsWithin`) compute correctly; does NOT build the time-bounded universal simulation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ConcreteTMRun` |
| **BT additive bottom-layer merge — pooling into one symmetric gate (PROVED)** | The complementary *additive* merge to the multiplicative `SYM∘AND` tree (`hasSymAndFormFanIn_combine`, `s₁+(s₁+1)·s₂`). **`hasSymAndFormFanIn_pool`** (PROVED): when two monomial families feed *one* symmetric gate (the top reads `saCount mono₁ x + saCount mono₂ x`), the form has size `card ι₁ + card ι₂` (**ADDITIVE**, no blow-up) and fan-in `w` — via `saCount_sum_elim` (pooled count = sum) + `Fintype.card_sum`. **`hasSymAndFormFanIn_family`** (PROVED): a `SYM∘AND` over `m` monomials (`Fin m → Finset`) has size exactly `m`. Honest: proves the additive bottom-layer count (single symmetric layer over poly-many monomials), the `FanInStaysPolylog` regime; what stays socketed is the BT depth-collapse (an entire `ACC⁰` circuit collapses to ONE symmetric layer of quasipoly monomials — the multiplicative tree replaced by a single additive layer). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymAndPool` |
| **Beigel–Tarui depth-collapse — low degree ⇒ quasipolynomial `SYM∘AND` (PROVED, RS socketed)** | The *quasipolynomial* collapse (vs the exponential multiplicative `acc0circuit_hasSymAndFormFanIn`). The exact collapse's `symAndSize` is multiplicative (exponential); BT's contribution is quasipoly, via the probabilistic polynomial method: approximate by a low-degree polynomial, then collapse that. **HEART (proved): low degree ⇒ few monomials.** **`LowDegRep f D := ∃ m mono h, Function.Injective mono ∧ (∀ j, (mono j).card ≤ D) ∧ f = fun x => h (saCount mono x)`** (the RS low-degree representation). **`lowDeg_symAnd_quasipoly`** (PROVED): an injective degree-`≤D` family is a `SYM∘AND` of fan-in `≤D` with size `m ≤ (D+1)·n^D` — via `monomial_count_le` (`m ≤ ∑_{i≤D} C(n,i)`, distinct `≤D`-monomials) + `lowDegMonomialCount_le_pow` (`∑_{i≤D} C(n,i) ≤ (D+1)·n^D`). **`btQuasipolyCollapse`** (PROVED): `LowDegRep f D → ∃ m, HasSymAndFormFanIn f m D ∧ m ≤ (D+1)·n^D`. **`AccToLowDeg f p t d := LowDegRep f (((p-1)·t)^d)`** — the RS socket: a depth-`d` `ACC⁰[p]` circuit has a degree-`((p−1)t)^d` representation (probabilistic polynomial method). **`btDepthCollapse`** (PROVED, `[propext, Classical.choice, Quot.sound]`): `AccToLowDeg f p t d → ∃ m, HasSymAndFormFanIn f m (((p−1)t)^d) ∧ m ≤ (((p−1)t)^d + 1)·n^{((p−1)t)^d}` — quasipoly (`n^polylog`) for `d` constant, `t` polylog. Honest: proves the **degree⇒size** half (low degree ⇒ quasipoly `SYM∘AND`, the combinatorial heart, fixing the exponential naive bound); the **circuit⇒degree** half (`AccToLowDeg`, the Razborov–Smolensky approximation — each gate by a low-degree polynomial with small error, composed across constant depth) remains the named socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BTDepthCollapse` |
| **Connecting `AccToLowDeg` to the proved RS approximation** | Discharges the entry-203 `AccToLowDeg` socket from the **already-proved** RS approximation `Layer3ACC0LowRank.acc0_approx_by_lowRankPredictor` (an `AC⁰[p]` circuit at horizon `t`, `p^t ≥ 4·#subcircuits`, has an `F_p`-span approximant of degree **`((p−1)·t)^C.depth`** agreeing on `≥3/4` — the genuine probabilistic polynomial method), reducing it to the precise residual span→`SYM∘AND` bridge. **`SpanApproxToLowDegRep C p t`** := (RS `F_p`-span approximant of `C`, degree `((p−1)·t)^C.depth`, `≥3/4` agreement) → `LowDegRep (C.eval) (((p−1)·t)^C.depth)` — the residual bridge (named socket: monomials with `≤p−1` multiplicity so value = count-mod-`p` of satisfied `AND`s; `3/4`→exact amplification). **`accToLowDeg_via_rs`** (PROVED, `[propext, Classical.choice, Quot.sound]`): for `AC⁰[p]` `C` with the size hypotheses, `SpanApproxToLowDegRep C p t → AccToLowDeg (C.eval) p t C.depth`, by `bridge (acc0_approx_by_lowRankPredictor …)`. The RS degree `((p−1)·t)^C.depth` matches `AccToLowDeg`'s `((p−1)·t)^d` **exactly** (`d = C.depth`). Honest: the *circuit⇒low-degree* half is no longer assumed — it is taken from the existing RS proof; what remains is the **span→`SYM∘AND` representation bridge** (`F_p`-linear approximant ⇒ exact Boolean count-mod-`p` `SYM∘AND`). Reduces the bare `AccToLowDeg` socket to that bridge. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BTRSConnection` |
| **The count-mod-`p` multiplicity collapse — the arithmetic heart of the span→`SYM∘AND` bridge (PROVED)** | Splits the entry-204 `SpanApproxToLowDegRep` bridge into its two pieces — the multiplicity/count-mod-`p` expansion (provable arithmetic) and the `3/4`→exact amplification (deep) — and **proves the arithmetic heart**. The idea: an `F_p`-combination `∑_S c_S·[S sat]` is a *count of `AND`-copies* (replace `S` by `c_S` copies). **`saCount_sigma_expand`** (PROVED): `saCount` over the copy index `Σ s, Fin (c s)` `= ∑_s c_s·[base s sat]` (the weighted count = polynomial value over `ℕ`; via `Finset.sum_sigma` + `Fin.sum_const`). **`saCount_sigma_cast`** (PROVED): that count cast to `ZMod p` `= ∑_s (c_s : ZMod p)·[base s sat]` — the count mod `p` *is* the `F_p` polynomial value (`push_cast`). **`countModP_collapse`** (PROVED): a decode `h ∘ saCount` of the `AND`-copy multiset (degree `≤ D`) is `HasSymAndFormFanIn` of size `∑_s c_s`, fan-in `≤ D` — *no injectivity needed* (multiplicity via the sigma index). **`expand_card_le_mult`** (PROVED): `∑_s c_s ≤ K·M` for `c_s ≤ M`; with `M = p−1`, `K ≤ (D+1)·n^D`, size `≤ (p−1)(D+1)n^D` (quasipoly). **`ExactCountModPRep f D`** — the residual socket: `f` has an *exact* count-mod-`p` multiset representation of degree `≤ D` (the `3/4`→exact amplification + span-coefficient extraction). **`bridge_via_countModP`** (PROVED): `ExactCountModPRep f D → ∃ m, HasSymAndFormFanIn f m D`. Honest: proves the multiplicity/count-mod-`p` arithmetic (an `F_p`-combination *is* a count-mod-`p` symmetric function, collapsing to a quasipoly `SYM∘AND`) completely; removes that content from the entry-204 bridge, leaving only the amplification socket `ExactCountModPRep`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CountModP` |
| **The `3/4`→exact amplification — the majority-vote exactness skeleton (PROVED)** | Proves the deterministic skeleton of the amplification residual (`ExactCountModPRep`'s deep content): a pointwise-majority-correct family of approximants has an *exact* majority vote. **`majBool v := decide (k < 2·#{i | v i = true})`** — Boolean majority. **`count_compl`** (PROVED): `#{i | v i = true} + #{i | v i = false} = k` (`card_filter_add_card_filter_not` + `filter_congr` on `v i = false ↔ ¬ v i = true`). **`majBool_eq_of_count`** (PROVED): `k < 2·#{i | v i = b} → majBool v = b` (cases on `b`; `true` direct, `false` via `count_compl` + `omega`). **`MajVote g x := majBool (fun i => g i x)`**. **`maj_exact`** (PROVED): `(∀ x, k < 2·#{i | g i x = f x}) → MajVote g = f` — if everywhere more than half the family is correct, the majority is exact (`funext` + pointwise). **`MajorityGoodFamily f`** := `∃ k g, ∀ x, k < 2·#{i | g i x = f x}` — the residual probabilistic socket (such a family exists, via independence/Chernoff per-input error `< 2^{-n}` union-bounded over `2^n` inputs). **`exact_from_majorityGood`** (PROVED): `MajorityGoodFamily f → ∃ k g, MajVote g = f`. Honest: proves the **deterministic majority-vote skeleton** (majority of a pointwise-correct family is exact) — genuine Boolean/counting arithmetic, complete. Residuals: the *probabilistic existence* of the good family (`MajorityGoodFamily`, needs a probability framework) and the `MAJ∘SYM∘AND` closure (that the majority of `SYM∘AND`s is itself a `SYM∘AND`). Proves the skeleton, not the probabilistic existence. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Amplification` |
| **Span-coefficient extraction — `F_p`-span ⇒ explicit count-mod-`p` multiplicities (PROVED)** | Discharges the span-coefficient extraction residual of the span→`SYM∘AND` bridge, connecting entry-204's RS span output to entry-205's count-mod-`p` input. **`squarefree_eq_monoAND_ind`** (PROVED): `squarefreeEvalMonomial p S x = ∏_{i∈S} boolToZMod(x_i) = (if monoAND S x then 1 else 0)` over `ZMod p` (`by_cases` on `∀ i∈S, x i`; `prod_eq_one`/`prod_eq_zero`). **`span_eval_weightedCount`** (PROVED, `[propext, Classical.choice, Quot.sound]`): an `F_p`-span element `f` of the degree-`≤D` squarefree monomials has explicit coefficients `c : {S | S ∈ lowDegMonomials n D} → ZMod p` with each multiplicity `(c S).val < p` (`ZMod.val_lt`) and `f x = ∑_S c_S·(if monoAND S.1 x then 1 else 0)` ∀`x` — via `Submodule.mem_span_range_iff_exists_fun` (the span element *is* `∑ c_S • v_S`), `Finset.sum_apply` + `Pi.smul_apply` + `smul_eq_mul`, and `squarefree_eq_monoAND_ind`. This is exactly the count-mod-`p` weighted-value form that entry-205's `saCount_sigma_cast` consumes (`c_S.val` = the `AND`-copy multiplicities). Honest: proves the extraction completely (the RS `F_p`-span output *is* an explicit weighted count of `monoAND` indicators with bounded `<p` multiplicities) — linear algebra + `ZMod.val`. Connects entry-204 span → entry-205 input; only the `3/4`→exact amplification (entry 206 skeleton + `MajorityGoodFamily` socket) remains of the bridge. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SpanExtract` |
| **Chernoff amplification existence — probabilistic method + union bound (PROVED), concentration socketed** | Discharges the entry-206 `MajorityGoodFamily` socket from a *single* per-input concentration socket, proving the probabilistic-method scaffolding. Over `k`-tuples `Fin k → ((Fin n→Bool)→Bool)`, a tuple is *bad* if at some input the majority errs (`errsAt := 2·#{i | g i x = f x} ≤ k`). **`exists_good_of_bad_lt`** (PROVED): `#{bad tuples} < #tuples → ∃` tuple good at every input — pigeonhole (`by_contra`; all-bad ⇒ bad filter `= univ`, `card_univ`, `lt_irrefl`). **`union_bound`** (PROVED): `#{bad} ≤ ∑_x #{bad at x}` (`Finset.card_biUnion_le`). **`ChernoffPerInput f k := ∃ M, (∀ x, #{tuples bad at x} ≤ M) ∧ #inputs·M < #tuples`** — the per-input concentration socket (bad tuples a `< 2^{-n}` fraction at each input). **`majorityGoodFamily_of_chernoff`** (PROVED, `[propext, Classical.choice, Quot.sound]`): `ChernoffPerInput f k → MajorityGoodFamily f` — sum bound (`∑_x #{bad at x} ≤ #inputs·M`) + union bound ⇒ `#bad < #tuples` ⇒ a good tuple exists ⇒ pointwise-majority-correct family (`not_le.mp`). Honest: proves the **probabilistic-method logic** (positive count ⇒ existence, pigeonhole) + union + sum bounds completely; the single residual `ChernoffPerInput` *is* the genuine Chernoff concentration (independence of the `k` approximants ⇒ per-input majority-error `< 2^{-n}`), needing a probability/independence framework absent here. Proves the existence scaffolding, not the concentration inequality. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ChernoffExist` |
| **The combinatorial Chernoff bound — the binomial-tail inequality (PROVED)** | The genuine analytic heart of the per-input concentration, proved *directly without measure theory*. **CORRECTION (honest):** entry 208's `ChernoffPerInput` over *all* `k`-tuples of *all* functions is **false** (uniform all-functions ⇒ each correct w.p. `1/2` ⇒ majority errs w.p. `≈1/2`, not `<2^{-n}`); the genuine concentration is over a **`≥3/4` supply** (the RS-distribution support). **`term_mono`** (PROVED): for `j ≤ m ≤ k`, `b ≤ a`, `a^j·b^{k−j} ≤ a^m·b^{k−m}` (`b^{k−j}=b^{k−m}·b^{m−j}`, `b^{m−j}≤a^{m−j}` by `Nat.pow_le_pow_left`). **`binomial_tail_le`** (PROVED, clean axioms): `∑_{j≤m} C(k,j)·a^j·b^{k−j} ≤ 2^k·a^m·b^{k−m}` — every tail term `≤` the top term (`term_mono`), and `∑_{j≤m} C(k,j) ≤ ∑_{j≤k} C(k,j) = 2^k` (`Nat.sum_range_choose`). In the `3/4` regime (`a ≥ 3b`, `m ≈ k/2`) this is `exp(−Ω(k))` of `(a+b)^k` — the Chernoff decay. Honest: proves the combinatorial Chernoff *inequality* completely (pure `Nat` arithmetic, no probability infrastructure) and documents the entry-208 space correction. Does NOT prove (i) the fiber-counting reduction (bad-tuple count at a fixed input `= ∑ C(k,j)·a^j·b^{k−j}`, a standard multinomial count) or (ii) the `k=Ω(n)` decay-to-`2^{-n}` arithmetic — the plumbing to the corrected `ChernoffPerInput`-over-supply. Proves the inequality, not the full per-input concentration. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ChernoffBound` |
| **The fiber-count reduction — bad approximant-tuples bounded by the binomial tail (PROVED)** | Connects the entry-209 Chernoff bound to the tuple-counting: the number of `k`-tuples of supply approximants whose majority errs at a fixed input is `≤ 2^k·a^m·b^{k−m}` (`a = #correct`, `b = #wrong` at the input). Reduction via the **correctness pattern** `pat g := fun i => pred (g i) : Fin k → Bool`. **`fiber_card`** (PROVED): `#{g : Fin k → α | (fun i => pred (g i)) = p} = a^{#trues p}·b^{#falses p}` — `Fintype.card_piFinset` (the fiber is `Π i, {x | pred x = p i}`), then `∏_i (if p i then a else b) = a^{#trues}·b^{#falses}` (`Finset.prod_ite` + `Finset.prod_const`). **`bad_tuple_count_le`** (PROVED, clean axioms): `#{g | #{i | pred (g i)} ≤ m} ≤ 2^k·a^m·b^{k−m}` (for `b ≤ a`, `m ≤ k`) — `Finset.card_eq_sum_card_fiberwise` partitions the bad tuples by pattern; each fiber `≤ a^m·b^{k−m}` (`fiber_card`, then entry-209 `term_mono` since a bad pattern has `#trues ≤ m`, with `#falses = k − #trues` by `card_filter_add_card_filter_not`); and there are `≤ #{patterns} = 2^k` (`Fintype.card_fun`). Honest: proves the genuine **tuple-count ≤ binomial-tail reduction** completely (`Finset`/`Fintype` counting, reusing entry-209 `term_mono`); with entry 209 this is the per-input bound over a supply with `a` correct, `b` wrong. Residual to the corrected `ChernoffPerInput`-over-supply: the **`k=Ω(n)` decay** (`2^k·a^m·b^{k−m}·2^n < (a+b)^k` in the `≥3/4` regime) + the construction of a `≥3/4` supply (from `acc0_approx_by_lowRankPredictor`). Proves the counting reduction, not the decay arithmetic. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FiberCount` |
| **The decay arithmetic — full per-input concentration over a `3/4` supply (PROVED)** | Proves the decay that drives the entry-210 bound below the `2^{-n}` fraction, and **assembles the full per-input concentration**. **`decay_core`** (PROVED): `2^n·3^j < 4^j` for `j ≥ 3n+1` — split at `j=3n`: `2^n·3^{3n} = 54^n ≤ 64^n = 4^{3n}` (`Nat.pow_le_pow_left`, `54≤64`) and `3^{j−3n} < 4^{j−3n}` (`Nat.pow_lt_pow_left`, strict since `j−3n ≥ 1`). **`decay_full`** (PROVED): `2^n·(2^{2j}·(3b)^j·b^{2j−j}) < (3b+b)^{2j}` (`b ≥ 1`, `j ≥ 3n+1`) — `#inputs·(entry-210 bound at a=3b,k=2j,m=j) < #tuples`; reduces to `decay_core` by factoring the common `4^j·b^{2j}` and `Nat.mul_lt_mul_right`. **`chernoff_per_input`** (PROVED, clean axioms): for a `3/4` supply (`#correct = 3·#wrong`, `#wrong ≥ 1`), `k = 2j`, `j ≥ 3n+1`, `2^n · #{g : Fin(2j)→α | #correct ≤ j} < (#correct+#wrong)^{2j}` — the per-input concentration `#inputs·#{bad} < #tuples`, assembling entry-210 `bad_tuple_count_le` (`#{bad} ≤ 2^{2j}·(3b)^j·b^j`) with `decay_full` (`Nat.mul_le_mul_left` then `decay_full`). Honest: proves the decay arithmetic + the **full per-input concentration over an (assumed) `3/4` supply** at `k = 2j = Θ(n)` — completely, pure `Nat`, combining entries 209/210/decay. Exactly the per-input bound the corrected (supply-restricted) `ChernoffPerInput` requires. The **last** residual of the whole BT depth-collapse: the **construction of a `3/4` supply** (that RS approximants — `acc0_approx_by_lowRankPredictor`, `3/4` *global* — arrange as a supply with `≥3/4` correct *at every input*). Proves the concentration over an assumed supply, not the supply's existence. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ChernoffDecay` |
| **The supply construction — per-input `3/4` supply ⇒ tuple good at every input (PROVED)** | The amplification assembly: a per-input `3/4` supply yields a tuple correct-majority at *every* input. **Honest subtlety:** the repo's RS `acc0_approx_by_lowRankPredictor` gives only *global* `3/4` (one fn, `3/4` of inputs), which does NOT give a per-input `3/4` supply (a fixed fn is wrong on a fixed `1/4`); the per-input property is the genuine RS per-point bound (`≤1/4` over the random polynomial), stronger than the formalised global one. **`Uniform34 corr w`** := `∀ x, #{a | corr x a} = 3w ∧ #{a | ¬corr x a} = w` — the per-input `3/4`-supply socket (the RS per-point guarantee; `|α| = 4w`). **`exists_good_tuple`** (PROVED, clean axioms): `Uniform34 corr w → w ≥ 1 → 3n+1 ≤ j → ∃ g : Fin (2j) → α, ∀ x, ¬ #{i | corr x (g i)} ≤ j` (majority correct at every input) — the bad tuples (bad at some input) number `≤ ∑_x #{bad at x}` (union bound, `card_biUnion_le`) `≤ 2^n · M` (entry-210 `bad_tuple_count_le` per input, `M = 2^{2j}·(3w)^j·w^j` uniform; `card α = 4w` via `card_filter_add_card_filter_not`) `< #tuples = (4w)^{2j}` (entry-211 `decay_full`); fewer than all tuples bad ⇒ a good tuple exists (pigeonhole `filter_true_of_mem` + `lt_irrefl`). Honest: proves the **amplification assembly** completely (union bound + entries 210/211 + pigeonhole, pure `Nat`/`Finset`) — the amplified `MajorityGoodFamily` witness the NW reconstruction needs (entry 206 then makes its majority *exact*). What remains is the named socket **`Uniform34`**: the *existence* of a per-input `3/4` supply — the irreducible RS per-point analytic bound, stronger than the repo's global `3/4`. Proves everything downstream of the supply, not the supply's existence. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SupplyConstruction` |
| **The Razborov–Smolensky per-point error bound — the detection involution (PROVED)** | The algebraic heart of the RS per-point guarantee — the source of the `Uniform34` per-input `3/4`-supply (entry 212). To detect a nonzero input vector, a random subset `S` tests `∑_{i∈S} x i ≠ 0` (the `OR`-approximant). **`toggle j S := S △ {j}`** (`erase`/`insert`), **`toggle_involutive`** (PROVED, an involution). **`subsetSum_toggle`** (PROVED): `∑_{toggle j S} x = ∑_S x ± x j` (`Finset.sum_erase_eq_sub`/`sum_insert` + `abel`). **`detection_half`** (PROVED, clean axioms): if `x j ≠ 0`, `2^s ≤ 2·#{S | ∑_{i∈S} x i ≠ 0}` (random subset detects w.p. `≥1/2`) — the involution `toggle j` maps each missed subset (`sum=0`) injectively to a detected one (`sum=±x j≠0`, `card_le_card_of_injOn`), so `#{miss} ≤ #{detect}`, with `#{miss}+#{detect}=2^s`. **`detection_two_thirds_four`** (PROVED): `3·4^s ≤ 4·#{(S₁,S₂) | sum₁≠0 ∨ sum₂≠0}` (two trials boost to `≥3/4`, per-point error `≤1/4`) — `#{both miss}=miss²` (`card_product`), `miss≤2^{s−1}`, so `4·miss²≤4^s` and `#{detect}=4^s−miss²≥3·4^{s−1}`. Honest: proves the genuine **per-point detection bound** (`≥1/2` single-trial via the involution, `≥3/4` two-trial) — the analytic core of RS per-point — completely, pure `AddCommGroup`/`Finset`, no measure theory. What remains to fully discharge `Uniform34`: the **gate-to-supply packaging** — that this subset-sum test *is* the `OR`/`MOD`-gate `F_p`-approximant (`(∑)^{p−1}` Fermat indicator; `OR(x)=0` ⇒ sum always `0`, always correct), and the RS **composition** across the constant-depth circuit. Proves the per-point detection, not the gate-encoding/composition packaging. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RSPerPoint` |
| **The gate-encoding — the subset-sum test is the `OR`-gate Fermat indicator over `F_p` (PROVED)** | Encodes the abstract entry-213 detection as the actual RS `OR`-gate approximant `(∑)^{p−1}` over `F_p`. **`fermat_ind`** (PROVED): `y^{p−1} = if y = 0 then 0 else 1` over `ZMod p` (`p` prime) — Fermat's little theorem (`ZMod.pow_card_sub_one_eq_one`) for `y ≠ 0`, `zero_pow` (`p−1 ≥ 1`) for `y = 0`. **`orApprox x S := (∑_{i∈S} x i)^{p−1}`** — the `OR`-approximant (degree-`(p−1)` `F_p`-polynomial). **`orApprox_eq_one_iff`** (PROVED): `orApprox x S = 1 ↔ ∑_{i∈S} x i ≠ 0` (via `fermat_ind`; `zero_ne_one` for the `sum=0` branch). **`orApprox_zero`** (PROVED): `(∀ i ∈ S, x i = 0) → orApprox x S = 0` — no false positive (`OR(x)=0` always correct, `Finset.sum_eq_zero` + `zero_pow`). **`orApprox_detection`** (PROVED, clean axioms): if `x j ≠ 0` then `2^s ≤ 2·#{S | orApprox x S = 1}` — the entry-213 detection bound transported to the gate (`filter_congr` with `orApprox_eq_one_iff`, then `detection_half`). Honest: proves the **single-gate encoding** completely (Fermat's little theorem, pure `ZMod p`/`Finset`) — the subset-sum detection *is* the `OR`-gate `(∑)^{p−1}` approximant, `OR=0` always correct, `OR=1` detected w.p. `≥1/2`; combined with 209–213 this gives a per-input `≥3/4` random approximant *at the gate level* (the `Uniform34` ingredient). Residual to discharge `Uniform34` for a *circuit*: the **RS composition** — composing per-gate approximants across the constant-depth `ACC⁰[p]` circuit (errors union-bounded over gates). Proves the single-gate encoding, not the circuit composition. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GateEncoding` |
| **Pointwise RS composition — single-gate per-point ⇒ whole-circuit per-input `3/4` supply (PROVED)** | The last BT-side structural bridge: lifts the single-gate per-point `≥3/4` (entry 214) to a whole constant-depth-circuit per-input `≥3/4` supply (`Uniform34`), via a union bound over the gates. **`Composes gateWrong circuitWrong := ∀ x σ, circuitWrong x σ → ∃ g, gateWrong g x σ`** — the composition socket (all gates right ⇒ circuit right; the gate-substitution semantics). **`circuit_perpoint_3_4`** (PROVED, clean axioms): given `Composes`, a per-gate per-point bound `∀ g x, #{σ | gateWrong g x σ} ≤ M`, and `4·(G·M) ≤ |Seed|` (`G = Fintype.card γ`, the gate count; each gate's per-point error `≤ 1/(4G)`), then `3·|Seed| ≤ 4·#{σ | ¬ circuitWrong x σ}` at *every* input `x` (correct on `≥3/4` of seeds). Proof: the circuit-wrong seeds inject into `⋃_g {gate g wrong}` (`Composes`, `card_le_card`), so `#{circuit wrong} ≤ ∑_g #{gate g wrong} ≤ G·M` (`Finset.card_biUnion_le` + `sum_le_sum` + `sum_const`); with `#{correct} + #{wrong} = |Seed|` (`card_filter_add_card_filter_not`) and `4·G·M ≤ |Seed|`, `omega`. Honest: proves the **pointwise composition** completely (the union bound over `G` gates, pure `Finset`/`Fintype` counting, no measure theory) — the seed space with the circuit-correct predicate *is* a per-input `≥3/4` supply, the `Uniform34` bridge. Residual: **`Composes`** (the gate-substitution circuit-semantics, cf. entry-195 reconstruction correctness). Also: entry-212 `Uniform34` used *exact* `3w`; this gives the genuine `≥3/4`. Proves the composition counting, not the gate-substitution semantics. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RSPointwiseComposition` |
| **RS supply ⇒ exact majority representation (step-2 assembly, PROVED)** | Assembles the BT-side amplification pipeline (209–215) into one chain: a per-input `3/4` supply of approximant-functions yields an *exact* majority representation of `f`. **`supply_to_exact_majority`** (PROVED, clean axioms): given `ev : α → (Fin n → Bool) → Bool` (each supply element is a Boolean function), `Uniform34 (fun x a => decide (ev a x = f x)) w` (the per-input `3/4` supply, agreement with `f`), `w ≥ 1`, `j ≥ 3n+1`, there is `g : Fin (2j) → ((Fin n → Bool) → Bool)` with `MajVote g = f` (entry-206) — an exact representation of `f` as a majority of `2j` approximants. Proof: entry-212 `exists_good_tuple` gives a tuple `g₀ : Fin (2j) → α` majority-correct at every input (`j < #{i | ev (g₀ i) x = f x}`, after `decide_eq_true_eq` count-matching via `filter_congr`); mapping through `ev` gives `MajVote (ev ∘ g₀) = f` by entry-206 `maj_exact` (the hypothesis `2j < 2·#{correct}` follows by `omega`). Honest: proves the **supply ∘ amplification ∘ majority-exactness** assembly completely (pure `Finset` counting), realising the BT-side amplification end to end — an `ACC⁰[p]` circuit has an exact representation as a majority vote of `2j` low-degree (`SYM∘AND`-encodable) approximants. Residual: (i) the `MAJ∘SYM∘AND` collapse to a *single* quasipoly `SYM∘AND` (entry-203 depth-collapse — separate structural step); (ii) uses entry-212 `Uniform34` (*exact* `3w`) vs entry-215's `≥3/4` (the idealisation gap). Proves the supply-to-exact-majority assembly, not the `MAJ∘SYM∘AND` collapse. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RSSupplyToExact` |
| **The `MAJ∘SYM∘AND` collapse — majority of `SYM∘AND`s is a `SYM∘AND`, but exponentially (PROVED)** | The structural collapse of the entry-216 majority-of-approximants into a single `SYM∘AND` — **with the honest size caveat**. **`IsSymAnd f := ∃ s w, HasSymAndFormFanIn f s w`**. **`isSymAnd_combine`** (PROVED): closure under any binary combiner (the existing `hasSymAndFormFanIn_combine`, mixed-radix, size `s₁+(s₁+1)·s₂`, existentialised). **`isSymAnd_list`** (PROVED, clean axioms): for any list `gs` of `SYM∘AND`s and any combiner `G : List Bool → Bool`, `fun x => G (gs.map (· x))` is a `SYM∘AND` — induction on `gs`: at the head, `G(g x :: rest) = if g x then G(true::rest) else G(false::rest) = (g x ∧ A) ∨ (¬g x ∧ B)` (`A,B` `SYM∘AND` by IH), via three binary combines (`isSymAnd_combine`). `MAJ` is a special case (`G = list-majority`), so the entry-216 `MajVote` is a `SYM∘AND`. **CRUCIAL HONEST CAVEAT:** the size is **multiplicative** (the combine's `s₁+(s₁+1)·s₂`), hence **exponential in `gs.length`** — so the entry-216 majority collapses to an exact `SYM∘AND` of **exponential** size, NOT quasipolynomial. **This is where the majority-route and the classical CRT-route diverge:** BT's quasipolynomial single `SYM∘AND` is the *direct low-degree route* (entry 203 `btQuasipolyCollapse` ← entry 204 RS degree), not majority-of-approximants. Honest: proves the structural collapse *exists* completely (via the binary combine), and documents that it is *exponential* — *not* the quasipolynomial path. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MajSymAnd` |
| **Step 3 via Route A — the quasipolynomial BT closure plugged into the Williams chain (PROVED)** | Assembles **Route A** (the *direct low-degree*, quasipolynomial path — the operative one for Williams' fast-SAT, vs the exponential majority route of entry 217) into the BT-closure → `¬ NEXP` chain. **`routeA_quasipoly`** (PROVED): an `AC⁰[p]` circuit `C` (every `MOD` modulus `p`, `4·#subcircuits ≤ p^t`, `1 ≤ n`, and the `SpanApproxToLowDegRep` span bridge) has `∃ m, HasSymAndFormFanIn (C.eval) m (((p−1)t)^{depth}) ∧ m ≤ (((p−1)t)^{depth}+1)·n^{((p−1)t)^{depth}}` — quasipoly `SYM∘AND`, via entry-203 `btDepthCollapse` ∘ entry-204 `accToLowDeg_via_rs`. **`RouteAClosure p t`** / **`routeA_closure_proved`** (PROVED): the route-A closure as a `Prop`. **`routeA_btClosure_to_NEXP`** (PROVED, clean axioms): plugs the proved Route-A closure into the entry-166 `dynamicClosure_to_NEXP_not_ACC0` — with the Williams sockets (`closure → BTQuasi`, `BTQuasi → ACC0Speed`, `williams`, `hierarchy : ¬ Collapse`), `¬ NEXPC` follows. Honest: assembles the *quasipolynomial* Route A; the route-A `SYM∘AND` is proved modulo the `SpanApproxToLowDegRep` span bridge (entry-204 residual, reduced by 205/207), and the chain's remaining premises are the named Williams-side sockets, not BT content. A second, independent (`HasSymAndFormFanIn`-based) discharge of the BT-quasipoly closure, parallel to the *toAgree*-based `DynamicClosesAtBT_AC0p` (entry 179). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RouteAClosure` |
| **Lazy diagonalization — the telescoping contradiction (PROVED), simulation socketed** | The genuine logical heart of the lazy nondeterministic hierarchy — the entry-200 `DiagonalInNexp` subtlety. The naive complement-diagonal isn't `NEXP`-decidable (nondet classes not known closed under complement); Cook's *lazy diagonalization* (Cook 1972 / SFM / Žák) lazily copies `M_i` on the *next* input across a block and complements only once, at the boundary. **`lazy_telescope`** (PROVED): `(∀ k < len, M(a+k)=M(a+k+1)) → M(a)=M(a+len)` (induction on `len`). **`lazy_diag_false`** (PROVED, `[propext, Quot.sound]`): if `M` decides its own lazy diagonal `L` (`∀x, M x = L x`; lazy copy `L(a+k)=M(a+k+1)` for `k<len`; boundary `L(a+len)=¬M(a)`), then `False` — the lazy equalities telescope to `M(a)=M(a+len)`, the boundary gives `M(a+len)=¬M(a)`, so `M(a)=¬M(a)` (`Bool.not_ne_self`). *No machine decides its own lazy diagonal*, using only the **single** boundary complement — so it applies to nondeterministic classes without closure under complement. Honest: proves the lazy-diagonalization *contradiction mechanism* completely (pure `Bool`/`ℕ`), the NTM-specific subtlety entry-200's `DiagonalInNexp` hid. Residual: the **simulation** (`L ∈ NEXP` — simulate `M_i` on the next input + one boundary complement within the time bound), now reduced to the lazy-feasible form (one complement, not closure). Proves the contradiction, not the time-bounded simulation. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LazyDiagonal` |
| **Physical TM transition tables — the clocked multi-step run (PROVED)** | The time-bounded-computation substrate the machine-cost sockets reference (lazy-diag simulation 219, `ProvidesFastVerifiers` 201, `ClockedInclusion`/`ConcreteHierarchy`). A deterministic table is a total `δ : (state, read) → (state, write, move)`. **`detStep δ c := applyTrans c ((c.1, readSym c), δ (c.1, readSym c))`**, **`detRun δ t := (detStep δ)^[t]`**. **`detRun_add`** (PROVED): `detRun δ (a+b) c = detRun δ b (detRun δ a c)` (clocked-additive, `Function.iterate_add_apply`). **`detNTM δ`** — the table as an abstract `NTM` (step `d = detStep δ c`). **`detRun_reachIn`** (PROVED, `[propext]`): `reachIn (detNTM δ) t c (detRun δ t c)` — the functional `t`-step run *is* a valid `t`-step reachability in the `NTM` model (induction via `Function.iterate_succ_apply`). Honest: proves the **physical-table → clocked-computation** realisation completely (built on entry-205/`…ACC0ConcreteNTM` `applyTrans`/`readSym`) — a deterministic transition table drives a well-defined, clocked-additive `t`-step run that is a genuine `reachIn` computation. Residual: the *time-bound counting* (relating `detRun δ t` to a step-budget `≤ f(|x|)` and the `NTIME(f)` classes), the *universal* table-driven simulation (one machine simulating another), and nondeterministic-branching cost — the `NTIME`-level machine-cost content. Proves the deterministic clocked run + `reachIn` validity, not the time-class accounting. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TransitionTable` |
| **The Babai–Fortnow–Lund / IKW collapse — `NEXP = MIP` + guessable prover (PROVED glue)** | Opens the deep Karp–Lipton inclusion (entry-202 `NexpSubsetMA_ofCircuits`, `NEXP ⊆ ACC⁰ ⟹ NEXP ⊆ MA`) into its genuine BFL-IKW structure. **`NexpEqMIP NEXP MIP := NEXP = MIP`** — Babai–Fortnow–Lund 1991, the multi-prover IP characterisation of `NEXP` (named socket). **`MIPSubsetMA_ofCircuits MIP ACC0 MA := MIP ⊆ ACC0 → MIP ⊆ MA`** — under circuits the `MIP` prover's strategy is a small circuit Arthur guesses, collapsing `MIP ⊆ MA` (named socket). **`nexpSubsetMA_via_MIP`** (PROVED): the composition glue — `NexpEqMIP` + `MIPSubsetMA_ofCircuits` ⇒ `NexpSubsetMA_ofCircuits`, by `fun hsub => heq ▸ guessable (heq ▸ hsub)` (`NEXP = MIP` rewrites the circuit hypothesis to `MIP ⊆ ACC⁰`, yields `MIP ⊆ MA`, rewrites back). **`karpLiptonCollapse_via_MIP`** (PROVED, `[propext, Quot.sound]`): + the trivial `MASubsetNexp` ⇒ `KarpLiptonCollapse` (via entry-202 antisymmetry). Honest: exposes the BFL-IKW *structure* (the collapse goes through `NEXP = MIP` ∘ guessable-prover) and proves the *composition*; the two genuine classical theorems — `NexpEqMIP` (the `MIP = NEXP` sum-check/multilinearity machinery) and `MIPSubsetMA_ofCircuits` (the guessable-prover collapse) — remain named sub-sockets, each needing interactive-proof infrastructure. Proves the structure and composition, not the two deep ingredients. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BFLCollapse` |
| **The derandomisation collapse — `MA = NP` via a PRG (PROVED glue)** | Opens the deep derandomisation direction (entry-202 `DerandMASubsetNP`, `Derand ⟹ MA ⊆ NP`) into its hardness–randomness structure (parallel to the entry-221 BFL-IKW decomposition). **`DerandGivesPRG Derand PRGExists := Derand → PRGExists`** — the derandomisation yields a PRG (Nisan–Wigderson / Impagliazzo–Wigderson hardness-to-pseudorandomness; named socket). **`PRGCollapsesMAtoNP PRGExists MA NP := PRGExists → MA ⊆ NP`** — a PRG fooling Arthur collapses `MA ⊆ NP` by seed enumeration (named socket). **`derandMASubsetNP_via_PRG`** (PROVED): the composition glue — `DerandGivesPRG` + `PRGCollapsesMAtoNP` ⇒ `DerandMASubsetNP`, by `fun hd => prg_collapses (derand_gives_prg hd)`. **`derandCollapsesMAtoNP_via_PRG`** (PROVED, `[propext, Quot.sound]`): + the trivial `NPSubsetMA` ⇒ `DerandCollapsesMAtoNP` (via entry-202 antisymmetry). Honest: exposes the hardness–randomness *structure* (`Derand ⟹ PRG ⟹ MA ⊆ NP`) and proves the *composition*; the two genuine classical ingredients — `DerandGivesPRG` (the NW/IW tradeoff) and `PRGCollapsesMAtoNP` (PRG-seed-enumeration derandomisation of `MA`) — remain named sub-sockets, each needing pseudorandomness infrastructure. Proves the structure and composition, not the two deep ingredients. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DerandCollapse` |
| **prime-power `MOD` — CRT divisibility generalises, the low-degree field gate does NOT (the obstruction)** | The honest boundary of the algebraic-observer route. PROVED (what generalises): **`primePower_coprime`** (distinct prime powers coprime), **`primePowerDvd_iff`** (`(p^a·q^b) ∣ s ↔ (p^a∣s ∧ q^b∣s)`, CRT), **`modPrimePower_observer_decides`** (`(s : ZMod (p^e)) = 0 ↔ p^e ∣ s`, ring residue observer). NOT provable (the obstruction, documented not faked): the low-degree *field* gate polynomial — `ZMod (p^e)` (`e≥2`) is not a field, `p` is a zero divisor, no Fermat-style indicator. This is exactly the `ACC⁰[composite]` barrier (polynomial method gives `MOD_p`/`AC⁰[p]` but `ACC⁰[m]` composite is open); the squarefree case worked by avoiding prime powers (distinct primes → product of fields). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PrimePowerGate` |
| **the `AND`-layer assembly — the `AND`-count as a polynomial under the `MOD` gate** | Connects the bottom `AND` layer to the `MOD` gate polynomial. **`andCountPoly = ∑_j ∏_{i∈supports j} X_i`** (sum of `AND`-feature monomials). **`andCountPoly_eval`** (PROVED): `eval (boolVal∘x) andCountPoly = satCount` over `F_p` — the count of satisfied `AND`s (via `prod_boolVal_eq_andFeature` + `card_filter`/`Nat.cast_sum`). **`andCountPoly_degree`** (`≤ w` for `AND`-width `≤ w`). **`modPAndPoly = aeval ![andCountPoly] (modPGate p)`** (the `MOD_p∘AND` polynomial); **`modPAndPoly_degree`** (PROVED): degree `≤ (p−1)·w` (gate degree `p−1` × `AND`-count degree `w`, via `unaGate_degree`). Wires the `AND` features to the `MOD` gate; the composite-`MOD` pipeline is now `AND → count → MOD → compositeBT`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AndLayer` |
| **integration — the `MOD_p` gate polynomials feed `compositeBT_representation`** | Wires the composite-`MOD` algebraic observer into the composite-BT pipeline. **`modP_circuit_representation`** (PROVED): a `MOD_p`-gate circuit (unary gates `= modPGate p`, degree `≤ p−1` by `modPGate_degree`; binary gates degree `≤ p−1`), composed via `aeval`, gets the quasipolynomial low-degree sparse representation with `δ = p−1` — degree `≤ (p−1)^{depth+1}`, `≤ (n+1)^{(p−1)^{depth+1}}` features, sparse cube count — by instantiating `compositeBT_representation`. For constant depth `(p−1)^{O(1)}` is `polylog` ⇒ genuinely quasipolynomial. The `MOD_p` gate polynomial is a valid degree-`δ` gate; binary `AND`/`OR` kept as a degree-`≤ p−1` parameter. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeBTIntegration` |
| **CRT over a `Finset` of primes — arbitrary squarefree composite `MOD` gate** | Iterates two-prime CRT to a finset. **`prod_primes_dvd_iff`** (`(∏ p ∈ S, p) ∣ s ↔ ∀ p ∈ S, p ∣ s` for `S` a finset of primes; `Finset.prod_primes_dvd` backward + `dvd_prod_of_mem` forward). **`modGateProd_decides`** (PROVED): `(∀ p ∈ S, modPGate p fires at s mod p) ↔ (∏ p ∈ S, p) ∣ s` — the family of exact low-degree (`p−1`) prime gate polynomials over `∏ F_p` decides `MOD_m` for any squarefree `m = ∏ S`. Completes the squarefree composite case. Remaining: prime powers `p^e` (`ZMod p^e` not a field), `AND`-layer, `compositeBT_representation`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CRTFinsetGate` |
| **CRT generalisation — the prime-modulus gate polynomial, composite via coprime CRT** | Generalises `MOD₆`'s gate polys to arbitrary prime/squarefree modulus. **`modP_indicator`** (Fermat: `1 − a^{p−1} = [a=0]` over `F_p`). **`modPGate p = 1 − X^{p−1}`** (degree `p−1` over `F_p`, the general form of both `MOD₆` components — `p=2`: `1−X=1+X`, `p=3`: `1−X²`); **`modPGate_apply`/`modPGate_degree (≤ p−1)`/`modPGate_eq_one`**. **`modPGate_decides_dvd`** (`eval (s) = 1 ↔ p ∣ s` — computes `MOD_p`). **`modPQGate_decides`** (PROVED): for distinct primes `p ≠ q`, both gates fire iff `p·q ∣ s` (coprime CRT via `residue_observer_compose_coprime` + `Nat.coprime_primes`) — arbitrary squarefree two-prime composite. Remaining: prime powers `p^e`, `Finset`-product, `AND`-layer, feed into `compositeBT_representation`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CRTGatePolys` |
| **composite-`MOD` gate polynomials — the `MOD₆` exact low-degree local gate polynomial** | The first concrete composite-`MOD` gate polynomial, over the product-residue observer. **`mod6GateF2 = 1+X`** (degree 1 over `F₂`), **`mod6GateF3 = 1−X²`** (degree 2 over `F₃`); **`mod6GateF2_apply`/`mod6GateF3_apply`** (compute the residue indicators, via `mod2_indicator`/`mod3_indicator`); **`mod6GateF2_degree ≤ 1`/`mod6GateF3_degree ≤ 2`** (low-degree — valid degree-`δ` gate polys). **`mod6Gate_decides`** (PROVED): both components fire iff `6 ∣ s` — the gate computes `MOD₆` of the count exactly (via `mod6_decided_by_residue_pair`). The concrete composite-`MOD` gate polynomial for `MOD₆` (the open ACC⁰-side input, base case); remaining = CRT generalisation to arbitrary composite modulus + `AND`-layer + feed into `compositeBT_representation`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeGatePolys` |
| **the IKW easy-witness lemma — proof architecture (contrapositive glue proved, deep steps socketed)** | Decomposes the deepest Williams ingredient. **`NoEasyWitnessHardFn SmallWitnessCircuits HardFunction := ¬SmallWitnessCircuits → HardFunction`** (socket: no easy witnesses ⇒ a hard truth table). **`HardFnSeparation NEXP ACC0 HardFunction := HardFunction → ¬(NEXP⊆ACC0)`** (socket: hard function derandomises via Nisan–Wigderson + Karp–Lipton collapse ⇒ separation). **`easyWitness_from_parts`** (PROVED, contrapositive glue): the two ⇒ `EasyWitnessLemma` (assuming `NEXP⊆ACC⁰`, witnesses must be easy else (1)+(2) refute it; `Classical.byContradiction`). **`nexp_not_acc0_full`** (PROVED): IKW parts + guess-verify + `NondetTimeHierarchy` + speedup ⇒ `¬(NEXP⊆ACC⁰)`, reducing the separation to `{NoEasyWitnessHardFn, HardFnSeparation, GuessVerify, NondetTimeHierarchy}`. The two deep sub-lemmas (hard-fn extraction, derandomisation) are NOT proved. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0IKWEasyWitness` |
| **guess-and-verify budgets — the budget closure proved, verify machine socketed** | Closes the verify-time budget via the composite-BT feature count. **`budget_split`** (`guessT ≤ b → verifyT ≤ b → 2·b ≤ target → guessT + verifyT ≤ target`). **`guess_verify_fits`** (PROVED): the two physical phases (guess reaches `mid` in `≤ b` steps, verify accepts in `≤ b` steps) with `2·b ≤ target` give `acceptsWithin M x target`. Taking `b = (n+1)^{δ^{depth+1}}` (the composite-BT feature count from `compositeBT_representation`), the verify time is quasipolynomial — the cube sum over `≤ (n+1)^D` features, not `2^n`. The budget arithmetic + composition proved; remaining sockets = the two physical phase machines (guess `poly`, verify = enumerate features = fast `ACC⁰`-SAT). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GuessVerifyBudgets` |
| **composite Beigel–Tarui capstone — the quasipoly low-degree sparse representation, packaged** | Assembles the proved approximation machinery into one theorem. **`compositeBT_representation`** (PROVED): for a gate-polynomial assignment `Q` composed via `aeval` from degree-`≤ δ` gate polynomials, the circuit polynomial `Q c` has (1) total degree `≤ δ^{depth c+1}` (quasipoly for const depth, via `psubst_degree` + `aeval` degree discharge), (2) `≤ (n+1)^{δ^{depth c+1}}` distinct monomial-supports (via `support_mem_lowDeg` + `beigelTarui_monomial_count_le`), (3) sparse cube count `∑ₓ eval = ∑_{d} coeff·2^{n-|supp|}` (via `multilinear_cube_sum`). The quasipolynomial low-degree sparse `SYM∘AND` representation. Assumptions explicit: gate-poly degrees `≤ δ` (the `OR`/`MOD` probabilistic polys) + `aeval` composition. Error side = separate `circuit_error_bound`+`error_calibration`. The cleanest remaining ACC⁰-side packaging. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeBTCapstone` |
| **guess-and-verify, the time bound — two-phase time composition proved** | The decider's complexity is two phases (guess + verify), additive in time. **`acceptsFrom M start t`** (acceptance from a start config within budget `t`). **`acceptsWithin_compose`** (PROVED): `reachIn M guessT (init x) mid` + `acceptsFrom M mid verifyT` ⇒ `acceptsWithin M x (guessT+verifyT)` (via `reachIn_add` — the phases compose additively). **`guess_verify_within`** (PROVED): with `guessT+verifyT ≤ target`, accepts within `target` — the guess-and-verify decider fits the `NTIME[2ⁿ/superpoly]` budget once the two phase budgets do. The time composition is proved; the remaining sockets are the two phase budgets (guess `poly`, verify `2ⁿ/superpoly` via fast SAT — the actual fast-SAT/read-off content). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GuessVerifyTime` |
| **guess-and-verify — construction correctness proved, complexity socketed** | Williams' guess-and-verify ingredient, made precise at the verifier/language level. **`HasWitness L V := ∀x, L x ↔ ∃w, V x w`** (verifier characterisation); **`SmallWitnessRed V decode := ∀x, (∃w, V x w) → ∃cw, V x (decode cw)`** (IKW small-witness reduction); **`gvDecider V decode x := ∃cw, V x (decode cw)`** (guess a small circuit, verify). **`gvDecider_eq`** (PROVED): `gvDecider V decode x ↔ L x` — the decider decides exactly `L` (guessing only small witnesses loses nothing). **`guessVerify_subset`** (PROVED glue): if every `NTIME2n` language has such a verifier whose guess-and-verify decider lies in `NTIME2nFast`, then `NTIME2n ⊆ NTIME2nFast` (decider `= L`, inherits complexity). The construction's correctness is proved; the remaining socket is the decider's time bound (guess `poly` + verify `2ⁿ/superpoly` via fast SAT). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0GuessVerify` |
| **the universal decode brick — recover `(machine, config)` from the tape** | Next brick of the physical universal machine: extract the simulation state from `U`'s tape. **`encodeSim M c := encodeTape ⟨M⟩ c`** (code + config on tape), **`decodeSim bs := (decodeTape bs).map (fun p => (machineEquiv.symm p.1, p.2))`** (parse to code+config, decode code to machine). **`decodeSim_encodeSim`** (PROVED): `decodeSim (encodeSim M c) = some (M, c)` — the round-trip recovers the machine and configuration (tape encoding + `machineEquiv`). **`decoded_machine_steps`** (PROVED): decode then step — tape decodes to `(M,c)` and the decoded machine reaches any `concreteStep` successor in one step (`reachIn_one`). The decode function + round-trip (the contract); physical scan/parse as `U`-transitions and the full decode→step→re-encode loop in `B` steps remain the construction. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalDecode` |
| **the physical single-step base — atomic-step correctness (first brick of the universal machine)** | Begins the physical `hstep` construction (option a). **`reachIn_one`** (PROVED): `reachIn (toNTM M) 1 c d ↔ concreteStep M c d` — one reachability step is exactly one transition (`simp [reachIn, exists_eq_right]` + `toNTM_step`). **`firing_rule_step`** (PROVED): `t ∈ M → t.1 = (c.1, readSym c) → reachIn (toNTM M) 1 c (applyTrans c t)` — a firing rule advances in one physical step. Genuine machine-transition content (the atomic base the physical simulation iterates). NOT the universal machine: the universal `U` decoding `M` from its own `encodeTape` and performing `M`'s step via its own transitions in `B` steps (assembling lookup/head/rewrite) is the large remaining construction. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PhysicalStep` |
| **the easy-witness collapse — proof architecture (glue proved, IKW + guess-verify socketed)** | Decomposes Williams' other deep socket `EasyWitnessCollapse`. **`EasyWitnessLemma NEXP ACC0 SmallWitnessCircuits := NEXP⊆ACC0 → SmallWitnessCircuits`** (socket: IKW easy-witness lemma). **`GuessVerify NTIME2n NTIME2nFast ACC0SatSpeedup SmallWitnessCircuits := SmallWitnessCircuits → ACC0SatSpeedup → NTIME2n⊆NTIME2nFast`** (socket: guess the small witness circuit, verify with fast SAT). **`easyWitnessCollapse_from_parts`** (PROVED, axiom-free): IKW ∘ guess-verify = `EasyWitnessCollapse`. **`nexp_not_acc0_from_witness_parts`** (PROVED, axiom-free): the two ingredients + `NondetTimeHierarchy` + speedup ⇒ `¬(NEXP⊆ACC⁰)`, reducing the separation to `{EasyWitnessLemma, GuessVerify, NondetTimeHierarchy}`. The two deep ingredients (circuit complexity + nondet simulation) are NOT proved. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0EasyWitness` |
| **the tape-rewrite sub-machine — write contract proved** | Third sub-machine of the physical universal machine: rewrite the simulated tape (`writeAt`). **`writeAt_getD_self`** (read-after-write: `(writeAt tape p w).getD p false = w`); **`writeAt_getD_ne`** (unchanged elsewhere: `q≠p → (writeAt tape p w).getD q false = tape.getD q false`, via `appendReplicate_getD` + `getElem?_set_ne`); **`writeAt_length`** (minimal extension: `= max tape.length (p+1)`); **`applyTrans_write`** (a rule writes its symbol at the old head: `(applyTrans c t).2.2.getD c.2.1 false = t.2.2.1`). The write contract the physical rewrite sub-machine must satisfy; realising it as `U`-transitions over `encodeTape` remains the socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TapeRewrite` |
| **the head-location sub-machine — head-movement locality proved** | Second sub-machine of the physical universal machine: locate the simulated head. The head is explicit in the config; the key property is locality. **`moveHead_le_succ`** (`moveHead h m ≤ h+1`, any move advances ≤1 cell). **`step_head_le`** (`concreteStep M c d → d.2.1 ≤ c.2.1+1`). **`reachIn_head_le`** (PROVED, induction): `reachIn (toNTM M) k c d → d.2.1 ≤ c.2.1 + k` — the simulated head stays within `head₀+k` after `k` steps, bounding the tape region the physical head-location sub-machine traverses (`O(k)` per step). The locality/region bound is proved; realising the navigation as `U`-transitions over the `encodeTape` layout remains the socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0HeadLocation` |
| **the rule-lookup sub-machine — specification proved (step = pick matching rule and apply)** | First sub-machine of the physical universal machine: find `M`'s transitions matching the simulated `(state, symbol)`. **`matchingRules M state sym := M.filter (·.1 = (state,sym))`**; **`mem_matchingRules`** (membership). **`concreteStep_iff_matching`** (PROVED): `concreteStep M c d ↔ ∃ t ∈ matchingRules M c.1 (readSym c), d = applyTrans c t` — the nondeterministic step IS selecting a matching rule and applying it (the lookup's correctness contract). **`matchingRules_length_le`** (the lookup scans `≤ M.length` rules — the cost bound). The lookup spec is proved correct against `concreteStep`; realising it as `U`-transitions scanning the encoded table on the tape remains the socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RuleLookup` |
| **the tape-layout encoding — faithful binary serialization of `(code, config)`** | The encoding the physical universal machine operates on. **`bitEquiv : List Bool ≃ ℕ`** (binary-string ↔ number, via `Denumerable`). **`encodeTape code c := bitEquiv.symm (Encodable.encode (code, c))`** (serialise machine code + simulated config to a binary tape), **`decodeTape bs := Encodable.decode (bitEquiv bs)`** (decoder). **`decodeTape_encodeTape`** (PROVED): `decodeTape (encodeTape code c) = some (code, c)` — the round-trip, so the tape determines the simulated config uniquely (faithful). **`encodeTape_inj`** (PROVED): same tape ⇒ same code and config. The faithful encoding for `hstep`; reading/rewriting it as `U`-transitions in one simulated step remains the socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TapeEncoding` |
| **the single-step simulation lemma — linear overhead accumulation; physical single step socketed** | Formalises the single-step simulation property of a physical universal machine and proves its consequences. Setup: `U : NTM`, encoding `enc : CConfig → U.Config`, per-step overhead `B`, single-step hypothesis `hstep : concreteStep M c d → reachIn U B (enc c) (enc d)`. **`sim_multi`** (PROVED): `reachIn (toNTM M) k c d → reachIn U (k·B) (enc c) (enc d)` (induction on `k`, splitting `(k+1)·B = B + k·B` via `reachIn_add`). **`sim_acceptsWithin`** (PROVED): `acceptsWithin (toNTM M) x t → acceptsWithin U x (t·B)` (overhead linear in steps), given `enc` carries initial/accept configs. The accumulation + overhead bound are proved; the single physical step (constructing `U`'s transition table to decode a rule, find the head, rewrite the simulated tape) is the remaining socket. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SimulationStep` |
| **universal interpreter simulation — `enum_covers` discharged; hierarchy down to one socket** | The universal simulation at the interpreter level (no physical overhead). **`univStep code := concreteStep (machineEquiv.symm code)`** with **`univStep_correct`** (`univStep ⟨M⟩ = concreteStep M` — one relation simulates any machine exactly). **`cNTIME f`** (concrete `NTIME` over encodable `TMachine`s), **`enum f k := langOf (machineEquiv.symm k) f`** (machine enumeration). **`enum_covers`** (PROVED): `cNTIME f ⊆ Set.range (enum f)` — every concrete-`NTIME` language equals `enum f ⟨M⟩`, **discharging a hierarchy socket** via `machineEquiv`. **`diag_not_mem_cNTIME`** (PROVED: diagonal `∉ cNTIME g`, Cantor + `enum_covers`). **`cTime_hierarchy`** (PROVED conditional): given the single remaining socket `diag_in_big` (`diagLang ∈ cNTIME f`), `¬(cNTIME f ⊆ cNTIME g)`. The interpreter sim has no overhead; `diag_in_big` still needs a physical universal machine with a verified overhead bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0UniversalNTM` |
| **concrete encodable NTM model — machines are enumerable** | The complexity-theory infrastructure the abstract model lacked. `Move := Fin 3`, `TMTrans := (ℕ×Bool)×(ℕ×Bool×Move)` (rule `(state,read)↦(state',write,move)`), `TMachine := List TMTrans` (nondet transition table), `CConfig := ℕ×ℕ×List Bool` (state,head,tape); `readSym`/`moveHead`/`writeAt`/`applyTrans`/`concreteStep` give the binary-tape semantics. **`machineEquiv : TMachine ≃ ℕ`** (PROVED, via `Encodable`+`Infinite`+`Denumerable.ofEncodableOfInfinite`) — machines are enumerable, the encode half the hierarchy `enum_covers` socket needs. **`toNTM`** bridges each concrete machine to an abstract `…ACC0NTM.NTM` (start `0`, accept state `1`), with `toNTM_step`/`_init`/`_accept`/`concrete_step_iff`. The encodable model + enumeration; the time-bounded universal simulator (to discharge `enum_covers`/`diag_in_big`) is the next step. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ConcreteNTM` |
| **the time-hierarchy diagonalisation — Cantor core proved, simulation/enumeration socketed** | The logical heart of the nondeterministic time hierarchy, machine-checked. **`idxEquiv`** (`List Bool ≃ ℕ` via `Denumerable.ofEncodableOfInfinite`). **`diagLang enum idx := fun w => ¬ enum (idx w) w`** (the diagonal language). **`diag_not_mem_range`** (PROVED, Cantor: `diagLang ∉ Set.range enum` — at `idx⁻¹ k`, `enum k = ¬ enum k`, absurd via `iff_not_self`). **`time_hierarchy_from_sockets`** (PROVED diagonal argument): given `enum_covers` (`NTIME g ⊆ range enum`, enumerability) and `diag_in_big` (`diagLang ∈ NTIME f`, simulatability in the bigger bound), `ConcreteHierarchy f g` holds. The diagonal ARGUMENT is fully proved; the two computational sockets (enumerability of `NTIME g`, diagonal decidable in `NTIME f`) need a string-encoded universal machine — a major project on top of `…ACC0NTM`. Does NOT prove the hierarchy outright. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TimeHierarchyDiagonal` |
| **NTM time-class library — foundation (model + `NTIME`/`NEXP` proved, hierarchy/simulation socketed)** | The concrete computation model the Williams sockets need. **`NTM`** (configuration type + nondeterministic `step` relation + `init` + `accept`); **`reachIn M k c c'`** (reachable in exactly `k` steps); **`acceptsWithin M x t`** (accepting config reached within `t` steps). Proved: **`reachIn_add`** (reachability composes), **`acceptsWithin_mono`** (more steps only help, axiom-free). **`NTIME f`** / **`NEXP`** (`NTIME(2^poly)`) complexity classes (`CClass = Set Lang`). **`williams_concrete`** (proved glue): the Williams meta-theorem instantiated at concrete `NTIME f`/`NTIME g` ⇒ `¬(NEXP⊆ACC0)`, given the concrete sockets. Deep SOCKETS over the model (NOT proved): **`ClockedInclusion`** (`f≤g ⇒ NTIME f ⊆ NTIME g`, clocking), **`ConcreteHierarchy`** (`¬(NTIME f ⊆ NTIME g)`, diagonalisation), universal simulation. A genuine foundation; the hierarchy/simulation are the deep content a full verified library would discharge (a separate major project). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0NTM` |
| **Williams' meta-theorem — proof architecture (glue proved, deep ingredients socketed)** | Unpacks the monolithic `williams`/`hierarchy` sockets into Williams' two genuinely-deep ingredients over an explicit class framework (`Lang := List Bool → Prop`, `CClass := Set Lang`). **`EasyWitnessCollapse`** (socket: fast `ACC⁰`-SAT + `NEXP⊆ACC⁰` ⇒ `NTIME[2ⁿ]⊆NTIME[2ⁿ/superpoly]` via IKW easy witnesses), **`NondetTimeHierarchy`** (socket: `¬(NTIME[2ⁿ]⊆NTIME[2ⁿ/superpoly])`, diagonalisation). **`williams_meta_theorem`** (PROVED, axiom-free): the two ingredients + `ACC0SatSpeedup` ⇒ `¬(NEXP⊆ACC⁰)` by modus tollens. **`nexp_not_acc0_via_williams`** (proved conditional): RS representation → SAT speedup (counting socket) → separation. The glue is machine-checked; the two deep ingredients need a verified NTM time-class library and are NOT proved. NOT a proof of Williams' theorem; NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0WilliamsMetaTheorem` |
| **error calibration + Route-B complete capstone — end-to-end conditional `¬NEXP⊆ACC⁰`** | Closes the `ℕ` bookkeeping and packages the whole arc. `…ACC0ErrorCalibration`: **`per_gate_error`** (cancel seed relation `p^t·k=cardS`, `cardI=2^n` ⇒ `p^t·E ≤ 2^n`), **`error_calibration`** (`10·size ≤ p^t` ⇒ `10·(size·E) ≤ 2^n`), **`circuit_error_below_tenth`** (combined ⇒ circuit error `< 2^n/10`). `…ACC0RouteBComplete`: **`error_calibrated`** (re-export) and **`routeB_to_NEXP_not_ACC0`** (the end-to-end conditional — proved RS approximation side [degree `δ^{depth+1}`, error `< 2^n/10`, sparse read-off over `≤(n+1)^{δ^{depth+1}}` features] backs the RS representation; abstract `counting`/`williams`/`hierarchy` sockets give `¬NEXPHasACC0Circuits`; depends on NO axioms — pure conditional). A faithful machine-checked REDUCTION of `NEXP ⊄ ACC⁰` to Williams's SAT-speedup meta-theorem + nondeterministic time hierarchy, NOT a proof of those. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ErrorCalibration`, `…ACC0RouteBComplete` |
| **the error-averaging bridge — per-input seed error ⇒ a fixed good seed (probabilistic method)** | The error-side analogue of the composition-degree discharge. **`exists_below_average`** (`g : ι→ℕ` over nonempty finite `ι` ⇒ some `i` with `(card ι)·g i ≤ ∑ⱼ g j` — the argmin is at/below average; `exists_min_image` + `sum_const`/`sum_le_sum`). **`exists_good_seed`** (if every input is erred on by `≤ k` seeds, some seed errs on few inputs: `(card Seed)·#{x:err s x} ≤ (card Input)·k`; Fubini `∑_s #{x} = ∑_x #{s}` via `card_filter`+`sum_comm`, then `exists_below_average`). Converts the seed-space gate errors (`orPoly_error`/`amplifiedOrPoly_error`) into the fixed-seed input-space `ε = 2^n/p^t` consumed by `circuit_error_bound` (total `≤ size·ε`); with `p^t > 10·size` the circuit errs on `< 2^n/10` inputs. Both degree AND error interfaces now proved; only abstract `williams`/`hierarchy` remain. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ErrorAveraging` |
| **the composition-degree lemma — `totalDegree (aeval f p) ≤ totalDegree p · D` (discharges the per-gate hypothesis)** | Removes the per-gate degree-factor *hypothesis* of the low-degree substitution. **`aeval_totalDegree_le`** (`∀i,(f i).totalDegree ≤ D ⇒ (aeval f p).totalDegree ≤ p.totalDegree·D`; proof: expand `aeval f p = ∑_{d∈support} C(coeff)·∏_i (f i)^{d_i}` via `eval₂_eq`, each monomial image has degree `≤ (∑ d_i)·D = deg(d)·D ≤ totalDegree p·D` via `totalDegree_finset_prod`/`totalDegree_pow`/`le_totalDegree`, sup kept by `totalDegree_finset_sum`). **`unaGate_degree`/`binGate_degree`** (`(aeval ![A,B] G).totalDegree ≤ G.totalDegree·max A.totalDegree B.totalDegree` — the per-gate degree factor, now a THEOREM). Capstone **`psubst_degree_aeval_frontier`** (frontier): a substitution built by `aeval`-composition with degree-`≤δ` gate polys has `totalDegree ≤ δ^{depth+1}` UNCONDITIONALLY on the factor — only the local fact that each gate poly has degree `≤δ` (which `OR`/`MOD` satisfy). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0AevalDegree` |
| **low-degree substitution — low degree survives constant-depth composition** | Fuses the probabilistic low-degree gate polynomials into the substitution: the degree analogue of `circuit_error_bound`. **`depth`** (circuit depth). **`psubst_degree`** (proved by structural induction): if every gate of a polynomial assignment `Q` raises total degree by `≤` factor `δ` (`totalDegree (Q gate) ≤ δ·max(child degrees)`, leaves `≤ δ`), then `totalDegree (Q c) ≤ δ^{depth c + 1}` — quasipolynomial for constant depth (the `OR` amplified poly has `δ=t(p-1)`, `MOD` has `δ=p-1`). **`psubst_features_lowDeg`** (every feature of `Q c` is a degree-`≤ δ^{depth c+1}` support in `lowDegMonomials`, via `support_mem_lowDeg` ⇒ `≤ (n+1)^{δ^{depth+1}}` features). With `circuit_error_bound` (error `≤ size·ε`) this is the low-degree, low-error substituted polynomial. Per-gate degree factor is the hypothesis (interface to the gate polys; Mathlib lacks a general `totalDegree`-of-composition lemma). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LowDegreeSubstitution` |
| **`MvPolynomial` re-modelling of circuit substitution — the circuit IS a polynomial; its accept count is sparse** | Fuses the substitution (`Bool`-valued) and multilinearisation (`MvPolynomial`) files. **`subst`** (each gate replaced by its exact multilinear `{0,1}` interpolation: unary `f(0)(1-A)+f(1)A`, binary the 4-term bilinear form, composed through the circuit). **`unary_interp_val`/`binary_interp_val`** (the interpolations are exact on `{0,1}`). **`subst_eval`** (`eval (boolVal∘x) (subst c) = boolVal (eval c x)` — the polynomial computes the circuit exactly on the cube). **`circuit_cube_count`** (`∑ₓ boolVal (eval c x) = ∑_{d∈(subst c).support} coeff_d·2^{n-|supp d|}` — the accept count IS the sparse cube count of the circuit's polynomial, fusing `subst_eval` with `multilinear_cube_sum`; the sub-`2^n` count for the actual circuit). **`circuit_features_lowDeg`** (every feature is a support of size `≤ totalDegree (subst c)`). Exact polynomial (high degree for wide gates); the low-degree version is the separate probabilistic content. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SubstitutionPoly` |
| **multilinearisation — a bounded-degree polynomial on the Boolean cube is a sparse `AND`-feature count** | Closes the read-off's input gap: over `{0,1}` every monomial collapses to its squarefree support (`x_i^k=x_i`) and `∏_{i∈S} x_i = monoAND_S`. **`boolVal`/`boolVal_pow`** (`boolVal b^k = boolVal b`, `k≥1`); **`prod_boolVal_eq_andFeature`** (the atom: `∏_{i∈S} boolVal(x i) = [monoAND_S x]` — a monomial IS an `AND` feature); **`eval_boolean_eq_sparse`** (`eval (boolVal∘x) Q = ∑_{d∈Q.support} coeff_d·[monoAND_{supp d} x]` — bounded poly evaluates on the cube to a sparse `AND`-feature sum, via `MvPolynomial.eval_eq`); **`multilinear_cube_sum`** (`∑ₓ eval (boolVal∘x) Q = ∑_{d∈Q.support} coeff_d·2^{n-|supp d|}` — closed sparse cube count, reusing `monoAND_cube_sum`); **`support_mem_lowDeg`** (`d∈Q.support`, `totalDegree Q ≤ D ⇒ supp d ∈ lowDegMonomials n D`, via `le_totalDegree` — features number `≤(n+1)^D`). Remaining: the modelling link identifying the circuit approximant with an `MvPolynomial`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Multilinearisation` |
| **the sparse-polynomial read-off — a low-degree sparse poly gives a sub-`2^n` cube count (wires the counting socket)** | Connects the circuit approximant to the existing counting machinery. **`sparsePoly 𝒮 c x := ∑_{S∈𝒮} c S · [monoAND_S x]`** (the sparse `SYM∘AND`/monomial form). **`sparsePoly_cube_sum`** (`∑ₓ sparsePoly = ∑_{S∈𝒮} c S · 2^{n-|S|}` — the cube sum is a SPARSE sum, no `2^n` enumeration; reuses `sparse_cube_sum`). **`sparse_features_le`** (`𝒮 ⊆ lowDegMonomials n D ⇒ 𝒮.card ≤ (n+1)^D`; reuses `beigelTarui_monomial_count_le`). **`sparse_readoff`** (the bridge: degree-`≤D` sparse poly's cube sum = sum of `≤(n+1)^D` feature weights — the sub-`2^n` `ACC⁰`-`SAT` counting input). **`sparse_readoff_to_NEXP_not_ACC0`** (given the sparse representation, counting+Williams ⇒ `¬NEXPHasACC0Circuits`). SOCKET: the multilinearisation turning the circuit-substitution approximant into this explicit sparse `monoAND` form. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SparsePolyReadoff` |
| **the circuit-substitution assembly — substitution error accumulates additively over gates (hybrid bound)** | The bookkeeping that turns per-gate probabilistic polynomials into a whole-circuit approximant. Circuit model **`Circ`** (`inp/cst/una/bin`; `AND`/`OR`/`MOD`/`NOT` are gate instances) with **`eval`** (true value), **`size`** (gate count), **`step`** (gate applied to children's approximants). **`circuit_error_bound`** (proved by structural induction, the hybrid argument): if every node's approximant `P c` agrees with `step P c` on all but `ε` inputs (the gate's own probabilistic-polynomial error), then `P c` agrees with `eval c` on all but `size c · ε` inputs — i.e. `#{x : P c x ≠ eval c x} ≤ size c · ε`. At each gate the error ⊆ local-error ∪ propagated-children-errors (`Finset.card_le_card` + `card_union_le`). Calibrated with `ε=2^n/(10·size)` (amplification + `error_choice`) ⇒ total error `<2^n/10`. SOCKET: the polynomial→`SYM∘AND` read-off (sparse cube-sum / level counts) = `…ACC0BTSizeRecurrence.QuasipolyApproxCompression`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CircuitSubstitution` |
| **Razborov–Smolensky amplification — the product error bound `(1/p)^t`** | Boosts the single-form `OR`-polynomial error `1/p` to sub-constant via `t` independent forms. **`amplified_form_balance`** (`v≠0` ⇒ `p^t·#{R : ∀ s, ∑ᵢ R s i vᵢ = 0} = p^{m·t}` — all `t` independent forms vanish on `v` for an exact `(1/p)^t` fraction; proof: joint vanishing set = `t`-fold `Fintype.piFinset` product of the single-form set, card `(single)^t`, then `p^t·(single)^t = (p·single)^t = (p^m)^t = p^{m·t}` via `linear_form_balance` + `mul_pow`/`pow_mul`). The amplified polynomial: **`amplifiedOrPoly`** (`1 − ∏ₛ(1 − orPoly v (R s))`, degree `t·(p-1)`), **`amplifiedOrPoly_eq_zero_iff`** (vanishes iff every form vanishes — `Finset.prod_eq_zero`/`prod_eq_one`), **`amplifiedOrPoly_zero`** (exact on all-zero), **`amplifiedOrPoly_error`** (`p^t·#{R : amplifiedOrPoly v R = 0} = p^{m·t}` — error exactly `(1/p)^t`). With `t=O(log_p s)` + `error_union_bound`, a size-`s` circuit's total error `<1/10`. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ProbabilisticAmplification` |
| **the `MOD₆` probabilistic polynomial — exact `MOD` indicators + the Razborov–Smolensky `OR` core (random linear form)** | Attacks the probabilistic-polynomial wall and the analysis splits: the `MOD` gates need NO probabilism (they are exactly low-degree), all probabilism is in the `AND`/`OR` layer. **`mod2_indicator`** (`1+a` over `F₂`, deg 1), **`mod3_indicator`** (`1−a²` over `F₃`, deg 2, Fermat), **`mod6_indicator_product`** (`MOD₆` = both exact indicators fire over `F₂×F₃`). The RS core PROVED: **`linear_form_balance`** (`p·#{r : ∑rᵢvᵢ=0}=p^m` for `v≠0` — the random `F_p`-linear form vanishes on a fixed nonzero vector with probability exactly `1/p`, via additive equidistribution of the dot-product hom's fibres). The `OR` probabilistic polynomial: **`orPoly_eq`** (`(∑rᵢvᵢ)^{p-1}` is a 0/1 indicator, Fermat), **`orPoly_zero`** (exact on the all-zero input), **`orPoly_error`** (error exactly `1/p` on nonzero inputs — degree `p-1`, the genuine RS `OR` probabilistic polynomial). SOCKET: **`AmplifiedErrorSocket`** (boost `1/p`→`(1/p)^t` by `t` independent forms + quasipoly assembly = the remaining bookkeeping). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Mod6ProbabilisticPolynomial` |
| **the Beigel–Tarui size recurrence — exact fold is exponential in fan-in; probabilistic-collapse socket** | Pins down why exact mixed-radix is insufficient and stages the probabilistic replacement. **`btComposeSize l r := l·(r+1)+r`** with **`btComposeSize_eq`** (= the proved `miniBT_collapse_size`); **`iterSize b k`** (fold a fan-in-`(k+1)` gate); **`iterSize_ge`** (`b·(b+1)^k ≤ iterSize b k` — EXPONENTIAL in fan-in, so a single poly-fan-in `ACC⁰` gate blows up exponentially under exact collapse). Bookkeeping PROVED: **`error_union_bound`** (`s` gates of error `≤ ε` ⇒ total `≤ s·ε`), **`error_choice`** (`ε=1/(10s)` ⇒ total `=1/10`). SOCKETS (open): **`ApproxSymAndRep`** (`ε`-approximate bounded-size rep), **`QuasipolyApproxCompression`** (joint ⇒ quasipoly approximate single-count — the probabilistic replacement for exact mixed-radix), **`MOD6ProbabilisticApprox`** (first concrete target: `MOD₆` probabilistic polynomial), **`probabilistic_route_to_NEXP_not_ACC0`** (quasipoly approx rep over const depth ⇒ `¬NEXP⊆ACC⁰`, conditional). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BTSizeRecurrence` |
| **restricted mini-Beigel–Tarui: the two-count collapse is PROVED exactly (the wall is size, not impossibility)** | Corrects the earlier guess. The achievable `AND`-counts are closed under addition (append layers) and integer scaling (replicate gates), so a mixed-radix single count `c⋆ = (t₂+1)·c₁ + c₂` recovers the pair (`c₂ ≤ t₂ < t₂+1` ⇒ `c₁ = c⋆/(t₂+1)`, `c₂ = c⋆%(t₂+1)`). **`satCountF_sumElim`** / **`satCountF_replicate`** / **`satCountF_le_card`** (the count algebra); **`miniBT_two_count_collapse`** (joint two-count ⇒ single `SYM∘AND`); **`miniBTCollapse_holds : MiniBTCollapse n`** (discharges the `…ACC0SymAndComposition` socket — it was provable, not open); **`hasSymAndRep_and`** / **`_or`** (∴ `SYM∘AND` unconditionally closed under AND/OR). **`miniBT_collapse_size`** (`t₁·(t₂+1)+t₂` gates — the collapse is MULTIPLICATIVE in size, compounding to a tower over depth = the real Beigel–Tarui wall, kept quasipoly only via probabilistic polynomials). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MiniBTTwoCount` |
| **the `SYM∘AND` composition lemma — what composes (proved) and the one wall (socketed)** | Attacks the mini-Beigel–Tarui composition question honestly, splitting proved from open. `HasSymAndRep F := ∃ supports sym, ∀x, F x = sym (satCount supports x)` (grounded by `mod6AndCircuit_hasSymAndRep`). PROVED composes (no count-dimension rise): **`hasSymAndRep_not`** (NOT, same layer); **`hasSymAndRep_and_sharedLayer`** / **`_or_`** (shared-layer AND/OR stay single-count); **`residue_observer_compose_coprime`** (`a*b ∣ m ↔ a∣m ∧ b∣m` for coprime `a,b` — general product-residue/mixed-modulus CRT composition). PROVED the dimension rise: **`hasBinarySymRep_and`** / **`_or_`** (cross-layer AND/OR → joint TWO-count representation `HasBinarySymRep`). THE WALL (NOT proved): **`MiniBTCollapse`** (every joint two-count rep collapses to a single `SYM∘AND` — the BT degree reduction; exact form too strong, real theorem is the quasipoly relaxation via probabilistic polynomials). Conditional bridges: **`symAndRep_closed_under_and_of_miniBT`** / **`_or_`** (`MiniBTCollapse` ⟹ closure under AND/OR). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymAndComposition` |
| **depth-2 `MOD₆∘AND` — exact `SYM∘AND` representation via the residue-pair observer (Route B first composite case)** | The first real composite-`MOD` representation. `satCount supports x` = # satisfied bottom monomial-`AND`s; `mod6AndCircuit = 6 ∣ satCount`. **`mod6_depth2_symAnd_repr`** (the circuit IS `mod6Sym ∘ satCount` — the exact `SYM∘AND` factorization, `Iff.rfl`); **`mod6_depth2_residue_pair`** (accepts iff `(satCount mod 2=0 ∧ satCount mod 3=0)` — the CRT product observer reads it, via `mod6_decided_by_residue_pair`); **`mod6_depth2_symmetric`** (output depends only on `satCount` — the genuine `SYM` invariance); **`mod6_bottom_count_le_quasipoly`** (distinct bottom `AND`s of support `≤ D` number `≤ (n+1)^D`, Beigel–Tarui — quasipoly size). Bridge socket: **`depth2_to_full_via_composition`** (proved base case + open mini-BT composition lemma ⇒ full `ACC⁰` `SYM∘AND` rep; the composition/blow-up is the named open content). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0Mod6SymAndDepth2` |
| **the composite-`MOD` Beigel–Tarui target — `MOD₆`: single-field RS fails, integer/product observer replaces it** | Isolates the one open ACC⁰ wall at the smallest composite obstruction. **`mod6_iff_mod2_and_mod3`** (`6∣m ↔ 2∣m ∧ 3∣m`); **`no_injective_ringHom_zmod6_to_prime_field`** / **`field_polynomial_projection_fails_for_MOD6`** (NO injective ring hom `ZMod 6 → ZMod p` for any prime `p` — the zero-divisor `2·3=0` of `ZMod 6` can't survive in a field, so the single-field RS span cannot carry both `MOD₂` and `MOD₃`); **`compositeResidueObserver`** + **`compositeResidueObserver_injective`** (the CRT product observer `ZMod 6 ≃+* ZMod 2 × ZMod 3` — the faithful integer/multi-prime replacement, a product ring not itself a field); **`mod6_decided_by_residue_pair`** (`6∣m ↔ (m mod 2=0 ∧ m mod 3=0)`); **`mod6_symAnd_residue_pair`** (restricted `SYM` representation: `MOD₆` of the Hamming weight = the residue-pair conjunction). OPEN socket: **`mod6_composite_route_to_NEXP_not_ACC0`** (quasipoly composite `SYM∘AND` representation surviving `ACC⁰` composition = full YBT, named hypothesis ⇒ `¬NEXP⊆ACC⁰`). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CompositeBTTarget` |
| **the tri-aspect boundary — one `Ω`, three projections (incidence refuted, polynomial viable, composite open)** | Turns "tri-aspect monism" into a theorem map: a single boundary object `TriAspectBoundary = (supports, deg)` induces three *derived* observers (projections), not three independent hacks. **`incidence_projection_fails`** (singleton `Ω` ⇒ `¬IncidenceCollapses` — the membership-rank projection never collapses, reusing the rank-shrink no-go); **`polynomial_projection_succeeds_for_AC0p`** (`deg < n`, `2≠0` in `ZMod p` ⇒ `algebraicProjectionSucceeds`: holonomy parity escapes the degree-`≤deg` evaluation span = the RS core — the effective-dimension projection is the working one); **`cost_projection_quasipoly`** (`|lowDegMonomials n deg| ≤ (n+1)^deg`, Beigel–Tarui); **`tri_aspect_redirect`** (on ONE singleton `Ω`: incidence fails AND algebraic succeeds — N-Frame redirects to Route B); **`composite_projection_socket`** (the one open projection: composite-`MOD` algebraic representation ⇒ `¬NEXPHasACC0Circuits`, named conditional). NOT vacuous: projections are real functions of `Ω`, positive theorems discharge to real prior theorems. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0TriAspectBoundary` |
| **the rank-shrink wall is real — `NFrameRankShrink` false for the membership-rank observer** | The honest attack on Route A's wall: tested whether `NFrameRankShrink` is true — it is NOT. **`rankCellCollapse_implies_collision`** (`RankCellCollapse ⇒` two live coords share a pattern — collapse NEEDS a collision); **`not_rankCellCollapse_of_injOn`** (injective patterns ⇒ `¬RankCellCollapse`); **`singleton_supports_injective`** (`supports j = {j}` ⇒ patterns `= Pi.single`, injective); **`singleton_supports_no_rank_collapse`** (`∀ L, ¬RankCellCollapse (singletons) L` — a concrete `ACC⁰`-realizable family where the rank observer NEVER collapses, refuting the socket). CONCLUSION: rank-shrink needs pattern collisions, which adversarial supports defeat (matching the cell-count characterization + `MOD` no-go); the rank that DOES separate is the polynomial-method effective dimension (`V_D`, Route B), not the observer incidence rank. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankShrinkWall` |
| **the ACC⁰ frontier — two final conditional theorems, two named walls** | The 'what remains' map: the whole programme reduces to TWO deep open theorems, everything downstream proved. **Route A (N-Frame):** `NFrameRankShrink sys` (∀ predictor: `∃ L, 2^{cellRank} < \|L\|`) — the open rank-shrink socket; **`rank_shrink_gives_acc0_lower_bound`** (`NFrameRankShrink ⇒ ACC0HolonomyLowerBound`, via the proved rank-cell bridge). Open = force `cellRank < log₂\|L\|` for wide overlapping `MOD` = rank analogue of the `MOD` wall. **Route B (Williams/Beigel–Tarui):** **`composite_route_to_NEXP_not_ACC0`** (`(composite_BT_degree : RSRep) + counting + williams + hierarchy ⇒ ¬NEXPHasACC0Circuits`, via `williams_cashout_skeleton`). Open = composite-modulus Yao/Beigel–Tarui degree theorem; count side + `AC⁰[p]` degree + cash-out proved. Both are CONDITIONALS; the sockets are the genuine open `NP/NEXP`-strength content, named. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankRouteFrontier` |
| **random restriction yields a `RankCellCollapse` witness** | Exposes the intermediate witness from the probabilistic rank route. **`exists_rankCellCollapse_whp`** (`2^a ≤ b ∧ Pr[cellRank≥a]+Pr[\|L\|≤b]<1 ⇒ ∃ L, RankCellCollapse supports L` — the two-event method: some `L` is simultaneously low-rank `cellRank<a` and large `\|L\|>b≥2^a`, so `2^{cellRank}<\|L\|`); **`randomRestriction_rank_collapse_low_correlation`** (⇒ low corr via the rank-cell bridge). Packages the probabilistic rank route into the `RankCellCollapse`/observer-state-space API; rank analogue of the cell-count whp, keyed on the sharper cell-rank tail. Open: the feasibility `Pr[cellRank≥a]` small for wide overlapping `MOD` = the rank-shrink balance. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RandomRestrictionRankCollapse` |
| **bounded support footprint ⇒ low observer rank** | The honest intersection/support-dimension rank bound. A coordinate outside `footprint = ⋃_j supports j` lies in no support ⇒ zero cell pattern, so the rank is bounded by the footprint size, INDEPENDENT of gate count. **`footprint`**; **`cellPatternVec_eq_zero_of_not_mem_footprint`**; **`cellRank_le_footprint`** (`cellRank ≤ \|footprint\|`; `cellSpan ⊆ span` of footprint patterns, `Submodule.finrank_mono` + `finrank_span_finset_le_card` + `card_image_le`); **`footprint_rank_cell_collapse`** / **`footprint_low_correlation`** (`2^{\|footprint\|} < \|L\| ⇒ RankCellCollapse ⇒` low corr). A `MOD` layer reading `t` coordinates has rank `≤ t` regardless of gate count. NOTE: pairwise small intersection does NOT bound rank (disjoint = full rank); the genuine bound is the total footprint. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0FootprintRank` |
| **block-diagonal MOD rank-shrink — `k` gates in `d` blocks ⇒ observer rank `≤ d`** | A structured-`MOD` rank-shrink fragment. A wide layer of `k` `MOD` gates in `d` blocks, each block's gates sharing a support (`supports j = center (blk j)`), has observer rank `≤ d` — a genuine shrink (`d ≪ k`; survivor count `k`, linear rank only `d`). **`BlockEqualSupports`**; **`blockEqual_clustered`** (it's `ClusteredSupports … d 0`; `Finset.sum_eq_single`); **`blockEqual_cellRank_le`** (`cellRank ≤ d` via `clustered_supports_low_rank`); **`blockEqual_rank_cell_collapse`** / **`blockEqual_low_correlation`** (`2^d < \|L\| ⇒ RankCellCollapse ⇒` low corr). Does NOT force low rank for general wide overlapping `MOD` under restriction = the open rank-shrink wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BlockRankCollapse` |
| **rank-cell collapse — the linear observer state space is exactly `2^rank`** | Upgrades the observer route to the right linear-algebraic invariant. **`observer_state_space_card`** (`Nat.card (cellSpan supports L) = 2^{cellRank supports L}` — the `F₂` observer state space has EXACTLY `2^rank` states, via `Module.card_eq_pow_finrank` + `ZMod.card`; sharper than `2^{survivingCount}`); **`RankCellCollapse`** (`2^{cellRank} < \|L\|`); **`cellPatternCount_le_observer_states`** (realized cells `≤ \|cellSpan\| = 2^rank`); **`rank_cell_collapse_implies_low_holonomy_correlation`** (`RankCellCollapse ⇒ LowHolonomyCorrelation`, directly, bypassing survivor count); **`rankCellCollapse_implies_cellCountCollapse`**. The bridge + fragments + composition were already in the corpus; this crystallizes the exact state-space identity. Open: rank-shrink under restriction for wide overlapping `MOD` (`cellRank < log₂\|L\|`) = the rank analogue of the `MOD` wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RankCellCollapse` |
| **Beigel–Tarui sparsity (count half) — `SYM∘AND` monomial count is quasipolynomial** | The count input to the counting socket. **`sum_choose_le_quasipoly`** (`∑_{i≤D} C(n,i) ≤ (n+1)^D`, via `Nat.choose_le_pow`: `C(n,i)≤n^i≤n^i·C(D,i)`, summed `= (n+1)^D` by `add_pow`); **`beigelTarui_monomial_count_le`** (`(lowDegMonomials n D).card ≤ (n+1)^D` — a degree-`≤D` `SYM∘AND` representation is `(n+1)^D`-sparse, quasipolynomial for `D = polylog`). HONEST: COUNT half only. DEGREE half: `AC⁰[p]` PROVED in RS layer (`D=((p-1)t)^depth`) ⇒ `AC⁰[p]` is `(n+1)^D`-sparse; general composite `ACC⁰` degree = the DEEP OPEN Yao/Beigel–Tarui symmetric-representation construction (Williams), NOT established. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BeigelTaruiSparsity` |
| **binomial inversion — recover the level-counts `N_t` from the moments `B_d`** | Inverts the unit-triangular `B_d = Σ_t N_t·C(t,d)`. **`binom_orthogonality`** (`Σ_{d≤K} (-1)^{d-s} C(d,s) C(t,d) = δ_{s,t}`, via `Nat.choose_mul` subset-of-subset + `Int.alternating_sum_range_choose`; factor `C(t,s)`, reindex `sum_Ico_eq_sum_range`, alternating row-sum); **`binomial_inversion`** (`N_s = Σ_d (-1)^{d-s} C(d,s) B_d` over `ℤ`); **`levelCount_eq_inversion`** (`(N_s:ℤ) = Σ_d (-1)^{d-s} C(d,s)·(∑_x C(andCount x,d))`). Closes the algebraic half: the `N_t` are an EXPLICIT alternating combination of kernel-computable moments. Remaining: Beigel–Tarui `#monomials` bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BinomialInversion` |
| **per-level counts from binomial moments — the `N_t` bridge** | Connects the `SYM∘AND` level-counts `N_t = #{x : exactly t fire}` to the kernel-computable sums via binomial moments. **`levelCount_sum`** (`∑_{t≤k} N_t = 2ⁿ`, `card_eq_sum_card_fiberwise` + `card_pi`); **`binomial_moment_eq_sum_levels`** (`∑_x C(andCount x, d) = ∑_{t≤k} N_t·C(t,d)`, fiberwise over `andCount`). The matrix `C(t,d)` is unit-upper-triangular, so the `k+1` moments determine the `k+1` level-counts by inclusion–exclusion; each moment is a sparse cube-sum (`e_d` of the `AND`-indicators), computable by the kernel — hence so are the `N_t`. HONEST: the moments↔levels bridge is proved; the explicit inversion `N_t = ∑_d (-1)^{d-t} C(d,t) B_d` + the `e_d`-sparsity identity + Beigel–Tarui bound remain. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0LevelCounts` |
| **the SYM-layer reduction — `SYM∘AND` SAT/count reduces to the `k+1` level-counts** | The symmetric top layer of Williams' counting socket. A `SYM∘AND` circuit is `sym(∑_j AND_j(x))`; the `AND`-layer fires `0..k` gates, so the cube partitions into `k+1` levels `N_t = #{x : exactly t fire}`. **`andCount`/`symAndEval`/`levelCount`**; **`andCount_le`** (`≤ k` fire); **`symAnd_satCount_eq_sum_levels`** (`#SAT = Σ_{t≤k} [sym t]·N_t`, via `card_eq_sum_card_fiberwise`); **`symAnd_sat_iff`** (`∃x` accepting `⟺ ∃ t≤k, sym t ∧ N_t ≠ 0` — decision over `k+1` levels, not `2ⁿ`). The symmetric layer collapses `SYM∘AND` SAT to `k+1` level-counts. HONEST: the reduction THROUGH the symmetric layer is proved; computing each `N_t` (fed by the sparse kernel) + the Beigel–Tarui quasipoly `#monomials` bound remain open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SymLayerReduction` |
| **the sparse-counting kernel — fast cube-sum over a `SYM∘AND` representation** | The genuinely-provable algorithmic kernel of Williams' counting socket. A monomial-`AND` over `S` is satisfied by exactly `2^{n−|S|}` inputs, so a sparse combination's cube-sum is a closed form over its coefficients (no `2ⁿ`-enumeration). **`monoAND_cube_count`** (`\|{x : monoAND S x}\| = 2^{n−|S|}`, via `piFinset` + `card_univ_diff`); **`monoAND_cube_sum`** (`∑_x [∏_{i∈S} x_i] = 2^{n−|S|}` over any comm ring); **`sparse_cube_sum`** (`∑_x Σ_{S∈𝒮} c_S·[∏_{i∈S} x_i] = Σ_{S∈𝒮} c_S·2^{n−|S|}`, computable in `\|𝒮\|` ops). WHY the representation gives a counting speedup: `#monomials < 2ⁿ` ⇒ count from coefficients alone. HONEST: the counting PRIMITIVE, not the full socket — the `SYM` top layer (sum→SAT) and the Beigel–Tarui quasipoly `#monomials` bound for `ACC⁰` are the remaining algorithmic inputs, open. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SparseCounting` |
| **control shrinkage under leaf restriction — the provable half + the barrier** | Attacks the open rung `random_restriction_makes_control_shallow`. **`leafFree`/`restrictedControlEval`**; **`restrictedControlEval_eq_of_agree`** (restricted control depends only on free leaves); **`control_restriction_shallow`** (the `ρ`-restricted control is a decision tree of depth `≤ |leafFree ρ|` — observer state count shrinks to `2^|free|`); **`control_restriction_searchable`** (the `ρ`-restricted composite is SAT-searchable `< 2ⁿ` once `2^{|leafFree ρ|} < 2^n`, via `acc0_over_mod_searchable`). BARRIER (honest): searches the RESTRICTED composite (fixed leaves ignore the oracle); lifting to the original needs the leaf restriction realized by an `x`-restriction, and a `MOD` leaf is `x`-constant only when its whole support is fixed (`parity_constant_iff_support_fully_fixed`, no absorbing value) — for wide/overlapping `MOD` a restriction rarely fully-fixes supports, so depth doesn't drop below `n`. Same `MOD` wall, localized to the control layer. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0ControlShrinkage` |
| **boundary-observer control — the boundary selects the observer (dynamic N-frame unification)** | Formalizes "observer is not a fixed mechanism; the boundary/context selects it", routing the proved routes by gate boundary, each grounded in a proved fact. **`GateKind`** (andOr/mod q/symmetric), **`BoundaryContext`** (absorbing/linearResidual/polynomialSpan/countingState), **`boundarySelect`**; **`boundarySelect_andOr_iff_absorbing`** (absorbing ⟺ AND/OR); **`andOr_boundary_absorbing`** (AND has absorbing value); **`mod_boundary_not_absorbing`** (MOD constant iff support fully fixed); **`mod_observer_reduces_to_membership`** (linearResidual = membership, bounded); **`mod_polynomial_observer_separates`** (polynomialSpan separates the MOD target); **`counting_state_cashes_out`** (countingState → Williams cash-out). Unification/routing layer, NOT new hard content. The countingState observer is realised concretely in `…ACC0OracleControl` (`oracle_control_over_mod_searchable`: few-MOD fragment SAT-searchable `< 2ⁿ`); open rung `random_restriction_makes_control_shallow` = the state-shrinkage. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0BoundaryObserverControl` |
| **Williams cash-out from the polynomial method (CONDITIONAL, representation half discharged)** | Williams' `ACC⁰`-SAT algorithm = representation (sparse `SYM∘AND`) + counting (sub-`2ⁿ`). **`RSMonoANDRepresentation`** / **`rsMonoANDRepresentation_proved`** — the representation half (1) DISCHARGED by the polynomial method (re-export of `lowDegPolyEval_mem_monoAND_span`: degree-`≤D` poly eval ∈ monoAND span). **`williams_cashout_skeleton`** — the Williams skeleton with representation factored out. **`williams_cashout_from_polynomial`** — same chain with `hrep` SUPPLIED by the polynomial method, leaving only the **counting** socket (rep ⇒ sub-`2ⁿ` SAT, Beigel–Tarui/Williams algorithmic heart), the **Williams** collapse, and the **time hierarchy** as inputs. Proves the IMPLICATION, NOT the separation; faithful account of which inputs close `NEXP ⊄ ACC⁰` and which the polynomial method supplies. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0WilliamsCashoutFromPolynomial` |
| **PIVOT to the polynomial method — exact effective-dimension separation** | Lands the polynomial method on the SAME target the observer route could not collapse (`fParity univ`). **`lowDegree_poly_ne_holonomy_parity`** (NEW): a polynomial of total degree `< n` cannot equal the holonomy parity `x ↦ ∏ᵢ pmOne(xᵢ)` on the cube — its evaluation `∈ V_D` (`eval_mem_lowDegSpan`), which the holonomy parity escapes (`holonomy_parity_not_lowDegEval`, effective dim `≥ n`). The DIMENSION analogue of the observer's swap-invariance — and unlike the observer route it bites on `MOD`. **`holonomy_parity_escapes_lowDegSpan`** (re-export); **`observer_route_no_escape_for_mod`** (re-export: the ceiling bypassed). Quantitative `AC⁰[p]` size `2^{Ω(n^{1/2d})}` = `…Layer3NFrameParityRS.nframe_parity_target_size_lower_bound`. Honest `F_p` ceiling: real `PARITY/MOD_p ∉ AC⁰[p]`; composite/general = Williams. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0PivotToPolynomialMethod` |
| **MOD gate semantics + residual observer reduces to membership (the ACC⁰ battlefield)** | Grounds the `MOD` no-go in gate SEMANTICS and proves the residual observer can't escape. Part A: **`effVal`/`parityVal`/`ParityConstant`**; **`parity_constant_iff_support_fully_fixed`** (a parity gate is constant under `ρ` IFF support entirely fixed — forward = the no-absorbing-value statement); **`and_constant_of_absorbing`** (contrast: `AND` deactivated by one fixed-`false` input — `AND` has an absorbing value, parity has none = why `AC⁰` switches, `ACC⁰` doesn't). Part B: **`residualSignature`** (free summand iff free ∧ in support); **`residual_eq_membership_of_free`** (free coord: residual signature = membership pattern); **`residual_merge_iff_sameCell`** (free coords merge under residual observer IFF share membership cell); **`residualPatternCount_eq_membership_on_free`** (residual count = membership count); **`residual_no_escape_in_hardRegime`**. CONCLUSION: a coordinate's affine contribution to a linear gate IS its membership ⇒ residual observer = membership observer on free coords ⇒ inherits the hard-regime ceiling ⇒ the ENTIRE observer/merging programme is membership-bounded for `MOD` ⇒ `ACC⁰` needs the polynomial method. NEGATIVE/localization result. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MODResidualObserver` |
| **MOD no-go — variable-fixing merging is INERT for symmetric gates** | A `MOD` gate has NO absorbing value (constant iff support ENTIRELY fixed), so **`MODGateActive ρ supports j := ∃ i ∈ supports j, ρ i = none`** (active iff it reads a free input). **`mod_separating_gate_active`** (a gate separating two FREE coords contains one of them ⇒ always active ⇒ can never be deactivated); **`modRefined_eq_membership_of_free`** (free coord: `MOD`-refined pattern = membership pattern — no gate drops out); **`mod_refined_merge_iff_sameCell`** (two free coords share a `MOD`-refined cell IFF a membership cell — NO merging gain); **`mod_no_collapse_in_hardRegime`** (distinct free coords never merge). Proves the `AND`/`OR` absorbing-value merging that powers the refined route is inert for `MOD` — exactly why `AC⁰` switches but `ACC⁰` does not. Pins the obstruction to `MOD`'s lack of an absorbing value. A NEGATIVE/localization result, NOT an `ACC⁰` lower bound. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0MODNoGo` |
| **richer variable-fixing observer model — restriction CAN merge** | The model the membership ceiling demanded. `Restriction n := Fin n → Option Bool` (`none`=free); `AND`-of-positive-literal gates become CONSTANT when an input is fixed-`false`, so a gate drops out even while reading a free coord — the merge membership couldn't do. **`Restriction`/`freeSet`/`GateActive`** (no fixed-false input ∧ has free input)/**`refinedCellPatternVec`** (membership over ACTIVE gates only)/**`refinedCellPatternCount`/`RefinedCellCollapse`**; **`exists_sameRefinedCell_of_collapse`** (pigeonhole); **`refined_merge_of_inactive_separators`** (THE new power: coords separated only by inactive gates merge); **`refined_strictly_beats_membership`** (concrete `n=3,k=1`, gate `{0,2}`, fix coord 2 false: `¬ CellCountCollapse` membership ∧ `RefinedCellCollapse` refined — strict gain over the proved ceiling). **`refinedWeightVec`** (support-count on active gates, 0 on inactive); **`refined_mem_of_eq`** (equal refined patterns ⇒ membership agrees on active gates); **`refinedWeightVec_pairSwap`** (refined-same-cell swap preserves the statistic); **`RefinedLowCorrelation`** / **`refinedCellCollapse_implies_refinedLowCorrelation`** (the CORRELATION BRIDGE, via the generic `low_correlation_of_pres`). OPEN (the genuine ACC⁰ wall): `MOD`/symmetric gates have NO absorbing value (constant only when support fully fixed) ⇒ this AND/OR-style merging is weak for `MOD` (AC⁰ switches, ACC⁰ doesn't). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RefinedObserverModel` |
| **EXACT characterization of the cell-count socket — false in the hard regime** | `cellPatternVec` is `L`-INDEPENDENT, so two live coords collapse iff their FULL patterns are equal, and any separating gate contains one of them so always SURVIVES ⇒ a restriction CANNOT merge patterns (only drops coords). **`globalCellCount_le_n`** (`≤ n`); **`forcesLowCellCount_iff_global_lt`** (`FullACC0ForcesLowCellCount ↔ globalCellCount < n`); **`forcesLowCellCount_iff_not_injective`** (`↔ ¬ Injective (cellPatternVec supports)`); **`not_forcesLowCellCount_of_hardRegime`** (`HardRegime univ ⇒ ¬ FullACC0ForcesLowCellCount`, since hard ⇒ `globalCellCount = n` = injective). HONEST CEILING: the route fires EXACTLY when two coords already share a global pattern (`2^k<n` pigeonhole / structured fragments force it); FALSE in the hard regime. `StructuredOverlappingMOD ⇒ collapse` is a FALSEHOOD for genuinely-hard families. Restriction-merging needs a RICHER model (fixing vars ⇒ gates constant). NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountCharacterization` |
| **sunflower (common-core) supports — wide overlap, `≤ k+2` cells** | `supports j = core ∪ petal j` with pairwise-disjoint petals: every pair OVERLAPS in the core (NOT laminar — `supports i ∩ supports j = core ≠ ∅`, neither nested). **`SunflowerSupports`**; **`sunflower_suppSet_core/_petal/_empty`** (patterns are `univ`, `{j₀}`, or `∅`); **`sunflower_cellPatternCount_le`** (`≤ k+2`; image `⊆ insert univ (insert ∅ (univ.image singleton))`); **`sunflower_low_correlation`** (`k+2 < \|L\| ⇒` low corr, regardless of rank/survivors). Genuine wide-overlap-⇒-few-patterns fragment outside laminar/low-rank. General overlapping `MOD` need not be sunflower-structured (can shatter) = the wall. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0SunflowerCellCount` |
| **hard-regime reduction — exactly where the open lemma lives** | **`HardRegime supports L`** := `\|L\| ≤ 2^{survivingCount L} ∧ \|L\| ≤ globalCellCount` (both general collapse bounds fail). **`collapse_of_not_hardRegime`** (outside ⇒ `CellCountCollapse`, via survivor route ∨ global route); **`full_of_hardRegime_resolved`** (`HardRegime univ → Full ⇒ Full`: the hard regime is the ONLY obstruction); **`hardRegime_univ_of`** (the realistic regime `n ≤ 2^{surv univ}`, `n ≤ globalCellCount` IS hard). A reduction isolating the open content (`cellPatternCount L < \|L\|` while both bounds fail = the merged-count concentration on surviving gates), not a resolution. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0CellCountHardRegime` |
| **direct cell-count concentration — not via `2^{survivors}`** | `cellPatternVec` is INDEPENDENT of `L` (ranges over all gates), so cell count = distinct patterns among live coords, monotone in `L`, bounded by restriction-independent `globalCellCount := cellPatternCount supports univ ≤ 2^k`. **`cellPatternCount_le_card`** (`≤ \|L\|`); **`cellPatternCount_mono`** / **`cellPatternCount_le_global`** / **`globalCellCount_le_two_pow_gates`**; **`Pr_cellPatternCount_ge_le_size_tail`** (`Pr[≥a] ≤ Pr[\|L\|≥a]`, no `2^surv`); **`Pr_cellPatternCount_ge_eq_zero_of_global_lt`**; **`expected_cellPatternCount_le_global`** (`Exp ≤ globalCellCount`, direct first moment); **`low_global_cellCount_collapse`** / **`low_global_forces_lowCellCount`** / **`few_gates_forces_lowCellCount`** (deterministic: `globalCellCount<\|L\|`, resp `<n`, resp `2^k<n` ⇒ collapse, NO probability). HONEST WALL: `globalCellCount` is restriction-independent, generically `=n` for `k≫log n`; the restriction's real power is MERGING patterns among surviving gates (`cellPatternCount L ≪ globalCellCount`), and bounding that merged count below both `2^surv` and `\|L\|`/`globalCellCount` is the open structural-concentration content. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0DirectCellConcentration` |
| **random restriction in cell-count language — first moment, Markov, sharp whp, the precise wall** | The cell-count analogue of the rank random-restriction/whp files, on `ACC0ForcesLowCellCount`. **`cellPatternCount_le_two_pow_cellRank`/`_survivingCount`** (monotone bounds); **`expected_cellPatternCount_le_expected_pow_survivors`** (`Exp[cellPatternCount] ≤ Exp[2^surv]`); **`Pr_cellPatternCount_ge_le_markov`** (`Pr[≥a] ≤ Exp/a`); **`Pr_cellPatternCount_ge_le_pow_survivor`** (proxy `≤ Exp[2^surv]/a`); **`randomRestriction_forces_low_cellCount`** (`Exp ≤ B < a ⇒ ∃ L, cellPatternCount < a`); **`cellCount_predictor_fails_whp`** (two-event balance, needs only `a ≤ b` — SHARPER than rank's `2^a ≤ b`); **`cellCount_whp_subsumes_rank`** (rank feasibility `2^a≤b` ⇒ cell-count fires — broadest whp); **`expected_cellPatternCount_le_of_bound`** + **laminar (`≤k+1`)** + **bounded-distinct (`≤2ᵈ`)** + **block-product (`≤cᵐ`)** discharges. HONEST WALL: first moment closes only for deterministic-bound structures; general wide overlapping `MOD` has only the exp-weak `Exp[2^surv]` handle — closing the gap needs structural concentration on the distinct-pattern count = the open switching lemma. NOT `NEXP ⊄ ACC⁰`, NOT `P ≠ NP` | `…ACC0RandomRestrictionCellCount` |
| **single small-error boosted form (averaging → per-gate error)** | From the majority-correct family to a *single* small-error form. **`errSet`** (`{w | (∃j w_j)∧∀l L_{σl}(w)=0}`) + **`errSet_eq`** (= the actual `boostPredict`-vs-`OR` error, via one-sidedness `pv=1→∃j`); **`sum_errSet_card_le`** (`∑_σ|errSet σ|≤2^k·(2^{k-1})^t`, Fubini double-count via `boost_wrong_card`); **`exists_small_errSet`** (averaging+pigeonhole: `∃σ (2^k)^t·|errSet σ|≤2^k·(2^{k-1})^t`, error fraction `≤2^{-t}`). The per-gate small-error input for `…ACC0ErrorAccumulation` | `…ACC0SmallErrorForm` |
| **circuit datatype + approximation invariant + base cases (depth-induction setup)** | The scaffolding for the RS depth induction. **`Circ n`** (unbounded-fan-in: `inp`/`const`/`not`/`or`[List]/`and`[List]) with **`Circ.eval`**/**`Circ.size`** (List recursion via `attach`+`sizeOf` termination). **`errCard P C`** (#inputs where poly `P` disagrees with circuit `C`), **`Approximable C D E := ∃P, totalDegree≤D ∧ errCard≤E`** (the invariant). Base cases PROVEN: **`approximable_inp`** (`X i`, `D=1,E=0`), **`approximable_const`** (`C(boolToZMod b)`, `D=0,E=0`), **`approximable_not`** (`1-P`, preserves `D,E`). OR/AND inductive step stated as remaining (combines small-error `…ACC0SmallErrorForm` + degree-comp `…ACC0LayerCompose` + per-point `…ACC0CompositionCorrect` + error-accum `…ACC0ErrorAccumulation`; `degree≤t·D, error≤∑E_i+2^{-t}2^n`). Honest: scaffolding+base cases; inductive step + composite MOD = Wall 1 | `…ACC0CircuitApprox` |
| **input-space small-error averaging (per-gate error for the circuit induction)** | Generalizes `…ACC0SmallErrorForm` from the subgate-output space to an arbitrary value map `v : X → (Fin k → Bool)` (the circuit case: `x↦v(x)` not measure-preserving, average over INPUTS). **`boostError_iff`** (boosted OR errs at `w` ↔ `(∃j w_j)∧∀l L_{σl}(w)=0`); **`errSetV`/`errSetV_eq`** (input error set for `v`, = actual `boostPredict`-vs-`OR` error on `v(x)`); **`sum_errSetV_card_le`** (`∑_σ|errSetV v σ|≤|X|·(2^{k-1})^t`, Fubini over inputs); **`exists_small_errSetV`** (`∃σ (2^k)^t·|errSetV v σ|≤|X|·(2^{k-1})^t`, error fraction `≤2^{-t}` over circuit inputs). The per-gate input for the OR/AND inductive step of `…ACC0CircuitApprox` | `…ACC0InputSmallError` |
| **the OR/AND inductive step (assembled)** | One OR/AND layer of the RS depth induction. `perr P f` (error set poly-vs-Bool-fn). **`or_step`**: subgates `h_i` with degree-`≤D` error-`≤E` approximants `P_i` ⇒ `∃ Q (=compPoly P σ), Eg` with `totalDegree Q≤t·D`, `perr Q (OR-of-h)≤k·E+Eg`, `(2^k)^t·Eg≤2^n·(2^{k-1})^t`. CRUX = error containment `perr(compPoly,OR)⊆(⋃_i perr(P_i,h_i))∪errSetV` (per-point composition `eval_compPoly_of_subgates` + `errSetV_eq`: where all subgates correct AND gate-boosting correct, compPoly=OR exactly), then `card_union_le`+`card_biUnion_le`+`exists_small_errSetV`. **`and_step`**: dual `AND=¬OR¬` (or_step on negated subgates `1-P_i`/`¬h_i` via `perr_not_eq` + `andTarget_eq`). Honest: one layer over Fin k; Circ List-to-Fin packaging + depth iteration + composite MOD = Wall 1 | `…ACC0OrStep` |
| **the depth induction: every MOD-free circuit is approximable (AC⁰ closed)** | Closes the RS depth induction for the MOD-free (`AC⁰`) fragment end-to-end. **`or_eval_bridge`**/**`and_eval_bridge`** (`Circ.eval (or/and cs)` = `orTarget`/`andTarget` of subcircuit values; List→Fin via `any_eq_true`/`all_eq_true`+`mem_iff_get`+`Bool.eq_iff_iff`); `errCard_eq_perr` (rfl). **`approximable_exists`**: `∀ C : Circ n, ∃ D E, Approximable C D E` — well-founded recursion on `sizeOf` (decreasing via `List.sizeOf_lt_of_mem (get_mem)`+omega), base cases (inp/const/not) + `or_step`/`and_step` bridged from List to Fin (`choose`+`Finset.sup` for uniform subgate bounds). Honest: **classical `AC⁰`/`AC⁰[2]`-level (Tier 1/2), NOT new math, NOT P≠NP**. MOD extends only to prime-power (`mod2_mem_monoAND_span`); composite MOD has no low-deg rep = the genuine `ACC⁰` barrier (Wall 1, why Williams) | `…ACC0DepthInduction` |
| **quantitative degree bound: `degree ≤ t^depth`** | Sharpens the AC⁰ induction to the RS degree form. `cdepth` (OR/AND add a layer, NOT free), `le_foldr_max`, **`cdepth_or/and_get_lt`** (child depth < parent), `Approximable.mono_deg`. **`approximable_quant`** (`ht : 1≤t`): `∀ C, ∃ E, Approximable C (t^cdepth C) E` — degree `≤ t^depth`, by well-founded recursion: base via `simp[cdepth,pow_zero]`/`mono_deg`, OR/AND via `or_step`/`and_step` with uniform `D=t^(depth-1)` (each subgate `t^cdepth(child)≤t^(depth-1)` by `pow_le_pow_right`+child<parent) then `t·t^(depth-1)=t^depth`. Each OR/AND layer ×t ⇒ `(log s)^{O(d)}` for `t=polylog`, constant depth. Honest: still classical AC⁰/AC⁰[2], error existential, composite MOD = Wall 1 | `…ACC0QuantDegree` |
| **quantitative error bound: degree `≤ t^depth` AND error `≤ size·2^{-t}`** | Completes the quantitative AC⁰ RS package. **`gate_bound`** (`∃σ, 2^t·|errSetV v σ|≤2^n` — per-gate boosting error `≤2^{-t}` fraction; cancels `(2^{k-1})^t` for `k≥1`, `errSetV=∅` for empty `k=0` gate); **`or_step_quant`/`and_step_quant`** (degree `≤t·D`, error `≤(∑ subgate errors)+Eg`, gate `2^t·Eg≤2^n` — sum form for size-additive accumulation); `sum_get_eq` (Fin↔List sum bridge via `ofFn`); **`approximable_full`** (`ht:1≤t`): `∀ C, ∃ Q, Q.totalDegree ≤ t^cdepth C ∧ 2^t·(perr Q ⟦C⟧).card ≤ size C·2^n` — degree `t^depth` AND error `size·2^{-t}`, by well-founded recursion (errors accumulate additively over `size` gates via union bound, each layer `≤2^{-t}` fraction). Honest: classical AC⁰/AC⁰[2] (Tier 1/2), NOT new math, NOT P≠NP; composite MOD = Wall 1 | `…ACC0QuantError` |
| **Williams `fastSat` concrete + quantitative** | Grounds the abstract `fastSat`/`savings` Prop (`…ObserverWilliams`/`…WilliamsCashout`) in the SYM∘AND combinatorics. `fastSatWork m=m+1`; **`fastSat_savings_of_work_le`** (`work≤2^{n-k}⇒2^k·work≤2^n`, via `SpeedupMargin`); **`fastSatWork_le_of_degree`** (deg-≤D inj monomial-AND ⇒ work ≤(∑_{i≤D}C(n,i))+1, via `monomial_count_le`); **`lowDegMonomialCount_le_pow`** (∑_{i≤D}C(n,i)≤(D+1)·n^D, via `Nat.choose_le_pow`); **`symAnd_williams_fastSat`** (SAT decided by count-cell search ∧ cells fit 2^{n-k} ∧ savings≥2^k); **`symAnd_williams_fastSat_quasipoly`** ((D+1)·n^D+1≤2^{n-k}⇒savings≥2^k; D polylog⇒k=Ω(n) margin). Makes combinatorial fastSat a counted procedure with exact margin. Honest: does NOT prove NEXP⊄ACC⁰; open links named/unfaked = (1) YBT exact normal form (MixedACCDepthReductionSocket; method only approximate, prime-power MOD only), (2) uniform TM realization (UniformWilliamsRealizationSocket = separation-strength per williams_socket_iff_separation). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0WilliamsFastSat` |
| **MIP = NEXP: the sum-check round-reduction engine (PROVED), arithmetization socketed** | Opens the entry-221 `NexpEqMIP` socket (Babai–Fortnow–Lund) and proves its genuine arithmetic engine — the sum-check round reduction. Over a `CommRing R`: **`boolToR : Bool → R`** (`{0,1}⊆R`), **`scSum g := ∑_{b:Fin m→Bool} g(boolToR∘b)`** (hypercube sum = the arithmetized claim), **`roundPoly g s := ∑_t g(Fin.cons s (boolToR∘t))`** (the round polynomial). **`scSum_succ_eq`** (PROVED): `scSum g = roundPoly g 0 + roundPoly g 1` — the verifier's per-round consistency check (split first coord via `consEquivBool`+`Fintype.sum_bool`, match `boolToR false/true = 0/1` via `boolToR_cons`). **`roundPoly_eq_scSum_restrict`** (PROVED, rfl): `roundPoly g r = scSum (g(r,·))` — recursion to an m-var instance after the challenge. **`scSum_zero`** (PROVED): `scSum g = g Fin.elim0` — termination at a single eval. Pure finite-sum algebra, NO measure theory. **`nexpEqMIP_of_subset`** (PROVED): discharges `NexpEqMIP` by antisymmetry from `NexpSubsetMIP`+`MipSubsetNexp`. Honest: proves the sum-check ENGINE (round consistency + recursion + base) + the antisymmetry assembly; residual sockets = `NexpArithmetization` (cert→low-deg poly), `SumCheckSoundness` (Schwartz–Zippel + multilinearity = why MIP not IP=PSPACE), `MipSubsetNexp` (simulation). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SumCheck` |
| **The ACC anatomy frozen — `CarryRefinementCrossing` as the single composite target (roadmap steps 1+3, PROVED bundle)** | Freezes the result and names the one remaining theorem. **Frozen:** the prime-`ACC⁰[p]` route is machine-assembled (264–279) modulo the standard binomial tail + the committed `Circ` recursion; the composite obstruction is isolated and its three natural attacks are all proved to fail (280–282). **`field_based_modes_all_fail`** (PROVED): bundles the three composite no-gos — (1) *separate* — `∀ F field, ¬(CharP F 2 ∧ CharP F 3)` (entry 280); (2) *stage* — bounded degree `t^d·D₀` cannot meet a strictly larger `R` (entry 281); (3) *carry* — `ZMod 6` has zero divisors and Fermat fails (entry 282). So *every* characteristic-committed (field-based) approach to `MOD₆` is refuted. **`CarryRefinementCrossing Mod6Computable FieldFreeUnboundedCarry`** (named socket — the real open `ACC⁰` theorem): *any bounded, field-compatible observer computing `MOD₆` is forced into field-free / unbounded-carry territory*; proving it is the composite lower bound, and by `field_based_modes_all_fail` it needs a **characteristic-independent invariant** (a new idea — candidate directions: ring/module invariant, communication/tensor rank, proof-complexity, observer-holonomy, carry-complexity). Step-5 first test (open, *not* proved): any bounded observer computing `MOD₆` collapses to a non-native single field (excluded by 280/281) or uses unbounded carry. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CarryRefinementCrossing` |
| **The composite wall — product-field observers with explicit carry state fail (roadmap step 5 cont., the namesake NO-GO)** | The third honest composite no-go, whose failure mode is literally `CarryRefinementCrossing`'s namesake. The natural next idea after 280/281: **track both fields at once via an explicit carry state** — work over the product `ZMod 2 × ZMod 3 ≃ ZMod 6`. This **proves why that fails**: the carry state is *not a field*. **`carry_state_has_zero_divisors`** (PROVED): `ZMod 6` has zero divisors (`2 · 3 = 0`, `2 ≠ 0`, `3 ≠ 0`); **`prodField_carry_has_zero_divisors`** (PROVED): same in product form (`(1,0)·(0,1) = 0`, CRT idempotents). **`carry_state_not_domain`** (PROVED): hence `ZMod 6` is not an integral domain (not a field). **`fermat_fails_on_carry`** (PROVED): `(2 : ZMod 6) ≠ 0` yet `(2 : ZMod 6)^{6-1} = 2 ≠ 1` — Fermat's little theorem, the *engine* of the degree-halving / Fermat indicator (entry 266), is **false** over the carry ring (6 not prime). **`product_field_carry_state_no_traction`** (PROVED): packages both — the carry state is not a field *and* Fermat fails there, so the entire Smolensky method (entries 264–279, all over `[Field F]` / `[Fact p.Prime]`, resting on the Fermat indicator) cannot be instantiated at the carry state. The carry that would couple the two component fields crosses into their product *ring*, losing the field property — the most literal form of the `CarryRefinementCrossing` obstruction (entry 238): the carry does *not* refine into a single-field computation. A *proved no-go*, **not** a circumvention; composite `ACC⁰` needs a method that works over such a ring or avoids fields — a new idea. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Mod6CarryState` |
| **The composite wall — bounded-depth staged observers for MOD₆ fail too (roadmap step 5 cont., proved NO-GO)** | The second honest composite no-go: the natural fix for entry-280's field-incompatibility is to *stage* the computation (`MOD₂` over `F₂`, `MOD₃` over `F₃`, then combine, each stage bounded-depth); this **proves where staging fails**. **`staged_degree_bound`** (PROVED): a depth-`d` staged observer's degree is `(·t)^[d] D₀ = t^d · D₀` (the layer recurrence, entry-273 `pow_depth_degree`) — *bounded depth gives bounded degree*. **`mod6_staged_output_field_not_native`** (PROVED): however many intermediate stages, the *final output* is over a single field `F`, which (entry 280) cannot be native for both `MOD₂` and `MOD₃` — staging does not manufacture a common field. **`bounded_depth_staged_no_go`** (PROVED): if the observer degree is `≤ t^d · D₀` (bounded depth), computing `MOD₆` forces degree `≥ R` (the non-native CRT factor, by the prime Smolensky bound, entries 275–279), and `t^d · D₀ < R` (the requirement grows with input size), then `False` (`omega`). So staging adds intermediate fields but the final output field is defeated by the growing non-native degree — `CarryRefinementCrossing` (entry 238) for the staged model. A *proved no-go* (bounded depth caps the degree; the non-native factor needs unbounded degree), **not** a circumvention; composite `ACC⁰` still needs a characteristic-independent method. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Mod6StagedObservers` |
| **The composite wall, smallest case — MOD₆ separated MOD₂/MOD₃ layers cross fields (roadmap step 5, proved NO-GO)** | The honest composite attack (not a claim of composite `ACC⁰`): take `6 = 2·3` and the natural "separated layers" idea, and **prove precisely where it fails** — pinning `CarryRefinementCrossing` (entry 238) at `n = 6`. **`mod6_fires_iff_mod2_and_mod3`** (PROVED): `MOD₆` fires (weight `≡ 0 mod 6`) iff *both* `MOD₂` and `MOD₃` fire — `6 ∣ #trues ↔ 2 ∣ #trues ∧ 3 ∣ #trues` (entry-245 `modm_iff_modp_and_modq`, coprime 2,3) + entry-270 `modpGate_fires_iff`. So a `MOD₆` gate genuinely separates into a `MOD₂` and a `MOD₃` layer. **`no_common_native_field`** (PROVED): no field `F` has `CharP F 2 ∧ CharP F 3` (entry-243 `no_common_char` at `2 ≠ 3`). **`mod6_layers_cross_fields`** (PROVED): therefore the separated layers — each component native *only* in its own characteristic (`MOD₂` deg 1 over `F₂`, `MOD₃` deg 2 over `F₃`, entry 270) — cannot both be native over a *single* field; the prime Smolensky route (which commits to one fixed characteristic) does *not* lift to `MOD₆`. **This is the carry-crossing obstruction in its smallest instance**: the polynomial method picks one characteristic, and composite modulus has CRT factors living over different ones. A *proved negative structural fact* (the natural separated-layers idea fails by field incompatibility), **not** a circumvention; composite `ACC⁰` needs a char-independent method (the genuinely-open `CarryRefinementCrossing`, entry 238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Mod6SeparatedLayers` |
| **Prime mechanical closure — `ApproximatorDegreeBound` discharged + the clean prime theorem (roadmap step 1) (PROVED)** | Discharges the genuinely-mechanical residual of the prime route and packages the clean prime theorem, **leaving exactly one frontier: composite carry-crossing (entry 238)**. **`approximator_times_complement_totalDegree`** (PROVED): `(P * Q).totalDegree ≤ D + d` from `P.totalDegree ≤ D`, `Q.totalDegree ≤ d` (`MvPolynomial.totalDegree_mul`) — **discharges `ApproximatorDegreeBound`** (the approximator, degree `≤ D`, times the complement monomial, degree `≤ |Sᶜ| ≤ n/2`, has degree `≤ D + n/2`; the product-degree bookkeeping the degree-halving needed). **`prime_smolensky_route_closed`** (PROVED, no axioms): `CrossFieldCountHard` (established by entry-278's `no_small_approximator`) + the entry-261 bridge ⇒ `ACC0CompositeComponent` — the clean prime route to the ACC component. The **binomial tail** (`lowDegreeDim n (n/2+D) < 2ⁿ − E` for `D = O(√n)`) is *not* bookkeeping — it is the standard Chernoff/normal-tail estimate; kept as an honest numeric hypothesis (the Smolensky parameter regime), with the weak form `lowDegreeDim n D < 2ⁿ` (`D<n`) proved (entry-264 `lowDegreeDim_lt_two_pow`). The `Circ` recursion is the committed circuit arc's. **After this the prime-`ACC⁰[p]` lower bound is assembled with every *mechanical* part proved; the single genuinely-open frontier is composite modulus — the `CarryRefinementCrossing` wall (entry 238), which needs a new idea.** NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0PrimeMechanicalClosure` |
| **The prime Smolensky lower bound, assembled + wired to the ACC component (roadmap steps 3–4) (PROVED)** | Assembles the prime-case Razborov–Smolensky lower bound from the proved pieces (pigeonhole 275 + degree-halving 276 + wiring 277) and wires the hardness to `ACC0CompositeComponent` (bridge 261). **Prime case only; composite stays the open `CarryRefinementCrossing` wall (238), untouched.** **`smolensky_prime_goodset_bound`** (PROVED): under `SmolenskyDegreeHalving (D') G` (good-set point functions degree `≤ D'`, from 276/277), `|G| ≤ lowDegreeDim n D'` — the good set is bounded by the low-degree dimension (contrapositive of entry-275's `smolensky_lower_bound_via_pigeonhole`). **`no_small_approximator`** (PROVED): degree-halving + good set `≥ 2ⁿ − E` (small-bad-set output of the approximation machinery) + `lowDegreeDim n D' < 2ⁿ − E` (binomial tail) ⇒ `False` (`omega` from `2ⁿ−E ≤ |G| ≤ lowDegreeDim n D' < 2ⁿ−E`): **no degree-`D'`, `≤ E`-error approximator of the symmetric `MOD`/parity target exists** — the prime Smolensky lower bound. **`prime_route_to_ACC0Component`** (PROVED, no axioms): `CrossFieldCountHard` + the entry-261 bridge `crossFieldHard_to_ACC0Component` ⇒ `ACC0CompositeComponent` (upstream of Williams). Residual (mechanical/standard): `ApproximatorDegreeBound` (entry 277, product-degree), the binomial tail `lowDegreeDim n (D+n/2) < 2ⁿ−E`, the prime-case `CrossFieldCountHard` identification. Composite = the open wall (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SmolenskyPrime` |
| **The prime-case wiring — replacing the full product by the approximator on the good set (PROVED)** | Roadmap step 1: the clean bridge from entry-276's degree-halving substitution into the proven approximation machinery (boosting, entry 267; `Approximable`, the committed arc). Discharges the *agreement-input* of entry-276's `FullProductLowDegreeOnGoodSet` socket. **`fullProduct_replace_on_goodSet`** (PROVED): given the approximator `P` agrees with the full product on the good set (`∀ x ∈ G, P x = ∏ᵢ yencₓ i`), then on `G` a monomial collapses — `∏_{i∈S} yencₓ i = P x · ∏_{i∈Sᶜ} yencₓ i` — by substituting the agreement into entry-276's `smolensky_substitution` (so the hard full product is *replaced* by the degree-`D` approximator inside the halving). **`monomial_eq_approx_times_lowComplement`** (PROVED): for `|S| > n/2`, on `G` the high-degree monomial equals `P x ·` (complement monomial) *and* the complement has `2·|Sᶜ| < n` factors (entry 276) — approximator × sub-half-degree complement. Socket: **`ApproximatorDegreeBound`** — `P` degree `≤ D` ⇒ `P x · ∏_{i∈Sᶜ} yencₓ i` has degree `≤ D + |Sᶜ| ≤ D + n/2`, so good-set point functions ∈ `lowDegreeSubmodule n (D + n/2)`, discharging `SmolenskyDegreeHalving`; product-degree bookkeeping. The *agreement* input — formerly the unspecified part of the socket — is now wired: it is exactly the approximation machinery's output (approximator = symmetric target off a small bad set, good set = complement). Prime case = textbook Smolensky; composite = the open `CarryRefinementCrossing` wall (entry-238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0FullProductGoodSet` |
| **The Smolensky degree-halving — the prime-case substitution identity (textbook Smolensky) (PROVED)** | Attacks the `SmolenskyDegreeHalving` socket (entry 275) — **prime case only, scoped to textbook Smolensky; composite untouched** (that is the `CarryRefinementCrossing` wall, 238). Proves the algebraic core of degree-halving over the `{±1}` encoding (values `yᵢ² = 1`, the `p`-odd setting). **`smolensky_substitution`** (PROVED): `∏_{i∈S} yᵢ = (∏ᵢ yᵢ) · (∏_{i∈Sᶜ} yᵢ)` — the full product is `∏_S · ∏_{Sᶜ}` (`Finset.prod_mul_prod_compl`), and `(∏_{Sᶜ} yᵢ)² = ∏_{Sᶜ} yᵢ² = 1`, so the right side collapses to `∏_S` (closed by `linear_combination`, avoiding the dependent-motive `rw` on the `univ` product). **`compl_card`** (PROVED): `|Sᶜ| = n − |S|`. **`compl_card_lt_half`** (PROVED): `n < 2·|S| ⇒ 2·|Sᶜ| < n`. **`monomial_halving`** (PROVED): for `|S| > n/2`, the monomial `∏_{i∈S} yᵢ = (∏ᵢ yᵢ) · (∏_{i∈Sᶜ} yᵢ)` with `2·|Sᶜ| < n` — *a high-degree monomial equals the full product times a sub-half-degree complement*. Socket: **`FullProductLowDegreeOnGoodSet`** — on the good set, the full product `∏ᵢ yᵢ` (the symmetric `MOD`-type function) equals a degree-`D` polynomial (the `AC⁰[p]` approximator); combined with `monomial_halving` it halves every monomial to degree `≤ n/2 + D` = `SmolenskyDegreeHalving` (entry 275). Prime case = textbook Smolensky; composite = the open `ACC⁰[composite]` wall (entry-238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SmolenskyDegreeHalving` |
| **The Smolensky pigeonhole — attacking the wall (step 4) via the entry-264 rank kernel (PROVED)** | Attacks the wall `SmolenskyNonNativeLowerBound`. The Razborov–Smolensky lower bound = (counting/rank contradiction) + (degree-halving representation); **this proves the counting/rank half** via the entry-264 rank kernel + entry-265 dimension, isolating the degree-halving as the single residual socket. **`ptInd g := fun x => if x = g then 1 else 0`** — the point indicator. **`ptInd_linearIndependent`** (PROVED): the point indicators over a finset `S` are linearly independent (`Fintype.linearIndependent_iff` + evaluation — only the diagonal term survives). **`smolensky_pigeonhole`** (PROVED): if every point indicator of `S` lies in a submodule `W` with `finrank W < |S|`, then `False` — the `|S|`-dim point-indicator span (`finrank_span_eq_card`) cannot embed in the smaller `W` (entry-264 `exists_notMem_of_finrank_lt`). **`smolensky_pigeonhole_lowDegree`** (PROVED): at `W = lowDegreeSubmodule n D'` (finrank `= lowDegreeDim n D'`, entry 265) — if every good-set point indicator is degree `≤ D'` and `lowDegreeDim n D' < |S|`, then `False`. **`smolensky_lower_bound_via_pigeonhole`** (PROVED): given the degree-halving (`SmolenskyDegreeHalving`) and a good set bigger than the low-degree dimension, `False` follows. Socket: **`SmolenskyDegreeHalving`** — a degree-`D` approximator of the non-native target makes every good-set point indicator degree `≤ D' = n/2+D` (the Razborov–Smolensky representation lemma, using `MOD_q`'s algebra over `F_p`); the genuine remaining core. Prime case = classical Smolensky; composite = the open `ACC⁰[composite]` wall (entry-238 `CarryRefinementCrossing`). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SmolenskyPigeonhole` |
| **The quantitative iteration — solving the size/depth/error recurrence in closed form (roadmap step 1) (PROVED)** | Solves the *iteration*: turns entry-273's per-layer estimates into the full closed-form bound, on top of entry 273 (not modifying the committed arc). **`error_accumulation`** (PROVED): the error closed form — if `E ≤ ∑ᵢ Eg i` (union bound, the committed `error_union_bound`) and each `2^t · Eg i ≤ 2ⁿ` (per-gate, entry 273), then **`2^t · E ≤ m · 2ⁿ`** (`Finset.mul_sum` + `Finset.sum_le_sum` + `Finset.sum_const`): the total error is a `m·2^{-t}` fraction. **`error_accumulation_size`** (PROVED): with `m ≤ s`, `2^t · E ≤ s · 2ⁿ` — error a `size·2^{-t}` fraction. **`quantitative_iteration_closed_form`** (PROVED): the combined solver — degree `≤ t^d · D₀` (`pow_depth_degree`) ∧ `2^t · E ≤ m · 2ⁿ` (`error_accumulation`), the closed-form solution of the depth/error recurrence. Socket: **`CircuitStructuralRecursion`** — the structural induction over the committed `Circ` producing the per-gate decomposition (size-`s`/depth-`d` circuit ⇒ degree-recurrence poly + `∑` of `s` per-gate `2^{-t}` errors), the quantitative refinement of `approximable_exists`, in the committed circuit arc. Feeding it discharges `QuantitativeDepthBound` (entry 272). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0QuantitativeIteration` |
| **The quantitative depth bound — clean per-gate error extraction + per-layer step (roadmap (A)) (PROVED)** | Makes the depth bound *quantitative*, built **on top of** the committed `or_step`/`and_step` (not modifying them). The committed steps leave the gate-boosting error in the awkward form `(2^k)^t·Eg ≤ 2ⁿ·(2^{k-1})^t`; this file extracts the clean fraction. **`gate_error_le`** (PROVED): `(card (Finset (Fin k)))^t · Eg ≤ N · (2^{k-1})^t` (with `1 ≤ k`) ⇒ **`2^t · Eg ≤ N`** — cancelling the common `2^{(k-1)t}` (`Nat.sub_one_mul` + `Nat.le_of_mul_le_mul_left` + `pow_pos`); the gate error is a `2^{-t}` fraction, *independent of fan-in*. **`card_input`** (PROVED): `Fintype.card (Fin n → Bool) = 2ⁿ`. **`or_layer_quant`** / **`and_layer_quant`** (PROVED): the clean per-layer step — `or_step`/`and_step` repackaged with `2^t · Eg ≤ 2ⁿ` (degree `≤ t·D`, error `≤ k·E + Eg`). **`pow_depth_degree`** (PROVED): the closed-form degree recurrence — `(·t)` iterated `d` times on `D₀` is `t^d · D₀` (`Function.iterate_succ_apply'` + `pow_succ`), so a depth-`d` circuit's approximant has degree `≤ t^d · D₀`. Remaining for `QuantitativeDepthBound`: the structural iteration over the committed `Circ` (apply the layer step gate-by-gate, accumulating degree `t^d`, error `size·2ⁿ·2^{-t}`) — the quantitative refinement of the committed `approximable_exists`, left to the circuit arc. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0QuantitativeDepthBound` |
| **The polynomial-method bridge — small AC⁰[p] observer ⇒ low-degree approximation (PROVED bridge)** | Roadmap step 3: a **bridge file** connecting the abstract `F_p` kernel (264–271) and the concrete `F₂` circuit arc (Codex's `CircuitApprox`/`DepthInduction`) **without re-proving either**. **`LowDegreeApprox f D E`** := `∃ F₂-poly` of total degree `≤ D` agreeing with target `f` off `≤ E` inputs — the common notion. **`approximable_iff_lowDegreeApprox`** (PROVED, `Iff.rfl`): Codex's `Approximable C D E` *is* `LowDegreeApprox (boolToZMod ∘ Circ.eval C) D E` — the two arcs coincide definitionally. **`small_AC0p_observer_implies_lowDegreeApprox`** (PROVED): every `MOD`-free `AC⁰` circuit's function has a low-degree `F₂` approximant — re-exporting the committed `approximable_exists` (the "small observer ⇒ low-degree approximation" direction). **`lowDegree_excludes_highDegree`** / **`polynomial_method_contradiction`** (PROVED): the contradiction core — a function cannot both have a degree-`≤D`, `≤E`-error approximant and require more (`QuantitativeDepthBound ∧ SmolenskyNonNativeLowerBound → False`). Sockets: **`QuantitativeDepthBound`** (the size/depth → `(D,E)` refinement, threading the committed `or_step`/`and_step` + boosting `t`; not yet assembled) and **`SmolenskyNonNativeLowerBound`** (`¬ LowDegreeApprox` for the non-native `MOD_q`/`F₂` target — the wall; mechanism = the proved `algExpander_forces_high_degree`/264; composite `q` = the open wall 238). Supplying both completes the *prime*-`MOD` lower bound; composite = the open `ACC⁰[composite]` barrier → `ACC0CompositeComponent` → Williams. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0PolynomialMethodApproximation` |
| **The native/non-native bridge — split made theorem-level + MOD_p wired to the PatternRich socket (PROVED)** | Makes the native-easy/non-native-hard split *theorem-level* (not commentary) and connects entry-270 `MOD_p` to the `PatternRich`/cross-field lower-bound socket (262/264) via the **dictator family**, whose cross-field count *is* `MOD_p`. **`modp_native_easy`** (PROVED): over `F_p`, `MOD_p` has the exact degree-`(p-1)` representation (entry 270). **`dictator_crossFieldCount_eq`** (PROVED, rfl): `crossFieldCount q (gᵢ:=xᵢ) x = #{true inputs} mod q`. **`modp_iff_dictator_crossFieldCount_zero`** (PROVED): `MOD_p` fires iff the dictator cross-field count mod `p` is `0` (`modpGate_fires_iff` + `Nat.dvd_iff_mod_eq_zero`) — identifying `MOD_p` with the entry-251 cross-field-count object. **`dictator_meets_patternRich_socket`** (PROVED): the dictator family is `AlgExpander` (260) ∧ `PatternRich (2^s)` (261) — exactly the antecedent of `PatternRichCrossFieldLowerBound` (262). **`native_nonnative_split`** (PROVED): the theorem-level split — (native) the exact `F_p` representation + (cross-field) the identification of `MOD_p` with the dictator cross-field count. Socket: **`NonNativeMODHard`** = `PatternRichCrossFieldLowerBound` (the central wall; reduced by 264 to the single `PolynomialMethodApproximation` ingredient, which 264–270 supply the proved RS kernel for; composite `q` = the open wall 238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0NativeNonNativeBridge` |
| **The MOD_p gate — exact native degree-(p-1) representation + the native/non-native dichotomy (PROVED)** | The third `AC⁰[p]` gate (the `SYM` of Beigel–Tarui `SYM∘AND`), completing the per-gate analytic kernel beyond AND/OR (266). **`modpGate p x := decide (∑ᵢ (if xᵢ then 1 else 0 : ZMod p) = 0)`** — fires iff `#{true inputs} ≡ 0 mod p`. **`linSum_eq_count`** (PROVED): `∑ᵢ (if xᵢ then 1 else 0) = ↑#{i : xᵢ}` in `F_p` (`Finset.sum_boole`). **`modp_native_repr`** (PROVED): `1 − (∑ᵢ xᵢ)^(p-1) = if modpGate p x then 1 else 0` — the **exact** degree-`(p-1)` *native* `F_p` polynomial for `MOD_p`, **fan-in-free** (degree independent of `n`), via the `fermat_indicator` (266). So `MOD_p` is *easy over its own field* (constant native degree). **`modpGate_fires_iff`** (PROVED): `modpGate p x ↔ p ∣ #{i : xᵢ}` (`ZMod.natCast_eq_zero_iff`) — `MOD_p` is the divisibility of the Hamming weight, i.e. the `SYM`/cross-field-count object (entry 251). Socket: **`ModpNonNativeHardOverOtherField`** — `MOD_p` over `F_ℓ` (`ℓ ≠ p`) requires high degree (the Razborov–Smolensky obstruction; mechanism = the proved `algExpander_forces_high_degree`/264; composite modulus = the open wall 238). The **dichotomy**: native (`F_p`) easy degree `p-1`; non-native (`F_ℓ`) high degree = the wall. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModpGate` |
| **Independence — joint failure of k clauses ≤ 2^{-k}, discharging `IndependentIntersectionBound` (PROVED)** | The last probabilistic ingredient of the single-gate low-degree approximation, proved in the honest model where the `k` random subsets are independent: the **product space `∀ j, Y j`** of the `k` draws, where each clause's bad event is a *cylinder* (depends only on coordinate `j`). **`jointBad_card`** (PROVED): the independence factorization — `|{y : ∀ j, y j ∈ B j}| = ∏ⱼ |B j|` (`Fintype.card_piFinset`), i.e. `|⋂ⱼ bad j| = ∏ⱼ |B j|`. **`independent_intersection_bound`** (PROVED): if each clause is bad on at most half its draws (`2·|B j| ≤ |Y j|` — the per-clause `1/2` of entry 268), then `2^k · |jointBad| ≤ ∏ⱼ |Y j|` (pull `2^k = ∏ⱼ 2` in via `Finset.prod_mul_distrib`, compare factorwise via `Finset.prod_le_prod`). **`joint_error_le`** (PROVED): equivalently `2^k · |jointBad| ≤ |∀ j, Y j|` (`Fintype.card_pi`) — the `k` independent clauses fail simultaneously on at most a `2^{-k}` fraction of the randomness space. **`IndependentIntersectionBound` (entry 267) is discharged in the product model.** Composed with the proved per-clause `1/2` (268) and boosting `bad(boost) ⊆ ⋂ⱼ bad j` (267), the single-gate degree-`k(p-1)`, error-`2^{-k}` approximation is assembled; only the structural single-gate → circuit → observer lifting (`PolynomialMethodApproximation` glue) remains. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Independence` |
| **Per-clause agreement over F₂ — discharging `SingleSubsetAgreement` by the parity-toggle involution (PROVED)** | Discharges entry-267's per-clause `≥ 1/2` socket over F₂ — and there it is *exactly* `1/2`. **`parF2 x S := ∑_{i∈S} xᵢ ∈ F₂`** (the Fermat clause sum at p=2); **`tog j S := S △ {j}`** (toggle membership of `j`). **`par_tog`** (PROVED): `parF2 x (tog j S) = parF2 x S + 1` for `xⱼ = true` — toggling `j ∈ supp x` flips the parity (via `Finset.add_sum_erase`/`Finset.sum_insert` + a `decide`d F₂ identity). **`tog_tog`** / **`tog_injective`** (PROVED): `tog j` is an involution (`Finset.insert_erase`/`erase_insert`), hence injective. **`singleSubsetAgreement_two`** (PROVED): for any `x` with a `true` coordinate, `SingleSubsetAgreement 2 n x` — the toggle bijects `{S : par = 0} ↔ {S : par ≠ 0}` (`Finset.card_image_of_injective` + the `image = filter` set equality), so the two fibers are equinumerous and sum to `2ⁿ` (`Finset.filter_card_add_filter_neg_card_eq_card`), giving `2ⁿ = 2·#{par ≠ 0}` hence `2ⁿ ≤ 2·#{par ≠ 0}`. **The `SingleSubsetAgreement` socket is discharged over F₂.** (Gotcha: a local `haveI : Fact (Nat.Prime 2)` pollutes ZMod 2's instances for `decide`; the global instance suffices, so it's dropped.) Composed with the proved boosting (267), only `IndependentIntersectionBound` remains for the single-gate low-degree approximation. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SingleSubsetF2` |
| **Boosting — driving k clause indicators to error 2^{-k} (the error-reduction core of RS) (PROVED)** | The second rung inside `PolynomialMethodApproximation` (entry 266 was the first). Boosting combines `k` one-sided clause approximators into one polynomial whose error set is the *intersection* of the individual ones. **`boostPoly e x := 1 − ∏ⱼ (1 − eⱼ x)`** — the OR of the approximators (degree `∑ⱼ deg eⱼ = k·(p-1)` for clause indicators). **`boostPoly_eq_one`** (PROVED): some `eⱼ x = 1` ⇒ `boostPoly = 1` (the `j`-th factor vanishes). **`boostPoly_eq_zero`** (PROVED): all `eⱼ x = 0` ⇒ `boostPoly = 0`. **`boost_correct`** (PROVED): `boostPoly e x = tgt x` under the good event (Boolean `tgt`; `tgt x = 0` ⇒ one-sidedness forces all `eⱼ = 0`; `tgt x = 1` ⇒ some `eⱼ` fires). **`boost_correct_off_iInter`** (PROVED): the error-reduction core — `boostPoly e` agrees with `tgt` on every `x` outside the **intersection** of the error sets (`∃ j, x ∉ bad j`), i.e. **`bad(boost) ⊆ ⋂ⱼ bad j`**. Probabilistic sockets (named): **`SingleSubsetAgreement`** (per-clause `≥ 1/2`: `#{S : ∑_{i∈S} xᵢ ≠ 0} ≥ 2ⁿ/2`, provable over F₂ by the parity-toggle involution) and **`IndependentIntersectionBound`** (`|⋂ⱼ bad j| ≤ ∏ⱼ |bad j| / 2^{n(k-1)}`, independence). Composed with the proved boosting they give `|bad(boost)| ≤ 2ⁿ·2^{-k}` — degree `k(p-1)`, error `2^{-k}`, the single-gate target of `RandomizedLowDegreeApproximation` (266). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Boosting` |
| **Polynomial approximation of a single AND gate — the base case of Razborov–Smolensky (PROVED)** | The first rung of the `PolynomialMethodApproximation` socket: the per-gate polynomial method. Formalizes the genuinely-provable algebraic substrate and isolates the probabilistic boosting as the one open ingredient. **`fermat_indicator`** (PROVED): over F_p (`p` prime), `y^(p-1) = if y = 0 then 0 else 1` (Fermat's little theorem, `ZMod.pow_card_sub_one_eq_one` + `zero_pow`) — the degree-`p-1` engine the whole method is built on. **`andExact`** (PROVED): `∏ᵢ (if xᵢ then 1 else 0) = if (∀i, xᵢ) then 1 else 0` — the exact AND monomial (degree `n`). **`orExact`** (PROVED): `1 − ∏ᵢ (1 − [xᵢ]) = if (∃i, xᵢ) then 1 else 0` — the exact OR (De Morgan, degree `n`). **`clauseIndicator`** (PROVED): `1 − (∑_{i∈S} [xᵢ])^(p-1) = if (∑_{i∈S} [xᵢ]) = 0 then 1 else 0` — the degree-`p-1` Fermat clause indicator, **fan-in-free** (degree independent of `|S|`), the RS building block. **`andIndicator_eq_mlMon`** / **`andIndicator_mem_lowDegree`** (PROVED): the AND indicator *is* the monomial `mlMon univ`, hence ∈ `lowDegreeSubmodule n n` (entry 265) — the exact degree-`n` representation lands in the framework's degree-`≤n` space. **`RandomizedLowDegreeApproximation F n D k`** (named socket): a degree-`≤D` polynomial agreeing with `AND_n` except on `≤k` inputs; **`and_exact_is_zeroError_approximation`** (PROVED): non-vacuous at the exact endpoint `D=n, k=0` (q = mlMon univ, bad = ∅). The open content — achieving `D ≪ n` with `k ≤ ε·2ⁿ` via random restriction + boosting of the clause indicators — is the single-gate base case of `PolynomialMethodApproximation`, the open RS core (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0AndGateApprox` |
| **The multilinear monomial basis over F — discharging the LowDegreeDimensionIdentity socket (PROVED)** | Builds the multilinear basis over an arbitrary field F (the F-analogue of the in-arc `anf_involutive`/F₂), discharging entry-264's *secondary* socket completely. **`mlMon S x := ∏_{i∈S} (xᵢ : F)`** over the Boolean cube (`true↦1`, `false↦0`), degree `|S|`. **`mlMon_eval`** (PROVED): the triangular evaluation `χ_S(e_T) = [S ⊆ T]` (`e_T(i)=[i∈T]`) — the unitriangular subset/zeta matrix. **`mlMon_linearIndependent`** (PROVED): the 2ⁿ monomials are linearly independent over F — *a genuine general-field generalisation of `anf_involutive`* — via `Fintype.linearIndependent_iff` + the triangular eval (`∑_{S⊆T} c_S = 0 ∀T`) + Möbius/strong induction on the subset lattice (`Finset.strongInductionOn` + `Finset.add_sum_erase`). **`card_subtype_card_le`** (PROVED): `#{S : S.card ≤ D} = lowDegreeDim n D` (partition subsets by cardinality via `Finset.biUnion`/`Finset.card_powersetCard`). **`lowDegreeSubmodule n D := span {χ_S : |S| ≤ D}`** and **`lowDegreeSubmodule_finrank`** (PROVED): `finrank = lowDegreeDim n D` (`finrank_span_eq_card` on the independent sub-family + `card_subtype_card_le`). **`lowDegreeDimensionIdentity_discharged`** (PROVED): therefore `LowDegreeDimensionIdentity (lowDegreeSubmodule n D) n D` holds — **the secondary socket is gone**. **`patternRichCrossFieldLowerBound_no_dimSocket`** (PROVED): for Boolean gate families, instantiating entry-264's reduction at `W := lowDegreeSubmodule n D` eliminates the dimension socket — the open target now rests on the **single** remaining socket `PolynomialMethodApproximation` (the genuine open RS core). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0MultilinearBasis` |
| **WORKSTREAM B — composite candidates collapse onto the single open socket (317, PROVED)** | The honest unification of `NFRAME_TWO_ROUTES.md` §4 candidates (1 char-0, 2 CRT, 4 approximate, 5 staged): NONE is an independent escape. **`exists_nontrivial_field_two_three_neZero`** (PROVED): ∃ nontrivial field with `2≠0 ∧ 3≠0` (witness ℚ) — the native obstruction (280/300/312) REQUIRES `2=0 ∧ 3=0`, so it is SILENT on the non-native candidates. **`native_obstruction_hypothesis_fails`** (PROVED): `2≠0 ⇒ ¬(2=0 ∧ 3=0)`. **`finrank_cube_fun`** (PROVED): `finrank F ((Fin n→Bool)→F) = 2ⁿ` (any field; `Module.finrank_pi`+`Fintype.card_fun`). **`lowDegreeSubmodule_ne_top`** (PROVED): for `D<n` the degree-≤D submodule is PROPER over ANY field (`finrank = lowDegreeDim n D < 2ⁿ`; `lowDegreeSubmodule_finrank`+`lowDegreeDim_lt_two_pow`+`finrank_top`) — field-INDEPENDENT. **`exists_high_degree_over_any_field`** (PROVED): hence ∃ a function of true degree >D over any field. **`charZero_no_low_degree`** (PROVED): the ℚ instance — candidate 1 (char-0) gains NOTHING dimensionally. ⇒ the choice of ambient field/structure is NOT where a separation can come from; every candidate reduces over its field to the SAME single open socket `PolynomialMethodApproximation`. The composite barrier is genuinely ONE open problem. Does NOT prove that socket. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CompositeCandidateUnification` |
| **WORKSTREAM B — why RS does not extend to composite: the foreign-gate obstruction (318, PROVED)** | Decomposes the open socket `PolynomialMethodApproximation` to expose WHY it stalls at composite (the natural-proofs-flavored wall). The polynomial method runs over a SINGLE field K. **`foreign_mod_no_lowDegree_approx`** (PROVED): for distinct primes `q∤p`, over `K=F_{p^{q-1}}` (char p), there is NO family of degree-≤Δ polys tightly agreeing (`4q·|complement|≤2ⁿ`) with the q residue indicators `modIndicator K q j` of MOD_q within the window `16Δ²<2m+3` — the proof-technique form of the foreign gate's hardness, repackaging the proved `Layer4.qary_full_contradiction` (rebuild ζ via `exists_primitiveRoot_galoisField`). **`composite_six_has_foreign_prime`** (PROVED): for any prime p, `¬2∣p ∨ ¬3∣p` (the 2≠3 root — a field is native for ≤1 of the two MOD components; `Nat.prime_dvd_prime_iff_eq`+omega). **`acc6_foreign_prime_exists`** (PROVED): hence over any single field ∃ foreign prime q∈{2,3}, q∣6, ¬q∣p — a MOD_q gate of AC⁰[6] foreign to K, whose RS approximation is impossible by `foreign_mod_no_lowDegree_approx`. ⇒ the single open socket cannot be discharged for composite the way it is for a single prime; the technique STALLS exactly there. NOT a lower bound against AC⁰[6] (≠ MOD₅∉AC⁰[6], the genuine open problem), NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ForeignGateObstruction` |
| **Entry 319 — the COUNTING OBSERVER realizes the Williams route (PROVED bridge)** | The precision the composite anatomy was missing: the composite barrier (280–318) blocks the NATIVE POLYNOMIAL branch, but Williams' route lives on the COUNTING-OBSERVER branch (char-0 integer counts → CRT residue readout → fast-SAT <2ⁿ → lazy hierarchy). This makes that branch explicit N-Frame data and proves it = the Williams route. FOUR ingredients, each grounded: **`CharacteristicZeroCountingObserver`** (`symEval g h x = h (gateCount g x)`; integer count `∑ⱼ[g j x] : ℕ` mediates every top — no characteristic/no p=0, NOT subject to native obstruction; holds by `rfl`); **`CRTResidueReadout`** (`Satisfiable(symEval g (modIndicator M)) ↔ ∃ c∈image(gateCount g), c%M=0`, every M — residue/quotient readout of an integer statistic, not a native polynomial; holds by `fastSat_decides_every_modulus`/291); **`FastSATCompression`** (`∀ n C, HasExactSymAndForm C`, <2ⁿ count cells — the Williams speedup object; `= WilliamsFastSatRoute` defeq); **`LazyHierarchyContradiction`** (complement-safe lazy diagonal `D∉range enum`; holds by `lazy_diag_not_mem_range`/294). **`countingObserver_to_williamsRoute`** (PROVED): the four ⇒ `WilliamsFastSatRoute`. **`nframe_counting_branch_eq_williams`** (PROVED): `NFrameCountingBranch ↔ WilliamsFastSatRoute` — the counting branch IS the Williams route (1/2/4 unconditional, 3 = the route). **`nframe_counting_branch_derives_separation`** (PROVED conditional): the full chain — branch → speedup → collapse → contradicts hierarchy ⇒ `¬(NEXP⊆ACC⁰)`, via `nframe_fastSat_to_timeHierarchy`. Removes the last "Williams special case" ambiguity: a single bridge theorem. The integer count carries every characteristic ⇒ never characteristic-blocked (unlike the polynomial branch). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CountingObserverWilliams` |
| **Entry 320 — counting-branch separation PACKAGED, all residues explicit (PROVED)** | Consolidates the 319 chain into ONE clean theorem with the entire residue surface as a single inspectable hypothesis. **`WilliamsClassicalResidues ACC0 f g speedup`** (structure, 3 named fields = the entire non-N-Frame input): `uniformRealization : NFrameWilliamsRoute → speedup` (the <2ⁿ fast-SAT ⇒ uniform speedup; 304–311 primitive TM ops); `easyWitnessNWCollapse : speedup → NEXP⊆ACC0 → NTIME f⊆NTIME g` (IKW+Karp-Lipton+NW-derand; 307/313/314/315/316); `nondetTimeHierarchy : ¬(NTIME f⊆NTIME g)` (diag core proved complement-free, 294). **`nframe_counting_branch_to_williams_separation`** (PROVED): `WilliamsClassicalResidues … → NFrameCountingBranch → ¬(NEXP⊆ACC0)` — the whole Williams/N-Frame route in one theorem, residues fully exposed (via 319's `nframe_counting_branch_derives_separation`). **`williams_separation_from_residues`** (PROVED): same from any `WilliamsFastSatRoute` (route ⇒ separation form). Does NOT prove the residues — names them (each a proven classical theorem, Williams 2011, being formalized). The Williams route is now as clean as possible without new lower bounds. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CountingBranchSeparation` |
| **Entry 321 — `bridgeYao` discharged in the NW reconstruction assembly (PROVED)** | Discharges the UPSTREAM of entry 316's two model-dependent bridges. 316's `bridgeYao : YaoNextBitPredictor f m ε → ComputesF` IS a `YaoCircuitEfficiency` socket; entry 194 (`…NisanWigdersonYaoCircuit`) built the explicit predictor circuit `predictor D gidx := (x gidx)⊕¬D` over a gate-level `Circ` syntax and PROVED `yaoCircuitEfficiency_discharge` (`SmallPredictor D gidx`: size ≤ size D+3, guess-and-correct semantics). **`bridgeYao_discharged`** (PROVED): `YaoNextBitPredictor f m ε → SmallPredictor D gidx` (= the discharge at `ComputesF := SmallPredictor D gidx`). **`nwReconstruction_bridgeYao_discharged`** (PROVED): 316's `nwReconstruction_assembled` with `bridgeYao` supplied — `NWReconstruction (GlobalAdvantage f m ε) (HasCircuitOfSize fbool s)` from only `bridgeRecon` + design. **`hardFn_to_prgExists_bridgeYao_discharged`** (PROVED): 316's `HardFor fbool s → ¬GlobalAdvantage` (= `HardFunction→PRGExists`) with `bridgeYao` gone — only `bridgeRecon` + irreducible hardness + proved design remain. HONEST CAVEAT: `SmallPredictor` is a gate-count(+3)+semantics bridge, NOT a formalisation of `ACC⁰` membership (depth/gate-type) of `D`. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0NWReconstructionBridgeYao` |
| **Entry 322 — `bridgeRecon` CONSTRUCTED: the reconstructed circuit built syntactically (PROVED)** | The LAST model-dependent bridge of entry 316. Unlike `bridgeYao`, it can't be supplied unconditionally (its output `HasCircuitOfSize fbool s` would directly contradict `HardFor fbool s = ¬HasCircuitOfSize`) — it's load-bearing. So it's BUILT: the reconstructed circuit is `csubst tables P` (Yao predictor `P : Circ Nc` with the hardwired block tables substituted into its inputs; `Hardness.Circ = YaoCircuit.Circ`, same inductive). **`csubst`** + **`subst_eval`** (PROVED): substitution preserves semantics, `(csubst g P).eval x = P.eval (fun j => (g j).eval x)`. **`Circ.size_pos`** (PROVED). **`subst_size_le`** (PROVED): `(csubst g P).size ≤ P.size·M` when every sub-circuit has size ≤M (M≥1) — induction + nlinarith. **`reconstructed_computes`** (PROVED): `csubst tables P` computes `fbool` from composition correctness. **`reconstructed_hasCircuit`** (PROVED): `HasCircuitOfSize fbool (P.size·M)` — the constructive core (the small circuit for f EXISTS, computes f, size ≤ P.size·M). **`bridgeRecon_constructed`** (PROVED): the bridge as a concrete build. Remaining genuine inputs: the table circuits (size ≤M≈2^k, the design) + composition correctness (entry 196). CAVEAT (as 321): gate-count+semantics, NOT ACC⁰ membership. (Build bug found+fixed: `subst` clashes with the Lean tactic name AND `open Circ` failed since the opened type-name isn't a namespace → constructors out of scope → csubst compiled to sorry; fixed by renaming `csubst` + fully-qualified `Circ.var` patterns.) Both NW bridges now built; `HardFunction→PRGExists` rests on irreducible hardness + design. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0NWReconstructionBridgeRecon` |
| **Entry 323 — ACC⁰ depth/gate-type FAITHFULNESS for the circuit bridges (PROVED)** | Removes the 321/322 caveat: equips `Circ` (YaoCircuit.Circ) with `depth` + gate-type accounting, proves the bridges are ACC⁰[2]-faithful. The basis var/tru/fals/cnot/cand/cor/cxor = {input,const,¬,∧,∨,MOD₂} = exactly ACC⁰[2]; only modular gate is MOD₂ ⇒ NO foreign MOD_q possible (the gate-type concern of 318 can't arise). **`depth`** (var/const↦0, cnot↦1+·, binary↦1+max). **`predictor_depth`** (PROVED): `(predictor D gidx).depth = D.depth + 2` — Yao bridge adds CONSTANT depth (NOT then MOD₂). **`csubst_depth_le`** (PROVED): `(csubst g P).depth ≤ P.depth + dt` when all sub-circuits depth ≤dt — reconstruction depth ADDITIVE (induction+omega over max). **`predictor_preserves_acc0Depth`/`csubst_preserves_acc0Depth`** (PROVED): bounded depth in ⇒ bounded depth out (`InACC0Depth d C := depth C ≤ d`). **`xorFree`** + **`csubst_xorFree`** (PROVED, axiom-FREE): substitution preserves XOR-freeness — introduces no MOD₂ beyond P+tables (gate types genuinely tracked). **`predictor_uses_mod2`** (PROVED): predictor is not XOR-free — records the single MOD₂ the Yao step adds. REMAINING MODEL CHOICE (honest): fan-in is BINARY (formula model); unbounded-fan-in ACC⁰ is a separate larger formalisation. So depth+gate-type faithful, fan-in is the one residual. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CircuitBridgeACC0Faithful` |
| **Entry 324 — the UNBOUNDED-FAN-IN ACC⁰ circuit model (PROVED)** | Closes the fan-in residual of 323: builds the genuine unbounded-fan-in model. **`UCirc n`** (var, const, unot, uand/uor/umod taking a LIST of children — one gate of ANY fan-in; `umod m cs` = MOD_m fires iff #true children ≡0 mod m). **`eval`/`size`/`depth`/`moduli`** (nested-List recursion via `cs.map`; eval `.all`/`.any`/`.count true`; depth `1 + foldr max 0`; moduli = MOD-gate multiset for ACC⁰[m] accounting). NESTED-INDUCTIVE handled: `UCirc.induction` (custom eliminator via size-bounded strong induction — the `induction` tactic refuses nested inductives directly; `@[elab_as_elim]`, list-case IH ranges over `∀ c ∈ cs`). **`uand_vars_depth`** (PROVED): `depth (uand (L.map var)) = 1` for ANY L — UNBOUNDED FAN-IN, CONSTANT DEPTH, the defining property the binary `Circ` model couldn't express (AND of n inputs = 1 gate depth 1, not depth-log n tree). **`usubst`/`usubst_eval`** (PROVED): substitution + semantic preservation (reconstruction wiring, faithful to unbounded fan-in; via custom induction + `List.map_congr_left`). **`usubst_depth_le`** (PROVED): additive depth. **`IsACC0m`/`umod2_isACC0`** (PROVED): gate-type accounting, every MOD gate modulus m. Helpers: `size_pos`, `foldrMax_le`, `le_foldrMax`. The faithful ACC⁰ model now exists with semantics/depth/size/modulus + wiring; does NOT by itself reprove the NW bridges over UCirc (would re-run 321/322 here). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnboundedFanin` |
| **Entry 325 — the NW bridges PORTED onto the unbounded-fan-in model UCirc (PROVED)** | Ports the Yao predictor (321) and reconstruction (322) onto UCirc, so the bridges are faithful in depth, gate-type, AND unbounded fan-in. **`upredictor D gidx := unot (umod 2 [var gidx, unot D])`** — the Yao predictor as a MOD₂ gadget (XOR(a,b)=¬MOD₂([a,b])). **`upredictor_eval`** (PROVED): computes the guess-and-correct rule `if D then g else ¬g` (cases on the two bits). **`upredictor_depth`** (PROVED): `= D.depth + 3`, constant overhead. **`upredictor_isACC0`** (PROVED): ACC⁰[2] (moduli = 2::D.moduli) if D is. **`UComputes`/`HasUCircuitOfDepth`/`HasUCircuitOfSize`**. **`usubst_size_le`** (PROVED): `size (usubst g P) ≤ size P · M` (custom induction + List.sum_le_sum + List.sum_map_mul_right + nlinarith). **`ureconstructed_computes`** (PROVED): usubst tables P computes f (via usubst_eval/324). **`ureconstructed_hasDepth`** (PROVED): the reconstruction is bounded-depth `≤ P.depth + dt` (uses 324's usubst_depth_le) — the depth-faithful, unbounded-fan-in reconstruction. **`ureconstructed_hasSize`** (PROVED): bounded-size `≤ size P · M`. Both NW bridges now ACC⁰-faithful in ALL THREE dimensions (depth/gate-type/fan-in). Still rests on irreducible hardness + design + composition correctness. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnboundedBridges` |
| **Entry 326 — nondeterministic time-hierarchy residue, INTERNAL structure (PROVED)** | Opens the `ConcreteHierarchy f g := ¬(NTIME f ⊆ NTIME g)` residue of entry 320. **`langOf M g := fun x => acceptsWithin M x (g x.length)`** (the language of a g-clocked machine). **`mem_NTIME_iff`** (PROVED): `L ∈ NTIME g ↔ ∃ M, L = langOf M g` — the NTIME-class characterisation (funext+propext). **`not_mem_NTIME_of_no_machine`** (PROVED): `(∀ M, D ≠ langOf M g) → D ∉ NTIME g`. **`concreteHierarchy_of_diagonal`** (PROVED): `D ∈ NTIME f` + D differs from every g-clocked machine ⟹ `ConcreteHierarchy f g` — the diagonalisation ASSEMBLY, reducing the hierarchy socket to placement (D∈NTIME f, the clocked simulation of 296-311) + diagonalisation (the proved complement-safe lazy diagonal of 294). **`lazy_diag_escape`** (re-export of 294's `lazy_diag_not_mem_range`): the proved diagonalisation mechanism, valid WITHOUT closure under complement (NTIME lacks it). HONEST: does NOT discharge placement — abstract `NTM` has arbitrary `Config : Type` so isn't enumerable at this abstraction. Decomposes the hierarchy socket into proved glue + 2 precise machine residues. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0NondetHierarchyInternals` |
| **Entry 327 — concrete nondeterministic time hierarchy, complement-safe, on ONE residual (PROVED)** | Discharges the ENUMERABILITY piece 326 couldn't (abstract NTM has arbitrary Config); the concrete model has it. CORRECTNESS POINT: the existing `ACC0UniversalNTM.cTime_hierarchy` uses the CANTOR FLIP diagonal (`diag_not_mem_range`), UNSOUND for nondeterministic (flip needs complementing an NTM, which NTIME isn't closed under, so its `diag_in_big` socket is unachievable); the honest version uses the COMPLEMENT-SAFE LAZY diagonal (302). **`place_of_decider`** (PROVED): `D ∈ cNTIME f` from a routing TMachine deciding D within f. **`concrete_lazy_hierarchy`** (PROVED): `D ∉ range (enum g)` [complement-safe escape] + `D ∈ cNTIME f` ⟹ `¬(cNTIME f ⊆ cNTIME g)`, via the PROVED `enum_covers` (`cNTIME g ⊆ range (enum g)`, TMachine ≃ ℕ) — no flip. **`concrete_lazy_hierarchy_of_decider`** (PROVED): the hierarchy from (1) complement-safe escape [302 mechanism] + (2) a routing decider TMachine. **`cNTIME_enumerable`** (re-export of proved enum_covers). Lands the concrete nondet hierarchy on the LONE residual: the ROUTING DECIDER — one low-level TMachine running block-layout → clocked universal sim (296-298) → one boundary complement (299), within f. NOT built/faked (substantial transition-table engineering). Enumerability/diagonalisation/clocking/sim/complement all proved. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ConcreteLazyHierarchy` |
| **Entry 328 — the lazy-diagonal DECISION PROCEDURE, built+verified from proved primitives (PROVED)** | Builds the function the routing decider (327) must realise, grounding each case in a proved primitive. **`enumAccept g inp k m := decAccept (machineEquiv.symm k) (inp m) (g |inp m|)`** (the g-clocked decidable enumeration; noncomputable only via the classical machineEquiv, each value = finite search decAccept/299). **`lazyDiagDecide g inp := lazyDiagLang (enumAccept g inp)`** (total Bool; copy even, complement odd boundary; block layout = n%2,n/2 trivial). **`lazyDiagDecide_copy`** (PROVED): even n → accepts ↔ acceptsWithin (machine n/2) (inp(n+1)) (g..) — a nondet ACCEPTANCE (universal sim 296-298, NTIME), via acceptsWithin_iff_decAccept/299. **`lazyDiagDecide_boundary`** (PROVED): odd n → accepts ↔ ¬acceptsWithin (machine n/2)(inp(n-1))(g..) — the decidable bounded COMPLEMENT (299, the one complement lazy diag affords; cases on decAccept + simp). **`lazyDiagDecide_escapes`** (PROVED): escapes the enumeration (302) — the correct diagonal. ⇒ the decision procedure is BUILT+VERIFIED; remaining for the 327 routing TMachine = pure timing-aware compilation (realise this function in one transition table within f: copy via clocked universal sim, boundary via bounded search, routed by n%2/n/2). NOT built/faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0LazyDiagDecider` |
| **Entry 329 — routing-machine assembly: sub-table monotonicity (PROVED)** | The first verified brick of the routing-TMachine build (327/328's residual). The mechanism for assembling the routing table from phase sub-machines: a concrete machine's computation is preserved when its transition table is embedded in a larger one. **`concreteStep_mono`** (PROVED): `M⊆M' → concreteStep M c d → concreteStep M' c d` (concreteStep quantifies ∃t∈M; t∈M⊆M'). **`reachIn_mono`** (PROVED): `M⊆M' →` every k-step run of toNTM M is one of toNTM M' (induction on k). **`acceptsWithin_mono_machine`** (PROVED): bounded acceptance lifts (same init (0,0,x), accept state=1). **`acceptsWithin_union_left/right`** (PROVED): acceptance by either part of `M₁++M₂` lifts (List.subset_append_left/right) — the two-phase union primitive. Axioms just [propext]. ⇒ the routing table is built by union-ing phase sub-tables (decode, universal sim 296-298, bounded complement 299) on disjoint states, each phase's PROVED run surviving by monotonicity. REMAINING: control-flow WIRING (state offsets, decode→dispatch→sim/complement redirection) + the f-TIMING — the genuine low-level engineering, NOT built/faked. First verified brick. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0RoutingMachineComposition` |
| **Entry 330 — routing-machine wiring: state-offset preserves computation (PROVED)** | Second verified brick: to combine phases into one routing table they need DISJOINT state ranges; this proves offsetting a machine's states by `s` preserves its whole computation. **`shiftConfig s (st,hd,tp) := (st+s, hd, tp)`; `shiftTrans s` adds s to read/write states; `shiftMachine s M := M.map (shiftTrans s)`** (must use TMTrans/CConfig types — Move ≠ ℕ definitionally). **`readSym_shiftConfig`** (PROVED, rfl): read ignores state. **`applyTrans_shiftTrans`** (PROVED, rfl via structure-eta): `applyTrans (shiftConfig s c)(shiftTrans s t) = shiftConfig s (applyTrans c t)` — step commutes with offset (readSym/moveHead/writeAt touch only head/tape). **`concreteStep_shift`** (PROVED): step survives offset (witness shiftTrans s t, congrArg Prod.fst/snd h1). **`reachIn_shift`** (PROVED): whole k-step run survives offset (induction on k). **`reachIn_in_combined`** (PROVED): M₂'s run, offset by s, holds in `M₁ ++ shiftMachine s M₂` (reachIn_shift + 329 monotonicity via subset_append_right) — phase 2 on disjoint states keeps its run in the routing table. ⇒ with 329, assembles PARALLEL sub-tables on disjoint states. REMAINING: control-flow HANDOFF (redirect phase-1 exit → phase-2 shifted start, to SEQUENCE) + f-TIMING + instantiate phases with universal sim(296-298)/complement(299) — the remaining low-level engineering, NOT faked. Second brick. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0RoutingMachineWiring` |
| **Entry 331 — routing-machine sequencing: the control-flow handoff (PROVED)** | Third verified brick: the control flow that SEQUENCES the parallel phases (329/330 only placed them side by side). **`handoffRules qexit s := [((qexit,b),(s,b,2:Move))]_{b}`** (at exit state qexit, jump to state s = phase-2 shifted start, write-back, stay; Move=Fin 3, 2=stay). **`postHandoff qexit s c := applyTrans c ((qexit,readSym c),(s,readSym c,2))`** (state s, head unchanged, read written back). **`seqMachine M₁ M₂ qexit s := M₁ ++ handoffRules qexit s ++ shiftMachine s M₂`**. **`handoff_step`** (PROVED): at any state-qexit config, handoffRules steps to postHandoff (cases readSym + simp; t.1 eq via rw hq). **`reachIn_seq`** (PROVED): phase-1 run (a steps of M₁ to qexit) + handoff (1 step) + phase-2 run (b steps of shiftMachine s M₂ from postHandoff) ⟹ run of seqMachine of length a+1+b — via subset facts (subset_append_left/right + .trans) + 329 reachIn_mono/concreteStep_mono + reachIn_add (run concatenation). ⇒ the routing machine's CONTROL STRUCTURE fully verified (329/330 parallel disjoint-state assembly + 331 exit→entry sequencing). Remaining: instantiate phases = decode/dispatch + universal sim(296-298)/complement(299) as concrete tables + f-TIMING. NOT faked. Third brick. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0RoutingMachineSequencing` |
| **Entry 332 — phase instantiation: a concrete sequenced machine that ACCEPTS (PROVED)** | Fourth brick: instantiates the 329/331 control skeleton with CONCRETE tables and proves a real end-to-end accepting run. **`demoRouting qexit s := handoffRules 0 qexit ++ handoffRules qexit s ++ handoffRules s 1`** (three jump tables, `0→qexit→s→1`). **`demoRouting_accepts`** (PROVED, [propext]): `acceptsWithin (toNTM (demoRouting qexit s)) x 3` for every x — runs `(0,0,x)→(qexit,…)→(s,…)→(1,…)`, each step the proved `handoff_step` (331) lifted by `concreteStep_mono` (329), reaching GLOBAL accept state 1 (accept plumbing validated); subsets via subset_append_left/right+.trans, reachIn 3 = nested ⟨c1,step1,c2,step2,c3,step3,rfl⟩, accept by rfl. ⇒ the routing control structure genuinely accepts concrete TMachines. HONEST: phases are minimal jump tables, NOT the real decode/universal-sim/complement; instantiating THOSE needs bridging the universal sim (296-298, built as ABSTRACT NTM not concrete TMachine) into a transition table + complement-as-table + f-timing — the genuine remaining construction, not faked. Fourth verified brick. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0RoutingMachinePhaseDemo` |
| **Entry 333 — abstract-NTM → concrete-TMachine BRIDGE: simulation transfers acceptance (PROVED)** | Fifth brick: the bridge mechanism resolving the gap 332 exposed (universal sim is abstract NTM, routing assembly is concrete TMachine). **`Realizes A M φ cost`** (structure): a concrete TMachine M step-simulates abstract NTM A via config map φ — `step`: each A.step = cost steps of toNTM M (φc→φd); `accept`: A.accept c → (φ c).1=1; `init`: φ(A.init x)=(toNTM M).init x. **`reachIn_realize`** (PROVED): k-step abstract run → k*cost-step concrete run (induction on k + reachIn_add block concatenation; (k+1)*cost=cost+k*cost by ring). **`acceptsWithin_realize`** (PROVED): `acceptsWithin A x t → acceptsWithin (toNTM M) x (t*cost)` — acceptance transferred with ×cost blowup (k*cost ≤ t*cost by gcongr; init via h.init; accept via h.accept). ⇒ reduces the universal-simulation phase to ONE named construction: exhibit `Realizes physU M φ cost` for a concrete universal-TM table M — i.e. BUILD THE UNIVERSAL TURING MACHINE as a transition table (substantial classical construction, NOT faked). With the bridge proved, that table + f-timing is all that remains for the concrete universal-sim phase. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0AbstractConcreteBridge` |
| **Entry 334 — universal-TM-table build, BRICK 1: `Realizes` interface inhabited + verified traversal primitive (PROVED)** | Commits to the universal-TM construction (333's residual) with its first verified bricks. **`realizes_self`** (PROVED, [propext]): `Realizes (toNTM M) M id 1` — every concrete TMachine realizes its own abstract embedding (identity φ, 1 step/step); the 333 bridge interface is INHABITED, non-vacuous. **`moveHead_right`** (PROVED): `moveHead h 1 = h+1` (simp [moveHead]). **`walkRight := [((0,b),(0,b,1))]_b`** (state-0 loop, move right) + **`walkRight_run`** (PROVED): from (0,0,x), k steps → state 0 head k — the universal TM's tape-traversal SCANNER (induction on k, extend by one verified step head k→k+1 via reachIn_add). ⇒ brick 1 of the universal-TM build: interface inhabited + a genuine multi-step universal-TM component verified over the concrete model. REMAINING bricks (substantial classical construction, undertaken as real multi-brick effort, NOT faked): tape encoding of (machine,input), rule-lookup scan, apply-step, simulation loop, accept detection, assemble U with Realizes physU U φ cost + f-timing. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMBuild` |
| **Entry 335 — universal-TM build, BRICK 2: tape encoding of (machine, input) (PROVED)** | The data representation the universal machine reads off its tape. `(TMachine × List Bool)` is Encodable, `List Bool` denumerable. **`tapeEquiv : List Bool ≃ ℕ`** (Denumerable.ofEncodableOfInfinite). **`encodeTape M x := tapeEquiv.symm (Encodable.encode (M,x))`** / **`decodeTape t := Encodable.decode (tapeEquiv t)`**. **`decodeTape_encodeTape`** (PROVED): `decodeTape (encodeTape M x) = some (M,x)` — round-trip, machine+input recoverable from tape (Equiv.apply_symm_apply + Encodable.encodek). **`encodeTape_injective`** (PROVED): distinct pairs → distinct tapes. ⇒ brick 2: verified injective round-trip tape encoding = the universal machine's input format, pair provably recoverable. HONEST: this is the DATA foundation (recoverable), NOT yet a bit-by-bit SCANNABLE format with TM-level lookup — that's brick 3 (rule-lookup scan via brick-1 traversal), then apply-step/loop/accept/assemble U + f-timing. Not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMEncoding` |
| **Entry 336 — universal-TM build, BRICK 3: rule-lookup, sound + complete (PROVED)** | The heart of the simulation cycle: find the matching transition for the current (state,symbol) and apply it. **`lookup M c := M.find? (fun r => decide (r.1 = (c.1, readSym c)))`** / **`applyLookup M c := (lookup M c).map (applyTrans c)`**. **`lookup_sound`** (PROVED, [propext]): `lookup M c = some t → concreteStep M c (applyTrans c t)` (List.find?_some + mem_of_find?_eq_some + of_decide_eq_true; KEY: `have hp := List.find?_some h` with NO type ascription — ascription mis-infers the predicate). **`applyLookup_sound`** (PROVED): `applyLookup M c = some d → concreteStep M c d`. **`lookup_complete`** (PROVED): `concreteStep M c d → ∃ t, lookup M c = some t` (cases on lookup; none case → List.find?_eq_none + rw ht1 + simp ⊥). ⇒ the rule-lookup LOGIC is sound+complete w.r.t. concreteStep — exactly what the simulation cycle's lookup computes. HONEST: this is the lookup LOGIC (= List.find? on rules); the bit-level SCAN realising find? over the encoded table (via brick-1 walkRight traversal) + apply-step/loop/accept/assemble U + f-timing remain, not faked. Brick 3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMLookup` |
| **Entry 337 — universal-TM build, BRICK 4: the simulation loop, sound (PROVED)** | Iterates brick-3's lookup-and-apply into the deterministic simulation loop, proved a valid run. **`simIter M : ℕ → CConfig → Option CConfig`** (0↦some c; k+1↦(applyLookup M c).bind (simIter M k)). **`simIter_sound`** (PROVED): `simIter M k c = some d → reachIn (toNTM M) k c d` — the deterministic lookup-and-apply iteration IS a valid k-step nondeterministic run (induction on k; succ via Option.bind_eq_some_iff + applyLookup_sound/336 + ih). **`simIter_acceptsWithin`** (PROVED): k-step sim from (0,0,x) reaching accept (d.1=1) ⇒ acceptsWithin (toNTM M) x k. ⇒ the verified LOGICAL ENGINE of the universal simulation cycle (traverse/lookup/apply/repeat/accept), all at the config/rule level. HONEST: remaining = BIT-LEVEL realisation of simIter as one concrete TMachine U over the encoded tape + Realizes physU U φ cost + f-timing, not faked. Brick 4. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMLoop` |
| **Entry 338 — universal-TM build, BRICK 5: scannable encoding layer (PROVED)** | Replaces brick-2's OPAQUE Encodable tape with a concrete SCANNABLE bit-grammar (dumb unary+separators per user plan: nat n ↦ n trues + false). **`encodeNatBits`/`decodeNatBits`** + **`decodeNatBits_encodeNatBits`** (PROVED): `decodeNatBits (encodeNatBits n ++ rest) = (n, rest)` (decodeNatBits structural recursion counting trues; induction + encodeNatBits_succ + Prod.map/id_eq). **`encodeTransBits`/`decodeTransBits`** + **`decodeTransBits_encodeTransBits`** (PROVED): scan one transition (5 fields rs,rsym,ws,wsym,mv), leave rest; Fin 3 via dif_pos t.2.2.2.isLt. **`encodeMachineBits`/`decodeMachineBits`** + **`decodeMachineBits_encodeMachineBits`** (PROVED): length-prefixed transition list, scan whole table leave rest; decodeTransListBits + induction on M (simp with roundtrip+ih reducing matches). Each `decode (encode x ++ rest) = some (x, rest)` = the CURSOR lemma (consume exactly x's encoding, stop, leave rest) — scanability+correctness, efficiency irrelevant. ⇒ the bit-grammar a left-to-right scanner processes, verified. NEXT: 339 bit-level lookup (rule scan-match) implements lookup; 340 bit-level apply implements applyTrans; 341 assemble U + Realizes physU. Not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScannable` |
| **Entry 339 — universal-TM build, BRICK 6: bit-level rule lookup (PROVED)** | The rule lookup over the brick-338 scannable encoding, proved equal to the abstract lookup. **`bitLookup bits c := (decodeMachineBits bits).bind (fun p => lookup p.1 c)`** (scan the encoded table via 338's decode, match via 336's lookup). **`bitLookup_encodeMachineBits`** (PROVED): `bitLookup (encodeMachineBits M) c = lookup M c` (via decodeMachineBits round-trip with rest=[], ← List.append_nil + rfl). **`bitLookup_sound`** (PROVED): `bitLookup (encodeMachineBits M) c = some t → concreteStep M c (applyTrans c t)` (= 336 lookup_sound after the eq). **`bitLookup_complete`** (PROVED): concreteStep → ∃t bitLookup = some t. ⇒ the lookup step now operates on the ENCODED TAPE FORMAT, proved equal to the abstract lookup, sound+complete w.r.t. concreteStep. HONEST: the "scan" is decodeMachineBits (left-to-right bit consumption, 338); realising it as actual TMachine transitions (walkRight over the bits) is folded into the 341 assembly. Next: 340 bit-apply, 341 assemble U + Realizes physU. Not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMBitLookup` |
| **Entry 340 — universal-TM build, BRICK 7: bit-level apply (PROVED)** | The config-update step over the encoding, proved equal to abstract applyTrans. Config encoding via brick-338 grammar: **`encodeConfig c := encodeNatBits state ++ encodeNatBits head ++ encodeBoolList tape`** (encodeBoolList = length-prefixed bits). **`decodeBoolList_encodeBoolList`** (PROVED): bit-block round-trip (decodeNatBits + take/drop, simp). **`decodeConfig_encodeConfig`** (PROVED): `decodeConfig (encodeConfig c ++ rest) = (c, rest)` (scan a (state,head,tape) config, leave rest; simp with nat+boollist roundtrips, Prod eta). **`bitApply bits t := encodeConfig (applyTrans (decodeConfig bits).1 t)`** + **`bitApply_encodeConfig`** (PROVED): `bitApply (encodeConfig c) t = encodeConfig (applyTrans c t)` — bit-level apply IS the abstract applyTrans up to encoding (←append_nil + decodeConfig roundtrip). ⇒ with brick 6 (lookup), both halves of one simulation step at the encoding level: scan-match the rule, then update the encoded config. HONEST: remaining = assemble into ONE concrete TMachine U realising decode/scan/update as tape moves + Realizes physU U φ cost + f-timing (341), not faked. Brick 7. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMBitApply` |
| **Entry 341 — universal-TM build, BRICK 8: the encoded simulation run, correct (PROVED)** | Composes bricks 6+7 into one encoded step + iterates it; the full simulation runs correctly on bit-tapes. **`encodedStep Mbits cbits := (bitLookup Mbits (decodeConfig cbits).1).map (bitApply cbits)`**. **`decodeConfig_encodeConfig_fst`** (PROVED): (decodeConfig (encodeConfig c)).1 = c. **`encodedStep_correct`** (PROVED): `encodedStep (encodeMachineBits M) (encodeConfig c) = (applyLookup M c).map encodeConfig` (one encoded step = abstract step re-encoded; via decodeConfig_fst + bitLookup_encodeMachineBits/339 + Option.map_map + bitApply_encodeConfig/340). **`encodedRun`** (iterate) + **`encodedRun_correct`** (PROVED): `encodedRun (encodeMachineBits M) k (encodeConfig c) = (simIter M k c).map encodeConfig` — the ENTIRE k-step deterministic simulation runs correctly on encoded bit-tapes (induction on k, cases applyLookup, simpa+ih). ⇒ CULMINATION at the encoding level: the full universal simulation [simIter/337] runs correctly entirely on the scannable bit-encoding [config/lookup/apply/loop all verified equal to the abstract engine]. LONE RESIDUAL: realise encodedStep as transitions of one concrete TMachine U [walkRight-driven scans of bricks 1/5 wired into a transition table] → Realizes physU U φ cost via 333 bridge + f-timing. The function→transition gap, not faked; everything U must compute is now verified. Brick 8. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMEncodedRun` |
| **Entry 342 — universal-TM build, BRICK 9: emission ⇒ full universal simulation (PROVED)** | The final lifting (mirrors the 333 bridge, specialised to the encoded run). **`EmitsEncodedStep U φ cost := ∀ Mbits cbits next, encodedStep Mbits cbits = some next → reachIn (toNTM U) cost (φ Mbits cbits) (φ Mbits next)`** (the per-macro-step obligation: U realises ONE encoded lookup-and-apply in cost transitions, via tape layout φ). **`universalSim_of_emits`** (PROVED): EmitsEncodedStep + encodedStep_correct/341 ⟹ a whole k-step simIter simulation is a k*cost-step run of U [reachIn (toNTM U) (k*cost) (φ.. (encodeConfig c0)) (φ.. (encodeConfig cf))]; induction on k, each simIter step = encodedStep [341] = cost U-steps [emission], composed by reachIn_add; (k+1)*cost=cost+k*cost by ring. ⇒ the WHOLE universal-TM construction is reduced to ONE irreducible obligation: build concrete U + φ + prove EmitsEncodedStep [U's transitions realise one encoded step over the tape, walkRight-driven scans of bricks 1/5]. That single transition-table obligation is the genuine remaining low-level construction, NOT faked; everything it feeds [full run, acceptance via brick 4 + 333 bridge] is proved. Brick 9. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMEmit` |
| **Entry 343 — universal-TM build, BRICK 10: emission ⇒ the universal machine DECIDES (PROVED)** | Closes the conditional chain: emission + accept-layout ⟹ U decides what the simulated machine decides. **`AcceptLayout φ := ∀ Mbits c, (φ Mbits (encodeConfig c)).1 = 1 ↔ c.1 = 1`** (U's accept state mirrors the simulated config's — natural condition). **`universalReachesAccept_of_emits`** (PROVED): EmitsEncodedStep + AcceptLayout + simIter M k c0 = some cf + cf.1=1 ⟹ ∃ d, reachIn (toNTM U)(k*cost)(φ.. (encodeConfig c0)) d ∧ d.1=1 [U reaches its accept state when the simulation accepts]; via universalSim_of_emits/342 + hlay.mpr. ⇒ the ENTIRE Williams universal-sim phase is reduced to ONE construction: build concrete U + φ satisfying EmitsEncodedStep + AcceptLayout [a verified universal-TM transition table], with ALL consequences [run 342, correct acceptance 343] proved. That single transition-table construction is the genuine remaining low-level work, NOT faked. Brick 10. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMDecide` |
| **Entry 344 — universal-TM TRANSITION TABLE begins: scanNat (PROVED)** | Starts the actual transition-table construction (the EmitsEncodedStep socket). **`writeAt_getD`** (PROVED): `(writeAt tape p w).getD q false = if q=p then w else tape.getD q false` (a write changes only the written cell; via getElem?_set_self/ne + getElem?_append_left/right + getElem?_replicate). **`writeAt_getD_self`** (PROVED): `(writeAt tape p (tape.getD p false)).getD q false = tape.getD q false` — writing back the read symbol preserves EVERY cell, the non-destructive-scan invariant. **`scanNat := [((0,true),(0,true,1)),((0,false),(1,false,1))]`** (state 0 scan: true→right stay; false separator→state 1 right). **`scanNat_step_true`** (PROVED): at a true cell, (0,j,tp)→(0,j+1,…) head advances staying state 0. **`scanNat_step_false`** (PROVED): at false cell, (0,j,tp)→(1,j+1,…) halt state 1. ⇒ first VERIFIED FRAGMENT of the universal-TM transition table: the tape-preservation foundation + scanNat's per-step behaviour. REMAINING: full scanNat run (head reaches n+1 after one encoded nat, tape-invariant induction on writeAt_getD_self), field/rule scans, apply, EmitsEncodedStep — fragment by verified fragment, not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanNat` |
| **Entry 345 — universal-TM table: the full scanNat RUN (PROVED)** | The verified cursor-advance over one encoded nat. **`encNat_getD_lt`** (PROVED): (encodeNatBits n ++ rest).getD j false = true for j<n (the unary trues; getElem?_append_left + getElem?_replicate_of_lt). **`encNat_getD_eq`** (PROVED): (encodeNatBits n ++ rest).getD n false = false (the separator; getElem?_append_right). **`scanNat_scan`** (PROVED): the tape-INVARIANT — after j≤n steps, state 0, head j, tape contents preserved (induction on j; each step scanNat_step_true/344 + writeAt_getD_self/344 [write-back preserves: true=tp.getD j false] + reachIn_add). **`scanNat_run`** (PROVED): ∃ tp, reachIn (toNTM scanNat) (n+1) (0,0,encodeNatBits n ++ rest) (1,n+1,tp) — scans one encoded nat in n+1 steps, halts state 1 head n+1 (scanNat_scan to (0,n,tp) then scanNat_step_false + reachIn_add). ⇒ the universal scanner's core primitive [advance past one encoded nat] fully verified over the concrete model. NEXT fragments: compose scans to skip the 5 transition fields, scan rule table for a match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanNatRun` |
| **Entry 346 — universal-TM table: the relocatable field scanner `scanNatFrom` (PROVED)** | Generalises `scanNat` (entry 344/345, fixed `0→1` head-`0`) to the reusable primitive for chaining across an encoded transition's five fields (`nat,sym,nat,sym,nat`; entry 338). **`scanNatFrom s s'`** := `[((s,true),(s,true,→)), ((s,false),(s',false,→))]` — entry state `s` scans, exits to `s'` at the separator. **`scanNatFrom_step_true/false`** (PROVED): single-step behaviours at an arbitrary head. **`scanNatFrom_scan`** (PROVED): the relocatable tape-invariant — from `(s,h,tp)`, given cells `h..h+n-1` read `true`, after `j≤n` steps state `s` at head `h+j`, tape preserved (induction on `j`; `scanNatFrom_step_true` + `writeAt_getD_self`/344 + `reachIn_add`; `h+0=h`, `(h+j)+1=h+(j+1)` defeq). **`scanNatFrom_run`** (PROVED): `∃ tp', reachIn (toNTM (scanNatFrom s s')) (n+1) (s,h,tp) (s',h+n+1,tp')` from an ABSTRACT content hypothesis (cells `h..h+n-1`=`true`, cell `h+n`=`false`) — each chained field supplies only its local content, scanner reused verbatim. Compiled first try. ⇒ relocatable field-scan primitive over the concrete model. NEXT: wire disjoint-state copies into one machine scanning a whole `encodeTransBits` layout, then rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanField` |
| **Entry 347 — universal-TM table: rule-list non-interference + sequential composition (PROVED)** | The WIRING LAW for combining transition tables: a `concreteStep M c d` is existential over rules `t∈M`, so enlarging `M` to a superset only ADDS witnesses ⇒ a sub-table's step survives in the union, NO disjointness needed for reachability. **`concreteStep_append_left/right`** (PROVED): a step of `M₁` (resp `M₂`) is a step of `M₁++M₂` (witnessing rule survives via `List.mem_append`). **`reachIn_toNTM_mono`** (PROVED): step monotonicity ⇒ reachability monotonicity — if ∀cd concreteStep M c d→concreteStep M' c d then reachIn (toNTM M) k→reachIn (toNTM M') k (induction on k; zero case = equality defeq, succ threads hstep through the ∃d). **`reachIn_append_left/right`** (PROVED): a k-step sub-table run lifts VERBATIM to the union (instantiate mono with append_left/right). **`reachIn_seq`** (PROVED): sequential composition INSIDE the union — reachIn (toNTM M₁) a c d → reachIn (toNTM M₂) b d e → reachIn (toNTM (M₁++M₂)) (a+b) c e (reachIn_add.mpr + the two lift lemmas). Compiled first try; minimal axioms (reachIn_seq = [propext,Quot.sound], no Classical.choice). DISTINCT from entry-331 ACC0RoutingMachineSequencing.reachIn_seq (that = state-renumbering handoff w/ qexit hyp; this = rule-list union, no disjointness) — complementary, NOT a dup. ⇒ the wiring primitive letting scanNatFrom copies be combined into one machine + run back-to-back. NEXT: thread each field's tape-preservation into the next field's content hyp to scan a whole encodeTransBits in one machine, then rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCompose` |
| **Entry 348 — universal-TM table: preservation-tracking field run + first two-field composition (PROVED)** | Closes the tape-preservation threading gap (scanNatFrom_run/346 hid preservation, but the next field's content hyp is about the post-scan tape). **`scanNatFrom_run_pres`** (PROVED): strengthens scanNatFrom_run to ALSO expose ∀q tp'.getD q false = tp.getD q false (scan is non-destructive: true cells write true, separator writes false; scanNatFrom_scan gives preservation to head h+n, then the false step's write-back preserved via writeAt_getD_self since tp'.getD(h+n)=false). **`scanTwoNats`** (PROVED): FIRST genuine multi-field scan — given two consecutive encoded nats lengths n₁,n₂ at offsets h, h+n₁+1, the UNION machine scanNatFrom 0 1 ++ scanNatFrom 1 2 runs (n₁+1)+(n₂+1) steps from (0,h,tp) to state 2 at head h+n₁+1+n₂+1. Field 2's content hyps ht2'/hf2' discharged on the post-field-1 tape tp1 via hpres1 (rw hpres1 then original ht2/hf2), then reachIn_seq/347 composes field-1 run (0→1) + field-2 run (1→2) in the union. Mid-config (1,h+n₁+1,tp1) matches both runs; final head defeq by +-left-assoc. Compiled first try. ⇒ the threading mechanism the full encodeTransBits traversal is built from. NEXT: one-bit scanner for the interleaved SYM fields (encodeTransBits = nat,sym,nat,sym,nat), then whole-transition scan, rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMFieldCompose` |
| **Entry 349 — universal-TM table: the one-bit scanner `scanBit` (PROVED)** | The missing piece for the SYM fields interleaved in encodeTransBits (nat,sym,nat,sym,nat): scanNatFrom/346 handles nat fields, scanBit handles single symbol bits. **`scanBit s s'`** := `[((s,true),(s',true,→)), ((s,false),(s',false,→))]` — UNLIKE scanNatFrom (loops in s till separator), scanBit is a SINGLE step: consume one cell regardless of value, write the symbol back, exit to s', move right. **`scanBit_step`** (PROVED): single-step uniformly in the read symbol — concreteStep (scanBit s s') (s,j,tp) (s',j+1,writeAt tp j (tp.getD j false)); proof `cases h : tp.getD j false` [the `cases h :` form SUBSTITUTES tp.getD j false→false/true in the goal, so the third applyTrans goal closes by `simp [applyTrans,moveHead]` WITHOUT h — only the readSym goal needs `simp only [readSym,h]`]. **`scanBit_run_pres`** (PROVED): ∃tp', reachIn (toNTM (scanBit s s')) 1 (s,h,tp)(s',h+1,tp') ∧ tape preserved — one-step run [reachIn 1 = ⟨d, step, rfl⟩], preservation via writeAt_getD_self/344. Matches the SHAPE of scanNatFrom_run_pres/348 so nat+bit scans compose under reachIn_seq/347. Compiled clean (dropped 2 unused-simp-arg `h` linter warnings). NEXT: chain scanNatFrom/scanBit runs across whole encodeTransBits in one machine, then rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanBit` |
| **Entry 350 — universal-TM table: the full FIVE-FIELD transition scan `scanTrans` (PROVED)** | First COMPLETE traversal of one encodeTransBits record (layout nat,sym,nat,sym,nat = encodeNatBits rs ++ rsym :: (encodeNatBits ws ++ wsym :: encodeNatBits mv)). **`getD_append_shift`** (PROVED): (pre++X).getD (pre.length+i) false = X.getD i false (getElem?_append_right + Nat.add_sub_cancel_left). **`field_content`** (PROVED): for T = pre++(encodeNatBits n++rest) with pre.length=h, cells h..h+n-1 read true & cell h+n reads false (rw hT,←hlen,getD_append_shift; reuses entry-345 encNat_getD_lt/eq). **`scanTrans`** (PROVED): ∃tp', reachIn (toNTM (scanNatFrom 0 1 ++ scanBit 1 2 ++ scanNatFrom 2 3 ++ scanBit 3 4 ++ scanNatFrom 4 5)) (rs+ws+mv.val+5) (0,0,encodeTransBits t++rest) (5, rs+ws+mv.val+5, tp') — scans the whole transition in |encodeTransBits t| steps, ends state 5 head exactly past it. PROOF: destructure t; `set T`; 3 nesting eqs hT1/hT3/hT5 (simp [encodeTransBits,append_assoc,cons_append]); 5 field runs threaded — field 1 (rs, offset 0) via field_content []; fields 3/5 (ws/mv) via field_content with pre=encodeNatBits rs++[rsym] / +encodeNatBits ws++[wsym], hlen by `simp only [length lemmas]; omega`, content transported to current tape tp2/tp4 via accumulated preservation (pres_t2/pres_t4); fields 2/4 (sym bits) via scanBit_run_pres (no content); chained by reachIn_seq/347 (×4, left-assoc union matches goal machine); final bridge `convert c4 using 1` → count goal (omega) + config-triple goal (rw Prod.mk.injEq ×2; ⟨rfl, omega, rfl⟩). BUGS: convert using 2 wrongly splits sums addend-wise (5=mv.val+1) → use using 1; hlen simp left arith → append omega; dsimp reduces destructured projections for omega. ⇒ first whole-record scan. NEXT: rule-table scan-and-match (locate transition whose (state,read) key matches config — the genuinely new decision step), apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanTrans` |
| **Entry 351 — universal-TM table: the one-bit branch machine `branchBit` (PROVED)** | The FIRST non-advancing machine — everything 344-350 only scanned forward; rule-table matching needs BRANCHING on what's read. **`branchBit s sTrue sFalse`** := `[((s,true),(sTrue,true,stay)), ((s,false),(sFalse,false,stay))]` — read cell, write it back, STAY (move=2=Fin-3 stay, head unchanged), route to sTrue on true / sFalse on false. **`branchBit_step_true/false`** (PROVED): at (s,j,tp) reading true→(sTrue,j,…), false→(sFalse,j,…) — state branches, head unchanged (same refine+simp pattern; move 2 → moveHead else-branch = j, simp [applyTrans,moveHead] closes). **`branchBit_run_true/false`** (PROVED): one-step branch run preserving tape AND head — reading true reaches (sTrue,h,tp'), false reaches (sFalse,h,tp'), tp' agrees everywhere (writeAt_getD_self/344 via hb-rewrite). Compiled first try, no bug/warning. ⇒ the decision atom for scan-and-match. NEXT: assemble an equality test between encoded fields from these branches, then rule-table scan-and-match loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMBranch` |
| **Entry 352 — universal-TM table: the expected-bit checker `checkBit` (PROVED)** | branchBit/351 inspects+stays; to VERIFY A KNOWN BIT PATTERN you read+compare-to-constant+advance+branch. **`checkBit b s sCont sFail`** := `[((s,b),(sCont,b,→)), ((s,!b),(sFail,!b,→))]` — read cell, write back, move right, go to sCont if cell=b else sFail (b a compile-time constant baked into the rule; both keys (s,b)/(s,!b) distinct). **`checkBit_step_match/fail`** (PROVED): reading b advances to (sCont,j+1,…), reading !b to (sFail,j+1,…) (same refine+simp pattern; works for variable b since List.mem_cons + (s,b)=(s, tp.getD j false) via simp [readSym,h]). **`checkBit_run_match/fail`** (PROVED): one-step runs preserving tape — cell=b reaches (sCont,h+1,tp'), cell=!b reaches (sFail,h+1,tp') (writeAt_getD_self/344 via hb-rewrite). Compiled first try. ⇒ chaining checkBit b₁ s₀ s₁ F ++ checkBit b₂ s₁ s₂ F ++ … (failures→common F) verifies a fixed bit pattern, reaching final continue-state iff every bit matched. NEXT: chain into pattern match, then two-region key comparison + rule-table scan-and-match loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCheckBit` |
| **Entry 353 — universal-TM table: the fixed-pattern matcher `matchPattern` (PROVED)** | First COMPOSITE control-flow machine of the matching half — chains checkBit/352 into a whole-pattern verify. **`matchPattern : List Bool → ℕ → ℕ → TMachine`**: `[] ↦ []`, `b::bs ↦ checkBit b s (s+1) F ++ matchPattern bs (s+1) F` — state s+i checks pattern bit i, continue s+i+1 or fail to common F. **`matchPattern_run_match`** (PROVED): if tape from offset h agrees with constant pat (∀ i<pat.length, tp.getD (h+i) false = pat.getD i false), then ∃tp', reachIn (toNTM (matchPattern pat s F)) pat.length (s,h,tp) (s+pat.length, h+pat.length, tp') ∧ tape preserved — runs to SUCCESS state s+pat.length, head past pattern. PROOF: `induction pat generalizing s h tp` (F fixed across recursion). nil: ⟨tp,rfl,_⟩ (reachIn 0, s+0/h+0 defeq). cons b bs: bit-0 hb from hmatch 0 (rw Nat.add_zero, List.getD_cons_zero); checkBit_run_match→tp1; ih content hcont via hpres1 transport + index rw (h+1)+i=h+(i+1) + List.getD_cons_succ; ih→tp2; reachIn_seq/347 composes (1 step + bs.length steps); final `convert comp using 1` (count: List.length_cons+omega; config: List.length_cons+Prod.mk.injEq ×2 + ⟨omega,omega,rfl⟩). Compiled (1 fix: forgot to declare F param). ⇒ chaining checkBit verifies a fixed key pattern, reaching success iff all bits match. NEXT: two-region key comparison (both operands from tape, two-pointer), rule-table scan-and-match loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMMatchPattern` |
| **Entry 354 — universal-TM table: the leftward move `moveLeft` (PROVED)** | The MISSING DIRECTION: all of 344-353 move right (1: scanNatFrom/scanBit/checkBit) or stay (2: branchBit); the two-pointer key comparison needs the head to travel LEFT to return between tape regions. **`moveLeft s s'`** := `[((s,true),(s',true,0)), ((s,false),(s',false,0))]` — read cell, write back, move LEFT (move 0 = h-1 Nat), go to s' uniformly in symbol. **`moveLeft_step`** (PROVED): at (s,j,tp) → (s', j-1, writeAt tp j (tp.getD j false)) uniformly (`cases h : tp.getD j false`, pick rule per branch; move 0 → moveHead if-0-branch = j-1; simp [applyTrans,moveHead], h unused in 3rd goal by cases-substitution). **`moveLeft_run_pres`** (PROVED): ∃tp', reachIn (toNTM (moveLeft s s')) 1 (s,h,tp)(s',h-1,tp') ∧ tape preserved (writeAt_getD_self/344). Compiled first try. ⇒ completes the head-motion set right/stay/left. NOTE: at head 0, h-1=0 (Nat truncation) — regions live at positive offsets so fine. NEXT: assemble two-region key comparison (carry bit in control state, walk via scanBit/moveLeft, compare), rule-table scan-and-match loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMMoveLeft` |
| **Entry 355 — universal-TM table: the two-cell comparison `compareAdjacent` (PROVED)** | The CONCEPTUAL CORE — first machine to READ TWO TAPE CELLS AND COMPARE THEM (matching half 351-353 only compared vs constants baked into the machine; real match reads both operands from tape). **`compareAdjacent s`** := `branchBit s (s+1)(s+2) ++ ((scanBit (s+1)(s+3) ++ checkBit true (s+3)(s+5)(s+6)) ++ (scanBit (s+2)(s+4) ++ checkBit false (s+4)(s+5)(s+6)))` — read cell j & CARRY its value in control state (branchBit→s+1/s+2), advance (scanBit), check cell j+1 equals carried value (checkBit), reach equal-state s+5 iff agree. State layout: s reads/branches, s+1/s+2 carry true/false, s+3/s+4 post-advance, s+5 eq, s+6 ne. **`compareAdjacent_run_eq`** (PROVED): cells j,j+1 equal (tp.getD j false = tp.getD (j+1) false) ⇒ ∃tp', reachIn (toNTM (compareAdjacent s)) 3 (s,j,tp)(s+5,j+2,tp') — 3 steps to eq-state, head past both. PROOF: by_cases on carried bit (avoid `cases h:` substitution); each arm — branchBit_run_true/false carry + scanBit_run_pres advance + checkBit_run_match compare (cell j+1 value transported via p2,p1 preservation + heq); the TWO ARMS live in one machine, composed: reachIn_seq for the contiguous arm (scanBit++checkBit) then lifted into VER=(trueArm++falseArm) via reachIn_append_LEFT (true) / append_RIGHT (false) [the non-interference law/347], then reachIn_seq branchBit VER. Defeqs held: (j+1)+1≡j+2, 1+2≡3, B++VER≡compareAdjacent. Compiled FIRST TRY. ⇒ carry-and-compare reading two tape cells, the two-pointer match core. SCOPE: proves equal⇒eq-state for ADJACENT cells; NOT the ne-direction, NOT distant regions (walk via scanBit/moveLeft + round-trip invariant), NOT the rule-table loop/apply. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCompareAdjacent` |
| **Entry 356 — universal-TM table: the not-equal branch of `compareAdjacent` (PROVED)** | Completes the two-cell equality DECISION (entry 355 = equal⇒s+5; this = differ⇒s+6). **`compareAdjacent_run_ne`** (PROVED): cells j,j+1 DIFFER (tp.getD j false ≠ tp.getD (j+1) false) ⇒ ∃tp', reachIn (toNTM (compareAdjacent s)) 3 (s,j,tp)(s+6,j+2,tp') — 3 steps to NE-state s+6. PROOF mirrors run_eq but takes the FAILING checkBit branch: by_cases carried bit; derive cell-j+1 value (opposite of carried, via `have h2 : ... ≠ ... := fun hc => hne (hb.trans hc.symm); simpa using h2` — robust, avoids Bool.eq_false_or_eq_true branch-order pitfall); branchBit carry + scanBit advance + checkBit_run_FAIL [needs tp2.getD(j+1)=!b, via rw p2,p1,hjp1 then `rfl` to reduce !true→false]; two arms unified via reachIn_append_left/right + reachIn_seq (same as 355). BUGS: Bool.eq_false_or_eq_true wrong branch order → used ≠+simpa; `simp [hjp1]` unfolded getD→getElem? prematurely → used rw+rfl (rw's terminal rfl doesn't reduce !true, needed explicit `; rfl`). ⇒ compareAdjacent now a COMPLETE equality decision: s+5 iff match, s+6 iff mismatch. NEXT: distant-region comparison (walk via scanBit/moveLeft + round-trip invariant), rule-table scan-and-match loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCompareAdjacentNe` |
| **Entry 357 — universal-TM table: the fixed-distance walk `walkRightK` (PROVED)** | Entry-334 walkRight is a SINGLE-state self-loop (stays state 0); to chain a walk INTO a later comparison need a walk ending in a CONTROLLED distinct state. **`walkRightK : ℕ → ℕ → TMachine`**: `0↦[]`, `k+1 ↦ scanBit s (s+1) ++ walkRightK k (s+1)` — one scanBit per cell, advancing state each step. **`walkRightK_run`** (PROVED): ∃tp', reachIn (toNTM (walkRightK k s)) k (s,h,tp)(s+k,h+k,tp') ∧ tape preserved — k steps to state s+k at head h+k. PROOF: `induction k generalizing s h tp`; zero ⟨tp,rfl,_⟩ (reachIn 0, s+0/h+0/[].length defeq); succ — scanBit_run_pres (1 step s→s+1) + ih (walkRightK k (s+1)) + reachIn_seq/347, `convert comp using 1` [count k+1 vs 1+k by omega — NOT defeq since +recurses on 2nd arg; config Prod.mk.injEq + omega]. Compiled first try. ⇒ traverse a known k-cell gap ending in controlled state s+k — the gap-traversal piece for distant comparison. NEXT: distant comparison (carry bit via branchBit, walkRightK to other region, compare via checkBit, round-trip invariant), rule-table scan-and-match loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMWalkRightK` |
| **Entry 358 — universal-TM table: the distant-region comparison `compareDistant` (PROVED)** | FULL-GENERALITY two-pointer match — compareAdjacent/355-356 generalised to arbitrary gap m. A finite-control machine compares cell j with DISTANT cell j+m by carrying cell j in the control state, traversing the gap on a per-value track, comparing on arrival. **`compareDistant m s sT0 sF0 E N`** := `branchBit s sT0 sF0 ++ ((walkRightK m sT0 ++ checkBit true (sT0+m) E N) ++ (walkRightK m sF0 ++ checkBit false (sF0+m) E N))` — branch on cell j (carry), walkRightK m on matching track to region B, checkBit cell j+m vs carried value, reach E on match (N on mismatch). **`compareDistant_run_eq`** (PROVED): cells j,j+m agree (tp.getD j false = tp.getD (j+m) false) ⇒ ∃tp', reachIn (toNTM (compareDistant m s sT0 sF0 E N)) (m+2) (s,j,tp)(E,j+m+1,tp') — m+2 steps to eq-state E. PROOF: by_cases carried bit; branchBit carry + walkRightK_run/357 [m-step gap traversal] + checkBit_run_match [cell-j+m value transported via pW∘p1 preservation + heq]; two tracks unified via reachIn_append_left (true) / append_right (false) + reachIn_seq; `convert full using 1` [count m+2 vs 1+(m+1) by omega; config (E,j+m+1) vs (E,(j+m)+1) DEFEQ so convert auto-discharges — removed spurious 2nd bullet]. ⇒ the two-pointer comparison at full generality. SCOPE: equal⇒eq-state at distance m; NOT ne-direction at distance, NOT rule-table loop/apply. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCompareDistant` |
| **Entry 359 — universal-TM table: the overwrite primitive `writeConst` (PROVED)** | The FIRST machine to CHANGE a tape cell (all of 344-358 write back what they read = tape-preserving); the APPLY phase (rewrite encoded config with matched rule's RHS) needs to overwrite. **`writeConst b s s'`** := `[((s,true),(s',b,→)), ((s,false),(s',b,→))]` — read cell (whatever), WRITE constant b, move right, go to s'. NOT tape-preserving (sets head cell to b). **`writeConst_step`** (PROVED): (s,j,tp)→(s',j+1,writeAt tp j b) uniformly in read symbol (`cases h : tp.getD j false`, both rules write b; simp [applyTrans,moveHead]). **`writeConst_run`** (PROVED): reachIn (toNTM (writeConst b s s')) 1 (s,h,tp)(s',h+1,writeAt tp h b) = ⟨_, writeConst_step, rfl⟩. Compiled first try. Content downstream via writeAt_getD/344 [(writeAt tp h b).getD q = if q=h then b else tp.getD q]. ⇒ the apply's overwrite atom. NEXT: carry-and-write (copy value between regions, dual of compareDistant), full apply (rewrite config state+sym per matched rule), rule-table scan-and-match loop, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMWriteConst` |
| **Entry 360 — universal-TM table: the inter-region bit copy `copyBit` (PROVED)** | The DUAL of compareDistant/358 — read cell j, carry it, walk to region B, WRITE it there (copy j→j+m). compareDistant with checkBit replaced by writeConst. **`copyBit m s sT0 sF0 E`** := `branchBit s sT0 sF0 ++ ((walkRightK m sT0 ++ writeConst true (sT0+m) E) ++ (walkRightK m sF0 ++ writeConst false (sF0+m) E))` — carry cell j (branchBit), walkRightK m to region B, writeConst carried value at j+m, both tracks converge to E. **`copyBit_run`** (PROVED): ∃tp', reachIn (toNTM (copyBit m s sT0 sF0 E)) (m+2) (s,j,tp)(E,j+m+1,tp') ∧ tp'.getD (j+m) false = tp.getD j false [target holds source value] ∧ ∀ q≠j+m, tp'.getD q false = tp.getD q false [everything else unchanged] — FULL SPEC. PROOF: by_cases carried bit; branchBit carry + walkRightK_run/357 + writeConst_run/359 [exact tape writeAt tp2 (j+m) b]; tp' = writeAt tp2 (j+m) b; reachIn via reachIn_seq + append_left/right + convert using 1 (count omega, config defeq); content via writeAt_getD/344 [if_pos rfl → target=b=source via hb.symm; if_neg hq → tp2.getD q then pW∘p1 preservation]. Compiled first try. ⇒ the apply's transplant operation, fully specified. NEXT: full apply (copy whole new-state + new-symbol of matched rule into config), rule-table scan-and-match loop, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCopyBit` |
| **Entry 361 — universal-TM table: the constant block fill `fillConst` (PROVED)** | Write-side analog of walkRightK/357 — writeConst/359 overwrites ONE cell, fillConst overwrites a BLOCK. **`fillConst (b : Bool) : ℕ → ℕ → TMachine`**: `0↦[]`, `L+1 ↦ writeConst b s (s+1) ++ fillConst b L (s+1)` — one writeConst/cell. **`fillConst_run`** (PROVED, FULL SPEC): ∃tp', reachIn (toNTM (fillConst b L s)) L (s,h,tp)(s+L,h+L,tp') ∧ (∀ i<L, tp'.getD (h+i) false = b) [block set] ∧ (∀ q<h, tp'.getD q = tp.getD q) [before unchanged] ∧ (∀ q, h+L≤q → tp'.getD q = tp.getD q) [after unchanged]. PROOF: induction L generalizing s h tp; zero ⟨tp,rfl,vacuous,rfl,rfl⟩; succ — writeConst_run/359 (exact writeAt tp h b) + ih on (s+1)(h+1)(writeAt tp h b) [gives tp2 + 3 specs] + reachIn_seq + convert using 1 [count L+1 vs 1+L omega; config state/head NOT defeq → Prod.mk.injEq + omega]; written via cases i (zero: hbef2 h + writeAt_getD if_pos; succ: index rw h+(i'+1)=(h+1)+i' + hwr2); before/after via hbef2/haft2 + writeAt_getD if_neg. Compiled first try. ⇒ block-write primitive of apply phase, fully specified. NEXT: full apply (transplant matched rule's new-state + new-symbol into config, combining copyBit/fillConst), rule-table scan-and-match loop, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMFillConst` |
| **Entry 362 — universal-TM table: the fixed-distance left walk `walkLeftK` (PROVED)** | Left analog of walkRightK/357 — the RETURN trip for the apply's block copy (after copying a source cell forward, head must go back left to next source cell). **`walkLeftK : ℕ → ℕ → TMachine`**: `0↦[]`, `k+1 ↦ moveLeft s (s+1) ++ walkLeftK k (s+1)` — one moveLeft/cell. **`walkLeftK_run`** (PROVED): ∃tp', reachIn (toNTM (walkLeftK k s)) k (s,h,tp)(s+k,h-k,tp') ∧ tape preserved — k steps to state s+k at head h-k (Nat sub). PROOF: induction k generalizing s h tp [same as walkRightK]; zero ⟨tp,rfl,_⟩ (h-0=h, s+0=s defeq); succ moveLeft_run_pres/354 (h→h-1) + ih + reachIn_seq + convert using 1 [count k+1 vs 1+k omega; config state (s+1)+k vs s+(k+1), head (h-1)-k vs h-(k+1) — omega handles Nat sub; Prod.mk.injEq]. Compiled first try. ⇒ completes the walkRightK/walkLeftK fixed-distance motion pair, enabling round-trip block copy. NEXT: block copy (copyBit forward + walkLeftK back, per field cell), full apply, rule-table scan-and-match loop, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMWalkLeftK` |
| **Entry 363 — universal-TM table: the copy-and-return body `copyBitReturn` (PROVED)** | FIRST COMPOSITE of the apply phase — the block-copy loop BODY. copyBit/360 copies cell→cell but lands PAST the destination; copyBitReturn adds the walkLeftK/362 return so the head repositions for the next source cell. **`copyBitReturn m s sT0 sF0 E`** := `copyBit m s sT0 sF0 E ++ walkLeftK m E` — copy cell j to j+m, then walk m cells back left to next source j+1. **`copyBitReturn_run`** (PROVED, FULL SPEC): ∃tp', reachIn (toNTM (copyBitReturn m s sT0 sF0 E)) (2*m+2) (s,j,tp)(E+m,j+1,tp') ∧ tp'.getD (j+m) false = tp.getD j false [target=source] ∧ ∀ q≠j+m, tp'.getD q = tp.getD q [rest unchanged]. PROOF: copyBit_run/360 (→E, j+m+1, tp1, content) + walkLeftK_run/362 m E (j+m+1) tp1 (→E+m, (j+m+1)-m=j+1, tp2, preserved) + reachIn_seq; convert using 1 [count 2m+2 vs (m+2)+m omega; config head j+1 vs (j+m+1)-m omega via Prod.mk.injEq]; content transported through walkLeftK preservation (pWalk) onto copyBit's hTarget/hOther. NOTE: reachIn_seq needs NO disjoint states (non-interference/347, existential reachability) so copyBit's + walkLeftK's state ranges may overlap harmlessly. Compiled first try. ⇒ verified iteration unit a field-copy loops. NEXT: loop body over field length (block copy), full apply, rule-table scan-and-match loop, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCopyBitReturn` |
| **Entry 364 — universal-TM table: the single-base copy step `copyStep` (PROVED)** | copyBitReturn/363 takes 4 state args (s,sT0,sF0,E); to LOOP it need ONE entry + ONE exit state. **`copyStep m s`** := `copyBitReturn m s (s+1) (s+m+2) (s+2*m+3)` — internal states laid out disjointly relative to base s. **`copyStep_run`** (PROVED, FULL SPEC): ∃tp', reachIn (toNTM (copyStep m s)) (2*m+2) (s,j,tp)(s+3*m+3,j+1,tp') ∧ tp'.getD (j+m) false = tp.getD j false ∧ ∀ q≠j+m, =tp. PROOF: copyBitReturn_run m s (s+1)(s+m+2)(s+2*m+3) j tp gives end state (s+2*m+3)+m; `rw [show (s+2*m+3)+m = s+3*m+3 from by omega] at hr` then exact (machine def-unfolds to match, count 2m+2 matches). Compiled first try. ⇒ loopable copy unit, one entry s / one exit s+3m+3, advancing head by 1 per call. NEXT: block copy (loop copyStep over field with region-disjointness invariant m≥L), full apply, rule-table scan-and-match loop, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCopyStep` |
| **Entry 365 — universal-TM table: the block copy `copyBlock` (PROVED)** | Loops copyStep/364 to transplant a whole FIELD — the apply's new-state copy. **`copyBlock (m : ℕ) : ℕ → ℕ → TMachine`**: `0↦[]`, `L+1 ↦ copyStep m s ++ copyBlock m L (s + (3*m+3))` [state base shifts by Δ=3m+3 per iter]. **`copyBlock_run`** (PROVED): for **L ≤ m** [REGION-DISJOINTNESS invariant — dest [j+m,j+m+L) must not overlap src [j,j+L), i.e. m≥L, so each iter reads ORIGINAL source not an already-written dest], ∃tp', reachIn (toNTM (copyBlock m L s)) (L*(2*m+2)) (s,j,tp)(s+L*(3*m+3), j+L, tp') ∧ (∀ i<L, tp'.getD (j+m+i) false = tp.getD (j+i) false)[dest mirrors src] ∧ (∀ q<j+m, tp'.getD q = tp.getD q)[before dest, incl whole src, unchanged]. PROOF: induction L generalizing s j tp (hL:L≤m auto-reverted); zero ⟨tp, (simp [Nat.zero_mul,Nat.add_zero];rfl), vacuous, rfl⟩; succ — copyStep_run/364 (→s+3m+3, j+1, tp1, hT1/hO1) + ih on (s+(3*m+3))(j+1)tp1 [hL→ by omega] + reachIn_seq + convert using 1 [count (L+1)*(2m+2) via `rw [add_mul, one_mul]; omega` since omega can't do nonlinear distribution — must distribute FIRST; state same; head omega via Prod.mk.injEq]; copied via cases i (zero: hbef2 (j+m)[<(j+1)+m] + hT1; succ i': index rw + hcopy2 i' + hO1 ((j+1)+i')[≠j+m since 1+i'≠m from i'<L≤m-1] + congr/omega); before via hbef2+hO1. KEY: omega atomizes nonlinear L*(2m+2) so after add_mul distribution both sides share the atom. Compiled FIRST TRY. ⇒ field transplant with disjointness invariant. NEXT: full apply (combine copyBlock new-state + writeConst new-symbol + move), rule-table scan-and-match loop, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCopyBlock` |
| **Entry 366 — universal-TM table: the head-neutral block copy `copyBlockReturn` (PROVED)** | copyBlock/365 leaves head at j+L (past source); apply does SEVERAL field ops so make field-copy HEAD-NEUTRAL (field-level analog of copyBitReturn/363). **`copyBlockReturn m L s`** := `copyBlock m L s ++ walkLeftK L (s + L*(3*m+3))`. **`copyBlockReturn_run`** (PROVED): for L≤m, ∃tp', reachIn (toNTM (copyBlockReturn m L s)) (L*(2*m+2)+L) (s,j,tp)(s+L*(3*m+3)+L, j, tp') ∧ (∀ i<L, tp'.getD (j+m+i) false = tp.getD (j+i) false) ∧ (∀ q<j+m, =tp) — head back at source start j. PROOF: copyBlock_run/365 (→ s+L*Δ, j+L, tp1) + walkLeftK_run/362 L (s+L*Δ) (j+L) tp1 (→ (s+L*Δ)+L, (j+L)-L=j, tp2, preserved) + reachIn_seq; convert using 1 [count L*(2m+2)+L defeq matches; config head (j+L)-L vs j omega via Prod.mk.injEq, state defeq]; content transported through walkLeftK preservation pW. Compiled first try. ⇒ field transplant that returns head, so apply-phase field ops compose from common base. NEXT: full apply (rewrite config state+symbol per matched rule, sequencing copies/writes), rule-table scan-and-match loop, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCopyBlockReturn` |
| **Entry 367 — universal-TM table: the relocatable record scanner `scanTransFrom` (PROVED)** | scanTrans/350 fixed states 0..5 at head 0; the rule-table loop scans SUCCESSIVE transitions at different heads continuing the state. Generalised to relocatable: **`scanTransFrom base`** := `scanNatFrom base (base+1) ++ scanBit (base+1)(base+2) ++ scanNatFrom (base+2)(base+3) ++ scanBit (base+3)(base+4) ++ scanNatFrom (base+4)(base+5)` [states base..base+5]. **`scanTransFrom_run`** (PROVED): ∃tp', reachIn (toNTM (scanTransFrom base)) (rs+ws+mv+5) (base, pre.length, pre ++ encodeTransBits t ++ rest) (base+5, pre.length+(rs+ws+mv+5), tp') — from state base at head pre.length (start of record), scans whole transition, ends base+5 head past it. PROOF: MIRRORS scanTrans/350 exactly but with state 0→base and head 0→pre.length, field_content given prefix `pre` (so field i at offset pre.length+δᵢ); reused field_content (from ACC0UniversalTMScanTrans), scanNatFrom_run_pres/348 + scanBit_run_pres/349, reachIn_seq ×4, convert using 1 (dsimp+omega count, Prod.mk.injEq head). BUG: forgot to `open ...(TMachine)` → def's `:TMachine` mismatch cascaded into bogus omega/rw errors downstream; adding TMachine to open cleared ALL of them. ⇒ loopable record scanner. NEXT: scanTable (loop scanTransFrom over rule count), rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanTransFrom` |
| **Entry 368 — universal-TM table: the record scan preserves the tape (PROVED)** | scanTransFrom_run/367 discards preservation; looping the scanner + interleaving with key-comparison both need to RE-READ the tape after a scan. Every field scanner (scanNatFrom_run_pres, scanBit_run_pres) writes back, so the composed scan preserves. **`scanTransFrom_run_pres`** (PROVED): scanTransFrom_run/367 PLUS `∀ q, tp'.getD q false = (pre ++ encodeTransBits t ++ rest).getD q false`. PROOF: identical to scanTransFrom_run/367 (same field obtains run1..run5/pres1..pres5, reachIn_seq chain, convert) but the final refine adds the preservation conjunct = pres5∘pres4∘pres3∘pres2∘pres1 (the per-field preservations composed). Strengthening, not a re-derivation of new content. Compiled first try. ⇒ the non-destructive-scan property needed to compose scans (re-read tape across them). NEXT: scanTable (loop scanTransFrom over rule list), rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanTransFromPres` |
| **Entry 369 — universal-TM table: in-bounds write-back is the identity `writeAt_id_of_lt` (PROVED)** | The scanners preserve CONTENT (getD pointwise), but looping over the rule table needs the post-scan tape = original AS A LIST (so the ih re-runs the scanner on it). Foundation: writing back within bounds is the identity. **`writeAt_id_of_lt`** (PROVED): p < tape.length → writeAt tape p (tape.getD p false) = tape. PROOF: unfold writeAt = (tape ++ replicate (p+1-len) false).set p w; p<len ⇒ p+1-len=0 ⇒ replicate_zero+append_nil ⇒ tape.set p w; w = tape.getD p false = tape[p] (getD_eq_getElem?_getD + getElem?_eq_getElem hp + Option.getD_some); tape.set p tape[p] = tape via **List.set_getElem_self** (found via exact?). Compiled first try. ⇒ list-level (not just getD) write-back identity — the foundation for list-preserving scanners. SCOPE: this lemma alone doesn't upgrade scanners to list-preservation (needs in-bounds invariant threaded per step) — it's the key ingredient. NEXT: list-preserving scanners → scanTable loop → scan-and-match → apply → EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMWriteAtId` |
| **Entry 370 — universal-TM table: list-preserving field scanners (PROVED)** | Route A executed: writeAt_id_of_lt/369 (in-bounds write-back = identity) ⇒ scanners that write back exactly what they read leave the tape UNCHANGED AS A LIST when scanned positions in-bounds (not just getD-equal). **`scanBit_run_eq`** (PROVED): h < tp.length → reachIn (toNTM (scanBit s s')) 1 (s,h,tp)(s',h+1,tp) [SAME tp]; proof = scanBit_step + rw writeAt_id_of_lt. **`scanNatFrom_run_eq`** (PROVED): given content hyps + h+n < tp.length [whole field incl separator in-bounds], reachIn (toNTM (scanNatFrom s s')) (n+1) (s,h,tp)(s',h+n+1,tp) [SAME tp]; proof = scan invariant (induction j: each scanNatFrom_step_true → writeAt tp (h+j) true = tp via `rw [← htrue']; writeAt_id_of_lt (by omega: h+j<len)`, reachIn_add) then scanNatFrom_step_false → writeAt tp (h+n) false = tp via writeAt_id_of_lt hbound. Compiled first try. ⇒ list-level preservation (tp'=tp exactly), the property the table loop's ih needs. NEXT: list-preserving scanTransFrom (compose these), scanTable loop, scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanListPres` |
| **Entry 371 — universal-TM table: list-preserving record scanner `scanTransFrom_run_eq` (PROVED)** | Composes the list-preserving field scanners/370 into a record scan that returns the tape EXACTLY. **`scanTransFrom_run_eq`** (PROVED): reachIn (toNTM (scanTransFrom base)) (rs+ws+mv+5) (base, pre.length, pre ++ encodeTransBits t ++ rest) (base+5, pre.length+(rs+ws+mv+5), pre ++ encodeTransBits t ++ rest) — SAME tape T at end, NO existential. PROOF: destructure, set T; hTlen : pre.length+(rs+ws+mv.val+5) ≤ T.length (simp length lemmas + omega); hT1/hT3/hT5 nesting; field_content for nat-field content; scanNatFrom_run_eq/370 (fields 1,3,5, in-bounds `by omega` from hTlen) + scanBit_run_eq/370 (fields 2,4) — ALL on the SAME T (no transport, unlike 367/368!) since each _eq scan keeps T identical; reachIn_seq ×4, convert using 1 (dsimp+omega count, Prod.mk.injEq head). Compiled first try. KEY SIMPLIFICATION: list-preservation means every field runs on T, eliminating the preservation-transport that 367/368 needed. ⇒ record scan returns identical tape, enabling scanTable's ih to re-run on the preserved list. NEXT: scanTable (loop scanTransFrom_run_eq over List TMTrans), scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanTransFromEq` |
| **Entry 372 — universal-TM table: the rule-table scan `scanTable` (PROVED)** | Loops the list-preserving record scanner/371 over the WHOLE transition list — first traversal of the encoded rule table. **`encodeTransBits_length`** (PROVED): (encodeTransBits t).length = t.1.1+t.2.1+t.2.2.2.val+5 (simp length lemmas + omega). **`scanTable base : List TMTrans → TMachine`**: `[]↦[]`, `_t::ts ↦ scanTransFrom base ++ scanTable (base+5) ts` (t UNUSED — scanTransFrom is the same machine for any transition; renamed `_t`). **`scanTable_run`** (PROVED): reachIn (toNTM (scanTable base Ms)) (Ms.flatMap encodeTransBits).length (base, pre.length, pre ++ Ms.flatMap encodeTransBits ++ rest) (base+5*Ms.length, pre.length+(Ms.flatMap encodeTransBits).length, pre ++ Ms.flatMap encodeTransBits ++ rest) — SAME tape, head past table. PROOF: induction Ms generalizing base pre; nil simp [flatMap_nil,length_nil,mul_zero,add_zero,append_nil] + rfl; cons — scanTransFrom_run_eq/371 (run1, `simp only [← append_assoc] at run1` to left-assoc its tape) + ih (run2, `rw [length_append, encodeTransBits_length] at run2` to align head) + reachIn_seq → comp; `simp only [flatMap_cons, ← append_assoc, length_append, encodeTransBits_length, length_cons]` on goal then convert using 1 (count matched by simp; config via Prod.mk.injEq ⟨omega state, omega head, rfl tape⟩). KEY: list-preservation (371) means run1 end tape = run2 start tape EXACTLY (after left-assoc normalize) so reachIn_seq composes WITHOUT transport. BUG: had extra count bullet → convert gave only config goal → removed. Compiled. ⇒ whole-table traversal, list-preserving. NEXT: rule-table scan-and-MATCH (interleave compareDistant of each key vs config key), apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMScanTable` |
| **Entry 373 — universal-TM table: list-preserving branch/check runs (PROVED)** | Just as the table scan needed list-preserving SCANNERS (370-372), the scan-and-match needs list-preserving COMPARISON primitives (key comparison must leave the tape intact for the apply + next comparison). branchBit/checkBit write back what they read → identity in-bounds (writeAt_id_of_lt/369). **`branchBit_run_true_eq`/`branchBit_run_false_eq`** (PROVED): h<tp.length + read symbol → branch leaving tape IDENTICAL ((sTrue/sFalse, h, tp)). **`checkBit_run_match_eq`/`checkBit_run_fail_eq`** (PROVED): h<tp.length → cell=b reaches (sCont,h+1,tp), cell=!b reaches (sFail,h+1,tp), tape IDENTICAL. PROOF (each): the _step lemma + `rw [show writeAt tp h <v> = tp from by rw [← hb]; exact writeAt_id_of_lt tp h hbound] at hstep` + ⟨_,hstep,rfl⟩. Compiled first try. ⇒ list-preserving decision/comparison steps (mirror of 370 for the read-side). NEXT: list-preserving compareDistant, key comparison, rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMBranchCheckEq` |
| **Entry 374 — universal-TM table: list-preserving fixed-distance walk `walkRightK_run_eq` (PROVED)** | walkRightK_run/357 only getD-preserves; list-preserving compareDistant needs the walk to leave tape IDENTICAL. walkRightK = fold of scanBit; scanBit_run_eq/370 is list-identity in-bounds ⇒ whole walk is. **`walkRightK_run_eq`** (PROVED): h+k ≤ tp.length → reachIn (toNTM (walkRightK k s)) k (s,h,tp)(s+k,h+k,tp) [SAME tp]. PROOF: induction k generalizing s h; zero rfl; succ scanBit_run_eq/370 (h<len by omega) + ih ((h+1)+k≤len by omega) + reachIn_seq + convert using 1 (omega count, Prod.mk.injEq state/head, rfl tape). Compiled first try. ⇒ list-preserving walk for compareDistant. NEXT: list-preserving compareDistant, key comparison, rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMWalkRightKEq` |
| **Entry 375 — universal-TM table: list-preserving distant comparison (PROVED)** | compareDistant/358 only getD-preserves; scan-and-match needs the comparison to leave tape IDENTICAL (apply re-reads original). Components branchBit/walkRightK/checkBit all list-preserving now (373/374), all on SAME tape. **`compareDistant_run_eq`** (PROVED): cells j,j+m agree + j+m<tp.length → reachIn (toNTM (compareDistant m s sT0 sF0 E N)) (m+2) (s,j,tp)(E,j+m+1,tp) [SAME tp]. **`compareDistant_run_ne`** (PROVED): cells differ + j+m<tp.length → reaches (N,j+m+1,tp) [SAME tp]. PROOF (each): by_cases carried bit; branchBit_run_*_eq/373 + walkRightK_run_eq/374 + checkBit_run_match_eq (eq) / checkBit_run_fail_eq (ne) /373 — ALL same tp; cell-j+m value via heq/hne (ne: `have h2:≠; simpa`); reachIn_seq + append_left/right + convert using 1 + omega [config defeq head, only count]. in-bounds all `by omega` from j+m<tp.length. Compiled first try. ⇒ list-preserving distant comparison, both outcomes. NEXT: multi-bit KEY COMPARISON (commit config layout), rule-table scan-and-match loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCompareDistantEq` |
| **Entry 376 — universal-TM table: list-preserving left motions (PROVED)** | The comparison loop returns the head left between iterations; the return must leave tape identical. moveLeft writes back → identity in-bounds (writeAt_id_of_lt/369), walkLeftK (fold of moveLeft) inherits. **`moveLeft_run_eq`** (PROVED): h<tp.length → reachIn (toNTM (moveLeft s s')) 1 (s,h,tp)(s',h-1,tp) [SAME tp]; moveLeft_step + rw writeAt_id_of_lt. **`walkLeftK_run_eq`** (PROVED): h<tp.length → reachIn (toNTM (walkLeftK k s)) k (s,h,tp)(s+k,h-k,tp) [SAME tp]; induction k generalizing s h (hbound h<len reverted as it mentions h), moveLeft_run_eq + ih (h-1<len by omega) + reachIn_seq + convert using 1. All written positions ≤ h < len. Compiled first try. ⇒ completes the list-preserving MOTION set (right=walkRightK_run_eq/374, left here). NEXT: compare-and-return loop body, unary-field equality comparison, rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMWalkLeftKEq` |
| **Entry 377 — universal-TM table: the compare-and-return body `compareBitReturn` (PROVED)** | Loop body of cell-by-cell field comparison: compareDistant + (on match) walkLeftK return. **`compareBitReturn m s sT0 sF0 E N`** := `compareDistant m s sT0 sF0 E N ++ walkLeftK m E`. **`compareBitReturn_run_eq`** (PROVED): equal cells + j+m+1<tp.length → reachIn ... (2*m+2) (s,j,tp)(E+m, j+1, tp) [match, head returned to next pair, SAME tp]; compareDistant_run_eq/375 (→E,j+m+1,tp) + walkLeftK_run_eq/376 m E (j+m+1) (→E+m, (j+m+1)-m=j+1, tp) + reachIn_seq + convert. **`compareBitReturn_run_ne`** (PROVED): differing cells + j+m<tp.length → reachIn ... (m+2) (s,j,tp)(N, j+m+1, tp) [mismatch halts at N; the walkLeftK (entered only at E) never runs]; compareDistant_run_ne/375 lifted via reachIn_append_left into the union. Compiled first try. KEY: on the N-exit, the appended walkLeftK m E (states E..E+m) is never entered (N≠E states) so the run halts at N — reachIn_append_left gives this directly. ⇒ the comparison loop body (match returns head, mismatch aborts). NEXT: loop it for unary-field equality comparison (3-way continue/match/mismatch), rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCompareBitReturn` |
| **Entry 378 — universal-TM table: the three-way comparison step `compareStep3` (PROVED)** | Loop body of unary-field equality: per cell pair, route 3 ways — both true→cont (fields running), both false→matchSt (aligned separators=equal), differ→noMatch. INSIGHT: compareDistant's carry ALREADY splits on cell j's value (true-track sT0 / false-track sF0), so giving the two tracks DISTINCT match-exits gives the 3-way for free. **`compareStep3 m s sT0 sF0 cont matchSt noMatch`** := branchBit s sT0 sF0 ++ ((walkRightK m sT0 ++ checkBit true (sT0+m) cont noMatch) ++ (walkRightK m sF0 ++ checkBit false (sF0+m) matchSt noMatch)). **`compareStep3_run_cont`** (both true→cont), **`compareStep3_run_match`** (both false→matchSt), **`compareStep3_run_ne`** (differ→noMatch) — all PROVED, all (m+2) steps to (·, j+m+1, tp) [SAME tp]. PROOF: 3 copies of the 375 pattern (branchBit_run_*_eq + walkRightK_run_eq + checkBit_run_match_eq/fail_eq, append_left true-track/append_right false-track, reachIn_seq, convert+omega). Compiled first try. ⇒ the 3-way unary-comparison loop body. NEXT: loop it (unary-field equality), rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCompareStep3` |
| **Entry 379 — universal-TM table: unary-field equality comparison `compareUnary` (PROVED, match direction)** | FIRST CYCLIC, DATA-DEPENDENT loop of the build. Loops compareStep3/378 into a comparison of two whole unary fields. **`compareUnary m sT0 sF0 sCont matchSt noMatch`** := `compareStep3 m (sCont+m) sT0 sF0 sCont matchSt noMatch ++ walkLeftK m sCont` — CYCLIC: loop head = sCont+m (where walkLeftK m sCont ends), so compareStep3's cont-exit → walkLeftK return → back to loop head (one iteration per true-pair); matchSt exit = "equal". (State overlaps harmless — reachIn existential.) **`compareUnary_run_match`** (PROVED): two EQUAL unary fields length L (A at j, B at j+m) → ∃N, reachIn (toNTM (compareUnary ...)) N (sCont+m, j, tp)(matchSt, j+L+m+1, tp) [SAME tp]. PROOF: induction on L; zero — compareStep3_run_match (both separators) lifted via append_left (exit j+0+m+1 ≡ j+m+1 defeq → exact); succ — compareStep3_run_cont (true-pair, append_left) + walkLeftK_run_eq m sCont (rw (j+m+1)-m=j+1, append_right) composed by reachIn_add = one iteration (sCont+m,j,tp)→(sCont+m,j+1,tp), then IH at j+1 (hyps shifted by 1 via simpa+omega index rw) composed by reachIn_add; exit (j+1)+L+m+1 rw to j+(L+1)+m+1. minimal axioms [propext,Quot.sound]. BUGS: zero convert→exact (defeq); succ convert misfired (tp.length goal)→rw exit-head at hcomp + exact. Compiled. ⇒ unary-field equality (match dir), the first real data-dependent loop. NEXT: mismatch/length-diff dirs, full key comparison (state-field + readsym bit), rule-table scan-and-match, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCompareUnary` |
| **Entry 380 — universal-TM table: unary comparison MISMATCH direction `compareUnary_run_ne` (PROVED)** | Complements 379's match: if fields agree on a true-prefix length k but DIFFER at position k (genuine disagreement incl. unequal-length: one separator meets the other's true), loop reaches noMatch. **`compareUnary_run_ne`** (PROVED): ∀i<k both true at j+i,j+m+i, and tp.getD(j+k)≠tp.getD(j+m+k), and bound → ∃N, reachIn N (sCont+m,j,tp)(noMatch, j+k+m+1, tp) [SAME tp]. PROOF: SAME cyclic structure as 379 (induction on agreeing-prefix k) but base = compareStep3_run_ne (no-match at differing pair) instead of _run_match; succ = one cont iteration (compareStep3_run_cont + walkLeftK_run_eq via reachIn_add) + IH at j+1 (hyps incl hdiff shifted +1 via simpa+omega index-rw). Compiled FIRST TRY (379 pattern transferred cleanly). ⇒ compareUnary now DECIDES unary-field equality: matchSt (equal/379) vs noMatch (disagree/380). NEXT: full KEY comparison (state-field via compareUnary + readsym bit via compareDistant), rule-table scan-and-match loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMCompareUnaryNe` |
| **Entry 381 — universal-TM table: the full key match `keyMatch` (PROVED, match direction)** | Composes unary state-field equality (compareUnary/379) with symbol-bit equality (compareDistant/375) into the full transition-key match. Transition key = (state,readSym) = unary state field then symbol bit (encodeTransBits layout). **`keyMatch m sT0 sF0 sCont matchSt noMatch bT0 bF0 fullMatch`** := compareUnary m sT0 sF0 sCont matchSt noMatch ++ walkLeftK m matchSt ++ compareDistant m (matchSt+m) bT0 bF0 fullMatch noMatch. **`keyMatch_run_match`** (PROVED): equal state fields (length L, config at j, rule at j+m) + equal symbol bits (config-sym j+L+1 = rule-sym (j+L+1)+m) + bound → ∃N, reachIn N (sCont+m,j,tp)(fullMatch, (j+L+1)+m+1, tp) [SAME tp]. PROOF exploits LAYOUT: compareUnary_run_match → matchSt at head j+L+m+1 (= rule symbol position, config symbol m to the left); walkLeftK_run_eq m matchSt [rw (j+L+m+1)-m=j+L+1] → head at config-symbol j+L+1; compareDistant_run_eq m (matchSt+m) at j+L+1 (gap m to rule symbol) → fullMatch. 3 phases composed reachIn_seq ×2 (mid configs match: matchSt at j+L+m+1, matchSt+m at j+L+1). bounds all by omega from j+L+m+1<tp.length. minimal axioms [propext,Quot.sound]. Compiled first try. KEY: the encodeTransBits layout (state-field then symbol, gap m between config & rule) makes the post-state-compare head land EXACTLY on the rule symbol, so only a fixed walkLeftK m reposition is needed. ⇒ full key match (match dir). NEXT: key-match no-match dirs, rule-table scan-and-match LOOP (iterate keyMatch over rule list — 2nd cyclic loop), apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMKeyMatch` |
| **Entry 382 — universal-TM table: key match NO-MATCH directions (PROVED)** | Completes the per-rule decision: 2 ways a key fails. **`keyMatch_run_ne_state`** (PROVED): state fields differ (agree true-prefix k, differ at k) → noMatch; compareUnary_run_ne/380 lifted via reachIn_append_left ×2 [keyMatch = ((compareUnary++walkLeftK)++compareDistant), compareUnary deep-left; the symbol phase entered only at matchSt≠noMatch so run halts at noMatch]. **`keyMatch_run_ne_sym`** (PROVED): state equal (length L) but symbols differ → noMatch; compareUnary_run_match/379 → matchSt + walkLeftK_run_eq/376 reposition + compareDistant_run_ne/375 (symbols differ → noMatch N-exit), 3 phases reachIn_seq ×2 (same as keyMatch_run_match/381 but compareDistant_run_ne). Compiled first try. ⇒ keyMatch now COMPLETE per-rule decision: fullMatch iff keys equal, noMatch otherwise (state-differ OR symbol-differ). NEXT: rule-table scan-and-MATCH LOOP (iterate keyMatch over rule LIST — 2nd cyclic loop; fullMatch→apply, noMatch→advance scanTransFrom to next rule + repeat), apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTMKeyMatchNe` |
| **Entry 383 — universal-TM table: the 3-symbol (MARKER) tape model (PROVED)** | DESIGN FORK RESOLVED (user chose "add 3rd tape symbol"): the rule-loop needs VARYING-distance key comparison, impossible with fixed-gap 2-symbol/unary machines (one machine hardcodes one gap; Bool tape has no spare marker). Founds a parallel 3-symbol model reusing the generic NTM/reachIn layer. **`Sym3`** (O/I/M = blank/one/MARKER, DecidableEq), CConfig3/TMTrans3/TMachine3, readSym3/writeAt3/applyTrans3/concreteStep3/toNTM3 (mirror ACC0ConcreteNTM over Sym3; toNTM3 init maps Bool true↦I/false↦O). **`writeAt3_getD`** (PROVED) + **`writeAt3_id_of_lt`** (PROVED) — Bool proofs (344/369) ported INTACT (default O). Compiled first try. ⇒ marker model founded; M will let comparison cross off cells + shuttle between regions at arbitrary distance (solves varying-gap). The 39-entry Bool library (344-382) STAYS; this is a PARALLEL track for the loop. NEXT: marker-based varying-distance comparison primitives, rule-table loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Sym` |
| **Entry 384 — universal-TM table: composition over the 3-symbol model (PROVED)** | Ports entry-347 wiring (reachIn_seq + non-interference) to concreteStep3 (Bool versions are Bool-specific). **`concreteStep3_append_left/right`** (PROVED: step of M₁/M₂ is step of M₁++M₂ via List.mem_append — concreteStep3 existential over rules, no disjointness). **`reachIn_toNTM3_mono`** (PROVED: step mono ⇒ reachability mono; induction on k). **`reachIn_append_left3/right3`** (PROVED: sub-table run lifts to union). **`reachIn_seq3`** (PROVED: reachIn (toNTM3 M₁) a c d → reachIn (toNTM3 M₂) b d e → reachIn (toNTM3 (M₁++M₂)) (a+b) c e). IDENTICAL to 347 with toNTM→toNTM3, concreteStep→concreteStep3. Compiled first try. ⇒ the wiring to assemble marker-based machines over Sym3. NEXT: marker-based varying-distance comparison primitives [write M, shuttle, compare], rule-table loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Compose` |
| **Entry 385 — universal-TM table: marker write + 3-symbol right move (PROVED)** | First marker-track primitives over Sym3/383. **`writeMark3 s s'`** := `[((s,O),(s',M,→)),((s,I),(s',M,→)),((s,M),(s',M,→))]` — write marker M (returnable anchor) on any symbol, move right. **`writeMark3_step/run`** (PROVED: (s,j,tp)→(s',j+1,writeAt3 tp j M); rcases readSym3 ×3 cases, each picks matching rule writing M; `by simp [writeMark3]` membership, `by simp [h]` key, `by simp [applyTrans3,moveHead]` apply). **`moveRight3 s s'`** := writes back read symbol, move right (Sym3 analog of scanBit). **`moveRight3_step`** (PROVED) + **`moveRight3_run_eq`** (PROVED: j<tp.length → reachIn (toNTM3 (moveRight3 s s')) 1 (s,j,tp)(s',j+1,tp) [SAME tp]; moveRight3_step + rw [readSym3=tp.getD j O rfl, writeAt3_id_of_lt]). BUG: 3 unused-simp-arg h warnings (rcases substituted readSym3 so 3rd-goal h unused) → removed. ⇒ marker anchor write + preserving right shuttle. NEXT: left move + seek-to-marker (walk to M), marker two-pointer comparison, rule-table loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Move` |
| **Entry 386 — universal-TM table: left move + branch-on-marker (PROVED)** | 2 more marker-track primitives. **`moveLeft3 s s'`** (Sym3 left shuttle, write back + move 0). **`moveLeft3_run_eq`** (PROVED: j<tp.length → reachIn (toNTM3 (moveLeft3 s s')) 1 (s,j,tp)(s',j-1,tp) [SAME]; mirror moveRight3/385 move 0). **`branchMark3 s sFound sCont`** := `[((s,M),(sFound,M,stay)),((s,O),(sCont,O,stay)),((s,I),(sCont,I,stay))]` — STAY, route to sFound if cell=M else sCont. THIS makes the gap IRRELEVANT (a seek loop finds the anchor regardless of distance). **`branchMark3_run_mark`** (PROVED: readSym3=M + j<len → (sFound,j,tp) [SAME]; the M-rule, writeAt3 tp j M = tp via `rw [← h]; writeAt3_id_of_lt`). **`branchMark3_run_notmark`** (PROVED: readSym3≠M + j<len → (sCont,j,tp); rcases readSym3 — O/I cases via cont-rule + writeAt3_id, M case absurd h hne). Compiled first try, NO warnings. ⇒ left shuttle + the marker branch (anchor-find heart). NEXT: seek-to-marker LOOP (cyclic: branchMark3 notmark→moveLeft3→loop, mark→found; induction on distance), marker comparison, rule-table loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Mark` |
| **Entry 387 — universal-TM table: the seek-to-marker loop `seekMarkLeft` (PROVED)** | The DISTANCE-AGNOSTIC anchor-find — a cyclic machine walks LEFT to the marker M regardless of distance, the construction that makes the varying gap IRRELEVANT. **`seekMarkLeft s found cont`** := `branchMark3 s found cont ++ moveLeft3 cont s` — CYCLIC: loop head s; on M→found; else moveLeft3 cont s returns to s (loop). **`seekMarkLeft_run`** (PROVED): marker at p (tp.getD p O = M), d cells above p (p+1..p+d) all non-M → ∃N, reachIn (toNTM3 (seekMarkLeft s found cont)) N (s,p+d,tp)(found,p,tp) [SAME tp]. PROOF: induction on distance d (like compareUnary/379); zero — branchMark3_run_mark/386 at p (cell M) lifted reachIn_append_left3; succ — branchMark3_run_notmark (cell p+d+1 ≠ M via hclear, append_left3) + moveLeft3_run_eq/386 [rw p+(d+1)-1=p+d, append_right3] via reachIn_add = one iteration (s,p+d+1)→(s,p+d), then IH (hclear restricted by omega) via reachIn_add. Compiled FIRST TRY. ⇒ 2nd cyclic-loop pattern over Sym3; the seek finds the anchor at ANY distance, solving the varying-gap problem (the universal machine marks one operand, shuttles to the other, seeks back to the marker). NEXT: rightward seek + marker two-pointer COMPARISON (mark, shuttle, carry bit, seek back, compare, advance marker), rule-table loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Seek` |
| **Entry 388 — universal-TM table: rightward seek `seekMarkRight` (PROVED)** | Mirror of seekMarkLeft/387 — cyclic walk RIGHT to marker M regardless of distance. **`seekMarkRight s found cont`** := `branchMark3 s found cont ++ moveRight3 cont s` (cyclic, cont→loop head s). **`seekMarkRight_run`** (PROVED): marker at h+d, d cells below [h..h+d-1] non-M → ∃N, reachIn N (s,h,tp)(found,h+d,tp) [SAME]. PROOF: induction on d generalizing h (marker pos h+d is d-dependent, so hmark/hclear/hbound all inside ∀d; reverted); zero branchMark3_run_mark + append_left3; succ branchMark3_run_notmark (cell h≠M) + moveRight3_run_eq/385 + reachIn_add iteration, IH at h+1 [hmark/hclear shifted via simpa+omega: h+1+d=h+(d+1), h+(k+1)=h+1+k], `rw [show h+1+d=h+(d+1)] at hrec` to bridge exit. Compiled first try. ⇒ completes the distance-agnostic seek PAIR (left/right). NEXT: marker two-pointer COMPARISON (writeMark3 config cell, branch-carry its bit, seekMarkRight to rule cell, read, seekMarkLeft back, compare, advance marker — loop), rule-table loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3SeekR` |
| **Entry 389 — universal-TM table: anchor-and-carry `markCarry3` (PROVED)** | First piece of the marker comparison: read the cell's bit, REMEMBER it in the control state, overwrite with marker M (anchor). **`markCarry3 s sO sI sM`** := `[((s,O),(sO,M,stay)),((s,I),(sI,M,stay)),((s,M),(sM,M,stay))]` — write M, STAY, branch state on original value (sO/sI/sM). NOT tape-preserving (writes M over O/I; value preserved IN STATE, restored later). **`markCarry3_run_O`** (PROVED: cell=O → reachIn 1 (s,j,tp)(sO,j,writeAt3 tp j M); M written, O remembered via sO). **`markCarry3_run_I`** (PROVED: cell=I → (sI,j,writeAt3 tp j M)). PROOF: refine ⟨target, ?_, rfl⟩ + ⟨matching-rule, simp[markCarry3], simp[h], simp[applyTrans3,moveHead]⟩ (move 2 stay). Compiled first try, minimal axioms [propext]. ⇒ anchor-laid + bit-in-state, the START of a distance-independent cell-pair comparison. NEXT: shuttle to other operand (seekMarkRight/388 to a delimiter or fixed pos), read+compare to carried bit, seekMarkLeft/387 back to M, restore, advance — assemble the marker compare loop, then rule-table loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MarkCarry` |
| **Entry 390 — universal-TM table: restore primitive `unmark3` (PROVED)** | Dual of markCarry3/389 — after carrying a cell's bit and comparing, RESTORE the cell (write carried value over M). **`unmark3 s s' w`** := `[((s,O),(s',w,stay)),((s,I),(s',w,stay)),((s,M),(s',w,stay))]` (write w, stay, uniform). **`unmark3_step`** (PROVED) + **`unmark3_run`** (PROVED: reachIn (toNTM3 (unmark3 s s' w)) 1 (s,j,tp)(s',j,writeAt3 tp j w); rcases readSym3 ×3, each writes w; ⟨_,step,rfl⟩). Generalizes writeMark3/385 (write any w, stay). Compiled first try [propext]. ⇒ the restore phase (writes the carried bit w back over M); the caller picks w = carried value (from sO/sI track). Now mark→carry→shuttle→compare→RESTORE all have primitives. NEXT: assemble the shuttle-and-compare LOOP (markCarry → shuttle → compare → unmark/restore → advance), rule-table loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Unmark` |
| **Entry 391 — universal-TM table: double-write collapse `writeAt3_writeAt3` (PROVED)** | Algebraic basis of the restore: writing twice at the same cell keeps only the last. **`writeAt3_writeAt3`** (PROVED): writeAt3 (writeAt3 tape p a) p b = writeAt3 tape p b. PROOF: unfold writeAt3; the outer write's pad is empty (since (writeAt3 tape p a).length ≥ p+1, so p+1-it=0 by simp [length_set,length_append,length_replicate]+omega → replicate_zero+append_nil); then List.set_set collapses the double .set p → .set p b; pads equal (both replicate(p+1-tape.length)). Compiled first try. ⇒ anchor-then-restore (write M then carried value) = single write of the value; if value=original (in bounds) it's the identity (writeAt3_id_of_lt). The restore phase is now algebraically grounded. NEXT: assemble the marker shuttle-and-compare loop (markCarry+shuttle+compare+unmark), rule-table loop, apply, EmitsEncodedStep. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3WriteWrite` |
| **Entry 392 — universal-TM table: the 3-symbol encoding `encodeNatBits3` (PROVED)** | USER CHOSE (AskUserQuestion): "Migrate to Sym3 and push to EmitsEncodedStep" — the full faithful completion (re-derive encoding 338-343 + restate EmitsEncodedStep over toNTM3, then rule-loop/apply; ~15-30 more clean fragments, many turns, no faking). MIGRATION STARTS: the encoding. **`encodeNatBits3 n`** := `List.replicate n Sym3.I ++ [Sym3.O]` (n ones then zero separator). **`encodeNatBits3_length`** (PROVED: = n+1). **`encNat3_getD_lt`** (PROVED: j<n → (encodeNatBits3 n ++ rest).getD j O = I; getElem?_append_left + getElem?_replicate_of_lt, Sym3 port of 345). **`encNat3_getD_eq`** (PROVED: separator = O). BUG: [O].length not reduced for omega → added List.length_cons/length_nil to simp only. Compiled. ⇒ Sym3 encoding foundation. NEXT: Sym3 scanners (scanNatFrom3/scanBit3, ports of 346/349), scanTransFrom3, scanTable3, then marker rule-loop + apply + EmitsEncodedStep3 over toNTM3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Encode` |
| **Entry 393 — universal-TM table: the 3-symbol transition encoding `encodeTransBits3` (PROVED)** | The simulated machine is a BOOL TM; U is Sym3 (for markers). So the encoding maps a Bool transition onto Sym3: nat fields as I-runs, symbol bits via boolToSym3 (true↦I, false↦O). **`boolToSym3 b`** := if b then I else O. **`encodeTransBits3 t`** := encodeNatBits3 t.1.1 ++ boolToSym3 t.1.2 :: (encodeNatBits3 t.2.1 ++ boolToSym3 t.2.2.1 :: encodeNatBits3 t.2.2.2.val). **`encodeTransBits3_length`** (PROVED: = t.1.1+t.2.1+t.2.2.2.val+5, same as Bool). Sym3 port of 338/372. Compiled first try. NEXT: encodeMachineBits3/encodeConfig3, Sym3 scanners, marker rule-loop, apply, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3EncTrans` |
| **Entry 394 — universal-TM table: the 3-symbol machine encoding `encodeMachineBits3` (PROVED)** | Sym3 port of encodeMachineBits/338. **`encodeMachineBits3 M`** := `encodeNatBits3 M.length ++ M.flatMap encodeTransBits3` (length I-run+sep, then transitions concatenated). **`encodeMachineBits3_length`** (PROVED: = (M.length+1) + (M.flatMap encodeTransBits3).length; rw encodeMachineBits3 + length_append + encodeNatBits3_length). Compiled first try. ⇒ encoding layer's machine shape ported. Sym3 encoding now: encodeNatBits3/392, encodeTransBits3/393, encodeMachineBits3/394. NEXT: encodeConfig3 (port 340), Sym3 scanners (scanNatFrom3/scanBit3/scanTransFrom3/scanTable3), marker rule-loop, apply3, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3EncMachine` |
| **Entry 395 — universal-TM table: the 3-symbol field scanners `scanNatFrom3`/`scanBit3` (PROVED)** | Sym3 ports of the field scanners (346/349) + their list-preserving runs (370). **`scanNatFrom3 s s'`** := `[((s,I),(s,I,1)),((s,O),(s',O,1))]` (on I stay+right, on O separator go s'+right). **`scanNatFrom3_run_eq`** (PROVED): content hyps (`∀i<n, getD(h+i)=I`; `getD(h+n)=O`) + `h+n<tp.length` ⇒ `reachIn (n+1) (s,h,tp) (s',h+n+1,tp)`, tape IDENTICAL (each write-back = id via `writeAt3_id_of_lt`). **`scanBit3 s s'`** := write-back any symbol+right→s'; **`scanBit3_run_eq`** (PROVED): `j<tp.length ⇒ reachIn 1 (s,j,tp)(s',j+1,tp)`, tape identical. Compiled first try (Bool port transferred). NEXT: scanTransFrom3/scanTable3 record scanners, marker rule-loop, apply3, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Scan` |
| **Entry 396 — universal-TM table: the 3-symbol field-content lemma `field_content3` (PROVED)** | Sym3 port of 350's tape-content helper. **`getD_append_shift3`** (PROVED: `(pre++X).getD (pre.length+i) O = X.getD i O`). **`field_content3`** (PROVED: `T = pre ++ (encodeNatBits3 n ++ rest)` & `pre.length=h` ⇒ `(∀i<n, T.getD(h+i)=I) ∧ T.getD(h+n)=O`) — exactly the hyps `scanNatFrom3_run_eq` consumes. Reuses encNat3_getD_lt/encNat3_getD_eq (392). First try. NEXT: scanTransFrom3 record scanner. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3FieldContent` |
| **Entry 397 — universal-TM table: the 3-symbol record scanner `scanTransFrom3` (PROVED)** | Sym3 port of 371 (list-preserving record scanner). **`scanTransFrom3 base`** := scanNatFrom3 base (base+1) ++ scanBit3 (base+1)(base+2) ++ scanNatFrom3 (base+2)(base+3) ++ scanBit3 (base+3)(base+4) ++ scanNatFrom3 (base+4)(base+5). **`scanTransFrom3_run_eq`** (PROVED: `reachIn (rs+ws+mv+5) (base, pre.length, pre++encodeTransBits3 t++rest) (base+5, pre.length+(rs+ws+mv+5), SAME tape)`). All 5 field scans on the SAME T (the _eq versions), no transport — composed by reachIn_seq3; field_content3 supplies each field's content (boolToSym3 symbol cells handled by scanBit3). First try. NEXT: scanTable3 loop. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ScanTrans` |
| **Entry 398 — universal-TM table: the 3-symbol rule-table scan `scanTable3` (PROVED)** | Sym3 port of 372. **`scanTable3 base`** : `[]↦[]`, `t::ts ↦ scanTransFrom3 base ++ scanTable3 (base+5) ts`. **`scanTable3_run`** (PROVED: `reachIn (Ms.flatMap encodeTransBits3).length (base, pre.length, pre++Ms.flatMap encodeTransBits3++rest) (base+5*Ms.length, pre.length+len, SAME tape)`). Induction on Ms generalizing base/pre; list-preservation ⇒ inductive step re-runs scanner on identical tape (clean recursion, no transport). Reuses encodeTransBits3_length (393). First try. NEXT: rule-table scan-and-match (key compare via marker rule-loop), apply3, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ScanTable` |
| **Entry 399 — universal-TM table: the fixed-symbol bit test `testBit3` (PROVED, NEW not a port)** | The match atom of the rule-loop — the first GENUINELY-NEW marker-route construction (the Bool route hit the data-vs-data / varying-distance wall exactly here). Mechanism: data-vs-data becomes state-vs-CONSTANT because markCarry3 (389) reads the config-key bit into the control-state lineage (sO/sI), so at the distant rule-key cell the test is against a constant `w` fixed by lineage. **`testBit3 s sEq sNe w`** := per-symbol write-back + STAY, route to sEq if cell=w else sNe. **`testBit3_run`** (PROVED: `j<tp.length ⇒ reachIn 1 (s,j,tp) ((if readSym3=w then sEq else sNe), j, tp)`, head fixed, tape identical). KEY proof: rcases h:readSym3 generalizes it in the goal; set step result tape = writeAt3 tp j (symbol) (matches applyTrans3 like markCarry3); `show _ = _` coerces reachIn 0 → Eq so rw [hid] closes (writeAt3_id_of_lt). NEXT: shuttle (seekMarkRight/moveRight3) + chain markCarry3→shuttle→testBit3→unmark3 into distance-independent single-bit compare, then unary-field compare, key compare, rule-table match loop, apply3, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3TestBit` |
| **Entry 400 — universal-TM table: the Sym3 emission lifting `universalSim_of_emits3` (PROVED)** | Re-anchors the TOP of the Sym3 tower (port of 342). KEY: encodedStep/simIter/encodeConfig/encodeMachineBits/encodedStep_correct are SYMBOL-AGNOSTIC (pure bit-list fns) → reused VERBATIM; only U, φ:List Bool→List Bool→CConfig3, reachIn (toNTM3 U) are Sym3-specific. **`EmitsEncodedStep3 U φ cost`** := ∀ Mbits cbits next, encodedStep Mbits cbits=some next → reachIn (toNTM3 U) cost (φ Mbits cbits)(φ Mbits next). **`universalSim_of_emits3`** (PROVED: emit+encodedStep_correct ⇒ reachIn (toNTM3 U)(k*cost) over full simIter, induction on k + reachIn_add). ⇒ whole Sym3 universal-TM reduced to ONE obligation: concrete U+φ+EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Emit` |
| **Entry 401 — universal-TM table: the n-step right move `moveRightN3` (PROVED)** | The outbound leg of the marker shuttle. **`moveRightN3 s`** : 0↦[], n+1↦moveRight3 s (s+1)++moveRightN3 (s+1) n (corridor). **`moveRightN3_run`** (PROVED: j+n≤tp.length ⇒ reachIn n (s,j,tp)(s+n,j+n,tp), tape identical). Induction generalizing s/j; GOTCHA: rewrite counts/states in `comp` not the goal (global rw [n+1=1+n] corrupts moveRightN3 s (n+1) reduction). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MoveN` |
| **Entry 402 — universal-TM table: marker-shuttle return-and-restore `probeTail3` (PROVED)** | The DISTANCE-INDEPENDENT half. **`probeTail3 sIn sFound sCont sOut b`** := seekMarkLeft sIn sFound sCont ++ unmark3 sFound sOut b. **`probeTail3_run`** (PROVED: tp[p]=M, no M in (p,p+d], p+d<length ⇒ ∃N reachIn N (sIn,p+d,tp)(sOut,p,writeAt3 tp p b)). seekMarkLeft finds anchor regardless of outbound distance; unmark restores b. Lift seek via append_left3, unmark via append_right3, chain reachIn_add. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ProbeTail` |
| **Entry 403 — universal-TM table: single-bit compare-after-mark `probe3` (PROVED, THE CRUX)** | The heart of the marker route: distance-independent DATA-VS-DATA single-bit compare. **`probe3 s sEq sNe d b`** := moveRightN3 s d ++ testBit3 (s+d)(s+d+1)(s+d+2) b ++ probeTail3 (s+d+1)(s+d+3)(s+d+4) sEq b ++ probeTail3 (s+d+2)(s+d+5)(s+d+6) sNe b. **`probe3_run`** (PROVED, first try: anchor M at p, no M in (p,p+d], p+d<length ⇒ ∃N reachIn N (s,p,tp) ((if tp[p+d]=b then sEq else sNe), p, writeAt3 tp p b)). BRANCH (testBit3 routes to one of two tails, only one executes per run): `set` A/B/C/D, lift each sub-run via append_left3/right3 through the 4-way nesting, by_cases on tp[p+d]=b + if_pos/if_neg, chain reachIn_add. NEXT: prepend markCarry3 (lay anchor+carry b) → self-contained bitCompareAtDist3, then key-field loop, rule-match loop, apply3, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Probe` |
| **Entry 404 — universal-TM table: self-contained single-bit compare `bitCompareAtDist3` (PROVED)** | probe3 with its own anchor management. **`bitCompareAtDist3 s sEq sNe d`** := markCarry3 s (s+1)(s+d+8) 0 ++ probe3 (s+1) sEq sNe d O ++ probe3 (s+d+8) sEq sNe d I. **`bitCompareAtDist3_run`** (PROVED first try: bit cell tp[p]∈{O,I}, d≥1, no M in (p,p+d], p<length, p+d<length ⇒ ∃N reachIn N (s,p,tp) ((if tp[p+d]=tp[p] then sEq else sNe), p, tp), tape RESTORED). markCarry3 reads config bit into O/I lineage + lays M; matching probe3 lineage tests rule bit vs constant. KEY tape algebra: tp'=writeAt3 tp p M; writeAt3_getD (off-position p+d≠p ⇒ tp'[p+d]=tp[p+d]); writeAt3_writeAt3/391 collapses writeAt3 tp' p b = writeAt3 tp p b; writeAt3_id_of_lt ⇒ =tp (since b=tp[p]); inline writeAt3_length_eq. Lift markCarry/probe via append_left3/right3, reachIn_add. NEXT: loop over a whole unary key FIELD (compare config key vs rule key bit-by-bit), rule-table MATCH loop, apply3, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3BitCompare` |
| **Entry 405 — universal-TM table: three-way field-loop body `fieldStep3` (PROVED)** | bitCompareAtDist3 REFINED so the two agreeing cases get distinct targets — the genuine field-compare LOOP BODY. **`fieldStep3 s sCont sMatch sFail d`** := markCarry3 s (s+1)(s+d+8) 0 ++ probe3 (s+1) sMatch sFail d O ++ probe3 (s+d+8) sCont sFail d I. **`fieldStep3_run`** (PROVED first try: bit cell tp[p]∈{O,I}, d≥1, no M in (p,p+d], p<length, p+d<length ⇒ ∃N reachIn N (s,p,tp) (end,p,tp), tape RESTORED) where end = if tp[p]=O then (if tp[p+d]=O then sMatch else sFail) else (if tp[p+d]=I then sCont else sFail)). THREE-WAY routing decides field termination: config-O (separator) → sMatch (rule O, fields ended together) / sFail (rule I, config shorter); config-I (a one) → sCont (rule I, both continue) / sFail (rule O, rule shorter). bitCompareAtDist3 is the special case sCont=sMatch=sEq, sFail=sNe. Proof mirrors entry 404 (rcases config bit, markCarry3_run_O/I into the matching probe3 lineage, collapse double-write, lift append_left3/right3 + reachIn_add) but reduces the OUTER if (if_pos rfl / if_neg by decide) since targets differ per lineage. NEXT: advance head to p+1 on sCont, induct over field length ⇒ full fieldCompare3 loop. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3FieldStep` |
| **Entry 406 — universal-TM table: full unary field compare `fieldCompare3` (PROVED)** | The WHOLE key-field compare, the field loop isolated as a clean theorem. **`fieldCompare3 s sMatch sFail d`** : 0↦[], L+1↦fieldStep3 s (s+2d+15) sMatch sFail d ++ moveRight3 (s+2d+15)(s+2d+16) ++ fieldCompare3 (s+2d+16) sMatch sFail d L (continue target = advance state, then recurse; STRIDE=2d+16). **`fieldCompare3_run`** (PROVED) — no M anywhere on tp (hM), d≥1, c+a+d<length, budget min a b<L, config field = a ones then O at c, rule field = b ones then O at c+d ⇒ ∃N reachIn N (s,c,tp) ((if a=b then sMatch else sFail), c+min a b, tp): routes to sMatch IFF the two unary fields are equal, head ends at c+min a b, tape RESTORED. INDUCTION on budget L (s,c,a,b generalized): zero vacuous (min<0); succ does fieldStep3 at p=c (bounds all from hbnd+hd; hbit from cfg field by cases on a; hno=hM), then 4-way rcases (Nat.eq_zero_or_pos a, then b): a=0/b=0→sMatch, a=0/b>0 & a>0/b=0→sFail (terminate: reduce END via if_pos/if_neg, lift hFS via append_left3×2), a>0/b>0→continue (reduce END to sCont, moveRight3 advance, IH at (c+1,a-1,b-1) with same d; reduces (a,b)→(a-1,b-1); chain reachIn_seq3×2). GOTCHA: rewriting (a-1=b-1)↔(a=b) inside an `if` needs `simp only` not `rw` (decidable-instance motive). Head=c+min a b in ALL cases (terminate min=0; continue (c+1)+min(a-1,b-1)=c+min a b via omega). NEXT: rule-table MATCH loop (scan to each record key, fieldCompare3 vs config key, jump on fail / apply on match), apply3, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3FieldCompare` |
| **Entry 407 — universal-TM table: single-record key matcher `recordKeyMatch3` (PROVED)** | The per-record "does THIS rule fire?" decision, composing the field loop (406) + single-bit compare (404). A transition record = encodeNatBits3 state ++ boolToSym3 read :: RHS (393), so the KEY = unary state field + one symbol cell. **`recordKeyMatch3 base recMatch recFail d L`** := fieldCompare3 base M₀ recFail d L ++ moveRight3 M₀ (M₀+1) ++ bitCompareAtDist3 (M₀+1) recMatch recFail d, where M₀=base+L*(2d+16) is the first state past the field-compare corridor. **`recordKeyMatch3_run`** (PROVED) — no M on tp (hM), d≥1, budget min a b<L, bound c+a+1+d<length, config state field=a ones then O at c, rule state field=b ones then O at c+d, symbol cells cs at c+a+1 (∈{O,I}) and rs at c+d+b+1 ⇒ ∃N q reachIn N (base,c,tp) ((if a=b ∧ rs=cs then recMatch else recFail), q, tp), tape RESTORED. KEY GEOMETRY: once state fields equal (a=b) the symbol cells sit at the SAME distance d (config c+a+1, rule c+d+b+1=(c+a+1)+d), so the bit compare reuses d. Proof: fieldCompare3_run; by_cases a=b; match→if_pos hab collapses position to c+a, moveRight3 onto c+a+1, bitCompareAtDist3_run with hbit from hcsym+hcs (rs need NOT be a valid bit — dropped that hyp, stronger), rewrite symbol condition via simp only [hcsym,hRsym'], chain reachIn_seq3×2; fail→if_neg hab→recFail, lift append_left3×2; goal condition reduced via if_neg(fun h=>hab h.1) / simp only iff. q existential (match q=c+a+1, fail q=c+min a b). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_recordKeyMatch3_run_frontier. NEXT: rule-table MATCH loop (on recFail scan to next record + re-run matcher; on recMatch enter apply), apply3, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3KeyMatch` |
| **Entry 408 — universal-TM table: config-home reset `resetToHome3` (PROVED)** | The inter-record head reset the table-match loop needs. Each recordKeyMatch3 (407) leaves the head at a DATA-DEPENDENT config-side position (c+min(a,bᵢ) or c+a+1); a data-dependent left move can't be a fixed corridor — needs a marker SEEK. With a persistent home marker at home=c-1, **`resetToHome3 s found cont out`** := seekMarkLeft s found cont ++ moveRight3 found out. **`resetToHome3_run`** (PROVED) — home marker at home (tp[home]=M), no M in (home,home+d], home+d<length ⇒ ∃N reachIn N (s,home+d,tp) (out,home+1,tp): from any config-window head position home+d, return to c=home+1, tape identical. Proof: seekMarkLeft_run (entry 387, distance-independent left seek to marker) + moveRight3 onto home+1, chain reachIn_seq3. [propext,Quot.sound], no sorry. Wired nw_resetToHome3_run_frontier. NOTE: the loop using this must maintain the home marker, so the matcher's GLOBAL no-marker hyp (hM) must become WINDOWED (no M only in [c,c+a+d]) so the home cell c-1 is tolerated — that refactor + matchTable3 assembly is entry 409+. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ResetHome` |
| **Entry 409 — universal-TM table: windowed matcher (PROVED)** | The hypothesis-windowing that lets the table loop (410) keep a persistent home marker at c-1. 406/407 assume NO marker ANYWHERE (hM : ∀ j, tp[j]≠M), which a home marker violates. This re-proves both runs with the no-marker hyp WINDOWED to the cells actually shuttled. **`fieldCompare3_run_windowed`** (PROVED) — fieldCompare3_run (406) with hM → `∀ j, c<j → j≤c+a+d → tp[j]≠M`, carried INSIDE the ∀ L s c a b (it shifts c→c+1, shrinks as the field is consumed; threaded through the IH). **`recordKeyMatch3_run_windowed`** (PROVED) — recordKeyMatch3_run (407) with hM → `∀ j, c<j → j≤c+a+1+d → tp[j]≠M` (covers both the field-compare window (c,c+a+d] and the symbol-compare window (c+a+1,c+a+1+d]). Same machines, WEAKER hyp ⇒ strictly stronger theorems. Proofs mirror 406/407 verbatim except: hno derived from hWin (windowed) not global; the IH gets the shrunk window `fun j hj1 hj2 => hWin j (by omega)(by omega)`; recordKeyMatch passes fieldCompare3's window via `fun j hj1 hj2 => hWin j hj1 (by omega)`. KEY DESIGN NOTE: the window MUST be inside the ∀ for fieldCompare (depends on c,a which the induction changes); it's an ordinary hyp for recordKeyMatch (no induction). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_recordKeyMatch3_run_windowed_frontier. NEXT (entry 410): assemble matchTable3 — list-recursive corridor, recordKeyMatch3_run_windowed per record (config@c, dᵢ) → recMatch exits to apply / recFail → resetToHome3 (408) → next record. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3KeyMatchWin` |
| **Entry 410 — universal-TM table: rule-table match loop `matchTable3` (PROVED)** | The full rule lookup. Walks the encoded transition list; per record decide its key vs config key (recordKeyMatch3 windowed/409), mismatch → resetToHome3 (408) back to config home → next record, match → exit recMatch. Records modelled as descriptors (d,b,rs). **`RecMatch a cs (d,b,rs)`** := a=b ∧ rs=cs. **`RecOK tp c a L (d,b,rs)`** := 1≤d ∧ min a b<L ∧ c+a+1+d<length ∧ record state field (b ones then O at c+d) ∧ symbol rs at c+d+b+1. **`matchTable3 recMatch L`** : base,[]↦[]; base,(d,_,_)::rest ↦ recordKeyMatch3 base recMatch RF d L ++ resetToHome3 RF (RF+1)(RF+2) nextBase ++ matchTable3 recMatch L nextBase rest, RF=base+L*(2d+16)+2d+16, nextBase=RF+3. **`matchTable3_run`** (PROVED) — home marker @ c-1 (hmark), no M elsewhere (hclean), config content (a,cs), ∀ rec∈recs RecOK, ∃ rec∈recs RecMatch ⇒ ∃N q reachIn N (base,c,tp) (recMatch,q,tp). INDUCTION on recs: nil vacuous (∃ in [] false); cons by_cases head matches — match→if_pos→recMatch, lift append_left3×2; mismatch→if_neg→recFail, resetToHome3 from fail-pos q (needs 409's q-bound c≤q≤c+a+1 to discharge q<length & home≤q for the seek), then IH on tail (∃ moved to tail since head≠match), chain reachIn_seq3×2. PREREQ: strengthened 409 recordKeyMatch3_run_windowed conclusion with ∧ c≤q ∧ q≤c+a+1 (both fail c+min a b and match c+a+1 positions satisfy). HONEST: ∃-direction only (some applicable rule ⇒ match-found reachable, faithful for non-det toNTM3). Exhaustion (no rule⇒reject) + apply3 deferred. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_matchTable3_run_frontier. NEXT: apply3 (copy matched RHS into config), exhaustion direction, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MatchTable` |
| **Entry 411 — universal-TM table: single-cell copy `copyBitAtDist3` (PROVED)** | Foundational APPLY-phase brick, the copy-side analogue of bitCompareAtDist3 (404). KEY TRICK: marker-free — read source bit without modifying it (readCarry3 writes the read symbol back, branches O/I), walk right d, write carried bit at dest, walk BACK the fixed d cells (moveLeftN3) — no seek (return distance is exactly d, known). Only dest changes. **`moveLeftN3 s n`** (mirror moveRightN3): 0↦[], n+1↦moveLeft3 s (s+1)++moveLeftN3 (s+1) n. **`moveLeftN3_run`** (PROVED): n≤j, j<length ⇒ reachIn n (s,j,tp)(s+n,j-n,tp). **`readCarry3 s sO sI`** := [((s,O),(sO,O,stay)),((s,I),(sI,I,stay)),((s,M),(sO,M,stay))]. **`readCarry3_run_O/I`** (PROVED): j<length ⇒ tape+head unchanged, control routed (writeAt3 tp j (read) = tp via writeAt3_id_of_lt + hh▸). **`copyArm3 s sOut d b`** := moveRightN3 s d ++ unmark3 (s+d)(s+d+1) b ++ moveLeftN3 (s+d+1) d ++ unmark3 (s+2d+1) sOut b. **`copyArm3_run`** (PROVED): tp[p]=b, d≥1, p<len, p+d<len ⇒ ∃N reachIn N (s,p,tp)(sOut,p,writeAt3 tp (p+d) b). GOTCHA: hfix (writeAt3 (writeAt3 tp (p+d) b) p b = writeAt3 tp (p+d) b) via `have hid := writeAt3_id_of_lt …; rwa [hp1] at hid` (NOT hp1▸ which rewrites the wrong b — inner b inside tp1 aliases). **`copyBitAtDist3 s sOut d`** := readCarry3 s (s+1)(s+2d+3) ++ copyArm3 (s+1) sOut d O ++ copyArm3 (s+2d+3) sOut d I. **`copyBitAtDist3_run`** (PROVED): bit∈{O,I}, d≥1, p<len, p+d<len ⇒ ∃N reachIn N (s,p,tp)(sOut,p,writeAt3 tp (p+d)(tp.getD p O)): copies tp[p]→p+d, head back at p, source unchanged. rcases bit; O→readCarry_O+copyArm O lifted append_left3; I→readCarry_I+copyArm I lifted via append_left3×2+append_right3+reachIn_add. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_copyBitAtDist3_run_frontier. NEXT: loop copyBit over a unary field (new-state copy, copyField3 ≈ fieldCompare3 analogue), symbol write + head move, assemble apply3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CopyBit` |
| **Entry 412 — universal-TM table: unary field copy `copyField3` (PROVED)** | The new-state copy of the apply phase — loops copyBitAtDist3 (411) over a whole unary field, copy-side analogue of fieldCompare3 (406). **`copyStep3 s sCont sDone d`** := readCarry3 s (s+1)(s+2d+3) ++ copyArm3 (s+1) sDone d O ++ copyArm3 (s+2d+3) sCont d I — refines copyBitAtDist3 so I→sCont (one, continue), O→sDone (separator, stop). **`copyStep3_run`** (PROVED): bit∈{O,I}, d≥1, p/p+d<len ⇒ ∃N reachIn N (s,p,tp)((if tp[p]=O then sDone else sCont),p,writeAt3 tp (p+d)(tp.getD p O)). **`copyBlock tp c d`** : 0↦writeAt3 tp (c+d)(tp.getD c O), m+1↦copyBlock (writeAt3 tp (c+d)(tp.getD c O)) (c+1) d m — the copied tape, DEFINED IN LOOP ORDER so induction aligns (no write-commuting). **`copyField3 s sDone d`** : 0↦[], L+1↦copyStep3 s (s+4d+5) sDone d ++ moveRight3 (s+4d+5)(s+4d+6) ++ copyField3 (s+4d+6) sDone d L (STRIDE 4d+6). **`copyField3_run`** (PROVED): m<L (budget), m<d (source/dest DISJOINT — no overlap), c+m+d<len, field = m ones then O at c ⇒ ∃N reachIn N (s,c,tp)(sDone, c+m, copyBlock tp c d m): reproduces the field at c+d..c+m+d, source unchanged, head c+m. KEY: tp GENERALIZED inside the ∀ (copy MODIFIES the tape, unlike fieldCompare3 which preserved it) → recursion runs on tp1=writeAt3 tp (c+d)(tp.getD c O). Induction on L; cases m: 0→copyStep O→sDone, copyBlock 0 def-matches; m'+1→copyStep I→sCont(=advance), moveRight3, IH on tp1 at (c+1,m'), copyBlock tp1 (c+1) d m' = copyBlock tp c d (m'+1) by def, chain reachIn_seq3×2. Field content of tp1 via writeAt3_getD off-position (m<d ⇒ writes at c+d beyond field). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_copyField3_run_frontier. Rightward only (source left of dest); leftward variant + symbol write + head move + apply3 next. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CopyField` |
| **Entry 413 — universal-TM table: config layout & symbol write `configEncode3` (PROVED)** | PINS the simulated-config encoding (deferred design decision) + proves the apply phase's symbol write. **`configEncode3 q sym rest`** := encodeNatBits3 q ++ boolToSym3 sym :: rest (current state unary, then current-symbol cell, then rest of simulated tape — CONSISTENT with the rule key encodeTransBits3/393). On tape pre++configEncode3 q sym rest (pre.length=c): state field c..c+q (q ones, O sep at c+q), symbol cell at c+q+1 — exactly the matcher's positions (a=q, sym at c+a+1). **`configEncode3_content`** (PROVED) — bridge to matcher: (∀i<q, getD(c+i)=I) ∧ getD(c+q)=O ∧ getD(c+q+1)=boolToSym3 sym, via field_content3 (396) + getD_append_shift3. **`configEncode3_set_sym`** (PROVED) — list surgery: writeAt3 (pre++configEncode3 q sym rest) (c+q+1) (boolToSym3 sym') = pre++configEncode3 q sym' rest. Proof: writeAt3_eq_set (in-bounds write = List.set, private helper), append_assoc to expose boundary, set_at_boundary (private: (A++(x::l)).set A.length y = A++(y::l), induction on A) at index (pre++encodeNatBits3 q).length. **`writeSym3_run`** (PROVED) — head at c+q+1, one unmark3 step: reachIn 1 (s, c+q+1, pre++configEncode3 q sym rest) (s', c+q+1, pre++configEncode3 q sym' rest) — symbol sym→sym', rest untouched (unmark3_run + set_sym surgery). After matcher match head is ALREADY at c+q+1 (bitCompareAtDist3 anchor), so write is one step. [propext,Quot.sound], no sorry. Wired nw_writeSym3_run_frontier. NEXT: carry write symbol from matched rule (a copyBit), move simulated head, assemble apply3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Config` |
| **Entry 414 — universal-TM table: leftward single-cell copy `copyBitAtDistLeft3` (PROVED)** | MIRROR of copyBitAtDist3 (411), the direction apply needs: config sits LEFT of the rule table (matcher: config@c, rule@c+d), so apply copies RIGHT→LEFT. Read source bit marker-free (readCarry3), walk LEFT d (moveLeftN3), write at p-d, walk back RIGHT the fixed d (moveRightN3) — no seek. **`copyArmLeft3 s sOut d b`** := moveLeftN3 s d ++ unmark3 (s+d)(s+d+1) b ++ moveRightN3 (s+d+1) d ++ unmark3 (s+2d+1) sOut b. **`copyArmLeft3_run`** (PROVED): tp[p]=b, 1≤d, d≤p, p<len ⇒ ∃N reachIn N (s,p,tp)(sOut,p,writeAt3 tp (p-d) b). Same hfix (rwa [hp1] at hid). **`copyBitAtDistLeft3 s sOut d`** := readCarry3 s (s+1)(s+2d+3) ++ copyArmLeft3 (s+1) sOut d O ++ copyArmLeft3 (s+2d+3) sOut d I. **`copyBitAtDistLeft3_run`** (PROVED): bit∈{O,I}, 1≤d, d≤p (room to go left), p<len ⇒ ∃N reachIn N (s,p,tp)(sOut,p,writeAt3 tp (p-d)(tp.getD p O)): copies tp[p]→p-d, head back p, source unchanged. Compiled FIRST TRY (exact left/right swap of 411). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_copyBitAtDistLeft3_run_frontier. NEXT: leftward field copy (copyFieldLeft3), carry specific rule fields (write sym, new state) into config, simulated head move, apply3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CopyBitLeft` |
| **Entry 415 — universal-TM table: leftward unary field copy `copyFieldLeft3` (PROVED)** | New-state copy in the apply direction — loops copyBitAtDistLeft3 (414) over a unary field, leftward mirror of copyField3 (412). **`copyStepLeft3 s sCont sDone d`** := readCarry3 s (s+1)(s+2d+3) ++ copyArmLeft3 (s+1) sDone d O ++ copyArmLeft3 (s+2d+3) sCont d I (I→sCont, O→sDone). **`copyStepLeft3_run`** (PROVED): bit∈{O,I}, 1≤d, d≤p, p<len ⇒ ∃N reachIn N (s,p,tp)((if tp[p]=O then sDone else sCont),p,writeAt3 tp (p-d)(tp.getD p O)). **`copyBlockLeft tp c d`** : 0↦writeAt3 tp (c-d)(tp.getD c O), m+1↦copyBlockLeft (writeAt3 tp (c-d)(tp.getD c O)) (c+1) d m (loop order). **`copyFieldLeft3 s sDone d`** : 0↦[], L+1↦copyStepLeft3 s (s+4d+5) sDone d ++ moveRight3 (s+4d+5)(s+4d+6) ++ copyFieldLeft3 (s+4d+6) sDone d L (SAME corridor as copyField3 — source advances RIGHT, only copy direction differs). **`copyFieldLeft3_run`** (PROVED): m<L (budget), 1≤d, d≤c (LEFT ROOM), c+m<len, field=m ones then O at c ⇒ ∃N reachIn N (s,c,tp)(sDone,c+m,copyBlockLeft tp c d m): field reproduced at c-d..c+m-d, head c+m. SIMPLIFICATION vs 412: leftward needs only d≤c, NOT m<d — the dest (c+i-d) is always BEHIND the advancing source pointer (c+i), so writes never touch a future-read cell; field content preserved for ANY d (writes go left of the remaining field c+1..c+m). tp generalized inside ∀ (modifies tape); induction on L, cases m, chain reachIn_seq3×2. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_copyFieldLeft3_run_frontier. NEXT: carry specific rule RHS fields (write sym already done @413, new-state field via this) into config, simulated head move, apply3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CopyFieldLeft` |
| **Entry 416 — universal-TM table: unary head-pointer step `unaryInc3`/`unaryDec3` (PROVED)** | DESIGN FORK RESOLVED (user picked explicit unary head-pointer field): config = state | symCache | headPtr(unary) | tape(O/I block); a simulated head MOVE = inc/dec the headPtr field. With the field padded by reserved O cells, inc/dec are SINGLE cell flips: inc n→n+1 = flip separator h+n O→I (reserved h+n+1 becomes new sep); dec n→n-1 = flip last-one h+n-1 I→O (old sep h+n stays). **`unaryInc3 s s'`** := scanNatFrom3 s (s+1) ++ moveLeft3 (s+1)(s+2) ++ unmark3 (s+2) s' I (scan to end, step left onto sep, write I). **`unaryInc3_run`** (PROVED): field n ones+O at h, h+n+1<len ⇒ ∃N reachIn N (s,h,tp)(s', h+n, writeAt3 tp (h+n) I). **`unaryDec3 s s'`** := scanNatFrom3 ++ moveLeft3 ++ moveLeft3 ++ unmark3 s' O (scan, left TWICE onto last one, write O). **`unaryDec3_run`** (PROVED): n≥1, field, h+n+1<len ⇒ ∃N reachIn N (s,h,tp)(s', h+n-1, writeAt3 tp (h+n-1) O). **`unaryInc3_field`/`unaryDec3_field`** (PROVED): content corollaries — result is a unary field of length n+1 (given h+n+1 reserved O) resp. n-1, via writeAt3_getD case split. scanNatFrom3 lands past sep (h+n+1), moveLeft backs onto target; chain reachIn_seq3. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_unaryInc3/Dec3_run_frontier. NEXT: refresh cached current symbol from tape[headPtr] after a move, assemble apply3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3UnaryStep` |
| **Entry 417 — universal-TM table: distant-cell fetch `fetchBitFromDist3` (PROVED)** | The apply's READ-FAR/WRITE-NEAR primitive — mirror of the matcher's probe3 (which reaches a far cell but only COMPARES). Brings rule/tape values INTO the config (cache refresh, write-symbol carry). **`fetchArm3 s sOut d`** := moveRightN3 s d ++ testBit3 (s+d)(s+d+1)(s+d+2) O ++ probeTail3 (s+d+1)(s+d+3)(s+d+4) sOut O ++ probeTail3 (s+d+2)(s+d+5)(s+d+6) sOut I — like probe3 but the two tails write the READ value (O resp I) not a restored constant. **`fetchArm3_run`** (PROVED): anchor M at p, no M in (p,p+d], p+d<len, tp[p+d]∈{O,I} ⇒ ∃N reachIn N (s,p,tp)(sOut,p,writeAt3 tp p (tp.getD (p+d) O)). Mirrors probe3_run (set A/B/C/D, lift via append_left3/right3, by_cases tp[p+d]=O / if_pos-if_neg / probeTail3_run each tail, chain reachIn_add); ne-case uses hfar.resolve_left to get =I. **`fetchBitFromDist3 s sOut d`** := markCarry3 s (s+1)(s+1) 0 ++ fetchArm3 (s+1) sOut d (markCarry both lineages→s+1, local value discarded). **`fetchBitFromDist3_run`** (PROVED): p∈{O,I}, d≥1, no M in (p,p+d], p+d<len, tp[p+d]∈{O,I} ⇒ ∃N reachIn N (s,p,tp)(sOut,p,writeAt3 tp p (tp.getD (p+d) O)): cell p ← tp[p+d], source unchanged. markCarry lays M (writeAt3 tp p M); fetchArm on the marked tape; collapse writeAt3(writeAt3 tp p M) p (tp[p+d]) = writeAt3 tp p (tp[p+d]) via writeAt3_getD off-pos + writeAt3_writeAt3. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_fetchBitFromDist3_run_frontier. NEXT: data-dependent walk to tape[headPtr] (cache refresh — the hard addressing piece), then apply3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Fetch` |
| **Entry 418 — universal-TM table: counter-driven walk `walkRightClearField3` (PROVED)** | First genuine piece of unary head-pointer DATA-DEPENDENT ADDRESSING: walk the head right by an amount given by a unary tally, CONSUMING it (vs scanNatFrom3 which walks non-destructively). **`walkStep3 s sCont sDone`** := [((s,I),(sCont,O,right)),((s,O),(sDone,O,stay)),((s,M),(sDone,M,stay))] — on I clear to O + move right + →sCont; on O (sep) stay + →sDone. **`walkStep3_run_I`** (PROVED): tp[j]=I ⇒ reachIn 1 (s,j,tp)(sCont,j+1,writeAt3 tp j O). **`walkStep3_run_O`** (PROVED): tp[j]=O, j<len ⇒ reachIn 1 (s,j,tp)(sDone,j,tp) (write O at O = identity via h▸writeAt3_id_of_lt). **`clearBlock tp h`** : 0↦tp, m+1↦clearBlock (writeAt3 tp h O) (h+1) m (loop order). **`walkRightClearField3 s sDone`** : 0↦[], L+1↦walkStep3 s (s+1) sDone ++ walkRightClearField3 (s+1) sDone L (STRIDE 1 — walkStep_I already moves right, so no separate advance). **`walkRightClearField3_run`** (PROVED): tally m ones+O at h, m<L (budget), h+m<len ⇒ ∃N reachIn N (s,h,tp)(sDone, h+m, clearBlock tp h m): head moves right by m, tally cleared. Induction on L, cases m (0→walkStep_O→done; m'+1→walkStep_I clears+advances, IH on writeAt3 tp h O at (h+1,m'), chain reachIn_seq3). tp generalized inside ∀ (modifies tape). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_walkRightClearField3_run_frontier. HONEST: a data-dependent walk component; full cache refresh must still relate the headPtr count to the tape offset (tape[m] at H+2m+1). NEXT: cache-refresh wiring, apply3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Walk` |
| **Entry 419 — universal-TM table: marker-on-current-cell head move `headMoveRight3`/`headMoveLeft3` (PROVED)** | HEAD REPRESENTATION SWITCHED (user's call): head = marker M on the current cell. DECISIVE WIN over the unary-pointer (416/418): a head MOVE is now LOCAL (swap M with its neighbour symbol) — NO data-dependent walk. And it REUSES the proven single-cell copies. **`headMoveRight3 s sOut`** := moveRight3 s (s+1) ++ copyBitAtDistLeft3 (s+1)(s+10) 1 ++ unmark3 (s+10) sOut M. Right move from M@p: step onto p+1, carry c=tp[p+1] one cell LEFT onto the old marker p (copyBitAtDistLeft3 d=1 → p←c, p+1 still c), write M at p+1. **`headMoveRight3_run`** (PROVED): tp[p+1]∈{O,I}, p+1<len ⇒ ∃N reachIn N (s,p,tp)(sOut, p+1, writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M): M moves p→p+1, p←neighbour symbol, head ends on new M. **`headMoveLeft3 s sOut`** := moveLeft3 s (s+1) ++ copyBitAtDist3 (s+1)(s+10) 1 ++ unmark3 (s+10) sOut M (mirror, carry RIGHT via copyBitAtDist3). **`headMoveLeft3_run`** (PROVED): tp[p-1]∈{O,I}, 1≤p, p<len ⇒ ∃N reachIn N (s,p,tp)(sOut, p-1, writeAt3 (writeAt3 tp p (tp.getD (p-1) O)) (p-1) M). Proofs: moveRight/Left3_run + copyBitAtDist(Left)3_run (411/414) + unmark3_run, chain reachIn_seq3×2; (p±1)∓1=p rewrite. Compiled FIRST TRY. The non-M cell sequence is preserved (M is a position marker between cells). Head ends on the new M (good for chaining). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_headMove{Right,Left}3_run_frontier. NEXT: rework matcher to read current symbol at the marker (seekMarkRight, data-independent), assemble apply3. The unary headPtr (416/418) is now superseded for the head; kept as proven components. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3HeadMove` |
| **Entry 420 — universal-TM table: read current symbol at marker `readHeadSym3` (PROVED)** | The BRIDGE reconnecting the marker head rep (419) to the matcher/apply: read the current symbol distance-independently. M marks the head; current symbol = cell right after M. **`readHeadSym3 s sO sI`** := seekMarkRight s (s+1)(s+2) ++ moveRight3 (s+1)(s+3) ++ readCarry3 (s+3) sO sI — seek RIGHT to M (388, data-independent), step onto current cell, branch O→sO/I→sI (411). **`readHeadSym3_run`** (PROVED): marker at h+d (no M in [h,h+d)), h+d<len, h+d+1<len, current cell h+d+1∈{O,I} ⇒ ∃N reachIn N (s,h,tp)((if tp.getD (h+d+1) O = O then sO else sI), h+d+1, tp): head ends ON the current cell, control routed on the current symbol, tape IDENTICAL. Proof: seekMarkRight_run + moveRight3_run_eq + readCarry3_run_O/I (rcases current bit, if_pos/if_neg), chain reachIn_seq3×2. readSym3 (s+3,h+d+1,tp) = tp.getD (h+d+1) O by rfl. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_readHeadSym3_run_frontier. No fixed-offset cache needed — this is why the marker rep is clean (vs unary-pointer's data-dependent addressing). NEXT: rework matcher's current-symbol read to use this, assemble apply3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ReadHead` |
| **Entry 421 — universal-TM table: apply tape step `applyStep3` (PROVED)** | The TAPE TRANSFORMATION of one simulated step under the marker head rep: write the rule's symbol at the current cell + move the head. M at q, current cell q+1. **stay** **`writeSymAtHead3 s sOut w`** := moveRight3 s (s+1) ++ unmark3 (s+1)(s+2) w ++ moveLeft3 (s+2) sOut. **`writeSymAtHead3_run`** (PROVED): q+1<len ⇒ ∃N reachIn N (s,q,tp)(sOut, q, writeAt3 tp (q+1) w): write w at current cell, head back at marker. **right** **`moveRightWrite3 s sOut w`** := unmark3 s (s+1) w ++ moveRight3 (s+1)(s+2) ++ unmark3 (s+2) sOut M. **`moveRightWrite3_run`** (PROVED): q+1<len ⇒ ∃N reachIn N (s,q,tp)(sOut, q+1, writeAt3 (writeAt3 tp q w) (q+1) M): CLEAN 2-cell result q←w, q+1←M (TM-correct: written symbol sits left of new head). KEY: direct construction avoids the writeAt3-commute nesting (write w at q then M at q+1, sequential different cells). **left** **`moveLeftWrite3 s sOut w`** := writeSymAtHead3 s (s+3) w ++ headMoveLeft3 (s+3) sOut. **`moveLeftWrite3_run`** (PROVED): 1≤q, q+1<len, tp[q-1]∈{O,I} ⇒ ∃N reachIn N (s,q,tp)(sOut, q-1, writeAt3 (writeAt3 (writeAt3 tp (q+1) w) q (tp.getD (q-1) O)) (q-1) M) (3-cell: q-1←M, q←old(q-1), q+1←w). All chain reachIn_seq3, reuse moveRight/Left3, unmark3, headMoveLeft3 (419). [propext,(Classical.choice for left),Quot.sound], no sorry. Wired nw_{moveRightWrite3,writeSymAtHead3,moveLeftWrite3}_run_frontier. NEXT: update simulated STATE field (field copy), wire write-sym/direction from matched rule, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Apply` |
| **Entry 422 — universal-TM table: unary-field writer `writeUnaryFieldN3` (PROVED)** | The OUTPUT dual of the field scanner (395) / clearer (418): lay down encodeNatBits3 n (n ones then O sep) at the head — the primitive for EMITTING encoded fields toward EmitsEncodedStep3. **`writeIMove3 s s'`** := [((s,_),(s',I,right))×3] writes I, moves right. **`writeIMove3_run`** (PROVED): reachIn 1 (s,j,tp)(s', j+1, writeAt3 tp j I). **`writeFieldBlock tp j`** : 0↦writeAt3 tp j O, n+1↦writeFieldBlock (writeAt3 tp j I)(j+1) n (loop order: j..j+n-1←I, j+n←O). **`writeUnaryFieldN3 s sOut`** : 0↦unmark3 s sOut O, n+1↦writeIMove3 s (s+1) ++ writeUnaryFieldN3 (s+1) sOut n. **`writeUnaryFieldN3_run`** (PROVED): ∃N reachIn N (s,j,tp)(sOut, j+n, writeFieldBlock tp j n): writes the unary field of length n at j, head on the separator. Induction on n generalizing s/j/tp (writeAt3 modifies tape), chain reachIn_seq3; (j+1)+n=j+(n+1) rewrite. No length bound (writeAt3 extends, moveHead always advances). Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_writeUnaryFieldN3_run_frontier. HONEST: writes a GIVEN-length field; the simulated STATE update transfers a field whose length is read from the matched rule = data-dependent copy (415) after clear (418) — that wiring + EmitsEncodedStep3 = cross-region addressing integration, remaining. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3WriteField` |
| **Entry 423 — universal-TM table: field-then-bit emitter `writeFieldBit3` (PROVED)** | First OUTPUT-side composition for encoded structures: lay down encodeNatBits3 n ++ [w] (unary field then a symbol cell) = the transition/config prefix shape (cf encodeTransBits3/393, configEncode3/413). **`writeFieldBlock_length`** (PROVED): j+n<len ⇒ (writeFieldBlock tp j n).length = tp.length (the 422 writer stays in bounds; induction on n generalizing j/tp via writeAt3_length_eq). **`writeFieldBit3 s sOut n w`** := writeUnaryFieldN3 s (s+n+1) n ++ moveRight3 (s+n+1)(s+n+2) ++ unmark3 (s+n+2) sOut w. **`writeFieldBit3_run`** (PROVED): j+n<len ⇒ ∃N reachIn N (s,j,tp)(sOut, j+n+1, writeAt3 (writeFieldBlock tp j n) (j+n+1) w): writes the field at j then symbol w at j+n+1, head on the symbol cell. Chains writeUnaryFieldN3_run (422) + moveRight3 (over the sep, length lemma) + unmark3, reachIn_seq3×2. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_writeFieldBit3_run_frontier. NEXT: chain to emit a full encoded config, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3WriteFieldBit` |
| **Entry 424 — universal-TM table: writer emits the encoding `writeFieldBlock_eq_encode` (PROVED)** | WRITER-CORRECTNESS — the field writer's (422) output IS the abstract encoding encodeNatBits3 (392). The output-side link between the machine's tape and the encoding it must emit (toward EmitsEncodedStep3). **`writeFieldBlock_eq_encode`** (PROVED, pure list lemma): n < X.length ⇒ writeFieldBlock (pre ++ X) pre.length n = pre ++ encodeNatBits3 n ++ X.drop (n+1) — writing a unary field of length n at pre.length lays encodeNatBits3 n over the next n+1 cells, prefix & tail intact. Proof: induction on n generalizing pre/X; rcases X=x::xs; writeAt3_eq_set (in-bounds = List.set) + set_at_boundary ((A++x::l).set A.length y = A++y::l, induction on A); succ uses encodeNatBits3_succ (encodeNatBits3 (n+1) = I :: encodeNatBits3 n via replicate_succ) + reassoc pre++(I::xs)=(pre++[I])++xs, IH at (pre++[I], xs). **`writeUnaryFieldN3_emits`** (PROVED, run corollary): ∃N reachIn N (s, pre.length, pre++X)(sOut, pre.length+n, pre ++ encodeNatBits3 n ++ X.drop(n+1)) — running the writer EMITS the encoded field (422 run + the surgery rewrite). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_writeUnaryFieldN3_emits_frontier. NEXT: chain to emit a full encoded config, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3WriteEncode` |
| **Entry 425 — universal-TM table: emitter emits encoded prefix `writeFieldBit3_emits` (PROVED)** | The field-then-bit emitter (423) emits EXACTLY the encoded prefix encodeNatBits3 n ++ w :: rest (the transition/config prefix shape, cf 393/413). **`writeFieldBit3_emits`** (PROVED): n+1 < X.length ⇒ ∃N reachIn N (s, pre.length, pre++X)(sOut, pre.length+n+1, pre ++ encodeNatBits3 n ++ w :: X.drop(n+2)): running the emitter lays encodeNatBits3 n ++ [w] over the next n+2 cells, prefix & tail intact. Proof: writeFieldBit3_run (423) + writeFieldBlock_eq_encode (424, the field part = encoding) + List.drop_eq_getElem_cons (expose the symbol cell) + writeAt3_cons_boundary (boundary write of the symbol; writeAt3_eq_set + set_at_boundary privates); hidx rewrites the index to (pre++encodeNatBits3 n).length and back (avoid rewriting the head pos). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_writeFieldBit3_emits_frontier. NEXT: emit remaining fields → full encoded config, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3EmitPrefix` |
| **Entry 426 — universal-TM table: emitter emits a configuration `writeFieldBit3_emits_config` (PROVED)** | The CLEAN PAYOFF of the emit-side chain: the field-then-bit emitter's output IS the config encoding configEncode3 (413). **`writeFieldBit3_emits_config`** (PROVED): n+1 < X.length ⇒ ∃N reachIn N (s, pre.length, pre++X)(sOut, pre.length+n+1, pre ++ configEncode3 n sym (X.drop(n+2))): running writeFieldBit3 with w=boolToSym3 sym emits the ENCODED CONFIGURATION configEncode3 n sym _ (state field + current symbol + rest). Short corollary of 425: rw the emitted prefix (encodeNatBits3 n ++ boolToSym3 sym :: rest) = configEncode3 n sym rest via [configEncode3, List.append_assoc]. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_writeFieldBit3_emits_config_frontier. Output side now fully linked to the encoding (writer 422 → =encoding 424 → field-bit 423 → emits prefix 425 → emits configEncode3 426). NEXT: connect emitted config to the abstract simulated STEP + the input/transfer side, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3EmitConfig` |
| **Entry 427 — universal-TM table: transfer-side navigation `navigateToSource3` (PROVED)** | The CLEAN ADDRESSING SOLUTION for the apply transfer (the recurring cross-region wall): reach a DATA-DEPENDENT rule position WITHOUT a counter-driven walk. Combine resetToHome3 (408, distance-independent return to config home c via the home marker — absorbs the data-dependent "where is the head now") with a FIXED-OFFSET moveRightN3 d' (401, d' a machine PARAMETER, like the matcher took d) reaching the rule source c+d'. **`navigateToSource3 s found cont mid d'`** := resetToHome3 s found cont mid ++ moveRightN3 mid d'. **`navigateToSource3_run`** (PROVED): home marker at home, no M in (home,home+dq], home+dq<len, home+1+d'≤len ⇒ ∃N reachIn N (s, home+dq, tp)(mid+d', home+1+d', tp): from ANY config-window position home+dq, head ends at home+1+d' = c+d' (the rule source), tape IDENTICAL. Proof: resetToHome3_run (408) + moveRightN3_run (401), chain reachIn_seq3. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_navigateToSource3_run_frontier. KEY: this is how the transfer escapes the unary-pointer's data-dependent-addressing wall — home marker + parameterized offset, both proven. NEXT: copyFieldLeft3 (415) at the source → transfer the rule field into config, state transfer, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Navigate` |
| **Entry 428 — universal-TM table: full field transfer `transferFieldLeft3` (PROVED)** | The cross-region field transfer END TO END (the recurring wall, crossed): bring a unary field from the matched rule into the config. Composes navigateToSource3 (427, reach the data-dependent rule source c+d' from anywhere in the config window) + copyFieldLeft3 (415, copy the field leftward to the config at c). **`transferFieldLeft3 s found cont mid d' sDone L`** := navigateToSource3 s found cont mid d' ++ copyFieldLeft3 (mid+d') sDone d' L. **`transferFieldLeft3_run`** (PROVED): home marker at home, no M in (home,home+dq], m<L, 1≤d', home+1+d'+m<len, rule field (m ones+O at home+1+d') ⇒ ∃N reachIn N (s, home+dq, tp)(sDone, home+1+d'+m, copyBlockLeft tp (home+1+d') d' m): the rule field copied into config at home+1, from ANY config-window start. Proof: navigateToSource3_run (427) + copyFieldLeft3_run (415), chain reachIn_seq3; bounds via omega. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_transferFieldLeft3_run_frontier. The transfer wall is crossed (no counter-driven walk; home marker + parameterized offset + leftward copy, all proven). HONEST: doesn't yet clear the old config field before the copy (assumes clean/sized dest); full state update + EmitsEncodedStep3 next. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Transfer` |
| **Entry 429 — universal-TM table: clearer's content `clearBlock_getD_outside/_inside` (PROVED)** | CLEARER-CORRECTNESS — characterises walkRightClearField3's (418) clearBlock effect; dual of writer-correctness writeFieldBlock_eq_encode (424). Needed to clear the old config field before transfer (428) while preserving a distant rule field. **`clearBlock_getD_outside`** (PROVED): j<h ∨ h+m≤j ⇒ (clearBlock tp h m).getD j O = tp.getD j O (cells OUTSIDE [h,h+m) preserved). **`clearBlock_getD_inside`** (PROVED): h≤j → j<h+m ⇒ (clearBlock tp h m).getD j O = O (cells INSIDE cleared). Both by induction on m generalizing h/tp; succ peels writeAt3 tp h O via writeAt3_getD (if_neg outside / if_pos at j=h); inside's j=h case reuses outside (h is outside [h+1,h+1+m)). The OUTSIDE direction shows a distant field (rule's new-state field at home+1+d', d'≥oldlen) survives clearing the config field. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_clearBlock_getD_{outside,inside}_frontier. NEXT: clear-then-transfer state update (clear old config field 418 + transfer new 428, rule field survival via outside), EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ClearContent` |
| **Entry 430 — universal-TM table: state-field update `clearStateField3` (PROVED)** | THE COMPLETE SIMULATED-STATE UPDATE: replace the config's old state field with the matched rule's new-state field, HANDLING THE LENGTH CHANGE. Clears old (walkRightClearField3, 418) then transfers new (transferFieldLeft3, 428). **`clearStateField3 s sMid found cont mid d' sDone L1 L2`** := walkRightClearField3 s sMid L1 ++ transferFieldLeft3 sMid found cont mid d' sDone L2. **`clearStateField3_run`** (PROVED): home marker @ home, no M elsewhere (hclean), oldlen<d' (rule beyond cleared region), budgets, bounds, old config field (oldlen ones+O at home+1), rule field (newlen ones+O at home+1+d') ⇒ ∃N reachIn N (s, home+1, tp)(sDone, home+1+d'+newlen, copyBlockLeft (clearBlock tp (home+1) oldlen) (home+1+d') d' newlen): config state field cleared & replaced by a copy of the rule's new state. KEY: the two phases run on DIFFERENT tapes (transfer on the cleared tape); cross-tape obligations (home marker + distant rule field survive clearing; cleared cells read O so reset window is M-free) discharged by clearBlock_getD_outside/inside (429). Plus clearBlock_length (private, clearer preserves length). Chain reachIn_seq3 + align head home+(oldlen+1)=home+1+oldlen. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_clearStateField3_run_frontier. NEXT: sequence lookup→apply-tape-step→state-update into one simulated step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3StateUpdate` |
| **Entry 431 — universal-TM table: navigation to symbol cache `navigateToCache3` (PROVED)** | REP-UNIFICATION addressing: reconcile the matcher's fixed-offset symbol cache (cell after the state field, c+a+1) with the marker head rep — after a head move the cache needs refresh, which first means REACHING the cache cell from anywhere. **`navigateToCache3 s found cont mid sOut`** := resetToHome3 s found cont mid ++ scanNatFrom3 mid sOut. **`navigateToCache3_run`** (PROVED): home marker @ home, no M in (home,home+dq], state field (a ones+O at home+1), bounds ⇒ ∃N reachIn N (s, home+dq, tp)(sOut, home+1+a+1, tp): from ANY config-window position, head ends on the cache cell home+1+a+1 = c+a+1, tape IDENTICAL. KEY: reaches the FIXED-offset cache by SCANNING the DATA-length state field (scanNatFrom3 395 walks to the field sep without needing a statically) after resetToHome3 (408). Chain reachIn_seq3. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_navigateToCache3_run_frontier. NEXT: cache refresh (read marker symbol 420 + write here), full step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3NavigateCache` |
| **Entry 432 — universal-TM table: skip a marker leftward `skipMarkLeft3` (PROVED)** | RESOLVES A REAL COHERENCE CONFLICT: with single symbol M, the HOME marker (config nav, c-1) and the simulated HEAD marker (in tape) are indistinguishable, so a leftward seek from inside the tape stops at the head M not home M → navigateToCache3 (431) can't be called from inside the tape (head M blocks the seek). FIX: SKIP the head marker — seek to it, step one cell past it, so a SECOND leftward seek reaches home. **`skipMarkLeft3 s found cont s'`** := seekMarkLeft s found cont ++ moveLeft3 found s'. **`skipMarkLeft3_run`** (PROVED): marker at p, no M in (p,p+d], p+d<len ⇒ ∃N reachIn N (s, p+d, tp)(s', p-1, tp): from any position right of the marker, head ends just LEFT of it (p-1), tape IDENTICAL. Chain seekMarkLeft_run (387) + moveLeft3_run_eq (386) via reachIn_seq3. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_skipMarkLeft3_run_frontier. This is the single-symbol tool to navigate past the head marker (skipMarkLeft3 head-M → resetToHome3 home-M → scanNatFrom3 cache). NEXT: cache refresh = readHeadSym3 (420) + skip head M + navigateToCache3 + write; full step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3SkipMark` |
| **Entry 433 — universal-TM table: cache-refresh navigation `cacheRefreshNav3` (PROVED)** | The navigation half of the cache refresh: carry the head from INSIDE the tape (right of the head marker) to the cache cell, crossing the head marker (the two-M conflict, resolved). **`cacheRefreshNav3 s found1 cont1 s2 found2 cont2 mid sOut`** := skipMarkLeft3 s found1 cont1 s2 ++ navigateToCache3 s2 found2 cont2 mid sOut. **`cacheRefreshNav3_run`** (PROVED): head M @ hm (head @ hm+e right of it), home M @ home<hm, state field (a ones+O at home+1), no other M, bounds ⇒ ∃N reachIn N (s, hm+e, tp)(sOut, home+1+a+1, tp): from inside the tape head ends on the cache cell c+a+1, tape IDENTICAL. Proof: skipMarkLeft3_run (432, skip head M → hm-1) + navigateToCache3_run (431, from hm-1 = home+(hm-1-home) seek past to home M, scan state field to cache); rw home+(hm-1-home)=hm-1 to align; chain reachIn_seq3. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_cacheRefreshNav3_run_frontier. NEXT: write the read symbol into the cache (complete refresh), full step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CacheNav` |
| **Entry 434 — universal-TM table: navigate-and-write the cache `navigateAndWriteCache3` (PROVED)** | The WRITE end of the cache refresh: after cacheRefreshNav3 (433) carries the head from inside the tape to the cache cell, write a given symbol w there (unmark3 write-and-stay). **`navigateAndWriteCache3 s found1 cont1 s2 found2 cont2 mid sMidW sOut2 w`** := cacheRefreshNav3 s found1 cont1 s2 found2 cont2 mid sMidW ++ unmark3 sMidW sOut2 w. **`navigateAndWriteCache3_run`** (PROVED): under the cacheRefreshNav3 hyps ⇒ ∃N reachIn N (s, hm+e, tp)(sOut2, home+1+a+1, writeAt3 tp (home+1+a+1) w): from inside the tape, head ends on the cache cell and the cache cell now holds w. Proof: cacheRefreshNav3_run (433) + unmark3_run, chain reachIn_seq3. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_navigateAndWriteCache3_run_frontier. The symbol-parameterised cache-set; remaining glue = supply w from the marker symbol (readHeadSym3 420 O/I branch). NEXT: thread the O/I branch for the full refresh, full step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CacheWrite` |
| **Entry 435 — universal-TM table: full cache refresh `cacheRefresh3` (PROVED)** | THE REP-UNIFICATION OPERATION cache := current symbol, completing the matcher/marker reconciliation. Reads the symbol at the head marker (readHeadSym3 420, routes O/I), then per lineage navigates to the cache and writes it (navigateAndWriteCache3 434). TWO-BRANCH ASSEMBLY: appends BOTH lineage sub-machines after readHeadSym3; the relevant one runs, the other never entered (lifted into the union by reachIn_append_left3/right3). **`cacheRefresh3 s sO sI <7 O-internals> <7 I-internals> sFin`** := readHeadSym3 s sO sI ++ (navigateAndWriteCache3 sO … O ++ navigateAndWriteCache3 sI … I). **`cacheRefresh3_run`** (PROVED): head M @ h+d (no M in [h,h+d)), current cell a bit, home M @ home<h+d, state field @ home+1, bounds ⇒ ∃N reachIn N (s, h, tp)(sFin, home+1+a+1, writeAt3 tp (home+1+a+1) (tp.getD (h+d+1) O)): cache cell c+a+1 ← current symbol, BOTH lineages converge to sFin. Proof: readHeadSym3_run (420) routes to sO/sI; rcases the current bit; per branch navigateAndWriteCache3_run (434, hm=h+d e=1) + lift to the union (append_left for O / append_right for I) + reachIn_seq3; rw the read bit to identify the written symbol with the current symbol. hnoHead (k≤1: current cell ≠ M) from hcur. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_cacheRefresh3_run_frontier. NEXT: rework matcher to use the (now-coherent) cache; sequence read→lookup→apply→refresh into one simulated step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CacheRefresh` |
| **Entry 436 — universal-TM table: advance head right + return home `headRightReturnHome3` (PROVED)** | TOP-LEVEL SEQUENCING begins: the RETURN-TO-HOME DISCIPLINE that makes phases compose (each phase ends at the canonical config home c=home+1). **`headRightReturnHome3 s s1 f1 c1 s2 f2 c2 out`** := headMoveRight3 s s1 ++ skipMarkLeft3 s1 f1 c1 s2 ++ resetToHome3 s2 f2 c2 out. **`headRightReturnHome3_run`** (PROVED): home M @ home, head M @ p (home<p), no other M in (home,p), right neighbor p+1 a bit, p+1<len ⇒ ∃N reachIn N (s, p, tp)(out, home+1, writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M): head marker advances p→p+1 AND head returns to config home, tape = the moved tape. Proof: headMoveRight3_run (419, marker→p+1, tp') + skipMarkLeft3_run (432, skip relocated marker at p+1 → p) + resetToHome3_run (408, p→home+1); threads resetToHome3's no-M window on tp' via writeAt3_getD (cells <p unchanged from hclean; cell p = neighbor symbol ≠M from hc) + clearBlock-style length lemma; rw home+(p-home)=p to align; chain reachIn_seq3. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_headRightReturnHome3_run_frontier. NEXT: symmetric left move, home→marker seek prepend, move-direction branch, full step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3HeadRightHome` |
| **Entry 437 — universal-TM table: advance head left + return home `headLeftReturnHome3` (PROVED)** | Symmetric companion of 436 (the second canonical move phase). **`headLeftReturnHome3 s s1 f1 c1 s2 f2 c2 out`** := headMoveLeft3 s s1 ++ skipMarkLeft3 s1 f1 c1 s2 ++ resetToHome3 s2 f2 c2 out. **`headLeftReturnHome3_run`** (PROVED): home M @ home, head M @ p (home+2≤p), no other M in (home,p), left neighbor p-1 a bit, p<len ⇒ ∃N reachIn N (s, p, tp)(out, home+1, writeAt3 (writeAt3 tp p (tp.getD (p-1) O)) (p-1) M): head marker retreats p→p-1 AND head returns to config home, tape = moved tape. Proof: headMoveLeft3_run (419, marker→p-1, tp') + skipMarkLeft3_run (432, skip relocated marker at p-1 → p-2) + resetToHome3_run (408, p-2→home+1); SIMPLER no-M threading than 436 (window cells home+1..p-2 all <p-1, unchanged, all in (home,p) → hclean; no neighbor-cell case); rw p-1-1=p-2 and home+(p-2-home)=p-2 to align; chain reachIn_seq3. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_headLeftReturnHome3_run_frontier. Both move directions now canonical phases. NEXT: move-direction branch (select L/R by rule's move bit, two-branch pattern), home→marker seek, full step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3HeadLeftHome` |
| **Entry 438 — universal-TM table: move-direction dispatch `moveHeadDispatch3` (PROVED)** | The move direction is carried in the CONTROL STATE (matcher routes to a move-R/move-L state), so the honest realisation is a DISPATCH: one machine holding both canonical move phases (436, 437), entered at sR (right) or sL (left). The entry state selects; the unused phase never entered (lifted into the union by append_left/right). Both return to config home home+1, so either direction ends canonically. **`moveHeadDispatch3 <8 right states> <8 left states>`** := headRightReturnHome3 … ++ headLeftReturnHome3 …. **`moveHeadDispatch3_run_right`** (PROVED): entered at sR (home<p, hyps of 436) ⇒ ∃N reachIn N (sR, p, tp)(outR, home+1, writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M): right move. **`moveHeadDispatch3_run_left`** (PROVED): entered at sL (home+2≤p, hyps of 437) ⇒ left move (…(p-1)…). Each = the proven phase run lifted via reachIn_append_left3 (right) / reachIn_append_right3 (left). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_moveHeadDispatch3_run_{right,left}_frontier. NEXT: connect entry state to the matched rule's move bit; home→marker seek; full step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MoveDispatch` |
| **Entry 439 — universal-TM table: home-to-home right move `moveRightFromHome3` (PROVED)** | First HOME-TO-HOME phase (starts AND ends at config home c=home+1), the unit of the master step sequence — chains by reachIn_seq3 with no positioning glue. **`moveRightFromHome3 sStart sFound sCont s1 f1 c1 s2 f2 c2 out`** := seekMarkRight sStart sFound sCont ++ headRightReturnHome3 sFound s1 f1 c1 s2 f2 c2 out. **`moveRightFromHome3_run`** (PROVED): home M @ home, head M @ p (home<p), no other M in (home,p), right neighbor p+1 a bit, p+1<len ⇒ ∃N reachIn N (sStart, home+1, tp)(out, home+1, writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M): from config home, head marker advances p→p+1 AND head returns to config home. Proof: seekMarkRight_run (388, c→p, with d=p-(home+1), hclear from hclean, rw (home+1)+(p-(home+1))=p) + headRightReturnHome3_run (436); chain reachIn_seq3. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_moveRightFromHome3_run_frontier. NEXT: symmetric left/dispatch home-to-home; chain multiple phases (thread moved-tape cleanliness); full step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MoveRightHome` |
| **Entry 440 — universal-TM table: head move preserves clean config `moveRight/Left_preserves_clean` (PROVED)** | THE CHAINING-ENABLER: the marker invariant (home M fixed, single head M, no M strictly between) PERSISTS across a head move with the head marker relocated — so one home-to-home phase's output meets the NEXT phase's hypotheses (pure list lemmas, no reachIn). **`moveRight_preserves_clean`** (PROVED): home<p, home M @ home, clean (home,p), p+1 a bit ⇒ the moved tape writeAt3 (writeAt3 tp p (tp.getD(p+1) O)) (p+1) M has: home M @ home, head M @ p+1, no M in (home,p+1). Proof: writeAt3_getD per component (home: 2×if_neg → hmarkHome; p+1: if_pos → M; window j<p: 2×if_neg → hclean; j=p: cur = neighbor bit ≠M from hc). **`moveLeft_preserves_clean`** (PROVED): symmetric, head M @ p-1, no M in (home,p-1) (SIMPLER, no neighbor case since window <p-1<p). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_move{Right,Left}_preserves_clean_frontier. NEXT: chain home-to-home phases via reachIn_seq3 (using these to re-establish hyps), full step, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MoveClean` |
| **Entry 441 — universal-TM table: chaining two home-to-home phases `twoMoveRight3` (PROVED)** | First MASTER-SEQUENCE composition: two home-to-home right moves (439) chained, the 2nd phase's preconditions discharged from the 1st's output via moveRight_preserves_clean (440). **`movedRight tp p`** := writeAt3 (writeAt3 tp p (tp.getD (p+1) O)) (p+1) M. **`twoMoveRight3 <phase1 states> <phase2 states>`** := moveRightFromHome3 … ++ moveRightFromHome3 …. **`twoMoveRight3_run`** (PROVED): home/head markers, clean (home,p), cells p+1,p+2 bits, bounds ⇒ ∃N reachIn N (a0, home+1, tp)(out2, home+1, movedRight (movedRight tp p) (p+1)): head marker advances p→p+1→p+2, head returns home. Proof: moveRightFromHome3_run (439) phase1 + moveRight_preserves_clean (440) discharges phase2 marker hyps + neighbor/length on movedRight tp p + moveRightFromHome3_run phase2; chain reachIn_seq3. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_twoMoveRight3_run_frontier. Demonstrates the chaining mechanism (invariant carried across). NEXT: moveLeftFromHome3, move-from-home dispatch, heterogeneous master sequence, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3TwoMove` |
| **Entry 442 — universal-TM table: home-to-home left move `moveLeftFromHome3` (PROVED)** | Symmetric to 439. **`moveLeftFromHome3 …`** := seekMarkRight … ++ headLeftReturnHome3 …. **`moveLeftFromHome3_run`** (PROVED): home M @ home, head M @ p (home+2≤p), clean (home,p), left neighbor p-1 a bit, p<len ⇒ ∃N reachIn N (sStart, home+1, tp)(out, home+1, writeAt3 (writeAt3 tp p (tp.getD (p-1) O)) (p-1) M): home-to-home left move. Proof: seekMarkRight_run (388) + headLeftReturnHome3_run (437); chain reachIn_seq3. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_moveLeftFromHome3_run_frontier. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MoveLeftHome` |
| **Entry 443 — universal-TM table: home-to-home move dispatch `moveFromHome3` (PROVED)** | The apply's MOVE PHASE as a single home-to-home dispatch: both home-to-home move phases (439, 442) under one machine, entered at rS (right) or lS (left). **`moveFromHome3 <10 right states> <10 left states>`** := moveRightFromHome3 … ++ moveLeftFromHome3 …. **`moveFromHome3_run_right`** (PROVED): entered at rS (hyps of 439) ⇒ home-to-home right move. **`moveFromHome3_run_left`** (PROVED): entered at lS (hyps of 442) ⇒ left move. Each = the proven home-to-home phase lifted via reachIn_append_left3/right3. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_moveFromHome3_run_{right,left}_frontier. The move phase is now drop-in for either direction. NEXT: heterogeneous master sequence (lookup+apply+emit), EmitsEncodedStep3 — the SINGLE remaining obligation that universalSim_of_emits3 (entry 400) reduces the whole UTM to; requires concrete U+φ + matcher↔bitLookup + apply↔bitApply correspondence (large, NOT a few entries; will NOT be faked/socketed). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MoveFromHome` |
| **Entry 444 — universal-TM table: home-to-home cache refresh `cacheRefreshHome3` (PROVED)** | The cache refresh (435) made HOME-TO-HOME by appending resetToHome3 (408) from the cache cell back to home. **`cacheRefreshHome3 <18 cacheRefresh3 states> rf rc rout`** := cacheRefresh3 … ++ resetToHome3 sFin rf rc rout. **`cacheRefreshHome3_run`** (PROVED): from config home, head M @ (home+1)+d, state field @ home+1, bounds (incl. hcache: home+1+a+1<len) ⇒ ∃N reachIn N (s, home+1, tp)(rout, home+1, writeAt3 tp (home+1+a+1) (tp.getD ((home+1)+d+1) O)): cache cell ← current symbol, head returns to config home. Proof: cacheRefresh3_run (435, h=home+1) + resetToHome3_run (408) on RT = the single-writeAt3 result; reset's no-M window (home, home+1+a+1] covered by state ones (hco), sep (hcsep), cache (newsym ≠M from hcur) via writeAt3_getD case-split; rw home+(a+2)=home+1+a+1; chain reachIn_seq3. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_cacheRefreshHome3_run_frontier. The head marker (right of cache) is OUTSIDE the reset window so untouched. NEXT: home-to-home state update (needs copyBlockLeft content lemma), master apply, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CacheRefreshHome` |
| **Entry 445 — universal-TM table: leftward copier content `copyBlockLeft_getD_outside/_inside` (PROVED)** | Content of copyBlockLeft (415) in the DISJOINT case m<d (dest strictly left of source; holds for the state transfer since newlen<d'). **`copyBlockLeft_getD_outside`** (PROVED): m<d, d≤c, (k<c-d ∨ c+m-d<k) ⇒ (copyBlockLeft tp c d m).getD k = tp.getD k (cells outside dest [c-d,c+m-d] preserved, incl. source & home marker). **`copyBlockLeft_getD_inside`** (PROVED): m<d, d≤c, c-d≤k≤c+m-d ⇒ = tp.getD (k+d) (dest reads source). Both induction on m generalizing tp/c (ih arg order tp-then-c), succ peels writeAt3 at c-d via writeAt3_getD; inside's k=c-d boundary case reuses outside. So a marker-free source ⇒ marker-free dest. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_copyBlockLeft_getD_{outside,inside}_frontier. NEXT: home-to-home state update (re-establish marker invariant via these + clearBlock content 429), master apply, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CopyContent` |
| **Entry 446 — universal-TM table: home-to-home state update `clearStateFieldHome3` (PROVED)** | The state update (430) made HOME-TO-HOME by appending resetToHome3 (408); the marker invariant re-established on the transformed tape RT = copyBlockLeft (clearBlock tp (home+1) oldlen) (home+1+d') d' newlen via copier content (445, disjoint newlen<d') + clearer content (429). **`clearStateFieldHome3 <state-update states> rf rc rout`** := clearStateField3 … ++ resetToHome3 sDone rf rc rout. **`clearStateFieldHome3_run`** (PROVED): single home M, oldlen<d', newlen<d', budgets, bounds, old+rule fields ⇒ ∃N reachIn N (s, home+1, tp)(rout, home+1, RT): config state field ← rule's new state, head returns to config home. KEY no-M-in-window proof (3-way): dest region [home+1,home+1+newlen] → copyBlockLeft_getD_inside → rule field (hcnew/hcnewsep, I/O); outside dest & cleared → clearBlock_getD_inside → O; outside dest & uncleared → clearBlock_getD_outside → tp (hclean). Plus copyBlockLeft_length + clearBlock_length for RT.length=tp.length. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_clearStateFieldHome3_run_frontier. The state-update + cache-refresh apply phases are now both home-to-home. NEXT: symbol-write home-to-home, master apply sequence, matcher↔bitLookup, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3StateUpdateHome` |
| **Entry 447 — universal-TM table: home-to-home symbol write `writeSymHome3` (PROVED)** | The LAST apply sub-phase made home-to-home: write the matched rule's symbol at the simulated current cell. **`writeSymHome3 <11 states> w`** := seekMarkRight … ++ moveRight3 … ++ unmark3 … w ++ skipMarkLeft3 … ++ resetToHome3 …. **`writeSymHome3_run`** (PROVED): home M @ home, head M @ p (home<p), no other M in (home,p), w a bit, p+1<len ⇒ ∃N reachIn N (a, home+1, tp)(out, home+1, writeAt3 tp (p+1) w): current cell p+1 ← w, head marker unchanged @ p, head returns home. Proof: 5-piece chain (seek→step→write→skip-marker→reset); RT=writeAt3 tp (p+1) w; skip/reset hyps via writeAt3_getD (p,home ≠ p+1; window cells <p ≠p+1; w≠M from hw); chain reachIn_seq3. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_writeSymHome3_run_frontier. ALL FOUR apply sub-phases now home-to-home (state-update 446, symbol-write 447, move 443, cache-refresh 444). NEXT: master apply chain — needs marker-model reconciliation (clearStateField3 used global no-head-M; the others assume a head M → windowed-clean re-proof) + per-transition invariant threading; then matcher↔bitLookup, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3WriteSymHome` |
| **Entry 448 — universal-TM table: windowed-clean state update `clearStateField3_runW`/`clearStateFieldHome3_runW` (PROVED)** | THE MARKER-MODEL RECONCILIATION. The state update (430/446) used a GLOBAL no-marker hyp (∀ j≠home, ≠M) forbidding a head marker — incompatible with the symbol-write/move/cache-refresh phases (which carry a head M). KEY observation: the update's ONLY use of global clean was at the old separator cell, already pinned to O by hcoldsep. So: **`clearStateField3_runW`** (PROVED): the state update with NO global clean hyp (separator via hcoldsep). **`clearStateFieldHome3_runW`** (PROVED): home-to-home state update with a WINDOWED clean hyp ∀ j, home<j → j≤home+1+d'+newlen → ≠M — a head marker BEYOND home+1+d'+newlen (in the simulated tape, right of the rule) is now PERMITTED. Proof: clearStateField3_runW (re-proof of 430 swapping hclean→hcoldsep at the separator) + resetToHome3 with windowed hcleanW for the gap/uncleared cells; 3-way no-M window (dest→rule field; cleared→O; uncleared→hcleanW); copyBlockLeft_length+clearBlock_length. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_clearStateFieldHome3_runW_frontier. ALL FOUR apply phases now share ONE marker model (home M + head M beyond the window). NEXT: chain the four into the master apply (per-transition threading), then matcher↔bitLookup, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3StateUpdateWindowed` |
| **Entry 449 — universal-TM table: write-then-move apply core `applyWriteMoveRight3` (PROVED)** | FIRST HETEROGENEOUS MASTER-APPLY CHAIN: the core tape transformation of one TM step (rightward) as ONE home-to-home machine. Semantics check (marker-right-of-current-cell): writeSym puts w at current cell p+1; moveRight CARRIES w to cell p (preserving it left of the marker) and relocates M to p+1 = correct 'write then step right'. **`applyWriteMoveRight3 <write states> mid <move states> w`** := writeSymHome3 … mid w ++ moveRightFromHome3 mid …. **`applyWriteMoveRight3_run`** (PROVED): home/head markers, clean (home,p), w a bit, p+1<len ⇒ ∃N reachIn N (a, home+1, tp)(outM, home+1, writeAt3 (writeAt3 (writeAt3 tp (p+1) w) p w) (p+1) M): w written & carried to p, marker → p+1, head returns home. Proof: writeSymHome3_run (447) + moveRightFromHome3_run (439); 2nd phase hyps discharged from RT1=writeAt3 tp (p+1) w via writeAt3_getD (home,p ≠ p+1; window <p; p+1→w); rw RT1.getD(p+1)=w; chain reachIn_seq3. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_applyWriteMoveRight3_run_frontier. HONEST: this is the write+move core; cache-refresh extension = more threading; state-update integration hits the variable-length unary-field issue (changing state length shifts the layout → needs a tape-shift op, not built). NEXT: cache-refresh extension, tape-shift for state-update, matcher↔bitLookup, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ApplyWriteMove` |
| **Entry 450 — universal-TM table: tape-side master apply `applyTapeRight3` (PROVED)** | The COHERENT TAPE-SIDE MASTER APPLY (rightward): write symbol + advance head + refresh cache, THREE home-to-home phases in one machine. **`applyTapeRight3 <write/move states> mid2 <cache states> w`** := applyWriteMoveRight3 … mid2 w ++ cacheRefreshHome3 mid2 …. **`applyTapeRight3_run`** (PROVED): home/head markers, clean (home,p), state field @ home+1 (len a'), w & cell p+2 bits, home+1+a'+1<p (cache left of head), p+2<len ⇒ ∃N reachIn N (a, home+1, tp)(rout, home+1, writeAt3 (writeAt3 (writeAt3 (writeAt3 tp (p+1) w) p w) (p+1) M) (home+1+a'+1) (tp.getD (p+2) O)): symbol written & carried, head advanced, cache ← new current symbol, head returns home. Proof: applyWriteMoveRight3_run (449) + cacheRefreshHome3_run (444, d=p-home); cache hyps discharged on post-move tape T3 via 3 getD facts (T3.getD(p+1)=M, T3.getD p=w, others=tp); state field/cache intact since home+1+a'+1<p; chain reachIn_seq3. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_applyTapeRight3_run_frontier. The tape-side step (write+move+cache) is fully assembled & proven. NEXT: left/dispatch variant, state-update tape-shift, matcher↔bitLookup, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ApplyTape` |
| **Entry 451 — universal-TM table: tape-shift content primitive `copyBlockLeft_getD_*_gen`/`shiftLeftBlock_getD` (PROVED)** | THE TAPE-SHIFT PRIMITIVE (leftward). The unary state field changes length on a state change, so the rest of the tape must shift. KEY: copyBlockLeft (the copyFieldLeft3 machine 415) with d≥1 ALREADY performs a left shift (reads each source before overwrite); only its OVERLAPPING-case content was missing (445 covered disjoint m<d). Re-examined: m<d was NEVER USED. **`copyBlockLeft_getD_outside_gen`** (PROVED): d≤c, (k<c-d ∨ c+m-d<k) ⇒ (copyBlockLeft tp c d m).getD k = tp.getD k (any d). **`copyBlockLeft_getD_inside_gen`** (PROVED): d≤c, c-d≤k≤c+m-d ⇒ = tp.getD (k+d) (any d). **`shiftLeftBlock_getD`** (PROVED): the shift spec — block [c-d,c+m-d] reads source shifted left by d, rest unchanged. Proofs = 445's minus hmd (induction on m generalizing tp/c). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_shiftLeftBlock_getD_frontier. Machine = copyFieldLeft3 (415); rightward shift would need copyBlockRight. NEXT: state-update-with-shift, matcher↔bitLookup, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3TapeShift` |
| **Entry 452 — universal-TM table: rightward tape-shift content `copyBlockRight`/`shiftRightBlock_getD` (PROVED)** | The DUAL of 451 for state GROWTH (newlen>oldlen): a RIGHTWARD shift. copyBlock (412, left-to-right) CLOBBERS on overlapping right shift (overwrites a source before reading), so a genuine right shift copies HIGH-TO-LOW. **`copyBlockRight tp c d m`** (def, high-to-low fold): write c+m→c+m+d first, recurse on lower block. **`copyBlockRight_getD_outside`** (PROVED): 1≤d, (k<c+d ∨ c+m+d<k) ⇒ (copyBlockRight tp c d m).getD k = tp.getD k. **`copyBlockRight_getD_inside`** (PROVED): 1≤d, c+d≤k≤c+m+d ⇒ = tp.getD (k-d). **`shiftRightBlock_getD`** (PROVED): the shift spec — [c+d,c+m+d] reads source shifted right by d, rest unchanged. Induction on m generalizing tp (boundary k=c+m+1+d via outside). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_shiftRightBlock_getD_frontier. HONEST: list-level content only — the right-to-left MACHINE realising it (dual of copyFieldLeft3) is NOT built here. NEXT: right-to-left shift machine, state-update-with-shift, matcher↔bitLookup, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3TapeShiftRight` |
| **Entry 453 — universal-TM table: right-to-left shift machine `copyFieldRight3` (PROVED)** | The MACHINE realising the rightward shift (452); rightward shift now COMPLETE (machine 453 + content 452), dual of leftward (415+451). Copies HIGH-TO-LOW. **`copyFieldRight3 s sDone d n`** := copyBitAtDist3 s (s+4d+5) d ++ moveLeft3 (s+4d+5)(s+4d+6) ++ copyFieldRight3 (s+4d+6) sDone d n (base: copyBitAtDist3 s sDone d). **`copyFieldRight3_run`** (PROVED): 1≤d, block c..c+n all bits, c+n+d<len ⇒ ∃N reachIn N (s, c+n, tp)(sDone, c, copyBlockRight tp c d n): from the high cell c+n, block shifted right by d, head ends at c. Proof: induction on n; copyBitAtDist3_run (411, copy current→+d, head stays) + moveLeft3_run_eq (386) + ih; tp'=writeAt3 tp (c+n+1+d) … = copyBlockRight's recursion step (defeq); hbit' propagated via writeAt3_getD. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_copyFieldRight3_run_frontier. Both tape-shift directions now have machine+content. NEXT: state-update-with-shift, matcher↔bitLookup, EmitsEncodedStep3. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ShiftRightMachine` |
| **Entry 454 — universal-TM table: full state update `stateUpdateFull3` (PROVED)** | THE COMPLETE STATE UPDATE handling the variable-length unary state field — WITHOUT shifting the marker-containing sim-tape. INSIGHT: a state-length change only invalidates the CACHE position (navigateToCache3 scans the state field to its sep). So RE-PLACE the cache instead of physically shifting: clearStateFieldHome3 (448, write new state field) ++ cacheRefreshHome3 (444, read current symbol from head marker, write at the NEW cache cell home+1+newlen+1). Old cache cell = harmless gap (matcher never looks there; sim-tape found via marker seek). So the tape-shift primitives 451-453 are NOT needed for this (still reusable). **`stateUpdateFull3 <state-update states> mid2 <cache states>`** := clearStateFieldHome3 … mid2 ++ cacheRefreshHome3 mid2 …. **`stateUpdateFull3_run`** (PROVED): home M, head M @ hm beyond rule region (home+1+d'+newlen<hm), no other M in (home,hm), old/new(rule) state fields, current cell bit, bounds ⇒ ∃N reachIn N (s, home+1, tp)(rout, home+1, writeAt3 (copyBlockLeft (clearBlock tp (home+1) oldlen) (home+1+d') d' newlen) (home+1+newlen+1) (tp.getD (hm+1) O)): state field ← rule's new state, cache re-placed at new boundary with current symbol, head returns home. Proof: clearStateFieldHome3_runW (448) + cacheRefreshHome3_run (444, d=hm-(home+1), a=newlen); cache hyps derived on RT=copyBlockLeft(clearBlock) via content lemmas (451 gen + 429): hRT_noM (3-way: dest→rule field, cleared→O, else→hclean), hRT_state/statesep (dest=rule field), hRT_far (cells beyond=tp); copyBlockLeft_length+clearBlock_length. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_stateUpdateFull3_run_frontier. NEXT: matcher↔bitLookup correspondence, EmitsEncodedStep3 (+ the fixed-cost spec obstruction). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3StateUpdateFull` |
| **Entry 455 — universal-TM table: matcher↔lookup correspondence `recMatch_iff_lookup/bitLookup` (PROVED)** | THE CRUX BRIDGE (condition level): the concrete matcher's (410) match-existence condition coincides with the abstract lookup/bitLookup finding a rule. RecMatch a cs (d,b,rs) = (a=b ∧ rs=cs); lookup matches r when r.1=(c.1, readSym c). SAME CONDITION: encode each rule r→descriptor (1, r.1.1, boolToSym3 r.1.2); config state c.1 plays the unary state-length a, boolToSym3(readSym c) plays cs; boolToSym3 injective. **`recsOf M c`** := M.map (fun r => (1, r.1.1, boolToSym3 r.1.2)). **`boolToSym3_inj`** (PROVED). **`recMatch_iff_lookup`** (PROVED): (∃ rec ∈ recsOf M c, RecMatch c.1 (boolToSym3 (readSym c)) rec) ↔ (lookup M c).isSome. **`recMatch_iff_bitLookup`** (PROVED): same with bitLookup (encodeMachineBits M) c (via bitLookup=lookup). Proof: List.find?_isSome + List.mem_map + boolToSym3_inj + Prod projections; decide_eq_true_eq for the Bool match. [propext,Quot.sound], no sorry. Wired nw_recMatch_iff_bitLookup_frontier. Combined with matchTable3_run (410): a rule in the encoded table ⇒ the matcher reaches the match-found state. HONEST: condition-level only — the full layout-φ/run-level correspondence remains, subject to the fixed-cost obstruction of EmitsEncodedStep3 (a fixed cost can't bound scan time on unbounded tapes). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MatcherLookup` |
| **Entry 456 — universal-TM table: existence-cost emission lifting `universalSimEx_of_emits3` (PROVED)** | THE HONEST REFORMULATION removing the fixed-cost obstruction of entry 400. EmitsEncodedStep3 demanded ONE cost for every step — impossible for a real UTM (per-step time grows with sizes). FIX: existence-cost spec — each step realised in SOME number of transitions. **`EmitsEncodedStepEx3 U φ`** := ∀ Mbits cbits next, encodedStep Mbits cbits = some next → ∃N reachIn (toNTM3 U) N (φ Mbits cbits)(φ Mbits next). **`universalSimEx_of_emits3`** (PROVED): EmitsEncodedStepEx3 U φ ⟹ ∀ M k c0 cf, simIter M k c0 = some cf → ∃N reachIn (toNTM3 U) N (φ (encodeMachineBits M)(encodeConfig c0))(φ … cf): the whole k-step sim is realised in some number of transitions. Induction on k; encodedStep_correct + emission + reachIn_add; zero case ⟨0,rfl⟩. Mirrors 400 but ∃N (no k*cost). [propext,Quot.sound], no sorry. Wired nw_universalSimEx_of_emits3_frontier. KEY: this is the spec the CONSTRUCTION can discharge — every apply phase (404-455) is an ∃N reachIn run. So the UTM reduces to ONE ACHIEVABLE obligation: concrete U+φ + EmitsEncodedStepEx3 (assemble the phases). The fixed-cost obstruction is dissolved (was an over-specification, not a real barrier). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3EmitEx` |
| **Entry 457 — universal-TM table: config-region layout `homeConfigTape3` (PROVED)** | First concrete artifact of the assembly layout φ: the CONFIG REGION (home marker + encoded config), with exactly the phase preconditions at home=0, c=1. **`homeConfigTape3 q sym rest`** := Sym3.M :: configEncode3 q sym rest. **`homeConfigTape3_content`** (PROVED): getD 0 = M (home); ∀ i<q, getD (1+i) = I (state field); getD (1+q) = O (sep); getD (1+q+1) = boolToSym3 sym (cache). Follows from configEncode3_content (413) with prefix [M]. [propext,Quot.sound], no sorry. Wired nw_homeConfigTape3_content_frontier. HONEST: config region only — full φ (rule table + sim-tape with head marker) + EmitsEncodedStepEx3 are the large remaining assembly (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3HomeLayout` |
| **Entry 458 — universal-TM table: rule-record layout `record_RecOK` (PROVED)** | The rule-table region's PER-RECORD bridge: an encoded rule record satisfies the matcher's RecOK (410). A rule's key is encoded exactly as a config: configEncode3 b rs rest. **`record_RecOK`** (PROVED): pre.length = c+d, 1≤d, min a b<L, c+a+1+d<len ⇒ RecOK (pre ++ configEncode3 b rs rest) c a L (d, b, boolToSym3 rs): the record at offset c+d meets the per-record obligation with key (b, boolToSym3 rs). Content fields = configEncode3_content (413) with prefix pre (rw pre.length=c+d). [propext,Quot.sound], no sorry. Wired nw_record_RecOK_frontier. HONEST: per-record only — list-of-records at cumulative offsets + sim-tape head marker + full φ + EmitsEncodedStepEx3 remain (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3RecordLayout` |
| **Entry 459 — universal-TM table: records-list cumulative layout `recordsTape3`/`records_RecOK` (PROVED)** | The rule table laid CONTIGUOUSLY (cumulative offsets), the matcher can walk the whole table. **`recordBlock r`** := encodeNatBits3 r.1 ++ [boolToSym3 r.2] (length r.1+2). **`recordsTape3`** : configEncode3-fold over the list. **`configEncode3_eq_recordBlock`** (PROVED): configEncode3 b rs X = recordBlock (b,rs) ++ X. **`recordsTape3_split`** (PROVED): recordsTape3 (front ++ (b,rs)::back) = front.flatMap recordBlock ++ configEncode3 b rs (recordsTape3 back) (induction on front; Prod eta defeq). **`records_RecOK`** (PROVED): (pre ++ front.flatMap recordBlock).length = c+d, 1≤d, min a b<L, bound ⇒ RecOK (pre ++ recordsTape3 (front++(b,rs)::back)) c a L (d, b, boolToSym3 rs): every record at its cumulative offset meets the per-record obligation (via split + record_RecOK 458). [propext,Quot.sound], no sorry. Wired nw_records_RecOK_frontier. HONEST: rule-table layout done; sim-tape head marker + full φ + EmitsEncodedStepEx3 remain (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3RecordsLayout` |
| **Entry 460 — universal-TM table: simulated-tape region `simTapeRegion` (PROVED)** | The sim-tape region of φ: the abstract TM tape (List Bool) encoded as Sym3 bits with the head marker M before the head cell (marker-right-of-current-cell rep). **`simTapeRegion tp h`** := (tp.take h).map boolToSym3 ++ M :: (tp.drop h).map boolToSym3 (marker at h, current cell h+1). **`mapBool_getElem?_bit`** (PROVED): a bit-encoded cell reads O or I. **`simTapeRegion_marker`** (PROVED): h≤tp.length ⇒ getD h = M. **`simTapeRegion_current`** (PROVED): getD (h+1) ∈ {O,I} (current cell a bit). **`simTapeRegion_clean`** (PROVED): h≤tp.length, j≠h ⇒ getD j ≠ M (no other marker). Proofs via getD_eq_getElem?_getD + getElem?_append_left/right (arbitrary index + hyp) + getElem?_cons_succ + mapBool bit helper; take_map_length. [propext,Quot.sound], no sorry. Wired nw_simTapeRegion_{marker,current,clean}_frontier. HONEST: sim-tape region done; full φ (stitch config+rules+simtape) + U + EmitsEncodedStepEx3 remain (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3SimTapeLayout` |
| **Entry 461 — universal-TM table: stitched full tape `fullTape3` (PROVED)** | The φ STITCHING of config + rule table: **`fullTape3 a cs rules simtape`** := M :: configEncode3 a cs (recordsTape3 rules ++ simtape) (layout: home M | config key | rule table | sim-tape). **`fullTape3_config`** (PROVED): config-key invariants (home, state field, cache) — definitionally homeConfigTape3, so = homeConfigTape3_content (457). **`configEncode3_absorb_tail`** (PROVED): configEncode3 b rs X ++ Y = configEncode3 b rs (X++Y). **`fullTape3_record_RecOK`** (PROVED): every rule record of `rules` satisfies RecOK on the FULL tape at c=1 (via recordsTape3_split 459 + absorb-tail + record_RecOK 458; offset d = a+2+front-len). [propext,Quot.sound], no sorry. Wired nw_fullTape3_record_RecOK_frontier. The matcher side of φ is now assembled on one tape. HONEST: sim-tape head-marker at full-tape offset + bit-decoding φ + windowed matcher + U + EmitsEncodedStepEx3 remain (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3FullLayout` |
| **Entry 462 — universal-TM table: head marker at full-tape offset `fullTape3_head_marker/_current` (PROVED)** | The apply-side stitching: with simTapeRegion (460) as the trailing region of fullTape3 (461), the head marker M sits at the full-tape offset (cfgHead a cs ++ recordsTape3 rules).length + h, current cell at +h+1. **`fullTape3_head_marker`** (PROVED): h≤simTp.length ⇒ (fullTape3 a cs rules (simTapeRegion simTp h)).getD ((cfgHead++records).length + h) = M. **`fullTape3_head_current`** (PROVED): the cell at +h+1 ∈ {O,I}. Proofs: fullTape3_eq_cfgHead + ←append_assoc + getElem?_append_right (le_add_right) + Nat.add_sub_cancel_left + simTapeRegion_marker/_current (460). [propext,Quot.sound], no sorry. Wired nw_fullTape3_head_{marker,current}_frontier. The sim-tape head is now located on the stitched tape. HONEST: config+rule region full-offset cleanliness (windowed matcher) + bit-decoding φ + U + EmitsEncodedStepEx3 remain (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3FullHead` |
| **Entry 463 — universal-TM table: config+rule region marker-free `fullTape3_matcher_clean` (PROVED)** | The matcher's WINDOWED CLEANLINESS on the stitched tape: everything after the home marker, before the sim-tape, is a bit (built only from encodeNatBits3/boolToSym3) — so matchTable3 (re-proved windowed à la 448) can walk it with the head marker BEYOND (in the sim-tape). **`allBits_getD_ne_M`** (PROVED): an all-O/I list never reads M. **`encodeNatBits3_allBits`**, **`recordsTape3_allBits`**, **`configEncode3_allBits`** (PROVED): the encodings are bit-only (mem_append/mem_cons + eq_of_mem_replicate + boolToSym3 cases). **`configRules_clean`** (PROVED): 0<j ⇒ (cfgHead a cs ++ recordsTape3 rules).getD j ≠ M (region = M :: configEncode3 …, tail all-bits). **`fullTape3_matcher_clean`** (PROVED): 0<j, j<(cfgHead++records).length ⇒ (fullTape3 …).getD j ≠ M (getElem?_append_left lifts the region clean to the full tape). [propext,Quot.sound], no sorry. Wired nw_fullTape3_matcher_clean_frontier. HONEST: windowed matchTable3 re-proof + bit-decoding φ + U + EmitsEncodedStepEx3 remain (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3RegionClean` |
| **Entry 464 — universal-TM table: windowed rule-table match loop `matchTable3_run_windowed` (PROVED)** | The matcher (410) re-proved with a WINDOWED clean hypothesis — head marker beyond a bound hm permitted (the stitched-tape situation; 463 supplies the window). Replaces global hclean (∀ j≠c-1, ≠M) with **hcleanW : ∀ j, c-1<j → j<hm → ≠M** + **hHead : ∀ rec ∈ recs, c+a+1+rec.1 < hm** (records before the head marker). **`matchTable3_run_windowed`** (PROVED): home M @ c-1, hcleanW, config key, every record RecOK + before hm, some record matches ⇒ ∃N q reachIn N (base, c, tp)(recMatch, q, tp). Proof mirrors 410; the two clean uses (recordKeyMatch3_run_windowed 409 with window ≤c+a+1+d, resetToHome3 408 with positions ≤q≤c+a+1) discharged from hcleanW via omega (q≤c+a+1<c+a+1+d<hm from hHeadHead, hd, hq2). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_matchTable3_run_windowed_frontier. Now matchable on fullTape3 (463's window). HONEST: bit-decoding φ + U + EmitsEncodedStepEx3 remain (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MatchTableWin` |
| **Entry 465 — universal-TM table: bit-decoding layout `layoutPhi3` (PROVED)** | The concrete φ : List Bool → List Bool → CConfig3 for EmitsEncodedStepEx3 (456): decode cbits (decodeConfig, as encodedStep does) + Mbits (decodeMachineBits) and build the stitched tape (461) — home M | config key (config state + current symbol) | rule table (decoded machine keys M.map (·.1)) | sim-tape (simTapeRegion). Control @ initState, head @ config home 1. **`layoutPhi3 initState Mbits cbits`** := (initState, 1, fullTape3 ((decodeConfig cbits).1).1 (readSym …) (rules) (simTapeRegion …)). **`layoutPhi3_wellformed`** (PROVED): control=initState (rfl), head=1 (rfl), home marker @0 (fullTape3_config.1). **`layoutPhi3_config`** (PROVED): config-key invariants (home, state field, sep, cache) = fullTape3_config. [propext,Quot.sound], no sorry. Wired nw_layoutPhi3_wellformed_frontier. φ is now concretely defined & well-formed. HONEST: global U + EmitsEncodedStepEx3 U layoutPhi3 remain (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Phi` |
| **Entry 466 — universal-TM table: lookup phase on the stitched tape `phiLookup_fullTape` (PROVED)** | First step toward global U: the matcher RUNS on the concrete fullTape3. Instantiates matchTable3_run_windowed (464) on fullTape3 a csBool rules simtape @ c=1, discharging EVERY layout hyp from proven invariants: home marker + config key (461 fullTape3_config), windowed cleanliness (463 fullTape3_matcher_clean, window=(cfgHead++records).length, head marker beyond), hcs (boolToSym3 ∈{O,I}). **`phiLookup_fullTape`** (PROVED): descriptors recs each RecOK + before head marker + one matching (a, boolToSym3 csBool) ⇒ ∃N q reachIn N (base, 1, fullTape3 …)(recMatch, q, fullTape3 …). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_phiLookup_fullTape_frontier. The lookup phase is grounded on the concrete tape. HONEST: descriptor-list construction + apply phase + global U + EmitsEncodedStepEx3 remain (obstruction-free per 456). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3PhiLookup` |
| **Entry 467 — universal-TM table: descriptor list `descriptorsOf` / `descriptorsOf_match` (PROVED) + FIXED-U GAP FINDING** | The matcher's record descriptors (cumulative offsets) for a rule-key list. **`descriptorsOf a rules`** := descriptorsAux a 0 rules (each record distance = a+2 + prior records' total length). **`descriptorsOf_match`** (PROVED): (∃ rec ∈ descriptorsOf a rules, RecMatch a (boolToSym3 csBool) rec) ↔ (∃ p ∈ rules, a=p.1 ∧ p.2=csBool) — offset irrelevant to RecMatch, induction + boolToSym3 inj. [propext,Quot.sound], no sorry. Wired nw_descriptorsOf_match_frontier. **HONEST ARCHITECTURAL FINDING (NOT FAKED OVER): matchTable3 (410) is UNROLLED — recursion over the record list, one transition block per record, so machine SIZE ∝ rule count. Hence matchTable3 (and any U built from it) is PER-MACHINE-COMPILED, NOT a fixed universal machine. EmitsEncodedStep(Ex)3 requires a SINGLE FIXED U for all Mbits. A true fixed U needs a GENERIC TAPE-DRIVEN SCAN LOOP (fixed-size machine reading each record from the tape + looping) — a separate construction NOT built in this arc.** The 404-466 layout+matcher arc proves the unrolled-model lookup correctness on φ; the fixed-U requires the generic loop. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Descriptors` |
| **Entry 468 — generic scan loop: unary-consume component `skipOnesRight` (PROVED)** | FIRST fixed-state component of the generic rule-table scan loop the FIXED universal U needs (per 467's fixed-U finding; unrolled matchTable3 is NOT fixed). Records self-delimiting (unary I-field + O sep + symbol), so a generic loop must consume a unary field with CONSTANT states. **`branchOne3 s sCont sStop`** (3 rules, stay): I→sCont, O/M→sStop; run_one/run_stop PROVED (mirror branchMark3). **`skipOnesRight s stop cont`** := branchOne3 s cont stop ++ moveRight3 cont s (fixed 3-state machine, back-edge cont→s). **`skipOnesRight_run`** (PROVED): h…h+d-1 all I, h+d non-I, bound ⇒ ∃N reachIn N (s,h,tp)(stop,h+d,tp) — induction on d (data), MACHINE FIXED. Mirrors seekMarkRight (388). Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_skipOnesRight_run_frontier. The fixed-machine-data-dependent-steps pattern, now for unary fields. HONEST: per-record key comparison + record-advance + table-end test + full scan loop + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3SkipOnes` |
| **Entry 469 — generic scan loop: record-advance `advanceRecord3` (PROVED)** | Second fixed-state component. A record = unary I-field (len b) + O sep + symbol = b+2 cells. **`advanceRecord3 s m1 m2 stop cont`** := skipOnesRight s m1 cont ++ moveRight3 m1 m2 ++ moveRight3 m2 stop. **`advanceRecord3_run`** (PROVED): h…h+b-1 all I, h+b=O, h+b+1<len ⇒ ∃N reachIn N (s,h,tp)(stop,h+b+2,tp) — next record start, regardless of field length (skipOnesRight 468 + 2×moveRight3, chained reachIn_seq3). Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_advanceRecord3_run_frontier. HONEST: per-record key comparison + table-end test + full scan loop + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3AdvanceRecord` |
| **Entry 470 — generic scan loop: leftward unary-consume `skipOnesLeft` (PROVED)** | Leftward mirror of skipOnesRight (468) for the comparison shuttle. **`skipOnesLeft s stop cont`** := branchOne3 s cont stop ++ moveLeft3 cont s (fixed 3-state, back-edge). **`skipOnesLeft_run`** (PROVED): p+1…p+d all I, p non-I, p+d<len ⇒ ∃N reachIn N (s,p+d,tp)(stop,p,tp) — induction on d, machine fixed (mirror seekMarkLeft 387). Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_skipOnesLeft_run_frontier. Bidirectional unary-consume pair complete. HONEST: per-record key comparison + table-end test + full scan loop + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3SkipOnesL` |
| **Entry 471 — generic scan loop: record-walk SKELETON `skipRecordsToEnd` (PROVED)** | THE ARCHITECTURAL HEART of the fixed-size loop: a SINGLE FIXED machine walks the whole rule table to the terminator M. **`skipRecordsToEnd s done advE m1 m2 cont`** := branchMark3 s done advE ++ advanceRecord3 advE m1 m2 s cont (at each record start: M→done exit, else→advance to next record + loop via back-edge to s). **`skipRecordsToEnd_run`** (PROVED): ∀ recs pre, tp = pre ++ recordsTape3 recs ++ M::tail ⇒ ∃N reachIn N (s,pre.length,tp)(done,(pre++recordsTape3 recs).length,tp) — INDUCTION ON RECORD COUNT, machine fixed. Per-record content via configEncode3_content (413) + configEncode3_absorb_tail (461) + recordBlock split (459); chained branchMark3 (386) + advanceRecord3 (469) via reachIn_add. [propext,Quot.sound], no sorry. Wired nw_skipRecordsToEnd_run_frontier. Proves the fixed-loop-over-records WORKS — backbone for the compare branch. HONEST: per-record key comparison (3-symbol relative shuttle) + match-or-advance branch + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ScanLoop` |
| **Entry 472 — generic scan loop: cursor-advance `markAdvance3` (PROVED)** | The CORE OP of the comparison shuttle (tape-preserving). A cursor = marker M on a unary field; advancing = restore marked cell to I, move marker one right, DETECT field continue/end. **`markAdvance3 s smid sCont sEnd`** (3 rules): at M→restore I, move right; then I→re-mark M (→sCont), O→stop (→sEnd). **`markAdvance3_run_cont`** (PROVED): tp[j]=M, tp[j+1]=I ⇒ reachIn 2 (s,j,tp)(sCont, j+1, writeAt3 (writeAt3 tp j I)(j+1) M). **`markAdvance3_run_end`** (PROVED): tp[j]=M, tp[j+1]=O ⇒ reachIn 2 →(sEnd, j+1, writeAt3 (writeAt3 tp j I)(j+1) O). 2-step concreteStep3 chains via reachIn_add + writeAt3_getD. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_markAdvance3_run_{cont,end}_frontier. The tape-preserving cursor step for unary comparison. HONEST: two-field comparison loop + match-or-advance branch + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3MarkAdvance` |
| **Entry 473 — generic scan loop: inter-cursor shuttle `crossRight`/`crossLeft` (PROVED)** | The head-transport of the comparison shuttle: cross from one cursor to its partner across the M-free gap (step off current cursor + seek to partner). **`crossRight s mid found cont`** := moveRight3 s mid ++ seekMarkRight mid found cont; **`crossLeft`** := moveLeft3 ++ seekMarkLeft. **`crossRight_run`** (PROVED): cursor at p, partner M at p+1+d, cells p+1…p+d M-free ⇒ ∃N reachIn N (s,p,tp)(found,p+1+d,tp). **`crossLeft_run`** (PROVED): cursor at q+d+1, partner M at q, cells q+1…q+d M-free ⇒ ∃N reachIn N (s,q+d+1,tp)(found,q,tp). Compositions of move + seek (388/387), chained reachIn_seq3. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_cross{Right,Left}_run_frontier. HONEST: two-field comparison loop + match-or-advance branch + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3Cross` |
| **Entry 474 — generic scan loop: half-tandem move `advanceCrossRight` (PROVED)** | Fuses cursor-advance (472) + inter-cursor cross (473): advance the cursor one cell + cross right to partner — the reusable iteration HALF of the two-cursor comparison loop. **`advanceCrossRight s smid sCont sEnd crMid crFound crCont`** := markAdvance3 s smid sCont sEnd ++ crossRight sCont crMid crFound crCont. **`advanceCrossRight_cont_run`** (PROVED): cursor cp (M), field continues (cp+1=I), partner M at cp+2+d, gap cp+2…cp+1+d M-free ⇒ ∃N reachIn N (s,cp,tp)(crFound, cp+2+d, writeAt3(writeAt3 tp cp I)(cp+1) M). markAdvance3_run_cont + crossRight_run chained reachIn_seq3; M-freedom on modified tape via writeAt3_getD; writeAt3 length helper. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_advanceCrossRight_cont_run_frontier. HONEST: leftward mirror + full tandem loop + four-way end-branch + symbol compare + match-or-advance + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3AdvanceCross` |
| **Entry 475 — generic scan loop: leftward half-tandem move `advanceCrossLeft` (PROVED)** | Mirror of 474: advance the right cursor one cell + cross LEFT back to partner. advanceCrossRight then advanceCrossLeft = one full tandem iteration (both cursors step right, head back on left cursor). **`advanceCrossLeft s smid sCont sEnd crMid crFound crCont`** := markAdvance3 s smid sCont sEnd ++ crossLeft sCont crMid crFound crCont. **`advanceCrossLeft_cont_run`** (PROVED): 1≤d, record cursor pc+d (M), field continues (pc+d+1=I), partner M at pc, gap pc+1…pc+d-1 M-free ⇒ ∃N reachIn N (s,pc+d,tp)(crFound, pc, writeAt3(writeAt3 tp (pc+d) I)(pc+d+1) M). markAdvance3_run_cont + crossLeft_run chained reachIn_seq3; hclear splits k<d (gap) / k=d (restored I). Compiled clean. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_advanceCrossLeft_cont_run_frontier. Half-tandem PAIR complete. HONEST: full tandem loop + four-way end-branch + symbol compare + match-or-advance + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3AdvanceCrossL` |
| **Entry 476 — generic scan loop: comparison loop BODY `tandemStep3` (PROVED)** | One full tandem iteration fuses advanceCrossRight (474) + advanceCrossLeft (475): both unary-field cursors step right by one, head back on config cursor — the constant-gap two-cursor walk. **`tandemStep3 …`** (13 state params) := advanceCrossRight s … crFound … ++ advanceCrossLeft crFound …. **`tandemStep3_run`** (PROVED): config cursor cp (M), cp+1=I; record cursor cp+g (M, g≥2), cp+g+1=I; inter-cursor gap M-free ⇒ ∃N reachIn N (s,cp,tp)(lcrFound, cp+1, tp₂) with tp₂ moving BOTH cursors right one. 474+475 chained reachIn_seq3; 475's hyps on tp1 (post-474) via writeAt3_getD; partner tp1[cp+1]=M via if_pos. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_tandemStep3_run_frontier. The comparison loop body PROVEN. HONEST: iterate (induction min(a,b)) + four-way end-branch + symbol compare + match-or-advance + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3TandemStep` |
| **Entry 477 — generic scan loop: in-bounds tape-write algebra `writeAt3_eq_set`/`_overwrite`/`_comm` (PROVED)** | Reusable tape algebra to iterate the tandem comparison step (476, whose output is a nested writeAt3). In bounds writeAt3 adds no padding = List.set. **`writeAt3_eq_set`** (PROVED): p<t.length ⇒ writeAt3 t p w = t.set p w (unfold + Nat.sub_eq_zero_of_le + simp). **`writeAt3_overwrite`** (PROVED): p<t.length ⇒ writeAt3(writeAt3 t p a) p b = writeAt3 t p b (List.set_set). **`writeAt3_comm`** (PROVED): p≠q, p,q<t.length ⇒ writeAt3(writeAt3 t p a) q b = writeAt3(writeAt3 t q b) p a (List.set_comm). [propext,Quot.sound], no sorry. Wired nw_writeAt3_{eq_set,comm}_frontier. Unblocks the iteration tape-tracking. HONEST: iterate tandemStep3 (induction) + four-way end-branch + symbol compare + match-or-advance + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3WriteAlg` |
| **Entry 478 — generic scan loop: canonical comparison tape `cursTape` cell lemmas (PROVED)** | The two-cursor comparison state in canonical form for the iteration. **`cursTape tp cp g i`** := writeAt3(writeAt3 tp (cp+i) M)(cp+g+i) M (base tp all-I in fields; config cursor M @ cp+i, record cursor M @ cp+g+i). **`cursTape_cursorL`** (PROVED, g≥1): getD(cp+i)=M. **`cursTape_cursorR`** (PROVED): getD(cp+g+i)=M. **`cursTape_other`** (PROVED): x≠cp+i, x≠cp+g+i ⇒ getD x = tp.getD x. **`cursTape_length`** (PROVED, in bounds): length = tp.length. Via writeAt3_getD + writeAt3_eq_set (477). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_cursTape_{cursorL,other}_frontier. Feeds tandemStep3's hyps across iteration. HONEST: one canonical step (nested-write collapse) + iterate (induction) + four-way end-branch + symbol compare + match-or-advance + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3CursTape` |
| **Entry 479 — generic scan loop: one canonical tandem step `tandemStepCanon_run` (PROVED)** | One full tandem iteration (476) maps cursTape (478) at step i → step i+1 (both cursors advance one). The post-step SIX-layer writeAt3 tape shown = cursTape(i+1) by **List.ext_getElem** (getD extensionality via writeAt3_getD + split_ifs <;> simp_all <;> omega) — robust where nested rw was fragile. **`tandemStepCanon_run`** (PROVED): base field cells I, gap M-free, in bounds ⇒ ∃N reachIn N (s, cp+i, cursTape tp cp g i)(lcrFound, cp+i+1, cursTape tp cp g (i+1)). tandemStep3_run + cursTape cell lemmas for hyps; ext_getElem for the tape collapse; length via wlen chain. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_tandemStepCanon_run_frontier. The per-step collapse DONE. HONEST: iterate (induction on step count) + four-way end-branch + symbol compare + match-or-advance + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3TandemCanon` |
| **Entry 480 — generic scan loop: iterated tandem comparison `tandemLoop` (PROVED)** | Closes the comparison loop with a back-edge (lcrFound := s): one tandem step (479) returns to loop head s, so d iterations walk both cursors step 0→d. **`tandemLoop …`** := tandemStep3 … (lcrFound := s) …. **`tandemLoop_run`** (PROVED): ∀d, base fields all-I over cp…cp+d & cp+g…cp+g+d, inter-field M-free, cp+g+d<len ⇒ ∃N reachIn N (s, cp, cursTape tp cp g 0)(s, cp+d, cursTape tp cp g d) — INDUCTION ON d, machine fixed (chain tandemStepCanon_run via reachIn_add; monotone hyps). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_tandemLoop_run_frontier. The distance-agnostic unary comparison loop DONE. HONEST: four-way end-branch (match vs mismatch at field ends) + symbol compare + match-or-advance + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3TandemLoop` |
| **Entry 481 — generic scan loop: comparison match verdict `endMatch` (PROVED)** | The POSITIVE verdict (a=b): both unary fields end at the same step ⇒ confirm match + restore tape. **`endMatch s mid1 cont1 e1 m2 sc2 mid3 matchSt recEnd`** := markAdvance3 s mid1 cont1 e1 ++ seekMarkRight e1 m2 sc2 ++ markAdvance3 m2 mid3 recEnd matchSt (config cursor onto sep [end-case, removes it], seek to record cursor, record cursor onto sep [end→matchSt]). **`endMatch_run`** (PROVED): last field cells I (cp+d, cp+g+d), both seps O (cp+d+1, cp+g+d+1), gap M-free, in bounds ⇒ ∃N reachIn N (s, cp+d, cursTape tp cp g d)(matchSt, cp+g+d+1, tp) — TAPE RESTORED to base. markAdvance3_run_end ×2 (472) + seekMarkRight_run (388) chained reachIn_seq3; final tape = tp via List.ext_getElem collapse. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_endMatch_run_frontier. The comparison's a=b conclusion DONE. HONEST: mismatch branches + symbol compare + match-or-advance into scan loop + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3EndMatch` |
| **Entry 482 — generic scan loop: a comparison mismatch verdict `endMismatchCR` (PROVED)** | NEGATIVE verdict: config field ends but record continues (a<b). **`eraseMark3 s fail`** := [(s,M)→(fail,I,stay)] (erase a cursor); eraseMark3_run PROVED. **`endMismatchCR …`** := markAdvance3(config) ++ seekMarkRight ++ markAdvance3(record) ++ eraseMark3 (config cursor onto sep [end, removed], seek to record cursor, record cursor advances [CONT, leaves cursor], ERASE leftover cursor). **`endMismatchCR_run`** (PROVED): config last I + sep O, record last I + CONTINUES (I ahead), gap M-free, in bounds ⇒ ∃N reachIn N (s, cp+d, cursTape tp cp g d)(failSt, cp+g+d+1, tp) — TAPE RESTORED. markAdvance3_run_end + seekMarkRight_run + markAdvance3_run_cont + eraseMark3_run chained reachIn_seq3 ×3; final tape = tp via List.ext_getElem (overwrite cp+g+d+1: M→I). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_endMismatchCR_run_frontier. The a<b verdict DONE. HONEST: symmetric mismatch (a>b) + symbol compare + match-or-advance into scan loop + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3EndMismatch` |
| **Entry 483 — generic scan loop: symmetric mismatch verdict `endMismatchRC` (PROVED)** | The THIRD/FINAL length-verdict: config field continues but record ends (a>b); leftover cursor is the CONFIG one. **`endMismatchRC …`** := markAdvance3(config,cont) ++ crossRight ++ markAdvance3(record,end) ++ seekMarkLeft ++ eraseMark3 (config advances, cross to record cursor, record onto sep [removed], seek LEFT back to config cursor, erase it). **`endMismatchRC_run`** (PROVED): config last I + CONTINUES (I ahead), record last I + sep O, gap M-free, in bounds ⇒ ∃N reachIn N (s, cp+d, cursTape tp cp g d)(failSt, cp+d+1, tp) — TAPE RESTORED. 5-machine compose (markAdvance3_run_cont + crossRight_run + markAdvance3_run_end + seekMarkLeft_run + eraseMark3_run) chained reachIn_seq3 ×4; seekMarkLeft cells via trichotomy; final tape=tp via List.ext_getElem. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_endMismatchRC_run_frontier. ALL THREE comparison verdicts (=, <, >) DONE, each tape-restoring. HONEST: symbol compare + match-or-advance into scan loop + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3EndMismatchRC` |
| **Entry 484 — generic scan loop: structural symbol read `readSymbolAt` (PROVED)** | Reach + read a record's one-cell symbol generically (the symbol-compare's read half; verdict half = branchOne3). **`readSymbolAt s m1 cont m2 sI sO`** := skipOnesRight s m1 cont ++ moveRight3 m1 m2 ++ branchOne3 m2 sI sO (consume unary field to terminator, step, branch on symbol → sI/sO; NO marker, value carried in control state). **`readSymbolAt_run_I`** (PROVED): a ones, sep O, symbol I ⇒ ∃N reachIn N (s, fs, tp)(sI, fs+a+1, tp). **`readSymbolAt_run_O`** (PROVED): symbol O ⇒ reach sO. skipOnesRight_run (468) + moveRight3_run_eq + branchOne3_run_one/_stop chained reachIn_seq3. [propext,Quot.sound], no sorry. Wired nw_readSymbolAt_run_{I,O}_frontier. HONEST: combine config+record symbol reads into symbol compare + match-or-advance into scan loop + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ReadSymbol` |
| **Entry 485 — generic scan loop: return to config start `gotoConfigStart` (PROVED)** | Navigation glue: reposition the head at the config field start (cell 1) via the home marker. **`gotoConfigStart s mid found cont`** := seekMarkLeft s mid cont ++ moveRight3 mid found (seek left to home M @0, step right to 1). **`gotoConfigStart_run`** (PROVED): home M @0, cells 1…p M-free, p<len ⇒ ∃N reachIn N (s, p, tp)(found, 1, tp). seekMarkLeft_run (387) + moveRight3_run_eq chained reachIn_seq3. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_gotoConfigStart_run_frontier. HONEST: combine config+record symbol reads (via this glue) into symbol compare + match-or-advance into scan loop + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3GotoConfig` |
| **Entry 486 — generic scan loop: read the config symbol `configSymbolVerdict` (PROVED)** | Config-side half of the symbol compare: from a record position, navigate to the config start (gotoConfigStart 485) + read config symbol (readSymbolAt 484), route on value. **`configSymbolVerdict s gm gf gc rm rc rm2 sI sO`** := gotoConfigStart s gm gf gc ++ readSymbolAt gf rm rc rm2 sI sO. **`configSymbolVerdict_run_I`** (PROVED): home M @0, region clean to p, config field ac ones + sep O + symbol I ⇒ ∃N reachIn N (s, p, tp)(sI, 1+ac+1, tp). **`configSymbolVerdict_run_O`** (PROVED): config symbol O ⇒ sO. gotoConfigStart_run + readSymbolAt_run_I/_O chained reachIn_seq3. Compiled FIRST TRY. [propext,Quot.sound], no sorry. Wired nw_configSymbolVerdict_run_{I,O}_frontier. HONEST: prefix record-symbol read → full symbol compare + match-or-advance into scan loop + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3ConfigSymbol` |
| **Entry 487 — generic scan loop: full symbol compare `symbolCompare` (PROVED)** | Complete 1-cell symbol comparison of two records. **`symbolCompare …`** := readSymbolAt(record) ++ configSymbolVerdict(rec=I path: I↦match,O↦fail) ++ configSymbolVerdict(rec=O path: I↦fail,O↦match). **`symbolCompare_run`** (PROVED): record symbol boolToSym3 rsym, config boolToSym3 csym (field/home/clean hyps) ⇒ ∃N reachIn N (s, fsR, tp)((if rsym=csym then matchSt else failSt), 1+ac+1, tp) — matchSt IFF symbols equal. 4-case (rsym×csym): readSymbolAt_run_I/_O (484) + configSymbolVerdict_run_I/_O (486) chained reachIn_seq3/append_left3; boolToSym3 reductions via rfl, if_pos/if_neg. [propext,Quot.sound], no sorry. Wired nw_symbolCompare_run_frontier. Symbol half of RecMatch DONE (state half = 480-483). HONEST: combine state+symbol → full record comparison + match-or-advance into scan loop + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3SymbolCompare` |
| **Entry 488 — generic scan loop: cursor placement `placeCursor` (PROVED)** | Setup primitive: write a comparison cursor M onto a field cell (turns the real rule table into the canonical cursTape the comparison machines consume). Inverse of eraseMark3 (482). **`placeCursor s s'`** := [((s,I),(s',M,stay))]. **`placeCursor_run`** (PROVED): tp.getD j=I ⇒ reachIn 1 (s,j,tp)(s', j, writeAt3 tp j M). 1-step concreteStep3. [propext] (tightest), no sorry. Wired nw_placeCursor_run_frontier. HONEST: place both cursors + run full record comparison + match-or-advance into scan loop + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3PlaceCursor` |
| **Entry 489 — generic scan loop: match-tail connector `endMatchTail` (PROVED)** | endMatch (481) MINUS its config step, generic over the tape — the reusable tail to attach after a config-end step in the branching loop. **`endMatchTail s skf skc rm rc matchSt`** := seekMarkRight s skf skc ++ markAdvance3 skf rm rc matchSt. **`endMatchTail_run`** (PROVED): head at q, record cursor M at r≥q, record sep O at r+1, span [q,r) M-free, in bounds ⇒ ∃N reachIn N (s, q, t)(matchSt, r+1, writeAt3(writeAt3 t r I)(r+1) O) — record cursor removed. seekMarkRight_run (388) + markAdvance3_run_end (472) chained reachIn_seq3. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_endMatchTail_run_frontier. HONEST: branching loop (tandem walk + verdict exits) + full record comparison + scan-loop wiring + generic apply + fixed U + EmitsEncodedStepEx3 remain. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UniversalTM3EndMatchTail` |
| **Brick A.1 — general composite-MOD_m CRT residue decomposition `modm_iff_residues` (PROVED)** | Pivot from fixed-U plumbing to the ACTUAL Williams content (YBT). The Beigel–Tarui integer/product-observer move generalised from the tree's MOD₆ to ARBITRARY m. **`modm m x`** := m ∣ hammingWeight x. **`dvd_iff_primePow_residues`** (PROVED): m≠0 ⇒ (m∣k ↔ ∀p∈m.primeFactors, p^(m.factorization p)∣k) — via Nat.factorization_le_iff_dvd + Prime.pow_dvd_iff_le_factorization + ordProj_dvd. **`modm_iff_residues`** (PROVED): m≠0 ⇒ (modm m x ↔ ∀p∈m.primeFactors, (hammingWeight x : ZMod (p^(m.factorization p)))=0) — general CRT residue decomposition (ZMod.natCast_eq_zero_iff per prime power). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modm_iff_residues_frontier. CRT-reduction half of single composite gate done; single-field method PROVEN dead for composite m (CompositeBTTarget). HONEST: per-prime-power polylog-degree bound (Toda/RS symmetrization, A.2) + quasipoly SYM∘AND packaging + degree-additive depth composition (C) = general YBT/composite_BT_degree, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CompositeMODFactor` |
| **Brick A.2 — single prime-MOD_p gate as degree-(p-1) F_p polynomial `modp_iff_fermat` (PROVED)** | The RS/Fermat seed representing A.1's e=1 residue terms. **`prime_pow_zero_iff`** (PROVED): in ZMod p, y=0 ↔ y^(p-1)=0 (zero_pow + ZMod.pow_card_sub_one_eq_one). **`hammingWeight_cast`** (PROVED): (hammingWeight x : ZMod p) = ∑ i, if x i then 1 else 0 (the LINEAR form; card_filter + Nat.cast_sum). **`modp_iff_fermat`** (PROVED): (hammingWeight x : ZMod p)=0 ↔ (∑ i, if x i then (1:ZMod p) else 0)^(p-1)=0 — exact degree-(p-1) F_p representation of one prime-MOD gate (linear form raised to p-1). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modp_iff_fermat_frontier. A.1+A.2 ⇒ low-degree representation of every SQUAREFREE composite-MOD gate (MOD6, MOD30, …). HONEST: prime-power e≥2 Toda lifting + MvPolynomial degree-membership + quasipoly SYM∘AND packaging + degree-additive depth composition (C) = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0PrimeMODLowDegree` |
| **Brick A.2b — squarefree composite-MOD_m = product of degree-(p-1) F_p polynomials `modm_squarefree_fermat` (PROVED)** | Assembles A.1 (CRT) ∘ A.2 (Fermat) into one explicit theorem for an entire family of composite gates. **`squarefree_dvd_primeFactors`** (PROVED): m≠0, Squarefree m ⇒ (m∣k ↔ ∀p∈primeFactors, p∣k) — via dvd_iff_primePow_residues (A.1) + squarefree_iff_factorization_le_one (factorization p=1 ⇒ p^1=p, pow_one). **`modm_squarefree_fermat`** (PROVED): m≠0, Squarefree m ⇒ (modm m x ↔ ∀p∈primeFactors, (∑ i, if x i then (1:ZMod p) else 0)^(p-1)=0) — squarefree composite gate as product of degree-(p-1) F_p polys (natCast_eq_zero_iff + modp_iff_fermat per prime). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modm_squarefree_fermat_frontier. Squarefree composite case (MOD6, MOD30, MOD105, …) FULLY low-degree-representable. HONEST: prime-power e≥2 Toda lifting + MvPolynomial degree-membership + quasipoly SYM∘AND packaging + degree-additive depth composition (C) = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SquarefreeMOD` |
| **Brick A.2c — MOD_p indicator as MvPolynomial of total degree ≤ p-1 `modp_iff_modpPoly` (PROVED)** | Makes the degree claim TYPED (Mathlib MvPolynomial.totalDegree). **`modpPoly p n`** := (∑ᵢ Xᵢ)^(p-1) : MvPolynomial (Fin n) (ZMod p). **`modpPoly_totalDegree_le`** (PROVED): (modpPoly p n).totalDegree ≤ p-1 — totalDegree_pow + totalDegree_finset_sum + totalDegree_X (needs Nontrivial, from Fact p.Prime) + Nat.mul_one. **`modpPoly_eval`** (PROVED): eval (fun i => if x i then 1 else 0) (modpPoly p n) = (∑ if x i then 1 else 0)^(p-1) — map_pow + map_sum + eval_X. **`modp_iff_modpPoly`** (PROVED): (hammingWeight x : ZMod p)=0 ↔ eval … modpPoly = 0 (via A.2). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modpPoly_totalDegree_le_frontier + nw_modp_iff_modpPoly_frontier. Degree bound now a theorem, ready for degree-additive composition. HONEST: prime-power e≥2 Toda lifting + quasipoly SYM∘AND packaging + degree-additive depth composition (C) = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0PrimeMODMvPoly` |
| **Brick D — quasipoly monomial count `degLeMonomials_card_le` (PROVED)** | The size guarantee of SYM∘AND: a degree-≤D poly has ≤ (n+1)^D monomials = quasipoly for D polylog (< 2^n). **`degLeMonomials n D`** := univ.powerset.filter (·.card ≤ D) (subsets of Fin n size ≤ D = AND-terms of fan-in ≤ D). **`degLeMonomials_card_le`** (PROVED): (degLeMonomials n D).card ≤ (n+1)^D — via SURJECTION from Fin D → Option (Fin n) (list a subset's ≤D elements via toList[k]?, pad with none); card_le_card_of_surjOn + Fintype.card_fun + getElem? membership (mem_iff_getElem?, getElem?_eq_some_iff). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_degLeMonomials_card_le_frontier. The quasipoly heart of YBT packaging. HONEST: full symmetric-count SYM∘AND assembly + prime-power e≥2 Toda lifting + degree-additive depth composition (C) = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0MonomialCount` |
| **Brick D-assembly — single MOD_m gate has exact SYM∘AND form `modGate_hasExactSymAndForm` (PROVED)** | The full SYM∘AND assembly for a single gate (the easy case): MOD_m is a symmetric function of the weight = gateCount of the n singleton ANDs (monoAND {i} = xᵢ). **`monoAND_singleton`** (PROVED): monoAND {i} x = x i. **`gateCount_singletons`** (PROVED): gateCount (singletons) x = hammingWeight x (card_filter). **`modGate_eq_symEval`** (PROVED): (fun x => decide (m ∣ hammingWeight x)) = symEval (singletons)(fun c => decide (m ∣ c)). **`modGate_hasExactSymAndForm`** (PROVED): HasExactSymAndForm SHAPE for MOD_m function — ∃M mono h, modGate = symEval(monoAND∘mono) h ∧ M+1<2^n, with M=n, n+1<2^n. Compiled clean. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modGate_hasExactSymAndForm_frontier. HONEST POINT: single gate trivially SYM∘AND (degree-1, n terms); the YBT difficulty is DEPTH COMPOSITION (where degree→polylog, count→Brick D's (n+1)^D), not the single gate. HONEST: composed circuits (depth≥2) + prime-power e≥2 Toda + degree-additive depth composition (C) + ACC0Circuit eval-wrap = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModSymAndForm` |
| **Brick C — degree composes multiplicatively under substitution `totalDegree_bind₁_le` (PROVED)** | THE YBT depth-reduction CORE. The tree had proven naive composition over GATE COUNT blows up double-exp (Composition); the RIGHT parameter is DEGREE, which composes multiplicatively. **`totalDegree_bind₁_le`** (PROVED): (∀i, (g i).totalDegree ≤ d) ⇒ (bind₁ g P).totalDegree ≤ P.totalDegree * d — via bind₁=∑ C·∏(g i)^(e i) (eval₂_eq, algebraMap=C), totalDegree_finsetSum_le + totalDegree_mul + totalDegree_finset_prod + totalDegree_pow + le_totalDegree; gcongr. **`totalDegree_bind₁_comp_le`** (PROVED): 2 layers ⇒ ≤ P.totalDegree·dh·dg. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_totalDegree_bind₁_{le,comp_le}_frontier. ⇒ constant depth d: degree ≤ D^d polylog ⇒ Brick D: monomials ≤ (n+1)^(D^d) quasipoly. The degree control the gate-count blow-up MISSED. HONEST: cross-prime composition (product observer A.1) + prime-power e≥2 Toda + wiring degree^d+D+SYM∘AND into composite_BT_degree over ACC0Circuit = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0DegreeCompose` |
| **Brick C-iterate — depth-d composition keeps degree ≤ D^d `totalDegree_bind₁_iterate` (PROVED)** | The depth-degree wiring. **`totalDegree_bind₁_iterate`** (PROVED): (∀i, (g i).totalDegree ≤ D) ⇒ ((bind₁ g)^[d] P).totalDegree ≤ P.totalDegree·D^d — induction on d over Brick C (iterate_succ_apply' + pow_succ + gcongr). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_totalDegree_bind₁_iterate_frontier. Constant d, polylog D ⇒ degree polylog ⇒ Brick D: ≤ (n+1)^(D^d) AND-terms quasipoly. CROSS-PRIME: Brick C/iterate hold over ANY CommSemiring incl. CRT product ring ∏ZMod pᵢ (A.1), so different-modulus composition needs no new degree lemma. HONEST: prime-power e≥2 Toda lifting (genuinely hard classical theorem, NOT socketed) + final ACC0Circuit-level assembly of degree^d+D+SYM∘AND into composite_BT_degree = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0DepthDegree` |
| **Brick A.3 — prime-power MOD_{p^e}: single gate done, composition obstruction HONEST `primePowerMod_*` (PROVED + honest)** | The prime-power case handled WITHOUT faking. **`primePowerMod_iff_residue`** (PROVED): modm (p^e) x ↔ (hammingWeight x : ZMod (p^e))=0 (ZMod.natCast_eq_zero_iff, NeZero p^e). **`primePowerMod_hasExactSymAndForm`** (PROVED): single MOD_{p^e} gate is exact SYM∘AND — it's a symmetric function of the weight, so Brick D-assembly (any modulus) applies. With A.1 ⇒ EVERY single composite-MOD gate (any modulus) is exact SYM∘AND (single-gate YBT CLOSED). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_primePowerMod_hasExactSymAndForm_frontier. HONEST OBSTRUCTION (NOT socketed): the composition-level low-degree lift for e≥2 has NO clean F_p/ZMod(p^e) representation (no Fermat in non-field; RS-barrier-adjacent) — BT uses integer/symmetric method, genuinely open, deliberately NOT faked. HONEST: composition-level prime-power lift + final ACC0Circuit assembly = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0PrimePowerMOD` |
| **Brick C∘A.2c — same-prime constant-depth MOD_p circuit bounded degree `modp_composition_degree` (PROVED)** | Concrete ASSEMBLY: the YBT degree machinery composing end-to-end on a real (restricted) circuit class. A depth-(d+1) uniform same-prime MOD_p circuit = d iterated substitutions of modpPoly p n = (∑ᵢ Xᵢ)^(p-1) (A.2c). **`modp_composition_degree`** (PROVED): ((bind₁ (fun _ => modpPoly p n))^[d] (modpPoly p n)).totalDegree ≤ (p-1)^(d+1) — C-iterate (totalDegree_bind₁_iterate, D=p-1) at A.2c's per-gate degree (modpPoly_totalDegree_le); calc + gcongr + pow_succ. Constant d,p ⇒ CONSTANT degree ⇒ (post-multilinearization, Brick D) polynomial-size SYM∘AND. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modp_composition_degree_frontier. NO socket. HONEST: multilinearization→Brick D + AND/OR (approximate RS poly, degree=fan-in exactly) + cross-prime nesting + prime-power composition + ACC0Circuit composite_BT_degree = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModpCompositionDegree` |
| **Bridge — degree ⇒ quasipoly AND-term count `composed_andTerms_card_le` (PROVED)** | The multilinearization bridge connecting Brick C (degree) to Brick D (AND-term count). On Boolean inputs each monomial ∏Xᵢ^eᵢ collapses to the AND of its variable-set e.support (xᵢ^k=xᵢ). **`support_card_le_totalDegree`** (PROVED): e ∈ P.support ⇒ e.support.card ≤ P.totalDegree (card_eq_sum_ones + sum_le_sum + le_totalDegree). **`andTerms_card_le`** (PROVED): P.totalDegree ≤ D ⇒ (P.support.image (·.support)).card ≤ (n+1)^D (image ⊆ degLeMonomials, card_le_card + Brick D). **`composed_andTerms_card_le`** (PROVED): depth-d composition ⇒ ≤ (n+1)^(P.totalDegree·D^d) AND-terms (andTerms_card_le ∘ C-iterate) — quasipoly for constant d, the SYM∘AND size guarantee. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_{andTerms,composed_andTerms}_card_le_frontier. Degree(C)→AND-term-count(D) bridge CLOSED. HONEST: AND/OR approximate RS polys + cross-prime nesting + prime-power composition + ACC0Circuit composite_BT_degree = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Multilinearize` |
| **Brick (AND) — AND gate as exact polynomial ∏Xᵢ `andPoly_eval` (PROVED)** | The exact AND/OR representation. **`andPoly S`** := ∏_{i∈S} Xᵢ. **`andPoly_eval`** (PROVED): eval (fun i => if x i then 1 else 0) (andPoly S) = if (∀ i ∈ S, x i = true) then 1 else 0 — exactly AND of bits in S (map_prod + eval_X + prod_eq_one/prod_eq_zero by_cases). **`andPoly_totalDegree_le`** ([Nontrivial R], PROVED): (andPoly S).totalDegree ≤ S.card = fan-in (totalDegree_finset_prod + totalDegree_X). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_andPoly_{eval,totalDegree_le}_frontier. HONEST: exact AND degree = fan-in; under composition multiplies fan-in^depth (NOT polylog) ⇒ YBT needs APPROXIMATE RS probabilistic polynomial for AND/OR (genuinely different method, OPEN, NOT faked) + cross-prime nesting + prime-power composition + ACC0Circuit composite_BT_degree = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0AndPoly` |
| **Brick (OR-approx) — RS random-linear-form OR approximator `orApprox_eval_of_form_ne_zero` (PROVED, deterministic core)** | The genuine low-degree approximate OR over F_p. **`orApprox p n a`** := (∑ᵢ C(aᵢ)·Xᵢ)^(p-1). **`orApprox_totalDegree_le`** (PROVED): totalDegree ≤ p-1 INDEPENDENT of fan-in (totalDegree_pow/finset_sum/mul/C/X, mirrors A.2c). **`orApprox_eval`** (PROVED): eval at bool x = (∑ᵢ aᵢ·[xᵢ])^(p-1) (map_pow/map_sum/map_mul/eval_C/eval_X). **`orApprox_eval_allFalse`** (PROVED): all-false ⇒ eval=0 (=OR; sum_eq_zero + zero_pow). **`orApprox_eval_of_form_ne_zero`** (PROVED): ∑ aᵢ[xᵢ]≠0 ⇒ eval=1 (=OR; ZMod.pow_card_sub_one_eq_one Fermat). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_orApprox_{totalDegree_le,eval_of_form_ne_zero}_frontier. SINGLE random linear form computes OR EXACTLY off the bad set {x≠0 : ∑aᵢxᵢ=0}, at degree p-1 not fan-in — the deterministic skeleton of the RS probabilistic polynomial making AND/OR low-degree under composition. AND via De Morgan. HONEST: probabilistic bad-set bound (≤1/p, balancedness of nonzero linear functional) + t-fold amplification (error p^-t, degree t(p-1)) + averaging/existence of good poly = OPEN, NOT faked. + cross-prime + prime-power composition + ACC0Circuit composite_BT_degree = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0OrApprox` |
| **Brick (balancedness) — nonzero linear form over F_p vanishes w.p. 1/p `card_linearForm_eq_zero` (PROVED)** | The genuine PROBABILISTIC HEART of the RS OR approximator. **`card_linearForm_eq_zero`** (PROVED): x≠0 ⇒ (univ.filter (fun a => ∑ᵢ xᵢ·aᵢ = 0)).card = p^(n-1) — the zero-fiber is a 1/p fraction. PROOF via rank-nullity: L := ∑ᵢ (xᵢ)•proj i linear functional (hLa: L a = ∑ xᵢaᵢ); L SURJECTIVE (xⱼ≠0 ⇒ Pi.single j (c·xⱼ⁻¹) hits c, mul_inv_cancel₀); range_eq_top ⇒ finrank range=1 (finrank_top+finrank_self); LinearMap.finrank_range_add_finrank_ker ⇒ finrank ker=n-1; Module.card_eq_pow_finrank (K:=ZMod p)+ZMod.card ⇒ card ker=p^(n-1); filter=ker via ext+mem_ker+hLa, Fintype.card_subtype. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_card_linearForm_eq_zero_frontier. ⇒ with orApprox_eval_of_form_ne_zero: a SINGLE random linear form computes OR with error ≤ 1/p. HONEST: t-fold amplification (error p^-t, degree t(p-1)) + averaging/existence of globally-good poly + prime-power composition + ACC0Circuit composite_BT_degree = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Balancedness` |
| **Brick (amplify) — t-fold amplification shrinks bad set to p^-t `card_allBad_eq` (PROVED)** | The amplification step of RS OR. **`card_allBad_eq`** (PROVED): x≠0 ⇒ (univ.filter (fun a : Fin t → (Fin n → ZMod p) => ∀ j, ∑ᵢ xᵢ·(aⱼ)ᵢ = 0)).card = (p^(n-1))^t — the all-bad t-tuples (every form vanishes) = product of single bad sets = (single bad)^t, a p^-t fraction of all (p^n)^t tuples. PROOF: filter = Fintype.piFinset (const univ.filter P) via ext+mem_piFinset; Fintype.card_piFinset (∏) + balancedness (card_linearForm_eq_zero=p^(n-1)) + prod_const+card_fin. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_card_allBad_eq_frontier. ⇒ t independent forms compute OR on each nonzero input with error ≤ p^-t at degree t(p-1) — INDEPENDENCE MULTIPLIES ERROR. HONEST: union bound / existence of globally-good poly (2^n·p^-t<1) + prime-power composition + ACC0Circuit composite_BT_degree = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Amplify` |
| **Brick (union bound) — existence of one globally-good random-form choice `exists_good_form` (PROVED)** | The PROBABILISTIC METHOD completion of RS OR. **`exists_good_form`** (PROVED): X (nonzero vectors), X.card·(p^(n-1))^t < (p^n)^t ⇒ ∃ a : Fin t → (Fin n → ZMod p), ∀ x ∈ X, ∃ j, ∑ᵢ xᵢ·(aⱼ)ᵢ ≠ 0 — one tuple good on EVERY test input simultaneously. PROOF: bad set = X.biUnion (bad_x); card ≤ ∑ card(bad_x) = X.card·(p^(n-1))^t (card_biUnion_le + amplify + sum_const) < (p^n)^t = card univ (Fintype.card_fun nested) ⇒ bad ≠ univ (eq_univ_iff_forall) ⇒ ∃ a ∉ bad ⇒ good. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_exists_good_form_frontier. (avoided whnf-blowup: no top-level by_contra/classical-instance-mismatch; matched filter instances to amplify.) ⇒ threshold |X|<p^t (X=nonzero Boolean inputs, |X|≤2^n, t with p^t>2^n) gives ONE FIXED degree-t(p-1) poly correct on all inputs — the RS approximate OR exists. HONEST: packaging as single MvPolynomial w/ degree cert + prime-power composition + ACC0Circuit composite_BT_degree = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnionBound` |
| **Brick (OR package) — one degree-t(p-1) poly computing OR everywhere `exists_orPoly` (PROVED)** | PACKAGES the full RS approximate-OR. **`orPoly a`** := 1 - ∏ⱼ(1 - orApprox aⱼ). **`orApprox_eval_gen`** (PROVED): eval at general F_p input = (∑ᵢ aᵢxᵢ)^(p-1). **`orPoly_totalDegree_le`** (PROVED): ≤ t·(p-1) (totalDegree_sub+max_le, totalDegree_finset_prod, sum_const). **`orPoly_eval_eq_one`** (PROVED): (∃j, ∑ᵢ(aⱼ)ᵢxᵢ≠0) ⇒ eval x = 1 (Fermat ⇒ factor 0 ⇒ prod_eq_zero). **`orPoly_eval_zero`** (PROVED): eval 0 = 0 (0^(p-1)=0 ⇒ prod_eq_one). **`exists_orPoly`** (PROVED): X nonzero, |X|·(p^(n-1))^t<(p^n)^t ⇒ ∃ P, totalDegree ≤ t(p-1) ∧ (∀x∈X, eval x P=1) ∧ eval 0 P=0 (via exists_good_form + mul_comm bridge). Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_exists_orPoly_frontier. ⇒ X=nonzero inputs, t with p^t>|X|: ONE fixed degree-t(p-1) poly computes OR correctly EVERYWHERE — the FULLY-ASSEMBLED RS approximate-OR. HONEST: prime-power MOD_{p^e} composition (A.3 obstruction, NO clean F_p form, NOT faked) + ACC0Circuit composite_BT_degree = general YBT, still OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0OrPolyPackage` |
| **Brick (circuit repr) — faithful polynomial representation of AND/OR/NOT fragment `repr_eval` (PROVED)** | The MODEL BRIDGE ACC0Circuit → MvPolynomial. **`ModFree`** (no mod gates = AC⁰), **`repr`** (const→C(0/1), var→Xᵢ, not→1-c, and→a·b, or→a+b-a·b, mod→0), **`reprDeg`** (leaf measure). **`repr_eval`** (PROVED): ModFree C ⇒ eval (bv∘x) (repr C) = bv (ACC0Circuit.eval C x) on Booleans (induction; bv_not/bv_and/bv_or helpers: bv(¬/∧/∨)=1-/·/+-· ; map_sub/add/mul + eval_X). **`repr_totalDegree_le`** (PROVED): (repr C).totalDegree ≤ reprDeg C (totalDegree_sub/add/mul/X/one + max_le). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_circuitRepr_{eval,totalDegree_le}_frontier. The exact circuit.eval=poly.eval connection. HONEST: MOD gates EXCLUDED — MOD_q over F_p (q≠p) NOT low-degree (RS), q=p^e (e≥2) A.3 obstruction ⇒ faithful single-field rep of full circuit DOESN'T EXIST; quasipoly (approximate) degree + composite_BT_degree = general YBT, still OPEN, NOT faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CircuitRepr` |
| **Brick (approx wiring) — depth-d composition of approximate-OR keeps degree ≤ (t(p-1))^(d+1) `orPoly_composition_degree` (PROVED)** | The DEGREE SIDE of the RS approximate-polynomial wiring through a circuit. **`orPoly_composition_degree`** (PROVED): ((bind₁ (fun _ => orPoly p n t a))^[d] (orPoly p n t a)).totalDegree ≤ (t·(p-1))^(d+1) — C-iterate (totalDegree_bind₁_iterate, D=t(p-1)) at orPoly's per-gate degree (orPoly_totalDegree_le); calc+gcongr+pow_succ. Approximate analogue of modp_composition_degree. Constant d, t polylog ⇒ degree polylog ⇒ (bridge) (n+1)^((t(p-1))^(d+1)) AND-terms quasipoly. Compiled FIRST TRY. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_orPoly_composition_degree_frontier. HONEST: per-gate ERROR accounting through recursion (union over gates×inputs = exists_good_form combinator) + MOD-gate handling + full faithful RS-for-circuits composite_BT_degree = general YBT, still OPEN, NOT faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0OrPolyComposition` |
| **Brick (AC⁰[p] repr) — recursive faithful representation of AC⁰[p] circuits over F_p `reprP_eval` (PROVED)** | THE RECURSIVE FAITHFUL CIRCUIT REPRESENTATION (RS setting). **`ModpOnly`** (every mod gate has modulus p), **`reprP`** (const/var/not/and/or as before + **mod p S t → 1-(∑_{i∈S} Xᵢ - C(cast t))^(p-1)**, the Fermat indicator), **`reprDegP`** (mod→p-1). **`weight_cast`**: ∑_{i∈S} bv(xᵢ) = (weightOn S x : ZMod p). **`reprP_eval`** (PROVED): ModpOnly p C ⇒ eval (bv∘x) (reprP p C) = bv (ACC0Circuit.eval C x) EXACTLY on all Booleans (induction; mod case: subst q, weight_cast, by_cases (weightOn:ZMod p)=t, Fermat ZMod.pow_card_sub_one_eq_one + zero_pow, decide=true/false). **`reprP_totalDegree_le`** (PROVED): ≤ reprDegP (mod: totalDegree_pow + finset_sum ≤1). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_reprP_{eval,totalDegree_le}_frontier. BINARY gates ⇒ EXACT, no approximation needed; constant depth ⇒ constant degree. This is the object RS runs on: AC⁰[p] is exactly low-degree over F_p; MOD_q (q≠p) is NOT (= the lower bound's content, not a gap). HONEST: prime-power composition + large-fan-in approximation wiring + composite_BT_degree cash-out = general YBT, still OPEN, NOT faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CircuitReprP` |
| **Brick (AC⁰[p] cash-out) — AC⁰[p] circuits use ≤ (n+1)^(reprDegP C) AND-terms `reprP_andTerms_card_le` (PROVED)** | The genuine SIZE CASH-OUT of the proven bricks: reprP (AC⁰[p] representation) ∘ andTerms_card_le (degree→AND-term bridge). **`reprP_andTerms_card_le`** (PROVED): ((reprP p C).support.image (·.support)).card ≤ (n+1)^(reprDegP p C). **`reprP_andTerms_lt`** (PROVED): (n+1)^(reprDegP p C)<2^n ⇒ count < 2^n (searchable cell bound). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_reprP_andTerms_{card_le,lt}_frontier. Polynomial for constant depth, <2^n under degree budget — exactly the size quantity the Williams/Route-B counting step consumes, DISCHARGED WITH REAL MATH for AC⁰[p]. HONEST: the abstract conditional cash-out mod6_composite_route_to_NEXP_not_ACC0 (RSRep=open composite_BT_degree + counting+williams+hierarchy, P≠NP-strength) + SYM∘AND form object + MOD_q(q≠p)/prime-power gates = general YBT + full Williams cash-out, still OPEN, NOT faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ReprPAndTerms` |
| **Brick (SYM∘AND form) — AC⁰[p] value as F_p-weighted sum over AND-gates `reprP_eq_sum_andTerms` (PROVED)** | THE SYM∘AND FORM OBJECT built from reprP. **`andVal S x`** := ∏_{i∈S} bv(xᵢ); **`andVal_eq_bv_monoAND`** (PROVED): andVal S x = bv(monoAND S x) (the AND-gate value; by_cases ∀i∈S xᵢ, prod_eq_one/prod_eq_zero + decide). **`eval_eq_sum_andTerms`** (PROVED): eval (bv∘x) P = ∑ e∈P.support, coeff e P · andVal(e.support) x (MvPolynomial.eval_eq; per-monomial (bv xᵢ)^(eᵢ)=bv xᵢ via cases x i + zero_pow). **`reprP_eq_sum_andTerms`** (PROVED): ModpOnly p C ⇒ bv(eval C x) = ∑ e, coeff e (reprP p C)·andVal(e.support) x (reprP_eval ∘ eval_eq_sum_andTerms). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_reprP_eq_sum_andTerms_frontier. AC⁰[p] circuit value = F_p-linear (SYM) sum over AND-gates monoAND(e.support), fan-in ≤ reprDegP C, ≤ (n+1)^(reprDegP C) terms — faithful on all Booleans = the SYM∘AND form. HONEST: repackaging F_p-sum into count-based symEval (replicate each AND by coeff, count mod p) + MOD_q(q≠p)/prime-power gates + abstract Williams cash-out = general YBT, still OPEN, NOT faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SymAndForm` |
| **Brick (count-symEval) — AC⁰[p] value as count-based symmetric function `reprP_symEval` (PROVED)** | The COUNT-BASED symEval repackaging. **`repCount P x`** := ∑ e∈P.support, (coeff e P).val * (if monoAND e.support x then 1 else 0) (plain count of coefficient-replicated AND-gates). **`repCount_cast`** (PROVED): (repCount P x : ZMod p) = eval (bv∘x) P (Nat.cast_sum + ZMod.natCast_zmod_val + andVal_eq_bv_monoAND + eval_eq_sum_andTerms). **`reprP_symEval`** (PROVED): ModpOnly p C ⇒ ACC0Circuit.eval C x = decide((repCount (reprP p C) x : ZMod p) = 1) — circuit value as h(count) with h(c)=decide((c:ZMod p)=1); cases eval C x + zero_ne_one. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_reprP_symEval_frontier. AC⁰[p] output depends only on HOW MANY replicated AND-gates accept, read mod p = the count-based symEval shape (tree's symEval/HasExactSymAndForm requirement). HONEST: literal Fin m → Finset re-indexing (equiv) + MOD_q(q≠p)/prime-power gates + Williams cash-out = general YBT, still OPEN, NOT faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SymEvalRepack` |
| **Brick (Fin m symEval) — literal HasExactSymAndForm object for AC⁰[p] `reprP_hasSymAndForm` (PROVED)** | Realises repCount as the tree's literal Fin m gateCount. **`weightedCount_realizable`** (PROVED): ∃ m mono, m=∑w ∧ ∀y, (∑ j:Fin m, if monoAND (mono j) y then 1 else 0) = ∑ e∈S, w e * (if monoAND e.support y then 1 else 0) — replicate each AND-gate by its weight via Idx=(e:↥S)×Fin(w e.val), eqv:Fin(card)≃Idx; Equiv.sum_comp + univ_sigma_univ+sum_sigma + sum_const(card_fin) + sum_coe_sort. **`reprP_hasSymAndForm`** (PROVED): ModpOnly p C, (∑(coeff).val)+1<2^n ⇒ ∃ m mono hh, ACC0Circuit.eval C = symEval (monoAND∘mono) hh ∧ m+1<2^n (hh c = decide((c:ZMod p)=1); gateCount=repCount via weightedCount + reprP_symEval). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_reprP_hasSymAndForm_frontier. THE EXACT SYM∘AND NORMAL-FORM OBJECT, completed for AC⁰[p]. HONEST: MOD_q(q≠p)/prime-power gates (RS/A.3 obstruction, no clean F_p object) + Williams cash-out (RSRep/counting/williams/hierarchy, P≠NP-strength) = general YBT + NEXP⊄ACC⁰, still OPEN, NOT faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SymEvalFinM` |
| **Brick (low-degree barrier) — RS barrier from the AC⁰[p] representation `not_acc0p_of_no_lowdeg_repr` (PROVED)** | Turns the representation into the LOWER-BOUND direction. **`acc0p_low_degree_repr`** (PROVED): ModpOnly p C ⇒ ∃ P, totalDegree ≤ reprDegP p C ∧ ∀x, eval(bv∘x) P = bv(eval C x) (= reprP packaged). **`not_acc0p_of_no_lowdeg_repr`** (PROVED): f has NO degree-≤D F_p representation ⇒ ¬∃ C, ModpOnly p C ∧ reprDegP p C ≤ D ∧ eval C = f (contrapositive of reprP_eval+reprP_totalDegree_le; rintro+rw). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_not_acc0p_of_no_lowdeg_repr_frontier. The RS low-degree barrier as a consequence of the representation: AC⁰[p] ⊆ low-degree ⇒ anything not low-degree escapes AC⁰[p]. Feeding the not-low-degree witness for MOD_q (q≠p) gives MOD_q ∉ AC⁰[p]. HONEST: the witness itself (RS lower bound, hypothesis hno here, NOT re-proved; tree's RS layer/Layer4 has it) + MOD_q/prime-power gates inside circuits + Williams cash-out = full separation, OPEN, NOT faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0LowDegreeBarrier` |
| **Brick (counting witness) — some function has no low-degree F_p representation `exists_no_lowdeg_repr` (PROVED)** | The Shannon/COUNTING not-low-degree witness in the barrier's shape. **`degLeMonomials_card_lt`** (PROVED): D<n ⇒ (degLeMonomials n D).card < 2^n (strict subset of powerset; univ has card n>D). **`exists_no_lowdeg_repr`** (PROVED): D<n ⇒ ∃ g:(Fin n→Bool)→ZMod p, ¬∃ P, totalDegree ≤ D ∧ ∀x eval(bv∘x) P = g x. PROOF: every representable g ∈ image of andComb (c:↥degLeMonomials→ZMod p) ↦ ∑ c S · andVal S, via eval_eq_sum_andTerms + sum_fiberwise_of_maps_to (group support by e.support∈degLeMonomials); card image ≤ p^(degLeMonomials.card) < p^(2^n) = card all functions (Nat.pow_lt_pow_right + degLeMonomials_card_lt); ∃ g ∉ image ⇒ not representable. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_exists_no_lowdeg_repr_frontier. Feeds the low-degree barrier ⇒ some function ∉ bounded-degree AC⁰[p]. HONEST: this is the COUNTING (non-explicit) witness; the EXPLICIT MOD_q witness = Razborov–Smolensky lower bound (deep analytic, tree's Layer4.mod_q_indicators_false / Layer3Smolensky.parity_function_lower_bound in BoolCircuitSyntax form), NOT re-proved/faked. + MOD_q/prime-power gates + Williams cash-out = full separation, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CountingWitness` |
| **Brick (parity witness) — EXPLICIT function with no low-degree F_p representation `signWt_no_lowdeg_repr` (PROVED)** | The EXPLICIT Razborov–Smolensky witness (MOD_2 in ±1 form, odd p). **`signWt p x`** := ∏ᵢ hsign(xᵢ), hsign b = if b then -1 else 1 (= (-1)^weight, the {±1}-parity). **`signWt_sq`** (PROVED): signWt²=1 (∏ hsign²=∏1). **`signWt_toggle`** (PROVED): signWt(update x i₀ (!x i₀)) = -signWt x (mul_prod_erase + update_self/of_ne + hsign_neg). **`andVal_toggle`** (PROVED): i₀∉S ⇒ andVal S(toggle)=andVal S. **`Dsign_andVal_zero`** (PROVED): S≠univ ⇒ ∑ₓ signWt x · andVal S x = 0 — TOGGLE-BIT INVOLUTION (i₀∉S): Equiv.sum_comp τ, signWt flips, andVal fixed ⇒ ∑=-∑ ⇒ ∑=0 (2≠0, p odd). **`Dsign_signWt_ne`** (PROVED): ∑ₓ signWt²= (2^n:ZMod p) ≠ 0 (p∤2^n). **`signWt_no_lowdeg_repr`** (PROVED): p≠2, D<n ⇒ ¬∃ P, totalDegree ≤ D ∧ ∀x eval(bv∘x)P = signWt x — Dsign kills low-degree (eval_eq_sum_andTerms, e.support≠univ since card≤D<n) but Dsign(signWt)=2^n≠0. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_signWt_no_lowdeg_repr_frontier. THE EXPLICIT WITNESS (not just counting). HONEST: general MOD_q (q>2) = full RS bound (tree Layer4); Boolean barrier instance via affine signWt=1-2·parity; + MOD_q/prime-power gates + Williams cash-out = full separation, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ParityWitness` |
| **Brick (parity barrier) — MOD_2 ∉ bounded-degree AC⁰[p] for odd p `parityFn_not_acc0p` (PROVED)** | The concrete q=2 Razborov–Smolensky SEPARATION in-framework. **`parityFn x`** := decide(Odd (wt x)) (Boolean MOD_2 = odd Hamming weight). **`signWt_eq_pow`** (PROVED): signWt p x = (-1)^(wt x) (prod_ite + prod_const). **`signWt_affine`** (PROVED): signWt p x = 1 - 2*bv(parityFn x) (Odd/Even.neg_one_pow). **`parityFn_no_lowdeg`** (PROVED): p≠2, D<n ⇒ ¬∃ P, totalDegree ≤ D ∧ ∀x eval(bv∘x)P = bv(parityFn x) — AFFINE from signWt_no_lowdeg: a degree-≤D rep of parity gives one of signWt (Q=C 1 - C 2*P, totalDegree ≤ D, eval = signWt), contradiction. **`parityFn_not_acc0p`** (PROVED): p≠2, D<n ⇒ ¬∃ C, ModpOnly p C ∧ reprDegP p C ≤ D ∧ eval C = parityFn — MOD_2 NOT computable by bounded-degree AC⁰[p] (fed not_acc0p_of_no_lowdeg_repr). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_parityFn_not_acc0p_frontier. BOTH RS HALVES ASSEMBLED: AC⁰[p] low-degree (reprP) + MOD_2 not (this) ⇒ MOD_2 ∉ bounded-degree AC⁰[p]. HONEST: general MOD_q (q>2) = full RS (tree Layer4); reprDegP≤D → constant-depth/any-size needs depth→degree blow-up; Williams cash-out = NEXP⊄ACC⁰, all OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ParityBarrier` |
| **Brick (depth→degree) — MOD_2 ∉ constant-DEPTH AC⁰[p] for odd p `parity_not_acc0p_depth` (PROVED)** | The depth→degree blow-up upgrading bounded-degree to bounded-DEPTH. **`reprDegP_le_depth`** (PROVED): reprDegP p C ≤ (p-1)*2^(depth C) — induction: binary AND/OR additive ⇒ doubles per level (h1,h2: 2^da,2^db ≤ 2^max; calc + pow_add; var needs p≥2; mod p-1≤(p-1)*2). **`parity_not_acc0p_depth`** (PROVED): p≠2, (p-1)*2^d < n ⇒ ¬∃ C, ModpOnly p C ∧ depth C ≤ d ∧ eval C = parityFn — depth-d circuit has degree ≤ (p-1)2^d < n ⇒ barrier kills MOD_2. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_parity_not_acc0p_depth_frontier. THE STANDARD CONSTANT-DEPTH q=2 RAZBOROV–SMOLENSKY SEPARATION (explicit n-threshold (p-1)2^d<n; any size since reprDegP is depth-only, ACC0Circuit a formula). HONEST: general MOD_q (q>2) = full RS (tree Layer4); Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0DepthDegreeBound` |
| **Brick (char witness) — MOD_q character has no low-degree F_p rep `charWt_no_lowdeg_repr` (PROVED)** | The GENERAL-q witness (q>2). **`charWt ζ x`** := ∏ᵢ (if xᵢ then ζ else 1) = ζ^weight (q-ary character). **`Dsign_charWt`** (PROVED): ∑ₓ signWt x · charWt ζ x = (1-ζ)^n — via product factorization over Boolean cube (Finset.prod_univ_sum; ∑_b (if b then -ζ else 1) = 1-ζ; prod_const). **`Dsign_eval_zero`** (PROVED): deg P ≤ D<n ⇒ Dsign(eval P)=0 (eval_eq_sum_andTerms + Dsign_andVal_zero, e.support≠univ). **`charWt_no_lowdeg_repr`** (PROVED): p≠2, ζ≠1, D<n ⇒ ¬∃ P, totalDegree≤D ∧ ∀x eval(bv∘x)P=charWt ζ x — Dsign kills low-degree but Dsign(charWt)=(1-ζ)^n≠0 (pow_ne_zero, sub_ne_zero). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_charWt_no_lowdeg_repr_frontier. GENERALISES parity (ζ=-1): every MOD_q character (ζ primitive q-th root, q∣p-1) is not low-degree. HONEST: Boolean MOD_q indicator = (1/q)∑ charWt_{ζ^j}, its degree needs ∑(1-ζ^j)^n≠0 (harder); Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CharWitness` |
| **Brick (MOD_q indicator) — Boolean MOD_q ∉ constant-depth AC⁰[p] when Dsign(MOD_q)≠0 `modq_not_acc0p_depth` (PROVED, conditional)** | **`modqFn q x`** := decide(q ∣ wt x) (Boolean MOD_q indicator). **`no_lowdeg_of_Dsign_ne`** (PROVED): Dsign f ≠ 0, D<n ⇒ ¬∃ P deg≤D, eval(bv∘x)P=f x — the GENERAL principle (Dsign kills low-degree via Dsign_eval_zero; generalises all witnesses). **`modqFn_no_lowdeg`** (PROVED): Dsign(bv∘MOD_q)≠0, D<n ⇒ MOD_q no degree-≤D rep. **`modq_not_acc0p_depth`** (PROVED): Dsign(bv∘MOD_q)≠0, (p-1)2^d<n ⇒ MOD_q ∉ depth-d AC⁰[p] (reprDegP_le_depth + barrier). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_not_acc0p_depth_frontier. HONEST: CONDITIONAL on Dsign(MOD_q)≠0 = top coefficient (1/q)∑(1-ζ^j)^n — ALWAYS for q=2; for q>2 holds for many n (e.g. n=1, MOD_q=¬) but CAN VANISH, where unconditional bound = full RS rank argument (tree Layer4), NOT proved/faked. Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModqWitness` |
| **Brick (MOD_q Fourier) — character decomposition of Boolean MOD_q indicator `Dsign_modq` (PROVED)** | The discrete-Fourier structural identity. **`charWt_eq_pow`** (PROVED): charWt ζ x = ζ^(wt x) (prod_ite). **`modq_fourier`** (PROVED): orderOf ζ = q, (q:ZMod p)≠0 ⇒ bv(MOD_q x) = q⁻¹ · ∑_{j<q} charWt(ζ^j) x — via geometric sum ∑_j (ζ^wt)^j = if q∣wt then q else 0 (geom_sum_eq, ζ^wt=1↔q∣wt by orderOf_dvd_iff_pow_eq_one; (ζ^wt)^q=1 by pow_orderOf). **`Dsign_modq`** (PROVED): ∑ₓ signWt x · bv(MOD_q x) = q⁻¹ · ∑_{j<q}(1-ζ^j)^n (Fourier + Dsign_charWt + sum_comm). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_Dsign_modq_frontier. The EXACT top coefficient of MOD_q, expressing the conditional-separation hypothesis (Dsign(MOD_q)≠0) as the character sum ∑(1-ζ^j)^n; ζ primitive q-th root exists when q∣p-1. HONEST: nonvanishing of ∑(1-ζ^j)^n for all large n = RS rank bound (tree Layer4); Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModqFourier` |
| **Brick (MOD_q period) — UNCONDITIONAL MOD_q ∉ constant-depth AC⁰[p] for n≡1 mod(p-1) `modq_not_acc0p_depth_period` (PROVED)** | Discharges the conditional separation for an INFINITE family of arities. **`pow_eq_self_period`** (PROVED): x≠0, (p-1)∣(n-1), 1≤n ⇒ x^n = x (Fermat: n=1+(p-1)k, pow_card_sub_one_eq_one). **`Dsign_modq_eq_one`** (PROVED): n≡1 mod(p-1) ⇒ Dsign(MOD_q)=1 — (1-ζ^j)^n=1-ζ^j (period), ∑(1-ζ^j)=q-∑ζ^j=q-0=q (geom_sum=0), q⁻¹·q=1. **`modq_not_acc0p_depth_period`** (PROVED): p≠2, q≥2, orderOf ζ=q, n≡1 mod(p-1), (p-1)2^d<n ⇒ MOD_q ∉ depth-d AC⁰[p] UNCONDITIONALLY (Dsign=1≠0 ⇒ modq_not_acc0p_depth). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_not_acc0p_depth_period_frontier. THE GENUINE q>2 RAZBOROV–SMOLENSKY SEPARATION for n≡1 mod(p-1) (infinite arity family; ζ primitive q-th root exists when q∣p-1). HONEST: all large n (not just n≡1 mod p-1) = full RS rank bound (tree Layer4); Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModqPeriod` |
| **Brick (MOD_q infinite) — MOD_q ∉ constant-depth AC⁰[p] for arbitrarily large arities `exists_large_arity_modq_not_acc0p` (PROVED)** | The ASYMPTOTIC packaging. **`exists_large_arity_modq_not_acc0p`** (PROVED): ∀ N, ∃ n > N, ¬∃ C : ACC0Circuit n, ModpOnly p C ∧ depth C ≤ d ∧ eval C = modqFn q — pick n = 1+(N+(p-1)2^d+1)(p-1) (≡1 mod p-1, > N, > (p-1)2^d via Nat.le_mul_of_pos_right + omega), apply modq_not_acc0p_depth_period. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_exists_large_arity_modq_not_acc0p_frontier. THE STANDARD 'infinitely many n' q>2 RAZBOROV–SMOLENSKY SEPARATION, unconditional & in-framework (ζ primitive q-th root, q∣p-1, p odd). HONEST: all large n (not just n≡1 mod p-1) = full RS rank bound (tree Layer4); Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModqInfinite` |
| **Brick (MOD_q infinite set) — MOD_q-hard arities form an infinite set `modq_hard_arities_infinite` (PROVED)** | The Set.Infinite packaging. **`modq_hard_arities_infinite`** (PROVED): {n | ¬∃ C : ACC0Circuit n, ModpOnly p C ∧ depth C ≤ d ∧ eval C = modqFn q}.Infinite — Set.infinite_of_not_bddAbove + not_bddAbove_iff + exists_large_arity_modq_not_acc0p. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_hard_arities_infinite_frontier. The cleanest standard form: the set of MOD_q-hard arities is infinite. HONEST: all large n = full RS rank bound (tree Layer4); Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModqInfiniteSet` |
| **Bridge (model translation) — ACC0Circuit → BoolCircuitSyntax eval/AC0p-preserving `toBoolSyntax_eval` (PROVED)** | Faithful translation to the tree's Layer3/Layer4 model (BoolCircuitSyntax). **`toBoolSyntax`**: const→const, var→input, not→not, and a b→andGate[·,·], or→orGate[·,·], mod p S t→modGate p t.val (S inputs). **`count_support_eq_weight`** (PROVED): count of accepting input-subcircuits over S = weightOn S x (List induction + sum_map_toList). **`toBoolSyntax_eval`** (PROVED, [NeZero p]): ModpOnly p C ⇒ BoolCircuitSyntax.eval (toBoolSyntax C) x = ACC0Circuit.eval C x (mod case: count=weight + ZMod.natCast_eq_natCast_iff = ModEq + decide_eq_decide; needs q=p≠0). **`toBoolSyntax_isAC0p`** (PROVED): ModpOnly p C ⇒ IsAC0pSyntax p (toBoolSyntax C). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_toBoolSyntax_{eval,isAC0p}_frontier. THE MODEL-BRIDGE ARTIFACT connecting the two circuit formalizations. HONEST: does NOT discharge MOD_q — Layer4.mod_q_indicators_false is a JOINT residue-family SIZE bound (different shape); using it needs residue-family construction + size bound, NOT done/faked. Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ToBoolSyntax` |
| **Bridge (Layer4 discharge) — MOD_q residue-family LB in ACC0Circuit model `modq_indicators_false_acc0` (PROVED)** | Discharges Layer4.mod_q_indicators_false into THIS framework via toBoolSyntax. **`modq_indicators_false_acc0`** (PROVED): p,q prime, q∤p ⇒ no family C : ℕ → ACC0Circuit (2m+1) of AC⁰[p] circuits (ModpOnly) computing all q residue indicators [weight≡j mod q] with RS depth/size window ⇒ False. PROOF: translate each C j via toBoolSyntax (toBoolSyntax_eval preserves eval, toBoolSyntax_isAC0p preserves AC0p), apply Layer4.mod_q_indicators_false. open Classical (toFinset DecidableEq); NeZero p from Fact prime. [propext,Classical.choice,Quot.sound], no sorry (inherits Layer4's clean axioms). Wired nw_modq_indicators_false_acc0_frontier. THE RS FAMILY LOWER BOUND, now NATIVE to the ACC0Circuit framework via the bridge. HONEST: this is the FAMILY bound (Layer4's shape); single MOD_q ∉ AC⁰[p] all-n needs the residue-family CONSTRUCTION (build all q from one), separate/intricate, NOT done/faked. size/depth on the translation (natural resource measures). Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Layer4Discharge` |
| **Bridge (residue-family construction) — pinTrue arity-extension eval-correct `pinTrue_residue_shift` (PROVED)** | The family-from-one operation. **`extend`**: pad input with k true bits (Fin.append x (const true)). **`weight_extend`** (PROVED): weight(x⧺1ᵏ)=weight(x)+k (card_filter + Fin.sum_univ_add). **`weightOn_extend_split`** (PROVED): weightOn S (extend x)=weightOn (lowSupport S) x + highCard S (filter split + sum_bij' reindex castAdd/castLT). **`pinTrue`**: ACC0Circuit (n+k)→ACC0Circuit n fixing last k inputs to true (var i→if i<n then var (castLT i) else const true; mod q S t→mod q (lowSupport S) (t-highCard)). **`pinTrue_eval`** (PROVED): eval (pinTrue C) x = eval C (extend x) (mod case: weightOn_extend_split + decide_eq_decide + eq_sub_iff_add_eq). **`pinTrue_modpOnly`**, **`pinTrue_depth`** (≤) (PROVED). **`pinTrue_residue_shift`** (PROVED): residue-0 circuit at n+k, pinned, computes [weight(x)+k≡0 mod q] — builds any residue from one. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_pinTrue_residue_shift_frontier, nw_weight_extend_frontier. THE RESIDUE-FAMILY CONSTRUCTION OPERATION (eval/AC⁰[p]/depth correct). HONEST: full single-MOD_q all-n via modq_indicators_false_acc0 additionally needs subcircuit-count (size) bound through pinTrue + uniform residue-0 family across arities n..n+q-1 — quantitative step NOT done/faked. Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ResidueFamily` |
| **Bridge (size bound through pinTrue) — subcircuit count monotone under input-pinning `pinTrue_card_le` (PROVED)** | The quantitative step. **`subcircuitsList_map_input_length`** (PROVED): subcircuitsList of a list of input gates has length = index list length. **`pinTrue_tbs_length_le`** (PROVED): (subcircuits (toBoolSyntax (pinTrue C))).length ≤ (subcircuits (toBoolSyntax C)).length — induction; mod case via subcircuitsList_map_input_length + (lowSupport S).card ≤ S.card (card_le_card_of_injOn castAdd). **`pinTrue_card_le`** (PROVED): (subcircuits (toBoolSyntax (pinTrue C))).toFinset.card ≤ (subcircuits (toBoolSyntax C)).length (toFinset_card_le + length monotone). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_pinTrue_card_le_frontier. THE SIZE BOUND THROUGH PINTRUE: residue-0 size bound transfers to every pinned residue indicator (feeds modq_indicators_false_acc0's subcircuit-count hyp). HONEST: final single-MOD_q all-n still needs the uniform residue-0 family assembly across arities n..n+q-1 + arity/window bookkeeping (wiring). Williams cash-out = NEXP⊄ACC⁰, OPEN. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0PinSize` |
| **Bridge (final assembly) — MOD_q ∉ AC⁰[p] from a uniform residue-0 family `modq_not_acc0p_uniform` (PROVED)** | THE LAYER4 ROUTE, ASSEMBLED END TO END. **`mod_shift`** (PROVED): j<q ⇒ ((w+(q-j))%q=0 ↔ w%q=j) (via ZMod: natCast_eq_zero_iff + natCast_eq_natCast_iff + cast_sub + sub_eq_zero). **`tbs_depth_eq`** (PROVED): BoolCircuitSyntax.depth (toBoolSyntax C) = ACC0 depth C (foldl_depth_map_input for mod; omega+max for and/or). **`pinTrue_tbs_depth_le`** (PROVED): depth non-increasing under pinTrue (tbs_depth_eq + pinTrue_depth). **`modq_not_acc0p_uniform`** (PROVED): p,q prime, q∤p, RS window 16((p-1)t)^d)²<2m+3 ⇒ NO uniform AC⁰[p] family D (one per arity N, eval=residue-0, ModpOnly, depth (toBoolSyntax (D N))≤d, 4q·(subcircuits(toBoolSyntax(D N))).length≤p^t) computes MOD_q ⇒ False. PROOF: C j := pinTrue (D ((2m+1)+(q-j))); pinTrue_residue_shift + mod_shift give eval=[weight%q=j]; pinTrue_modpOnly/pinTrue_tbs_depth_le/pinTrue_card_le transfer the bounds; apply modq_indicators_false_acc0. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_not_acc0p_uniform_frontier. THE FULL LAYER4 ROUTE TO MOD_q ∉ AC⁰[p] (uniform RS resource regime). HONEST: Williams cash-out to NEXP⊄ACC⁰ is a DIFFERENT P≠NP-strength theorem, OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModqUniform` |
| **Bridge (size lower bound) — MOD_q needs super-polynomial AC⁰[p] size `modq_requires_large_size` (PROVED)** | THE RS SIZE LOWER BOUND. **`modq_requires_large_size`** (PROVED): p,q prime, q∤p, any uniform AC⁰[p] family D computing MOD_q at depth d, any t≥1 ⇒ ∃N, p^t < 4q·(subcircuits(toBoolSyntax(D N))).length — so size NOT polynomially bounded (t arbitrary). PROOF: by_contra + push_neg (∀N size ≤ p^t/(4q)) ⇒ choose m=8((p-1)t)^d)² (satisfies RS window 16(...)²<2m+3 by omega) ⇒ modq_not_acc0p_uniform ⇒ False. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_requires_large_size_frontier. MOD_q ∉ poly-size constant-depth AC⁰[p] — the classical exponential size LB. HONEST: Williams cash-out to NEXP⊄ACC⁰ is a DIFFERENT P≠NP-strength theorem (algorithmic method), OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModqSize` |
| **Bridge (concrete instantiation) — MOD_3 needs super-poly AC⁰[5] size `mod3_requires_large_size_acc05` (PROVED)** | A concrete named separation. **`mod3_requires_large_size_acc05`** (PROVED): p=5, q=3 (3∤5 by decide), any uniform AC⁰[5] family D computing MOD_3 at depth d, any t≥1 ⇒ ∃N, 5^t < 12·(subcircuits(toBoolSyntax(D N))).length — MOD_3 ∉ poly-size constant-depth AC⁰[5]. PROOF: Fact 5/3 prime (norm_num), instantiate modq_requires_large_size (hpt1 4t≥1 by omega). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_mod3_requires_large_size_acc05_frontier. Concrete demonstration that the abstract polynomial-method machinery yields named separations. HONEST: Williams cash-out to NEXP⊄ACC⁰ OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Mod3Acc5` |
| **Bridge (explicit exponential) — MOD_q size reaches p^t by polynomial arity `modq_size_blowup` (PROVED)** | THE EXPLICIT EXPONENTIAL SIZE LB. **`modq_size_blowup`** (PROVED): p,q prime, q∤p, uniform AC⁰[p] family D computing MOD_q at depth d, t≥1 ⇒ ∃N ≤ 16((p-1)t)^d)²+1+q (polynomial in t), p^t < 4q·(subcircuits(toBoolSyntax(D N))).length — i.e. size(n) ≥ p^{Ω(n^{1/2d})}. PROOF: by_contra + push_neg (∀N≤G, 4q·length ≤ p^t) ⇒ re-run modq_indicators_false_acc0 with m=8(((p-1)t)^d)², residue family at arities (2m+1)+(q-j) ≤ 2m+1+q = G; size via pinTrue_card_le + hcon at bounded arity; window omega; (d:=d) explicit. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_size_blowup_frontier. THE EXPLICIT EXPONENTIAL FORM: size reaches p^t (exponential) by arity polynomial in t = no roots needed, captures the 2^{n^{1/2d}} blow-up exactly. HONEST: Williams cash-out to NEXP⊄ACC⁰ OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModqExp` |
| **Bridge (super-polynomial) — MOD_q ∉ poly-size AC⁰[p] `modq_superpoly` (PROVED)** | THE DEFINITIVE SUPER-POLYNOMIAL SEPARATION. **`exp_beats_poly`** (PROVED): p≥2 ⇒ ∃t≥1, a·t^B < p^t (exponential beats polynomial; via tendsto_pow_const_mul_const_pow_of_abs_lt_one with r=1/p, eventually_lt_const, cast). **`modq_superpoly`** (PROVED): p,q prime, q∤p, uniform AC⁰[p] family computing MOD_q at depth d, ∀c ⇒ ∃N, N^c < 4q·(subcircuits(toBoolSyntax(D N))).length — size beats EVERY polynomial. PROOF: exp_beats_poly(K^c, 2dc) with K=16(p-1)^{2d}+1+q gives t with K^c·t^{2dc}<p^t; modq_size_blowup gives N≤G(t)=16(((p-1)t)^d)²+1+q; G(t)≤K·t^{2d} (hexpand ((p-1)t)^d)²=(p-1)^{2d}t^{2d} + nlinarith); N^c ≤ G(t)^c ≤ K^c·t^{2dc} < p^t < 4q size N. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_superpoly_frontier. MOD_q ∉ POLYNOMIAL-SIZE constant-depth AC⁰[p] — the definitive polynomial-method separation. HONEST: Williams cash-out to NEXP⊄ACC⁰ OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Superpoly` |
| **Bridge (PARITY ∉ AC⁰) — the classic constant-depth LB `parity_not_ac0` (PROVED)** | THE FAMOUS FSS/HÅSTAD LOWER BOUND, via Razborov–Smolensky (AC⁰ ⊆ AC⁰[p]). **`ModFree`**: the AC⁰ predicate (no MOD gates; const/var True, not/and/or recurse, mod→False). **`modFree_modpOnly`** (PROVED): ModFree C ⇒ ModpOnly p C for EVERY p (mod case vacuous since ModFree(mod)=False). **`parity_not_ac0`** (PROVED): n > 2^{d+1} (2*2^d < n) ⇒ no depth-d MOD-free (AC⁰) circuit computes PARITY. PROOF: a MOD-free circuit computing PARITY is ModpOnly 3, contradicting parity_not_acc0p_depth (p=3, MOD_2 ∉ AC⁰[3]). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_parity_not_ac0_frontier. PARITY ∉ AC⁰ — the most famous constant-depth lower bound, as a clean RS corollary. HONEST: Williams cash-out to NEXP⊄ACC⁰ OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ParityAC0` |
| **Bridge (MOD_q ∉ AC⁰) — MOD-counting LB in plain AC⁰ `mod3_not_ac0` (PROVED)** | The q>2 analogue of PARITY ∉ AC⁰ (classic MOD_m ∉ AC⁰), via RS. **`modq_not_ac0_period`** (PROVED): p≠2 prime, primitive q-th root ζ in F_p, n≡1 mod(p-1), (p-1)2^d<n ⇒ no depth-d AC⁰ circuit computes MOD_q (ModFree→ModpOnly p + modq_not_acc0p_depth_period). **`mod3_not_ac0`** (PROVED): concrete q=3, p=7, ζ=2 (orderOf(2:ZMod 7)=3 via orderOf_eq_prime), n≡1 mod 6, 6*2^d<n ⇒ MOD_3 ∉ AC⁰. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_mod3_not_ac0_frontier. MOD_q ∉ AC⁰ (the MOD-counting analogue of Håstad), concretely MOD_3 ∉ AC⁰. HONEST: general q needs prime p≡1 mod q (Dirichlet), supplied concretely. Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModqAC0` |
| **Bridge (AC⁰ ⊊ AC⁰[2]) — the constant-depth hierarchy is STRICT `ac0_strict_subset_acc02` (PROVED)** | A CLASS SEPARATION (not just a function LB): MOD gates add power. **`mod2_cast_eq_one`** (PROVED): (n:ZMod 2)=1 ↔ Odd n (natCast_eq_natCast_iff + ModEq + omega). **`eval_mod2_univ`** (PROVED): the single gate mod 2 univ 1 computes PARITY (weightOn univ = wt + mod2_cast_eq_one + decide_eq_decide). **`parity_mem_acc02`** (PROVED): PARITY ∈ AC⁰[2] at depth 1 (witness mod 2 univ 1, ModpOnly 2 by rfl). **`ac0_strict_subset_acc02`** (PROVED): n>2^{d+1} ⇒ PARITY ∈ AC⁰[2] \ AC⁰ ⇒ AC⁰ ⊊ AC⁰[2]. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_ac0_strict_subset_acc02_frontier. THE STRICT SEPARATION AC⁰ ⊊ AC⁰[2]: a single MOD_2 gate computes PARITY but no AC⁰ circuit does. HONEST: Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0StrictSep` |
| **Bridge (AC⁰[2] ⊄ AC⁰[3]) — the prime modulus matters `acc02_not_subset_acc03` (PROVED)** | Separating two MOD-PRIME classes (modulus not interchangeable). **`acc02_not_subset_acc03`** (PROVED): n>2^{d+1} ⇒ PARITY ∈ AC⁰[2] (one MOD_2 gate, parity_mem_acc02) but PARITY ∉ AC⁰[3] (parity_not_acc0p_depth p=3, all n) ⇒ AC⁰[2] ⊄ AC⁰[3], so AC⁰[2] ≠ AC⁰[3]. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_acc02_not_subset_acc03_frontier. THE PRIME MODULUS MATTERS: AC⁰[2] ≠ AC⁰[3]. HONEST: reverse AC⁰[3] ⊄ AC⁰[2] needs MOD_3 ∉ AC⁰[2] (p=2 has no primitive cube root — not supplied). Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0PrimeSep` |
| **Bridge (AC⁰ ⊊ AC⁰[3]) — every prime modulus strictly extends AC⁰ `ac0_strict_subset_acc03` (PROVED)** | The q=3 companion to AC⁰ ⊊ AC⁰[2]. **`eval_mod3_univ`** (PROVED): mod 3 univ 0 computes MOD_3 (weightOn univ = wt + ZMod.natCast_eq_zero_iff + decide_eq_decide). **`mod3_mem_acc03`** (PROVED): MOD_3 ∈ AC⁰[3] depth 1. **`ac0_strict_subset_acc03`** (PROVED): n≡1 mod 6, 6*2^d<n ⇒ MOD_3 ∈ AC⁰[3] \ AC⁰ ⇒ AC⁰ ⊊ AC⁰[3]. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_ac0_strict_subset_acc03_frontier. AC⁰ ⊊ AC⁰[3] — MOD_3 gate strictly increases constant-depth power. HONEST: Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0StrictSep3` |
| **Bridge (AC⁰[3] ⊊ AC⁰[{2,3}]) — adding moduli strictly grows the class `acc03_strict_subset_acc023` (PROVED)** | The MULTI-MODULUS hierarchy grows. **`ModIn23`**: AC⁰[{2,3}] predicate (MOD moduli ∈ {2,3}). **`modpOnly3_modIn23`** (PROVED): AC⁰[3] ⊆ AC⁰[{2,3}] (mod case 3=3 → Or.inr). **`parity_mem_acc023`** (PROVED): PARITY ∈ AC⁰[{2,3}] depth 1 (mod 2 univ 1, ModIn23 by Or.inl rfl). **`acc03_strict_subset_acc023`** (PROVED): n>2^{d+1} ⇒ PARITY ∈ AC⁰[{2,3}] \ AC⁰[3] ⇒ AC⁰[3] ⊊ AC⁰[{2,3}]. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_acc03_strict_subset_acc023_frontier. AC⁰[3] ⊊ AC⁰[{2,3}] — granting a new prime strictly increases power. HONEST: Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0MultiMod` |
| **Bridge (model upgrade) — PARITY needs super-poly size in UNBOUNDED-fan-in AC⁰[p] `parity_superpoly_ac0p` (PROVED)** | THE REAL RS BOUND (genuine model, not binary). The earlier ACC0Circuit uses BINARY and/or (constant-depth = ≤2^d leaves, weak). This states the REAL bound in BoolCircuitSyntax (List-based UNBOUNDED-fan-in gates, the Layer3/Layer4 model). **`parity_superpoly_ac0p`** (PROVED): IsAC0pSyntax p Cir, depth ≤ d, computes PARITY, window 8((p-1)t)^d)²≤m ⇒ p^t < 4·(subcircuits Cir).toFinset.card — super-poly size. PROOF: Layer3.parity_function_lower_bound + Layer4.hmod_of_isAC0p (IsAC0pSyntax ⇒ all MOD gates modulus p). open Classical (toFinset). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_parity_superpoly_ac0p_frontier. THE GENUINE HÅSTAD/RS THEOREM in the real unbounded-fan-in model — upgrades the binary-fan-in parity_not_ac0. HONEST: Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnboundedParity` |
| **Bridge (PARITY ∉ poly-size AC⁰, real model) — Håstad via RS, unbounded fan-in `parity_superpoly_ac0` (PROVED)** | THE CANONICAL FSS/HÅSTAD LOWER BOUND in the genuine model. **`isAC0_of_mem_subcircuits`** (PROVED): AC⁰ (no-MOD) inherited by subcircuits (match-recursion mirroring isAC0p_of_mem_subcircuits). **`hmod_of_isAC0`** (PROVED): AC⁰ circuit has no MOD gates ⇒ hmod vacuous. **`parity_superpoly_ac0`** (PROVED): IsAC0Syntax Cir, depth ≤ d, computes PARITY, RS window ⇒ p^t < 4·(subcircuits Cir).toFinset.card (any odd prime p) — super-poly size. PROOF: parity_function_lower_bound + hmod_of_isAC0. open Classical. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_parity_superpoly_ac0_frontier. PARITY ∉ poly-size unbounded-fan-in AC⁰ — the real FSS/Håstad theorem. HONEST: Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnboundedAC0` |
| **Bridge (MOD_q ∉ AC⁰, real model) — residue-family no-go, unbounded fan-in `modq_indicators_false_ac0` (PROVED)** | The q-counting companion to parity_superpoly_ac0, real model. **`isAC0_isAC0p`** (PROVED): IsAC0Syntax C → IsAC0pSyntax p C (AC⁰ ⊆ AC⁰[p] syntactically; match-recursion on nested inductive, mod case vacuous). **`modq_indicators_false_ac0`** (PROVED): p,q prime, q∤p ⇒ no unbounded-fan-in AC⁰ (IsAC0Syntax) family C : ℕ → BoolCircuitSyntax (2m+1) computes all q residue indicators [weight≡j mod q] in RS window ⇒ False. PROOF: isAC0_isAC0p + Layer4.mod_q_indicators_false. open Classical. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_indicators_false_ac0_frontier. MOD_q residue-family no-go for real unbounded-fan-in AC⁰. HONEST: single-MOD_q needs residue construction; Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnboundedModq` |
| **Bridge (AC⁰ ⊊ AC⁰[2], real model) — strict separation, unbounded fan-in `ac0_strict_subset_acc02_unbounded` (PROVED)** | Class separation in the genuine model. **`parity_mem_acc02_unbounded`** (PROVED): PARITY ∈ AC⁰[2] depth 1 — witness toBoolSyntax (mod 2 univ 1) (one unbounded-fan-in MOD_2 gate); IsAC0pSyntax 2 via toBoolSyntax_isAC0p, depth via tbs_depth_eq, eval via toBoolSyntax_eval + eval_mod2_univ. **`ac0_strict_subset_acc02_unbounded`** (PROVED): ht1, RS window ⇒ PARITY is a depth-1 AC⁰[2] circuit ∧ every depth-d AC⁰ circuit computing it has 3^t < 4·subcircuits (super-poly) ⇒ AC⁰ ⊊ AC⁰[2]. PROOF: parity_mem_acc02_unbounded + parity_superpoly_ac0 (p=3). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_ac0_strict_subset_acc02_unbounded_frontier. AC⁰ ⊊ AC⁰[2] in the REAL unbounded-fan-in model. HONEST: Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnboundedSep` |
| **Bridge (AC⁰[2] ⊄ AC⁰[3], real model) — prime modulus matters, unbounded fan-in `acc02_not_subset_acc03_unbounded` (PROVED)** | Prime-modulus separation in the genuine model. **`acc02_not_subset_acc03_unbounded`** (PROVED): ht1, RS window ⇒ PARITY is a depth-1 AC⁰[2] circuit (parity_mem_acc02_unbounded, one MOD_2 gate) ∧ every depth-d AC⁰[3] circuit computing it has 3^t < 4·subcircuits (super-poly, parity_superpoly_ac0p p=3) ⇒ AC⁰[2] ⊄ AC⁰[3], AC⁰[2] ≠ AC⁰[3]. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_acc02_not_subset_acc03_unbounded_frontier. AC⁰[2] ≠ AC⁰[3] in the REAL unbounded-fan-in model. HONEST: reverse AC⁰[3]⊄AC⁰[2] needs MOD_3∉AC⁰[2] (p=2 no cube root); Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnboundedPrimeSep` |
| **Bridge (MOD_q ∉ AC⁰[p], real model, single function) — uniform residue-0 no-go `modq_uniform_false_real` (PROVED)** | The single-MOD_q lower bound in unbounded fan-in (real-model analogue of modq_not_acc0p_uniform). **`modq_uniform_false_real`** (PROVED): p,q prime, q∤p, uniform AC⁰[p] family D : (N)→BoolCircuitSyntax N computing MOD_q (residue-0) at every arity, IsAC0pSyntax, depth ≤ d, padding-adjusted size ≤ p^t/(4q), RS window ⇒ False. PROOF: instantiate Layer4.mod_q_family_false with D j = D ((2m+1)+(q-j)) (the repo's real-model residue construction: padTrue + padTrue_computes_indicator). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_uniform_false_real_frontier. Single-MOD_q residue-0 no-go for unbounded-fan-in AC⁰[p], via the existing clean Layer4 padding machinery (no ACC0Circuit pinTrue needed). HONEST: Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnboundedModqUniform` |
| **Bridge (MOD_q ∉ AC⁰, real model, single function) — uniform no-go, unbounded fan-in `modq_uniform_false_ac0_real` (PROVED)** | The single-MOD_q residue-0 no-go for plain AC⁰ (no MOD), real model. **`modq_uniform_false_ac0_real`** (PROVED): p,q prime, q∤p, uniform AC⁰ (IsAC0Syntax) family computing MOD_q (residue-0), bounded depth/size, RS window ⇒ False. PROOF: isAC0_isAC0p (AC⁰ ⊆ AC⁰[p]) + modq_uniform_false_real. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modq_uniform_false_ac0_real_frontier. Completes the real-model single-function MOD_q picture (no-MOD companion to modq_uniform_false_real, single-residue companion to modq_indicators_false_ac0). HONEST: Williams cash-out OPEN/not faked. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0UnboundedModqAC0` |
| **Bridge (Williams via N-Frame, honest anatomy) — collapse socket IS separation-strength `nframe_collapse_socket_iff_separation` (PROVED)** | THE HONEST WILLIAMS CONDITIONAL ANATOMY (self-audit, like the HM-socket-vacuous move). The N-Frame counting route = fast-SAT route (nframe_williams_route_equiv); char-0 escape handles composite MOD (nframe_observer_characteristic_free). **`nframe_collapse_socket_iff_separation`** (PROVED): given the proved-structure counting route + uniform-realization socket + nondeterministic time hierarchy, the algorithmic-method collapse socket (speedup → NEXP⊆ACC0 → NTIME collapse) ↔ ¬(NEXP⊆ACC0). Forward: full Williams composition (nframe_fastSat_to_timeHierarchy); backward: ¬(NEXP⊆ACC0) makes the collapse VACUOUS. So the route does NOT reduce the separation — the load-bearing socket IS the separation (P≠NP-strength). **`nexp_not_acc0_via_nframe`** (PROVED conditional): ¬(NEXP⊆ACC0) from YBT socket (WilliamsFastSatRoute = Beigel-Tarui, classical/formalization-gap) + algorithmic-method sockets. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_nframe_collapse_socket_iff_separation_frontier. HONEST CONDITIONAL ANATOMY: two open contents named/classified — YBT (classical, NOT P≠NP-strength) + collapse socket (PROVED = the separation). NO fake unconditional NEXP⊄ACC⁰. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0NFrameWilliamsAnatomy` |
| **Bridge (HARD MATH, Beigel–Tarui gate-degree rung) — Boolean gates are low-degree `gateBinary_degree` (PROVED)** | A genuine rung of the Beigel–Tarui hard combinatorics (the residual content of compositeBT_representation's hgu/hgb). **`gateUnary`/`gateBinary`**: exact multilinear interpolation of any unary/binary Boolean function (chiU/chiB selector basis). **`gateUnary_degree`** (PROVED): totalDegree ≤ 1. **`gateBinary_degree`** (PROVED): totalDegree ≤ 2 (totalDegree_add/mul/C + chi degree + omega). **`gateUnary_eval`/`gateBinary_eval`** (PROVED): eval-correct on Boolean inputs (cases b <;> simp, selector collapses the sum). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_gateBinary_degree_frontier. THE δ=2 GATE-DEGREE INPUT TO BT genuinely established for all Boolean (AND/OR/NOT) gates. HONEST: the MOD_m gate's polylog (Toda) degree is the remaining hard combinatorics; unconditional NEXP⊄ACC⁰ = P≠NP-strength, not done. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0GatePolyDegree` |
| **Bridge (HARD MATH, Toda/RS MOD degree) — MOD_p is degree p−1 over F_p `modpPoly_degree` (PROVED)** | THE BASE RUNG of the composite-MOD Toda degree (the MOD gate's low-degree poly that BT needs). **`modpPoly`**: 1 − (∑_{i∈S} X_i)^{p−1} over ZMod p. **`modpPoly_degree`** (PROVED): totalDegree ≤ p−1 (totalDegree_sub + totalDegree_pow + sumPoly_degree ≤ 1 via totalDegree_finset_sum + totalDegree_X). **`modpPoly_eval`** (PROVED): evaluates to the MOD_p residue-0 indicator on Boolean inputs (Fermat: a^{p−1}=0 if a=0 else 1; ZMod.pow_card_sub_one_eq_one). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modpPoly_degree_frontier. THE EXACT LOW-DEGREE (p−1) Toda/RS REPRESENTATION of the prime-modulus MOD gate — degree O(1) in fan-in, the input driving BT's quasipoly SYM∘AND count. HONEST: composite m needs prime-power+CRT lifting (remaining rung); unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModpTodaPoly` |
| **Bridge (HARD MATH, prime-power Toda lifting) — MOD_{p^e} root-of-unity representation `primePowerMod_charSum` (PROVED)** | The prime-power rung: where Fermat F_p fails (e≥2, RS barrier), the root-of-unity representation lifts. **`primePowerMod_charSum`** (PROVED): over F_ℓ with a primitive (p^e)-th root ζ (ℓ prime, p^e | ℓ-1, ℓ≠p), bv(MOD_{p^e} x) = (p^e)⁻¹ ∑_{j<p^e} charWt(ζʲ) x — instantiating the proved modq_fourier at q = p^e. **`mod4_charSum`** (PROVED): concrete MOD_4 = MOD_{2²} over F_5, ζ=2 (orderOf 2 = 4 via orderOf_eq_prime_pow). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_primePowerMod_charSum_frontier. THE TODA INTEGER/ROOT-OF-UNITY REPRESENTATION of the prime-power MOD gate — the lift the prime-power case genuinely needs (the doc's 'different representation'), where the low-degree F_p form does not exist. HONEST: the SYM∘AND count consequence for composition = separate BT count argument; unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0PrimePowerToda` |
| **Bridge (HARD MATH, SYM∘AND composition count for MOD_p circuits) — unconditional quasipoly count `modp_endToEnd` (PROVED)** | The MOD_p analogue of endToEnd_BT, made UNCONDITIONAL. **`substP`**: the MOD_p-circuit substitution (una→MOD_p gate modPGate p=1−X^{p−1}; bin→bilinear Boolean gbP). **`modp_endToEnd`** (PROVED): odd prime p, MOD_p-circuit c of depth d ⇒ (substP p c).totalDegree ≤ (p−1)^{d+1} ∧ monomial-support (SYM∘AND AND-term) count ≤ (n+1)^{(p−1)^{d+1}} ∧ sparse sum — quasipoly for constant depth. PROOF: discharge modP_circuit_representation with substP, gbP (hinp X deg ≤ p−1, hgb gbP_deg 2 ≤ p−1 via omega, huna/hbin by rfl). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modp_endToEnd_frontier. THE UNCONDITIONAL SYM∘AND COMPOSITION COUNT for MOD_p-circuits — the AND-term count is quasipoly, via the degree-(p−1) Toda/Fermat MOD_p gate. HONEST: composite MOD_m CRT assembly + unconditional NEXP⊄ACC⁰ = P≠NP-strength, not done. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModpEndToEnd` |
| **Bridge (HARD MATH, composite MOD_m CRT assembly) — squarefree MOD_m = AND of prime MOD_p `modm_squarefree_and` (PROVED)** | The CRT reduction of composite (squarefree) MOD to prime gates. **`squarefree_factorization_eq_one`** (PROVED): squarefree m ⇒ factorization p = 1 on prime factors (squarefree_iff_factorization_le_one + factorization_pos_of_dvd + omega). **`modm_squarefree_and`** (PROVED): m squarefree ⇒ MOD_m(x) ↔ ∀ p ∈ m.primeFactors, (weight x : ZMod p) = 0 — composite gate = AND of prime MOD_p gates (modm_iff_residues + factorization=1 + pow_one). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modm_squarefree_and_frontier. THE CRT ASSEMBLY carrying the per-prime Toda/Fermat low-degree + quasipoly SYM∘AND count (modp_endToEnd) to composite squarefree moduli (composite gate = AND of degree-(p−1) prime gates). HONEST: prime-power multiplicity e≥2 (via primePowerMod_charSum root-of-unity) + unconditional NEXP⊄ACC⁰ = P≠NP-strength, not done. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CompositeMODCRT` |
| **Bridge (HARD MATH, BT count ⇒ fast-SAT regime) — quasipoly SYM∘AND count drops below 2^n `modp_fastsat_regime` (PROVED)** | The link from the BT count to the Williams fast-SAT ingredient. **`quasipoly_lt_exp`** (PROVED): ∀ D, ∃ n≥1, (n+1)^D < 2^n (via exp_beats_poly: (t+1)^D ≤ (2t)^D = 2^D·t^D < 2^t). **`modp_fastsat_regime`** (PROVED): odd prime p, constant depth d ⇒ ∃ arity n, every depth-d MOD_p-circuit has SYM∘AND AND-term count < 2^n (modp_endToEnd count ≤ (n+1)^{(p−1)^{d+1}} < 2^n). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modp_fastsat_regime_frontier. THE BT COUNT DROPS BELOW 2^n — the sub-exponential cell search = Williams' algorithmic fast-SAT ingredient, reached for constant-depth MOD_p circuits. HONEST: realization/collapse sockets to NEXP⊄ACC⁰ remain (collapse socket = P≠NP-strength, proved separation-equivalent). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModpFastSAT` |
| **Bridge (HARD MATH, composite MOD_m prime-power multiplicity) — root-of-unity rep for any m `modm_charSum` (PROVED)** | The composite-with-multiplicity case via the factorization-independent root-of-unity representation. **`modm_charSum`** (PROVED): any m, over F_ℓ with a primitive m-th root ζ (ℓ prime, m∣ℓ−1, ℓ∤m), bv(MOD_m x) = m⁻¹ ∑_{j<m} charWt(ζʲ) x — modq_fourier at q=m, NO dependence on factorization. **`mod12_charSum`** (PROVED): concrete MOD_{12} = MOD_{2²·3} (multiplicity 2 on prime 2) over F_13, ζ=2 (orderOf 2 = 12 via orderOf_eq_of_pow_and_pow_div_prime + interval_cases). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modm_charSum_frontier. THE PRIME-POWER MULTIPLICITY COMPOSITE CASE: any MOD_m (any factorization) has the exact root-of-unity rep, seamlessly covering e≥2 multiplicities. HONEST: SYM∘AND count for composition + unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CompositeMultiplicity` |
| **Bridge (HARD MATH, depth-2 prime-power SYM∘AND count) — MOD_{p^e}∘AND linear cell count `modpe_depth2_count` (PROVED)** | The depth-2 count via the symmetric structure (where low-degree poly fails for e≥2). **`gateCount_le`** (PROVED): count of satisfied gates ≤ k (sum of k indicators). **`cells_card_le`** (PROVED): count-cell image ≤ k+1 cells (image ⊆ range(k+1)). **`modpe_depth2_count`** (PROVED): MOD_{p^e}(AND_1,…,AND_k) has ≤ k+1 cells ∧ Satisfiable ↔ ∃ count-cell c with p^e ∣ c (fastSat_decides_every_modulus). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modpe_depth2_count_frontier. THE DEPTH-2 PRIME-POWER COUNT: the symmetric MOD_{p^e} top reduces SAT over the high-degree composition to a LINEAR (≤k+1) cell search — the count the polynomial-degree route can't give for e≥2. HONEST: deeper (depth≥3) compositions + unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModpeDepth2` |
| **Bridge (HARD MATH, depth-3 prime-power composition) — cell count multiplies per layer `depth3_modpe_cells` (PROVED)** | The depth-3 recursion via the joint count vector. **`jointCells_card_le`** (PROVED): the joint count vector of t count-gates (each ≤ m gates) takes ≤ (m+1)^t values (image ⊆ Fintype.piFinset (range(m+1)); card_piFinset + prod_const). **`depth3_modpe_cells`** (PROVED): depth-3 over t depth-2 MOD_{p^e}∘AND blocks (each k AND-gates) factors through the joint count vector ⇒ ≤ (k+1)^t cells. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_depth3_modpe_cells_frontier. THE CELL COUNT MULTIPLIES PER LAYER: t blocks ⇒ (k+1)^t joint cells — polynomial for constant depth, the symmetric-structure count surviving where the polynomial method fails for e≥2. HONEST: arbitrary-depth nesting + unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModpeDepth3` |
| **Bridge (HARD MATH, arbitrary-depth nesting recursion) — gate cells ≤ product over sub-circuits `cells_factor` (PROVED)** | The general inductive step for ANY depth. **`cells_compose`** (PROVED): t functions each with ≤ C cells ⇒ ≤ C^t joint cells (abstract over value type; image ⊆ piFinset, card_piFinset + prod_le_prod). **`cells_factor`** (PROVED): F x = H(G_1 x,…,G_t x), each G_i ≤ C cells ⇒ (image F).card ≤ C^t (image F ⊆ H '' joint, card_image_le + cells_compose). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_cells_factor_frontier. THE ARBITRARY-DEPTH CELL-COUNT RECURSION: a gate's cells ≤ product over its sub-circuits' cells; applied at every node of a depth-d fan-in-f circuit ⇒ C^{f^d} bound — quasipoly for constant depth, FIELD-INDEPENDENT (surviving the prime-power RS barrier). Generalizes modpe_depth2_count / depth3_modpe_cells (jointCells). HONEST: concrete circuit-tree assembly + unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CellRecursion` |
| **Bridge (HARD MATH, circuit-tree assembly) — any count-tree cells ≤ product over the tree `ctree_cells_le` (PROVED)** | The end-to-end recursion over a concrete recursive datatype. **`cells_pair`** (PROVED): joint of two functions ≤ product of cell counts. **`CTree`/`ceval`/`cbound`**: binary count-tree (leaves = count-gates, nodes = binary aggregations), eval, product-of-leaf-cells bound. **`ctree_cells_le`** (PROVED): (image (ceval t)).card ≤ cbound t for every tree — structural induction, each node via cells_pair (card_image_le through the agg). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_ctree_cells_le_frontier. THE CONCRETE CIRCUIT-TREE ASSEMBLY: any binary count-tree of ARBITRARY DEPTH has cell count ≤ product of leaf cells; with MOD_{p^e}∘AND leaves (≤k+1 cells) ⇒ quasipoly for constant depth, field-independent. HONEST: unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CellTree` |
| **Bridge (HARD MATH, quantitative tree bound) — tree of prime-power blocks cell count ≤ (k+1)^{#leaves} `modpe_tree_cells_le` (PROVED)** | The quantitative end-to-end output. **`cbound_le`** (PROVED): LeafBound C t ⇒ cbound t ≤ C^{leafCount t} (induction, pow_add + mul_le_mul). **`ctree_cells_le_pow`** (PROVED): LeafBound C t ⇒ (image (ceval t)).card ≤ C^{leafCount t}. **`isModpeTree_leafBound`** (PROVED): a tree of gateCount-of-k-AND leaves is (k+1)-leaf-bounded (cells_card_le). **`modpe_tree_cells_le`** (PROVED): tree of MOD_{p^e}∘AND blocks ⇒ cell count ≤ (k+1)^{#leaves}. [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modpe_tree_cells_le_frontier. THE QUANTITATIVE END-TO-END: arbitrary-depth nesting of prime-power SYM∘AND blocks stays quasipoly ((k+1)^{#leaves}, constant depth ⇒ poly #leaves), field-independent. HONEST: unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CellTreeBound` |
| **Bridge (CONCRETE↔ABSTRACT) — faithful ACC0Circuit → CTree translation `ceval_toCTree` (PROVED)** | Connects the abstract cell-count machinery to the concrete circuit model. **`toCTree`**: ACC0Circuit → CTree (const/var/mod → {0,1} leaves; not/and/or → ℕ→ℕ→ℕ nodes 1−a, a·b, max a b). **`ceval_toCTree`** (PROVED): ceval (toCTree C) x = if eval C x then 1 else 0 (eval-preservation, structural induction; not/and/or via standalone Bool helper lemmas not_lemma/and_lemma/or_lemma to dodge the if-Decidable generalize issue). **`satisfiable_iff_toCTree`** (PROVED): (∃x, eval C x = true) ↔ ∃x, ceval (toCTree C) x = 1. [propext,Quot.sound], no sorry. Wired nw_ceval_toCTree_frontier. Every ACC0Circuit IS a count-tree ⇒ ctree_cells_le applies to real circuits. HONEST: naive Boolean leaves ⇒ loose 2^{#leaves} bound (Boolean output has ≤2 cells); the quasipoly benefit needs MOD∘AND-block leaves (modpe_tree_cells_le regime). Unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CircuitToCellTree` |
| **Bridge (CONCRETE↔ABSTRACT, symmetric leaf) — ACC0Circuit MOD gate has ≤ |S|+1 cells `modGate_satisfiable_iff` (PROVED)** | The honest refinement delivering on the flagged gap: the REAL MOD gate is a low-cell symmetric leaf. **`weightOn_le`** (PROVED): weightOn S x ≤ |S| (sum of |S| indicators). **`weightOn_cells_le`** (PROVED): (image (weightOn S)).card ≤ |S|+1 (image ⊆ range(|S|+1)). **`modGate_satisfiable_iff`** (PROVED): Satisfiable (eval (.mod q S t)) ↔ ∃ w ∈ image (weightOn S), (w:ZMod q)=t — SAT over ≤|S|+1 weight-cells (modQStatOn = (weightOn:ZMod q)). [propext,Classical.choice,Quot.sound], no sorry. Wired nw_modGate_satisfiable_iff_frontier. THE MOD GATE OVER INPUTS IS THE GENUINE MOD∘AND-BLOCK LEAF (AND-gates = single literals): linear (≤|S|+1) cells, fast-SAT, NOT 2^{|S|} — the symmetric structure the polynomial method can't exploit for prime powers. Feeding such leaves through the count-tree (modpe_tree_cells_le) = the quasipoly regime. HONEST: unconditional NEXP⊄ACC⁰ = P≠NP-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ModGateCells` |
| **Attacking PatternRichCrossFieldLowerBound via the non-native degree route — the Smolensky COUNTING ENGINE, PROVED (PROVED)** | The direct attack on the single open target along its RS route (entry-262 `NonNativeDegreeLowerBound`): factor the lower bound through *non-native polynomial degree over F*. Route has two halves — **(B)** richness ⇒ high non-native degree (the *counting* half) and **(A)** small resources ⇒ low non-native degree (the *polynomial-method* half). This file **PROVES the entire (B) engine** and reduces the target to the single named (A) socket. **`lowDegreeDim n D := ∑_{i≤D} C(n,i)`** (dimension of the non-native degree-≤D function space). **`lowDegreeDim_lt_two_pow`** (PROVED): `D<n ⇒ lowDegreeDim n D < 2^n` (via `Nat.sum_range_choose` + `Finset.sum_le_sum_of_subset` — the low-degree space is strictly smaller than the full one; the heart of why low-degree can't compute rich functions). **`exists_notMem_of_finrank_lt`** (PROVED): the rank kernel (family spanning dim > finrank W ⇒ some member ∉ W; via `Submodule.span_le` + `Submodule.finrank_mono`). **`algExpander_forces_high_degree`** (PROVED): the Smolensky **pigeonhole** — an AlgExpander family of `s` gates (rank `s` via `finrank_span_eq_card`) with `lowDegreeDim n D < s` must contain a gate of non-native degree `>D` (too many independent gates can't all be low-degree). **`crossFieldCount_eq_firePattern_card_mod`** (PROVED, rfl): the count IS `|firing pattern| mod q` = MOD_q on the pattern indicator (the function hard by in-arc `Layer4.mod_q_indicators_false`). SOCKETS: **`PolynomialMethodApproximation`** (the (A) half = the probabilistic polynomial method, `AC⁰[p] ⇒` low-degree approximation — the genuine open RS core; instances = in-arc Layer3/Layer4); **`LowDegreeDimensionIdentity`** (secondary: `finrank W = lowDegreeDim n D`, the multilinear/Möbius basis count). **`nonNativeDegreeLowerBound_via_counting`** (PROVED): builds a concrete `NonNativeDegreeLowerBound` instance — (B) half proved by the engine, (A) half = the socket. **`patternRichCrossFieldLowerBound_via_nonNativeDegree`** (PROVED): feeds it through entry-262 `patternRich_lb_of_nonNativeDegree` ⇒ `PatternRichCrossFieldLowerBound`, modulo the polynomial-method socket. The Smolensky counting+rank mechanism (the part that makes the route work) is now MACHINE-PROVED; the wall is reduced to ONE analytic socket. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0NonNativeDegree` |
| **Reed–Muller / low-degree polynomial gates — a THIRD proved AlgExpander + PatternRich family, closest to RS (PROVED)** | The third hard-family candidate and the one closest to Razborov–Smolensky, via the clean reduction *low-degree polynomial gate = affine gate in monomial feature space*. **`rmGate feat coeff b i x := decide (∑_m coeffᵢ,m · feat m x = bᵢ)`** = `Pᵢ(x) = bᵢ`, where `feat : M → X → ZMod p` is the monomial feature map (`M` = degree-≤d monomials) and `Pᵢ = ∑_m coeffᵢ,m · m` is the degree-≤d polynomial; the Reed–Muller code `RM(d,n)` is the span of such `Pᵢ`. **`rmGate_degree_one_eq_varGate`** (PROVED): the degree-1 features `feat m x = x_m` give `rmGate = varGate` — the entry-262 varying-affine family is the degree-1 special case (exact). **Feature general position** = `Function.Surjective (rmEval feat coeff)` (every value-vector `(Pᵢ(x))ᵢ` attained — non-degeneracy / large RM distance). **`rmGate_patternImage_eq_univ`** (PROVED): every subset realized (solve `rmEval feat coeff x = (i↦ if i∈T then bᵢ else bᵢ+1)`). **`rmGate_patternRich`** (PROVED): `PatternRich (2^s)`. **`rmGate_coFiringRich`** (PROVED): `CoFiringRich s` (solving `rmEval feat coeff x = b` fires all gates on one input). **`rmGate_algExpander`** (PROVED): private witnesses ⇒ AlgExpander over any field `F`. **`rmGate_ACC0_chain`** (PROVED): given the entry-262 LB socket `PatternRichCrossFieldLowerBound` + entry-261 bridge `crossFieldHard_to_ACC0Component`, Reed–Muller yields ACC0CompositeComponent with AlgExpander+PatternRich DISCHARGED. RS attack invariant = non-native degree over F_q = Reed–Muller distance (reuses `NonNativeDegreeLowerBound`). THREE proved families now satisfy the antecedent (dictator/MOD_q + varying-affine + Reed–Muller). Only the two named sockets remain (Smolensky-strength, 238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ReedMullerGates` |
| **Varying-direction affine hyperplanes — a SECOND proved AlgExpander + PatternRich family, beyond dictator/MOD_q (PROVED)** | The program's second genuinely-different hard-family candidate, with the parallel family pinned as the degenerate easy subcase. **`varGate a b i x := decide (⟨a i, x⟩ = b i)`** — gate `i` is the affine hyperplane `{x : ∑ⱼ (a i j)·xⱼ = b i}` with its OWN direction `a i` (vs the parallel family's one fixed all-ones direction). **General position** = `Function.Surjective (dotEval a)` (every value-vector attained; for `n=s` = invertible direction matrix). **`varyingAffine_patternImage_eq_univ`** (PROVED): under general position EVERY subset `T` is realized (solve `dotEval a x = (i↦ if i∈T then bᵢ else bᵢ+1)`). **`varyingAffine_patternRich`** (PROVED): `PatternRich (2^s)`. **`varyingAffine_coFiringRich`** (PROVED): `CoFiringRich s` — solving `dotEval a x = b` makes ALL `s` gates co-fire on one input (the content parallel lacks). **`varyingAffine_algExpander`** (PROVED): private witnesses (singleton-pattern solutions) ⇒ AlgExpander over any field `F`. **`parallel_pattern_image_ne_univ`** (PROVED, s≥2): parallel = degenerate easy subcase — the all-fire pattern is NEVER realized (no input co-fires ≥2 gates, 259), so pattern-image ≠ univ; general position FAILS (rank-deficient direction). Real target socket: **`PatternRichCrossFieldLowerBound`** (the single named missing theorem: AlgExpander → PatternRich(2^s) → CrossFieldCountHard). RS attack invariant: **`NonNativeDegreeLowerBound`** (factor through non-native polynomial degree over F_q, closest to Razborov–Smolensky), with **`patternRich_lb_of_nonNativeDegree`** (PROVED: the attack route implies the target). **`varyingAffine_ACC0_chain`** (PROVED): given the LB socket + entry-261 bridge `crossFieldHard_to_ACC0Component`, varying-affine yields ACC0CompositeComponent with AlgExpander+PatternRich DISCHARGED. Two proved families now satisfy the antecedent (dictator/MOD_q + varying-affine). Only the two named sockets remain (Smolensky-strength, 238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0VaryingAffinePatterns` |
| **Fire-pattern richness: strengthening co-firing to MANY distinct patterns + implication bridge (PROVED)** | Strengthens entry-260's existential `CoFiringRich` (one large co-fire — too weak) to pattern richness. **`PatternRich gates k := k ≤ #(Finset.image (firePattern gates) univ)`** (≥k distinct fire-patterns). **`dictator_firePatternImage_eq_univ`** (PROVED): the dictator/MOD_q family realizes EVERY subset T as a fire-pattern (input `fun i => decide (i∈T)`), so the pattern-image = all of `Finset (Fin s)`. **`dictator_patternRich`** (PROVED): `PatternRich (fun i x => x i) (2^s)` — exponentially many distinct patterns (`Fintype.card_finset`). **`parallel_no_large_patterns`** (PROVED): parallel affine has NO fire-pattern of size ≥2 (all ≤1, entry 259) — fails pattern richness in the strong sense, matching its easy count. SOCKETS: **`PatternRichCountObstruction`** (strengthened: AlgExpander → PatternRich → CrossFieldCountHard); **`crossFieldHard_to_ACC0Component`** (the implication bridge: CrossFieldCountHard → ACC0CompositeComponent, the ACC⁰[composite] lower-bound component upstream of Williams; proven MOD_q instance via Layer4.mod_q_indicators_false, 244). Separation: dictator/MOD_q (2^s patterns, hard) vs parallel (no large patterns, easy). General = Smolensky-strength (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0FirePatternRichness` |
| **Co-firing richness: the corrected hardness invariant, separating parallel (easy) from dictator/MOD_q (hard) (PROVED)** | Defines the missing ingredient from entry 259. **`firePattern gates x := univ.filter (fun i => gates i x)`** (gates firing on x); **`CoFiringRich gates k := ∃ x, k ≤ #(firePattern gates x)`** (some input fires ≥k gates simultaneously). **`parallel_not_coFiringRich`** (PROVED): parallel affine fails `CoFiringRich 2` (all patterns ≤1, entry 259) — exactly why its count collapses to {0,1}. **`dictator_coFiringRich`** (PROVED): the dictator family (gate i fires iff bit i set) is `CoFiringRich s` (all-true input fires all s gates). **`dictator_algExpander`** (PROVED): the dictator family is also AlgExpander (private witnesses) — so AlgExpander ∧ CoFiringRich is JOINTLY SATISFIABLE (corrected socket non-vacuous). CORRECTED SOCKET `CoFiringCountObstruction`: `AlgExpander → CoFiringRich → CrossFieldCountHard` (adds the entry-259-required CoFiringRich). PROVEN INSTANCE: the dictator family's mod-q fire-count = Hamming weight mod q = MOD_q, hard in AC⁰[p] by the in-arc Razborov-Smolensky (Layer4.mod_q_indicators_false, 244). So co-firing exactly tracks easy/hard: parallel fails it & easy; dictator/MOD_q satisfies it & hard. General socket = Smolensky-strength (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CoFiring` |
| **Affine hyperplane lower bound: the honest NEGATIVE — fire-count is EASY, refining the hard hypothesis (PROVED)** | Attacking the proposed `affineHyperplane_fireCount_modq_hard` reveals it is FALSE for this family. **`affineHyperplane_fireCount_le_one`** (PROVED): parallel hyperplanes `{∑ⱼ xⱼ = targets i}` (injective targets) are DISJOINT — any input has one coordinate-sum value, lies on ≤1 hyperplane — so fire-count ≤ 1 (`Finset.card_le_one` + injectivity). **`affineHyperplane_crossFieldCount_le_one`** (PROVED): the mod-q fire-count ≤ 1 (`Nat.mod_le`) — trivially easy (2-cell observer). KEY THEOREM-LEVEL FINDING: the entry-258 family is AlgExpander (full indicator rank) YET count-EASY ⇒ **AlgExpander (indicator rank) is NOT sufficient for count-hardness**. The count LB needs CO-FIRING RICHNESS (gates firing simultaneously in many patterns — overlapping/varying-direction), which parallel/disjoint families lack. Consequence: the naive bridge `AlgExpander ⇒ NEXP⊄ACC⁰` is UNSOUND (this counterexample); the genuine upstream condition is co-firing-rich count-hardness = Razborov-Smolensky, proved in-arc for the (co-firing) MOD_q family (Layer4.mod_q_indicators_false, 244). Refined hard family = varying-direction hyperplanes `{⟨aᵢ,x⟩=bᵢ}`; LB = Smolensky socket. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0AffineHyperplaneLowerBound` |
| **Affine hyperplane indicators: a genuine non-trivial algebraic-expander family (PROVED)** | The focused program's next concrete family (beyond the trivial dictator family, 257). **`private_witness_indep`** (PROVED, reusable tool): if each gate i fires on a private witness wit i (`gates i (wit i)=true`, `gates j (wit i)=false` for j≠i), the indicators are linearly independent ⇒ AlgExpander (evaluate the dependence at each witness; `Fintype.linearIndependent_iff`+`Finset.sum_eq_single`). **`affineHyperplane_algExpander`** (PROVED): for injective `targets : Fin s → ZMod p`, the parallel affine hyperplanes `fun i x => decide (∑ⱼ xⱼ = targets i)` over `F_p^{n+1}` are AlgExpander — private witness for gate i = `Pi.single 0 (targets i)` (coord-sum = targets i, on hyperplane i only by injectivity). A genuine AFFINE (non-dictator) full-rank algebraic-expander family. The count LOWER BOUND on it (that the mod-q fire-count of these affine gates needs superpoly) = the entry-256/257 AlgExpanderCountObstruction socket, Smolensky-strength, NOT proved. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0AffineHyperplanes` |
| **Indicator rank: the algebraic-hardness invariant + concrete family + RS link (PROVED)** | The invariant stronger than treewidth (the user's step 5/6). **`gateRank gates := Module.finrank F (span F (range (gateInd gates)))`** — the indicator-span dimension (treewidth=DP geometry; rank=count-hardness geometry). **`algExpander_gateRank_eq`** (PROVED): `AlgExpander gates → gateRank gates = s` (full rank; via `finrank_span_eq_card`). **`pointFamily_algExpander`** (PROVED): the point/dictator gates `fun i x => decide (x=i)` have indicators = standard basis `Pi.basisFun`/`Pi.single i 1`, linearly independent ⇒ AlgExpander — a concrete full-rank algebraic-expander family (the honest "one nontrivial family"). RS LINK (step 3): the count LB under AlgExpander (entry-256 `AlgExpanderCountObstruction`) IS Razborov-Smolensky (linearly-independent F_p indicators ⇒ mod-q fire-count superpoly) = the ACC⁰[composite] component, formally upstream of Williams NEXP⊄ACC⁰; the arc PROVES it for the MOD_q family (`Layer4.mod_q_indicators_false`, entry 244 — a proven restricted instance). General AlgExpander bound = open Smolensky core (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0IndicatorRank` |
| **Algebraic expansion: the refined condition — gate-function independence, not cut (PROVED)** | Refines `ExpanderIncidence` into the CORRECT (algebraic) hypothesis after the entry-255 negative (cut-expansion ≠ count-hardness). **`gateInd gates i := fun x => if gates i x then 1 else 0`** (F-valued indicator); **`AlgExpander gates := LinearIndependent F (gateInd gates)`** — gate indicators linearly independent over F (about gate FUNCTIONS, not variable supports). **`not_algExpander_of_duplicate`** (PROVED): duplicate gates (i≠j, gates i=gates j) ⇒ ¬AlgExpander (equal indicators ⇒ dependent, via LinearIndependent.injective) — RULES OUT the entry-255 full-support degeneracy. **`algExpander_indicators_injective`** (PROVED): AlgExpander ⇒ gate-indicator family injective (genuine non-redundancy). **`AlgExpanderCountObstruction`** (SOCKET, refined): AlgExpander ⇒ count needs large resources — the entry-254 socket restated under the CORRECT algebraic hypothesis, still Smolensky-strength, NOT proved. HONEST: the two notions genuinely differ — cut-expansion (treewidth) governs DP tractability EXACTLY (251-253); algebraic expansion (gate-function rank) is the hypothesis needed for count-hardness. The count LB under it = open ACC⁰[composite] core (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0AlgebraicExpansion` |
| **Special expander families: instantiating `ExpanderIncidence`, + the honest refinement (PROVED)** | Attacks special expander families on the entry-254 bridge. **`expander_of_pairwise_inter`** (PROVED): if every 2 distinct gates share ≥ d variables, then ExpanderIncidence supp d (any separator with parts A,B contains supp ga∩supp gb for ga∈A,gb∈B distinct, ≥ d, via Finset.card_le_card + Finset.disjoint_left). **`fullSupport_expander`** (PROVED): the all-variables family `fun _ => univ` ⇒ ExpanderIncidence (fun _ => univ) (Fintype.card V) — no separator smaller than all n vars. HONEST REFINEMENT (a genuine NEGATIVE, surfaced not hidden): the full-support family is a separator-expander yet its mod-q fire-count is plausibly EASY (all gates share support ⇒ the fire-count is a simple symmetric function, NO genuine cross-field mixing). So ExpanderIncidence ALONE does NOT imply count-hardness: separator-expansion is necessary for DP-failure but NOT sufficient for the count lower bound. This sharpens the bridge — the candidate invariant (separator-width/treewidth) governs DP-tractability EXACTLY, but the genuine count LB needs the ALGEBRAIC/polynomial-method hardness (the 254 socket), not just no-small-separator. Confirms ExpanderCountObstruction is genuinely Smolensky-strength. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ExpanderFamilies` |
| **Expander count obstruction: the conjectural bridge — DP-restricted bound PROVED, general bound SOCKETED** | The negative side of the dichotomy, formalized honestly per the user's warning (the general bound is Smolensky-strength). **`IsSeparator supp S`** (gates split into 2 nonempty parts sharing vars only in S); **`ExpanderIncidence supp k`** (no small separator: every separator ≥ k); **`ObserverBoundarySize := Finset.card`**. **`dp_observer_needs_large_boundary`** (PROVED, trivial by def): a separator-DP observer's boundary IS a separator ⇒ on an expander, boundary ≥ k (= `hexp boundary hsep`). This bounds ONLY separator-DP observers. **`ExpanderCountObstruction`** (SOCKET, a `def`/named conjecture, NOT a theorem): for ANY observer computing the mod-q fire-count (`ComputesCrossFieldCount` abstract — covers ALGEBRAIC observers that need not decompose along a separator), expander incidence ⇒ boundary ≥ k. The gap from the DP bound = ruling out the clever algebraic shortcuts = Smolensky-strength, OPEN. **`boundedSeparator_factorizes`** (PROVED, re-export of `ACC0TreewidthCount.separator_factor`): the positive tractable side (251-253). HONEST FINDING: the DP-restricted lower bound is FREE (trivial); the hard part is precisely ruling out non-DP/algebraic observers = the open ACC⁰[composite] wall (238). Next = special expander families. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ExpanderCountObstruction` |
| **Treewidth count: separator conditioning — the bounded-treewidth DP core (PROVED)** | Pushes the tractable boundary from block-diagonal (252) to bounded treewidth via its DP core. **`separator_factor`** (PROVED): a separator S splitting the gates into parts A,B sharing only S (PA : S→A→Bool, PB : S→B→Bool) ⇒ `#{x : S×A×B | PA x.1 x.2.1 ∧ PB x.1 x.2.2} = ∑ s, #{a : PA s a}·#{b : PB s b}` — conditioning on the separator makes the two sides independent (the variable-elimination/junction-tree recurrence; via `Fintype.sum_prod_type`+`Finset.sum_mul_sum`+`Finset.card_filter`). **`separator_factor_indep`** (PROVED): empty separator ⇒ product `#{x : A×B | PA x.1 ∧ PB x.2} = #{a:PA a}·#{b:PB b}` — the block/disjoint product (251/252) recovered as the treewidth-0 case. FINDING: iterating separator_factor over a tree decomposition of width ≤w (separators ≤2^w) = the FPT-in-treewidth DP; disjoint (251)=tw 0, block-diagonal (252)=bounded-tw disconnected bags. OPEN: unbounded treewidth — connected/expander incidence with no small separators — where the count does not factor and the mod-q fire-count is the cross-field mixing (Smolensky, 244/249/250) = open ACC⁰[composite] core (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0TreewidthCount` |
| **Block-overlap count: tractable fragment beyond disjoint — bounded local overlap factorizes (PROVED)** | Extends the entry-251 tractable boundary past overlap rank 1. **`block_pattern_count`** (PROVED): for independent variable blocks `Z : Fin m → Type` with an ARBITRARY per-block predicate `P b` (within-block sub-computation — gates may overlap freely INSIDE a block), `#{x : ∀b, P b (x b)} = ∏ b, #{z : P b z}` — the count FACTORS across blocks (`Fintype.card_piFinset`). **`block_count_le`** (PROVED): bounded block size (`∀b, card (Z b) ≤ W`) ⇒ count ≤ W^m — product of m bounded factors (each brute-forceable; cross-field count = convolution of per-block distributions, poly for bounded W). FINDING: tractability boundary = the gate-variable INCIDENCE GRAPH's block decomposition (bounded LOCAL overlap; within-block arbitrary), NOT global disjointness. Disjoint (251) = blocks of size 1; this = bounded-size blocks. Refines the overlap-rank invariant: bounded-size connected components ⇒ fast (proved); large connected components (high global overlap, expander-like) = the open Smolensky core (244/249/250). General connected case = open ACC⁰[composite] (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0BlockOverlapCount` |
| **CrossFieldCount core: the exposed ACC wall, attacked directly; disjoint gates tractable (PROVED)** | The exposed core object formalized directly (no new architecture). **`crossFieldCount q gates x := (#{i : gate i fires on x}).card % q`** — the object ALL six crossing routes (single-field 244, product-field 245, staged 246/250, one-layer 248, depth-2 249, exactification 234) bottom out at (p≠q). **`crossFieldCount_lt`** (PROVED): `< q` (≤ q cells — cheap; difficulty is the per-cell input count). **`fireCount_eq_sum`** (PROVED): fire-count = ∑ᵢ [gate i fires]. **`disjoint_pattern_count`** (PROVED, the TRACTABLE FRAGMENT): for DISJOINT gate supports (product space, gate i depends only on coord i), the pattern-count factors `#{x : ∀i, gᵢ(xᵢ)=bᵢ} = ∏ᵢ #{y : gᵢ y=bᵢ}` (via `Fintype.piFinset`/`card_piFinset`) — independence ⇒ the cross-field count distribution is a convolution ⇒ FAST, NO cross-field mixing. FINDING: the obstruction is an OVERLAP phenomenon — low overlap/incidence rank ⇒ tractable (proved for disjoint, overlap rank 1); high overlap rank ⇒ Smolensky (overlapping F_p-gates whose mod-q fire-count is the cross-field mixing). Candidate invariant = overlap/incidence rank. General dichotomy (quasipoly observer = crossing, or exp LB = barrier) for arbitrary-overlap families = open ACC⁰[composite] core (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CrossFieldCountCore` |
| **Staged fast-SAT without flattening: outer stages for free; obstruction RELOCATES into the per-cell count (PROVED)** | The candidate crossing route (246+249), done honestly — the outer staging is proved free; the obstruction relocates, it does not vanish. **`stagedSAT_outer_decomp`** (PROVED, **NO axioms**): `(∃x, P x ∈ S) ↔ ∃ v ∈ S, ∃x, P x = v` — SAT through the outer gate is a bounded disjunction over accepting residue-classes; the outer MOD₃ is a ≤3-way case split, NOT flattened into a single polynomial (= "without flattening"). **`staged_work_le`** (PROVED): `(∀v∈S, W v ≤ M) → ∑_{v∈S} W v ≤ |S|·M` — the outer staging is only a `|S| ≤ 3` constant factor. CONCLUSION: each per-cell branch counts inputs by `#(inner MOD₂ gates firing) mod 3` = cross-field MIXING in COUNTING form (244/249). So staging without flattening does **NOT** bypass the wall — it RELOCATES the cross-field mixing into the per-cell inner count. The outer-staging facts are proved; the per-cell count (count F₂-fires mod 3 efficiently) is the relocated obstruction = the open ACC⁰[composite] core (238), neither proved tractable (crossing) nor hard (no-go). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0StagedFastSAT` |
| **Depth-2 cross-modulus (outer MOD₃ fed by inner MOD₂): staged composition works; flattening is the wall (PROVED)** | The smallest depth-2 hard case. **`inner_mod2_deg_le_one`** (PROVED): inner MOD₂ degree ≤ 1 over F₂ (entry-242 modpIndicator, p=2). **`outer_mod3_deg_le_two`** (PROVED): outer MOD₃ degree ≤ 2 over F₃ IN THE INNER BITS (entry-242, p=3, p-1=2). **`outer_mod3_reads_inner_bits`** (PROVED): the outer F₃ polynomial evaluated on the inner bits computes `[#true inner bits ≡ 0 mod 3]` (entry-242 modpIndicator_eval) — it consumes the already-computed inner bits, no MOD₂-over-F₃. ANSWER to the core question: YES — the product observer composes through the outer MOD₃ WITHOUT representing MOD₂ over F₃, because each layer is low-degree over its OWN field and the outer reads the inner BITS (staged, not flattened). The MOD₂-over-F₃ collapse is forced ONLY by FLATTENING the depth-2 structure to a single F₃ polynomial in the original inputs (substituting bᵢ=MOD₂(inputs), arithmetizing high-degree MOD₂ over F₃) = BT depth-reduction = Smolensky-blocked (244) = the original ApproxToExactCount wall. Whether a staged/multi-sorted fast-SAT (246) consumes the depth-2 staged observer WITHOUT flattening is the open core (238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Depth2CrossModulus` |
| **MOD₆ one-layer product-field mixing: one layer composes WITHOUT collapse (PROVED); depth is the wall** | The smallest honest cross-field-mixing laboratory (MOD_6 = MOD_2 ∧ MOD_3). **`mixedOut comb k := comb (decide (k%2=0)) (decide (k%3=0))`** (one-layer mixed MOD_2/MOD_3 output). **`mixedOut_determined`** (PROVED, **NO axioms**): determined by the product observer state (count mod 2, count mod 3) — equal residues ⇒ equal output; NO collapse to F_2/F_3 (gate outputs are bits, combiner is Boolean, touches no field arithmetic). **`mixedOut_determined_by_mod6`** (PROVED): equivalently read by count mod 6 (CRT ZMod 6 ≅ ZMod 2 × ZMod 3, via Nat.mod_mod_of_dvd). **`mixedOut_cells_le_six`** (PROVED): ≤ 6 cells (bounded, no blow-up). FINDING: ONE LAYER of mixed MOD_2/MOD_3 gates → Boolean combiner composes WITHOUT collapse — a genuine partial crossing. The wall is localized to DEPTH ≥ 2: a MOD_q gate FED BY a non-native MOD_p output forces arithmetizing MOD_p over F_q = Smolensky-blocked (Layer3.parity_function_lower_bound / Layer4.mod_q_indicators_false, 244). Depth-2 nested representation NOT built (the open core, 238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ProductFieldMixing` |
| **Multi-sorted fast-SAT: cell budget composes (PROVED); per-field gate representation is the blocked core** | The multi-sorted fast-SAT step (the deepest open question), done honestly — the counting budget is proved fine; building per-field representations is the blocked ACC⁰[composite] core. **`jointCells mcount := ∏ᵢ (mcountᵢ + 1)`** (the k-prime joint count-tuple space). **`jointCells_le_pow`** (PROVED): `∏ᵢ(mᵢ+1) ≤ (M+1)^k` when each mᵢ≤M (`Finset.prod_le_prod`+`prod_const`) — the cell budget composes multiplicatively, quasipoly for constant k (counting side fine, analogue of 239). **`multiSorted_savings`** (PROVED): `j≤n → W≤2^(n-j) → 2^j·W ≤ 2^n` — joint cells meeting the budget give a 2^j speedup (same savings form as single-field fast-SAT, `…ACC0WilliamsFastSat`). FINDING: the multi-sorted fast-SAT COUNTING does not blow up — it WOULD run IF the per-field SYM∘AND representations existed. BLOCKED CORE (socket): per-prime counting needs each MOD_m gate as a low-degree SYM∘AND over a single F_{pᵢ}, IMPOSSIBLE (Smolensky 244: MOD_{pⱼ} j≠i not low-degree over F_{pᵢ}), and downstream gates MIX the per-field computations — the cross-field mixing INSIDE the circuit (not the readout) is the actual ACC⁰[composite] barrier (238). If buildable → crossing; if not → no-go. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0MultiSortedFastSAT` |
| **Multi-sorted product-field observer: construction PROVED; feeding fast-SAT is the open socket** | The only remaining escape (step 4) after the field-route no-go (244), done honestly — the product-observer construction is proved; feeding fast-SAT is the open socket. **`modm_iff_modp_and_modq`** (PROVED): for coprime p,q, `(p·q)∣k ↔ p∣k ∧ q∣k` — MOD_{pq}=MOD_p∧MOD_q, the product-field combiner. **`prod_field_iso`** (PROVED): `ZMod(p·q)≃+*ZMod p×ZMod q` (CRT) — the product structure ∏F_{pᵢ}. **`prodObs_bijective`** (PROVED): the CRT residue tuple is a faithful bijection (reads count mod p·q with no loss, unlike a single small field, 239). Each per-prime component MOD_{pᵢ} is low-degree over its OWN field (entry-242 modpIndicator; cited). FINDING (honest middle): the multi-sorted observer EXISTS and faithfully decomposes MOD_m into per-prime low-degree ANDs — works AS AN OBSERVER. OPEN SOCKET (step 5): can it feed Williams/BT fast-SAT (quasipoly SYM∘AND) WITHOUT collapsing to one field (Smolensky-blocked, 244)? Needs a product-sorted fast-SAT machinery (single SYM∘AND over one field doesn't natively consume a multi-sorted observer) — NOT constructed. If yes → ACC⁰[m] crossing; if no → multi-sorted no-go. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0MultiSortedObserver` |
| **Smolensky connector: the field route is a NO-GO (PROVED, cites the arc's RS layer)** | KEY FINDING: the Smolensky non-native MOD lower bound (the entry-243 "open wall") is ALREADY PROVED in this arc — `Layer4.mod_q_indicators_false` (MOD_q ∉ AC⁰[p] for q∤p, sorry-free clean axioms) + `Layer3.parity_function_lower_bound`. This file CONNECTS it (does NOT rebuild). **`crossField_modq_fieldRoute_nogo`** (PROVED, = mod_q_indicators_false): no AC⁰[p] family computes the MOD_q residue indicators within the Smolensky budget (q∤p). **`parity_nonnative_size_lb`** (PROVED, = parity_function_lower_bound): parity in AC⁰[p] (p odd) forces p^t < 4·#subcircuits. CONCLUSION (step 3): a single-field low-degree F_p representation of non-native MOD_q is IMPOSSIBLE ⇒ the field route to CarryRefinementCrossing (238/243) is a definitive NO-GO. Only open escape = multi-sorted/product-field observer in ∏_p F_p (step 4, NOT reducible to one F_p per entry-243 no_common_char, NOT touched by Smolensky). NOTE: Smolensky bounds AC⁰[p] (single prime), NOT ACC⁰[m] with MOD_m available (Williams' open territory). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SmolenskyLowerBound` |
| **Cross-field combination: the structural root of the Smolensky wall (PROVED)** | The cross-field low-degree combination (the actual ACC⁰[composite]/Smolensky barrier), done honestly — the structural ROOT proved, the deep degree lower bound named (NOT proved/crossed/faked). **`no_common_char`** (PROVED): no field is simultaneously CharP p and CharP q for distinct p≠q (via `CharP.eq`) — the per-prime indicators can't natively share a field. **`self_prime_zero`** (`(p:ZMod p)=0`) + **`other_prime_unit`** (PROVED: `(q:ZMod p)≠0` for distinct primes, via `ZMod.natCast_eq_zero_iff`+`Nat.prime_dvd_prime_iff_eq`) — in F_p the characteristic privileges its own prime (Fermat indicator native to mod-p, not mod-q). **`reduction_blind_to_other_prime`** (PROVED): ∃ counts (0, p) equal mod p but differing mod q (q∤p) ⇒ any decode factoring through count-mod-p can't compute MOD_q. These PROVE the root of why naive cross-field combination fails. RESIDUAL (deep, open): MOD_q low-degree over F_p (q≠p) = Smolensky's lower bound (high degree required), the open ACC⁰[composite] wall = entry-238 CarryRefinementCrossing; native MOD_p is degree ≤p-1 (entry 242), non-native is the wall. NOT proved, NOT crossed. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CrossFieldCombination` |
| **Layered-carry degree: per-prime layers are low-degree; composite combination is the wall (PROVED)** | The layered-carry degree analysis (the crossing/no-go question), done honestly — the per-prime layer is proved cheap; the composite combination is the named open wall. **`countSum x := ∑ boolToZMod(x_i)`** (count mod p); **`linearCountPoly := ∑ X_i`**, **`linearCount_totalDegree_le_one`** (PROVED: residue mod p is LINEAR), **`linearCount_eval`** (=countSum). **`modpIndicatorPoly := 1-(∑X_i)^(p-1)`**; **`modpIndicator_totalDegree_le`** (PROVED: total degree ≤ p-1, INDEPENDENT of n — contrast entry-241's Θ(N) single-field full count); **`modpIndicator_eval`** (PROVED: computes `[count≡0 mod p]` via Fermat `s^(p-1)=[s≠0]`). FINDING (honest middle — NOT a crossing, NOT a faked no-go): per-prime layers are LOW-degree (≤p-1 indep of n) — good news, layering doesn't blow up degree within a prime. OPEN WALL (not settled): for composite m, MOD_m = ⋀ MOD_{pᵢ} lives over DIFFERENT fields F_{pᵢ}; combining the per-prime low-degree indicators into ONE low-degree composite indicator (or the higher p-adic carry digits for prime-powers) is the open ACC⁰[m] barrier (Smolensky-strength) = entry-238 CarryRefinementCrossing. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0LayeredCarryDegree` |
| **Composite count degree: the formal obstruction — field decode costs Θ(N) (PROVED)** | The degree-complexity of exact composite count realisation; proves WHY entry-240's field decode is too expensive. **`indicator_natDegree_ge`** (PROVED): over a field, a nonzero poly vanishing at N distinct points has natDegree ≥ N (#roots ≤ natDegree, `Polynomial.card_roots'`). **`faithful_decode_degree_ge`** (PROVED, the obstruction): a field decode nonzero at one point + 0 at N other count-points needs natDegree ≥ N=S.card. **`pointIndicator_natDegree_ge`** (PROVED): entry-240 prime indicator degree ≥ p-1 (vanishes on the p-1 other elements). **`prime_indicator_natDegree_eq`** (PROVED, tightness): degree EXACTLY p-1 = (field size)-1 (≥ here + ≤ entry 240). DEGREE-VS-FAITHFUL-RANGE: faithful needs field ≥ N+1 (239), so field-route decode degree = Θ(N) — linear, NOT low-degree (=polylog/n^{o(1)}). Formal obstruction FOR THE SINGLE-FIELD ROUTE. OPEN: can p-adic carry/digit LAYERS realise exact composite count at degree ≪ N while keeping quasipoly size? (= entry-238 CarryRefinementCrossing; the bound is per-single-field-polynomial, layered route may evade). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CompositeCountDegree` |
| **Carry observer realisation: the decode is realisable over a field; the count is the barrier (PROVED)** | Attacks the algebraic realisation (entry-238 `CarryRefinementCrossing`); honest finding parallels 239 — the count-DECODE is realisable over a field, relocating the barrier onto the exact COUNT computation (entry 234). **`pointIndicator p a := 1-(X-C a)^(p-1)`** (Fermat singleton indicator). **`pointIndicator_eval`** (PROVED): `eval y = if y=a then 1 else 0` (via `(y-a)^(p-1)=[y≠a]`, ZMod.pow_card_sub_one_eq_one). **`pointIndicator_natDegree_le`** (≤p-1). **`decPoly`/`decPoly_eval`/`decPoly_natDegree_le`** (PROVED): the decode poly `∑_{a:dec a} pointIndicator a` evals to `[dec y]`, degree ≤p-1. **`RealizesCarryObserver m D := ∀ dec, ∃ P, natDegree≤D ∧ ∀y, eval=[dec y]`**. **`prime_realizes`** (PROVED): `RealizesCarryObserver p (p-1)` — every decode over a prime field is degree-≤(p-1) poly. **`faithful_of_size`** (PROVED, re-export 239): m≥N+1 ⇒ Faithful. **`squarefree_crt_reduction`** (CRT iso, reuse 235); **`primePow_no_power_indicator`** (e≥2: no power indicator over ZMod p^e, reuse 235 — Fermat route fails, needs digit layers). HONEST CAVEATS: (i) realises the FREE symmetric decode, not the count (degree cost is in the AND layer = entry 234); (ii) Fermat degree p-1=(field size)-1 ⇒ faithful single-field realisation is degree ~N, NOT low-degree. Barrier = low-degree exact count for composite m (entry-234/238). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CarryRealization` |
| **Carry observer size: quasipoly vs exponential — the answer (PROVED bounds)** | Defines the carry observer's state + size and settles the quasipoly-vs-exponential question for the STATE COUNT. **`CarryProfileState m k := (k : ZMod m)`**; **`Faithful m N := Injective (Fin (N+1) → ZMod m via count mod m)`**; **`CarryObserverSize m N := #{k mod m : k ∈ range(N+1)}`**. **`faithful_iff_le`** (PROVED, centerpiece): `Faithful m N ↔ N+1 ≤ m` (pigeonhole `Fintype.card_le_of_injective`+`ZMod.card` one way; `ZMod.val_cast_of_lt` the other). **`carryObserverSize_le_succ`** (≤N+1) + **`carryObserverSize_le_modulus`** (≤m, so a FIXED modulus caps states ⇒ carry layers needed for growing N). **`exists_faithful`** (PROVED, THE finding): `Faithful (N+1) N` — a faithful observer with EXACTLY N+1 states exists ⇒ state count QUASIPOLY when N is (e.g. N=#monomials), NO exponential blow-up. **`field_faithful_iff`** (trivial profile ⟹ field observer faithful when N<p); **`primePow_faithful_iff`** (`Faithful (p^e) N ↔ N+1≤p^e` ⇒ needs e≳log_p N carry layers). KEY: the barrier is NOT the state count (quasipoly) but the LOW-DEGREE REALISATION (= entry-238 `CarryRefinementCrossing`). Relocates the obstruction honestly. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CarryObserverSize` |
| **Carry-refinement crossing condition: NAMED SOCKET (the open ACC⁰[m] wall, documented)** | Names+documents the crossing condition for the composite barrier, with scaffolding PROVED so the socket sits faithfully at the wall. **`fieldObs p k := (k : ZMod p)`** (field count-observer). **`fieldObs_carry_blind`** (PROVED): `fieldObs p (k+p) = fieldObs p k` — collapses counts differing by a carry-sized shift (count-level form of entry-235). **`fieldObs_not_injective`** (PROVED, p≥2): can't recover the exact count (`fieldObs p 0 = fieldObs p p`). **`lowDegRep_of_observer`** (PROVED): a count observer `obs` + decode `dec` with `f x = dec(obs(saCount mono x))` and low-degree injective monomials ⇒ `LowDegRep f D` (SYM gate `dec∘obs`) — so crossing reduces EXACTLY to a CARRY-FAITHFUL such observer for composite m. **`CarryRefinementCrossing approxHyp f D := approxHyp → LowDegRep f D`** (NAMED SOCKET, NOT proved): a carry-faithful low-degree count observer exists for the composite-modulus approximant — the open ACC⁰[m] barrier-crossing condition (entry-234 ApproxToExactCount in carry-observer terms, obstructed by the entry-237 carry profile). **`lowDegRep_of_crossing`** (PROVED conditional): the socket discharges the barrier. NOT a crossing; the field observer is carry-blind, the crossing needs a carry-faithful one for composite m (open). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CarryCrossing` |
| **CRT carry profile: composite-modulus observer from per-prime Kummer carries (PROVED)** | Assembles the entry-236 per-prime carryCounts into a composite-modulus carry profile. **`CarryProfileTrivial m n k := ∀ p ∈ m.primeFactors, carryCount p n k = 0`** (no carry at any prime layer of m — joint per-prime Kummer data over m's factorisation). **`carryProfileTrivial_iff_no_prime_dvd`** (PROVED): ⟺ ∀ p ∈ m.primeFactors, ¬p∣C(n+k,k) (per-prime via entry-236 carryCount_eq_zero_iff_not_dvd + Fact.Prime from mem_primeFactors). **`carryProfileTrivial_iff_coprime`** (PROVED, m≠0): ⟺ `Nat.Coprime m (C(n+k,k))` — composite-modulus observer sees full count ⟺ m coprime to binomial (via `Nat.disjoint_primeFactors`). **`carryProfile_mul`** (PROVED, CRT decomposition): `CarryProfileTrivial (a*b) ↔ CarryProfileTrivial a ∧ CarryProfileTrivial b` (a,b≠0; via `Nat.primeFactors_mul`) — the profile JOINS over the factorisation, matching the entry-235 CRT residue split `ZMod(ab)≃ZMod a×ZMod b`. Conservative; characterises which prime layers the mod-m observer is blind to; does NOT cross the composite barrier. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CRTCarryProfile` |
| **Kummer carry count via factorization: quantitative carry seam (PROVED)** | Makes the entry-235 carry seam quantitative via Kummer's theorem. **`carryCount p n k := padicValNat p (Nat.choose (n+k) k)`** = #base-p carries adding n,k (Kummer). **`carryCount_digits_identity`** (PROVED, re-export of Mathlib `sub_one_mul_padicValNat_choose_eq_sub_sum_digits'`): `(p-1)·carryCount = S_p(k)+S_p(n)-S_p(n+k)` (Kummer digit-sum form — each carry costs p-1). **`carryCount_eq_zero_iff_digits`** (PROVED): no carry ⟺ digit-sum diff = 0 (via p-1≠0). **`carryCount_eq_zero_iff_not_dvd`** (PROVED, the field-observer link): `carryCount=0 ↔ ¬p∣C(n+k,k)` — no carry ⟺ binomial is a p-adic unit ⟺ survives mod p (field/weighted-F_p observer sees it); via `padicValNat.eq_zero_of_not_dvd` + `one_le_padicValNat_of_dvd`. **`carryCount_pos_iff_dvd`** (PROVED): `0<carryCount ↔ p∣C(n+k,k)` — a carry ⟺ mod-p-invisible divisibility; QUANTITATIVE refinement of entry-235 `field_observer_blind_to_carry`. Conservative; characterises+quantifies the seam, does NOT cross the composite barrier. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0KummerCarry` |
| **Carry invariant: the p-adic seam, conservative fragments (PROVED)** | The N-Frame invariant search at the entry-234 `ApproxToExactCount` wall — formalises the **field power-indicator** and proves SAFE fragments (no global "obstructs exactification" claim). **`PowerIndicator m := ∃ e≥1, ∀ y:ZMod m, y^e = if y=0 then 0 else 1`** (the primitive turning a weighted "`AND` satisfied" bit into an exact `0/1` count). **`powerIndicator_of_prime`** (PROVED, MOD_p: `e=p-1` via Fermat — no carry obstruction). **`not_powerIndicator_of_zeroDivisor`** (PROVED: a zero-divisor kills the indicator). **`not_powerIndicator_primePow`** (PROVED, MOD_{p^e} e≥2: `ZMod (p^e)`-level indicator FAILS, `p` a zero-divisor `p·p^{e-1}=p^e=0` — carry/field layer genuinely needed, RS uses `GF(p^e)≠ZMod p^e`). **`field_observer_blind_to_carry`** (PROVED: `k=0,k'=p` agree mod-p but differ mod-p² — the field/weighted-F_p observer is blind to the p-adic carry the exact count must see). **`crt_residue_observer_suffices`** (PROVED: coprime a,b ⇒ `ZMod(ab)≃+*ZMod a×ZMod b` — squarefree residue profile, no carry). Conservative fragments CHARACTERIZING the seam (boundary forces observer refinement); NOT a crossing of the composite barrier. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0CarryInvariant` |
| **RS approx→exact: the composite-`ACC⁰` barrier honestly separated (structural fragment PROVED)** | ⚠️ HIGHEST false-closure risk target — handled honestly. Separates entry-204 `SpanApproxToLowDegRep` into the easy structural direction (PROVED) + the named barrier-atom (NOT closed). **`ExactUnitCount p D f := ∃ injective deg-≤D mono, ∀x, boolToZMod p (f x) = (saCount mono x : ZMod p)`** (f's F_p value is EXACTLY the count mod p — unit-weight SYM∘AND). **`lowDegRep_of_exactUnitCount`** (PROVED): `ExactUnitCount p D f → LowDegRep f D`, SYM gate `h k := decide ((k:ZMod p)=1)` (f x=true↔count≡1, false↔count≡0; 0≠1 in field ZMod p) — the genuine EASY direction of BT conversion. **`ApproxToExactCount approxHyp p D f := approxHyp → ExactUnitCount p D f`** (the barrier socket: 3/4 weighted approximant ⇒ exact unit-count rep; subsumes amplification + weighted→unit injective conversion). **`lowDegRep_via_amplification`** (PROVED glue): approxHyp + ApproxToExactCount ⇒ LowDegRep — exhibits `ApproxToExactCount` as the SOLE residual between the proved RS approximant and LowDegRep. ⚠️ `ApproxToExactCount` is the composite-`ACC⁰[m]` barrier (amplification provably fails for composite m, separation-strength); NAMED, NOT closed. Structural span=weighted-count is entry 207. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ApproxToExact` |
| **IKW small-prover: easy-witness failure = a hard function (PROVED extraction)** | Opens the entry-224 `GuessableProver` socket (the IKW easy-witness atom) as far as the discipline allows. **`IncompressibleAcceptingProver Ver proverOf x := ∃ p, Ver x p ∧ ∀ C, proverOf C ≠ p`** (an accepting strategy no circuit describes = a hard object). **`not_easy_gives_incompressible`** (PROVED, **NO axioms**): accepted `x` (`∃p, Ver x p`) with no circuit prover (`¬∃C, Ver x (proverOf C)`) ⇒ `IncompressibleAcceptingProver` — the failure of easy-witness IS hardness. **`EasyWitnessLemma circuitHyp := circuitHyp → ∀x, ¬IncompressibleAcceptingProver`** (the IKW socket: NEXP⊆ACC⁰ rules out incompressible witnesses; via Karp-Lipton 202/221 + witness-search). **`guessableProver_of_easyWitness`** (PROVED): discharges entry-224 `GuessableProver` from `EasyWitnessLemma` (by_contra + extraction). **`not_guessableProver_gives_incompressible`** (PROVED): `GuessableProver` failure ⇒ a hard function — so the IKW small-prover BOTTOMS OUT at the SAME separation-strength hard-function atom as step 5 (why IKW/magnification is P≠NP-strength). Residual socket: `EasyWitnessLemma` (the IKW easy-witness theorem). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0IKWSmallProver` |
| **Low-degree arithmetization: the multilinear extension (PROVED), discharges `LowDegArithmetization`** | Constructs the MLE explicitly and proves BOTH halves, discharging the entry-226 `LowDegArithmetization` socket with a concrete low-degree witness. `chi (xi) ai := if ai then xi else 1-xi`, `mleB f x := ∑_a boolToR(f a)·∏_i chi(x_i,a_i)`, `mleP f := ∑_a C(boolToR(f a))·∏_i chiP i (a i)` (MvPolynomial). **`chi_boolToR`/`prod_chi_eq`** (PROVED): the coordinate factor is the equality indicator, `∏_i chi(boolToR(b_i),a_i)=[a=b]` (via `Finset.prod_eq_zero` when `a≠b`). **`mleB_agrees`** (PROVED): `mleB f (boolToR∘b)=boolToR(f b)` (only the `a=b` term survives, `Finset.sum_eq_single`). **`chiP_degree`/`prod_chiP_degree`/`mleP_degree`** (PROVED, `[Nontrivial R]`): totalDegree `≤1`, `≤m`, `≤m` (via `totalDegree_sub`/`totalDegree_X`/`totalDegree_finset_prod`/`totalDegree_finset_sum`/`totalDegree_mul`) — genuine multilinearity, feeds entry-227 SZ. **`mleP_eval`** (PROVED): `eval x (mleP f)=mleB f x` — the MvPolynomial IS the function MLE. **`mleP_arithmetizes`** (PROVED): discharges `LowDegArithmetization f (eval·(mleP f))`. NO socket remains for the MLE itself; residual = machine-level certificate encoding (`f` = NEXP verifier, `m`=poly(time)). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Multilinear` |
| **ClockedSimulation cost: the universal lazy-diagonal sim fits the NEXP budget (PROVED accounting)** | Proves the cost-accounting half of the entry-223 `ClockedSimulation` socket, connecting 219/220/223. **`sim_budget_fits`** (PROVED): exponent additivity `g+s≤F ⇒ 2^g·2^s ≤ 2^F` (overhead × simulated-steps composes by adding exponents). **`exp_bound`/`nat_overhead_bound`** (PROVED, ALL n, a≥1): `2^n·2^(n^a+a) ≤ 2^(n^(a+1)+(a+1))` — poly overhead `2^n` × simulated `NTIME(2^(n^a+a))` still fits `NTIME(2^(n^(a+1)+(a+1)))` (cases n=0,1 + n≥2 via `n^(a+1)≥2n^a≥n^a+n`). **`detRun_accepts`** (PROVED): a deterministic table run (220 `detRun_reachIn`) reaching state 1 witnesses `acceptsWithin` (223). **`lazy_clockedSim`** (PROVED): clocked sim from `hbudget`(proved accounting)+`hcomplete`+`hsound`; FORWARD `L x→acceptsWithin budget` proved via `acceptsWithin_mono`. **`lazy_universal_in_nexp`** (PROVED): composes entry-223 `clocked_in_NEXP` ⇒ lazy diagonal ∈ NEXP. Residual sockets: `hcomplete`/`hsound` (universal decode→step→re-encode correctness, the `…UniversalHStep` contract). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0ClockedSimulation` |
| **Arithmetization as counting: `scSum(arith) = #accepting` (PROVED)** | Discharges the counting half of the entry-225 `NexpArithmetization` socket. **`scSum_indicator`** (PROVED): if `g` agrees with boolean predicate `f` on the hypercube (`∀b, g(boolToR∘b)=boolToR(f b)`), then `scSum g = (#{b\|f b} : R)` — the BFL claim value `H` IS the accepting count (via `Finset.sum_boole`). Residual socket: `LowDegArithmetization` (the low-degree multilinear extension realising the agreement). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0Arithmetization` |
| **Sum-check soundness: Schwartz–Zippel one variable (PROVED)** | Discharges the root-bound core of the entry-225 `SumCheckSoundness` socket. Over a finite field: **`agreement_le_degree`** (distinct `p≠q` agree at `≤(p-q).natDegree` pts — agreement set injects into roots of `p-q≠0`, `Polynomial.card_roots'`), **`agreement_le_max`** (distinct deg-`≤d` polys agree `≤d` pts), **`few_agreements`** (if `d<\|F\|` they disagree somewhere — a separating challenge exists). The per-round soundness amplifier. Residual socket: `SumCheckRoundSoundness` (round polys ARE deg `≤d` + multi-prover multilinearity). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SchwartzZippel` |
| **MIP ⊆ NEXP: the prover table is the nondet guess (PROVED mechanism)** | Discharges the entry-225 `MipSubsetNexp` simulation socket. **`mipLang_eq_nexpGuess`** (PROVED, rfl, NO axioms): `MIPLang Ver = NexpGuessLang Ver` — the ∃-over-provers IS a NEXP nondeterministic guess of the (exp) prover table. **`mipSubsetNexp_of_realized`** discharges `MipSubsetNexp` from `MIPRealizedInNexp`. Residual socket: `MIPRealizedInNexp` (Ver exp-time checkable given the table). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0MipSimulation` |
| **PRG ⟹ MA ⊆ NP: seed enumeration (PROVED mechanism)** | Discharges the entry-222 `PRGCollapsesMAtoNP` socket. **`maLang_eq_detLang`** (PROVED): a fooling deterministic seed-enumeration check preserves the language (`PRGFools Arthur ArthurDet → MALang Arthur = MALang ArthurDet`); so MA collapses to NP (∃ Merlin, det check over seeds). **`prgCollapses_of_realizes`** discharges `PRGCollapsesMAtoNP` from `PRGRealizesNP`. Residual socket: `PRGFools` (the PRG actually fools Arthur). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0SeedEnumeration` |
| **Derand ⟹ PRG: the NW reconstruction contradiction (PROVED glue)** | Discharges the entry-222 `DerandGivesPRG` socket. **`prgFools_of_hard`** (PROVED, NO axioms): NW reconstruction (`Distinguisher→SmallCircuit f`) + hardness (`¬SmallCircuit f`) ⇒ `¬Distinguisher` (PRG fools), via `fun d => hard (recon d)`. **`derandGivesPRG_via_nw`** discharges `DerandGivesPRG` from `DerandGivesHardFn`. Residual sockets: `NWReconstruction` (entries 191-196 analysis) + the IW hard function. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0NWFromHardness` |
| **Guessable-prover collapse: `MIP ⊆ ACC⁰ ⟹ MIP ⊆ MA`, the mechanism (PROVED)** | Opens the entry-221 `MIPSubsetMA_ofCircuits` socket (the second BFL-IKW ingredient) into its genuine mechanism and proves it. **`MIPLang Ver := fun x => ∃ prover, Ver x prover`** (MIP language — the ∃-over-provers is the NEXP-strength guess), **`MALang Arthur := fun x => ∃ w, Arthur x w`** (MA language). **`GuessableProver Ver proverOf := ∀ x, (∃ p, Ver x p) → ∃ C, Ver x (proverOf C)`** (the IKW small-prover socket: an accepting prover has a small-circuit description). **`mipLang_eq_maLang`** (PROVED, `[propext, Quot.sound]`): `GuessableProver ⇒ MIPLang Ver = MALang (λ x C, Ver x (proverOf C))` — Merlin guesses the prover circuit `C`, Arthur evaluates the verifier on `proverOf C`; **backward (a circuit-prover IS a prover) is FREE** (`⟨proverOf C, hC⟩`), forward = the socket. **`mipLang_mem_ma`** (PROVED): with the collapsed MA-language in MA, `MIPLang Ver ∈ MA`. **`mipSubsetMA_of_realized`** (PROVED): discharges the class-level `MIPSubsetMA_ofCircuits MIP ACC0 MA` from `MIPRealizedGuessable` (under circuits every MIP language realizes as an `MIPLang` with a guessable prover + MA-class collapsed language). Honest: proves the collapse MECHANISM (incl. the free backward inclusion) + class threading completely; residual socket = `GuessableProver` (the IKW small-prover / easy-witness theorem — small circuits ⇒ optimal prover is a small circuit). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0GuessableProver` |
| **NTIME accounting: the lazy diagonal machine lives inside `NEXP` (placement PROVED)** | Turns the hierarchy side from "diagonal *mechanism* proved" (entry 219 `lazy_diag_false`) + "clocked *run* proved" (entry 220 `detRun_reachIn`) into "time-class *placement* proved". `NTIME f={L\|∃M,∀x, L x↔acceptsWithin M x (f\|x\|)}`, `NEXP=⋃_c NTIME(2^{n^c+c})` (`…ACC0NTM`). **`accepts_within_budget`** (cost-fits-budget: `acceptsWithin M x s→s≤t→acceptsWithin M x t`, = `acceptsWithin_mono`); **`ClockedSimulation L M c := ∀x, L x↔acceptsWithin M x (2^{\|x\|^c+c})`** (socket: M decides L within the NEXP budget); **`clocked_in_NTIME`/`clocked_in_NEXP`** (PROVED: ClockedSimulation ⇒ `L∈NTIME(2^{n^c+c})⊆NEXP`); **`lazy_diagonal_in_nexp`** (PROVED: the lazy diagonal `∈NEXP` — the entry-200 `DiagonalInNexp` membership discharged from the clocked-machine model, reduced to the `ClockedSimulation` socket). Honest: proves the time-class **placement** (clocked-within-budget ⇒ class membership) completely; residual socket = `ClockedSimulation` for the lazy diagonal (the universal sim of M_i + one boundary complement clocked to exp, from 219/220) — the lazy-feasible form (one complement, not closure under complement). NTM model, no measure theory. NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0NTIMEAccounting` |

**Progress on the depth-reduction wall:** the residue-observer algebra (`observed_top_pi`) makes depth composition
reusable, the toy discharges the socket for the bounded-bottom (`SYM`-of-`AND_w`) fragment, and `acc0_residueObserved`
now *derives* the residue/mixed observer from the raw `ACC0Circuit` inductive (no assumed bottom) — every circuit is
observed by a product statistic of state count `stateBound C` (the occurrence product). The honest gap is unchanged:
that bound is the *occurrence* product, so the gain holds only for few-leaf circuits where MOD gates supply the
compression; getting a *small* bound for an *arbitrary* `ACC⁰` circuit is the structural shrinkage of full
Yao–Beigel–Tarui — still open.

**Architecture closed (conditionally):** the residue-speedup chain (steps 1–4, proved) cashes out to `NEXP ⊄ ACC⁰`
through exactly **two named open sockets**, both separation-strength (proved equivalent to the separation by the
self-audit theorems):
1. `MixedACCDepthReductionSocket` — Yao–Beigel–Tarui depth-2 normal form (switching can't give it: the MOD no-go).
2. `UniformWilliamsRealizationSocket` — uniform `2^{n−n^ε}` realization + Williams' algorithmic method.
The only *proved* link in the cash-out is `mixedACC_speedup_of_depthReduction` (the residue chain). `NEXP ⊄ ACC⁰`
remains an abstract `Prop` — **not defined, not proved**. This is the honest architecture, not a result.

**Why the *lower bound* is open:** `MOD_6`'s two CRT components are each low-degree but over *incompatible* fields —
`MOD_3` over `F_2` and `MOD_2` over `F_3` are high-degree (Smolensky). A single-`F_p` polynomial observer captures
only one component; a *modulus-stratified / hybrid* observer is needed, and whether it yields a lower bound is the
ACC⁰ frontier.

**The route that does cross it — STARTED:** not more RS, but **Williams' algorithmic method** (faster ACC⁰-SAT +
the nondeterministic time hierarchy → `NEXP ⊄ ACC⁰`). The **SAT-speedup kernel is now proved**
(`…ACC0ModResidueSpeedup`): a depth-2 `MOD`-circuit factors through its *residue vector* (∈ `∏ ZMod q_j`), so SAT is
a search over `≤ ∏ q_j` cells (`sat_iff_residue_image`, `residue_cell_count_le`) — `< 2^n` whenever `∏ q_j < 2^n`
(e.g. `6^k < 2^n` for `MOD_6`). This is the structural residue-compression Williams exploits. **Remaining gap:**
realising the speedup as a uniform (nondeterministic) algorithm + the time-hierarchy cash-out (the named gap in
`…NFrameACC0Master`) — that is what `NEXP ⊄ ACC⁰` needs, and this cell-count bound does not supply it.

## The Yao–Beigel–Tarui pipeline (target, with the proved links marked)

```
ACC⁰[p] circuit
  ──[RS approximation: APPROXIMATE agreement 1-ε, Layer3]──►   toAgree, low-degree poly over ZMod p
  ──[toAgree_totalDegree_le: PROVED, degree ≤ ((p-1)t)^depth DISCHARGED]──►   degree-bounded poly
  ──[acc0p_toAgree_in_monoAND_span = eval_mem_lowDegSpan + squarefreeEvalMonomial_eq_monoAND: PROVED]──►   ZMod p-combination of monomial-AND gates
  ──[weightedGateCount_cast_eq (coefficient duplication): PROVED]──►   count-mod-p SYM over duplicated monomial-ANDs
  ──[lowDegreePoly_searchable / weightedSym_searchable / monomial_count_le: PROVED]──►   SYM ∘ AND_{≤D}  (≤ ∑_{i≤D} C(n,i) gates, duplicated ≤ (p-1)·∑C(n,i))
  ──[sym_observed / sym_count_card_le: PROVED]──►   count observer (boundary m+1, not 2^m)
  ──[sym_searchable: PROVED]──►   SAT-searchable in < 2^n (once m+1 < 2^n)
```

Every arrow **after the RS approximation is proved** in the cell/observer model (`…ACC0RSToSymAnd`, `…ACC0PolyToSymAnd`,
`…ACC0SymmetricObserver`): the RS low-degree span IS the monomial-`AND` (`SYM∘AND` bottom) span, and the count observer
makes the `SYM` top searchable.  The **open wall** is the first arrow being *exact* (not just `1-ε`-approximate) and
holding for arbitrary `ACC⁰` across depth — the Razborov–Smolensky approximation + constant-depth composition
(`MixedACCDepthReductionSocket`).  So the YBT *cash-out* (poly → `SYM∘AND` → searchable) is complete and now wired to
the RS generators; the YBT *reduction* (`ACC⁰ → exact low-degree poly`) is the wall.

## The exact-quasipoly fragment ladder (where exactness reaches, and the one clause that stops it)

Four probes map the boundary of *exact-and-quasipolynomial* `SYM∘AND` — one result from four sides:

```
back half     MOD_M on the integer count          exact (CRT)         M cells          …ACC0IntegerPolynomialCRT
top kind      OR ∘ AND_w  (DNF)                    exact               ∑_{i≤w}C(n,i)    …ACC0ExactQuasipolyDepth2
one MOD layer MOD_M ∘ AND_w                        exact               bottom + M       …ACC0ExactQuasipolyModTop
one depth     top ∘ (MOD_M ∘ AND_w)^k              exact               PRODUCT 2^k/M^k  …ACC0ExactQuasipolyDepth3
```

**Invariant: exactness is never the obstruction.**  Every symmetric top (`OR`/`MOD`/threshold) is *exactly* a
count/CRT-decoded gate, and every *bounded-fan-in* `AND` *is* a bounded-degree monomial, exactly — no RS approximation
anywhere in this fragment.  The only thing that must stay bounded is the **bottom `AND` degree**, which controls both
the monomial count (`∑_{i≤w}C(n,i)`, quasipoly for `w=polylog`) and the **product** composition cost (`2^k`/`M^k`,
quasipoly for `k=polylog`).  So the boundary is sharp and one-dimensional: exact-quasipoly `SYM∘AND` holds **iff the
bottom degree stays polylog**; the symmetric structure above is irrelevant to exactness.  This localizes **Wall 1
entirely to the bottom** (`ACC⁰ → exact low-degree integer polynomial across depth`).  The real obstruction, made
precise: *exact composition works, but exact observer state multiplies across wide layers (`2^k`/`M^k`); YBT's content
is avoiding that product blow-up for arbitrary `ACC⁰`.*

## The bottom clause, pinned from both sides — and the only route left

The bottom clause is now bounded above and below, so the remaining wall is laser-sharp:

* **Exact low-degree per gate is IMPOSSIBLE** (`…ACC0ExactDegreeNoGo`): over `F₂` an exact unbounded-fan-in `OR`/`AND`
  has degree = fan-in `n` (top ANF coefficient `= 1`), so `2^n` monomials — exponential.  The naive route is ruled out.
* **Approximate low-degree EXISTS** (`toAgree_totalDegree_le`, Layer3): the RS approximant has polylog degree but agrees
  only on a `1-ε` fraction.
* **The only route left** is the Beigel–Tarui *approximate→exact* conversion (`…ACC0ApproxToExactDecode`): recover the
  exact value from several low-degree approximants by a **symmetric/count decoder**.  Socket
  `ApproxToExactSymmetricDecode` names it (non-trivial *only* via its low-degree clause — `r` copies of `f` satisfy it
  otherwise; with it, holding for arbitrary `ACC⁰` **is** Wall 1).  The *decoding* half is PROVED (`majority_decode`:
  majority-correct ⇒ exact threshold-count decode); the open part is producing **low-degree** approximants that are
  majority-correct at *every* point — the irreducible BT/Yao probabilistic construction, **Wall 1**.

## Honest ceiling

Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP`. Tier 1 = genuine AC⁰ switching; Tier 2 = genuine `PARITY ∉ AC⁰[p]`
(classical, constant depth, prime `p`); Tier 3 = open, needs a different (algorithmic) tool.  The YBT pipeline above is
proved from the `SYM∘AND` normal form onward; the reduction *into* that form is the open structural wall.
