# ACC⁰ theorem map — at-a-glance status

Consolidated status of the ACC⁰ lower-bound programme (pall-lean, PathB). Companion to `ACC_ROADMAP.md`
(narrative) and `WHAT_IS_PROVED.md` (full ledger). Updated 2026-06-14.

## Tier 1 — AC⁰ (switching method): **DONE & FENCED**

| result | theorem | file |
|---|---|---|
| clause-count-free switching tail | `hastad_switching_prob_tail` (`∑_Bad pweight ≤ (4pw/(1-p))^s`) | `…Depth3SwitchingProbTail` |
| depth collapse whp | `depth_collapse_mass_ge` (width-`w` DNF → depth ≤ 2 on `1-(4pw/(1-p))^s`) | `…Depth3SwitchingDepthReduction` |
| **switching cannot cross MOD** | `mod_gate_parity_nonconstant` (MOD gate non-constant on any free-support cube) | `…ACCSwitchingModBridge` |

⇒ Switching kills AC⁰ depth; provably does **not** reduce MOD/ACC⁰ depth.

## Tier 2 — AC⁰[p] (polynomial / Razborov–Smolensky, `p` prime): **DONE & FENCED**

| result | theorem | file |
|---|---|---|
| PARITY ∉ AC⁰[p] | `parity_function_lower_bound` (`2^{Ω(n^{1/2d})}`) | `…Layer3Smolensky` |
| N-frame target = parity | `fParity_univ_eq_parity`, `nframe_parity_target_size_lower_bound` | `…Layer3NFrameParityRS` |
| socket 1: target escapes `V_D` | `holonomy_parity_not_lowDegEval` | `…Layer3LowDegHolonomy` |
| socket 2: AC⁰[p] ⊆ low-rank | `acc0_approx_by_lowRankPredictor`, `eval_mem_lowDegSpan` | `…Layer3ACC0LowRank` |
| socket 3: modulus boundary | `fermat_indicator`, `modp_eval_mem_lowDegSpan` (`MOD_p` ∈ `V_{p-1}` over `F_p`) | `…Layer3ModulusBoundary` |
| MOD_q ∉ AC⁰[p], `q≠p` prime | `mod_q_indicators_false` (Smolensky) | `…Layer4ModqChar` |

⇒ Polynomial method linearises exactly `MOD_p` gates over `F_p`; the lower bound is real.

## Tier 3 — general ACC⁰ (mixed / composite MOD): **OPEN — the genuine frontier**

The wall is precisely **mixed modulus**: no single prime field linearises gates of different moduli.

| structural fact | theorem | file |
|---|---|---|
| `F_p` statistic = count mod p | `fp_statistic_eq_count` | `…Layer3MixedModulus` |
| **MOD_6 = MOD_2 ∧ MOD_3** (CRT) | `mod6_eq_mod2_and_mod3` | `…Layer3MixedModulus` |
| MOD_2 low-degree over `F_2` | `mod2_detector_lowdeg_F2` (∈ `V_1`) | `…Layer3MixedModulus` |
| MOD_3 low-degree over `F_3` | `mod3_detector_lowdeg_F3` (∈ `V_2`) | `…Layer3MixedModulus` |
| hybrid observer | `MixedModulusStratifiedObserverSocket` (named OPEN socket) | `…Layer3MixedModulus` |
| **mixed-modulus SAT speedup** | `mod_circuit_sat_speedup` / `mod6_circuit_residue_speedup`: `∏q_j<2^n` ⇒ `<2^n` residue cells | `…ACC0ModResidueSpeedup` |
| **operational residue machine** | `residueSearch` (timed algorithm); `residueSearch_decides`, `residueSearch_beats_bruteforce` (`steps≤∏q_j`, `<2^n`) | `…ACC0ResidueMachine` |
| **branched residue cost** | `branched_residue_beats_bruteforce` / `…_mod6_…`: `2^killed·∏surviving q_j < 2^n` past the `∏q_j<2^n` base regime | `…ACC0BranchedResidue` |
| **restriction ⇒ few survivors** | `residue_cells_le_surviving_moduli`: `\|cells(C↾L)\| ≤ ∏_{surviving} q_j` (discharges step 2's per-branch bound) | `…ACC0ResidueRestriction` |
| **depth iteration (socket)** | `acc0_depth_reduction_speedup`: `MixedACCDepthReductionSocket` (Yao–Beigel–Tarui depth-2 normal form, OPEN) ⇒ residue search decides `Satisfiable(eval C)` in `<2^n` | `…ACC0ResidueDepthReduction` |
| **Williams cash-out (interface)** | `residue_cashout_bundled`: depth socket + `UniformWilliamsRealizationSocket` ⇒ `NEXP⊄ACC⁰`; self-audit `*_iff_separation` (sockets ⟺ separation) | `…ACC0WilliamsCashout` |
| **residue-observer algebra** | `ObservedBy`, `observed_top_pi` (composition law), `.and`/`.or`/`.comp`, cell-count bounds | `…ACC0ResidueObserver` |
| **toy Beigel–Tarui (attacking the wall)** | `toy_bounded_bottom_searchable`: SYM-of-`AND_w` (fan-in `≤w` bottom) searchable in `<2^n` — depth-reduction socket **discharged for the bounded-bottom fragment** | `…ACC0BeigelTaruiToy` |
| **depth-3 MIXED fragment** | `depth_mixed_searchable`: arbitrary top over a bottom **mixing** `MOD_q` + bounded-`AND_w` gates is searchable in `<2^n` when `∏(per-gate states)<2^n` — socket **discharged for the mixed-bottom fragment** | `…ACC0Depth3Mixed` |
| **support normal form (syntax-level)** | `acc0_observed_by_projection`: ANY `ACC0Circuit` observed by projection to its `support` (`eval_depends_on_support`, induction); `acc0_junta_searchable` (junta ⇒ `<2^n`); `acc0_top_over_subcircuits_searchable` (top over ACC⁰ subcircuits jointly reading `<n` vars ⇒ `<2^n`, union support) — `ACC⁰⊆mixed-bottom` chipped at circuit syntax | `…ACC0SupportNormalForm` |
| **depth-3 MOD normalization (residue gain)** | `and_of_mods_searchable`: syntactic `MOD_{q₁}∧MOD_{q₂}` ⇒ `Depth2ModCircuit`, residue-searchable `<2^n` when `q₁·q₂<2^n` **regardless of support** (junta gives nothing here); `mod_bottom_circuit_searchable` (arbitrary top over MOD family) | `…ACC0Depth3ModNormalize` |
| **AC⁰-over-MOD normalization (general)** | `modComb_normalizes`: ANY AND/OR/NOT combination of a MOD-gate family `= Depth2ModCircuit.eval ⟨gates, combTop C⟩` (factors through MOD outputs, induction); `modComb_searchable` (residue-searchable `<2^n` when `∏q_i<2^n`) | `…ACC0ModCombNormalize` |
| **observer extracted from raw syntax** | `acc0_residueObserved`: by induction on `ACC0Circuit`, ANY circuit (positive MOD moduli) is `ObservedBy` a product statistic of state count `≤ stateBound C` (occurrence product: `q` per MOD, `2` per var, mult. through `∧`/`∨`) — `MOD` leaf→`modGate_observedBy`, `var`→coord projection, gates→`.comp`/`.and`/`.or`; `acc0_modcircuit_searchable` (`stateBound C<2^n` ⇒ `<2^n` cells). Bottom *derived*, not assumed | `…ACC0ExtractObserver` |
| **deduplicated state bound (collapses occurrence overcount)** | `acc0_dedupObserved`: by induction, `eval C` observed by `(mod-residue, ONE projection to `varSupp C`)` — `MOD`-internal vars absorbed into residue (`modOcc C`, no `2` cost), `var`-leaves counted by *deduplicated* union support (the `∧`/`∨` arms share a single projection onto `varSupp a ∪ varSupp b`, recovered by restriction); `acc0_dedup_searchable` (`modOcc C·2^\|varSupp C\| < 2^n` ⇒ `<2^n` cells). Strictly tighter than `stateBound` (vars once, not per occurrence) | `…ACC0DedupShrink` |
| **syntactic restriction + boundary shrinkage** | `subst1 C i v` (substitute `v` for var `i`; `MOD` gate `mod q S t ↦ mod q (S.erase i)(t-[i∈S]·v)`); `eval_subst1` (`= eval C (update x i v)`, induction); `varSupp_subst1_subset`/`modOcc_subst1_le`/`ModsPos_subst1` (boundary monotone under restriction); **`varSupp_subst1_card_lt`** (restricting a *read* var strictly shrinks the dedup boundary); **`sat_branch_subst1`** (SAT branches over the two restrictions of any var). The restriction algebra for shrinkage — NOT a switching lemma (one branch halves each boundary, no asymptotic gain alone) | `…ACC0RestrictShrink` |
| **random-restriction collapse of the dedup boundary** | `restrList` (multi-var restriction = iterated `subst1`); `eval_restrList`/`varSupp_restrList`/`modOcc_restrList_le`/`ModsPos_restrList` (lifts from single-var lemmas); `fixedListOf Lᶜ β`/`varSupp_restr_compl_subset` (fixing `L`'s complement ⇒ `varSupp ⊆ varSupp C ∩ L`); **`exp_liveBoundary_eq`** (`Exp_p \|varSupp C ∩ L\| = p·\|varSupp C\|` — proportional collapse, via `exp_sum`+`exp_indicator_eq_survProb` on the existing `p`-biased measure); `exists_small_liveBoundary` (Markov+prob. method); **`dedup_restriction_collapses`** (∃ live `L`: ∀ fixed-assignment `β`, restricted circuit's dedup boundary `< a ≈ p·\|varSupp C\|`). Honest: proportional (first-moment) collapse, NOT switching — no SAT speedup alone (branching over `2^{#fixed}` offsets) | `…ACC0RandomRestrictShrink` |
| **function-level collapse: decision-tree observer** | **`dt_observed`**: `BoolDecisionTree.eval T` is `ObservedBy` a statistic of cell-count `≤ 2^{T.depth}` (induction; query node uses the *sum*-typed leaf observer `S_low ⊕ S_high` = additive leaf count, so the bound is `2^depth` not `2^{#queried vars}`) — the *function's* leaf boundary, INDEPENDENT of syntactic support; `dt_searchable`/`function_collapse_of_dt` (a function computed by a shallow DT, `2^depth<2^n`, is SAT-searchable `<2^n`). Cash-out converting decision-tree depth (what switching reduces) into the observer boundary. AC⁰ layer only (MOD no-go) | `…ACC0DecisionTreeObserver` |
| **`DTree → BoolDecisionTree` bridge (switching's canonical tree, observed)** | `toBoolDT` (constructor-for-constructor map) + `toBoolDT_eval`/`toBoolDT_depth` (semantics + depth preserved, one-line inductions); `dtree_observed`/`dtree_searchable` (the observer cash-out transferred to `Depth3.DTree`); **`canonicalDTree_observed`/`canonicalDTree_searchable`**: the switching arc's `canonicalDTree` (width-`w` DNF, fuel `F`, depth `≤ F·w` by `canonicalDTree_depth_le`) has a `≤ 2^{F·w}`-cell observer boundary, SAT-searchable `<2^n` when `2^{F·w}<2^n`. The representation step flagged separate is now DONE — function-level collapse composes end-to-end with switching's canonical tree. (Caveats: `eval(canonicalDTree)=restricted DNF` is cited soundness; MOD layer excluded) | `…ACC0DTreeBridge` |
| **AC⁰ + MOD composition across the no-go** | `dt_oracle_observed` (a depth-`d` DT over Boolean *oracle*-subfunctions `h : Fin m → (Fin n→Bool)→Bool` has a `≤ 2^d` boundary — sum-typed leaf, branch on `h i x`); **`acc0_over_mod_observed`/`acc0_over_mod_searchable`**: `x ↦ T(g₁(x),…,g_m(x))` (`T` depth-`d` DT top, `gⱼ` MOD gates) is `ObservedBy` a `≤ 2^d` boundary / SAT-searchable `<2^n` when `2^d<2^n` — MOD gates QUERIED as oracles along each path, never collapsed (respects the no-go), boundary independent of moduli & vars read. Honest: this is the *deterministic* composition law; supplying a shallow `T` over the gate outputs is the OPEN part — gate outputs `gⱼ(x)` are determined by `x` (not independent), so switching's restriction can't collapse the top over them = the no-go's deeper form. Composes ONE AC⁰-over-MOD level; iterating through MOD layers stays blocked | `…ACC0ModComposition` |
| **oracle-control syntax → DT observer** | `OracleControl` (Boolean control circuit over `m` oracle leaves) + `controlEval`; `treeOf`/`treeOf_eval`/`treeOf_depth_le` + `fullTree` (complete query tree: ANY `f` on `m` bits → depth-`≤m` DT computing it); **`control_to_decision_tree`** (every control reduces to a depth-`≤m` DT); **`oracle_control_dt_searchable`** (control computed by depth-`d` DT ⇒ AC⁰-over-MOD composite SAT-searchable `<2^n` when `2^d<2^n`); **`oracle_control_over_mod_searchable`** (unconditional fragment: control over `m` MOD oracles searchable below brute force when `2^m<2^n`, i.e. `<≈n` MOD gates, ANY control structure). Deterministic control→DT (depth `m` in general) | `…ACC0OracleControl` |
| **step 3 (DNF fragment): random restriction makes the control shallow** | **`dnf_control_switching_searchable`**: for a DNF control `D` over `k` oracle positions (width `≤w`, `≤M` clauses, switching union-bound `(2p/(1-p))(2wM)<1` & `r^s/(1-r)<1`) and MOD gates with `s≤n`, **∃ position-restriction `ρ`** s.t. `(canonicalDT D F ρ)` is shallow (depth `<s`) and `x ↦ (canonicalDT D F ρ).eval(gate-outputs)` is SAT-searchable `<2^n`. The collapse is the PROVED fully-discharged switching lemma `Depth3.exists_shallow_all_tight_uncond` (no reconstruction socket — only width/clause-count + union bound); `canonicalDT` returns a `BoolDecisionTree` directly so it composes with `acc0_over_mod_searchable` with no bridge. HONEST: `ρ` restricts the oracle POSITIONS, not `x` — the position/`x` gap (oracle outputs determined-not-independent = the no-go's deeper form) is NOT crossed; this is the control-position-level collapse composed with oracles, not an `x`-level circuit speedup | `…ACC0DNFControlSwitching` |
| **position → `x` bridge: realizing oracle restrictions** | `Extends`/`RealizableByInputRestriction` (∃ input restriction `σ` forcing every fixed gate to its `ρ`-value on all completions); `modGate_eval_congr` (MOD value depends only on support); **`realizable_of_disjoint`** (POSITIVE fragment: pairwise-disjoint supports + per-gate achievability ⇒ realizable — set each fixed gate's support independently via `Classical.choose`, disjointness ⇒ no conflict); **`position_restriction_not_always_realizable`** (NO-GO: two MOD₂ gates same support `{0}` targets `0`/`1`, both forced `true` ⇒ `0=1` in `ZMod 2`, impossible). So the bridge is TRUE on the disjoint fragment, FALSE in general — must stay fragment-restricted/a socket. HONEST: solves the *forcing* half (fix determined gates); full composition needs free gates to stay free under `σ` (disjoint supports it, not assembled) | `…ACC0OracleRestrictionRealization` |
| **disjoint-fragment `x`-level speedup (exact reduction)** | **`gate_vector_realizable`** (disjoint supports + each gate both-achievable ⇒ the gate-output vector is SURJECTIVE: `∀ y, ∃ x, ∀ j, (gⱼ).eval x = y j`, via `realizable_of_disjoint` at `ρ=some∘y`); **`disjoint_fragment_sat_iff`** (the EXACT reduction `Satisfiable (x ↦ C(g₁(x),…,g_k(x))) ↔ Satisfiable C` over `Fin k → Bool`); **`disjoint_fragment_speedup`** (`f`-SAT over `Fin n` ↔ search over `Fin k → Bool`, card `2^k < 2^n`). The `n`-variable circuit collapses EXACTLY to the `k`-variable control — a genuine `x`-level speedup. Disjointness is load-bearing: the no-go breaks surjectivity for overlapping gates, so the reduction fails there | `…ACC0DisjointFragmentSpeedup` |
| **parity (MOD₂) constraint realization: realizable ⟺ F₂ system consistent** | **`realizable_iff_achievable`** (arbitrary MOD, no disjointness needed: `RealizableByInputRestriction ρ gate ↔ ∃ x, ∀ j b, ρ j = some b → (gⱼ).eval x = b` — disjointness only *constructs* the input, the no-go is when none exists); `parityGate`/`zmod2_ne_iff`/**`parity_force_linear`** (forcing a parity gate = the F₂ equation `parity Sⱼ = (tⱼ if b else tⱼ+1)`); **`parity_realizable_iff_consistent`** (realizable ⟺ the F₂ parity system has a Boolean solution); **`control_sat_iff_reachable_image`** (arbitrary MOD: SAT ⟺ search the *reachable* gate-output image, not pretended-independent `2^k`); `parityVector_xor`/`parity_reachable_zero_mem`/**`parity_reachable_add_mem`** (the reachable parity image is an F₂-SUBSPACE — closed under `0`,`+` via xor-additivity — so `\|image\| = 2^rank`, `2^k` only in the disjoint/full-rank case). Replaces "disjoint-only" with linear-algebraic compatibility | `…ACC0ParityConstraintRealization` |
| **exact `2^rank` cardinality of the reachable parity image** | `parityLinMap S : (Fin n→ZMod 2) →ₗ[ZMod 2] (Fin k→ZMod 2)` (F₂-linear realization of the parity statistic); `parityVector_eq_linMap`; **`range_parityVector`** (reachable image `= ↑(LinearMap.range parityLinMap)` — Boolean-input embedding surjective via `zmod2_ite`); `parity_subspace_card` (`Fintype.card ↥(range) = 2^finrank` via `Module.card_eq_pow_finrank`+`ZMod.card`); **`parity_reachable_card`**: `Nat.card (Set.range (parityVector S)) = 2 ^ finrank (ZMod 2) (range parityLinMap)`. The parity-gate observer boundary is EXACTLY `2^rank` (`2^k` iff the gates are independent / full rank). Quantitative closure of the reachable-image story (MOD₂); general MOD_q is 0/1-feasibility over ZMod q | `…ACC0ParityRankCardinality` |
| **rank-based `x`-level speedup (parity)** | `image_parityVector_card` (`\|univ.image (parityVector S)\| = 2^rank`, via `Set.toFinset_range`+`Set.toFinset_card`+`parity_reachable_card`); **`parity_rank_speedup`**: a parity-gate control's `f`-SAT over `Fin n` ⟺ search the reachable gate-output image (gate vector = relabel ∘ parityVector), whose card is `< 2^n` once `2^rank < 2^n` (i.e. `rank < n`). Strictly stronger than disjoint `2^k`: `rank ≤ k`, strict under linear dependence — a parity control whose gates span an `<n`-dim row space is sub-`2^n` searchable EVEN with overlapping supports | `…ACC0ParityRankSpeedup` |
| **general `MOD_q` realization = 0/1-feasibility** | `modGate_eval_true_iff`/`modGate_eval_false_iff` (forcing a `MOD_q` gate is the count `=`/`≠` target constraint); **`modq_realizable_iff_feasible`** (realizable ⟺ ∃ Boolean `x` meeting the equality/disequality count system — the general-`q` analogue of `parity_realizable_iff_consistent`, now a 0/1-INTEGER feasibility problem NOT linear algebra); `modQStatOn_singleton`, **`modq_residue_image_not_subspace`** (THE OBSTRUCTION proved: for `MOD₃` the reachable residues `{0,1}⊆ZMod 3` are NOT closed under `+` (`1+1=2` unreachable), so the `2^rank` subspace structure is MOD₂-specific). The `x`-level speedup itself is `q`-independent (`≤2^k` gate-output bits, via `oracle_control_over_mod_searchable`); `MOD_q` only changes *which* patterns are reachable (the 0/1-feasibility region — no closed-form card for `q≥3`) | `…ACC0ModQFeasibility` |
| **mixed moduli `MOD₆ = MOD₂ ∧ MOD₃` via CRT** | **`mod6_eval_true_iff`** (CRT split: `(⟨6,S,t⟩).eval x = true ↔ count_S(x) ≡ t.val [MOD 2] ∧ ≡ [MOD 3]`, via `Nat.modEq_and_modEq_iff_modEq_mul` `2⊥3` + `ZMod.natCast_eq_natCast_iff`); **`mod6_realizable_iff_mixed`** (a forced-true `MOD₆` family realizable ⟺ `∃ x` solving the mod-2 system AND the mod-3 system simultaneously). Combines the F₂-linear/rank story with the F₃-feasibility story, COUPLED through the shared `x` — neither rank nor feasibility alone suffices, no product/closed-form shortcut. (Forced-true case; general `ρ` makes each gate a conjunction/disjunction of the CRT parts) | `…ACC0Mod6Mixed` |
| **mixed moduli `MOD₆`, general `ρ` (true ∧, false ∨)** | **`mod6_eval_false_iff`** (forcing false negates the CRT conjunction → DISJUNCTION `¬(count ≡ t mod 2) ∨ ¬(count ≡ t mod 3)`, via `Bool.eq_false_iff`+`mod6_eval_true_iff`+`not_and_or`); **`mod6_realizable_iff_general`** (arbitrary `ρ`: realizable ⟺ `∃ x` meeting per-gate the conjunction of CRT congruences (forced-true) / disjunction of CRT disequalities (forced-false), coupled through `x`). The fully-general mixed-modulus realization = a mixed CSP over the Boolean cube (linear mod-2 ∧ feasibility mod-3 atoms, conjoined/disjoined per gate) — no rank or product shortcut | `…ACC0Mod6MixedGeneral` |
| **YBT `SYM`-top count observer (the tractable heart of `SYM∘AND`)** | `gateCount`/`symEval` (count of accepting sub-gates; a symmetric = count-function of them); `gateCount_le` (`≤ m`); **`sym_observed`** (`symEval g h` `ObservedBy` the count); **`sym_count_card_le`** (count boundary `≤ m+1` — the symmetric top collapses `2^m` → `m+1`); **`sym_searchable`** (`SYM∘(m gates)` SAT-searchable `<2^n` once `m+1<2^n`; at the YBT form `m`=quasipoly, `<2^n`). Proves WHY the YBT `SYM` top is cheap (count boundary), for arbitrary sub-gates. The YBT REDUCTION (arbitrary ACC⁰ → `SYM∘AND`, `m` quasipoly) is NOT proved — the open structural wall, socketed as `MixedACCDepthReductionSocket` | `…ACC0SymmetricObserver` |
| **low-degree polynomial → `SYM∘AND` (YBT chip)** | `monoAND` (monomial `AND` gate, fan-in `\|S\|`); `lowDegSubsets`/**`lowDegSubsets_card`** (degree-`≤D` monomials count `= ∑_{i≤D} C(n,i)`, via `card_biUnion` over `powersetCard`); **`monomial_count_le`** (distinct degree-`≤D` monomials ⇒ `≤ ∑_{i≤D} C(n,i)` bottom gates); **`lowDegreePoly_searchable`** (a `SYM` gate over `m` monomial-`AND`s is SAT-searchable `<2^n` once `m+1<2^n` — the poly value is `gateCount` over its monomial-`AND`s, so `sym_searchable` applies). The polynomial→`SYM∘AND` cash-out (0/1-coeff multilinear; general coeffs duplicate gates). The hard direction — arbitrary ACC⁰ *is* such a low-degree poly (RS approximation) — is NOT proved (the wall) | `…ACC0PolyToSymAnd` |
| **RS low-degree span → `SYM∘AND` bottom layer** | **`squarefreeEvalMonomial_eq_monoAND`** (the RS generator `∏_{i∈S} boolToZMod p (xᵢ) = if monoAND S x then (1:ZMod p) else 0` — the low-degree-span generators ARE the monomial-`AND` gates); **`lowDegPolyEval_mem_monoAND_span`** (lifts Layer3's `eval_mem_lowDegSpan` through the bridge: a degree-`≤D` polynomial's cube eval ∈ `ZMod p`-span of the monomial-`AND` indicators — the `SYM∘AND` bottom, `≤∑_{i≤D}C(n,i)` gates). Connects the RS machinery to the `SYM∘AND` world exactly. Honest gaps: RS is *approximate* (`acc0_approx_by_lowRankPredictor`, agrees on `1-ε`) so this is an *approximate* `SYM∘AND`; the `ZMod p`-linear top → count-mod-`p` `SYM` needs gate duplication; the exact `AC⁰[p]→SYM∘AND` across depth is the wall | `…ACC0RSToSymAnd` |
| **weighted-count `SYM` observer (closes the `ZMod p`-linear → count gap)** | `weightedGateCount c g x := ∑ⱼ cⱼ·[gⱼ accepts]` (= count over the multiset with gate `j` duplicated `cⱼ` times) + `weightedSymEval`; `weightedGateCount_le` (`≤ ∑ⱼ cⱼ`); **`weightedSym_observed`/`weightedSym_count_card_le`/`weightedSym_searchable`** (weighted `SYM` observer, boundary `≤ ∑ⱼcⱼ+1` not `2^m`, searchable `<2^n` once `∑cⱼ+1<2^n`); **`weightedGateCount_cast_eq`** (over `ZMod p` with `cⱼ=(coeffⱼ).val`, the weighted count's residue = the polynomial value `∑ⱼ coeffⱼ·monoAND_j` — the coefficient-duplication identity). Closes the gap exactly: a `ZMod p`-combination of monomial-`AND`s IS a weighted-count `SYM∘AND`. (RS still approximate; exact reduction still the wall) | `…ACC0WeightedSymAnd` |
| **approximate vs exact (what RS approximation buys)** | `disagreeSet f g` (inputs where `f≠g`); **`sat_card_le_of_disagree`** (`\|{f=true}\| ≤ \|{g=true}\| + \|disagreeSet\|` — an approximating `g` bounds `f`'s *solution count*, not SAT); **`exact_sat_iff`** (`disagreeSet = ∅` ⇒ `Satisfiable f ↔ Satisfiable g` — only EXACT agreement transfers SAT). Pins down why the cash-out needs the exact `SYM∘AND`: approximation gives only a sparsity/correlation bound; exactness gives the SAT equivalence | `…ACC0ApproxConsequence` |
| **YBT exact normal-form socket (the structural wall, conditional cash-out)** | `HasExactSymAndForm C` (socket: `eval C = symEval (monomial-ANDs) h` with `m+1<2^n`); **`ybt_socket_searchable`** (socket ⇒ `Satisfiable (eval C)` decided by `<2^n`-cell search, via `lowDegreePoly_searchable`). The socket for arbitrary `ACC⁰` (`m` quasipoly) IS the YBT theorem — a known classical result, NOT formalized (the structural wall, cf. `MixedACCDepthReductionSocket`). Needs the EXACT form (approximate only bounds solution count) | `…ACC0YBTSocket` |
| **YBT reduction core: `AC⁰[p]` circuit → `SYM∘AND`-bottom span (conditional on degree)** | **`acc0p_circuit_in_monoAND_span`**: `IsAC0Syntax c` + `(toPoly p c).totalDegree ≤ D` ⇒ `(x ↦ boolToZMod p (c.eval x)) ∈ ZMod p`-span of degree-`≤D` monomial-`AND` indicators. Wires Layer3's EXACT circuit→poly reduction (`toPoly_eval_AC0`) into the `SYM∘AND` bottom via `squarefreeEvalMonomial_eq_monoAND`/`lowDegPolyEval_mem_monoAND_span`. **The degree bound IS the wall**: `toPoly` is EXACT but high-degree (unbounded fan-in `∧` = product of inputs ⇒ degree grows with fan-in); polylog degree is the *approximate* RS (`toAgree`), so the exact polylog form for arbitrary `AC⁰[p]` is open. (Span→explicit combination + `weightedSym_searchable` = remaining mechanical glue) | `…ACC0YBTReduction` |
| **`toAgree` approximant in the bounded `SYM∘AND` span (degree bound DISCHARGED)** | **`acc0p_toAgree_in_monoAND_span`**: `(x ↦ eval (embed x) (toAgree p t R C)) ∈ ZMod p`-span of degree-`≤((p-1)t)^{depth}` monomial-`AND` indicators — **UNCONDITIONAL** (the degree bound is *proven* by Layer3's `toAgree_totalDegree_le`, not assumed, unlike the exact `toPoly`). So the RS approximant is genuinely a bounded `SYM∘AND`: polylog degree (const depth, polylog `t`) ⇒ quasipoly gates `∑_{i≤D}C(n,i)` — the YBT size, for the approximant. Remaining gap is purely approximate-vs-exact (`toAgree` agrees `1-ε`, bounds solution count not SAT, per `…ACC0ApproxConsequence`); the exact form is the wall | `…ACC0ToAgreeDegree` |
| **symmetric-count exact trick (`AND`/`OR` are exactly symmetric, no ε)** | `gateCount_eq_card_iff` (`gateCount g x = m ↔ ∀ j, gⱼ x` — `AND` = count hits `m`); `gateCount_pos_iff` (`1 ≤ gateCount g x ↔ ∃ j, gⱼ x` — `OR` = count positive); **`and_exact_sym`/`or_exact_sym`** (`AND`/`OR` over `m` gates EQUAL `symEval g [·=m]`/`symEval g [1≤·]` — EXACT, no approximation); **`and_exact_searchable`/`or_exact_searchable`** (hence SAT-searchable `≤ m+1` cells, exactly). The Beigel–Tarui exact trick at the GATE level: `AND`/`OR`/`MOD` need no RS approximation — they are exactly symmetric count functions, the exactness from the `SYM` top reading the exact count. Closes the approximate/exact gap per-gate; depth composition (keeping size quasipoly) is the remaining structural step | `…ACC0SymmetricExact` |
| **depth composition law (top over observed subcircuits → searchable, product boundary)** | **`depth_compose_searchable`**: a top over `k` subcircuits each `ObservedBy` a statistic with `≤\|Sᵢ\|` cells, with `∏ᵢ\|Sᵢ\| < 2^n`, is SAT-searchable in `< 2^n` (joint statistic = product, via `observed_top_pi`+`observed_sat_iff`+`observed_pi_cellCount_le`). The depth-composition law — composes at any depth. Honest: the boundary is the **product** `∏ᵢ\|Sᵢ\|` (EXPONENTIAL in width); the YBT quasipoly control replaces this multiplicative blow-up by the *additive* polynomial-degree composition (`…ACC0AdditiveDegree`/`…ACC0ToAgreeDegree`, approximate) — the exact quasipoly depth-composition is the wall | `…ACC0DepthCompose` |
| **additive degree control (`AND` degree = sum of input degrees)** | `toPolyList_eq_map`; **`toPoly_andGate_totalDegree_le`** (`totalDegree (toPoly (AND cs)) ≤ ∑_{c∈cs} totalDegree (toPoly c)` — the gate degree is the SUM, not product, of input degrees, via `MvPolynomial.totalDegree_list_prod`); **`toPoly_andGate_totalDegree_le_of_bounded`** (fan-in `≤w`, inputs `≤D` ⇒ `AND` degree `≤ w·D` — one layer multiplies degree by `≤` fan-in, via `List.sum_le_card_nsmul`). The additivity that makes the polynomial method quasipoly: degree adds across `∧`, so depth-`d` ⇒ polylog degree (`((p-1)t)^depth`) ⇒ quasipoly monomial-`AND`s — vs the observer's exponential product (`…ACC0DepthCompose`). Within-layer additive; exact polylog-across-depth is the wall | `…ACC0AdditiveDegree` |
| **exact depth composition (Bool specialization, exponential boundary)** | **`exact_depth_composes`**: for `sub : Fin k → (Fin n → Bool) → Bool`, any `top`, once `2^k < 2^n`: `(fun x => top (fun i => subᵢ x))` is SAT-searchable in `< 2^n` with the subcircuit-output vector as the cell statistic (boundary `2^k`). The exact (no-approximation) side of composition — the `…ACC0SymmetricExact` exact gates compose at any depth, but with the **exponential** `2^k` product boundary. Specializes `depth_compose_searchable` at `S = fun _ => Bool` (`Fintype.card_bool`); makes the exact-vs-quasipoly tension precise: exact ⇒ exponential boundary, quasipoly ⇒ additive degree but only *approximate* — having *both* (exact `SYM∘AND` of quasipoly size) is the Beigel–Tarui ℤ-polynomial+CRT wall | `…ACC0ExactCompose` |
| **integer-polynomial CRT decode (exact BACK half of Beigel–Tarui)** | **`count_crt_iff`** (CRT for the integer count `gateCount`: `≡t [MOD qs.prod] ↔ ∀i ≡t [MOD qs.get i]` for pairwise-coprime `qs`, via `Nat.modEq_list_prod_iff`); **`modCount_factors_through_resVec`** (the exact `MOD_{qs.prod}` count decision `= G(countResVec)`, factoring through the residue vector `i↦(gateCount : ZMod (qs.get i))`, via CRT + `ZMod.natCast_eq_natCast_iff`); **`countResVec_card_le`** (`≤ qs.prod`); **`count_crt_sat_speedup`** (`qs.prod<2^n ⇒ <2^n` residue cells); **`mod6_count_crt_speedup`** (`[2,3]` concrete). Generalizes `mod6_eq_mod2_and_mod3` to arbitrary squarefree `M=∏qᵢ` and attaches it to the **count polynomial** (not one MOD-gate's support count). Honest: the EXACT back half only — CRT decodes from the EXACT count; it does **NOT** turn approximate RS residues into an exact decision, so it does not close the approximate/exact gap = Wall 1 (front half: `ACC⁰→exact low-degree ℤ-poly across depth`) | `…ACC0IntegerPolynomialCRT` |
| **toy exact-AND-quasipoly `SYM∘AND` (depth-2 bounded-fan-in DNF)** | The one restricted fragment where **exact and quasipolynomial hold at once** (what general Wall 1 can't have). For a depth-2 `OR∘AND_w` DNF with distinct bottom monomials of fan-in `≤w`: **`dnf_exact_symAnd`** (`= symEval (monoAND∘mono) [1≤·]` **exactly**, via `or_exact_sym` — no approximation); **`dnf_bottom_count_le`** (`m ≤ ∑_{i≤w}C(n,i)` distinct degree-`≤w` monomials, quasipoly `n^{O(w)}`, via `monomial_count_le`); **`dnf_exact_quasipoly_searchable`** (both at once + SAT-searchable in `<2^n` once `(∑_{i≤w}C(n,i))+1<2^n`). Bounded fan-in `w` keeps degree bounded **exactly** (`monoAND` is a genuine `AND`, not an approximant). Honest: holds ONLY for this fragment — the general wall is the loss of any of {bounded fan-in, no MOD layer, bounded depth}; unbounded fan-in/MOD/depth force exact⇒exponential (Wall 1) | `…ACC0ExactQuasipolyDepth2` |
| **exact-quasipoly fragment + one `MOD` top (`MOD_M ∘ AND_w`)** | Extends the fragment toward YBT by replacing `OR` with a `MOD_M` top — both layers still EXACT, no RS approximation: **`modAnd_bottom_count_le`** (bottom `AND_w` quasipoly, `m≤∑_{i≤w}C(n,i)`); **`modAnd_exact_observed`** (top `MOD_M` decoded EXACTLY by the count-residue vector via CRT, `…ACC0IntegerPolynomialCRT`); **`modAnd_exact_quasipoly_searchable`** (bottom quasipoly ∧ searchable `<2^n` once `M=qs.prod<2^n` — the modular top compresses the count to `M` cells); **`mod6_and_exact_quasipoly_searchable`** (`[2,3]` concrete). `MOD` is *exactly* symmetric so the top needs no approximation. Honest: stops at depth-2 bounded-fan-in bottom; general YBT needs arbitrary `ACC⁰` bottom where exact⇒exponential = Wall 1 (the exact `MOD` top buys nothing — it's the *bottom* `AND_w→exact low-deg poly across depth` that breaks) | `…ACC0ExactQuasipolyModTop` |
| **depth-3 exact composition over `MOD_M ∘ AND_w` (the "across depth" clause)** | Tests whether exactness survives ONE composition step: a top over `k` middle `MOD_M∘AND_w` gates. **`depth3_middle_exact`** (each middle gate CRT-decoded EXACTLY by its residue vector); **`depth3_modAnd_compose_searchable`** (every middle bottom quasipoly `m≤∑_{d≤w}C(n,d)` ∧ depth-3 circuit searchable `<2^n` once `2^k<2^n`, via `exact_depth_composes` k-bit middle-output observer). ANSWER: exactness survives, at the **product** cost `2^k` (quasipoly only for `k=polylog`). Honest: this is exactly why the general construction breaks — as top fan-in/depth grow the exact boundary multiplies to exponential = Wall 1 (the bottom degree-across-depth; the exact top buys nothing) | `…ACC0ExactQuasipolyDepth3` |
| **anti-product trick: symmetric top avoids the `2^k` blow-up** | The smallest case where the depth-3 product wall is genuinely AVOIDED. A *symmetric* top over `k` middle `MOD_M∘AND_w` gates reads only the COUNT of accepting middle gates: **`antiproduct_count_card_le`** (`≤ k+1` cells, NOT `2^k`); **`antiproduct_sym_modAnd_searchable`** (bottom quasipoly ∧ `≤k+1` cells ∧ searchable `<2^n` once `k+1<2^n`). Two symmetric layers (outer `SYM` over inner `MOD∘AND_w`=`SYM∘AND`) compose with LINEAR observer state, not the product `2^k`/`M^k`. Honest: genuine anti-product but NOT YBT — needs pre-grouping as one `SYM` layer, can't iterate (symmetric in middle outputs not inputs), bottom degree still Wall 1. This is the YBT `SYM` top being tractable (= `sym_count_card_le`, `m+1` not `2^m`) one layer up | `…ACC0AntiProductSym` |
| **bottom-clause no-go: exact unbounded-fan-in `OR`/`AND` = full degree** | Attacks Wall 1's bottom clause directly and proves the obstruction is REAL. Over `F₂` the ANF is unique (`anf_involutive`): **`anf_andFn_univ`**/**`anf_orFn_univ`** (top ANF coeff of `AND`/`OR` `= 1` — for `OR` via parity of the `2ⁿ−1` nonempty subsets); **`or/and_exact_degree_full`** (`∃ S, |S|=n ∧ anf≠0`); **`or/and_not_anf_degree_lt`** (NO exact `F₂` representation of degree `<n`). So exact unbounded-fan-in `OR`/`AND` has degree = fan-in `n` ⇒ `2^n` monomials ⇒ exponential. Rules out the naive route (single exact low-degree poly per gate); the only escape is approximate→exact symmetric-top conversion = the irreducible Beigel–Tarui core (Wall 1, NOT crossed). Exact counterpart to the proved *approximate* side (`toAgree_totalDegree_le`) | `…ACC0ExactDegreeNoGo` |
| **BT approximate→exact conversion: socket + proven decoding** | The only route left after the no-go. **`ApproxToExactSymmetricDecode`** (the missing BT theorem made explicit: `f` = exact symmetric/count decode of `r` `IsLowDegreeGate D` gates; non-trivial ONLY via the low-degree clause — `r` copies of `f` satisfy it otherwise — so this for arbitrary `ACC⁰` = Wall 1); **`socket_searchable`** (socket ⇒ `≤r+1` cells); **`majority_decode`** (TOY, PROVED: strict-majority-correct at every point ⇒ `f x = decide(r<2·gateCount)` EXACTLY — error-corrected threshold decoding); **`majority_decode_symmetric`** (`f = symEval g [>r/2]`); **`majority_decode_gives_socket`** (low-degree + majority-correct ⇒ socket). Honest: the DECODING half is proven; the open part = low-degree approximants majority-correct everywhere for `ACC⁰` = the BT/Yao construction = Wall 1 (not faked) | `…ACC0ApproxToExactDecode` |
| **balancedness seed of the RS/BT probabilistic `OR`** | Attacking the construction at its atom: the balancedness of nonzero `F₂` linear forms. **`pv`** (the form `⊕_{i∈S}v_i`), **`tog`**/`tog_tog` (toggle involution `S△{j}`), **`pv_tog`** (toggling a `v_j=1` coord flips parity), **`pv_false`** (form `=0` on all-`0` input ⇒ one-sided, no false positive for `OR`), **`pv_balanced`** (THE SEED: for `v≠0` exactly `2^{m-1}` of `2^m` subsets give form `=1` — a random parity predicts `OR=1` w.p. exactly `1/2`; proved by the parity-flipping involution + `card_bij'`). Honest: this is the atom; degree-`t` boosting (`1-2^{-t}`) + quasipoly sampling (Chernoff+union) remain socketed = Wall 1. Basis caveat: parity forms are low *poly*-degree but high *monoAND*-degree vs `IsLowDegreeGate` | `…ACC0ProbabilisticForm` |
| **degree-`t` boosting: full `t`-tuple family majority-correct everywhere** | The boosting step over the atom. `OR` of `t` independent forms is wrong only when all `t` vanish: **`pv_zero_card`** (`#{=0}=2^{m-1}`), **`boost_wrong_card`** (all-zero tuples `=(2^{m-1})^t`, via `card_piFinset` independence), **`boost_total`** (`(2^m)^t`), **`boost_correct_card`** (`(2^m)^t-(2^{m-1})^t`), **`boost_predict_zero`** (one-sided: never fires on all-`0` input), **`boost_majority_nonzero`** (`t≥2, v≠0 ⇒` strict majority correct, via `2^t>2`). So the FULL `t`-tuple family (`t≥2`) is majority-correct at EVERY input — **discharges the "majority-correct" clause of the socket for `OR`**. Sole remaining gap: family size `(2^m)^t` exponential vs quasipoly = the Chernoff+union *sampling* (Wall 1) | `…ACC0ProbabilisticBoost` |
| **self-contained finite Chernoff (sampling concentration engine)** | Route B: a purely combinatorial Chernoff — no measure theory, no `exp`, in `Finset`/`ℕ`/`ℝ`. **`finite_chernoff_lower`** (for `c≥1`: `#{σ:Fin r→P | 2·#{i|σ i∈G}≤r} ≤ (|G|/c + c(|P|-|G|))^r` — Markov-on-a-product, engine `∑_σ∏_i f(σ i)=(∑_x f x)^r` via `Finset.sum_prod_piFinset`, the finite `E[∏]=∏E`); **`finite_chernoff_majority`** (`c=2`, good-fraction `≥3/4` ⇒ `≤(7|P|/8)^r`, geometric base `7/8<1`). Honest: the concentration engine; the union bound over `2^n` inputs + instantiation to the boosted forms + basis bridge remain = Wall 1 assembly (routine given this) | `…ACC0FiniteChernoff` |
| **union-bound sampling existence (abstract, closes the sampling gate)** | The probabilistic-method existence argument, finite + self-contained. **`exists_sample_majority_correct_all`**: abstract over input set `X`, pool `P`, good sets `Good : X → Finset P`; given `∀ x, 3|P|≤4|Good x|` (density `≥3/4`), `0<|P|`, `|X|·(7/8)^r<1` ⇒ `∃ σ : Fin r → P, ∀ x, r < 2·#{i | σ i ∈ Good x}` (majority-correct at EVERY input). Proof: union the per-input minority-good sample sets (`card_biUnion_le`), bound each by `(7|P|/8)^r` (`finite_chernoff_majority`), bad samples `< |P|^r` total ⇒ good sample exists (pigeonhole). Next: instantiate `X=Fin m→Bool`, `P=`boosted `t`-tuples, `Good x=`correct forms (density from `boost_majority_nonzero`, `|X|=2^m ⇒ r=O(m)`); then basis bridge | `…ACC0SamplingExistence` |
| **instantiation to parity forms: quasipoly majority-correct family EXISTS** | Plugs the boosted parity forms into the abstract lemma. `X=Fin m→Bool`, `P=Fin t→Finset(Fin m)`, **`boostGood v`**=tuples correct on `v`. **`card_pool`** (`|P|=(2^m)^t`); **`boostGood_card_ge`** (`∀ v, 3|P|≤4|boostGood v|`, density `≥3/4` for `t≥2` — nonzero via `boost_correct_card`+`4·(2^{m-1})^t≤(2^m)^t`, zero-input all-correct via `pv_all_false`); **`exists_quasipoly_majority_correct_forms`** (`t≥2, 2^m·(7/8)^r<1 ⇒ ∃ σ:Fin r→(Fin t→Finset(Fin m)), ∀ v, r<2·#{i|σ i∈boostGood v}` — `r=O(m)` boosted forms majority-correct at EVERY input). **The socket's open clause, discharged for the parity-form construction.** Remaining: the basis bridge (parity low poly-deg vs `monoAND`-deg) = Wall 1 | `…ACC0SamplingForms` |
| **the basis bridge: parity forms ARE low `monoAND` degree (`F₂`-poly framing)** | FRAMING: low-degree = `F₂`-poly total degree. A linear form `L_S=∑_{i∈S}X_i` has POLY degree 1 (it's the SUM, not product) regardless of `|S|`; so `boostPoly σ=1-∏_k(1-L_{σk})` has degree `≤t`, and a degree-`≤t` `F₂` poly's cube-eval ∈ fan-in-`≤t` monoAND span. No mismatch — the wide set is in the linear (deg-1) part. **`linForm_totalDegree_le`** (`≤1`), **`boostPoly_totalDegree_le`** (`≤t`, the crux), **`eval_linForm`**(`=pv`), **`eval_boostPoly_eq`** (`=boolToZMod 2 (boostPredict)`), **`boostPredict_mem_monoAND_span`** (THE BRIDGE: boosted predictor's `F₂`-embedding ∈ span of degree-`≤t` monoAND indicators, via `lowDegPolyEval_mem_monoAND_span`). Resolves the bridge for the OR-gate construction. Honest: full BT front half = arbitrary `ACC⁰` across depth (this OR is one gate) = remaining Wall 1 | `…ACC0BasisBridge` |
| **AND-gate BT front half (via OR-duality)** | `AND(v)=¬OR(¬v)`. Dualizes OR: linear form `∑X_i` → affine form **`affForm`**`=∑(1+X_i)` (evaluates to `L_S(¬v)`, still degree 1), `boostPoly` → **`andPoly`**`=∏_k(1-A_{σk})`. **`affForm_totalDegree_le`** (`≤1`), **`andPoly_totalDegree_le`** (`≤t`), **`eval_andPoly_eq`** (`=boolToZMod 2 (andPredict)`), **`andPredict_eq_not_boostPredict`** (duality: `andPredict σ v=¬boostPredict σ (¬v)` — OR's majority-correctness transfers via input-negation bijection), **`andPredict_mem_monoAND_span`** (AND basis bridge: AND predictor's `F₂`-embedding ∈ span of degree-`≤t` monoAND indicators). 2nd of 3 `ACC⁰` gate types (after OR; MOD next). Remaining: MOD + depth composition = Wall 1 | `…ACC0AndBasisBridge` |
| **one-layer composition of BT approximants (degree multiplies by `t`)** | The depth-composition step: replace input vars `X_i` by the `k` subgate approximant polynomials `P_j` (each degree `≤d`). **`compLinForm`**`=∑_{j∈S}P_j` (linear form over OUTPUTS), **`compPoly`**`=1-∏_l(1-compLinForm(σl))` (boosted OR over subgates). **`compLinForm_totalDegree_le`** (`≤d`), **`compPoly_totalDegree_le`** (`≤t·d` — THE CRUX: degree `×t` per layer, so across depth `D` it's `t^D=(log s)^{O(D)}`, polynomial not exponential), **`eval_compPoly`** (= boosted OR over subgate values), **`compPoly_eval_mem_monoAND_span`** (composed approximant ∈ degree-`≤t·d` monoAND span). Basis bridge = `P_j=X_j, d=1` special case. Honest: one OR-layer; full depth induction (error accumulation, MOD layer, SYM∘AND assembly) = Wall 1 | `…ACC0LayerCompose` |
| **across-depth error accumulation (linear in gate count)** | The error counterpart to `…ACC0LayerCompose`'s degree composition. Composite errs only where SOME gate errs (correctness composes), so composite error ⊆ union of per-gate errors. **`error_union_bound`** (`|⋃_g Err g| ≤ #gates·e` via `card_biUnion_le`), **`composite_error_subset`** (comp-error ⊆ gate-error union, given comp correct off the union), **`composite_error_bound`** (`|comp error| ≤ #gates·e`), **`exists_majority_correct_composite`** (`2·#gates·e<|X| ⇒ |X|<2·|correct|` strict majority). LINEAR-in-`s` error (not exponential) is WHY constant depth works: per-gate `e<|X|/(2s)` keeps composite majority-correct. With entry 51 (polylog degree) = the RS depth composition. Honest: abstract union-bound; full assembly (per-point composition + boosting `e=2^{-t}|X|` + MOD + SYM∘AND) = Wall 1 | `…ACC0ErrorAccumulation` |
| **per-point composition (discharges `hcomp`)** | **`boost_formula`** (`1-∏(1-pv w)=boolToZMod(boostPredict σ w)`), **`eval_compPoly_of_subgates`** (if `P_j` compute `h_j` at `x`, `compPoly P σ` computes the boosted OR of subgate values `(h_1 x,...,h_k x)`). The bridge from degree composition (`…ACC0LayerCompose`) + error accumulation (`…ACC0ErrorAccumulation`) to actual Boolean computation | `…ACC0CompositionCorrect` |
| **`MOD₂` is exactly degree 1 (error 0)** | Unlike OR/AND (no exact low-degree, `…ACC0ExactDegreeNoGo`), `MOD₂`=parity IS the linear form. **`mod2_exact_eval`** (`eval (linForm univ)=boolToZMod(parityBool)`), **`mod2_mem_monoAND_span`** (degree-`≤1`, exact). Honest: `MOD₂` only (=`AC⁰[2]`); composite `MOD_m` has NO low-degree rep over any field (RS works for prime-power only) — the genuine `ACC⁰` barrier (why Williams needed a different method) | `…ACC0Mod2Exact` |
| **`MOD_p` is exactly degree `p−1` over `F_p` (Fermat) — `AC⁰[p]` ingredient** | Prime-`p` generalisation of `…ACC0Mod2Exact`. By Fermat (`ZMod.pow_card_sub_one_eq_one`): `MOD_p(x)=[∑_{i∈S}x_i≡0 mod p]=1−(∑X_i)^{p−1}` exactly. **`modpPoly`/`modpBool`**, **`modp_exact_eval`** (exact, error 0, via `set s` + case-split `s=0`/`zero_pow`/Fermat), **`modpPoly_totalDegree_le`** (`≤p−1`, `totalDegree_pow`), **`modp_mem_monoAND_span`** (degree-`≤(p−1)` monoAND span over `F_p`, via `lowDegPolyEval_mem_monoAND_span` at prime `p`). Composes with degree `p−1`, error 0; prime powers → `F_{p^k}` ⇒ full `AC⁰[p^k]`. Honest: prime only; composite `m=p·q` pins Fermat exponent to `p−1≠m−1` ⇒ no single field (`mod6_eq_mod2_and_mod3`, `modq_residue_image_not_subspace`). Classical `AC⁰[p]`; NOT `NEXP⊄ACC⁰`, NOT `P≠NP` | `…ACC0ModPExact` |
| **single small-error boosted form (averaging → per-gate error)** | From the majority-correct family to a *single* small-error form. **`errSet`** (`{w | (∃j w_j)∧∀l L_{σl}(w)=0}`) + **`errSet_eq`** (= the actual `boostPredict`-vs-`OR` error, via one-sidedness `pv=1→∃j`); **`sum_errSet_card_le`** (`∑_σ|errSet σ|≤2^k·(2^{k-1})^t`, Fubini double-count via `boost_wrong_card`); **`exists_small_errSet`** (averaging+pigeonhole: `∃σ (2^k)^t·|errSet σ|≤2^k·(2^{k-1})^t`, error fraction `≤2^{-t}`). The per-gate small-error input for `…ACC0ErrorAccumulation` | `…ACC0SmallErrorForm` |
| **circuit datatype + approximation invariant + base cases (depth-induction setup)** | The scaffolding for the RS depth induction. **`Circ n`** (unbounded-fan-in: `inp`/`const`/`not`/`or`[List]/`and`[List]) with **`Circ.eval`**/**`Circ.size`** (List recursion via `attach`+`sizeOf` termination). **`errCard P C`** (#inputs where poly `P` disagrees with circuit `C`), **`Approximable C D E := ∃P, totalDegree≤D ∧ errCard≤E`** (the invariant). Base cases PROVEN: **`approximable_inp`** (`X i`, `D=1,E=0`), **`approximable_const`** (`C(boolToZMod b)`, `D=0,E=0`), **`approximable_not`** (`1-P`, preserves `D,E`). OR/AND inductive step stated as remaining (combines small-error `…ACC0SmallErrorForm` + degree-comp `…ACC0LayerCompose` + per-point `…ACC0CompositionCorrect` + error-accum `…ACC0ErrorAccumulation`; `degree≤t·D, error≤∑E_i+2^{-t}2^n`). Honest: scaffolding+base cases; inductive step + composite MOD = Wall 1 | `…ACC0CircuitApprox` |
| **input-space small-error averaging (per-gate error for the circuit induction)** | Generalizes `…ACC0SmallErrorForm` from the subgate-output space to an arbitrary value map `v : X → (Fin k → Bool)` (the circuit case: `x↦v(x)` not measure-preserving, average over INPUTS). **`boostError_iff`** (boosted OR errs at `w` ↔ `(∃j w_j)∧∀l L_{σl}(w)=0`); **`errSetV`/`errSetV_eq`** (input error set for `v`, = actual `boostPredict`-vs-`OR` error on `v(x)`); **`sum_errSetV_card_le`** (`∑_σ|errSetV v σ|≤|X|·(2^{k-1})^t`, Fubini over inputs); **`exists_small_errSetV`** (`∃σ (2^k)^t·|errSetV v σ|≤|X|·(2^{k-1})^t`, error fraction `≤2^{-t}` over circuit inputs). The per-gate input for the OR/AND inductive step of `…ACC0CircuitApprox` | `…ACC0InputSmallError` |
| **the OR/AND inductive step (assembled)** | One OR/AND layer of the RS depth induction. `perr P f` (error set poly-vs-Bool-fn). **`or_step`**: subgates `h_i` with degree-`≤D` error-`≤E` approximants `P_i` ⇒ `∃ Q (=compPoly P σ), Eg` with `totalDegree Q≤t·D`, `perr Q (OR-of-h)≤k·E+Eg`, `(2^k)^t·Eg≤2^n·(2^{k-1})^t`. CRUX = error containment `perr(compPoly,OR)⊆(⋃_i perr(P_i,h_i))∪errSetV` (per-point composition `eval_compPoly_of_subgates` + `errSetV_eq`: where all subgates correct AND gate-boosting correct, compPoly=OR exactly), then `card_union_le`+`card_biUnion_le`+`exists_small_errSetV`. **`and_step`**: dual `AND=¬OR¬` (or_step on negated subgates `1-P_i`/`¬h_i` via `perr_not_eq` + `andTarget_eq`). Honest: one layer over Fin k; Circ List-to-Fin packaging + depth iteration + composite MOD = Wall 1 | `…ACC0OrStep` |
| **the depth induction: every MOD-free circuit is approximable (AC⁰ closed)** | Closes the RS depth induction for the MOD-free (`AC⁰`) fragment end-to-end. **`or_eval_bridge`**/**`and_eval_bridge`** (`Circ.eval (or/and cs)` = `orTarget`/`andTarget` of subcircuit values; List→Fin via `any_eq_true`/`all_eq_true`+`mem_iff_get`+`Bool.eq_iff_iff`); `errCard_eq_perr` (rfl). **`approximable_exists`**: `∀ C : Circ n, ∃ D E, Approximable C D E` — well-founded recursion on `sizeOf` (decreasing via `List.sizeOf_lt_of_mem (get_mem)`+omega), base cases (inp/const/not) + `or_step`/`and_step` bridged from List to Fin (`choose`+`Finset.sup` for uniform subgate bounds). Honest: **classical `AC⁰`/`AC⁰[2]`-level (Tier 1/2), NOT new math, NOT P≠NP**. MOD extends only to prime-power (`mod2_mem_monoAND_span`); composite MOD has no low-deg rep = the genuine `ACC⁰` barrier (Wall 1, why Williams) | `…ACC0DepthInduction` |
| **quantitative degree bound: `degree ≤ t^depth`** | Sharpens the AC⁰ induction to the RS degree form. `cdepth` (OR/AND add a layer, NOT free), `le_foldr_max`, **`cdepth_or/and_get_lt`** (child depth < parent), `Approximable.mono_deg`. **`approximable_quant`** (`ht : 1≤t`): `∀ C, ∃ E, Approximable C (t^cdepth C) E` — degree `≤ t^depth`, by well-founded recursion: base via `simp[cdepth,pow_zero]`/`mono_deg`, OR/AND via `or_step`/`and_step` with uniform `D=t^(depth-1)` (each subgate `t^cdepth(child)≤t^(depth-1)` by `pow_le_pow_right`+child<parent) then `t·t^(depth-1)=t^depth`. Each OR/AND layer ×t ⇒ `(log s)^{O(d)}` for `t=polylog`, constant depth. Honest: still classical AC⁰/AC⁰[2], error existential, composite MOD = Wall 1 | `…ACC0QuantDegree` |
| **quantitative error bound: degree `≤ t^depth` AND error `≤ size·2^{-t}`** | Completes the quantitative AC⁰ RS package. **`gate_bound`** (`∃σ, 2^t·|errSetV v σ|≤2^n` — per-gate boosting error `≤2^{-t}` fraction; cancels `(2^{k-1})^t` for `k≥1`, `errSetV=∅` for empty `k=0` gate); **`or_step_quant`/`and_step_quant`** (degree `≤t·D`, error `≤(∑ subgate errors)+Eg`, gate `2^t·Eg≤2^n` — sum form for size-additive accumulation); `sum_get_eq` (Fin↔List sum bridge via `ofFn`); **`approximable_full`** (`ht:1≤t`): `∀ C, ∃ Q, Q.totalDegree ≤ t^cdepth C ∧ 2^t·(perr Q ⟦C⟧).card ≤ size C·2^n` — degree `t^depth` AND error `size·2^{-t}`, by well-founded recursion (errors accumulate additively over `size` gates via union bound, each layer `≤2^{-t}` fraction). Honest: classical AC⁰/AC⁰[2] (Tier 1/2), NOT new math, NOT P≠NP; composite MOD = Wall 1 | `…ACC0QuantError` |
| **Williams `fastSat` concrete + quantitative** | Grounds the abstract `fastSat`/`savings` Prop (`…ObserverWilliams`/`…WilliamsCashout`) in the SYM∘AND combinatorics. `fastSatWork m=m+1`; **`fastSat_savings_of_work_le`** (`work≤2^{n-k}⇒2^k·work≤2^n`, via `SpeedupMargin`); **`fastSatWork_le_of_degree`** (deg-≤D inj monomial-AND ⇒ work ≤(∑_{i≤D}C(n,i))+1, via `monomial_count_le`); **`lowDegMonomialCount_le_pow`** (∑_{i≤D}C(n,i)≤(D+1)·n^D, via `Nat.choose_le_pow`); **`symAnd_williams_fastSat`** (SAT decided by count-cell search ∧ cells fit 2^{n-k} ∧ savings≥2^k); **`symAnd_williams_fastSat_quasipoly`** ((D+1)·n^D+1≤2^{n-k}⇒savings≥2^k; D polylog⇒k=Ω(n) margin). Makes combinatorial fastSat a counted procedure with exact margin. Honest: does NOT prove NEXP⊄ACC⁰; open links named/unfaked = (1) YBT exact normal form (MixedACCDepthReductionSocket; method only approximate, prime-power MOD only), (2) uniform TM realization (UniformWilliamsRealizationSocket = separation-strength per williams_socket_iff_separation). NOT NEXP⊄ACC⁰, NOT P≠NP | `…ACC0WilliamsFastSat` |

**Progress on the depth-reduction wall:** the residue-observer algebra (`observed_top_pi`) makes depth composition
reusable, the toy discharges the socket for the bounded-bottom (`SYM`-of-`AND_w`) fragment, and `acc0_residueObserved`
now *derives* the residue/mixed observer from the raw `ACC0Circuit` inductive (no assumed bottom) — every circuit is
observed by a product statistic of state count `stateBound C` (the occurrence product). The honest gap is unchanged:
that bound is the *occurrence* product, so the gain holds only for few-leaf circuits where MOD gates supply the
compression; getting a *small* bound for an *arbitrary* `ACC⁰` circuit is the structural shrinkage of full
Yao–Beigel–Tarui — still open.

**Architecture closed (conditionally):** the residue-speedup chain (steps 1–4, proved) cashes out to `NEXP ⊄ ACC⁰`
through exactly **two named open sockets**, both separation-strength (proved equivalent to the separation by the
self-audit theorems):
1. `MixedACCDepthReductionSocket` — Yao–Beigel–Tarui depth-2 normal form (switching can't give it: the MOD no-go).
2. `UniformWilliamsRealizationSocket` — uniform `2^{n−n^ε}` realization + Williams' algorithmic method.
The only *proved* link in the cash-out is `mixedACC_speedup_of_depthReduction` (the residue chain). `NEXP ⊄ ACC⁰`
remains an abstract `Prop` — **not defined, not proved**. This is the honest architecture, not a result.

**Why the *lower bound* is open:** `MOD_6`'s two CRT components are each low-degree but over *incompatible* fields —
`MOD_3` over `F_2` and `MOD_2` over `F_3` are high-degree (Smolensky). A single-`F_p` polynomial observer captures
only one component; a *modulus-stratified / hybrid* observer is needed, and whether it yields a lower bound is the
ACC⁰ frontier.

**The route that does cross it — STARTED:** not more RS, but **Williams' algorithmic method** (faster ACC⁰-SAT +
the nondeterministic time hierarchy → `NEXP ⊄ ACC⁰`). The **SAT-speedup kernel is now proved**
(`…ACC0ModResidueSpeedup`): a depth-2 `MOD`-circuit factors through its *residue vector* (∈ `∏ ZMod q_j`), so SAT is
a search over `≤ ∏ q_j` cells (`sat_iff_residue_image`, `residue_cell_count_le`) — `< 2^n` whenever `∏ q_j < 2^n`
(e.g. `6^k < 2^n` for `MOD_6`). This is the structural residue-compression Williams exploits. **Remaining gap:**
realising the speedup as a uniform (nondeterministic) algorithm + the time-hierarchy cash-out (the named gap in
`…NFrameACC0Master`) — that is what `NEXP ⊄ ACC⁰` needs, and this cell-count bound does not supply it.

## The Yao–Beigel–Tarui pipeline (target, with the proved links marked)

```
ACC⁰[p] circuit
  ──[RS approximation: APPROXIMATE agreement 1-ε, Layer3]──►   toAgree, low-degree poly over ZMod p
  ──[toAgree_totalDegree_le: PROVED, degree ≤ ((p-1)t)^depth DISCHARGED]──►   degree-bounded poly
  ──[acc0p_toAgree_in_monoAND_span = eval_mem_lowDegSpan + squarefreeEvalMonomial_eq_monoAND: PROVED]──►   ZMod p-combination of monomial-AND gates
  ──[weightedGateCount_cast_eq (coefficient duplication): PROVED]──►   count-mod-p SYM over duplicated monomial-ANDs
  ──[lowDegreePoly_searchable / weightedSym_searchable / monomial_count_le: PROVED]──►   SYM ∘ AND_{≤D}  (≤ ∑_{i≤D} C(n,i) gates, duplicated ≤ (p-1)·∑C(n,i))
  ──[sym_observed / sym_count_card_le: PROVED]──►   count observer (boundary m+1, not 2^m)
  ──[sym_searchable: PROVED]──►   SAT-searchable in < 2^n (once m+1 < 2^n)
```

Every arrow **after the RS approximation is proved** in the cell/observer model (`…ACC0RSToSymAnd`, `…ACC0PolyToSymAnd`,
`…ACC0SymmetricObserver`): the RS low-degree span IS the monomial-`AND` (`SYM∘AND` bottom) span, and the count observer
makes the `SYM` top searchable.  The **open wall** is the first arrow being *exact* (not just `1-ε`-approximate) and
holding for arbitrary `ACC⁰` across depth — the Razborov–Smolensky approximation + constant-depth composition
(`MixedACCDepthReductionSocket`).  So the YBT *cash-out* (poly → `SYM∘AND` → searchable) is complete and now wired to
the RS generators; the YBT *reduction* (`ACC⁰ → exact low-degree poly`) is the wall.

## The exact-quasipoly fragment ladder (where exactness reaches, and the one clause that stops it)

Four probes map the boundary of *exact-and-quasipolynomial* `SYM∘AND` — one result from four sides:

```
back half     MOD_M on the integer count          exact (CRT)         M cells          …ACC0IntegerPolynomialCRT
top kind      OR ∘ AND_w  (DNF)                    exact               ∑_{i≤w}C(n,i)    …ACC0ExactQuasipolyDepth2
one MOD layer MOD_M ∘ AND_w                        exact               bottom + M       …ACC0ExactQuasipolyModTop
one depth     top ∘ (MOD_M ∘ AND_w)^k              exact               PRODUCT 2^k/M^k  …ACC0ExactQuasipolyDepth3
```

**Invariant: exactness is never the obstruction.**  Every symmetric top (`OR`/`MOD`/threshold) is *exactly* a
count/CRT-decoded gate, and every *bounded-fan-in* `AND` *is* a bounded-degree monomial, exactly — no RS approximation
anywhere in this fragment.  The only thing that must stay bounded is the **bottom `AND` degree**, which controls both
the monomial count (`∑_{i≤w}C(n,i)`, quasipoly for `w=polylog`) and the **product** composition cost (`2^k`/`M^k`,
quasipoly for `k=polylog`).  So the boundary is sharp and one-dimensional: exact-quasipoly `SYM∘AND` holds **iff the
bottom degree stays polylog**; the symmetric structure above is irrelevant to exactness.  This localizes **Wall 1
entirely to the bottom** (`ACC⁰ → exact low-degree integer polynomial across depth`).  The real obstruction, made
precise: *exact composition works, but exact observer state multiplies across wide layers (`2^k`/`M^k`); YBT's content
is avoiding that product blow-up for arbitrary `ACC⁰`.*

## The bottom clause, pinned from both sides — and the only route left

The bottom clause is now bounded above and below, so the remaining wall is laser-sharp:

* **Exact low-degree per gate is IMPOSSIBLE** (`…ACC0ExactDegreeNoGo`): over `F₂` an exact unbounded-fan-in `OR`/`AND`
  has degree = fan-in `n` (top ANF coefficient `= 1`), so `2^n` monomials — exponential.  The naive route is ruled out.
* **Approximate low-degree EXISTS** (`toAgree_totalDegree_le`, Layer3): the RS approximant has polylog degree but agrees
  only on a `1-ε` fraction.
* **The only route left** is the Beigel–Tarui *approximate→exact* conversion (`…ACC0ApproxToExactDecode`): recover the
  exact value from several low-degree approximants by a **symmetric/count decoder**.  Socket
  `ApproxToExactSymmetricDecode` names it (non-trivial *only* via its low-degree clause — `r` copies of `f` satisfy it
  otherwise; with it, holding for arbitrary `ACC⁰` **is** Wall 1).  The *decoding* half is PROVED (`majority_decode`:
  majority-correct ⇒ exact threshold-count decode); the open part is producing **low-degree** approximants that are
  majority-correct at *every* point — the irreducible BT/Yao probabilistic construction, **Wall 1**.

## Honest ceiling

Nothing here is `NEXP ⊄ ACC⁰` or `P ≠ NP`. Tier 1 = genuine AC⁰ switching; Tier 2 = genuine `PARITY ∉ AC⁰[p]`
(classical, constant depth, prime `p`); Tier 3 = open, needs a different (algorithmic) tool.  The YBT pipeline above is
proved from the `SYM∘AND` normal form onward; the reduction *into* that form is the open structural wall.
